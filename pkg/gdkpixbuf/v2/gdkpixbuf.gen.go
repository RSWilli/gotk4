// Code generated by girgen. DO NOT EDIT.

package gdkpixbuf

import (
	"unsafe"
	"runtime"
)

// #cgo pkg-config: gdk-pixbuf-2.0
// #cgo CFLAGS: -Wno-deprecated-declarations
// #include <gdk-pixbuf/gdk-pixbuf.h>
import "C"

// GType values.
var (
	GTypeColorspace                   = glib.Type(C.gdk_colorspace_get_type())
	GTypeInterpType                   = glib.Type(C.gdk_interp_type_get_type())
	GTypePixbufAlphaMode              = glib.Type(C.gdk_pixbuf_alpha_mode_get_type())
	GTypePixbufError                  = glib.Type(C.gdk_pixbuf_error_get_type())
	GTypePixbufRotation               = glib.Type(C.gdk_pixbuf_rotation_get_type())
	GTypePixbufInstance               = glib.Type(C.gdk_pixbuf_get_type())
	GTypePixbufAnimationInstance      = glib.Type(C.gdk_pixbuf_animation_get_type())
	GTypePixbufAnimationIterInstance  = glib.Type(C.gdk_pixbuf_animation_iter_get_type())
	GTypePixbufLoaderInstance         = glib.Type(C.gdk_pixbuf_loader_get_type())
	GTypePixbufNonAnimInstance        = glib.Type(C.gdk_pixbuf_non_anim_get_type())
	GTypePixbufSimpleAnimInstance     = glib.Type(C.gdk_pixbuf_simple_anim_get_type())
	GTypePixbufSimpleAnimIterInstance = glib.Type(C.gdk_pixbuf_simple_anim_iter_get_type())
	GTypePixbufFormat                 = glib.Type(C.gdk_pixbuf_format_get_type())
)

func init() {
	glib.RegisterGValueMarshalers([]glib.TypeMarshaler{
		glib.TypeMarshaler{T: GTypeColorspace, F: marshalColorspace},
		glib.TypeMarshaler{T: GTypeInterpType, F: marshalInterpType},
		glib.TypeMarshaler{T: GTypePixbufAlphaMode, F: marshalPixbufAlphaMode},
		glib.TypeMarshaler{T: GTypePixbufError, F: marshalPixbufError},
		glib.TypeMarshaler{T: GTypePixbufRotation, F: marshalPixbufRotation},
		glib.TypeMarshaler{T: GTypePixbufInstance, F: marshalPixbufInstance},
		glib.TypeMarshaler{T: GTypePixbufAnimationInstance, F: marshalPixbufAnimationInstance},
		glib.TypeMarshaler{T: GTypePixbufAnimationIterInstance, F: marshalPixbufAnimationIterInstance},
		glib.TypeMarshaler{T: GTypePixbufLoaderInstance, F: marshalPixbufLoaderInstance},
		glib.TypeMarshaler{T: GTypePixbufNonAnimInstance, F: marshalPixbufNonAnimInstance},
		glib.TypeMarshaler{T: GTypePixbufSimpleAnimInstance, F: marshalPixbufSimpleAnimInstance},
		glib.TypeMarshaler{T: GTypePixbufSimpleAnimIterInstance, F: marshalPixbufSimpleAnimIterInstance},
		glib.TypeMarshaler{T: GTypePixbufFormat, F: marshalPixbufFormat},
	})
}

// PIXBUFMAJOR wraps GDK_PIXBUF_MAJOR
//
// Major version of gdk-pixbuf library, that is the "0" in
// "0.8.2" for example.
const PIXBUFMAJOR = C.GDK_PIXBUF_MAJOR
// PIXBUFMICRO wraps GDK_PIXBUF_MICRO
//
// Micro version of gdk-pixbuf library, that is the "2" in
// "0.8.2" for example.
const PIXBUFMICRO = C.GDK_PIXBUF_MICRO
// PIXBUFMINOR wraps GDK_PIXBUF_MINOR
//
// Minor version of gdk-pixbuf library, that is the "8" in
// "0.8.2" for example.
const PIXBUFMINOR = C.GDK_PIXBUF_MINOR
// Colorspace wraps GdkColorspace
//
// This enumeration defines the color spaces that are supported by
// the gdk-pixbuf library.
// 
// Currently only RGB is supported.
type Colorspace C.int

const (
	// ColorspaceRGB wraps GDK_COLORSPACE_RGB
	//
	// Indicates a red/green/blue additive color space.
	ColorspaceRGB Colorspace = C.GDK_COLORSPACE_RGB
)

func marshalColorspace(p uintptr) (interface{}, error) {
	return Colorspace(gobject.ValueFromNative(unsafe.Pointer(p)).Enum()), nil
}
// InterpType wraps GdkInterpType
//
// Interpolation modes for scaling functions.
// 
// The `GDK_INTERP_NEAREST` mode is the fastest scaling method, but has
// horrible quality when scaling down; `GDK_INTERP_BILINEAR` is the best
// choice if you aren't sure what to choose, it has a good speed/quality
// balance.
// 
// **Note**: Cubic filtering is missing from the list; hyperbolic
// interpolation is just as fast and results in higher quality.
type InterpType C.int

const (
	// InterpNearest wraps GDK_INTERP_NEAREST
	//
	// Nearest neighbor sampling; this is the fastest
	//  and lowest quality mode. Quality is normally unacceptable when scaling
	//  down, but may be OK when scaling up.
	InterpNearest InterpType = C.GDK_INTERP_NEAREST
	// InterpTiles wraps GDK_INTERP_TILES
	//
	// This is an accurate simulation of the PostScript
	//  image operator without any interpolation enabled.  Each pixel is
	//  rendered as a tiny parallelogram of solid color, the edges of which
	//  are implemented with antialiasing.  It resembles nearest neighbor for
	//  enlargement, and bilinear for reduction.
	InterpTiles InterpType = C.GDK_INTERP_TILES
	// InterpBilinear wraps GDK_INTERP_BILINEAR
	//
	// Best quality/speed balance; use this mode by
	//  default. Bilinear interpolation.  For enlargement, it is
	//  equivalent to point-sampling the ideal bilinear-interpolated image.
	//  For reduction, it is equivalent to laying down small tiles and
	//  integrating over the coverage area.
	InterpBilinear InterpType = C.GDK_INTERP_BILINEAR
	// InterpHyper wraps GDK_INTERP_HYPER
	//
	// This is the slowest and highest quality
	//  reconstruction function. It is derived from the hyperbolic filters in
	//  Wolberg's "Digital Image Warping", and is formally defined as the
	//  hyperbolic-filter sampling the ideal hyperbolic-filter interpolated
	//  image (the filter is designed to be idempotent for 1:1 pixel mapping).
	//  **Deprecated**: this interpolation filter is deprecated, as in reality
	//  it has a lower quality than the @GDK_INTERP_BILINEAR filter
	//  (Since: 2.38)
	InterpHyper InterpType = C.GDK_INTERP_HYPER
)

func marshalInterpType(p uintptr) (interface{}, error) {
	return InterpType(gobject.ValueFromNative(unsafe.Pointer(p)).Enum()), nil
}
// PixbufAlphaMode wraps GdkPixbufAlphaMode
//
// Control the alpha channel for drawables.
// 
// These values can be passed to gdk_pixbuf_xlib_render_to_drawable_alpha()
// in gdk-pixbuf-xlib to control how the alpha channel of an image should
// be handled.
// 
// This function can create a bilevel clipping mask (black and white) and use
// it while painting the image.
// 
// In the future, when the X Window System gets an alpha channel extension,
// it will be possible to do full alpha compositing onto arbitrary drawables.
// For now both cases fall back to a bilevel clipping mask.
//
// Deprecated: (since 2.42.0) There is no user of GdkPixbufAlphaMode in GdkPixbuf,
//   and the Xlib utility functions have been split out to their own
//   library, gdk-pixbuf-xlib
type PixbufAlphaMode C.int

const (
	// PixbufAlphaBilevel wraps GDK_PIXBUF_ALPHA_BILEVEL
	//
	// A bilevel clipping mask (black and white)
	//  will be created and used to draw the image.  Pixels below 0.5 opacity
	//  will be considered fully transparent, and all others will be
	//  considered fully opaque.
	PixbufAlphaBilevel PixbufAlphaMode = C.GDK_PIXBUF_ALPHA_BILEVEL
	// PixbufAlphaFull wraps GDK_PIXBUF_ALPHA_FULL
	//
	// For now falls back to #GDK_PIXBUF_ALPHA_BILEVEL.
	//  In the future it will do full alpha compositing.
	PixbufAlphaFull PixbufAlphaMode = C.GDK_PIXBUF_ALPHA_FULL
)

func marshalPixbufAlphaMode(p uintptr) (interface{}, error) {
	return PixbufAlphaMode(gobject.ValueFromNative(unsafe.Pointer(p)).Enum()), nil
}
// PixbufError wraps GdkPixbufError
//
// An error code in the `GDK_PIXBUF_ERROR` domain.
// 
// Many gdk-pixbuf operations can cause errors in this domain, or in
// the `G_FILE_ERROR` domain.
type PixbufError C.int

const (
	// PixbufErrorCorruptImage wraps GDK_PIXBUF_ERROR_CORRUPT_IMAGE
	//
	// An image file was broken somehow.
	PixbufErrorCorruptImage PixbufError = C.GDK_PIXBUF_ERROR_CORRUPT_IMAGE
	// PixbufErrorInsufficientMemory wraps GDK_PIXBUF_ERROR_INSUFFICIENT_MEMORY
	//
	// Not enough memory.
	PixbufErrorInsufficientMemory PixbufError = C.GDK_PIXBUF_ERROR_INSUFFICIENT_MEMORY
	// PixbufErrorBadOption wraps GDK_PIXBUF_ERROR_BAD_OPTION
	//
	// A bad option was passed to a pixbuf save module.
	PixbufErrorBadOption PixbufError = C.GDK_PIXBUF_ERROR_BAD_OPTION
	// PixbufErrorUnknownType wraps GDK_PIXBUF_ERROR_UNKNOWN_TYPE
	//
	// Unknown image type.
	PixbufErrorUnknownType PixbufError = C.GDK_PIXBUF_ERROR_UNKNOWN_TYPE
	// PixbufErrorUnsupportedOperation wraps GDK_PIXBUF_ERROR_UNSUPPORTED_OPERATION
	//
	// Don't know how to perform the
	//  given operation on the type of image at hand.
	PixbufErrorUnsupportedOperation PixbufError = C.GDK_PIXBUF_ERROR_UNSUPPORTED_OPERATION
	// PixbufErrorFailed wraps GDK_PIXBUF_ERROR_FAILED
	//
	// Generic failure code, something went wrong.
	PixbufErrorFailed PixbufError = C.GDK_PIXBUF_ERROR_FAILED
	// PixbufErrorIncompleteAnimation wraps GDK_PIXBUF_ERROR_INCOMPLETE_ANIMATION
	//
	// Only part of the animation was loaded.
	PixbufErrorIncompleteAnimation PixbufError = C.GDK_PIXBUF_ERROR_INCOMPLETE_ANIMATION
)

func marshalPixbufError(p uintptr) (interface{}, error) {
	return PixbufError(gobject.ValueFromNative(unsafe.Pointer(p)).Enum()), nil
}
// PixbufRotation wraps GdkPixbufRotation
//
// The possible rotations which can be passed to gdk_pixbuf_rotate_simple().
// 
// To make them easier to use, their numerical values are the actual degrees.
type PixbufRotation C.int

const (
	// PixbufRotateNone wraps GDK_PIXBUF_ROTATE_NONE
	//
	// No rotation.
	PixbufRotateNone PixbufRotation = C.GDK_PIXBUF_ROTATE_NONE
	// PixbufRotateCounterclockwise wraps GDK_PIXBUF_ROTATE_COUNTERCLOCKWISE
	//
	// Rotate by 90 degrees.
	PixbufRotateCounterclockwise PixbufRotation = C.GDK_PIXBUF_ROTATE_COUNTERCLOCKWISE
	// PixbufRotateUpsidedown wraps GDK_PIXBUF_ROTATE_UPSIDEDOWN
	//
	// Rotate by 180 degrees.
	PixbufRotateUpsidedown PixbufRotation = C.GDK_PIXBUF_ROTATE_UPSIDEDOWN
	// PixbufRotateClockwise wraps GDK_PIXBUF_ROTATE_CLOCKWISE
	//
	// Rotate by 270 degrees.
	PixbufRotateClockwise PixbufRotation = C.GDK_PIXBUF_ROTATE_CLOCKWISE
)

func marshalPixbufRotation(p uintptr) (interface{}, error) {
	return PixbufRotation(gobject.ValueFromNative(unsafe.Pointer(p)).Enum()), nil
}
// PixbufFormatFlags wraps GdkPixbufFormatFlags
//
// Flags which allow a module to specify further details about the supported
// operations.
type PixbufFormatFlags C.gint

const (
	// PixbufFormatWritable wraps GDK_PIXBUF_FORMAT_WRITABLE
	//
	// the module can write out images in the format.
	PixbufFormatWritable PixbufFormatFlags = C.GDK_PIXBUF_FORMAT_WRITABLE
	// PixbufFormatScalable wraps GDK_PIXBUF_FORMAT_SCALABLE
	//
	// the image format is scalable
	PixbufFormatScalable PixbufFormatFlags = C.GDK_PIXBUF_FORMAT_SCALABLE
	// PixbufFormatThreadsafe wraps GDK_PIXBUF_FORMAT_THREADSAFE
	//
	// the module is threadsafe. gdk-pixbuf
	//     ignores modules that are not marked as threadsafe. (Since 2.28).
	PixbufFormatThreadsafe PixbufFormatFlags = C.GDK_PIXBUF_FORMAT_THREADSAFE
)

// Has returns true if p contains other
func (p PixbufFormatFlags) Has(other PixbufFormatFlags) bool {
	return (p & other) == other
}

// PixbufModuleFillInfoFunc wraps GdkPixbufModuleFillInfoFunc
//
// Defines the type of the function used to fill a
// #GdkPixbufFormat structure with information about a module.
type PixbufModuleFillInfoFunc func(info *PixbufFormat)

// PixbufModuleFillVtableFunc wraps GdkPixbufModuleFillVtableFunc
//
// Defines the type of the function used to set the vtable of a
// #GdkPixbufModule when it is loaded.
type PixbufModuleFillVtableFunc func(module *PixbufModule)

// PixbufModuleIncrementLoadFunc wraps GdkPixbufModuleIncrementLoadFunc
//
// Incrementally loads a buffer into the image data.
type PixbufModuleIncrementLoadFunc func(context unsafe.Pointer, buf []uint8) (ret bool, _goerr error)

// PixbufModuleLoadAnimationFunc wraps GdkPixbufModuleLoadAnimationFunc
//
// Loads a file from a standard C file stream into a new `GdkPixbufAnimation`.
// 
// In case of error, this function should return `NULL` and set the `error` argument.
type PixbufModuleLoadAnimationFunc func(f unsafe.Pointer) (ret PixbufAnimation, _goerr error)

// PixbufModuleLoadFunc wraps GdkPixbufModuleLoadFunc
//
// Loads a file from a standard C file stream into a new `GdkPixbuf`.
// 
// In case of error, this function should return `NULL` and set the `error` argument.
type PixbufModuleLoadFunc func(f unsafe.Pointer) (ret Pixbuf, _goerr error)

// PixbufModuleLoadXPMDataFunc wraps GdkPixbufModuleLoadXpmDataFunc
//
// Loads XPM data into a new `GdkPixbuf`.
type PixbufModuleLoadXPMDataFunc func(data []string) (ret Pixbuf)

// PixbufModulePreparedFunc wraps GdkPixbufModulePreparedFunc
//
// Defines the type of the function that gets called once the initial
// setup of @pixbuf is done.
// 
// #GdkPixbufLoader uses a function of this type to emit the
// "&lt;link linkend="GdkPixbufLoader-area-prepared"&gt;area_prepared&lt;/link&gt;"
// signal.
type PixbufModulePreparedFunc func(pixbuf Pixbuf, anim PixbufAnimation)

// PixbufModuleSaveFunc wraps GdkPixbufModuleSaveFunc
//
// Saves a `GdkPixbuf` into a standard C file stream.
// 
// The optional `param_keys` and `param_values` arrays contain the keys and
// values (in the same order) for attributes to be saved alongside the image
// data.
type PixbufModuleSaveFunc func(f unsafe.Pointer, pixbuf Pixbuf, paramKeys []string, paramValues []string) (ret bool, _goerr error)

// PixbufModuleSaveOptionSupportedFunc wraps GdkPixbufModuleSaveOptionSupportedFunc
//
// Checks whether the given `option_key` is supported when saving.
type PixbufModuleSaveOptionSupportedFunc func(optionKey string) (ret bool)

// PixbufModuleStopLoadFunc wraps GdkPixbufModuleStopLoadFunc
//
// Finalizes the image loading state.
// 
// This function is called on success and error states.
type PixbufModuleStopLoadFunc func(context unsafe.Pointer) (ret bool, _goerr error)

// PixbufModuleUpdatedFunc wraps GdkPixbufModuleUpdatedFunc
//
// Defines the type of the function that gets called every time a region
// of @pixbuf is updated.
// 
// #GdkPixbufLoader uses a function of this type to emit the
// "&lt;link linkend="GdkPixbufLoader-area-updated"&gt;area_updated&lt;/link&gt;"
// signal.
type PixbufModuleUpdatedFunc func(pixbuf Pixbuf, x int, y int, width int, height int)

// PixbufSaveFunc wraps GdkPixbufSaveFunc
//
// Save functions used by [method@GdkPixbuf.Pixbuf.save_to_callback].
// 
// This function is called once for each block of bytes that is "written"
// by `gdk_pixbuf_save_to_callback()`.
// 
// If successful it should return `TRUE`; if an error occurs it should set
// `error` and return `FALSE`, in which case `gdk_pixbuf_save_to_callback()`
// will fail with the same error.
type PixbufSaveFunc func(buf string) (err error, ret bool)

// PixbufInstance is the instance type used by all types extending GdkPixbuf. It is used internally by the bindings. Users should use the interface [Pixbuf] instead.
type PixbufInstance struct {
	_ [0]func() // equal guard
	gobject.Object
	// implemented interfaces:
	gio.IconInstance
	gio.LoadableIconInstance
}

var _ Pixbuf = (*PixbufInstance)(nil)

// PixbufInstance wraps GdkPixbuf
//
// A pixel buffer.
// 
// `GdkPixbuf` contains information about an image's pixel data,
// its color space, bits per sample, width and height, and the
// rowstride (the number of bytes between the start of one row
// and the start of the next).
// 
// ## Creating new `GdkPixbuf`
// 
// The most basic way to create a pixbuf is to wrap an existing pixel
// buffer with a [class@GdkPixbuf.Pixbuf] instance. You can use the
// [`ctor@GdkPixbuf.Pixbuf.new_from_data`] function to do this.
// 
// Every time you create a new `GdkPixbuf` instance for some data, you
// will need to specify the destroy notification function that will be
// called when the data buffer needs to be freed; this will happen when
// a `GdkPixbuf` is finalized by the reference counting functions. If
// you have a chunk of static data compiled into your application, you
// can pass in `NULL` as the destroy notification function so that the
// data will not be freed.
// 
// The [`ctor@GdkPixbuf.Pixbuf.new`] constructor function can be used
// as a convenience to create a pixbuf with an empty buffer; this is
// equivalent to allocating a data buffer using `malloc()` and then
// wrapping it with `gdk_pixbuf_new_from_data()`. The `gdk_pixbuf_new()`
// function will compute an optimal rowstride so that rendering can be
// performed with an efficient algorithm.
// 
// As a special case, you can use the [`ctor@GdkPixbuf.Pixbuf.new_from_xpm_data`]
// function to create a pixbuf from inline XPM image data.
// 
// You can also copy an existing pixbuf with the [method@Pixbuf.copy]
// function. This is not the same as just acquiring a reference to
// the old pixbuf instance: the copy function will actually duplicate
// the pixel data in memory and create a new [class@Pixbuf] instance
// for it.
// 
// ## Reference counting
// 
// `GdkPixbuf` structures are reference counted. This means that an
// application can share a single pixbuf among many parts of the
// code. When a piece of the program needs to use a pixbuf, it should
// acquire a reference to it by calling `g_object_ref()`; when it no
// longer needs the pixbuf, it should release the reference it acquired
// by calling `g_object_unref()`. The resources associated with a
// `GdkPixbuf` will be freed when its reference count drops to zero.
// Newly-created `GdkPixbuf` instances start with a reference count
// of one.
// 
// ## Image Data
// 
// Image data in a pixbuf is stored in memory in an uncompressed,
// packed format. Rows in the image are stored top to bottom, and
// in each row pixels are stored from left to right.
// 
// There may be padding at the end of a row.
// 
// The "rowstride" value of a pixbuf, as returned by [`method@GdkPixbuf.Pixbuf.get_rowstride`],
// indicates the number of bytes between rows.
// 
// **NOTE**: If you are copying raw pixbuf data with `memcpy()` note that the
// last row in the pixbuf may not be as wide as the full rowstride, but rather
// just as wide as the pixel data needs to be; that is: it is unsafe to do
// `memcpy (dest, pixels, rowstride * height)` to copy a whole pixbuf. Use
// [method@GdkPixbuf.Pixbuf.copy] instead, or compute the width in bytes of the
// last row as:
// 
// ```c
// last_row = width * ((n_channels * bits_per_sample + 7) / 8);
// ```
// 
// The same rule applies when iterating over each row of a `GdkPixbuf` pixels
// array.
// 
// The following code illustrates a simple `put_pixel()`
// function for RGB pixbufs with 8 bits per channel with an alpha
// channel.
// 
// ```c
// static void
// put_pixel (GdkPixbuf *pixbuf,
//            int x,
// 	   int y,
// 	   guchar red,
// 	   guchar green,
// 	   guchar blue,
// 	   guchar alpha)
// {
//   int n_channels = gdk_pixbuf_get_n_channels (pixbuf);
// 
//   // Ensure that the pixbuf is valid
//   g_assert (gdk_pixbuf_get_colorspace (pixbuf) == GDK_COLORSPACE_RGB);
//   g_assert (gdk_pixbuf_get_bits_per_sample (pixbuf) == 8);
//   g_assert (gdk_pixbuf_get_has_alpha (pixbuf));
//   g_assert (n_channels == 4);
// 
//   int width = gdk_pixbuf_get_width (pixbuf);
//   int height = gdk_pixbuf_get_height (pixbuf);
// 
//   // Ensure that the coordinates are in a valid range
//   g_assert (x &gt;= 0 &amp;&amp; x &lt; width);
//   g_assert (y &gt;= 0 &amp;&amp; y &lt; height);
// 
//   int rowstride = gdk_pixbuf_get_rowstride (pixbuf);
// 
//   // The pixel buffer in the GdkPixbuf instance
//   guchar *pixels = gdk_pixbuf_get_pixels (pixbuf);
// 
//   // The pixel we wish to modify
//   guchar *p = pixels + y * rowstride + x * n_channels;
//   p[0] = red;
//   p[1] = green;
//   p[2] = blue;
//   p[3] = alpha;
// }
// ```
// 
// ## Loading images
// 
// The `GdkPixBuf` class provides a simple mechanism for loading
// an image from a file in synchronous and asynchronous fashion.
// 
// For GUI applications, it is recommended to use the asynchronous
// stream API to avoid blocking the control flow of the application.
// 
// Additionally, `GdkPixbuf` provides the [class@GdkPixbuf.PixbufLoader`]
// API for progressive image loading.
// 
// ## Saving images
// 
// The `GdkPixbuf` class provides methods for saving image data in
// a number of file formats. The formatted data can be written to a
// file or to a memory buffer. `GdkPixbuf` can also call a user-defined
// callback on the data, which allows to e.g. write the image
// to a socket or store it in a database.
type Pixbuf interface {
	gobject.ObjectLike
	gio.Icon
	gio.LoadableIcon
	upcastToGdkPixbuf() *PixbufInstance

	// AddAlpha wraps gdk_pixbuf_add_alpha
	// 
	// The function takes the following parameters:
	// 
	// 	- substituteColor bool: Whether to set a color to zero opacity. 
	// 	- r uint8: Red value to substitute. 
	// 	- g uint8: Green value to substitute. 
	// 	- b uint8: Blue value to substitute. 
	// 
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Takes an existing pixbuf and adds an alpha channel to it.
	// 
	// If the existing pixbuf already had an alpha channel, the channel
	// values are copied from the original; otherwise, the alpha channel
	// is initialized to 255 (full opacity).
	// 
	// If `substitute_color` is `TRUE`, then the color specified by the
	// (`r`, `g`, `b`) arguments will be assigned zero opacity. That is,
	// if you pass `(255, 255, 255)` for the substitute color, all white
	// pixels will become fully transparent.
	// 
	// If `substitute_color` is `FALSE`, then the (`r`, `g`, `b`) arguments
	// will be ignored.
	AddAlpha(bool, uint8, uint8, uint8) Pixbuf
	// ApplyEmbeddedOrientation wraps gdk_pixbuf_apply_embedded_orientation
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Takes an existing pixbuf and checks for the presence of an
	// associated "orientation" option.
	// 
	// The orientation option may be provided by the JPEG loader (which
	// reads the exif orientation tag) or the TIFF loader (which reads
	// the TIFF orientation tag, and compensates it for the partial
	// transforms performed by libtiff).
	// 
	// If an orientation option/tag is present, the appropriate transform
	// will be performed so that the pixbuf is oriented correctly.
	ApplyEmbeddedOrientation() Pixbuf
	// Composite wraps gdk_pixbuf_composite
	// 
	// The function takes the following parameters:
	// 
	// 	- dest Pixbuf: the #GdkPixbuf into which to render the results 
	// 	- destX int: the left coordinate for region to render 
	// 	- destY int: the top coordinate for region to render 
	// 	- destWidth int: the width of the region to render 
	// 	- destHeight int: the height of the region to render 
	// 	- offsetX float64: the offset in the X direction (currently rounded to an integer) 
	// 	- offsetY float64: the offset in the Y direction (currently rounded to an integer) 
	// 	- scaleX float64: the scale factor in the X direction 
	// 	- scaleY float64: the scale factor in the Y direction 
	// 	- interpType InterpType: the interpolation type for the transformation. 
	// 	- overallAlpha int: overall alpha for source image (0..255) 
	//
	// Creates a transformation of the source image @src by scaling by
	// @scale_x and @scale_y then translating by @offset_x and @offset_y.
	// 
	// This gives an image in the coordinates of the destination pixbuf.
	// The rectangle (@dest_x, @dest_y, @dest_width, @dest_height)
	// is then alpha blended onto the corresponding rectangle of the
	// original destination image.
	// 
	// When the destination rectangle contains parts not in the source
	// image, the data at the edges of the source image is replicated
	// to infinity.
	// 
	// ![](composite.png)
	Composite(Pixbuf, int, int, int, int, float64, float64, float64, float64, InterpType, int)
	// CompositeColor wraps gdk_pixbuf_composite_color
	// 
	// The function takes the following parameters:
	// 
	// 	- dest Pixbuf: the #GdkPixbuf into which to render the results 
	// 	- destX int: the left coordinate for region to render 
	// 	- destY int: the top coordinate for region to render 
	// 	- destWidth int: the width of the region to render 
	// 	- destHeight int: the height of the region to render 
	// 	- offsetX float64: the offset in the X direction (currently rounded to an integer) 
	// 	- offsetY float64: the offset in the Y direction (currently rounded to an integer) 
	// 	- scaleX float64: the scale factor in the X direction 
	// 	- scaleY float64: the scale factor in the Y direction 
	// 	- interpType InterpType: the interpolation type for the transformation. 
	// 	- overallAlpha int: overall alpha for source image (0..255) 
	// 	- checkX int: the X offset for the checkboard (origin of checkboard is at -@check_x, -@check_y) 
	// 	- checkY int: the Y offset for the checkboard 
	// 	- checkSize int: the size of checks in the checkboard (must be a power of two) 
	// 	- color1 uint32: the color of check at upper left 
	// 	- color2 uint32: the color of the other check 
	//
	// Creates a transformation of the source image @src by scaling by
	// @scale_x and @scale_y then translating by @offset_x and @offset_y,
	// then alpha blends the rectangle (@dest_x ,@dest_y, @dest_width,
	// @dest_height) of the resulting image with a checkboard of the
	// colors @color1 and @color2 and renders it onto the destination
	// image.
	// 
	// If the source image has no alpha channel, and @overall_alpha is 255, a fast
	// path is used which omits the alpha blending and just performs the scaling.
	// 
	// See gdk_pixbuf_composite_color_simple() for a simpler variant of this
	// function suitable for many tasks.
	CompositeColor(Pixbuf, int, int, int, int, float64, float64, float64, float64, InterpType, int, int, int, int, uint32, uint32)
	// CompositeColorSimple wraps gdk_pixbuf_composite_color_simple
	// 
	// The function takes the following parameters:
	// 
	// 	- destWidth int: the width of destination image 
	// 	- destHeight int: the height of destination image 
	// 	- interpType InterpType: the interpolation type for the transformation. 
	// 	- overallAlpha int: overall alpha for source image (0..255) 
	// 	- checkSize int: the size of checks in the checkboard (must be a power of two) 
	// 	- color1 uint32: the color of check at upper left 
	// 	- color2 uint32: the color of the other check 
	// 
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Creates a new pixbuf by scaling `src` to `dest_width` x `dest_height`
	// and alpha blending the result with a checkboard of colors `color1`
	// and `color2`.
	CompositeColorSimple(int, int, InterpType, int, int, uint32, uint32) Pixbuf
	// Copy wraps gdk_pixbuf_copy
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Creates a new `GdkPixbuf` with a copy of the information in the specified
	// `pixbuf`.
	// 
	// Note that this does not copy the options set on the original `GdkPixbuf`,
	// use gdk_pixbuf_copy_options() for this.
	Copy() Pixbuf
	// CopyArea wraps gdk_pixbuf_copy_area
	// 
	// The function takes the following parameters:
	// 
	// 	- srcX int: Source X coordinate within @src_pixbuf. 
	// 	- srcY int: Source Y coordinate within @src_pixbuf. 
	// 	- width int: Width of the area to copy. 
	// 	- height int: Height of the area to copy. 
	// 	- destPixbuf Pixbuf: Destination pixbuf. 
	// 	- destX int: X coordinate within @dest_pixbuf. 
	// 	- destY int: Y coordinate within @dest_pixbuf. 
	//
	// Copies a rectangular area from `src_pixbuf` to `dest_pixbuf`.
	// 
	// Conversion of pixbuf formats is done automatically.
	// 
	// If the source rectangle overlaps the destination rectangle on the
	// same pixbuf, it will be overwritten during the copy operation.
	// Therefore, you can not use this function to scroll a pixbuf.
	CopyArea(int, int, int, int, Pixbuf, int, int)
	// CopyOptions wraps gdk_pixbuf_copy_options
	// 
	// The function takes the following parameters:
	// 
	// 	- destPixbuf Pixbuf: the destination pixbuf 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Copies the key/value pair options attached to a `GdkPixbuf` to another
	// `GdkPixbuf`.
	// 
	// This is useful to keep original metadata after having manipulated
	// a file. However be careful to remove metadata which you've already
	// applied, such as the "orientation" option after rotating the image.
	CopyOptions(Pixbuf) bool
	// Fill wraps gdk_pixbuf_fill
	// 
	// The function takes the following parameters:
	// 
	// 	- pixel uint32: RGBA pixel to used to clear (`0xffffffff` is opaque white,
	//   `0x00000000` transparent black) 
	//
	// Clears a pixbuf to the given RGBA value, converting the RGBA value into
	// the pixbuf's pixel format.
	// 
	// The alpha component will be ignored if the pixbuf doesn't have an alpha
	// channel.
	Fill(uint32)
	// Flip wraps gdk_pixbuf_flip
	// 
	// The function takes the following parameters:
	// 
	// 	- horizontal bool: `TRUE` to flip horizontally, `FALSE` to flip vertically 
	// 
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Flips a pixbuf horizontally or vertically and returns the
	// result in a new pixbuf.
	Flip(bool) Pixbuf
	// GetBitsPerSample wraps gdk_pixbuf_get_bits_per_sample
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the number of bits per color sample in a pixbuf.
	GetBitsPerSample() int
	// GetByteLength wraps gdk_pixbuf_get_byte_length
	// The function returns the following values:
	// 
	// 	- ret uint 
	//
	// Returns the length of the pixel data, in bytes.
	GetByteLength() uint
	// GetColorspace wraps gdk_pixbuf_get_colorspace
	// The function returns the following values:
	// 
	// 	- ret Colorspace 
	//
	// Queries the color space of a pixbuf.
	GetColorspace() Colorspace
	// GetHasAlpha wraps gdk_pixbuf_get_has_alpha
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Queries whether a pixbuf has an alpha channel (opacity information).
	GetHasAlpha() bool
	// GetHeight wraps gdk_pixbuf_get_height
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the height of a pixbuf.
	GetHeight() int
	// GetNChannels wraps gdk_pixbuf_get_n_channels
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the number of channels of a pixbuf.
	GetNChannels() int
	// GetOption wraps gdk_pixbuf_get_option
	// 
	// The function takes the following parameters:
	// 
	// 	- key string: a nul-terminated string. 
	// 
	// The function returns the following values:
	// 
	// 	- ret string 
	//
	// Looks up @key in the list of options that may have been attached to the
	// @pixbuf when it was loaded, or that may have been attached by another
	// function using gdk_pixbuf_set_option().
	// 
	// For instance, the ANI loader provides "Title" and "Artist" options.
	// The ICO, XBM, and XPM loaders provide "x_hot" and "y_hot" hot-spot
	// options for cursor definitions. The PNG loader provides the tEXt ancillary
	// chunk key/value pairs as options. Since 2.12, the TIFF and JPEG loaders
	// return an "orientation" option string that corresponds to the embedded
	// TIFF/Exif orientation tag (if present). Since 2.32, the TIFF loader sets
	// the "multipage" option string to "yes" when a multi-page TIFF is loaded.
	// Since 2.32 the JPEG and PNG loaders set "x-dpi" and "y-dpi" if the file
	// contains image density information in dots per inch.
	// Since 2.36.6, the JPEG loader sets the "comment" option with the comment
	// EXIF tag.
	GetOption(string) string
	// GetOptions wraps gdk_pixbuf_get_options
	// The function returns the following values:
	// 
	// 	- ret *glib.HashTable 
	//
	// Returns a `GHashTable` with a list of all the options that may have been
	// attached to the `pixbuf` when it was loaded, or that may have been
	// attached by another function using [method@GdkPixbuf.Pixbuf.set_option].
	GetOptions() *glib.HashTable
	// GetPixelsWithLength wraps gdk_pixbuf_get_pixels_with_length
	// The function returns the following values:
	// 
	// 	- length uint: The length of the binary data. 
	// 	- ret []uint8 
	//
	// Queries a pointer to the pixel data of a pixbuf.
	// 
	// This function will cause an implicit copy of the pixbuf data if the
	// pixbuf was created from read-only data.
	// 
	// Please see the section on [image data](class.Pixbuf.html#image-data) for information
	// about how the pixel data is stored in memory.
	GetPixelsWithLength() (uint, []uint8)
	// GetRowstride wraps gdk_pixbuf_get_rowstride
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the rowstride of a pixbuf, which is the number of bytes between
	// the start of a row and the start of the next row.
	GetRowstride() int
	// GetWidth wraps gdk_pixbuf_get_width
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the width of a pixbuf.
	GetWidth() int
	// NewSubpixbuf wraps gdk_pixbuf_new_subpixbuf
	// 
	// The function takes the following parameters:
	// 
	// 	- srcX int: X coord in @src_pixbuf 
	// 	- srcY int: Y coord in @src_pixbuf 
	// 	- width int: width of region in @src_pixbuf 
	// 	- height int: height of region in @src_pixbuf 
	// 
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Creates a new pixbuf which represents a sub-region of `src_pixbuf`.
	// 
	// The new pixbuf shares its pixels with the original pixbuf, so
	// writing to one affects both.  The new pixbuf holds a reference to
	// `src_pixbuf`, so `src_pixbuf` will not be finalized until the new
	// pixbuf is finalized.
	// 
	// Note that if `src_pixbuf` is read-only, this function will force it
	// to be mutable.
	NewSubpixbuf(int, int, int, int) Pixbuf
	// ReadPixelBytes wraps gdk_pixbuf_read_pixel_bytes
	// The function returns the following values:
	// 
	// 	- ret *glib.Bytes 
	//
	// Provides a #GBytes buffer containing the raw pixel data; the data
	// must not be modified.
	// 
	// This function allows skipping the implicit copy that must be made
	// if gdk_pixbuf_get_pixels() is called on a read-only pixbuf.
	ReadPixelBytes() *glib.Bytes
	// ReadPixels wraps gdk_pixbuf_read_pixels
	// The function returns the following values:
	// 
	// 	- ret *uint8 
	//
	// Provides a read-only pointer to the raw pixel data.
	// 
	// This function allows skipping the implicit copy that must be made
	// if gdk_pixbuf_get_pixels() is called on a read-only pixbuf.
	ReadPixels() *uint8
	// RemoveOption wraps gdk_pixbuf_remove_option
	// 
	// The function takes the following parameters:
	// 
	// 	- key string: a nul-terminated string representing the key to remove. 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Removes the key/value pair option attached to a `GdkPixbuf`.
	RemoveOption(string) bool
	// RotateSimple wraps gdk_pixbuf_rotate_simple
	// 
	// The function takes the following parameters:
	// 
	// 	- angle PixbufRotation: the angle to rotate by 
	// 
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Rotates a pixbuf by a multiple of 90 degrees, and returns the
	// result in a new pixbuf.
	// 
	// If `angle` is 0, this function will return a copy of `src`.
	RotateSimple(PixbufRotation) Pixbuf
	// SaturateAndPixelate wraps gdk_pixbuf_saturate_and_pixelate
	// 
	// The function takes the following parameters:
	// 
	// 	- dest Pixbuf: place to write modified version of @src 
	// 	- saturation float32: saturation factor 
	// 	- pixelate bool: whether to pixelate 
	//
	// Modifies saturation and optionally pixelates `src`, placing the result in
	// `dest`.
	// 
	// The `src` and `dest` pixbufs must have the same image format, size, and
	// rowstride.
	// 
	// The `src` and `dest` arguments may be the same pixbuf with no ill effects.
	// 
	// If `saturation` is 1.0 then saturation is not changed. If it's less than 1.0,
	// saturation is reduced (the image turns toward grayscale); if greater than
	// 1.0, saturation is increased (the image gets more vivid colors).
	// 
	// If `pixelate` is `TRUE`, then pixels are faded in a checkerboard pattern to
	// create a pixelated image.
	SaturateAndPixelate(Pixbuf, float32, bool)
	// SaveToBufferv wraps gdk_pixbuf_save_to_bufferv
	// 
	// The function takes the following parameters:
	// 
	// 	- typ string: name of file format. 
	// 	- optionKeys []string (nullable): name of options to set 
	// 	- optionValues []string (nullable): values for named options 
	// 
	// The function returns the following values:
	// 
	// 	- buffer []uint8: 
	//   location to receive a pointer to the new buffer. 
	// 	- bufferSize uint: location to receive the size of the new buffer. 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Vector version of `gdk_pixbuf_save_to_buffer()`.
	// 
	// Saves pixbuf to a new buffer in format @type, which is currently "jpeg",
	// "tiff", "png", "ico" or "bmp".
	// 
	// See [method@GdkPixbuf.Pixbuf.save_to_buffer] for more details.
	SaveToBufferv(string, []string, []string) ([]uint8, bool, error)
	// SaveToCallbackv wraps gdk_pixbuf_save_to_callbackv
	// 
	// The function takes the following parameters:
	// 
	// 	- saveFunc PixbufSaveFunc: a function that is called to save each block of data that
	//   the save routine generates. 
	// 	- typ string: name of file format. 
	// 	- optionKeys []string (nullable): name of options to set 
	// 	- optionValues []string (nullable): values for named options 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Vector version of `gdk_pixbuf_save_to_callback()`.
	// 
	// Saves pixbuf to a callback in format @type, which is currently "jpeg",
	// "png", "tiff", "ico" or "bmp".
	// 
	// If @error is set, `FALSE` will be returned.
	// 
	// See [method@GdkPixbuf.Pixbuf.save_to_callback] for more details.
	SaveToCallbackv(PixbufSaveFunc, string, []string, []string) (bool, error)
	// SaveToStreamv wraps gdk_pixbuf_save_to_streamv
	// 
	// The function takes the following parameters:
	// 
	// 	- stream gio.OutputStream: a `GOutputStream` to save the pixbuf to 
	// 	- typ string: name of file format 
	// 	- optionKeys []string (nullable): name of options to set 
	// 	- optionValues []string (nullable): values for named options 
	// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Saves `pixbuf` to an output stream.
	// 
	// Supported file formats are currently "jpeg", "tiff", "png", "ico" or
	// "bmp".
	// 
	// See [method@GdkPixbuf.Pixbuf.save_to_stream] for more details.
	SaveToStreamv(gio.OutputStream, string, []string, []string, gio.Cancellable) (bool, error)
	// SaveToStreamvAsync wraps gdk_pixbuf_save_to_streamv_async
	// 
	// The function takes the following parameters:
	// 
	// 	- stream gio.OutputStream: a `GOutputStream` to which to save the pixbuf 
	// 	- typ string: name of file format 
	// 	- optionKeys []string (nullable): name of options to set 
	// 	- optionValues []string (nullable): values for named options 
	// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
	// 	- callback gio.AsyncReadyCallback (nullable): a `GAsyncReadyCallback` to call when the pixbuf is saved 
	//
	// Saves `pixbuf` to an output stream asynchronously.
	// 
	// For more details see gdk_pixbuf_save_to_streamv(), which is the synchronous
	// version of this function.
	// 
	// When the operation is finished, `callback` will be called in the main thread.
	// 
	// You can then call gdk_pixbuf_save_to_stream_finish() to get the result of
	// the operation.
	SaveToStreamvAsync(gio.OutputStream, string, []string, []string, gio.Cancellable, gio.AsyncReadyCallback)
	// Savev wraps gdk_pixbuf_savev
	// 
	// The function takes the following parameters:
	// 
	// 	- filename string: name of file to save. 
	// 	- typ string: name of file format. 
	// 	- optionKeys []string (nullable): name of options to set 
	// 	- optionValues []string (nullable): values for named options 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Vector version of `gdk_pixbuf_save()`.
	// 
	// Saves pixbuf to a file in `type`, which is currently "jpeg", "png", "tiff", "ico" or "bmp".
	// 
	// If @error is set, `FALSE` will be returned.
	// 
	// See [method@GdkPixbuf.Pixbuf.save] for more details.
	Savev(string, string, []string, []string) (bool, error)
	// Scale wraps gdk_pixbuf_scale
	// 
	// The function takes the following parameters:
	// 
	// 	- dest Pixbuf: the #GdkPixbuf into which to render the results 
	// 	- destX int: the left coordinate for region to render 
	// 	- destY int: the top coordinate for region to render 
	// 	- destWidth int: the width of the region to render 
	// 	- destHeight int: the height of the region to render 
	// 	- offsetX float64: the offset in the X direction (currently rounded to an integer) 
	// 	- offsetY float64: the offset in the Y direction (currently rounded to an integer) 
	// 	- scaleX float64: the scale factor in the X direction 
	// 	- scaleY float64: the scale factor in the Y direction 
	// 	- interpType InterpType: the interpolation type for the transformation. 
	//
	// Creates a transformation of the source image @src by scaling by
	// @scale_x and @scale_y then translating by @offset_x and @offset_y,
	// then renders the rectangle (@dest_x, @dest_y, @dest_width,
	// @dest_height) of the resulting image onto the destination image
	// replacing the previous contents.
	// 
	// Try to use gdk_pixbuf_scale_simple() first; this function is
	// the industrial-strength power tool you can fall back to, if
	// gdk_pixbuf_scale_simple() isn't powerful enough.
	// 
	// If the source rectangle overlaps the destination rectangle on the
	// same pixbuf, it will be overwritten during the scaling which
	// results in rendering artifacts.
	Scale(Pixbuf, int, int, int, int, float64, float64, float64, float64, InterpType)
	// ScaleSimple wraps gdk_pixbuf_scale_simple
	// 
	// The function takes the following parameters:
	// 
	// 	- destWidth int: the width of destination image 
	// 	- destHeight int: the height of destination image 
	// 	- interpType InterpType: the interpolation type for the transformation. 
	// 
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Create a new pixbuf containing a copy of `src` scaled to
	// `dest_width` x `dest_height`.
	// 
	// This function leaves `src` unaffected.
	// 
	// The `interp_type` should be `GDK_INTERP_NEAREST` if you want maximum
	// speed (but when scaling down `GDK_INTERP_NEAREST` is usually unusably
	// ugly). The default `interp_type` should be `GDK_INTERP_BILINEAR` which
	// offers reasonable quality and speed.
	// 
	// You can scale a sub-portion of `src` by creating a sub-pixbuf
	// pointing into `src`; see [method@GdkPixbuf.Pixbuf.new_subpixbuf].
	// 
	// If `dest_width` and `dest_height` are equal to the width and height of
	// `src`, this function will return an unscaled copy of `src`.
	// 
	// For more complicated scaling/alpha blending see [method@GdkPixbuf.Pixbuf.scale]
	// and [method@GdkPixbuf.Pixbuf.composite].
	ScaleSimple(int, int, InterpType) Pixbuf
	// SetOption wraps gdk_pixbuf_set_option
	// 
	// The function takes the following parameters:
	// 
	// 	- key string: a nul-terminated string. 
	// 	- value string: a nul-terminated string. 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Attaches a key/value pair as an option to a `GdkPixbuf`.
	// 
	// If `key` already exists in the list of options attached to the `pixbuf`,
	// the new value is ignored and `FALSE` is returned.
	SetOption(string, string) bool
}

func unsafeWrapPixbuf(base *gobject.Object) *PixbufInstance {
	return &PixbufInstance{
		Object: *base,
		IconInstance: gio.IconInstance{
			Object: *base,
		},
		LoadableIconInstance: gio.LoadableIconInstance{
			Object: *base,
		},
	}
}

func marshalPixbufInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbuf(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufFromGlibBorrow is used to convert raw GdkPixbuf pointers to go. This is used by the bindings internally.
func UnsafePixbufFromGlibBorrow(c unsafe.Pointer) Pixbuf {
	return gobject.TODOBorrow(c).(Pixbuf)
}

// UnsafePixbufFromGlibNone is used to convert raw GdkPixbuf pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufFromGlibNone(c unsafe.Pointer) Pixbuf {
	return gobject.Take(c).(Pixbuf)
}

// UnsafePixbufFromGlibFull is used to convert raw GdkPixbuf pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufFromGlibFull(c unsafe.Pointer) Pixbuf {
	return gobject.AssumeOwnership(c).(Pixbuf)
}

func (p *PixbufInstance) upcastToGdkPixbuf() *PixbufInstance {
	return p
}

// UnsafePixbufToGlibNone is used to convert the instance to it's C value GdkPixbuf. This is used by the bindings internally.
func UnsafePixbufToGlibNone(c Pixbuf) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufToGlibFull is used to convert the instance to it's C value GdkPixbuf, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufToGlibFull(c Pixbuf) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// NewPixbufInstance wraps gdk_pixbuf_new
// 
// The function takes the following parameters:
// 
// 	- colorspace Colorspace: Color space for image 
// 	- hasAlpha bool: Whether the image should have transparency information 
// 	- bitsPerSample int: Number of bits per color sample 
// 	- width int: Width of image in pixels, must be &gt; 0 
// 	- height int: Height of image in pixels, must be &gt; 0 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Creates a new `GdkPixbuf` structure and allocates a buffer for it.
// 
// If the allocation of the buffer failed, this function will return `NULL`.
// 
// The buffer has an optimal rowstride. Note that the buffer is not cleared;
// you will have to fill it completely yourself.
func NewPixbufInstance(colorspace Colorspace, hasAlpha bool, bitsPerSample int, width int, height int) Pixbuf {
	var carg1 C.GdkColorspace // in, casted
	var carg2 C.gboolean      // in
	var carg3 C.int           // in, casted
	var carg4 C.int           // in, casted
	var carg5 C.int           // in, casted
	var cret  *C.GdkPixbuf    // return, full, class

	carg1 = C.GdkColorspace(colorspace)
	if hasAlpha {
		carg2 = C.TRUE
	}
	carg3 = C.int(bitsPerSample)
	carg4 = C.int(width)
	carg5 = C.int(height)

	cret = C.gdk_pixbuf_new(carg1, carg2, carg3, carg4, carg5)
	runtime.KeepAlive(colorspace)
	runtime.KeepAlive(hasAlpha)
	runtime.KeepAlive(bitsPerSample)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// NewPixbufInstanceFromBytes wraps gdk_pixbuf_new_from_bytes
// 
// The function takes the following parameters:
// 
// 	- data *glib.Bytes: Image data in 8-bit/sample packed format inside a #GBytes 
// 	- colorspace Colorspace: Colorspace for the image data 
// 	- hasAlpha bool: Whether the data has an opacity channel 
// 	- bitsPerSample int: Number of bits per sample 
// 	- width int: Width of the image in pixels, must be &gt; 0 
// 	- height int: Height of the image in pixels, must be &gt; 0 
// 	- rowstride int: Distance in bytes between row starts 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Creates a new #GdkPixbuf out of in-memory readonly image data.
// 
// Currently only RGB images with 8 bits per sample are supported.
// 
// This is the `GBytes` variant of gdk_pixbuf_new_from_data(), useful
// for language bindings.
func NewPixbufInstanceFromBytes(data *glib.Bytes, colorspace Colorspace, hasAlpha bool, bitsPerSample int, width int, height int, rowstride int) Pixbuf {
	var carg1 *C.GBytes       // in, none, record
	var carg2 C.GdkColorspace // in, casted
	var carg3 C.gboolean      // in
	var carg4 C.int           // in, casted
	var carg5 C.int           // in, casted
	var carg6 C.int           // in, casted
	var carg7 C.int           // in, casted
	var cret  *C.GdkPixbuf    // return, full, class

	carg1 = (*C.GBytes)(glib.UnsafeBytesToGlibNone(data))
	carg2 = C.GdkColorspace(colorspace)
	if hasAlpha {
		carg3 = C.TRUE
	}
	carg4 = C.int(bitsPerSample)
	carg5 = C.int(width)
	carg6 = C.int(height)
	carg7 = C.int(rowstride)

	cret = C.gdk_pixbuf_new_from_bytes(carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(data)
	runtime.KeepAlive(colorspace)
	runtime.KeepAlive(hasAlpha)
	runtime.KeepAlive(bitsPerSample)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(rowstride)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// NewPixbufInstanceFromFile wraps gdk_pixbuf_new_from_file
// 
// The function takes the following parameters:
// 
// 	- filename string: Name of file to load, in the GLib file
//   name encoding 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from a file.
// 
// The file format is detected automatically.
// 
// If `NULL` is returned, then @error will be set. Possible errors are:
// 
//  - the file could not be opened
//  - there is no loader for the file's format
//  - there is not enough memory to allocate the image buffer
//  - the image buffer contains invalid data
// 
// The error domains are `GDK_PIXBUF_ERROR` and `G_FILE_ERROR`.
func NewPixbufInstanceFromFile(filename string) (Pixbuf, error) {
	var carg1 *C.gchar     // in, casted
	var cret  *C.GdkPixbuf // return, full, class
	var _cerr *C.GError    // out, full, record, nullable

	carg1 = *C.gchar(filename)

	cret = C.gdk_pixbuf_new_from_file(carg1, &_cerr)
	runtime.KeepAlive(filename)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromFileAtScale wraps gdk_pixbuf_new_from_file_at_scale
// 
// The function takes the following parameters:
// 
// 	- filename string: Name of file to load, in the GLib file
//     name encoding 
// 	- width int: The width the image should have or -1 to not constrain the width 
// 	- height int: The height the image should have or -1 to not constrain the height 
// 	- preserveAspectRatio bool: `TRUE` to preserve the image's aspect ratio 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from a file.
// 
// The file format is detected automatically.
// 
// If `NULL` is returned, then @error will be set. Possible errors are:
// 
//  - the file could not be opened
//  - there is no loader for the file's format
//  - there is not enough memory to allocate the image buffer
//  - the image buffer contains invalid data
// 
// The error domains are `GDK_PIXBUF_ERROR` and `G_FILE_ERROR`.
// 
// The image will be scaled to fit in the requested size, optionally preserving
// the image's aspect ratio.
// 
// When preserving the aspect ratio, a `width` of -1 will cause the image
// to be scaled to the exact given height, and a `height` of -1 will cause
// the image to be scaled to the exact given width. When not preserving
// aspect ratio, a `width` or `height` of -1 means to not scale the image
// at all in that dimension. Negative values for `width` and `height` are
// allowed since 2.8.
func NewPixbufInstanceFromFileAtScale(filename string, width int, height int, preserveAspectRatio bool) (Pixbuf, error) {
	var carg1 *C.gchar     // in, casted
	var carg2 C.int        // in, casted
	var carg3 C.int        // in, casted
	var carg4 C.gboolean   // in
	var cret  *C.GdkPixbuf // return, full, class
	var _cerr *C.GError    // out, full, record, nullable

	carg1 = *C.gchar(filename)
	carg2 = C.int(width)
	carg3 = C.int(height)
	if preserveAspectRatio {
		carg4 = C.TRUE
	}

	cret = C.gdk_pixbuf_new_from_file_at_scale(carg1, carg2, carg3, carg4, &_cerr)
	runtime.KeepAlive(filename)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(preserveAspectRatio)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromFileAtSize wraps gdk_pixbuf_new_from_file_at_size
// 
// The function takes the following parameters:
// 
// 	- filename string: Name of file to load, in the GLib file
//     name encoding 
// 	- width int: The width the image should have or -1 to not constrain the width 
// 	- height int: The height the image should have or -1 to not constrain the height 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from a file.
// 
// The file format is detected automatically.
// 
// If `NULL` is returned, then @error will be set. Possible errors are:
// 
//  - the file could not be opened
//  - there is no loader for the file's format
//  - there is not enough memory to allocate the image buffer
//  - the image buffer contains invalid data
// 
// The error domains are `GDK_PIXBUF_ERROR` and `G_FILE_ERROR`.
// 
// The image will be scaled to fit in the requested size, preserving
// the image's aspect ratio. Note that the returned pixbuf may be smaller
// than `width` x `height`, if the aspect ratio requires it. To load
// and image at the requested size, regardless of aspect ratio, use
// [ctor@GdkPixbuf.Pixbuf.new_from_file_at_scale].
func NewPixbufInstanceFromFileAtSize(filename string, width int, height int) (Pixbuf, error) {
	var carg1 *C.gchar     // in, casted
	var carg2 C.int        // in, casted
	var carg3 C.int        // in, casted
	var cret  *C.GdkPixbuf // return, full, class
	var _cerr *C.GError    // out, full, record, nullable

	carg1 = *C.gchar(filename)
	carg2 = C.int(width)
	carg3 = C.int(height)

	cret = C.gdk_pixbuf_new_from_file_at_size(carg1, carg2, carg3, &_cerr)
	runtime.KeepAlive(filename)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromInline wraps gdk_pixbuf_new_from_inline
// 
// The function takes the following parameters:
// 
// 	- data []uint8: Byte data containing a
//   serialized `GdkPixdata` structure 
// 	- copyPixels bool: Whether to copy the pixel data, or use direct pointers
//   `data` for the resulting pixbuf 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a `GdkPixbuf` from a flat representation that is suitable for
// storing as inline data in a program.
// 
// This is useful if you want to ship a program with images, but don't want
// to depend on any external files.
// 
// GdkPixbuf ships with a program called `gdk-pixbuf-csource`, which allows
// for conversion of `GdkPixbuf`s into such a inline representation.
// 
// In almost all cases, you should pass the `--raw` option to
// `gdk-pixbuf-csource`. A sample invocation would be:
// 
// ```
// gdk-pixbuf-csource --raw --name=myimage_inline myimage.png
// ```
// 
// For the typical case where the inline pixbuf is read-only static data,
// you don't need to copy the pixel data unless you intend to write to
// it, so you can pass `FALSE` for `copy_pixels`. If you pass `--rle` to
// `gdk-pixbuf-csource`, a copy will be made even if `copy_pixels` is `FALSE`,
// so using this option is generally a bad idea.
// 
// If you create a pixbuf from const inline data compiled into your
// program, it's probably safe to ignore errors and disable length checks,
// since things will always succeed:
// 
// ```c
// pixbuf = gdk_pixbuf_new_from_inline (-1, myimage_inline, FALSE, NULL);
// ```
// 
// For non-const inline data, you could get out of memory. For untrusted
// inline data located at runtime, you could have corrupt inline data in
// addition.
//
// Deprecated: (since 2.32.0) Use `GResource` instead.
func NewPixbufInstanceFromInline(data []uint8, copyPixels bool) (Pixbuf, error) {
	var carg1 C.int        // implicit
	var carg2 *C.guint8    // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false
	var carg3 C.gboolean   // in
	var cret  *C.GdkPixbuf // return, full, class
	var _cerr *C.GError    // out, full, record, nullable

	panic("unimplemented conversion of *typesystem.Array (guint8*)")
	if copyPixels {
		carg3 = C.TRUE
	}

	cret = C.gdk_pixbuf_new_from_inline(carg1, carg2, carg3, &_cerr)
	runtime.KeepAlive(data)
	runtime.KeepAlive(copyPixels)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromResource wraps gdk_pixbuf_new_from_resource
// 
// The function takes the following parameters:
// 
// 	- resourcePath string: the path of the resource file 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from an resource.
// 
// The file format is detected automatically. If `NULL` is returned, then
// @error will be set.
func NewPixbufInstanceFromResource(resourcePath string) (Pixbuf, error) {
	var carg1 *C.gchar     // in, none, string
	var cret  *C.GdkPixbuf // return, full, class
	var _cerr *C.GError    // out, full, record, nullable

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(resourcePath)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_new_from_resource(carg1, &_cerr)
	runtime.KeepAlive(resourcePath)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromResourceAtScale wraps gdk_pixbuf_new_from_resource_at_scale
// 
// The function takes the following parameters:
// 
// 	- resourcePath string: the path of the resource file 
// 	- width int: The width the image should have or -1 to not constrain the width 
// 	- height int: The height the image should have or -1 to not constrain the height 
// 	- preserveAspectRatio bool: `TRUE` to preserve the image's aspect ratio 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from an resource.
// 
// The file format is detected automatically. If `NULL` is returned, then
// @error will be set.
// 
// The image will be scaled to fit in the requested size, optionally
// preserving the image's aspect ratio. When preserving the aspect ratio,
// a @width of -1 will cause the image to be scaled to the exact given
// height, and a @height of -1 will cause the image to be scaled to the
// exact given width. When not preserving aspect ratio, a @width or
// @height of -1 means to not scale the image at all in that dimension.
// 
// The stream is not closed.
func NewPixbufInstanceFromResourceAtScale(resourcePath string, width int, height int, preserveAspectRatio bool) (Pixbuf, error) {
	var carg1 *C.gchar     // in, none, string
	var carg2 C.int        // in, casted
	var carg3 C.int        // in, casted
	var carg4 C.gboolean   // in
	var cret  *C.GdkPixbuf // return, full, class
	var _cerr *C.GError    // out, full, record, nullable

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(resourcePath)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(width)
	carg3 = C.int(height)
	if preserveAspectRatio {
		carg4 = C.TRUE
	}

	cret = C.gdk_pixbuf_new_from_resource_at_scale(carg1, carg2, carg3, carg4, &_cerr)
	runtime.KeepAlive(resourcePath)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(preserveAspectRatio)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromStream wraps gdk_pixbuf_new_from_stream
// 
// The function takes the following parameters:
// 
// 	- stream gio.InputStream: a `GInputStream` to load the pixbuf from 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from an input stream.
// 
// The file format is detected automatically.
// 
// If `NULL` is returned, then `error` will be set.
// 
// The `cancellable` can be used to abort the operation from another thread.
// If the operation was cancelled, the error `G_IO_ERROR_CANCELLED` will be
// returned. Other possible errors are in the `GDK_PIXBUF_ERROR` and
// `G_IO_ERROR` domains.
// 
// The stream is not closed.
func NewPixbufInstanceFromStream(stream gio.InputStream, cancellable gio.Cancellable) (Pixbuf, error) {
	var carg1 *C.GInputStream // in, none, class
	var carg2 *C.GCancellable // in, none, class, nullable
	var cret  *C.GdkPixbuf    // return, full, class
	var _cerr *C.GError       // out, full, record, nullable

	carg1 = (*C.GInputStream)(gio.UnsafeInputStreamToGlibNone(stream))
	if cancellable != nil {
		carg2 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}

	cret = C.gdk_pixbuf_new_from_stream(carg1, carg2, &_cerr)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(cancellable)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromStreamAtScale wraps gdk_pixbuf_new_from_stream_at_scale
// 
// The function takes the following parameters:
// 
// 	- stream gio.InputStream: a `GInputStream` to load the pixbuf from 
// 	- width int: The width the image should have or -1 to not constrain the width 
// 	- height int: The height the image should have or -1 to not constrain the height 
// 	- preserveAspectRatio bool: `TRUE` to preserve the image's aspect ratio 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf by loading an image from an input stream.
// 
// The file format is detected automatically. If `NULL` is returned, then
// @error will be set. The @cancellable can be used to abort the operation
// from another thread. If the operation was cancelled, the error
// `G_IO_ERROR_CANCELLED` will be returned. Other possible errors are in
// the `GDK_PIXBUF_ERROR` and `G_IO_ERROR` domains.
// 
// The image will be scaled to fit in the requested size, optionally
// preserving the image's aspect ratio.
// 
// When preserving the aspect ratio, a `width` of -1 will cause the image to be
// scaled to the exact given height, and a `height` of -1 will cause the image
// to be scaled to the exact given width. If both `width` and `height` are
// given, this function will behave as if the smaller of the two values
// is passed as -1.
// 
// When not preserving aspect ratio, a `width` or `height` of -1 means to not
// scale the image at all in that dimension.
// 
// The stream is not closed.
func NewPixbufInstanceFromStreamAtScale(stream gio.InputStream, width int, height int, preserveAspectRatio bool, cancellable gio.Cancellable) (Pixbuf, error) {
	var carg1 *C.GInputStream // in, none, class
	var carg2 C.int           // in, casted
	var carg3 C.int           // in, casted
	var carg4 C.gboolean      // in
	var carg5 *C.GCancellable // in, none, class, nullable
	var cret  *C.GdkPixbuf    // return, full, class
	var _cerr *C.GError       // out, full, record, nullable

	carg1 = (*C.GInputStream)(gio.UnsafeInputStreamToGlibNone(stream))
	carg2 = C.int(width)
	carg3 = C.int(height)
	if preserveAspectRatio {
		carg4 = C.TRUE
	}
	if cancellable != nil {
		carg5 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}

	cret = C.gdk_pixbuf_new_from_stream_at_scale(carg1, carg2, carg3, carg4, carg5, &_cerr)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(preserveAspectRatio)
	runtime.KeepAlive(cancellable)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromStreamFinish wraps gdk_pixbuf_new_from_stream_finish
// 
// The function takes the following parameters:
// 
// 	- asyncResult gio.AsyncResult: a `GAsyncResult` 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
// 	- _goerr error (nullable): an error 
//
// Finishes an asynchronous pixbuf creation operation started with
// gdk_pixbuf_new_from_stream_async().
func NewPixbufInstanceFromStreamFinish(asyncResult gio.AsyncResult) (Pixbuf, error) {
	var carg1 *C.GAsyncResult // in, none, interface
	var cret  *C.GdkPixbuf    // return, full, class
	var _cerr *C.GError       // out, full, record, nullable

	carg1 = (*C.GAsyncResult)(gio.UnsafeAsyncResultToGlibNone(asyncResult))

	cret = C.gdk_pixbuf_new_from_stream_finish(carg1, &_cerr)
	runtime.KeepAlive(asyncResult)

	var ret    Pixbuf
	var _goerr error

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufInstanceFromXPMData wraps gdk_pixbuf_new_from_xpm_data
// 
// The function takes the following parameters:
// 
// 	- data []string: Pointer to inline XPM data. 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Creates a new pixbuf by parsing XPM data in memory.
// 
// This data is commonly the result of including an XPM file into a
// program's C source.
func NewPixbufInstanceFromXPMData(data []string) Pixbuf {
	var carg1 **C.gchar    // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false
	var cret  *C.GdkPixbuf // return, full, class

	panic("unimplemented conversion of *typesystem.Array (gchar**)")

	cret = C.gdk_pixbuf_new_from_xpm_data(carg1)
	runtime.KeepAlive(data)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// CalculateRowstride wraps gdk_pixbuf_calculate_rowstride
// 
// The function takes the following parameters:
// 
// 	- colorspace Colorspace: Color space for image 
// 	- hasAlpha bool: Whether the image should have transparency information 
// 	- bitsPerSample int: Number of bits per color sample 
// 	- width int: Width of image in pixels, must be &gt; 0 
// 	- height int: Height of image in pixels, must be &gt; 0 
// 
// The function returns the following values:
// 
// 	- ret int 
//
// Calculates the rowstride that an image created with those values would
// have.
// 
// This function is useful for front-ends and backends that want to check
// image values without needing to create a `GdkPixbuf`.
func CalculateRowstride(colorspace Colorspace, hasAlpha bool, bitsPerSample int, width int, height int) int {
	var carg1 C.GdkColorspace // in, casted
	var carg2 C.gboolean      // in
	var carg3 C.int           // in, casted
	var carg4 C.int           // in, casted
	var carg5 C.int           // in, casted
	var cret  C.int           // return, casted

	carg1 = C.GdkColorspace(colorspace)
	if hasAlpha {
		carg2 = C.TRUE
	}
	carg3 = C.int(bitsPerSample)
	carg4 = C.int(width)
	carg5 = C.int(height)

	cret = C.gdk_pixbuf_calculate_rowstride(carg1, carg2, carg3, carg4, carg5)
	runtime.KeepAlive(colorspace)
	runtime.KeepAlive(hasAlpha)
	runtime.KeepAlive(bitsPerSample)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)

	var ret int

	ret = int(cret)

	return ret
}

// GetFileInfo wraps gdk_pixbuf_get_file_info
// 
// The function takes the following parameters:
// 
// 	- filename string: The name of the file to identify. 
// 
// The function returns the following values:
// 
// 	- width int: Return location for the width of the image 
// 	- height int: Return location for the height of the image 
// 	- ret *PixbufFormat 
//
// Parses an image file far enough to determine its format and size.
func GetFileInfo(filename string) (int, int, *PixbufFormat) {
	var carg1 *C.gchar           // in, casted
	var carg2 C.int              // out, casted
	var carg3 C.int              // out, casted
	var cret  *C.GdkPixbufFormat // return, none, record

	carg1 = *C.gchar(filename)

	cret = C.gdk_pixbuf_get_file_info(carg1, &carg2, &carg3)
	runtime.KeepAlive(filename)

	var width  int
	var height int
	var ret    *PixbufFormat

	width = int(carg2)
	height = int(carg3)
	ret = UnsafePixbufFormatFromGlibNone(unsafe.Pointer(cret))

	return width, height, ret
}

// GetFileInfoAsync wraps gdk_pixbuf_get_file_info_async
// 
// The function takes the following parameters:
// 
// 	- filename string: The name of the file to identify 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 	- callback gio.AsyncReadyCallback (nullable): a `GAsyncReadyCallback` to call when the file info is available 
//
// Asynchronously parses an image file far enough to determine its
// format and size.
// 
// For more details see gdk_pixbuf_get_file_info(), which is the synchronous
// version of this function.
// 
// When the operation is finished, @callback will be called in the
// main thread. You can then call gdk_pixbuf_get_file_info_finish() to
// get the result of the operation.
func GetFileInfoAsync(filename string, cancellable gio.Cancellable, callback gio.AsyncReadyCallback) {
	var carg1 *C.gchar              // in, casted
	var carg2 *C.GCancellable       // in, none, class, nullable
	var carg3 C.GAsyncReadyCallback // callback, scope: async, closure: carg4, nullable
	var carg4 C.gpointer            // implicit

	carg1 = *C.gchar(filename)
	if cancellable != nil {
		carg2 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}
	if callback != nil {
		carg3 = (*[0]byte)(C._gotk4_gio2_AsyncReadyCallback)
		carg4 = C.gpointer(gbox.AssignOnce(callback))
	}

	C.gdk_pixbuf_get_file_info_async(carg1, carg2, carg3, carg4)
	runtime.KeepAlive(filename)
	runtime.KeepAlive(cancellable)
	runtime.KeepAlive(callback)
}

// GetFileInfoFinish wraps gdk_pixbuf_get_file_info_finish
// 
// The function takes the following parameters:
// 
// 	- asyncResult gio.AsyncResult: a `GAsyncResult` 
// 
// The function returns the following values:
// 
// 	- width int: Return location for the width of the image, or `NULL` 
// 	- height int: Return location for the height of the image, or `NULL` 
// 	- ret *PixbufFormat 
// 	- _goerr error (nullable): an error 
//
// Finishes an asynchronous pixbuf parsing operation started with
// gdk_pixbuf_get_file_info_async().
func GetFileInfoFinish(asyncResult gio.AsyncResult) (int, int, *PixbufFormat, error) {
	var carg1 *C.GAsyncResult    // in, none, interface
	var carg2 C.int              // out, casted
	var carg3 C.int              // out, casted
	var cret  *C.GdkPixbufFormat // return, none, record
	var _cerr *C.GError          // out, full, record, nullable

	carg1 = (*C.GAsyncResult)(gio.UnsafeAsyncResultToGlibNone(asyncResult))

	cret = C.gdk_pixbuf_get_file_info_finish(carg1, &carg2, &carg3, &_cerr)
	runtime.KeepAlive(asyncResult)

	var width  int
	var height int
	var ret    *PixbufFormat
	var _goerr error

	width = int(carg2)
	height = int(carg3)
	ret = UnsafePixbufFormatFromGlibNone(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return width, height, ret, _goerr
}

// GetFormats wraps gdk_pixbuf_get_formats
// The function returns the following values:
// 
// 	- ret *glib.SList 
//
// Obtains the available information about the image formats supported
// by GdkPixbuf.
func GetFormats() *glib.SList {
	var cret *C.GSList // return, container inner types: (GdkPixbufFormat), transfer: container, scope: , implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false

	cret = C.gdk_pixbuf_get_formats()

	var ret *glib.SList

	panic("unimplemented conversion of container *typesystem.PointerType (GSList*)")

	return ret
}

// InitModules wraps gdk_pixbuf_init_modules
// 
// The function takes the following parameters:
// 
// 	- path string: Path to directory where the `loaders.cache` is installed 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Initalizes the gdk-pixbuf loader modules referenced by the `loaders.cache`
// file present inside that directory.
// 
// This is to be used by applications that want to ship certain loaders
// in a different location from the system ones.
// 
// This is needed when the OS or runtime ships a minimal number of loaders
// so as to reduce the potential attack surface of carefully crafted image
// files, especially for uncommon file types. Applications that require
// broader image file types coverage, such as image viewers, would be
// expected to ship the gdk-pixbuf modules in a separate location, bundled
// with the application in a separate directory from the OS or runtime-
// provided modules.
func InitModules(path string) (bool, error) {
	var carg1 *C.gchar   // in, none, string
	var cret  C.gboolean // return
	var _cerr *C.GError  // out, full, record, nullable

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(path)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_init_modules(carg1, &_cerr)
	runtime.KeepAlive(path)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewFromStreamAsync wraps gdk_pixbuf_new_from_stream_async
// 
// The function takes the following parameters:
// 
// 	- stream gio.InputStream: a `GInputStream` from which to load the pixbuf 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 	- callback gio.AsyncReadyCallback (nullable): a `GAsyncReadyCallback` to call when the pixbuf is loaded 
//
// Creates a new pixbuf by asynchronously loading an image from an input stream.
// 
// For more details see gdk_pixbuf_new_from_stream(), which is the synchronous
// version of this function.
// 
// When the operation is finished, @callback will be called in the main thread.
// You can then call gdk_pixbuf_new_from_stream_finish() to get the result of
// the operation.
func NewFromStreamAsync(stream gio.InputStream, cancellable gio.Cancellable, callback gio.AsyncReadyCallback) {
	var carg1 *C.GInputStream       // in, none, class
	var carg2 *C.GCancellable       // in, none, class, nullable
	var carg3 C.GAsyncReadyCallback // callback, scope: async, closure: carg4, nullable
	var carg4 C.gpointer            // implicit

	carg1 = (*C.GInputStream)(gio.UnsafeInputStreamToGlibNone(stream))
	if cancellable != nil {
		carg2 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}
	if callback != nil {
		carg3 = (*[0]byte)(C._gotk4_gio2_AsyncReadyCallback)
		carg4 = C.gpointer(gbox.AssignOnce(callback))
	}

	C.gdk_pixbuf_new_from_stream_async(carg1, carg2, carg3, carg4)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(cancellable)
	runtime.KeepAlive(callback)
}

// NewFromStreamAtScaleAsync wraps gdk_pixbuf_new_from_stream_at_scale_async
// 
// The function takes the following parameters:
// 
// 	- stream gio.InputStream: a `GInputStream` from which to load the pixbuf 
// 	- width int: the width the image should have or -1 to not constrain the width 
// 	- height int: the height the image should have or -1 to not constrain the height 
// 	- preserveAspectRatio bool: `TRUE` to preserve the image's aspect ratio 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 	- callback gio.AsyncReadyCallback (nullable): a `GAsyncReadyCallback` to call when the pixbuf is loaded 
//
// Creates a new pixbuf by asynchronously loading an image from an input stream.
// 
// For more details see gdk_pixbuf_new_from_stream_at_scale(), which is the synchronous
// version of this function.
// 
// When the operation is finished, @callback will be called in the main thread.
// You can then call gdk_pixbuf_new_from_stream_finish() to get the result of the operation.
func NewFromStreamAtScaleAsync(stream gio.InputStream, width int, height int, preserveAspectRatio bool, cancellable gio.Cancellable, callback gio.AsyncReadyCallback) {
	var carg1 *C.GInputStream       // in, none, class
	var carg2 C.int                 // in, casted
	var carg3 C.int                 // in, casted
	var carg4 C.gboolean            // in
	var carg5 *C.GCancellable       // in, none, class, nullable
	var carg6 C.GAsyncReadyCallback // callback, scope: async, closure: carg7, nullable
	var carg7 C.gpointer            // implicit

	carg1 = (*C.GInputStream)(gio.UnsafeInputStreamToGlibNone(stream))
	carg2 = C.int(width)
	carg3 = C.int(height)
	if preserveAspectRatio {
		carg4 = C.TRUE
	}
	if cancellable != nil {
		carg5 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}
	if callback != nil {
		carg6 = (*[0]byte)(C._gotk4_gio2_AsyncReadyCallback)
		carg7 = C.gpointer(gbox.AssignOnce(callback))
	}

	C.gdk_pixbuf_new_from_stream_at_scale_async(carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(preserveAspectRatio)
	runtime.KeepAlive(cancellable)
	runtime.KeepAlive(callback)
}

// SaveToStreamFinish wraps gdk_pixbuf_save_to_stream_finish
// 
// The function takes the following parameters:
// 
// 	- asyncResult gio.AsyncResult: a `GAsyncResult` 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Finishes an asynchronous pixbuf save operation started with
// gdk_pixbuf_save_to_stream_async().
func SaveToStreamFinish(asyncResult gio.AsyncResult) (bool, error) {
	var carg1 *C.GAsyncResult // in, none, interface
	var cret  C.gboolean      // return
	var _cerr *C.GError       // out, full, record, nullable

	carg1 = (*C.GAsyncResult)(gio.UnsafeAsyncResultToGlibNone(asyncResult))

	cret = C.gdk_pixbuf_save_to_stream_finish(carg1, &_cerr)
	runtime.KeepAlive(asyncResult)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// AddAlpha wraps gdk_pixbuf_add_alpha
// 
// The function takes the following parameters:
// 
// 	- substituteColor bool: Whether to set a color to zero opacity. 
// 	- r uint8: Red value to substitute. 
// 	- g uint8: Green value to substitute. 
// 	- b uint8: Blue value to substitute. 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Takes an existing pixbuf and adds an alpha channel to it.
// 
// If the existing pixbuf already had an alpha channel, the channel
// values are copied from the original; otherwise, the alpha channel
// is initialized to 255 (full opacity).
// 
// If `substitute_color` is `TRUE`, then the color specified by the
// (`r`, `g`, `b`) arguments will be assigned zero opacity. That is,
// if you pass `(255, 255, 255)` for the substitute color, all white
// pixels will become fully transparent.
// 
// If `substitute_color` is `FALSE`, then the (`r`, `g`, `b`) arguments
// will be ignored.
func (pixbuf *PixbufInstance) AddAlpha(substituteColor bool, r uint8, g uint8, b uint8) Pixbuf {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 C.gboolean   // in
	var carg2 C.guint8     // in, casted
	var carg3 C.guint8     // in, casted
	var carg4 C.guint8     // in, casted
	var cret  *C.GdkPixbuf // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	if substituteColor {
		carg1 = C.TRUE
	}
	carg2 = C.guint8(r)
	carg3 = C.guint8(g)
	carg4 = C.guint8(b)

	cret = C.gdk_pixbuf_add_alpha(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(substituteColor)
	runtime.KeepAlive(r)
	runtime.KeepAlive(g)
	runtime.KeepAlive(b)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// ApplyEmbeddedOrientation wraps gdk_pixbuf_apply_embedded_orientation
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Takes an existing pixbuf and checks for the presence of an
// associated "orientation" option.
// 
// The orientation option may be provided by the JPEG loader (which
// reads the exif orientation tag) or the TIFF loader (which reads
// the TIFF orientation tag, and compensates it for the partial
// transforms performed by libtiff).
// 
// If an orientation option/tag is present, the appropriate transform
// will be performed so that the pixbuf is oriented correctly.
func (src *PixbufInstance) ApplyEmbeddedOrientation() Pixbuf {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  *C.GdkPixbuf // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))

	cret = C.gdk_pixbuf_apply_embedded_orientation(carg0)
	runtime.KeepAlive(src)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// Composite wraps gdk_pixbuf_composite
// 
// The function takes the following parameters:
// 
// 	- dest Pixbuf: the #GdkPixbuf into which to render the results 
// 	- destX int: the left coordinate for region to render 
// 	- destY int: the top coordinate for region to render 
// 	- destWidth int: the width of the region to render 
// 	- destHeight int: the height of the region to render 
// 	- offsetX float64: the offset in the X direction (currently rounded to an integer) 
// 	- offsetY float64: the offset in the Y direction (currently rounded to an integer) 
// 	- scaleX float64: the scale factor in the X direction 
// 	- scaleY float64: the scale factor in the Y direction 
// 	- interpType InterpType: the interpolation type for the transformation. 
// 	- overallAlpha int: overall alpha for source image (0..255) 
//
// Creates a transformation of the source image @src by scaling by
// @scale_x and @scale_y then translating by @offset_x and @offset_y.
// 
// This gives an image in the coordinates of the destination pixbuf.
// The rectangle (@dest_x, @dest_y, @dest_width, @dest_height)
// is then alpha blended onto the corresponding rectangle of the
// original destination image.
// 
// When the destination rectangle contains parts not in the source
// image, the data at the edges of the source image is replicated
// to infinity.
// 
// ![](composite.png)
func (src *PixbufInstance) Composite(dest Pixbuf, destX int, destY int, destWidth int, destHeight int, offsetX float64, offsetY float64, scaleX float64, scaleY float64, interpType InterpType, overallAlpha int) {
	var carg0  *C.GdkPixbuf    // in, none, class
	var carg1  *C.GdkPixbuf    // in, none, class
	var carg2  C.int           // in, casted
	var carg3  C.int           // in, casted
	var carg4  C.int           // in, casted
	var carg5  C.int           // in, casted
	var carg6  C.gdouble       // in, casted
	var carg7  C.gdouble       // in, casted
	var carg8  C.gdouble       // in, casted
	var carg9  C.gdouble       // in, casted
	var carg10 C.GdkInterpType // in, casted
	var carg11 C.int           // in, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(dest))
	carg2 = C.int(destX)
	carg3 = C.int(destY)
	carg4 = C.int(destWidth)
	carg5 = C.int(destHeight)
	carg6 = C.gdouble(offsetX)
	carg7 = C.gdouble(offsetY)
	carg8 = C.gdouble(scaleX)
	carg9 = C.gdouble(scaleY)
	carg10 = C.GdkInterpType(interpType)
	carg11 = C.int(overallAlpha)

	C.gdk_pixbuf_composite(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7, carg8, carg9, carg10, carg11)
	runtime.KeepAlive(src)
	runtime.KeepAlive(dest)
	runtime.KeepAlive(destX)
	runtime.KeepAlive(destY)
	runtime.KeepAlive(destWidth)
	runtime.KeepAlive(destHeight)
	runtime.KeepAlive(offsetX)
	runtime.KeepAlive(offsetY)
	runtime.KeepAlive(scaleX)
	runtime.KeepAlive(scaleY)
	runtime.KeepAlive(interpType)
	runtime.KeepAlive(overallAlpha)
}

// CompositeColor wraps gdk_pixbuf_composite_color
// 
// The function takes the following parameters:
// 
// 	- dest Pixbuf: the #GdkPixbuf into which to render the results 
// 	- destX int: the left coordinate for region to render 
// 	- destY int: the top coordinate for region to render 
// 	- destWidth int: the width of the region to render 
// 	- destHeight int: the height of the region to render 
// 	- offsetX float64: the offset in the X direction (currently rounded to an integer) 
// 	- offsetY float64: the offset in the Y direction (currently rounded to an integer) 
// 	- scaleX float64: the scale factor in the X direction 
// 	- scaleY float64: the scale factor in the Y direction 
// 	- interpType InterpType: the interpolation type for the transformation. 
// 	- overallAlpha int: overall alpha for source image (0..255) 
// 	- checkX int: the X offset for the checkboard (origin of checkboard is at -@check_x, -@check_y) 
// 	- checkY int: the Y offset for the checkboard 
// 	- checkSize int: the size of checks in the checkboard (must be a power of two) 
// 	- color1 uint32: the color of check at upper left 
// 	- color2 uint32: the color of the other check 
//
// Creates a transformation of the source image @src by scaling by
// @scale_x and @scale_y then translating by @offset_x and @offset_y,
// then alpha blends the rectangle (@dest_x ,@dest_y, @dest_width,
// @dest_height) of the resulting image with a checkboard of the
// colors @color1 and @color2 and renders it onto the destination
// image.
// 
// If the source image has no alpha channel, and @overall_alpha is 255, a fast
// path is used which omits the alpha blending and just performs the scaling.
// 
// See gdk_pixbuf_composite_color_simple() for a simpler variant of this
// function suitable for many tasks.
func (src *PixbufInstance) CompositeColor(dest Pixbuf, destX int, destY int, destWidth int, destHeight int, offsetX float64, offsetY float64, scaleX float64, scaleY float64, interpType InterpType, overallAlpha int, checkX int, checkY int, checkSize int, color1 uint32, color2 uint32) {
	var carg0  *C.GdkPixbuf    // in, none, class
	var carg1  *C.GdkPixbuf    // in, none, class
	var carg2  C.int           // in, casted
	var carg3  C.int           // in, casted
	var carg4  C.int           // in, casted
	var carg5  C.int           // in, casted
	var carg6  C.gdouble       // in, casted
	var carg7  C.gdouble       // in, casted
	var carg8  C.gdouble       // in, casted
	var carg9  C.gdouble       // in, casted
	var carg10 C.GdkInterpType // in, casted
	var carg11 C.int           // in, casted
	var carg12 C.int           // in, casted
	var carg13 C.int           // in, casted
	var carg14 C.int           // in, casted
	var carg15 C.guint32       // in, casted
	var carg16 C.guint32       // in, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(dest))
	carg2 = C.int(destX)
	carg3 = C.int(destY)
	carg4 = C.int(destWidth)
	carg5 = C.int(destHeight)
	carg6 = C.gdouble(offsetX)
	carg7 = C.gdouble(offsetY)
	carg8 = C.gdouble(scaleX)
	carg9 = C.gdouble(scaleY)
	carg10 = C.GdkInterpType(interpType)
	carg11 = C.int(overallAlpha)
	carg12 = C.int(checkX)
	carg13 = C.int(checkY)
	carg14 = C.int(checkSize)
	carg15 = C.guint32(color1)
	carg16 = C.guint32(color2)

	C.gdk_pixbuf_composite_color(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7, carg8, carg9, carg10, carg11, carg12, carg13, carg14, carg15, carg16)
	runtime.KeepAlive(src)
	runtime.KeepAlive(dest)
	runtime.KeepAlive(destX)
	runtime.KeepAlive(destY)
	runtime.KeepAlive(destWidth)
	runtime.KeepAlive(destHeight)
	runtime.KeepAlive(offsetX)
	runtime.KeepAlive(offsetY)
	runtime.KeepAlive(scaleX)
	runtime.KeepAlive(scaleY)
	runtime.KeepAlive(interpType)
	runtime.KeepAlive(overallAlpha)
	runtime.KeepAlive(checkX)
	runtime.KeepAlive(checkY)
	runtime.KeepAlive(checkSize)
	runtime.KeepAlive(color1)
	runtime.KeepAlive(color2)
}

// CompositeColorSimple wraps gdk_pixbuf_composite_color_simple
// 
// The function takes the following parameters:
// 
// 	- destWidth int: the width of destination image 
// 	- destHeight int: the height of destination image 
// 	- interpType InterpType: the interpolation type for the transformation. 
// 	- overallAlpha int: overall alpha for source image (0..255) 
// 	- checkSize int: the size of checks in the checkboard (must be a power of two) 
// 	- color1 uint32: the color of check at upper left 
// 	- color2 uint32: the color of the other check 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Creates a new pixbuf by scaling `src` to `dest_width` x `dest_height`
// and alpha blending the result with a checkboard of colors `color1`
// and `color2`.
func (src *PixbufInstance) CompositeColorSimple(destWidth int, destHeight int, interpType InterpType, overallAlpha int, checkSize int, color1 uint32, color2 uint32) Pixbuf {
	var carg0 *C.GdkPixbuf    // in, none, class
	var carg1 C.int           // in, casted
	var carg2 C.int           // in, casted
	var carg3 C.GdkInterpType // in, casted
	var carg4 C.int           // in, casted
	var carg5 C.int           // in, casted
	var carg6 C.guint32       // in, casted
	var carg7 C.guint32       // in, casted
	var cret  *C.GdkPixbuf    // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = C.int(destWidth)
	carg2 = C.int(destHeight)
	carg3 = C.GdkInterpType(interpType)
	carg4 = C.int(overallAlpha)
	carg5 = C.int(checkSize)
	carg6 = C.guint32(color1)
	carg7 = C.guint32(color2)

	cret = C.gdk_pixbuf_composite_color_simple(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(src)
	runtime.KeepAlive(destWidth)
	runtime.KeepAlive(destHeight)
	runtime.KeepAlive(interpType)
	runtime.KeepAlive(overallAlpha)
	runtime.KeepAlive(checkSize)
	runtime.KeepAlive(color1)
	runtime.KeepAlive(color2)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// Copy wraps gdk_pixbuf_copy
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Creates a new `GdkPixbuf` with a copy of the information in the specified
// `pixbuf`.
// 
// Note that this does not copy the options set on the original `GdkPixbuf`,
// use gdk_pixbuf_copy_options() for this.
func (pixbuf *PixbufInstance) Copy() Pixbuf {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  *C.GdkPixbuf // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_copy(carg0)
	runtime.KeepAlive(pixbuf)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// CopyArea wraps gdk_pixbuf_copy_area
// 
// The function takes the following parameters:
// 
// 	- srcX int: Source X coordinate within @src_pixbuf. 
// 	- srcY int: Source Y coordinate within @src_pixbuf. 
// 	- width int: Width of the area to copy. 
// 	- height int: Height of the area to copy. 
// 	- destPixbuf Pixbuf: Destination pixbuf. 
// 	- destX int: X coordinate within @dest_pixbuf. 
// 	- destY int: Y coordinate within @dest_pixbuf. 
//
// Copies a rectangular area from `src_pixbuf` to `dest_pixbuf`.
// 
// Conversion of pixbuf formats is done automatically.
// 
// If the source rectangle overlaps the destination rectangle on the
// same pixbuf, it will be overwritten during the copy operation.
// Therefore, you can not use this function to scroll a pixbuf.
func (srcPixbuf *PixbufInstance) CopyArea(srcX int, srcY int, width int, height int, destPixbuf Pixbuf, destX int, destY int) {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 C.int        // in, casted
	var carg2 C.int        // in, casted
	var carg3 C.int        // in, casted
	var carg4 C.int        // in, casted
	var carg5 *C.GdkPixbuf // in, none, class
	var carg6 C.int        // in, casted
	var carg7 C.int        // in, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(srcPixbuf))
	carg1 = C.int(srcX)
	carg2 = C.int(srcY)
	carg3 = C.int(width)
	carg4 = C.int(height)
	carg5 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(destPixbuf))
	carg6 = C.int(destX)
	carg7 = C.int(destY)

	C.gdk_pixbuf_copy_area(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(srcPixbuf)
	runtime.KeepAlive(srcX)
	runtime.KeepAlive(srcY)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(destPixbuf)
	runtime.KeepAlive(destX)
	runtime.KeepAlive(destY)
}

// CopyOptions wraps gdk_pixbuf_copy_options
// 
// The function takes the following parameters:
// 
// 	- destPixbuf Pixbuf: the destination pixbuf 
// 
// The function returns the following values:
// 
// 	- ret bool 
//
// Copies the key/value pair options attached to a `GdkPixbuf` to another
// `GdkPixbuf`.
// 
// This is useful to keep original metadata after having manipulated
// a file. However be careful to remove metadata which you've already
// applied, such as the "orientation" option after rotating the image.
func (srcPixbuf *PixbufInstance) CopyOptions(destPixbuf Pixbuf) bool {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 *C.GdkPixbuf // in, none, class
	var cret  C.gboolean   // return

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(srcPixbuf))
	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(destPixbuf))

	cret = C.gdk_pixbuf_copy_options(carg0, carg1)
	runtime.KeepAlive(srcPixbuf)
	runtime.KeepAlive(destPixbuf)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// Fill wraps gdk_pixbuf_fill
// 
// The function takes the following parameters:
// 
// 	- pixel uint32: RGBA pixel to used to clear (`0xffffffff` is opaque white,
//   `0x00000000` transparent black) 
//
// Clears a pixbuf to the given RGBA value, converting the RGBA value into
// the pixbuf's pixel format.
// 
// The alpha component will be ignored if the pixbuf doesn't have an alpha
// channel.
func (pixbuf *PixbufInstance) Fill(pixel uint32) {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 C.guint32    // in, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = C.guint32(pixel)

	C.gdk_pixbuf_fill(carg0, carg1)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(pixel)
}

// Flip wraps gdk_pixbuf_flip
// 
// The function takes the following parameters:
// 
// 	- horizontal bool: `TRUE` to flip horizontally, `FALSE` to flip vertically 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Flips a pixbuf horizontally or vertically and returns the
// result in a new pixbuf.
func (src *PixbufInstance) Flip(horizontal bool) Pixbuf {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 C.gboolean   // in
	var cret  *C.GdkPixbuf // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	if horizontal {
		carg1 = C.TRUE
	}

	cret = C.gdk_pixbuf_flip(carg0, carg1)
	runtime.KeepAlive(src)
	runtime.KeepAlive(horizontal)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// GetBitsPerSample wraps gdk_pixbuf_get_bits_per_sample
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the number of bits per color sample in a pixbuf.
func (pixbuf *PixbufInstance) GetBitsPerSample() int {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.int        // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_bits_per_sample(carg0)
	runtime.KeepAlive(pixbuf)

	var ret int

	ret = int(cret)

	return ret
}

// GetByteLength wraps gdk_pixbuf_get_byte_length
// The function returns the following values:
// 
// 	- ret uint 
//
// Returns the length of the pixel data, in bytes.
func (pixbuf *PixbufInstance) GetByteLength() uint {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.gsize      // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_byte_length(carg0)
	runtime.KeepAlive(pixbuf)

	var ret uint

	ret = uint(cret)

	return ret
}

// GetColorspace wraps gdk_pixbuf_get_colorspace
// The function returns the following values:
// 
// 	- ret Colorspace 
//
// Queries the color space of a pixbuf.
func (pixbuf *PixbufInstance) GetColorspace() Colorspace {
	var carg0 *C.GdkPixbuf    // in, none, class
	var cret  C.GdkColorspace // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_colorspace(carg0)
	runtime.KeepAlive(pixbuf)

	var ret Colorspace

	ret = Colorspace(cret)

	return ret
}

// GetHasAlpha wraps gdk_pixbuf_get_has_alpha
// The function returns the following values:
// 
// 	- ret bool 
//
// Queries whether a pixbuf has an alpha channel (opacity information).
func (pixbuf *PixbufInstance) GetHasAlpha() bool {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.gboolean   // return

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_has_alpha(carg0)
	runtime.KeepAlive(pixbuf)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// GetHeight wraps gdk_pixbuf_get_height
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the height of a pixbuf.
func (pixbuf *PixbufInstance) GetHeight() int {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.int        // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_height(carg0)
	runtime.KeepAlive(pixbuf)

	var ret int

	ret = int(cret)

	return ret
}

// GetNChannels wraps gdk_pixbuf_get_n_channels
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the number of channels of a pixbuf.
func (pixbuf *PixbufInstance) GetNChannels() int {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.int        // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_n_channels(carg0)
	runtime.KeepAlive(pixbuf)

	var ret int

	ret = int(cret)

	return ret
}

// GetOption wraps gdk_pixbuf_get_option
// 
// The function takes the following parameters:
// 
// 	- key string: a nul-terminated string. 
// 
// The function returns the following values:
// 
// 	- ret string 
//
// Looks up @key in the list of options that may have been attached to the
// @pixbuf when it was loaded, or that may have been attached by another
// function using gdk_pixbuf_set_option().
// 
// For instance, the ANI loader provides "Title" and "Artist" options.
// The ICO, XBM, and XPM loaders provide "x_hot" and "y_hot" hot-spot
// options for cursor definitions. The PNG loader provides the tEXt ancillary
// chunk key/value pairs as options. Since 2.12, the TIFF and JPEG loaders
// return an "orientation" option string that corresponds to the embedded
// TIFF/Exif orientation tag (if present). Since 2.32, the TIFF loader sets
// the "multipage" option string to "yes" when a multi-page TIFF is loaded.
// Since 2.32 the JPEG and PNG loaders set "x-dpi" and "y-dpi" if the file
// contains image density information in dots per inch.
// Since 2.36.6, the JPEG loader sets the "comment" option with the comment
// EXIF tag.
func (pixbuf *PixbufInstance) GetOption(key string) string {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 *C.gchar     // in, none, string
	var cret  *C.gchar     // return, none, string

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(key)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_get_option(carg0, carg1)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(key)

	var ret string

	ret = C.GoString((*C.gchar)(unsafe.Pointer(cret)))

	return ret
}

// GetOptions wraps gdk_pixbuf_get_options
// The function returns the following values:
// 
// 	- ret *glib.HashTable 
//
// Returns a `GHashTable` with a list of all the options that may have been
// attached to the `pixbuf` when it was loaded, or that may have been
// attached by another function using [method@GdkPixbuf.Pixbuf.set_option].
func (pixbuf *PixbufInstance) GetOptions() *glib.HashTable {
	var carg0 *C.GdkPixbuf  // in, none, class
	var cret  *C.GHashTable // return, container inner types: (gchar*, gchar*), transfer: container, scope: , implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_options(carg0)
	runtime.KeepAlive(pixbuf)

	var ret *glib.HashTable

	panic("unimplemented conversion of container *typesystem.PointerType (GHashTable*)")

	return ret
}

// GetPixelsWithLength wraps gdk_pixbuf_get_pixels_with_length
// The function returns the following values:
// 
// 	- length uint: The length of the binary data. 
// 	- ret []uint8 
//
// Queries a pointer to the pixel data of a pixbuf.
// 
// This function will cause an implicit copy of the pixbuf data if the
// pixbuf was created from read-only data.
// 
// Please see the section on [image data](class.Pixbuf.html#image-data) for information
// about how the pixel data is stored in memory.
func (pixbuf *PixbufInstance) GetPixelsWithLength() (uint, []uint8) {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 C.guint      // out, casted
	var cret  *C.guint8    // return, transfer: none, scope: , implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_pixels_with_length(carg0, &carg1)
	runtime.KeepAlive(pixbuf)

	var length uint
	var ret    []uint8

	length = uint(carg1)
	panic("unimplemented conversion of *typesystem.Array (guint8*)")

	return length, ret
}

// GetRowstride wraps gdk_pixbuf_get_rowstride
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the rowstride of a pixbuf, which is the number of bytes between
// the start of a row and the start of the next row.
func (pixbuf *PixbufInstance) GetRowstride() int {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.int        // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_rowstride(carg0)
	runtime.KeepAlive(pixbuf)

	var ret int

	ret = int(cret)

	return ret
}

// GetWidth wraps gdk_pixbuf_get_width
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the width of a pixbuf.
func (pixbuf *PixbufInstance) GetWidth() int {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  C.int        // return, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_get_width(carg0)
	runtime.KeepAlive(pixbuf)

	var ret int

	ret = int(cret)

	return ret
}

// NewSubpixbuf wraps gdk_pixbuf_new_subpixbuf
// 
// The function takes the following parameters:
// 
// 	- srcX int: X coord in @src_pixbuf 
// 	- srcY int: Y coord in @src_pixbuf 
// 	- width int: width of region in @src_pixbuf 
// 	- height int: height of region in @src_pixbuf 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Creates a new pixbuf which represents a sub-region of `src_pixbuf`.
// 
// The new pixbuf shares its pixels with the original pixbuf, so
// writing to one affects both.  The new pixbuf holds a reference to
// `src_pixbuf`, so `src_pixbuf` will not be finalized until the new
// pixbuf is finalized.
// 
// Note that if `src_pixbuf` is read-only, this function will force it
// to be mutable.
func (srcPixbuf *PixbufInstance) NewSubpixbuf(srcX int, srcY int, width int, height int) Pixbuf {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 C.int        // in, casted
	var carg2 C.int        // in, casted
	var carg3 C.int        // in, casted
	var carg4 C.int        // in, casted
	var cret  *C.GdkPixbuf // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(srcPixbuf))
	carg1 = C.int(srcX)
	carg2 = C.int(srcY)
	carg3 = C.int(width)
	carg4 = C.int(height)

	cret = C.gdk_pixbuf_new_subpixbuf(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(srcPixbuf)
	runtime.KeepAlive(srcX)
	runtime.KeepAlive(srcY)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// ReadPixelBytes wraps gdk_pixbuf_read_pixel_bytes
// The function returns the following values:
// 
// 	- ret *glib.Bytes 
//
// Provides a #GBytes buffer containing the raw pixel data; the data
// must not be modified.
// 
// This function allows skipping the implicit copy that must be made
// if gdk_pixbuf_get_pixels() is called on a read-only pixbuf.
func (pixbuf *PixbufInstance) ReadPixelBytes() *glib.Bytes {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  *C.GBytes    // return, full, record

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_read_pixel_bytes(carg0)
	runtime.KeepAlive(pixbuf)

	var ret *glib.Bytes

	ret = glib.UnsafeBytesFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// ReadPixels wraps gdk_pixbuf_read_pixels
// The function returns the following values:
// 
// 	- ret *uint8 
//
// Provides a read-only pointer to the raw pixel data.
// 
// This function allows skipping the implicit copy that must be made
// if gdk_pixbuf_get_pixels() is called on a read-only pixbuf.
func (pixbuf *PixbufInstance) ReadPixels() *uint8 {
	var carg0 *C.GdkPixbuf // in, none, class
	var cret  *C.guint8    // return, transfer: none, scope: , implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_read_pixels(carg0)
	runtime.KeepAlive(pixbuf)

	var ret *uint8

	panic("unimplemented conversion of *typesystem.PointerType (guint8*)")

	return ret
}

// RemoveOption wraps gdk_pixbuf_remove_option
// 
// The function takes the following parameters:
// 
// 	- key string: a nul-terminated string representing the key to remove. 
// 
// The function returns the following values:
// 
// 	- ret bool 
//
// Removes the key/value pair option attached to a `GdkPixbuf`.
func (pixbuf *PixbufInstance) RemoveOption(key string) bool {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 *C.gchar     // in, none, string
	var cret  C.gboolean   // return

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(key)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_remove_option(carg0, carg1)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(key)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// RotateSimple wraps gdk_pixbuf_rotate_simple
// 
// The function takes the following parameters:
// 
// 	- angle PixbufRotation: the angle to rotate by 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Rotates a pixbuf by a multiple of 90 degrees, and returns the
// result in a new pixbuf.
// 
// If `angle` is 0, this function will return a copy of `src`.
func (src *PixbufInstance) RotateSimple(angle PixbufRotation) Pixbuf {
	var carg0 *C.GdkPixbuf        // in, none, class
	var carg1 C.GdkPixbufRotation // in, casted
	var cret  *C.GdkPixbuf        // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = C.GdkPixbufRotation(angle)

	cret = C.gdk_pixbuf_rotate_simple(carg0, carg1)
	runtime.KeepAlive(src)
	runtime.KeepAlive(angle)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// SaturateAndPixelate wraps gdk_pixbuf_saturate_and_pixelate
// 
// The function takes the following parameters:
// 
// 	- dest Pixbuf: place to write modified version of @src 
// 	- saturation float32: saturation factor 
// 	- pixelate bool: whether to pixelate 
//
// Modifies saturation and optionally pixelates `src`, placing the result in
// `dest`.
// 
// The `src` and `dest` pixbufs must have the same image format, size, and
// rowstride.
// 
// The `src` and `dest` arguments may be the same pixbuf with no ill effects.
// 
// If `saturation` is 1.0 then saturation is not changed. If it's less than 1.0,
// saturation is reduced (the image turns toward grayscale); if greater than
// 1.0, saturation is increased (the image gets more vivid colors).
// 
// If `pixelate` is `TRUE`, then pixels are faded in a checkerboard pattern to
// create a pixelated image.
func (src *PixbufInstance) SaturateAndPixelate(dest Pixbuf, saturation float32, pixelate bool) {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 *C.GdkPixbuf // in, none, class
	var carg2 C.gfloat     // in, casted
	var carg3 C.gboolean   // in

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(dest))
	carg2 = C.gfloat(saturation)
	if pixelate {
		carg3 = C.TRUE
	}

	C.gdk_pixbuf_saturate_and_pixelate(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(src)
	runtime.KeepAlive(dest)
	runtime.KeepAlive(saturation)
	runtime.KeepAlive(pixelate)
}

// SaveToBufferv wraps gdk_pixbuf_save_to_bufferv
// 
// The function takes the following parameters:
// 
// 	- typ string: name of file format. 
// 	- optionKeys []string (nullable): name of options to set 
// 	- optionValues []string (nullable): values for named options 
// 
// The function returns the following values:
// 
// 	- buffer []uint8: 
//   location to receive a pointer to the new buffer. 
// 	- bufferSize uint: location to receive the size of the new buffer. 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Vector version of `gdk_pixbuf_save_to_buffer()`.
// 
// Saves pixbuf to a new buffer in format @type, which is currently "jpeg",
// "tiff", "png", "ico" or "bmp".
// 
// See [method@GdkPixbuf.Pixbuf.save_to_buffer] for more details.
func (pixbuf *PixbufInstance) SaveToBufferv(typ string, optionKeys []string, optionValues []string) ([]uint8, bool, error) {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg3 *C.gchar     // in, none, string
	var carg4 **C.gchar    // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg5 **C.gchar    // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg1 *C.guint8    // out, transfer: full, scope: call, implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false
	var carg2 C.gsize      // out, casted
	var cret  C.gboolean   // return
	var _cerr *C.GError    // out, full, record, nullable

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg3 = (*C.gchar)(unsafe.Pointer(C.CString(typ)))
	defer C.free(unsafe.Pointer(carg3))
	if optionKeys != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if optionValues != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}

	cret = C.gdk_pixbuf_save_to_bufferv(carg0, &carg1, &carg2, carg3, carg4, carg5, &_cerr)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(typ)
	runtime.KeepAlive(optionKeys)
	runtime.KeepAlive(optionValues)

	var buffer     []uint8
	var bufferSize uint
	var ret        bool
	var _goerr     error

	panic("unimplemented conversion of *typesystem.Array (guint8*)")
	bufferSize = uint(carg2)
	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return buffer, ret, _goerr
}

// SaveToCallbackv wraps gdk_pixbuf_save_to_callbackv
// 
// The function takes the following parameters:
// 
// 	- saveFunc PixbufSaveFunc: a function that is called to save each block of data that
//   the save routine generates. 
// 	- typ string: name of file format. 
// 	- optionKeys []string (nullable): name of options to set 
// 	- optionValues []string (nullable): values for named options 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Vector version of `gdk_pixbuf_save_to_callback()`.
// 
// Saves pixbuf to a callback in format @type, which is currently "jpeg",
// "png", "tiff", "ico" or "bmp".
// 
// If @error is set, `FALSE` will be returned.
// 
// See [method@GdkPixbuf.Pixbuf.save_to_callback] for more details.
func (pixbuf *PixbufInstance) SaveToCallbackv(saveFunc PixbufSaveFunc, typ string, optionKeys []string, optionValues []string) (bool, error) {
	var carg0 *C.GdkPixbuf        // in, none, class
	var carg1 C.GdkPixbufSaveFunc // callback, scope: call, closure: carg2
	var carg2 C.gpointer          // implicit
	var carg3 *C.gchar            // in, none, string
	var carg4 **C.gchar           // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg5 **C.gchar           // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var cret  C.gboolean          // return
	var _cerr *C.GError           // out, full, record, nullable

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = (*[0]byte)(C._gotk4_gdkpixbuf2_PixbufSaveFunc)
	carg2 = C.gpointer(gbox.Assign(saveFunc))
	defer gbox.Delete(uintptr(carg2))
	carg3 = (*C.gchar)(unsafe.Pointer(C.CString(typ)))
	defer C.free(unsafe.Pointer(carg3))
	if optionKeys != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if optionValues != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}

	cret = C.gdk_pixbuf_save_to_callbackv(carg0, carg1, carg2, carg3, carg4, carg5, &_cerr)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(saveFunc)
	runtime.KeepAlive(typ)
	runtime.KeepAlive(optionKeys)
	runtime.KeepAlive(optionValues)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// SaveToStreamv wraps gdk_pixbuf_save_to_streamv
// 
// The function takes the following parameters:
// 
// 	- stream gio.OutputStream: a `GOutputStream` to save the pixbuf to 
// 	- typ string: name of file format 
// 	- optionKeys []string (nullable): name of options to set 
// 	- optionValues []string (nullable): values for named options 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Saves `pixbuf` to an output stream.
// 
// Supported file formats are currently "jpeg", "tiff", "png", "ico" or
// "bmp".
// 
// See [method@GdkPixbuf.Pixbuf.save_to_stream] for more details.
func (pixbuf *PixbufInstance) SaveToStreamv(stream gio.OutputStream, typ string, optionKeys []string, optionValues []string, cancellable gio.Cancellable) (bool, error) {
	var carg0 *C.GdkPixbuf     // in, none, class
	var carg1 *C.GOutputStream // in, none, class
	var carg2 *C.gchar         // in, none, string
	var carg3 **C.gchar        // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg4 **C.gchar        // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg5 *C.GCancellable  // in, none, class, nullable
	var cret  C.gboolean       // return
	var _cerr *C.GError        // out, full, record, nullable

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = (*C.GOutputStream)(gio.UnsafeOutputStreamToGlibNone(stream))
	carg2 = (*C.gchar)(unsafe.Pointer(C.CString(typ)))
	defer C.free(unsafe.Pointer(carg2))
	if optionKeys != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if optionValues != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if cancellable != nil {
		carg5 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}

	cret = C.gdk_pixbuf_save_to_streamv(carg0, carg1, carg2, carg3, carg4, carg5, &_cerr)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(typ)
	runtime.KeepAlive(optionKeys)
	runtime.KeepAlive(optionValues)
	runtime.KeepAlive(cancellable)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// SaveToStreamvAsync wraps gdk_pixbuf_save_to_streamv_async
// 
// The function takes the following parameters:
// 
// 	- stream gio.OutputStream: a `GOutputStream` to which to save the pixbuf 
// 	- typ string: name of file format 
// 	- optionKeys []string (nullable): name of options to set 
// 	- optionValues []string (nullable): values for named options 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object, `NULL` to ignore 
// 	- callback gio.AsyncReadyCallback (nullable): a `GAsyncReadyCallback` to call when the pixbuf is saved 
//
// Saves `pixbuf` to an output stream asynchronously.
// 
// For more details see gdk_pixbuf_save_to_streamv(), which is the synchronous
// version of this function.
// 
// When the operation is finished, `callback` will be called in the main thread.
// 
// You can then call gdk_pixbuf_save_to_stream_finish() to get the result of
// the operation.
func (pixbuf *PixbufInstance) SaveToStreamvAsync(stream gio.OutputStream, typ string, optionKeys []string, optionValues []string, cancellable gio.Cancellable, callback gio.AsyncReadyCallback) {
	var carg0 *C.GdkPixbuf          // in, none, class
	var carg1 *C.GOutputStream      // in, none, class
	var carg2 *C.gchar              // in, none, string
	var carg3 **C.gchar             // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg4 **C.gchar             // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg5 *C.GCancellable       // in, none, class, nullable
	var carg6 C.GAsyncReadyCallback // callback, scope: async, closure: carg7, nullable
	var carg7 C.gpointer            // implicit

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = (*C.GOutputStream)(gio.UnsafeOutputStreamToGlibNone(stream))
	carg2 = (*C.gchar)(unsafe.Pointer(C.CString(typ)))
	defer C.free(unsafe.Pointer(carg2))
	if optionKeys != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if optionValues != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if cancellable != nil {
		carg5 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}
	if callback != nil {
		carg6 = (*[0]byte)(C._gotk4_gio2_AsyncReadyCallback)
		carg7 = C.gpointer(gbox.AssignOnce(callback))
	}

	C.gdk_pixbuf_save_to_streamv_async(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(typ)
	runtime.KeepAlive(optionKeys)
	runtime.KeepAlive(optionValues)
	runtime.KeepAlive(cancellable)
	runtime.KeepAlive(callback)
}

// Savev wraps gdk_pixbuf_savev
// 
// The function takes the following parameters:
// 
// 	- filename string: name of file to save. 
// 	- typ string: name of file format. 
// 	- optionKeys []string (nullable): name of options to set 
// 	- optionValues []string (nullable): values for named options 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Vector version of `gdk_pixbuf_save()`.
// 
// Saves pixbuf to a file in `type`, which is currently "jpeg", "png", "tiff", "ico" or "bmp".
// 
// If @error is set, `FALSE` will be returned.
// 
// See [method@GdkPixbuf.Pixbuf.save] for more details.
func (pixbuf *PixbufInstance) Savev(filename string, typ string, optionKeys []string, optionValues []string) (bool, error) {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 *C.gchar     // in, casted
	var carg2 *C.gchar     // in, none, string
	var carg3 **C.gchar    // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var carg4 **C.gchar    // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: true, caller-allocates: false, has closure: false, has destroy: false, nullable
	var cret  C.gboolean   // return
	var _cerr *C.GError    // out, full, record, nullable

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = *C.gchar(filename)
	carg2 = (*C.gchar)(unsafe.Pointer(C.CString(typ)))
	defer C.free(unsafe.Pointer(carg2))
	if optionKeys != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}
	if optionValues != nil {
		panic("unimplemented conversion of *typesystem.Array (gchar**)")
	}

	cret = C.gdk_pixbuf_savev(carg0, carg1, carg2, carg3, carg4, &_cerr)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(filename)
	runtime.KeepAlive(typ)
	runtime.KeepAlive(optionKeys)
	runtime.KeepAlive(optionValues)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// Scale wraps gdk_pixbuf_scale
// 
// The function takes the following parameters:
// 
// 	- dest Pixbuf: the #GdkPixbuf into which to render the results 
// 	- destX int: the left coordinate for region to render 
// 	- destY int: the top coordinate for region to render 
// 	- destWidth int: the width of the region to render 
// 	- destHeight int: the height of the region to render 
// 	- offsetX float64: the offset in the X direction (currently rounded to an integer) 
// 	- offsetY float64: the offset in the Y direction (currently rounded to an integer) 
// 	- scaleX float64: the scale factor in the X direction 
// 	- scaleY float64: the scale factor in the Y direction 
// 	- interpType InterpType: the interpolation type for the transformation. 
//
// Creates a transformation of the source image @src by scaling by
// @scale_x and @scale_y then translating by @offset_x and @offset_y,
// then renders the rectangle (@dest_x, @dest_y, @dest_width,
// @dest_height) of the resulting image onto the destination image
// replacing the previous contents.
// 
// Try to use gdk_pixbuf_scale_simple() first; this function is
// the industrial-strength power tool you can fall back to, if
// gdk_pixbuf_scale_simple() isn't powerful enough.
// 
// If the source rectangle overlaps the destination rectangle on the
// same pixbuf, it will be overwritten during the scaling which
// results in rendering artifacts.
func (src *PixbufInstance) Scale(dest Pixbuf, destX int, destY int, destWidth int, destHeight int, offsetX float64, offsetY float64, scaleX float64, scaleY float64, interpType InterpType) {
	var carg0  *C.GdkPixbuf    // in, none, class
	var carg1  *C.GdkPixbuf    // in, none, class
	var carg2  C.int           // in, casted
	var carg3  C.int           // in, casted
	var carg4  C.int           // in, casted
	var carg5  C.int           // in, casted
	var carg6  C.gdouble       // in, casted
	var carg7  C.gdouble       // in, casted
	var carg8  C.gdouble       // in, casted
	var carg9  C.gdouble       // in, casted
	var carg10 C.GdkInterpType // in, casted

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(dest))
	carg2 = C.int(destX)
	carg3 = C.int(destY)
	carg4 = C.int(destWidth)
	carg5 = C.int(destHeight)
	carg6 = C.gdouble(offsetX)
	carg7 = C.gdouble(offsetY)
	carg8 = C.gdouble(scaleX)
	carg9 = C.gdouble(scaleY)
	carg10 = C.GdkInterpType(interpType)

	C.gdk_pixbuf_scale(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7, carg8, carg9, carg10)
	runtime.KeepAlive(src)
	runtime.KeepAlive(dest)
	runtime.KeepAlive(destX)
	runtime.KeepAlive(destY)
	runtime.KeepAlive(destWidth)
	runtime.KeepAlive(destHeight)
	runtime.KeepAlive(offsetX)
	runtime.KeepAlive(offsetY)
	runtime.KeepAlive(scaleX)
	runtime.KeepAlive(scaleY)
	runtime.KeepAlive(interpType)
}

// ScaleSimple wraps gdk_pixbuf_scale_simple
// 
// The function takes the following parameters:
// 
// 	- destWidth int: the width of destination image 
// 	- destHeight int: the height of destination image 
// 	- interpType InterpType: the interpolation type for the transformation. 
// 
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Create a new pixbuf containing a copy of `src` scaled to
// `dest_width` x `dest_height`.
// 
// This function leaves `src` unaffected.
// 
// The `interp_type` should be `GDK_INTERP_NEAREST` if you want maximum
// speed (but when scaling down `GDK_INTERP_NEAREST` is usually unusably
// ugly). The default `interp_type` should be `GDK_INTERP_BILINEAR` which
// offers reasonable quality and speed.
// 
// You can scale a sub-portion of `src` by creating a sub-pixbuf
// pointing into `src`; see [method@GdkPixbuf.Pixbuf.new_subpixbuf].
// 
// If `dest_width` and `dest_height` are equal to the width and height of
// `src`, this function will return an unscaled copy of `src`.
// 
// For more complicated scaling/alpha blending see [method@GdkPixbuf.Pixbuf.scale]
// and [method@GdkPixbuf.Pixbuf.composite].
func (src *PixbufInstance) ScaleSimple(destWidth int, destHeight int, interpType InterpType) Pixbuf {
	var carg0 *C.GdkPixbuf    // in, none, class
	var carg1 C.int           // in, casted
	var carg2 C.int           // in, casted
	var carg3 C.GdkInterpType // in, casted
	var cret  *C.GdkPixbuf    // return, full, class

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(src))
	carg1 = C.int(destWidth)
	carg2 = C.int(destHeight)
	carg3 = C.GdkInterpType(interpType)

	cret = C.gdk_pixbuf_scale_simple(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(src)
	runtime.KeepAlive(destWidth)
	runtime.KeepAlive(destHeight)
	runtime.KeepAlive(interpType)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// SetOption wraps gdk_pixbuf_set_option
// 
// The function takes the following parameters:
// 
// 	- key string: a nul-terminated string. 
// 	- value string: a nul-terminated string. 
// 
// The function returns the following values:
// 
// 	- ret bool 
//
// Attaches a key/value pair as an option to a `GdkPixbuf`.
// 
// If `key` already exists in the list of options attached to the `pixbuf`,
// the new value is ignored and `FALSE` is returned.
func (pixbuf *PixbufInstance) SetOption(key string, value string) bool {
	var carg0 *C.GdkPixbuf // in, none, class
	var carg1 *C.gchar     // in, none, string
	var carg2 *C.gchar     // in, none, string
	var cret  C.gboolean   // return

	carg0 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))
	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(key)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = (*C.gchar)(unsafe.Pointer(C.CString(value)))
	defer C.free(unsafe.Pointer(carg2))

	cret = C.gdk_pixbuf_set_option(carg0, carg1, carg2)
	runtime.KeepAlive(pixbuf)
	runtime.KeepAlive(key)
	runtime.KeepAlive(value)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// PixbufAnimationInstance is the instance type used by all types extending GdkPixbufAnimation. It is used internally by the bindings. Users should use the interface [PixbufAnimation] instead.
type PixbufAnimationInstance struct {
	_ [0]func() // equal guard
	gobject.Object
}

var _ PixbufAnimation = (*PixbufAnimationInstance)(nil)

// PixbufAnimationInstance wraps GdkPixbufAnimation
//
// An opaque object representing an animation.
// 
// The GdkPixBuf library provides a simple mechanism to load and
// represent animations. An animation is conceptually a series of
// frames to be displayed over time.
// 
// The animation may not be represented as a series of frames
// internally; for example, it may be stored as a sprite and
// instructions for moving the sprite around a background.
// 
// To display an animation you don't need to understand its
// representation, however; you just ask `GdkPixbuf` what should
// be displayed at a given point in time.
type PixbufAnimation interface {
	gobject.ObjectLike
	upcastToGdkPixbufAnimation() *PixbufAnimationInstance

	// GetHeight wraps gdk_pixbuf_animation_get_height
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the height of the bounding box of a pixbuf animation.
	GetHeight() int
	// GetStaticImage wraps gdk_pixbuf_animation_get_static_image
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Retrieves a static image for the animation.
	// 
	// If an animation is really just a plain image (has only one frame),
	// this function returns that image.
	// 
	// If the animation is an animation, this function returns a reasonable
	// image to use as a static unanimated image, which might be the first
	// frame, or something more sophisticated depending on the file format.
	// 
	// If an animation hasn't loaded any frames yet, this function will
	// return `NULL`.
	GetStaticImage() Pixbuf
	// GetWidth wraps gdk_pixbuf_animation_get_width
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Queries the width of the bounding box of a pixbuf animation.
	GetWidth() int
	// IsStaticImage wraps gdk_pixbuf_animation_is_static_image
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Checks whether the animation is a static image.
	// 
	// If you load a file with gdk_pixbuf_animation_new_from_file() and it
	// turns out to be a plain, unanimated image, then this function will
	// return `TRUE`. Use gdk_pixbuf_animation_get_static_image() to retrieve
	// the image.
	IsStaticImage() bool
}

func unsafeWrapPixbufAnimation(base *gobject.Object) *PixbufAnimationInstance {
	return &PixbufAnimationInstance{
		Object: *base,
	}
}

func marshalPixbufAnimationInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbufAnimation(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufAnimationFromGlibBorrow is used to convert raw GdkPixbufAnimation pointers to go. This is used by the bindings internally.
func UnsafePixbufAnimationFromGlibBorrow(c unsafe.Pointer) PixbufAnimation {
	return gobject.TODOBorrow(c).(PixbufAnimation)
}

// UnsafePixbufAnimationFromGlibNone is used to convert raw GdkPixbufAnimation pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufAnimationFromGlibNone(c unsafe.Pointer) PixbufAnimation {
	return gobject.Take(c).(PixbufAnimation)
}

// UnsafePixbufAnimationFromGlibFull is used to convert raw GdkPixbufAnimation pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufAnimationFromGlibFull(c unsafe.Pointer) PixbufAnimation {
	return gobject.AssumeOwnership(c).(PixbufAnimation)
}

func (p *PixbufAnimationInstance) upcastToGdkPixbufAnimation() *PixbufAnimationInstance {
	return p
}

// UnsafePixbufAnimationToGlibNone is used to convert the instance to it's C value GdkPixbufAnimation. This is used by the bindings internally.
func UnsafePixbufAnimationToGlibNone(c PixbufAnimation) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufAnimationToGlibFull is used to convert the instance to it's C value GdkPixbufAnimation, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufAnimationToGlibFull(c PixbufAnimation) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// NewPixbufAnimationInstanceFromFile wraps gdk_pixbuf_animation_new_from_file
// 
// The function takes the following parameters:
// 
// 	- filename string: Name of file to load, in the GLib file
//   name encoding 
// 
// The function returns the following values:
// 
// 	- ret PixbufAnimation 
// 	- _goerr error (nullable): an error 
//
// Creates a new animation by loading it from a file.
// 
// The file format is detected automatically.
// 
// If the file's format does not support multi-frame images, then an animation
// with a single frame will be created.
// 
// Possible errors are in the `GDK_PIXBUF_ERROR` and `G_FILE_ERROR` domains.
func NewPixbufAnimationInstanceFromFile(filename string) (PixbufAnimation, error) {
	var carg1 *C.gchar              // in, casted
	var cret  *C.GdkPixbufAnimation // return, full, class
	var _cerr *C.GError             // out, full, record, nullable

	carg1 = *C.gchar(filename)

	cret = C.gdk_pixbuf_animation_new_from_file(carg1, &_cerr)
	runtime.KeepAlive(filename)

	var ret    PixbufAnimation
	var _goerr error

	ret = UnsafePixbufAnimationFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufAnimationInstanceFromResource wraps gdk_pixbuf_animation_new_from_resource
// 
// The function takes the following parameters:
// 
// 	- resourcePath string: the path of the resource file 
// 
// The function returns the following values:
// 
// 	- ret PixbufAnimation 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf animation by loading an image from an resource.
// 
// The file format is detected automatically. If `NULL` is returned, then
// @error will be set.
func NewPixbufAnimationInstanceFromResource(resourcePath string) (PixbufAnimation, error) {
	var carg1 *C.gchar              // in, none, string
	var cret  *C.GdkPixbufAnimation // return, full, class
	var _cerr *C.GError             // out, full, record, nullable

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(resourcePath)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_animation_new_from_resource(carg1, &_cerr)
	runtime.KeepAlive(resourcePath)

	var ret    PixbufAnimation
	var _goerr error

	ret = UnsafePixbufAnimationFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufAnimationInstanceFromStream wraps gdk_pixbuf_animation_new_from_stream
// 
// The function takes the following parameters:
// 
// 	- stream gio.InputStream: a `GInputStream` to load the pixbuf from 
// 	- cancellable gio.Cancellable (nullable): optional `GCancellable` object 
// 
// The function returns the following values:
// 
// 	- ret PixbufAnimation 
// 	- _goerr error (nullable): an error 
//
// Creates a new animation by loading it from an input stream.
// 
// The file format is detected automatically.
// 
// If `NULL` is returned, then @error will be set.
// 
// The @cancellable can be used to abort the operation from another thread.
// If the operation was cancelled, the error `G_IO_ERROR_CANCELLED` will be
// returned. Other possible errors are in the `GDK_PIXBUF_ERROR` and
// `G_IO_ERROR` domains.
// 
// The stream is not closed.
func NewPixbufAnimationInstanceFromStream(stream gio.InputStream, cancellable gio.Cancellable) (PixbufAnimation, error) {
	var carg1 *C.GInputStream       // in, none, class
	var carg2 *C.GCancellable       // in, none, class, nullable
	var cret  *C.GdkPixbufAnimation // return, full, class
	var _cerr *C.GError             // out, full, record, nullable

	carg1 = (*C.GInputStream)(gio.UnsafeInputStreamToGlibNone(stream))
	if cancellable != nil {
		carg2 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}

	cret = C.gdk_pixbuf_animation_new_from_stream(carg1, carg2, &_cerr)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(cancellable)

	var ret    PixbufAnimation
	var _goerr error

	ret = UnsafePixbufAnimationFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufAnimationInstanceFromStreamFinish wraps gdk_pixbuf_animation_new_from_stream_finish
// 
// The function takes the following parameters:
// 
// 	- asyncResult gio.AsyncResult: a #GAsyncResult 
// 
// The function returns the following values:
// 
// 	- ret PixbufAnimation 
// 	- _goerr error (nullable): an error 
//
// Finishes an asynchronous pixbuf animation creation operation started with
// [func@GdkPixbuf.PixbufAnimation.new_from_stream_async].
func NewPixbufAnimationInstanceFromStreamFinish(asyncResult gio.AsyncResult) (PixbufAnimation, error) {
	var carg1 *C.GAsyncResult       // in, none, interface
	var cret  *C.GdkPixbufAnimation // return, full, class
	var _cerr *C.GError             // out, full, record, nullable

	carg1 = (*C.GAsyncResult)(gio.UnsafeAsyncResultToGlibNone(asyncResult))

	cret = C.gdk_pixbuf_animation_new_from_stream_finish(carg1, &_cerr)
	runtime.KeepAlive(asyncResult)

	var ret    PixbufAnimation
	var _goerr error

	ret = UnsafePixbufAnimationFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewFromStreamAsync wraps gdk_pixbuf_animation_new_from_stream_async
// 
// The function takes the following parameters:
// 
// 	- stream gio.InputStream: a #GInputStream from which to load the animation 
// 	- cancellable gio.Cancellable (nullable): optional #GCancellable object 
// 	- callback gio.AsyncReadyCallback (nullable): a `GAsyncReadyCallback` to call when the pixbuf is loaded 
//
// Creates a new animation by asynchronously loading an image from an input stream.
// 
// For more details see gdk_pixbuf_new_from_stream(), which is the synchronous
// version of this function.
// 
// When the operation is finished, `callback` will be called in the main thread.
// You can then call gdk_pixbuf_animation_new_from_stream_finish() to get the
// result of the operation.
func NewFromStreamAsync(stream gio.InputStream, cancellable gio.Cancellable, callback gio.AsyncReadyCallback) {
	var carg1 *C.GInputStream       // in, none, class
	var carg2 *C.GCancellable       // in, none, class, nullable
	var carg3 C.GAsyncReadyCallback // callback, scope: async, closure: carg4, nullable
	var carg4 C.gpointer            // implicit

	carg1 = (*C.GInputStream)(gio.UnsafeInputStreamToGlibNone(stream))
	if cancellable != nil {
		carg2 = (*C.GCancellable)(gio.UnsafeCancellableToGlibNone(cancellable))
	}
	if callback != nil {
		carg3 = (*[0]byte)(C._gotk4_gio2_AsyncReadyCallback)
		carg4 = C.gpointer(gbox.AssignOnce(callback))
	}

	C.gdk_pixbuf_animation_new_from_stream_async(carg1, carg2, carg3, carg4)
	runtime.KeepAlive(stream)
	runtime.KeepAlive(cancellable)
	runtime.KeepAlive(callback)
}

// GetHeight wraps gdk_pixbuf_animation_get_height
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the height of the bounding box of a pixbuf animation.
func (animation *PixbufAnimationInstance) GetHeight() int {
	var carg0 *C.GdkPixbufAnimation // in, none, class
	var cret  C.int                 // return, casted

	carg0 = (*C.GdkPixbufAnimation)(UnsafePixbufAnimationToGlibNone(animation))

	cret = C.gdk_pixbuf_animation_get_height(carg0)
	runtime.KeepAlive(animation)

	var ret int

	ret = int(cret)

	return ret
}

// GetStaticImage wraps gdk_pixbuf_animation_get_static_image
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Retrieves a static image for the animation.
// 
// If an animation is really just a plain image (has only one frame),
// this function returns that image.
// 
// If the animation is an animation, this function returns a reasonable
// image to use as a static unanimated image, which might be the first
// frame, or something more sophisticated depending on the file format.
// 
// If an animation hasn't loaded any frames yet, this function will
// return `NULL`.
func (animation *PixbufAnimationInstance) GetStaticImage() Pixbuf {
	var carg0 *C.GdkPixbufAnimation // in, none, class
	var cret  *C.GdkPixbuf          // return, none, class

	carg0 = (*C.GdkPixbufAnimation)(UnsafePixbufAnimationToGlibNone(animation))

	cret = C.gdk_pixbuf_animation_get_static_image(carg0)
	runtime.KeepAlive(animation)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibNone(unsafe.Pointer(cret))

	return ret
}

// GetWidth wraps gdk_pixbuf_animation_get_width
// The function returns the following values:
// 
// 	- ret int 
//
// Queries the width of the bounding box of a pixbuf animation.
func (animation *PixbufAnimationInstance) GetWidth() int {
	var carg0 *C.GdkPixbufAnimation // in, none, class
	var cret  C.int                 // return, casted

	carg0 = (*C.GdkPixbufAnimation)(UnsafePixbufAnimationToGlibNone(animation))

	cret = C.gdk_pixbuf_animation_get_width(carg0)
	runtime.KeepAlive(animation)

	var ret int

	ret = int(cret)

	return ret
}

// IsStaticImage wraps gdk_pixbuf_animation_is_static_image
// The function returns the following values:
// 
// 	- ret bool 
//
// Checks whether the animation is a static image.
// 
// If you load a file with gdk_pixbuf_animation_new_from_file() and it
// turns out to be a plain, unanimated image, then this function will
// return `TRUE`. Use gdk_pixbuf_animation_get_static_image() to retrieve
// the image.
func (animation *PixbufAnimationInstance) IsStaticImage() bool {
	var carg0 *C.GdkPixbufAnimation // in, none, class
	var cret  C.gboolean            // return

	carg0 = (*C.GdkPixbufAnimation)(UnsafePixbufAnimationToGlibNone(animation))

	cret = C.gdk_pixbuf_animation_is_static_image(carg0)
	runtime.KeepAlive(animation)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// PixbufAnimationIterInstance is the instance type used by all types extending GdkPixbufAnimationIter. It is used internally by the bindings. Users should use the interface [PixbufAnimationIter] instead.
type PixbufAnimationIterInstance struct {
	_ [0]func() // equal guard
	gobject.Object
}

var _ PixbufAnimationIter = (*PixbufAnimationIterInstance)(nil)

// PixbufAnimationIterInstance wraps GdkPixbufAnimationIter
//
// An opaque object representing an iterator which points to a
// certain position in an animation.
type PixbufAnimationIter interface {
	gobject.ObjectLike
	upcastToGdkPixbufAnimationIter() *PixbufAnimationIterInstance

	// GetDelayTime wraps gdk_pixbuf_animation_iter_get_delay_time
	// The function returns the following values:
	// 
	// 	- ret int 
	//
	// Gets the number of milliseconds the current pixbuf should be displayed,
	// or -1 if the current pixbuf should be displayed forever.
	// 
	// The `g_timeout_add()` function conveniently takes a timeout in milliseconds,
	// so you can use a timeout to schedule the next update.
	// 
	// Note that some formats, like GIF, might clamp the timeout values in the
	// image file to avoid updates that are just too quick. The minimum timeout
	// for GIF images is currently 20 milliseconds.
	GetDelayTime() int
	// GetPixbuf wraps gdk_pixbuf_animation_iter_get_pixbuf
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Gets the current pixbuf which should be displayed.
	// 
	// The pixbuf might not be the same size as the animation itself
	// (gdk_pixbuf_animation_get_width(), gdk_pixbuf_animation_get_height()).
	// 
	// This pixbuf should be displayed for gdk_pixbuf_animation_iter_get_delay_time()
	// milliseconds.
	// 
	// The caller of this function does not own a reference to the returned
	// pixbuf; the returned pixbuf will become invalid when the iterator
	// advances to the next frame, which may happen anytime you call
	// gdk_pixbuf_animation_iter_advance().
	// 
	// Copy the pixbuf to keep it (don't just add a reference), as it may get
	// recycled as you advance the iterator.
	GetPixbuf() Pixbuf
	// OnCurrentlyLoadingFrame wraps gdk_pixbuf_animation_iter_on_currently_loading_frame
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Used to determine how to respond to the area_updated signal on
	// #GdkPixbufLoader when loading an animation.
	// 
	// The `::area_updated` signal is emitted for an area of the frame currently
	// streaming in to the loader. So if you're on the currently loading frame,
	// you will need to redraw the screen for the updated area.
	OnCurrentlyLoadingFrame() bool
}

func unsafeWrapPixbufAnimationIter(base *gobject.Object) *PixbufAnimationIterInstance {
	return &PixbufAnimationIterInstance{
		Object: *base,
	}
}

func marshalPixbufAnimationIterInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbufAnimationIter(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufAnimationIterFromGlibBorrow is used to convert raw GdkPixbufAnimationIter pointers to go. This is used by the bindings internally.
func UnsafePixbufAnimationIterFromGlibBorrow(c unsafe.Pointer) PixbufAnimationIter {
	return gobject.TODOBorrow(c).(PixbufAnimationIter)
}

// UnsafePixbufAnimationIterFromGlibNone is used to convert raw GdkPixbufAnimationIter pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufAnimationIterFromGlibNone(c unsafe.Pointer) PixbufAnimationIter {
	return gobject.Take(c).(PixbufAnimationIter)
}

// UnsafePixbufAnimationIterFromGlibFull is used to convert raw GdkPixbufAnimationIter pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufAnimationIterFromGlibFull(c unsafe.Pointer) PixbufAnimationIter {
	return gobject.AssumeOwnership(c).(PixbufAnimationIter)
}

func (p *PixbufAnimationIterInstance) upcastToGdkPixbufAnimationIter() *PixbufAnimationIterInstance {
	return p
}

// UnsafePixbufAnimationIterToGlibNone is used to convert the instance to it's C value GdkPixbufAnimationIter. This is used by the bindings internally.
func UnsafePixbufAnimationIterToGlibNone(c PixbufAnimationIter) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufAnimationIterToGlibFull is used to convert the instance to it's C value GdkPixbufAnimationIter, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufAnimationIterToGlibFull(c PixbufAnimationIter) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// GetDelayTime wraps gdk_pixbuf_animation_iter_get_delay_time
// The function returns the following values:
// 
// 	- ret int 
//
// Gets the number of milliseconds the current pixbuf should be displayed,
// or -1 if the current pixbuf should be displayed forever.
// 
// The `g_timeout_add()` function conveniently takes a timeout in milliseconds,
// so you can use a timeout to schedule the next update.
// 
// Note that some formats, like GIF, might clamp the timeout values in the
// image file to avoid updates that are just too quick. The minimum timeout
// for GIF images is currently 20 milliseconds.
func (iter *PixbufAnimationIterInstance) GetDelayTime() int {
	var carg0 *C.GdkPixbufAnimationIter // in, none, class
	var cret  C.int                     // return, casted

	carg0 = (*C.GdkPixbufAnimationIter)(UnsafePixbufAnimationIterToGlibNone(iter))

	cret = C.gdk_pixbuf_animation_iter_get_delay_time(carg0)
	runtime.KeepAlive(iter)

	var ret int

	ret = int(cret)

	return ret
}

// GetPixbuf wraps gdk_pixbuf_animation_iter_get_pixbuf
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Gets the current pixbuf which should be displayed.
// 
// The pixbuf might not be the same size as the animation itself
// (gdk_pixbuf_animation_get_width(), gdk_pixbuf_animation_get_height()).
// 
// This pixbuf should be displayed for gdk_pixbuf_animation_iter_get_delay_time()
// milliseconds.
// 
// The caller of this function does not own a reference to the returned
// pixbuf; the returned pixbuf will become invalid when the iterator
// advances to the next frame, which may happen anytime you call
// gdk_pixbuf_animation_iter_advance().
// 
// Copy the pixbuf to keep it (don't just add a reference), as it may get
// recycled as you advance the iterator.
func (iter *PixbufAnimationIterInstance) GetPixbuf() Pixbuf {
	var carg0 *C.GdkPixbufAnimationIter // in, none, class
	var cret  *C.GdkPixbuf              // return, none, class

	carg0 = (*C.GdkPixbufAnimationIter)(UnsafePixbufAnimationIterToGlibNone(iter))

	cret = C.gdk_pixbuf_animation_iter_get_pixbuf(carg0)
	runtime.KeepAlive(iter)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibNone(unsafe.Pointer(cret))

	return ret
}

// OnCurrentlyLoadingFrame wraps gdk_pixbuf_animation_iter_on_currently_loading_frame
// The function returns the following values:
// 
// 	- ret bool 
//
// Used to determine how to respond to the area_updated signal on
// #GdkPixbufLoader when loading an animation.
// 
// The `::area_updated` signal is emitted for an area of the frame currently
// streaming in to the loader. So if you're on the currently loading frame,
// you will need to redraw the screen for the updated area.
func (iter *PixbufAnimationIterInstance) OnCurrentlyLoadingFrame() bool {
	var carg0 *C.GdkPixbufAnimationIter // in, none, class
	var cret  C.gboolean                // return

	carg0 = (*C.GdkPixbufAnimationIter)(UnsafePixbufAnimationIterToGlibNone(iter))

	cret = C.gdk_pixbuf_animation_iter_on_currently_loading_frame(carg0)
	runtime.KeepAlive(iter)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// PixbufLoaderInstance is the instance type used by all types extending GdkPixbufLoader. It is used internally by the bindings. Users should use the interface [PixbufLoader] instead.
type PixbufLoaderInstance struct {
	_ [0]func() // equal guard
	gobject.Object
}

var _ PixbufLoader = (*PixbufLoaderInstance)(nil)

// PixbufLoaderInstance wraps GdkPixbufLoader
//
// Incremental image loader.
// 
// `GdkPixbufLoader` provides a way for applications to drive the
// process of loading an image, by letting them send the image data
// directly to the loader instead of having the loader read the data
// from a file. Applications can use this functionality instead of
// `gdk_pixbuf_new_from_file()` or `gdk_pixbuf_animation_new_from_file()`
// when they need to parse image data in small chunks. For example,
// it should be used when reading an image from a (potentially) slow
// network connection, or when loading an extremely large file.
// 
// To use `GdkPixbufLoader` to load an image, create a new instance,
// and call [method@GdkPixbuf.PixbufLoader.write] to send the data
// to it. When done, [method@GdkPixbuf.PixbufLoader.close] should be
// called to end the stream and finalize everything.
// 
// The loader will emit three important signals throughout the process:
// 
//  - [signal@GdkPixbuf.PixbufLoader::size-prepared] will be emitted as
//    soon as the image has enough information to determine the size of
//    the image to be used. If you want to scale the image while loading
//    it, you can call [method@GdkPixbuf.PixbufLoader.set_size] in
//    response to this signal.
//  - [signal@GdkPixbuf.PixbufLoader::area-prepared] will be emitted as
//    soon as the pixbuf of the desired has been allocated. You can obtain
//    the `GdkPixbuf` instance by calling [method@GdkPixbuf.PixbufLoader.get_pixbuf].
//    If you want to use it, simply acquire a reference to it. You can
//    also call `gdk_pixbuf_loader_get_pixbuf()` later to get the same
//    pixbuf.
//  - [signal@GdkPixbuf.PixbufLoader::area-updated] will be emitted every
//    time a region is updated. This way you can update a partially
//    completed image. Note that you do not know anything about the
//    completeness of an image from the updated area. For example, in an
//    interlaced image you will need to make several passes before the
//    image is done loading.
// 
// ## Loading an animation
// 
// Loading an animation is almost as easy as loading an image. Once the
// first [signal@GdkPixbuf.PixbufLoader::area-prepared] signal has been
// emitted, you can call [method@GdkPixbuf.PixbufLoader.get_animation] to
// get the [class@GdkPixbuf.PixbufAnimation] instance, and then call
// and [method@GdkPixbuf.PixbufAnimation.get_iter] to get a
// [class@GdkPixbuf.PixbufAnimationIter] to retrieve the pixbuf for the
// desired time stamp.
type PixbufLoader interface {
	gobject.ObjectLike
	upcastToGdkPixbufLoader() *PixbufLoaderInstance

	// Close wraps gdk_pixbuf_loader_close
	// The function returns the following values:
	// 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Informs a pixbuf loader that no further writes with
	// gdk_pixbuf_loader_write() will occur, so that it can free its
	// internal loading structures.
	// 
	// This function also tries to parse any data that hasn't yet been parsed;
	// if the remaining data is partial or corrupt, an error will be returned.
	// 
	// If `FALSE` is returned, `error` will be set to an error from the
	// `GDK_PIXBUF_ERROR` or `G_FILE_ERROR` domains.
	// 
	// If you're just cancelling a load rather than expecting it to be finished,
	// passing `NULL` for `error` to ignore it is reasonable.
	// 
	// Remember that this function does not release a reference on the loader, so
	// you will need to explicitly release any reference you hold.
	Close() (bool, error)
	// GetAnimation wraps gdk_pixbuf_loader_get_animation
	// The function returns the following values:
	// 
	// 	- ret PixbufAnimation 
	//
	// Queries the #GdkPixbufAnimation that a pixbuf loader is currently creating.
	// 
	// In general it only makes sense to call this function after the
	// [signal@GdkPixbuf.PixbufLoader::area-prepared] signal has been emitted by
	// the loader.
	// 
	// If the loader doesn't have enough bytes yet, and hasn't emitted the `area-prepared`
	// signal, this function will return `NULL`.
	GetAnimation() PixbufAnimation
	// GetFormat wraps gdk_pixbuf_loader_get_format
	// The function returns the following values:
	// 
	// 	- ret *PixbufFormat 
	//
	// Obtains the available information about the format of the
	// currently loading image file.
	GetFormat() *PixbufFormat
	// GetPixbuf wraps gdk_pixbuf_loader_get_pixbuf
	// The function returns the following values:
	// 
	// 	- ret Pixbuf 
	//
	// Queries the #GdkPixbuf that a pixbuf loader is currently creating.
	// 
	// In general it only makes sense to call this function after the
	// [signal@GdkPixbuf.PixbufLoader::area-prepared] signal has been
	// emitted by the loader; this means that enough data has been read
	// to know the size of the image that will be allocated.
	// 
	// If the loader has not received enough data via gdk_pixbuf_loader_write(),
	// then this function returns `NULL`.
	// 
	// The returned pixbuf will be the same in all future calls to the loader,
	// so if you want to keep using it, you should acquire a reference to it.
	// 
	// Additionally, if the loader is an animation, it will return the "static
	// image" of the animation (see gdk_pixbuf_animation_get_static_image()).
	GetPixbuf() Pixbuf
	// SetSize wraps gdk_pixbuf_loader_set_size
	// 
	// The function takes the following parameters:
	// 
	// 	- width int: The desired width of the image being loaded. 
	// 	- height int: The desired height of the image being loaded. 
	//
	// Causes the image to be scaled while it is loaded.
	// 
	// The desired image size can be determined relative to the original
	// size of the image by calling gdk_pixbuf_loader_set_size() from a
	// signal handler for the ::size-prepared signal.
	// 
	// Attempts to set the desired image size  are ignored after the
	// emission of the ::size-prepared signal.
	SetSize(int, int)
	// Write wraps gdk_pixbuf_loader_write
	// 
	// The function takes the following parameters:
	// 
	// 	- buf []uint8: Pointer to image data. 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Parses the next `count` bytes in the given image buffer.
	Write([]uint8) (bool, error)
	// WriteBytes wraps gdk_pixbuf_loader_write_bytes
	// 
	// The function takes the following parameters:
	// 
	// 	- buffer *glib.Bytes: The image data as a `GBytes` buffer. 
	// 
	// The function returns the following values:
	// 
	// 	- ret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Parses the next contents of the given image buffer.
	WriteBytes(*glib.Bytes) (bool, error)
}

func unsafeWrapPixbufLoader(base *gobject.Object) *PixbufLoaderInstance {
	return &PixbufLoaderInstance{
		Object: *base,
	}
}

func marshalPixbufLoaderInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbufLoader(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufLoaderFromGlibBorrow is used to convert raw GdkPixbufLoader pointers to go. This is used by the bindings internally.
func UnsafePixbufLoaderFromGlibBorrow(c unsafe.Pointer) PixbufLoader {
	return gobject.TODOBorrow(c).(PixbufLoader)
}

// UnsafePixbufLoaderFromGlibNone is used to convert raw GdkPixbufLoader pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufLoaderFromGlibNone(c unsafe.Pointer) PixbufLoader {
	return gobject.Take(c).(PixbufLoader)
}

// UnsafePixbufLoaderFromGlibFull is used to convert raw GdkPixbufLoader pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufLoaderFromGlibFull(c unsafe.Pointer) PixbufLoader {
	return gobject.AssumeOwnership(c).(PixbufLoader)
}

func (p *PixbufLoaderInstance) upcastToGdkPixbufLoader() *PixbufLoaderInstance {
	return p
}

// UnsafePixbufLoaderToGlibNone is used to convert the instance to it's C value GdkPixbufLoader. This is used by the bindings internally.
func UnsafePixbufLoaderToGlibNone(c PixbufLoader) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufLoaderToGlibFull is used to convert the instance to it's C value GdkPixbufLoader, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufLoaderToGlibFull(c PixbufLoader) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// NewPixbufLoaderInstance wraps gdk_pixbuf_loader_new
// The function returns the following values:
// 
// 	- ret PixbufLoader 
//
// Creates a new pixbuf loader object.
func NewPixbufLoaderInstance() PixbufLoader {
	var cret *C.GdkPixbufLoader // return, full, class

	cret = C.gdk_pixbuf_loader_new()

	var ret PixbufLoader

	ret = UnsafePixbufLoaderFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// NewPixbufLoaderInstanceWithMIMEType wraps gdk_pixbuf_loader_new_with_mime_type
// 
// The function takes the following parameters:
// 
// 	- mimeType string: the mime type to be loaded 
// 
// The function returns the following values:
// 
// 	- ret PixbufLoader 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf loader object that always attempts to parse
// image data as if it were an image of MIME type @mime_type, instead of
// identifying the type automatically.
// 
// This function is useful if you want an error if the image isn't the
// expected MIME type; for loading image formats that can't be reliably
// identified by looking at the data; or if the user manually forces a
// specific MIME type.
// 
// The list of supported mime types depends on what image loaders
// are installed, but typically "image/png", "image/jpeg", "image/gif",
// "image/tiff" and "image/x-xpixmap" are among the supported mime types.
// To obtain the full list of supported mime types, call
// gdk_pixbuf_format_get_mime_types() on each of the #GdkPixbufFormat
// structs returned by gdk_pixbuf_get_formats().
func NewPixbufLoaderInstanceWithMIMEType(mimeType string) (PixbufLoader, error) {
	var carg1 *C.gchar           // in, none, string
	var cret  *C.GdkPixbufLoader // return, full, class
	var _cerr *C.GError          // out, full, record, nullable

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(mimeType)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_loader_new_with_mime_type(carg1, &_cerr)
	runtime.KeepAlive(mimeType)

	var ret    PixbufLoader
	var _goerr error

	ret = UnsafePixbufLoaderFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// NewPixbufLoaderInstanceWithType wraps gdk_pixbuf_loader_new_with_type
// 
// The function takes the following parameters:
// 
// 	- imageType string: name of the image format to be loaded with the image 
// 
// The function returns the following values:
// 
// 	- ret PixbufLoader 
// 	- _goerr error (nullable): an error 
//
// Creates a new pixbuf loader object that always attempts to parse
// image data as if it were an image of type @image_type, instead of
// identifying the type automatically.
// 
// This function is useful if you want an error if the image isn't the
// expected type; for loading image formats that can't be reliably
// identified by looking at the data; or if the user manually forces
// a specific type.
// 
// The list of supported image formats depends on what image loaders
// are installed, but typically "png", "jpeg", "gif", "tiff" and
// "xpm" are among the supported formats. To obtain the full list of
// supported image formats, call gdk_pixbuf_format_get_name() on each
// of the #GdkPixbufFormat structs returned by gdk_pixbuf_get_formats().
func NewPixbufLoaderInstanceWithType(imageType string) (PixbufLoader, error) {
	var carg1 *C.gchar           // in, none, string
	var cret  *C.GdkPixbufLoader // return, full, class
	var _cerr *C.GError          // out, full, record, nullable

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(imageType)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_loader_new_with_type(carg1, &_cerr)
	runtime.KeepAlive(imageType)

	var ret    PixbufLoader
	var _goerr error

	ret = UnsafePixbufLoaderFromGlibFull(unsafe.Pointer(cret))
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// Close wraps gdk_pixbuf_loader_close
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Informs a pixbuf loader that no further writes with
// gdk_pixbuf_loader_write() will occur, so that it can free its
// internal loading structures.
// 
// This function also tries to parse any data that hasn't yet been parsed;
// if the remaining data is partial or corrupt, an error will be returned.
// 
// If `FALSE` is returned, `error` will be set to an error from the
// `GDK_PIXBUF_ERROR` or `G_FILE_ERROR` domains.
// 
// If you're just cancelling a load rather than expecting it to be finished,
// passing `NULL` for `error` to ignore it is reasonable.
// 
// Remember that this function does not release a reference on the loader, so
// you will need to explicitly release any reference you hold.
func (loader *PixbufLoaderInstance) Close() (bool, error) {
	var carg0 *C.GdkPixbufLoader // in, none, class
	var cret  C.gboolean         // return
	var _cerr *C.GError          // out, full, record, nullable

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))

	cret = C.gdk_pixbuf_loader_close(carg0, &_cerr)
	runtime.KeepAlive(loader)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// GetAnimation wraps gdk_pixbuf_loader_get_animation
// The function returns the following values:
// 
// 	- ret PixbufAnimation 
//
// Queries the #GdkPixbufAnimation that a pixbuf loader is currently creating.
// 
// In general it only makes sense to call this function after the
// [signal@GdkPixbuf.PixbufLoader::area-prepared] signal has been emitted by
// the loader.
// 
// If the loader doesn't have enough bytes yet, and hasn't emitted the `area-prepared`
// signal, this function will return `NULL`.
func (loader *PixbufLoaderInstance) GetAnimation() PixbufAnimation {
	var carg0 *C.GdkPixbufLoader    // in, none, class
	var cret  *C.GdkPixbufAnimation // return, none, class

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))

	cret = C.gdk_pixbuf_loader_get_animation(carg0)
	runtime.KeepAlive(loader)

	var ret PixbufAnimation

	ret = UnsafePixbufAnimationFromGlibNone(unsafe.Pointer(cret))

	return ret
}

// GetFormat wraps gdk_pixbuf_loader_get_format
// The function returns the following values:
// 
// 	- ret *PixbufFormat 
//
// Obtains the available information about the format of the
// currently loading image file.
func (loader *PixbufLoaderInstance) GetFormat() *PixbufFormat {
	var carg0 *C.GdkPixbufLoader // in, none, class
	var cret  *C.GdkPixbufFormat // return, none, record

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))

	cret = C.gdk_pixbuf_loader_get_format(carg0)
	runtime.KeepAlive(loader)

	var ret *PixbufFormat

	ret = UnsafePixbufFormatFromGlibNone(unsafe.Pointer(cret))

	return ret
}

// GetPixbuf wraps gdk_pixbuf_loader_get_pixbuf
// The function returns the following values:
// 
// 	- ret Pixbuf 
//
// Queries the #GdkPixbuf that a pixbuf loader is currently creating.
// 
// In general it only makes sense to call this function after the
// [signal@GdkPixbuf.PixbufLoader::area-prepared] signal has been
// emitted by the loader; this means that enough data has been read
// to know the size of the image that will be allocated.
// 
// If the loader has not received enough data via gdk_pixbuf_loader_write(),
// then this function returns `NULL`.
// 
// The returned pixbuf will be the same in all future calls to the loader,
// so if you want to keep using it, you should acquire a reference to it.
// 
// Additionally, if the loader is an animation, it will return the "static
// image" of the animation (see gdk_pixbuf_animation_get_static_image()).
func (loader *PixbufLoaderInstance) GetPixbuf() Pixbuf {
	var carg0 *C.GdkPixbufLoader // in, none, class
	var cret  *C.GdkPixbuf       // return, none, class

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))

	cret = C.gdk_pixbuf_loader_get_pixbuf(carg0)
	runtime.KeepAlive(loader)

	var ret Pixbuf

	ret = UnsafePixbufFromGlibNone(unsafe.Pointer(cret))

	return ret
}

// SetSize wraps gdk_pixbuf_loader_set_size
// 
// The function takes the following parameters:
// 
// 	- width int: The desired width of the image being loaded. 
// 	- height int: The desired height of the image being loaded. 
//
// Causes the image to be scaled while it is loaded.
// 
// The desired image size can be determined relative to the original
// size of the image by calling gdk_pixbuf_loader_set_size() from a
// signal handler for the ::size-prepared signal.
// 
// Attempts to set the desired image size  are ignored after the
// emission of the ::size-prepared signal.
func (loader *PixbufLoaderInstance) SetSize(width int, height int) {
	var carg0 *C.GdkPixbufLoader // in, none, class
	var carg1 C.int              // in, casted
	var carg2 C.int              // in, casted

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))
	carg1 = C.int(width)
	carg2 = C.int(height)

	C.gdk_pixbuf_loader_set_size(carg0, carg1, carg2)
	runtime.KeepAlive(loader)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
}

// Write wraps gdk_pixbuf_loader_write
// 
// The function takes the following parameters:
// 
// 	- buf []uint8: Pointer to image data. 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Parses the next `count` bytes in the given image buffer.
func (loader *PixbufLoaderInstance) Write(buf []uint8) (bool, error) {
	var carg0 *C.GdkPixbufLoader // in, none, class
	var carg1 *C.guint8          // in, transfer: none, scope: call, implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false
	var carg2 C.gsize            // implicit
	var cret  C.gboolean         // return
	var _cerr *C.GError          // out, full, record, nullable

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))
	panic("unimplemented conversion of *typesystem.Array (guint8*)")

	cret = C.gdk_pixbuf_loader_write(carg0, carg1, carg2, &_cerr)
	runtime.KeepAlive(loader)
	runtime.KeepAlive(buf)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// WriteBytes wraps gdk_pixbuf_loader_write_bytes
// 
// The function takes the following parameters:
// 
// 	- buffer *glib.Bytes: The image data as a `GBytes` buffer. 
// 
// The function returns the following values:
// 
// 	- ret bool 
// 	- _goerr error (nullable): an error 
//
// Parses the next contents of the given image buffer.
func (loader *PixbufLoaderInstance) WriteBytes(buffer *glib.Bytes) (bool, error) {
	var carg0 *C.GdkPixbufLoader // in, none, class
	var carg1 *C.GBytes          // in, none, record
	var cret  C.gboolean         // return
	var _cerr *C.GError          // out, full, record, nullable

	carg0 = (*C.GdkPixbufLoader)(UnsafePixbufLoaderToGlibNone(loader))
	carg1 = (*C.GBytes)(glib.UnsafeBytesToGlibNone(buffer))

	cret = C.gdk_pixbuf_loader_write_bytes(carg0, carg1, &_cerr)
	runtime.KeepAlive(loader)
	runtime.KeepAlive(buffer)

	var ret    bool
	var _goerr error

	if cret != 0 {
		ret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return ret, _goerr
}

// PixbufNonAnimInstance is the instance type used by all types extending PixbufNonAnim. It is used internally by the bindings. Users should use the interface [PixbufNonAnim] instead.
type PixbufNonAnimInstance struct {
	_ [0]func() // equal guard
	PixbufAnimationInstance
}

var _ PixbufNonAnim = (*PixbufNonAnimInstance)(nil)

// PixbufNonAnimInstance wraps PixbufNonAnim
type PixbufNonAnim interface {
	PixbufAnimation
	upcastTo() *PixbufNonAnimInstance

}

func unsafeWrapPixbufNonAnim(base *gobject.Object) *PixbufNonAnimInstance {
	return &PixbufNonAnimInstance{
		PixbufAnimationInstance: PixbufAnimationInstance{
			Object: *base,
		},
	}
}

func marshalPixbufNonAnimInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbufNonAnim(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufNonAnimFromGlibBorrow is used to convert raw PixbufNonAnim pointers to go. This is used by the bindings internally.
func UnsafePixbufNonAnimFromGlibBorrow(c unsafe.Pointer) PixbufNonAnim {
	return gobject.TODOBorrow(c).(PixbufNonAnim)
}

// UnsafePixbufNonAnimFromGlibNone is used to convert raw PixbufNonAnim pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufNonAnimFromGlibNone(c unsafe.Pointer) PixbufNonAnim {
	return gobject.Take(c).(PixbufNonAnim)
}

// UnsafePixbufNonAnimFromGlibFull is used to convert raw PixbufNonAnim pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufNonAnimFromGlibFull(c unsafe.Pointer) PixbufNonAnim {
	return gobject.AssumeOwnership(c).(PixbufNonAnim)
}

func (p *PixbufNonAnimInstance) upcastTo() *PixbufNonAnimInstance {
	return p
}

// UnsafePixbufNonAnimToGlibNone is used to convert the instance to it's C value PixbufNonAnim. This is used by the bindings internally.
func UnsafePixbufNonAnimToGlibNone(c PixbufNonAnim) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufNonAnimToGlibFull is used to convert the instance to it's C value PixbufNonAnim, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufNonAnimToGlibFull(c PixbufNonAnim) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// NewPixbufNonAnimInstance wraps gdk_pixbuf_non_anim_new
// 
// The function takes the following parameters:
// 
// 	- pixbuf Pixbuf 
// 
// The function returns the following values:
// 
// 	- ret PixbufAnimation 
func NewPixbufNonAnimInstance(pixbuf Pixbuf) PixbufAnimation {
	var carg1 *C.GdkPixbuf          // in, none, class
	var cret  *C.GdkPixbufAnimation // return, full, class

	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	cret = C.gdk_pixbuf_non_anim_new(carg1)
	runtime.KeepAlive(pixbuf)

	var ret PixbufAnimation

	ret = UnsafePixbufAnimationFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// PixbufSimpleAnimInstance is the instance type used by all types extending GdkPixbufSimpleAnim. It is used internally by the bindings. Users should use the interface [PixbufSimpleAnim] instead.
type PixbufSimpleAnimInstance struct {
	_ [0]func() // equal guard
	PixbufAnimationInstance
}

var _ PixbufSimpleAnim = (*PixbufSimpleAnimInstance)(nil)

// PixbufSimpleAnimInstance wraps GdkPixbufSimpleAnim
//
// An opaque struct representing a simple animation.
type PixbufSimpleAnim interface {
	PixbufAnimation
	upcastToGdkPixbufSimpleAnim() *PixbufSimpleAnimInstance

	// AddFrame wraps gdk_pixbuf_simple_anim_add_frame
	// 
	// The function takes the following parameters:
	// 
	// 	- pixbuf Pixbuf: the pixbuf to add 
	//
	// Adds a new frame to @animation. The @pixbuf must
	// have the dimensions specified when the animation
	// was constructed.
	AddFrame(Pixbuf)
	// GetLoop wraps gdk_pixbuf_simple_anim_get_loop
	// The function returns the following values:
	// 
	// 	- ret bool 
	//
	// Gets whether @animation should loop indefinitely when it reaches the end.
	GetLoop() bool
	// SetLoop wraps gdk_pixbuf_simple_anim_set_loop
	// 
	// The function takes the following parameters:
	// 
	// 	- loop bool: whether to loop the animation 
	//
	// Sets whether @animation should loop indefinitely when it reaches the end.
	SetLoop(bool)
}

func unsafeWrapPixbufSimpleAnim(base *gobject.Object) *PixbufSimpleAnimInstance {
	return &PixbufSimpleAnimInstance{
		PixbufAnimationInstance: PixbufAnimationInstance{
			Object: *base,
		},
	}
}

func marshalPixbufSimpleAnimInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbufSimpleAnim(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufSimpleAnimFromGlibBorrow is used to convert raw GdkPixbufSimpleAnim pointers to go. This is used by the bindings internally.
func UnsafePixbufSimpleAnimFromGlibBorrow(c unsafe.Pointer) PixbufSimpleAnim {
	return gobject.TODOBorrow(c).(PixbufSimpleAnim)
}

// UnsafePixbufSimpleAnimFromGlibNone is used to convert raw GdkPixbufSimpleAnim pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimFromGlibNone(c unsafe.Pointer) PixbufSimpleAnim {
	return gobject.Take(c).(PixbufSimpleAnim)
}

// UnsafePixbufSimpleAnimFromGlibFull is used to convert raw GdkPixbufSimpleAnim pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimFromGlibFull(c unsafe.Pointer) PixbufSimpleAnim {
	return gobject.AssumeOwnership(c).(PixbufSimpleAnim)
}

func (p *PixbufSimpleAnimInstance) upcastToGdkPixbufSimpleAnim() *PixbufSimpleAnimInstance {
	return p
}

// UnsafePixbufSimpleAnimToGlibNone is used to convert the instance to it's C value GdkPixbufSimpleAnim. This is used by the bindings internally.
func UnsafePixbufSimpleAnimToGlibNone(c PixbufSimpleAnim) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufSimpleAnimToGlibFull is used to convert the instance to it's C value GdkPixbufSimpleAnim, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimToGlibFull(c PixbufSimpleAnim) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// NewPixbufSimpleAnimInstance wraps gdk_pixbuf_simple_anim_new
// 
// The function takes the following parameters:
// 
// 	- width int: the width of the animation 
// 	- height int: the height of the animation 
// 	- rate float32: the speed of the animation, in frames per second 
// 
// The function returns the following values:
// 
// 	- ret PixbufSimpleAnim 
//
// Creates a new, empty animation.
func NewPixbufSimpleAnimInstance(width int, height int, rate float32) PixbufSimpleAnim {
	var carg1 C.int                  // in, casted
	var carg2 C.int                  // in, casted
	var carg3 C.gfloat               // in, casted
	var cret  *C.GdkPixbufSimpleAnim // return, full, class

	carg1 = C.int(width)
	carg2 = C.int(height)
	carg3 = C.gfloat(rate)

	cret = C.gdk_pixbuf_simple_anim_new(carg1, carg2, carg3)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
	runtime.KeepAlive(rate)

	var ret PixbufSimpleAnim

	ret = UnsafePixbufSimpleAnimFromGlibFull(unsafe.Pointer(cret))

	return ret
}

// AddFrame wraps gdk_pixbuf_simple_anim_add_frame
// 
// The function takes the following parameters:
// 
// 	- pixbuf Pixbuf: the pixbuf to add 
//
// Adds a new frame to @animation. The @pixbuf must
// have the dimensions specified when the animation
// was constructed.
func (animation *PixbufSimpleAnimInstance) AddFrame(pixbuf Pixbuf) {
	var carg0 *C.GdkPixbufSimpleAnim // in, none, class
	var carg1 *C.GdkPixbuf           // in, none, class

	carg0 = (*C.GdkPixbufSimpleAnim)(UnsafePixbufSimpleAnimToGlibNone(animation))
	carg1 = (*C.GdkPixbuf)(UnsafePixbufToGlibNone(pixbuf))

	C.gdk_pixbuf_simple_anim_add_frame(carg0, carg1)
	runtime.KeepAlive(animation)
	runtime.KeepAlive(pixbuf)
}

// GetLoop wraps gdk_pixbuf_simple_anim_get_loop
// The function returns the following values:
// 
// 	- ret bool 
//
// Gets whether @animation should loop indefinitely when it reaches the end.
func (animation *PixbufSimpleAnimInstance) GetLoop() bool {
	var carg0 *C.GdkPixbufSimpleAnim // in, none, class
	var cret  C.gboolean             // return

	carg0 = (*C.GdkPixbufSimpleAnim)(UnsafePixbufSimpleAnimToGlibNone(animation))

	cret = C.gdk_pixbuf_simple_anim_get_loop(carg0)
	runtime.KeepAlive(animation)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// SetLoop wraps gdk_pixbuf_simple_anim_set_loop
// 
// The function takes the following parameters:
// 
// 	- loop bool: whether to loop the animation 
//
// Sets whether @animation should loop indefinitely when it reaches the end.
func (animation *PixbufSimpleAnimInstance) SetLoop(loop bool) {
	var carg0 *C.GdkPixbufSimpleAnim // in, none, class
	var carg1 C.gboolean             // in

	carg0 = (*C.GdkPixbufSimpleAnim)(UnsafePixbufSimpleAnimToGlibNone(animation))
	if loop {
		carg1 = C.TRUE
	}

	C.gdk_pixbuf_simple_anim_set_loop(carg0, carg1)
	runtime.KeepAlive(animation)
	runtime.KeepAlive(loop)
}

// PixbufSimpleAnimIterInstance is the instance type used by all types extending PixbufSimpleAnimIter. It is used internally by the bindings. Users should use the interface [PixbufSimpleAnimIter] instead.
type PixbufSimpleAnimIterInstance struct {
	_ [0]func() // equal guard
	PixbufAnimationIterInstance
}

var _ PixbufSimpleAnimIter = (*PixbufSimpleAnimIterInstance)(nil)

// PixbufSimpleAnimIterInstance wraps PixbufSimpleAnimIter
type PixbufSimpleAnimIter interface {
	PixbufAnimationIter
	upcastTo() *PixbufSimpleAnimIterInstance

}

func unsafeWrapPixbufSimpleAnimIter(base *gobject.Object) *PixbufSimpleAnimIterInstance {
	return &PixbufSimpleAnimIterInstance{
		PixbufAnimationIterInstance: PixbufAnimationIterInstance{
			Object: *base,
		},
	}
}

func marshalPixbufSimpleAnimIterInstance(p uintptr) (interface{}, error) {
	return unsafeWrapPixbufSimpleAnimIter(gobject.ValueFromNative(unsafe.Pointer(p)).Object()), nil
}

// UnsafePixbufSimpleAnimIterFromGlibBorrow is used to convert raw PixbufSimpleAnimIter pointers to go. This is used by the bindings internally.
func UnsafePixbufSimpleAnimIterFromGlibBorrow(c unsafe.Pointer) PixbufSimpleAnimIter {
	return gobject.TODOBorrow(c).(PixbufSimpleAnimIter)
}

// UnsafePixbufSimpleAnimIterFromGlibNone is used to convert raw PixbufSimpleAnimIter pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimIterFromGlibNone(c unsafe.Pointer) PixbufSimpleAnimIter {
	return gobject.Take(c).(PixbufSimpleAnimIter)
}

// UnsafePixbufSimpleAnimIterFromGlibFull is used to convert raw PixbufSimpleAnimIter pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimIterFromGlibFull(c unsafe.Pointer) PixbufSimpleAnimIter {
	return gobject.AssumeOwnership(c).(PixbufSimpleAnimIter)
}

func (p *PixbufSimpleAnimIterInstance) upcastTo() *PixbufSimpleAnimIterInstance {
	return p
}

// UnsafePixbufSimpleAnimIterToGlibNone is used to convert the instance to it's C value PixbufSimpleAnimIter. This is used by the bindings internally.
func UnsafePixbufSimpleAnimIterToGlibNone(c PixbufSimpleAnimIter) unsafe.Pointer {
	return gobject.TODOToNone(c)
}

// UnsafePixbufSimpleAnimIterToGlibFull is used to convert the instance to it's C value PixbufSimpleAnimIter, while removeing the finalizer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimIterToGlibFull(c PixbufSimpleAnimIter) unsafe.Pointer {
	return gobject.TODOToFull(c)
}

// PixbufAnimationClass wraps GdkPixbufAnimationClass
//
// Modules supporting animations must derive a type from
// #GdkPixbufAnimation, providing suitable implementations of the
// virtual functions.
type PixbufAnimationClass struct {
	*pixbufAnimationClass
}

// pixbufAnimationClass is the struct that's finalized
type pixbufAnimationClass struct {
	native *C.GdkPixbufAnimationClass
}

// UnsafePixbufAnimationClassFromGlibBorrow is used to convert raw C.GdkPixbufAnimationClass pointers to go. This is used by the bindings internally.
func UnsafePixbufAnimationClassFromGlibBorrow(p unsafe.Pointer) *PixbufAnimationClass {
	return &PixbufAnimationClass{&pixbufAnimationClass{(*C.GdkPixbufAnimationClass)(p)}}
}

// UnsafePixbufAnimationClassFromGlibFull is used to convert raw C.GdkPixbufAnimationClass pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufAnimationClassFromGlibFull(p unsafe.Pointer) *PixbufAnimationClass {
	wrapped := UnsafePixbufAnimationClassFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufAnimationClass,
		func (intern *pixbufAnimationClass) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafePixbufAnimationClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufAnimationClass] is expected to work anymore.
func UnsafePixbufAnimationClassFree(p *PixbufAnimationClass) {
	C.free(unsafe.Pointer(p.native))
}

// UnsafePixbufAnimationClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufAnimationClassToGlibNone(p *PixbufAnimationClass) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufAnimationClassToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufAnimationClassToGlibFull(p *PixbufAnimationClass) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufAnimationClass, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufAnimationClass is invalid from here on
	return _p
}
// PixbufAnimationIterClass wraps GdkPixbufAnimationIterClass
//
// Modules supporting animations must derive a type from
// #GdkPixbufAnimationIter, providing suitable implementations of the
// virtual functions.
type PixbufAnimationIterClass struct {
	*pixbufAnimationIterClass
}

// pixbufAnimationIterClass is the struct that's finalized
type pixbufAnimationIterClass struct {
	native *C.GdkPixbufAnimationIterClass
}

// UnsafePixbufAnimationIterClassFromGlibBorrow is used to convert raw C.GdkPixbufAnimationIterClass pointers to go. This is used by the bindings internally.
func UnsafePixbufAnimationIterClassFromGlibBorrow(p unsafe.Pointer) *PixbufAnimationIterClass {
	return &PixbufAnimationIterClass{&pixbufAnimationIterClass{(*C.GdkPixbufAnimationIterClass)(p)}}
}

// UnsafePixbufAnimationIterClassFromGlibFull is used to convert raw C.GdkPixbufAnimationIterClass pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufAnimationIterClassFromGlibFull(p unsafe.Pointer) *PixbufAnimationIterClass {
	wrapped := UnsafePixbufAnimationIterClassFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufAnimationIterClass,
		func (intern *pixbufAnimationIterClass) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafePixbufAnimationIterClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufAnimationIterClass] is expected to work anymore.
func UnsafePixbufAnimationIterClassFree(p *PixbufAnimationIterClass) {
	C.free(unsafe.Pointer(p.native))
}

// UnsafePixbufAnimationIterClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufAnimationIterClassToGlibNone(p *PixbufAnimationIterClass) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufAnimationIterClassToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufAnimationIterClassToGlibFull(p *PixbufAnimationIterClass) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufAnimationIterClass, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufAnimationIterClass is invalid from here on
	return _p
}
// PixbufFormat wraps GdkPixbufFormat
//
// A `GdkPixbufFormat` contains information about the image format accepted
// by a module.
// 
// Only modules should access the fields directly, applications should
// use the `gdk_pixbuf_format_*` family of functions.
type PixbufFormat struct {
	*pixbufFormat
}

// pixbufFormat is the struct that's finalized
type pixbufFormat struct {
	native *C.GdkPixbufFormat
}

func marshalPixbufFormat(p uintptr) (interface{}, error) {
	b := gobject.ValueFromNative(unsafe.Pointer(p)).Boxed()
	return UnsafePixbufFormatFromGlibBorrow(b), nil
}

// UnsafePixbufFormatFromGlibBorrow is used to convert raw C.GdkPixbufFormat pointers to go. This is used by the bindings internally.
func UnsafePixbufFormatFromGlibBorrow(p unsafe.Pointer) *PixbufFormat {
	return &PixbufFormat{&pixbufFormat{(*C.GdkPixbufFormat)(p)}}
}

// UnsafePixbufFormatFromGlibFull is used to convert raw C.GdkPixbufFormat pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufFormatFromGlibFull(p unsafe.Pointer) *PixbufFormat {
	wrapped := UnsafePixbufFormatFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufFormat,
		func (intern *pixbufFormat) {
			C.gdk_pixbuf_format_free(intern.native)
		},
	)
	return wrapped
}

// UnsafePixbufFormatFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufFormat] is expected to work anymore.
func UnsafePixbufFormatFree(p *PixbufFormat) {
	C.gdk_pixbuf_format_free(p.native)
}

// UnsafePixbufFormatToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufFormatToGlibNone(p *PixbufFormat) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufFormatToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufFormatToGlibFull(p *PixbufFormat) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufFormat, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufFormat is invalid from here on
	return _p
}
// flags wraps flags
//
// a combination of `GdkPixbufFormatFlags`
func (p *PixbufFormat) Flags() uint32 {
	valptr := &p.native.flags
	var _v uint32
	_v = uint32(*valptr)
	return _v
}

// disabled wraps disabled
//
// a boolean determining whether the loader is disabled`
func (p *PixbufFormat) Disabled() bool {
	valptr := &p.native.disabled
	var _v bool
	_v = bool(*valptr)
	return _v
}

// flags wraps flags
//
// a combination of `GdkPixbufFormatFlags`
func (p *PixbufFormat) SetFlags(flags uint32) {
	valptr := &p.native.flags
	*valptr = C.guint32(flags)
}

// disabled wraps disabled
//
// a boolean determining whether the loader is disabled`
func (p *PixbufFormat) SetDisabled(disabled bool) {
	valptr := &p.native.disabled
	*valptr = C.gboolean(disabled)
}

// GetDescription wraps gdk_pixbuf_format_get_description
// The function returns the following values:
// 
// 	- ret string 
//
// Returns a description of the format.
func (format *PixbufFormat) GetDescription() string {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  *C.gchar           // return, full, string

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_get_description(carg0)
	runtime.KeepAlive(format)

	var ret string

	ret = C.GoString((*C.gchar)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return ret
}

// GetExtensions wraps gdk_pixbuf_format_get_extensions
// The function returns the following values:
// 
// 	- ret []string 
//
// Returns the filename extensions typically used for files in the
// given format.
func (format *PixbufFormat) GetExtensions() []string {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  **C.gchar          // return, transfer: full, scope: , implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_get_extensions(carg0)
	runtime.KeepAlive(format)

	var ret []string

	panic("unimplemented conversion of *typesystem.Array (gchar**)")

	return ret
}

// GetLicense wraps gdk_pixbuf_format_get_license
// The function returns the following values:
// 
// 	- ret string 
//
// Returns information about the license of the image loader for the format.
// 
// The returned string should be a shorthand for a well known license, e.g.
// "LGPL", "GPL", "QPL", "GPL/QPL", or "other" to indicate some other license.
func (format *PixbufFormat) GetLicense() string {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  *C.gchar           // return, full, string

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_get_license(carg0)
	runtime.KeepAlive(format)

	var ret string

	ret = C.GoString((*C.gchar)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return ret
}

// GetMIMETypes wraps gdk_pixbuf_format_get_mime_types
// The function returns the following values:
// 
// 	- ret []string 
//
// Returns the mime types supported by the format.
func (format *PixbufFormat) GetMIMETypes() []string {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  **C.gchar          // return, transfer: full, scope: , implicit: false, skip: false, optional: false, nullable: false, caller-allocates: false, has closure: false, has destroy: false

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_get_mime_types(carg0)
	runtime.KeepAlive(format)

	var ret []string

	panic("unimplemented conversion of *typesystem.Array (gchar**)")

	return ret
}

// GetName wraps gdk_pixbuf_format_get_name
// The function returns the following values:
// 
// 	- ret string 
//
// Returns the name of the format.
func (format *PixbufFormat) GetName() string {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  *C.gchar           // return, full, string

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_get_name(carg0)
	runtime.KeepAlive(format)

	var ret string

	ret = C.GoString((*C.gchar)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return ret
}

// IsDisabled wraps gdk_pixbuf_format_is_disabled
// The function returns the following values:
// 
// 	- ret bool 
//
// Returns whether this image format is disabled.
// 
// See gdk_pixbuf_format_set_disabled().
func (format *PixbufFormat) IsDisabled() bool {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  C.gboolean         // return

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_is_disabled(carg0)
	runtime.KeepAlive(format)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// IsSaveOptionSupported wraps gdk_pixbuf_format_is_save_option_supported
// 
// The function takes the following parameters:
// 
// 	- optionKey string: the name of an option 
// 
// The function returns the following values:
// 
// 	- ret bool 
//
// Returns `TRUE` if the save option specified by @option_key is supported when
// saving a pixbuf using the module implementing @format.
// 
// See gdk_pixbuf_save() for more information about option keys.
func (format *PixbufFormat) IsSaveOptionSupported(optionKey string) bool {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var carg1 *C.gchar           // in, none, string
	var cret  C.gboolean         // return

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))
	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(optionKey)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.gdk_pixbuf_format_is_save_option_supported(carg0, carg1)
	runtime.KeepAlive(format)
	runtime.KeepAlive(optionKey)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// IsScalable wraps gdk_pixbuf_format_is_scalable
// The function returns the following values:
// 
// 	- ret bool 
//
// Returns whether this image format is scalable.
// 
// If a file is in a scalable format, it is preferable to load it at
// the desired size, rather than loading it at the default size and
// scaling the resulting pixbuf to the desired size.
func (format *PixbufFormat) IsScalable() bool {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  C.gboolean         // return

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_is_scalable(carg0)
	runtime.KeepAlive(format)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// IsWritable wraps gdk_pixbuf_format_is_writable
// The function returns the following values:
// 
// 	- ret bool 
//
// Returns whether pixbufs can be saved in the given format.
func (format *PixbufFormat) IsWritable() bool {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var cret  C.gboolean         // return

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))

	cret = C.gdk_pixbuf_format_is_writable(carg0)
	runtime.KeepAlive(format)

	var ret bool

	if cret != 0 {
		ret = true
	}

	return ret
}

// SetDisabled wraps gdk_pixbuf_format_set_disabled
// 
// The function takes the following parameters:
// 
// 	- disabled bool: `TRUE` to disable the format @format 
//
// Disables or enables an image format.
// 
// If a format is disabled, GdkPixbuf won't use the image loader for
// this format to load images.
// 
// Applications can use this to avoid using image loaders with an
// inappropriate license, see gdk_pixbuf_format_get_license().
func (format *PixbufFormat) SetDisabled(disabled bool) {
	var carg0 *C.GdkPixbufFormat // in, none, record
	var carg1 C.gboolean         // in

	carg0 = (*C.GdkPixbufFormat)(UnsafePixbufFormatToGlibNone(format))
	if disabled {
		carg1 = C.TRUE
	}

	C.gdk_pixbuf_format_set_disabled(carg0, carg1)
	runtime.KeepAlive(format)
	runtime.KeepAlive(disabled)
}

// PixbufLoaderClass wraps GdkPixbufLoaderClass
type PixbufLoaderClass struct {
	*pixbufLoaderClass
}

// pixbufLoaderClass is the struct that's finalized
type pixbufLoaderClass struct {
	native *C.GdkPixbufLoaderClass
}

// UnsafePixbufLoaderClassFromGlibBorrow is used to convert raw C.GdkPixbufLoaderClass pointers to go. This is used by the bindings internally.
func UnsafePixbufLoaderClassFromGlibBorrow(p unsafe.Pointer) *PixbufLoaderClass {
	return &PixbufLoaderClass{&pixbufLoaderClass{(*C.GdkPixbufLoaderClass)(p)}}
}

// UnsafePixbufLoaderClassFromGlibFull is used to convert raw C.GdkPixbufLoaderClass pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufLoaderClassFromGlibFull(p unsafe.Pointer) *PixbufLoaderClass {
	wrapped := UnsafePixbufLoaderClassFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufLoaderClass,
		func (intern *pixbufLoaderClass) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafePixbufLoaderClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufLoaderClass] is expected to work anymore.
func UnsafePixbufLoaderClassFree(p *PixbufLoaderClass) {
	C.free(unsafe.Pointer(p.native))
}

// UnsafePixbufLoaderClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufLoaderClassToGlibNone(p *PixbufLoaderClass) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufLoaderClassToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufLoaderClassToGlibFull(p *PixbufLoaderClass) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufLoaderClass, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufLoaderClass is invalid from here on
	return _p
}
// PixbufModule wraps GdkPixbufModule
//
// A `GdkPixbufModule` contains the necessary functions to load and save
// images in a certain file format.
// 
// If `GdkPixbuf` has been compiled with `GModule` support, it can be extended
// by modules which can load (and perhaps also save) new image and animation
// formats.
// 
// ## Implementing modules
// 
// The `GdkPixbuf` interfaces needed for implementing modules are contained in
// `gdk-pixbuf-io.h` (and `gdk-pixbuf-animation.h` if the module supports
// animations). They are not covered by the same stability guarantees as the
// regular GdkPixbuf API. To underline this fact, they are protected by the
// `GDK_PIXBUF_ENABLE_BACKEND` pre-processor symbol.
// 
// Each loadable module must contain a `GdkPixbufModuleFillVtableFunc` function
// named `fill_vtable`, which will get called when the module
// is loaded and must set the function pointers of the `GdkPixbufModule`.
// 
// In order to make format-checking work before actually loading the modules
// (which may require calling `dlopen` to load image libraries), modules export
// their signatures (and other information) via the `fill_info` function. An
// external utility, `gdk-pixbuf-query-loaders`, uses this to create a text
// file containing a list of all available loaders and  their signatures.
// This file is then read at runtime by `GdkPixbuf` to obtain the list of
// available loaders and their signatures.
// 
// Modules may only implement a subset of the functionality available via
// `GdkPixbufModule`. If a particular functionality is not implemented, the
// `fill_vtable` function will simply not set the corresponding
// function pointers of the `GdkPixbufModule` structure. If a module supports
// incremental loading (i.e. provides `begin_load`, `stop_load` and
// `load_increment`), it doesn't have to implement `load`, since `GdkPixbuf`
// can supply a generic `load` implementation wrapping the incremental loading.
// 
// ## Installing modules
// 
// Installing a module is a two-step process:
// 
//  - copy the module file(s) to the loader directory (normally
//    `$libdir/gdk-pixbuf-2.0/$version/loaders`, unless overridden by the
//    environment variable `GDK_PIXBUF_MODULEDIR`)
//  - call `gdk-pixbuf-query-loaders` to update the module file (normally
//    `$libdir/gdk-pixbuf-2.0/$version/loaders.cache`, unless overridden
//    by the environment variable `GDK_PIXBUF_MODULE_FILE`)
type PixbufModule struct {
	*pixbufModule
}

// pixbufModule is the struct that's finalized
type pixbufModule struct {
	native *C.GdkPixbufModule
}

// UnsafePixbufModuleFromGlibBorrow is used to convert raw C.GdkPixbufModule pointers to go. This is used by the bindings internally.
func UnsafePixbufModuleFromGlibBorrow(p unsafe.Pointer) *PixbufModule {
	return &PixbufModule{&pixbufModule{(*C.GdkPixbufModule)(p)}}
}

// UnsafePixbufModuleFromGlibFull is used to convert raw C.GdkPixbufModule pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufModuleFromGlibFull(p unsafe.Pointer) *PixbufModule {
	wrapped := UnsafePixbufModuleFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufModule,
		func (intern *pixbufModule) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafePixbufModuleFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufModule] is expected to work anymore.
func UnsafePixbufModuleFree(p *PixbufModule) {
	C.free(unsafe.Pointer(p.native))
}

// UnsafePixbufModuleToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufModuleToGlibNone(p *PixbufModule) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufModuleToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufModuleToGlibFull(p *PixbufModule) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufModule, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufModule is invalid from here on
	return _p
}
// PixbufModulePattern wraps GdkPixbufModulePattern
//
// The signature prefix for a module.
// 
// The signature of a module is a set of prefixes. Prefixes are encoded as
// pairs of ordinary strings, where the second string, called the mask, if
// not `NULL`, must be of the same length as the first one and may contain
// ' ', '!', 'x', 'z', and 'n' to indicate bytes that must be matched,
// not matched, "don't-care"-bytes, zeros and non-zeros, respectively.
// 
// Each prefix has an associated integer that describes the relevance of
// the prefix, with 0 meaning a mismatch and 100 a "perfect match".
// 
// Starting with gdk-pixbuf 2.8, the first byte of the mask may be '*',
// indicating an unanchored pattern that matches not only at the beginning,
// but also in the middle. Versions prior to 2.8 will interpret the '*'
// like an 'x'.
// 
// The signature of a module is stored as an array of
// `GdkPixbufModulePatterns`. The array is terminated by a pattern
// where the `prefix` is `NULL`.
// 
// ```c
// GdkPixbufModulePattern *signature[] = {
//   { "abcdx", " !x z", 100 },
//   { "bla", NULL,  90 },
//   { NULL, NULL, 0 }
// };
// ```
// 
// In the example above, the signature matches e.g. "auud\0" with
// relevance 100, and "blau" with relevance 90.
type PixbufModulePattern struct {
	*pixbufModulePattern
}

// pixbufModulePattern is the struct that's finalized
type pixbufModulePattern struct {
	native *C.GdkPixbufModulePattern
}

// UnsafePixbufModulePatternFromGlibBorrow is used to convert raw C.GdkPixbufModulePattern pointers to go. This is used by the bindings internally.
func UnsafePixbufModulePatternFromGlibBorrow(p unsafe.Pointer) *PixbufModulePattern {
	return &PixbufModulePattern{&pixbufModulePattern{(*C.GdkPixbufModulePattern)(p)}}
}

// UnsafePixbufModulePatternFromGlibFull is used to convert raw C.GdkPixbufModulePattern pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufModulePatternFromGlibFull(p unsafe.Pointer) *PixbufModulePattern {
	wrapped := UnsafePixbufModulePatternFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufModulePattern,
		func (intern *pixbufModulePattern) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafePixbufModulePatternFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufModulePattern] is expected to work anymore.
func UnsafePixbufModulePatternFree(p *PixbufModulePattern) {
	C.free(unsafe.Pointer(p.native))
}

// UnsafePixbufModulePatternToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufModulePatternToGlibNone(p *PixbufModulePattern) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufModulePatternToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufModulePatternToGlibFull(p *PixbufModulePattern) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufModulePattern, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufModulePattern is invalid from here on
	return _p
}
// relevance wraps relevance
//
// relevance of this pattern
func (p *PixbufModulePattern) Relevance() int {
	valptr := &p.native.relevance
	var _v int
	_v = int(*valptr)
	return _v
}

// relevance wraps relevance
//
// relevance of this pattern
func (p *PixbufModulePattern) SetRelevance(relevance int) {
	valptr := &p.native.relevance
	*valptr = C.int(relevance)
}

// PixbufSimpleAnimClass wraps GdkPixbufSimpleAnimClass
type PixbufSimpleAnimClass struct {
	*pixbufSimpleAnimClass
}

// pixbufSimpleAnimClass is the struct that's finalized
type pixbufSimpleAnimClass struct {
	native *C.GdkPixbufSimpleAnimClass
}

// UnsafePixbufSimpleAnimClassFromGlibBorrow is used to convert raw C.GdkPixbufSimpleAnimClass pointers to go. This is used by the bindings internally.
func UnsafePixbufSimpleAnimClassFromGlibBorrow(p unsafe.Pointer) *PixbufSimpleAnimClass {
	return &PixbufSimpleAnimClass{&pixbufSimpleAnimClass{(*C.GdkPixbufSimpleAnimClass)(p)}}
}

// UnsafePixbufSimpleAnimClassFromGlibFull is used to convert raw C.GdkPixbufSimpleAnimClass pointers to go while taking a reference. This is used by the bindings internally.
func UnsafePixbufSimpleAnimClassFromGlibFull(p unsafe.Pointer) *PixbufSimpleAnimClass {
	wrapped := UnsafePixbufSimpleAnimClassFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.pixbufSimpleAnimClass,
		func (intern *pixbufSimpleAnimClass) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafePixbufSimpleAnimClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [PixbufSimpleAnimClass] is expected to work anymore.
func UnsafePixbufSimpleAnimClassFree(p *PixbufSimpleAnimClass) {
	C.free(unsafe.Pointer(p.native))
}

// UnsafePixbufSimpleAnimClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafePixbufSimpleAnimClassToGlibNone(p *PixbufSimpleAnimClass) unsafe.Pointer {
	return unsafe.Pointer(p.native)
}

// UnsafePixbufSimpleAnimClassToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafePixbufSimpleAnimClassToGlibFull(p *PixbufSimpleAnimClass) unsafe.Pointer {
	runtime.SetFinalizer(p.pixbufSimpleAnimClass, nil)
	_p := unsafe.Pointer(p.native)
	p.native = nil // PixbufSimpleAnimClass is invalid from here on
	return _p
}
