// Code generated by girgen for Pango-1. DO NOT EDIT.

package pango

import (
	"fmt"
	"runtime"
	"strings"
	"unsafe"

	"github.com/diamondburned/gotk4/pkg/core/classdata"
	"github.com/diamondburned/gotk4/pkg/core/userdata"
	"github.com/diamondburned/gotk4/pkg/gio/v2"
	"github.com/diamondburned/gotk4/pkg/glib/v2"
	"github.com/diamondburned/gotk4/pkg/gobject/v2"
)

// #cgo pkg-config: pango
// #cgo CFLAGS: -Wno-deprecated-declarations
// #include <pango/pango.h>
// extern gboolean _gotk4_pango1_AttrFilterFunc(PangoAttribute*, gpointer);
// extern gboolean _gotk4_pango1_FontsetForEachFunc(PangoFontset*, PangoFont*, gpointer);
// extern PangoFontDescription* _gotk4_pango1_Font_describe(PangoFont*);
// extern PangoFontDescription* _gotk4_pango1_Font_describe_absolute(PangoFont*);
// extern PangoCoverage* _gotk4_pango1_Font_get_coverage(PangoFont*, PangoLanguage*);
// extern PangoFontMap* _gotk4_pango1_Font_get_font_map(PangoFont*);
// extern void _gotk4_pango1_Font_get_glyph_extents(PangoFont*, PangoGlyph, PangoRectangle, PangoRectangle);
// extern PangoFontMetrics* _gotk4_pango1_Font_get_metrics(PangoFont*, PangoLanguage*);
// PangoFontDescription* _gotk4_pango1_Font_virtual_describe(void* fnptr, PangoFont* carg0) {
// 	return ((PangoFontDescription* (*) (PangoFont*))(fnptr))(carg0);
// }
// PangoFontDescription* _gotk4_pango1_Font_virtual_describe_absolute(void* fnptr, PangoFont* carg0) {
// 	return ((PangoFontDescription* (*) (PangoFont*))(fnptr))(carg0);
// }
// PangoCoverage* _gotk4_pango1_Font_virtual_get_coverage(void* fnptr, PangoFont* carg0, PangoLanguage* carg1) {
// 	return ((PangoCoverage* (*) (PangoFont*, PangoLanguage*))(fnptr))(carg0, carg1);
// }
// PangoFontMap* _gotk4_pango1_Font_virtual_get_font_map(void* fnptr, PangoFont* carg0) {
// 	return ((PangoFontMap* (*) (PangoFont*))(fnptr))(carg0);
// }
// void _gotk4_pango1_Font_virtual_get_glyph_extents(void* fnptr, PangoFont* carg0, PangoGlyph carg1, PangoRectangle* carg2, PangoRectangle* carg3) {
// 	return ((void (*) (PangoFont*, PangoGlyph, PangoRectangle*, PangoRectangle*))(fnptr))(carg0, carg1, carg2, carg3);
// }
// PangoFontMetrics* _gotk4_pango1_Font_virtual_get_metrics(void* fnptr, PangoFont* carg0, PangoLanguage* carg1) {
// 	return ((PangoFontMetrics* (*) (PangoFont*, PangoLanguage*))(fnptr))(carg0, carg1);
// }
// extern PangoFontDescription* _gotk4_pango1_FontFace_describe(PangoFontFace*);
// extern char* _gotk4_pango1_FontFace_get_face_name(PangoFontFace*);
// extern PangoFontFamily* _gotk4_pango1_FontFace_get_family(PangoFontFace*);
// extern gboolean _gotk4_pango1_FontFace_is_synthesized(PangoFontFace*);
// PangoFontDescription* _gotk4_pango1_FontFace_virtual_describe(void* fnptr, PangoFontFace* carg0) {
// 	return ((PangoFontDescription* (*) (PangoFontFace*))(fnptr))(carg0);
// }
// char* _gotk4_pango1_FontFace_virtual_get_face_name(void* fnptr, PangoFontFace* carg0) {
// 	return ((char* (*) (PangoFontFace*))(fnptr))(carg0);
// }
// PangoFontFamily* _gotk4_pango1_FontFace_virtual_get_family(void* fnptr, PangoFontFace* carg0) {
// 	return ((PangoFontFamily* (*) (PangoFontFace*))(fnptr))(carg0);
// }
// gboolean _gotk4_pango1_FontFace_virtual_is_synthesized(void* fnptr, PangoFontFace* carg0) {
// 	return ((gboolean (*) (PangoFontFace*))(fnptr))(carg0);
// }
// extern PangoFontFace* _gotk4_pango1_FontFamily_get_face(PangoFontFamily*, char*);
// extern char* _gotk4_pango1_FontFamily_get_name(PangoFontFamily*);
// extern gboolean _gotk4_pango1_FontFamily_is_monospace(PangoFontFamily*);
// extern gboolean _gotk4_pango1_FontFamily_is_variable(PangoFontFamily*);
// PangoFontFace* _gotk4_pango1_FontFamily_virtual_get_face(void* fnptr, PangoFontFamily* carg0, char* carg1) {
// 	return ((PangoFontFace* (*) (PangoFontFamily*, char*))(fnptr))(carg0, carg1);
// }
// char* _gotk4_pango1_FontFamily_virtual_get_name(void* fnptr, PangoFontFamily* carg0) {
// 	return ((char* (*) (PangoFontFamily*))(fnptr))(carg0);
// }
// gboolean _gotk4_pango1_FontFamily_virtual_is_monospace(void* fnptr, PangoFontFamily* carg0) {
// 	return ((gboolean (*) (PangoFontFamily*))(fnptr))(carg0);
// }
// gboolean _gotk4_pango1_FontFamily_virtual_is_variable(void* fnptr, PangoFontFamily* carg0) {
// 	return ((gboolean (*) (PangoFontFamily*))(fnptr))(carg0);
// }
// extern void _gotk4_pango1_FontMap_changed(PangoFontMap*);
// extern PangoFontFamily* _gotk4_pango1_FontMap_get_family(PangoFontMap*, char*);
// extern guint _gotk4_pango1_FontMap_get_serial(PangoFontMap*);
// extern PangoFont* _gotk4_pango1_FontMap_load_font(PangoFontMap*, PangoContext*, PangoFontDescription*);
// extern PangoFontset* _gotk4_pango1_FontMap_load_fontset(PangoFontMap*, PangoContext*, PangoFontDescription*, PangoLanguage*);
// void _gotk4_pango1_FontMap_virtual_changed(void* fnptr, PangoFontMap* carg0) {
// 	return ((void (*) (PangoFontMap*))(fnptr))(carg0);
// }
// PangoFontFamily* _gotk4_pango1_FontMap_virtual_get_family(void* fnptr, PangoFontMap* carg0, char* carg1) {
// 	return ((PangoFontFamily* (*) (PangoFontMap*, char*))(fnptr))(carg0, carg1);
// }
// guint _gotk4_pango1_FontMap_virtual_get_serial(void* fnptr, PangoFontMap* carg0) {
// 	return ((guint (*) (PangoFontMap*))(fnptr))(carg0);
// }
// PangoFont* _gotk4_pango1_FontMap_virtual_load_font(void* fnptr, PangoFontMap* carg0, PangoContext* carg1, PangoFontDescription* carg2) {
// 	return ((PangoFont* (*) (PangoFontMap*, PangoContext*, PangoFontDescription*))(fnptr))(carg0, carg1, carg2);
// }
// PangoFontset* _gotk4_pango1_FontMap_virtual_load_fontset(void* fnptr, PangoFontMap* carg0, PangoContext* carg1, PangoFontDescription* carg2, PangoLanguage* carg3) {
// 	return ((PangoFontset* (*) (PangoFontMap*, PangoContext*, PangoFontDescription*, PangoLanguage*))(fnptr))(carg0, carg1, carg2, carg3);
// }
// extern void _gotk4_pango1_Fontset_foreach(PangoFontset*, PangoFontsetForeachFunc, gpointer);
// extern PangoFont* _gotk4_pango1_Fontset_get_font(PangoFontset*, guint);
// extern PangoLanguage* _gotk4_pango1_Fontset_get_language(PangoFontset*);
// extern PangoFontMetrics* _gotk4_pango1_Fontset_get_metrics(PangoFontset*);
// void _gotk4_pango1_Fontset_virtual_foreach(void* fnptr, PangoFontset* carg0, PangoFontsetForeachFunc carg1, gpointer carg2) {
// 	return ((void (*) (PangoFontset*, PangoFontsetForeachFunc, gpointer))(fnptr))(carg0, carg1, carg2);
// }
// PangoFont* _gotk4_pango1_Fontset_virtual_get_font(void* fnptr, PangoFontset* carg0, guint carg1) {
// 	return ((PangoFont* (*) (PangoFontset*, guint))(fnptr))(carg0, carg1);
// }
// PangoLanguage* _gotk4_pango1_Fontset_virtual_get_language(void* fnptr, PangoFontset* carg0) {
// 	return ((PangoLanguage* (*) (PangoFontset*))(fnptr))(carg0);
// }
// PangoFontMetrics* _gotk4_pango1_Fontset_virtual_get_metrics(void* fnptr, PangoFontset* carg0) {
// 	return ((PangoFontMetrics* (*) (PangoFontset*))(fnptr))(carg0);
// }
// extern void _gotk4_pango1_Renderer_begin(PangoRenderer*);
// extern void _gotk4_pango1_Renderer_draw_error_underline(PangoRenderer*, int, int, int, int);
// extern void _gotk4_pango1_Renderer_draw_glyph(PangoRenderer*, PangoFont*, PangoGlyph, double, double);
// extern void _gotk4_pango1_Renderer_draw_glyph_item(PangoRenderer*, char*, PangoGlyphItem*, int, int);
// extern void _gotk4_pango1_Renderer_draw_glyphs(PangoRenderer*, PangoFont*, PangoGlyphString*, int, int);
// extern void _gotk4_pango1_Renderer_draw_rectangle(PangoRenderer*, PangoRenderPart, int, int, int, int);
// extern void _gotk4_pango1_Renderer_draw_shape(PangoRenderer*, PangoAttrShape*, int, int);
// extern void _gotk4_pango1_Renderer_draw_trapezoid(PangoRenderer*, PangoRenderPart, double, double, double, double, double, double);
// extern void _gotk4_pango1_Renderer_end(PangoRenderer*);
// extern void _gotk4_pango1_Renderer_part_changed(PangoRenderer*, PangoRenderPart);
// extern void _gotk4_pango1_Renderer_prepare_run(PangoRenderer*, PangoLayoutRun*);
// void _gotk4_pango1_Renderer_virtual_begin(void* fnptr, PangoRenderer* carg0) {
// 	return ((void (*) (PangoRenderer*))(fnptr))(carg0);
// }
// void _gotk4_pango1_Renderer_virtual_draw_error_underline(void* fnptr, PangoRenderer* carg0, int carg1, int carg2, int carg3, int carg4) {
// 	return ((void (*) (PangoRenderer*, int, int, int, int))(fnptr))(carg0, carg1, carg2, carg3, carg4);
// }
// void _gotk4_pango1_Renderer_virtual_draw_glyph(void* fnptr, PangoRenderer* carg0, PangoFont* carg1, PangoGlyph carg2, double carg3, double carg4) {
// 	return ((void (*) (PangoRenderer*, PangoFont*, PangoGlyph, double, double))(fnptr))(carg0, carg1, carg2, carg3, carg4);
// }
// void _gotk4_pango1_Renderer_virtual_draw_glyph_item(void* fnptr, PangoRenderer* carg0, char* carg1, PangoGlyphItem* carg2, int carg3, int carg4) {
// 	return ((void (*) (PangoRenderer*, char*, PangoGlyphItem*, int, int))(fnptr))(carg0, carg1, carg2, carg3, carg4);
// }
// void _gotk4_pango1_Renderer_virtual_draw_glyphs(void* fnptr, PangoRenderer* carg0, PangoFont* carg1, PangoGlyphString* carg2, int carg3, int carg4) {
// 	return ((void (*) (PangoRenderer*, PangoFont*, PangoGlyphString*, int, int))(fnptr))(carg0, carg1, carg2, carg3, carg4);
// }
// void _gotk4_pango1_Renderer_virtual_draw_rectangle(void* fnptr, PangoRenderer* carg0, PangoRenderPart carg1, int carg2, int carg3, int carg4, int carg5) {
// 	return ((void (*) (PangoRenderer*, PangoRenderPart, int, int, int, int))(fnptr))(carg0, carg1, carg2, carg3, carg4, carg5);
// }
// void _gotk4_pango1_Renderer_virtual_draw_shape(void* fnptr, PangoRenderer* carg0, PangoAttrShape* carg1, int carg2, int carg3) {
// 	return ((void (*) (PangoRenderer*, PangoAttrShape*, int, int))(fnptr))(carg0, carg1, carg2, carg3);
// }
// void _gotk4_pango1_Renderer_virtual_draw_trapezoid(void* fnptr, PangoRenderer* carg0, PangoRenderPart carg1, double carg2, double carg3, double carg4, double carg5, double carg6, double carg7) {
// 	return ((void (*) (PangoRenderer*, PangoRenderPart, double, double, double, double, double, double))(fnptr))(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7);
// }
// void _gotk4_pango1_Renderer_virtual_end(void* fnptr, PangoRenderer* carg0) {
// 	return ((void (*) (PangoRenderer*))(fnptr))(carg0);
// }
// void _gotk4_pango1_Renderer_virtual_part_changed(void* fnptr, PangoRenderer* carg0, PangoRenderPart carg1) {
// 	return ((void (*) (PangoRenderer*, PangoRenderPart))(fnptr))(carg0, carg1);
// }
// void _gotk4_pango1_Renderer_virtual_prepare_run(void* fnptr, PangoRenderer* carg0, PangoLayoutRun* carg1) {
// 	return ((void (*) (PangoRenderer*, PangoLayoutRun*))(fnptr))(carg0, carg1);
// }
import "C"

// GType values.
var (
	TypeAlignment              = gobject.Type(C.pango_alignment_get_type())
	TypeAttrType               = gobject.Type(C.pango_attr_type_get_type())
	TypeBaselineShift          = gobject.Type(C.pango_baseline_shift_get_type())
	TypeBidiType               = gobject.Type(C.pango_bidi_type_get_type())
	TypeCoverageLevel          = gobject.Type(C.pango_coverage_level_get_type())
	TypeDirection              = gobject.Type(C.pango_direction_get_type())
	TypeEllipsizeMode          = gobject.Type(C.pango_ellipsize_mode_get_type())
	TypeFontScale              = gobject.Type(C.pango_font_scale_get_type())
	TypeGravity                = gobject.Type(C.pango_gravity_get_type())
	TypeGravityHint            = gobject.Type(C.pango_gravity_hint_get_type())
	TypeLayoutDeserializeError = gobject.Type(C.pango_layout_deserialize_error_get_type())
	TypeOverline               = gobject.Type(C.pango_overline_get_type())
	TypeRenderPart             = gobject.Type(C.pango_render_part_get_type())
	TypeScript                 = gobject.Type(C.pango_script_get_type())
	TypeStretch                = gobject.Type(C.pango_stretch_get_type())
	TypeStyle                  = gobject.Type(C.pango_style_get_type())
	TypeTabAlign               = gobject.Type(C.pango_tab_align_get_type())
	TypeTextTransform          = gobject.Type(C.pango_text_transform_get_type())
	TypeUnderline              = gobject.Type(C.pango_underline_get_type())
	TypeVariant                = gobject.Type(C.pango_variant_get_type())
	TypeWeight                 = gobject.Type(C.pango_weight_get_type())
	TypeWrapMode               = gobject.Type(C.pango_wrap_mode_get_type())
	TypeFontMask               = gobject.Type(C.pango_font_mask_get_type())
	TypeLayoutDeserializeFlags = gobject.Type(C.pango_layout_deserialize_flags_get_type())
	TypeLayoutSerializeFlags   = gobject.Type(C.pango_layout_serialize_flags_get_type())
	TypeShapeFlags             = gobject.Type(C.pango_shape_flags_get_type())
	TypeShowFlags              = gobject.Type(C.pango_show_flags_get_type())
	TypeContext                = gobject.Type(C.pango_context_get_type())
	TypeCoverage               = gobject.Type(C.pango_coverage_get_type())
	TypeFont                   = gobject.Type(C.pango_font_get_type())
	TypeFontFace               = gobject.Type(C.pango_font_face_get_type())
	TypeFontFamily             = gobject.Type(C.pango_font_family_get_type())
	TypeFontMap                = gobject.Type(C.pango_font_map_get_type())
	TypeFontset                = gobject.Type(C.pango_fontset_get_type())
	TypeFontsetSimple          = gobject.Type(C.pango_fontset_simple_get_type())
	TypeLayout                 = gobject.Type(C.pango_layout_get_type())
	TypeRenderer               = gobject.Type(C.pango_renderer_get_type())
	TypeAttrIterator           = gobject.Type(C.pango_attr_iterator_get_type())
	TypeAttrList               = gobject.Type(C.pango_attr_list_get_type())
	TypeAttribute              = gobject.Type(C.pango_attribute_get_type())
	TypeColor                  = gobject.Type(C.pango_color_get_type())
	TypeFontDescription        = gobject.Type(C.pango_font_description_get_type())
	TypeFontMetrics            = gobject.Type(C.pango_font_metrics_get_type())
	TypeGlyphItem              = gobject.Type(C.pango_glyph_item_get_type())
	TypeGlyphItemIter          = gobject.Type(C.pango_glyph_item_iter_get_type())
	TypeGlyphString            = gobject.Type(C.pango_glyph_string_get_type())
	TypeItem                   = gobject.Type(C.pango_item_get_type())
	TypeLanguage               = gobject.Type(C.pango_language_get_type())
	TypeLayoutIter             = gobject.Type(C.pango_layout_iter_get_type())
	TypeLayoutLine             = gobject.Type(C.pango_layout_line_get_type())
	TypeMatrix                 = gobject.Type(C.pango_matrix_get_type())
	TypeScriptIter             = gobject.Type(C.pango_script_iter_get_type())
	TypeTabArray               = gobject.Type(C.pango_tab_array_get_type())
)

func init() {
	gobject.RegisterGValueMarshalers([]gobject.TypeMarshaler{
		gobject.TypeMarshaler{T: TypeAlignment, F: marshalAlignment},
		gobject.TypeMarshaler{T: TypeAttrType, F: marshalAttrType},
		gobject.TypeMarshaler{T: TypeBaselineShift, F: marshalBaselineShift},
		gobject.TypeMarshaler{T: TypeBidiType, F: marshalBidiType},
		gobject.TypeMarshaler{T: TypeCoverageLevel, F: marshalCoverageLevel},
		gobject.TypeMarshaler{T: TypeDirection, F: marshalDirection},
		gobject.TypeMarshaler{T: TypeEllipsizeMode, F: marshalEllipsizeMode},
		gobject.TypeMarshaler{T: TypeFontScale, F: marshalFontScale},
		gobject.TypeMarshaler{T: TypeGravity, F: marshalGravity},
		gobject.TypeMarshaler{T: TypeGravityHint, F: marshalGravityHint},
		gobject.TypeMarshaler{T: TypeLayoutDeserializeError, F: marshalLayoutDeserializeError},
		gobject.TypeMarshaler{T: TypeOverline, F: marshalOverline},
		gobject.TypeMarshaler{T: TypeRenderPart, F: marshalRenderPart},
		gobject.TypeMarshaler{T: TypeScript, F: marshalScript},
		gobject.TypeMarshaler{T: TypeStretch, F: marshalStretch},
		gobject.TypeMarshaler{T: TypeStyle, F: marshalStyle},
		gobject.TypeMarshaler{T: TypeTabAlign, F: marshalTabAlign},
		gobject.TypeMarshaler{T: TypeTextTransform, F: marshalTextTransform},
		gobject.TypeMarshaler{T: TypeUnderline, F: marshalUnderline},
		gobject.TypeMarshaler{T: TypeVariant, F: marshalVariant},
		gobject.TypeMarshaler{T: TypeWeight, F: marshalWeight},
		gobject.TypeMarshaler{T: TypeWrapMode, F: marshalWrapMode},
		gobject.TypeMarshaler{T: TypeFontMask, F: marshalFontMask},
		gobject.TypeMarshaler{T: TypeLayoutDeserializeFlags, F: marshalLayoutDeserializeFlags},
		gobject.TypeMarshaler{T: TypeLayoutSerializeFlags, F: marshalLayoutSerializeFlags},
		gobject.TypeMarshaler{T: TypeShapeFlags, F: marshalShapeFlags},
		gobject.TypeMarshaler{T: TypeShowFlags, F: marshalShowFlags},
		gobject.TypeMarshaler{T: TypeContext, F: marshalContextInstance},
		gobject.TypeMarshaler{T: TypeCoverage, F: marshalCoverageInstance},
		gobject.TypeMarshaler{T: TypeFont, F: marshalFontInstance},
		gobject.TypeMarshaler{T: TypeFontFace, F: marshalFontFaceInstance},
		gobject.TypeMarshaler{T: TypeFontFamily, F: marshalFontFamilyInstance},
		gobject.TypeMarshaler{T: TypeFontMap, F: marshalFontMapInstance},
		gobject.TypeMarshaler{T: TypeFontset, F: marshalFontsetInstance},
		gobject.TypeMarshaler{T: TypeFontsetSimple, F: marshalFontsetSimpleInstance},
		gobject.TypeMarshaler{T: TypeLayout, F: marshalLayoutInstance},
		gobject.TypeMarshaler{T: TypeRenderer, F: marshalRendererInstance},
		gobject.TypeMarshaler{T: TypeAttrIterator, F: marshalAttrIterator},
		gobject.TypeMarshaler{T: TypeAttrList, F: marshalAttrList},
		gobject.TypeMarshaler{T: TypeAttribute, F: marshalAttribute},
		gobject.TypeMarshaler{T: TypeColor, F: marshalColor},
		gobject.TypeMarshaler{T: TypeFontDescription, F: marshalFontDescription},
		gobject.TypeMarshaler{T: TypeFontMetrics, F: marshalFontMetrics},
		gobject.TypeMarshaler{T: TypeGlyphItem, F: marshalGlyphItem},
		gobject.TypeMarshaler{T: TypeGlyphItemIter, F: marshalGlyphItemIter},
		gobject.TypeMarshaler{T: TypeGlyphString, F: marshalGlyphString},
		gobject.TypeMarshaler{T: TypeItem, F: marshalItem},
		gobject.TypeMarshaler{T: TypeLanguage, F: marshalLanguage},
		gobject.TypeMarshaler{T: TypeLayoutIter, F: marshalLayoutIter},
		gobject.TypeMarshaler{T: TypeLayoutLine, F: marshalLayoutLine},
		gobject.TypeMarshaler{T: TypeMatrix, F: marshalMatrix},
		gobject.TypeMarshaler{T: TypeScriptIter, F: marshalScriptIter},
		gobject.TypeMarshaler{T: TypeTabArray, F: marshalTabArray},
	})
}

// ANALYSIS_FLAG_CENTERED_BASELINE wraps PANGO_ANALYSIS_FLAG_CENTERED_BASELINE
//
// Whether the segment should be shifted to center around the baseline.
// 
// This is mainly used in vertical writing directions.
const ANALYSIS_FLAG_CENTERED_BASELINE = 1
// ANALYSIS_FLAG_IS_ELLIPSIS wraps PANGO_ANALYSIS_FLAG_IS_ELLIPSIS
//
// Whether this run holds ellipsized text.
const ANALYSIS_FLAG_IS_ELLIPSIS = 2
// ANALYSIS_FLAG_NEED_HYPHEN wraps PANGO_ANALYSIS_FLAG_NEED_HYPHEN
//
// Whether to add a hyphen at the end of the run during shaping.
const ANALYSIS_FLAG_NEED_HYPHEN = 4
// ATTR_INDEX_FROM_TEXT_BEGINNING wraps PANGO_ATTR_INDEX_FROM_TEXT_BEGINNING
//
// Value for @start_index in `PangoAttribute` that indicates
// the beginning of the text.
const ATTR_INDEX_FROM_TEXT_BEGINNING = 0
// ATTR_INDEX_TO_TEXT_END wraps PANGO_ATTR_INDEX_TO_TEXT_END
//
// Value for @end_index in `PangoAttribute` that indicates
// the end of the text.
const ATTR_INDEX_TO_TEXT_END = 4294967295
// SCALE wraps PANGO_SCALE
//
// The scale between dimensions used for Pango distances and device units.
// 
// The definition of device units is dependent on the output device; it will
// typically be pixels for a screen, and points for a printer. %PANGO_SCALE is
// currently 1024, but this may be changed in the future.
// 
// When setting font sizes, device units are always considered to be
// points (as in "12 point font"), rather than pixels.
const SCALE = 1024
// VERSION_MAJOR wraps PANGO_VERSION_MAJOR
//
// The major component of the version of Pango available at compile-time.
const VERSION_MAJOR = 1
// VERSION_MICRO wraps PANGO_VERSION_MICRO
//
// The micro component of the version of Pango available at compile-time.
const VERSION_MICRO = 2
// VERSION_MINOR wraps PANGO_VERSION_MINOR
//
// The minor component of the version of Pango available at compile-time.
const VERSION_MINOR = 56
// Glyph wraps PangoGlyph
//
// A `PangoGlyph` represents a single glyph in the output form of a string.
type Glyph = uint32
// GlyphUnit wraps PangoGlyphUnit
//
// The `PangoGlyphUnit` type is used to store dimensions within
// Pango.
// 
// Dimensions are stored in 1/PANGO_SCALE of a device unit.
// (A device unit might be a pixel for screen display, or
// a point on a printer.) PANGO_SCALE is currently 1024, and
// may change in the future (unlikely though), but you should not
// depend on its exact value.
// 
// The PANGO_PIXELS() macro can be used to convert from glyph units
// into device units with correct rounding.
type GlyphUnit = int32
// LayoutRun wraps PangoLayoutRun
//
// A `PangoLayoutRun` represents a single run within a `PangoLayoutLine`.
// 
// It is simply an alternate name for [struct@Pango.GlyphItem].
// See the [struct@Pango.GlyphItem] docs for details on the fields.
type LayoutRun = GlyphItem
// Alignment wraps PangoAlignment
//
// `PangoAlignment` describes how to align the lines of a `PangoLayout`
// within the available space.
// 
// If the `PangoLayout` is set to justify using [method@Pango.Layout.set_justify],
// this only affects partial lines.
// 
// See [method@Pango.Layout.set_auto_dir] for how text direction affects
// the interpretation of `PangoAlignment` values.
type Alignment C.int

const (
	// AlignLeft wraps PANGO_ALIGN_LEFT
	//
	// Put all available space on the right
	AlignLeft Alignment = 0
	// AlignCenter wraps PANGO_ALIGN_CENTER
	//
	// Center the line within the available space
	AlignCenter Alignment = 1
	// AlignRight wraps PANGO_ALIGN_RIGHT
	//
	// Put all available space on the left
	AlignRight Alignment = 2
)

func marshalAlignment(p unsafe.Pointer) (any, error) {
	return Alignment(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Alignment(0)

func (e Alignment) GoValueType() gobject.Type {
	return TypeAlignment
}

func (e Alignment) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Alignment) String() string {
	switch e {
		case AlignCenter: return "AlignCenter"
		case AlignLeft: return "AlignLeft"
		case AlignRight: return "AlignRight"
		default: return fmt.Sprintf("Alignment(%d)", e)
	}
}

// AttrType wraps PangoAttrType
//
// The `PangoAttrType` distinguishes between different types of attributes.
// 
// Along with the predefined values, it is possible to allocate additional
// values for custom attributes using [func@AttrType.register]. The predefined
// values are given below. The type of structure used to store the attribute is
// listed in parentheses after the description.
type AttrType C.int

const (
	// AttrTypeInvalid wraps PANGO_ATTR_TYPE_INVALID
	//
	// does not happen
	AttrTypeInvalid AttrType = 0
	// AttrTypeLanguage wraps PANGO_ATTR_TYPE_LANGUAGE
	//
	// language ([struct@Pango.AttrLanguage])
	AttrTypeLanguage AttrType = 1
	// AttrTypeFamily wraps PANGO_ATTR_TYPE_FAMILY
	//
	// font family name list ([struct@Pango.AttrString])
	AttrTypeFamily AttrType = 2
	// AttrTypeStyle wraps PANGO_ATTR_TYPE_STYLE
	//
	// font slant style ([struct@Pango.AttrInt])
	AttrTypeStyle AttrType = 3
	// AttrTypeWeight wraps PANGO_ATTR_TYPE_WEIGHT
	//
	// font weight ([struct@Pango.AttrInt])
	AttrTypeWeight AttrType = 4
	// AttrTypeVariant wraps PANGO_ATTR_TYPE_VARIANT
	//
	// font variant (normal or small caps) ([struct@Pango.AttrInt])
	AttrTypeVariant AttrType = 5
	// AttrTypeStretch wraps PANGO_ATTR_TYPE_STRETCH
	//
	// font stretch ([struct@Pango.AttrInt])
	AttrTypeStretch AttrType = 6
	// AttrTypeSize wraps PANGO_ATTR_TYPE_SIZE
	//
	// font size in points scaled by %PANGO_SCALE ([struct@Pango.AttrInt])
	AttrTypeSize AttrType = 7
	// AttrTypeFontDesc wraps PANGO_ATTR_TYPE_FONT_DESC
	//
	// font description ([struct@Pango.AttrFontDesc])
	AttrTypeFontDesc AttrType = 8
	// AttrTypeForeground wraps PANGO_ATTR_TYPE_FOREGROUND
	//
	// foreground color ([struct@Pango.AttrColor])
	AttrTypeForeground AttrType = 9
	// AttrTypeBackground wraps PANGO_ATTR_TYPE_BACKGROUND
	//
	// background color ([struct@Pango.AttrColor])
	AttrTypeBackground AttrType = 10
	// AttrTypeUnderline wraps PANGO_ATTR_TYPE_UNDERLINE
	//
	// whether the text has an underline ([struct@Pango.AttrInt])
	AttrTypeUnderline AttrType = 11
	// AttrTypeStrikethrough wraps PANGO_ATTR_TYPE_STRIKETHROUGH
	//
	// whether the text is struck-through ([struct@Pango.AttrInt])
	AttrTypeStrikethrough AttrType = 12
	// AttrTypeRise wraps PANGO_ATTR_TYPE_RISE
	//
	// baseline displacement ([struct@Pango.AttrInt])
	AttrTypeRise AttrType = 13
	// AttrTypeShape wraps PANGO_ATTR_TYPE_SHAPE
	//
	// shape ([struct@Pango.AttrShape])
	AttrTypeShape AttrType = 14
	// AttrTypeScale wraps PANGO_ATTR_TYPE_SCALE
	//
	// font size scale factor ([struct@Pango.AttrFloat])
	AttrTypeScale AttrType = 15
	// AttrTypeFallback wraps PANGO_ATTR_TYPE_FALLBACK
	//
	// whether fallback is enabled ([struct@Pango.AttrInt])
	AttrTypeFallback AttrType = 16
	// AttrTypeLetterSpacing wraps PANGO_ATTR_TYPE_LETTER_SPACING
	//
	// letter spacing ([struct@PangoAttrInt])
	AttrTypeLetterSpacing AttrType = 17
	// AttrTypeUnderlineColor wraps PANGO_ATTR_TYPE_UNDERLINE_COLOR
	//
	// underline color ([struct@Pango.AttrColor])
	AttrTypeUnderlineColor AttrType = 18
	// AttrTypeStrikethroughColor wraps PANGO_ATTR_TYPE_STRIKETHROUGH_COLOR
	//
	// strikethrough color ([struct@Pango.AttrColor])
	AttrTypeStrikethroughColor AttrType = 19
	// AttrTypeAbsoluteSize wraps PANGO_ATTR_TYPE_ABSOLUTE_SIZE
	//
	// font size in pixels scaled by %PANGO_SCALE ([struct@Pango.AttrInt])
	AttrTypeAbsoluteSize AttrType = 20
	// AttrTypeGravity wraps PANGO_ATTR_TYPE_GRAVITY
	//
	// base text gravity ([struct@Pango.AttrInt])
	AttrTypeGravity AttrType = 21
	// AttrTypeGravityHint wraps PANGO_ATTR_TYPE_GRAVITY_HINT
	//
	// gravity hint ([struct@Pango.AttrInt])
	AttrTypeGravityHint AttrType = 22
	// AttrTypeFontFeatures wraps PANGO_ATTR_TYPE_FONT_FEATURES
	//
	// OpenType font features ([struct@Pango.AttrFontFeatures]). Since 1.38
	AttrTypeFontFeatures AttrType = 23
	// AttrTypeForegroundAlpha wraps PANGO_ATTR_TYPE_FOREGROUND_ALPHA
	//
	// foreground alpha ([struct@Pango.AttrInt]). Since 1.38
	AttrTypeForegroundAlpha AttrType = 24
	// AttrTypeBackgroundAlpha wraps PANGO_ATTR_TYPE_BACKGROUND_ALPHA
	//
	// background alpha ([struct@Pango.AttrInt]). Since 1.38
	AttrTypeBackgroundAlpha AttrType = 25
	// AttrTypeAllowBreaks wraps PANGO_ATTR_TYPE_ALLOW_BREAKS
	//
	// whether breaks are allowed ([struct@Pango.AttrInt]). Since 1.44
	AttrTypeAllowBreaks AttrType = 26
	// AttrTypeShow wraps PANGO_ATTR_TYPE_SHOW
	//
	// how to render invisible characters ([struct@Pango.AttrInt]). Since 1.44
	AttrTypeShow AttrType = 27
	// AttrTypeInsertHyphens wraps PANGO_ATTR_TYPE_INSERT_HYPHENS
	//
	// whether to insert hyphens at intra-word line breaks ([struct@Pango.AttrInt]). Since 1.44
	AttrTypeInsertHyphens AttrType = 28
	// AttrTypeOverline wraps PANGO_ATTR_TYPE_OVERLINE
	//
	// whether the text has an overline ([struct@Pango.AttrInt]). Since 1.46
	AttrTypeOverline AttrType = 29
	// AttrTypeOverlineColor wraps PANGO_ATTR_TYPE_OVERLINE_COLOR
	//
	// overline color ([struct@Pango.AttrColor]). Since 1.46
	AttrTypeOverlineColor AttrType = 30
	// AttrTypeLineHeight wraps PANGO_ATTR_TYPE_LINE_HEIGHT
	//
	// line height factor ([struct@Pango.AttrFloat]). Since: 1.50
	AttrTypeLineHeight AttrType = 31
	// AttrTypeAbsoluteLineHeight wraps PANGO_ATTR_TYPE_ABSOLUTE_LINE_HEIGHT
	//
	// line height ([struct@Pango.AttrInt]). Since: 1.50
	AttrTypeAbsoluteLineHeight AttrType = 32
	// AttrTypeTextTransform wraps PANGO_ATTR_TYPE_TEXT_TRANSFORM
	AttrTypeTextTransform AttrType = 33
	// AttrTypeWord wraps PANGO_ATTR_TYPE_WORD
	//
	// override segmentation to classify the range of the attribute as a single word ([struct@Pango.AttrInt]). Since 1.50
	AttrTypeWord AttrType = 34
	// AttrTypeSentence wraps PANGO_ATTR_TYPE_SENTENCE
	//
	// override segmentation to classify the range of the attribute as a single sentence ([struct@Pango.AttrInt]). Since 1.50
	AttrTypeSentence AttrType = 35
	// AttrTypeBaselineShift wraps PANGO_ATTR_TYPE_BASELINE_SHIFT
	//
	// baseline displacement ([struct@Pango.AttrInt]). Since 1.50
	AttrTypeBaselineShift AttrType = 36
	// AttrTypeFontScale wraps PANGO_ATTR_TYPE_FONT_SCALE
	//
	// font-relative size change ([struct@Pango.AttrInt]). Since 1.50
	AttrTypeFontScale AttrType = 37
)

func marshalAttrType(p unsafe.Pointer) (any, error) {
	return AttrType(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = AttrType(0)

func (e AttrType) GoValueType() gobject.Type {
	return TypeAttrType
}

func (e AttrType) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e AttrType) String() string {
	switch e {
		case AttrTypeAbsoluteLineHeight: return "AttrTypeAbsoluteLineHeight"
		case AttrTypeAbsoluteSize: return "AttrTypeAbsoluteSize"
		case AttrTypeAllowBreaks: return "AttrTypeAllowBreaks"
		case AttrTypeBackground: return "AttrTypeBackground"
		case AttrTypeBackgroundAlpha: return "AttrTypeBackgroundAlpha"
		case AttrTypeBaselineShift: return "AttrTypeBaselineShift"
		case AttrTypeFallback: return "AttrTypeFallback"
		case AttrTypeFamily: return "AttrTypeFamily"
		case AttrTypeFontDesc: return "AttrTypeFontDesc"
		case AttrTypeFontFeatures: return "AttrTypeFontFeatures"
		case AttrTypeFontScale: return "AttrTypeFontScale"
		case AttrTypeForeground: return "AttrTypeForeground"
		case AttrTypeForegroundAlpha: return "AttrTypeForegroundAlpha"
		case AttrTypeGravity: return "AttrTypeGravity"
		case AttrTypeGravityHint: return "AttrTypeGravityHint"
		case AttrTypeInsertHyphens: return "AttrTypeInsertHyphens"
		case AttrTypeInvalid: return "AttrTypeInvalid"
		case AttrTypeLanguage: return "AttrTypeLanguage"
		case AttrTypeLetterSpacing: return "AttrTypeLetterSpacing"
		case AttrTypeLineHeight: return "AttrTypeLineHeight"
		case AttrTypeOverline: return "AttrTypeOverline"
		case AttrTypeOverlineColor: return "AttrTypeOverlineColor"
		case AttrTypeRise: return "AttrTypeRise"
		case AttrTypeScale: return "AttrTypeScale"
		case AttrTypeSentence: return "AttrTypeSentence"
		case AttrTypeShape: return "AttrTypeShape"
		case AttrTypeShow: return "AttrTypeShow"
		case AttrTypeSize: return "AttrTypeSize"
		case AttrTypeStretch: return "AttrTypeStretch"
		case AttrTypeStrikethrough: return "AttrTypeStrikethrough"
		case AttrTypeStrikethroughColor: return "AttrTypeStrikethroughColor"
		case AttrTypeStyle: return "AttrTypeStyle"
		case AttrTypeTextTransform: return "AttrTypeTextTransform"
		case AttrTypeUnderline: return "AttrTypeUnderline"
		case AttrTypeUnderlineColor: return "AttrTypeUnderlineColor"
		case AttrTypeVariant: return "AttrTypeVariant"
		case AttrTypeWeight: return "AttrTypeWeight"
		case AttrTypeWord: return "AttrTypeWord"
		default: return fmt.Sprintf("AttrType(%d)", e)
	}
}

// AttrTypeGetName wraps pango_attr_type_get_name
// 
// The function takes the following parameters:
// 
// 	- typ AttrType: an attribute type ID to fetch the name for 
// 
// The function returns the following values:
// 
// 	- goret string (nullable) 
//
// Fetches the attribute type name.
// 
// The attribute type name is the string passed in
// when registering the type using
// [func@Pango.AttrType.register].
// 
// The returned value is an interned string (see
// g_intern_string() for what that means) that should
// not be modified or freed.
func AttrTypeGetName(typ AttrType) string {
	var carg1 C.PangoAttrType // in, none, casted
	var cret  *C.char         // return, none, string, casted *C.gchar, nullable

	carg1 = C.PangoAttrType(typ)

	cret = C.pango_attr_type_get_name(carg1)
	runtime.KeepAlive(typ)

	var goret string

	if cret != nil {
		goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	}

	return goret
}

// AttrTypeRegister wraps pango_attr_type_register
// 
// The function takes the following parameters:
// 
// 	- name string: an identifier for the type 
// 
// The function returns the following values:
// 
// 	- goret AttrType 
//
// Allocate a new attribute type ID.
// 
// The attribute type name can be accessed later
// by using [func@Pango.AttrType.get_name].
func AttrTypeRegister(name string) AttrType {
	var carg1 *C.char         // in, none, string, casted *C.gchar
	var cret  C.PangoAttrType // return, none, casted

	carg1 = (*C.char)(unsafe.Pointer(C.CString(name)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_attr_type_register(carg1)
	runtime.KeepAlive(name)

	var goret AttrType

	goret = AttrType(cret)

	return goret
}

// BaselineShift wraps PangoBaselineShift
//
// An enumeration that affects baseline shifts between runs.
type BaselineShift C.int

const (
	// BaselineShiftNone wraps PANGO_BASELINE_SHIFT_NONE
	//
	// Leave the baseline unchanged
	BaselineShiftNone BaselineShift = 0
	// BaselineShiftSuperscript wraps PANGO_BASELINE_SHIFT_SUPERSCRIPT
	//
	// Shift the baseline to the superscript position,
	//   relative to the previous run
	BaselineShiftSuperscript BaselineShift = 1
	// BaselineShiftSubscript wraps PANGO_BASELINE_SHIFT_SUBSCRIPT
	//
	// Shift the baseline to the subscript position,
	//   relative to the previous run
	BaselineShiftSubscript BaselineShift = 2
)

func marshalBaselineShift(p unsafe.Pointer) (any, error) {
	return BaselineShift(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = BaselineShift(0)

func (e BaselineShift) GoValueType() gobject.Type {
	return TypeBaselineShift
}

func (e BaselineShift) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e BaselineShift) String() string {
	switch e {
		case BaselineShiftNone: return "BaselineShiftNone"
		case BaselineShiftSubscript: return "BaselineShiftSubscript"
		case BaselineShiftSuperscript: return "BaselineShiftSuperscript"
		default: return fmt.Sprintf("BaselineShift(%d)", e)
	}
}

// BidiType wraps PangoBidiType
//
// `PangoBidiType` represents the bidirectional character
// type of a Unicode character.
// 
// The values in this enumeration are specified by the
// [Unicode bidirectional algorithm](http://www.unicode.org/reports/tr9/).
//
// Deprecated: (since 1.44.0) Use fribidi for this information
type BidiType C.int

const (
	// BidiTypeL wraps PANGO_BIDI_TYPE_L
	//
	// Left-to-Right
	BidiTypeL BidiType = 0
	// BidiTypeLre wraps PANGO_BIDI_TYPE_LRE
	//
	// Left-to-Right Embedding
	BidiTypeLre BidiType = 1
	// BidiTypeLro wraps PANGO_BIDI_TYPE_LRO
	//
	// Left-to-Right Override
	BidiTypeLro BidiType = 2
	// BidiTypeR wraps PANGO_BIDI_TYPE_R
	//
	// Right-to-Left
	BidiTypeR BidiType = 3
	// BidiTypeAl wraps PANGO_BIDI_TYPE_AL
	//
	// Right-to-Left Arabic
	BidiTypeAl BidiType = 4
	// BidiTypeRLE wraps PANGO_BIDI_TYPE_RLE
	//
	// Right-to-Left Embedding
	BidiTypeRLE BidiType = 5
	// BidiTypeRlo wraps PANGO_BIDI_TYPE_RLO
	//
	// Right-to-Left Override
	BidiTypeRlo BidiType = 6
	// BidiTypePDF wraps PANGO_BIDI_TYPE_PDF
	//
	// Pop Directional Format
	BidiTypePDF BidiType = 7
	// BidiTypeEn wraps PANGO_BIDI_TYPE_EN
	//
	// European Number
	BidiTypeEn BidiType = 8
	// BidiTypeES wraps PANGO_BIDI_TYPE_ES
	//
	// European Number Separator
	BidiTypeES BidiType = 9
	// BidiTypeEt wraps PANGO_BIDI_TYPE_ET
	//
	// European Number Terminator
	BidiTypeEt BidiType = 10
	// BidiTypeAn wraps PANGO_BIDI_TYPE_AN
	//
	// Arabic Number
	BidiTypeAn BidiType = 11
	// BidiTypeCs wraps PANGO_BIDI_TYPE_CS
	//
	// Common Number Separator
	BidiTypeCs BidiType = 12
	// BidiTypeNsm wraps PANGO_BIDI_TYPE_NSM
	//
	// Nonspacing Mark
	BidiTypeNsm BidiType = 13
	// BidiTypeBn wraps PANGO_BIDI_TYPE_BN
	//
	// Boundary Neutral
	BidiTypeBn BidiType = 14
	// BidiTypeB wraps PANGO_BIDI_TYPE_B
	//
	// Paragraph Separator
	BidiTypeB BidiType = 15
	// BidiTypeS wraps PANGO_BIDI_TYPE_S
	//
	// Segment Separator
	BidiTypeS BidiType = 16
	// BidiTypeWs wraps PANGO_BIDI_TYPE_WS
	//
	// Whitespace
	BidiTypeWs BidiType = 17
	// BidiTypeOn wraps PANGO_BIDI_TYPE_ON
	//
	// Other Neutrals
	BidiTypeOn BidiType = 18
	// BidiTypeLri wraps PANGO_BIDI_TYPE_LRI
	//
	// Left-to-Right isolate. Since 1.48.6
	BidiTypeLri BidiType = 19
	// BidiTypeRli wraps PANGO_BIDI_TYPE_RLI
	//
	// Right-to-Left isolate. Since 1.48.6
	BidiTypeRli BidiType = 20
	// BidiTypeFsi wraps PANGO_BIDI_TYPE_FSI
	//
	// First strong isolate. Since 1.48.6
	BidiTypeFsi BidiType = 21
	// BidiTypePdi wraps PANGO_BIDI_TYPE_PDI
	//
	// Pop directional isolate. Since 1.48.6
	BidiTypePdi BidiType = 22
)

func marshalBidiType(p unsafe.Pointer) (any, error) {
	return BidiType(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = BidiType(0)

func (e BidiType) GoValueType() gobject.Type {
	return TypeBidiType
}

func (e BidiType) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e BidiType) String() string {
	switch e {
		case BidiTypeAl: return "BidiTypeAl"
		case BidiTypeAn: return "BidiTypeAn"
		case BidiTypeB: return "BidiTypeB"
		case BidiTypeBn: return "BidiTypeBn"
		case BidiTypeCs: return "BidiTypeCs"
		case BidiTypeES: return "BidiTypeES"
		case BidiTypeEn: return "BidiTypeEn"
		case BidiTypeEt: return "BidiTypeEt"
		case BidiTypeFsi: return "BidiTypeFsi"
		case BidiTypeL: return "BidiTypeL"
		case BidiTypeLre: return "BidiTypeLre"
		case BidiTypeLri: return "BidiTypeLri"
		case BidiTypeLro: return "BidiTypeLro"
		case BidiTypeNsm: return "BidiTypeNsm"
		case BidiTypeOn: return "BidiTypeOn"
		case BidiTypePDF: return "BidiTypePDF"
		case BidiTypePdi: return "BidiTypePdi"
		case BidiTypeR: return "BidiTypeR"
		case BidiTypeRLE: return "BidiTypeRLE"
		case BidiTypeRli: return "BidiTypeRli"
		case BidiTypeRlo: return "BidiTypeRlo"
		case BidiTypeS: return "BidiTypeS"
		case BidiTypeWs: return "BidiTypeWs"
		default: return fmt.Sprintf("BidiType(%d)", e)
	}
}

// BidiTypeForUnichar wraps pango_bidi_type_for_unichar
// 
// The function takes the following parameters:
// 
// 	- ch uint32: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret BidiType 
//
// Determines the bidirectional type of a character.
// 
// The bidirectional type is specified in the Unicode Character Database.
// 
// A simplified version of this function is available as [func@unichar_direction].
func BidiTypeForUnichar(ch uint32) BidiType {
	var carg1 C.gunichar      // in, none, casted
	var cret  C.PangoBidiType // return, none, casted

	carg1 = C.gunichar(ch)

	cret = C.pango_bidi_type_for_unichar(carg1)
	runtime.KeepAlive(ch)

	var goret BidiType

	goret = BidiType(cret)

	return goret
}

// CoverageLevel wraps PangoCoverageLevel
//
// `PangoCoverageLevel` is used to indicate how well a font can
// represent a particular Unicode character for a particular script.
// 
// Since 1.44, only %PANGO_COVERAGE_NONE and %PANGO_COVERAGE_EXACT
// will be returned.
type CoverageLevel C.int

const (
	// CoverageNone wraps PANGO_COVERAGE_NONE
	//
	// The character is not representable with
	//   the font.
	CoverageNone CoverageLevel = 0
	// CoverageFallback wraps PANGO_COVERAGE_FALLBACK
	//
	// The character is represented in a
	//   way that may be comprehensible but is not the correct
	//   graphical form. For instance, a Hangul character represented
	//   as a a sequence of Jamos, or a Latin transliteration of a
	//   Cyrillic word.
	CoverageFallback CoverageLevel = 1
	// CoverageApproximate wraps PANGO_COVERAGE_APPROXIMATE
	//
	// The character is represented as
	//   basically the correct graphical form, but with a stylistic
	//   variant inappropriate for the current script.
	CoverageApproximate CoverageLevel = 2
	// CoverageExact wraps PANGO_COVERAGE_EXACT
	//
	// The character is represented as the
	//   correct graphical form.
	CoverageExact CoverageLevel = 3
)

func marshalCoverageLevel(p unsafe.Pointer) (any, error) {
	return CoverageLevel(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = CoverageLevel(0)

func (e CoverageLevel) GoValueType() gobject.Type {
	return TypeCoverageLevel
}

func (e CoverageLevel) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e CoverageLevel) String() string {
	switch e {
		case CoverageApproximate: return "CoverageApproximate"
		case CoverageExact: return "CoverageExact"
		case CoverageFallback: return "CoverageFallback"
		case CoverageNone: return "CoverageNone"
		default: return fmt.Sprintf("CoverageLevel(%d)", e)
	}
}

// Direction wraps PangoDirection
//
// `PangoDirection` represents a direction in the Unicode bidirectional
// algorithm.
// 
// Not every value in this enumeration makes sense for every usage of
// `PangoDirection`; for example, the return value of [func@unichar_direction]
// and [func@find_base_dir] cannot be `PANGO_DIRECTION_WEAK_LTR` or
// `PANGO_DIRECTION_WEAK_RTL`, since every character is either neutral
// or has a strong direction; on the other hand `PANGO_DIRECTION_NEUTRAL`
// doesn't make sense to pass to [func@itemize_with_base_dir].
// 
// The `PANGO_DIRECTION_TTB_LTR`, `PANGO_DIRECTION_TTB_RTL` values come from
// an earlier interpretation of this enumeration as the writing direction
// of a block of text and are no longer used. See `PangoGravity` for how
// vertical text is handled in Pango.
// 
// If you are interested in text direction, you should really use fribidi
// directly. `PangoDirection` is only retained because it is used in some
// public apis.
type Direction C.int

const (
	// DirectionLTR wraps PANGO_DIRECTION_LTR
	//
	// A strong left-to-right direction
	DirectionLTR Direction = 0
	// DirectionRTL wraps PANGO_DIRECTION_RTL
	//
	// A strong right-to-left direction
	DirectionRTL Direction = 1
	// DirectionTtbLTR wraps PANGO_DIRECTION_TTB_LTR
	//
	// Deprecated value; treated the
	//   same as `PANGO_DIRECTION_RTL`.
	DirectionTtbLTR Direction = 2
	// DirectionTtbRTL wraps PANGO_DIRECTION_TTB_RTL
	//
	// Deprecated value; treated the
	//   same as `PANGO_DIRECTION_LTR`
	DirectionTtbRTL Direction = 3
	// DirectionWeakLTR wraps PANGO_DIRECTION_WEAK_LTR
	//
	// A weak left-to-right direction
	DirectionWeakLTR Direction = 4
	// DirectionWeakRTL wraps PANGO_DIRECTION_WEAK_RTL
	//
	// A weak right-to-left direction
	DirectionWeakRTL Direction = 5
	// DirectionNeutral wraps PANGO_DIRECTION_NEUTRAL
	//
	// No direction specified
	DirectionNeutral Direction = 6
)

func marshalDirection(p unsafe.Pointer) (any, error) {
	return Direction(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Direction(0)

func (e Direction) GoValueType() gobject.Type {
	return TypeDirection
}

func (e Direction) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Direction) String() string {
	switch e {
		case DirectionLTR: return "DirectionLTR"
		case DirectionNeutral: return "DirectionNeutral"
		case DirectionRTL: return "DirectionRTL"
		case DirectionTtbLTR: return "DirectionTtbLTR"
		case DirectionTtbRTL: return "DirectionTtbRTL"
		case DirectionWeakLTR: return "DirectionWeakLTR"
		case DirectionWeakRTL: return "DirectionWeakRTL"
		default: return fmt.Sprintf("Direction(%d)", e)
	}
}

// EllipsizeMode wraps PangoEllipsizeMode
//
// `PangoEllipsizeMode` describes what sort of ellipsization
// should be applied to text.
// 
// In the ellipsization process characters are removed from the
// text in order to make it fit to a given width and replaced
// with an ellipsis.
type EllipsizeMode C.int

const (
	// EllipsizeNone wraps PANGO_ELLIPSIZE_NONE
	//
	// No ellipsization
	EllipsizeNone EllipsizeMode = 0
	// EllipsizeStart wraps PANGO_ELLIPSIZE_START
	//
	// Omit characters at the start of the text
	EllipsizeStart EllipsizeMode = 1
	// EllipsizeMiddle wraps PANGO_ELLIPSIZE_MIDDLE
	//
	// Omit characters in the middle of the text
	EllipsizeMiddle EllipsizeMode = 2
	// EllipsizeEnd wraps PANGO_ELLIPSIZE_END
	//
	// Omit characters at the end of the text
	EllipsizeEnd EllipsizeMode = 3
)

func marshalEllipsizeMode(p unsafe.Pointer) (any, error) {
	return EllipsizeMode(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = EllipsizeMode(0)

func (e EllipsizeMode) GoValueType() gobject.Type {
	return TypeEllipsizeMode
}

func (e EllipsizeMode) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e EllipsizeMode) String() string {
	switch e {
		case EllipsizeEnd: return "EllipsizeEnd"
		case EllipsizeMiddle: return "EllipsizeMiddle"
		case EllipsizeNone: return "EllipsizeNone"
		case EllipsizeStart: return "EllipsizeStart"
		default: return fmt.Sprintf("EllipsizeMode(%d)", e)
	}
}

// FontScale wraps PangoFontScale
//
// An enumeration that affects font sizes for superscript
// and subscript positioning and for (emulated) Small Caps.
type FontScale C.int

const (
	// FontScaleNone wraps PANGO_FONT_SCALE_NONE
	//
	// Leave the font size unchanged
	FontScaleNone FontScale = 0
	// FontScaleSuperscript wraps PANGO_FONT_SCALE_SUPERSCRIPT
	//
	// Change the font to a size suitable for superscripts
	FontScaleSuperscript FontScale = 1
	// FontScaleSubscript wraps PANGO_FONT_SCALE_SUBSCRIPT
	//
	// Change the font to a size suitable for subscripts
	FontScaleSubscript FontScale = 2
	// FontScaleSmallCaps wraps PANGO_FONT_SCALE_SMALL_CAPS
	//
	// Change the font to a size suitable for Small Caps
	FontScaleSmallCaps FontScale = 3
)

func marshalFontScale(p unsafe.Pointer) (any, error) {
	return FontScale(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = FontScale(0)

func (e FontScale) GoValueType() gobject.Type {
	return TypeFontScale
}

func (e FontScale) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e FontScale) String() string {
	switch e {
		case FontScaleNone: return "FontScaleNone"
		case FontScaleSmallCaps: return "FontScaleSmallCaps"
		case FontScaleSubscript: return "FontScaleSubscript"
		case FontScaleSuperscript: return "FontScaleSuperscript"
		default: return fmt.Sprintf("FontScale(%d)", e)
	}
}

// Gravity wraps PangoGravity
//
// `PangoGravity` represents the orientation of glyphs in a segment
// of text.
// 
// This is useful when rendering vertical text layouts. In those situations,
// the layout is rotated using a non-identity [struct@Pango.Matrix], and then
// glyph orientation is controlled using `PangoGravity`.
// 
// Not every value in this enumeration makes sense for every usage of
// `PangoGravity`; for example, %PANGO_GRAVITY_AUTO only can be passed to
// [method@Pango.Context.set_base_gravity] and can only be returned by
// [method@Pango.Context.get_base_gravity].
// 
// See also: [enum@Pango.GravityHint]
type Gravity C.int

const (
	// GravitySouth wraps PANGO_GRAVITY_SOUTH
	//
	// Glyphs stand upright (default) &lt;img align="right" valign="center" src="m-south.png"&gt;
	GravitySouth Gravity = 0
	// GravityEast wraps PANGO_GRAVITY_EAST
	//
	// Glyphs are rotated 90 degrees counter-clockwise. &lt;img align="right" valign="center" src="m-east.png"&gt;
	GravityEast Gravity = 1
	// GravityNorth wraps PANGO_GRAVITY_NORTH
	//
	// Glyphs are upside-down. &lt;img align="right" valign="cener" src="m-north.png"&gt;
	GravityNorth Gravity = 2
	// GravityWest wraps PANGO_GRAVITY_WEST
	//
	// Glyphs are rotated 90 degrees clockwise. &lt;img align="right" valign="center" src="m-west.png"&gt;
	GravityWest Gravity = 3
	// GravityAuto wraps PANGO_GRAVITY_AUTO
	//
	// Gravity is resolved from the context matrix
	GravityAuto Gravity = 4
)

func marshalGravity(p unsafe.Pointer) (any, error) {
	return Gravity(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Gravity(0)

func (e Gravity) GoValueType() gobject.Type {
	return TypeGravity
}

func (e Gravity) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Gravity) String() string {
	switch e {
		case GravityAuto: return "GravityAuto"
		case GravityEast: return "GravityEast"
		case GravityNorth: return "GravityNorth"
		case GravitySouth: return "GravitySouth"
		case GravityWest: return "GravityWest"
		default: return fmt.Sprintf("Gravity(%d)", e)
	}
}

// GravityGetForMatrix wraps pango_gravity_get_for_matrix
// 
// The function takes the following parameters:
// 
// 	- matrix *Matrix (nullable): a `PangoMatrix` 
// 
// The function returns the following values:
// 
// 	- goret Gravity 
//
// Finds the gravity that best matches the rotation component
// in a `PangoMatrix`.
func GravityGetForMatrix(matrix *Matrix) Gravity {
	var carg1 *C.PangoMatrix // in, none, converted, nullable
	var cret  C.PangoGravity // return, none, casted

	if matrix != nil {
		carg1 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	}

	cret = C.pango_gravity_get_for_matrix(carg1)
	runtime.KeepAlive(matrix)

	var goret Gravity

	goret = Gravity(cret)

	return goret
}

// GravityGetForScript wraps pango_gravity_get_for_script
// 
// The function takes the following parameters:
// 
// 	- script Script: `PangoScript` to query 
// 	- baseGravity Gravity: base gravity of the paragraph 
// 	- hint GravityHint: orientation hint 
// 
// The function returns the following values:
// 
// 	- goret Gravity 
//
// Returns the gravity to use in laying out a `PangoItem`.
// 
// The gravity is determined based on the script, base gravity, and hint.
// 
// If @base_gravity is %PANGO_GRAVITY_AUTO, it is first replaced with the
// preferred gravity of @script.  To get the preferred gravity of a script,
// pass %PANGO_GRAVITY_AUTO and %PANGO_GRAVITY_HINT_STRONG in.
func GravityGetForScript(script Script, baseGravity Gravity, hint GravityHint) Gravity {
	var carg1 C.PangoScript      // in, none, casted
	var carg2 C.PangoGravity     // in, none, casted
	var carg3 C.PangoGravityHint // in, none, casted
	var cret  C.PangoGravity     // return, none, casted

	carg1 = C.PangoScript(script)
	carg2 = C.PangoGravity(baseGravity)
	carg3 = C.PangoGravityHint(hint)

	cret = C.pango_gravity_get_for_script(carg1, carg2, carg3)
	runtime.KeepAlive(script)
	runtime.KeepAlive(baseGravity)
	runtime.KeepAlive(hint)

	var goret Gravity

	goret = Gravity(cret)

	return goret
}

// GravityGetForScriptAndWidth wraps pango_gravity_get_for_script_and_width
// 
// The function takes the following parameters:
// 
// 	- script Script: `PangoScript` to query 
// 	- wide bool: %TRUE for wide characters as returned by g_unichar_iswide() 
// 	- baseGravity Gravity: base gravity of the paragraph 
// 	- hint GravityHint: orientation hint 
// 
// The function returns the following values:
// 
// 	- goret Gravity 
//
// Returns the gravity to use in laying out a single character
// or `PangoItem`.
// 
// The gravity is determined based on the script, East Asian width,
// base gravity, and hint,
// 
// This function is similar to [func@Pango.Gravity.get_for_script] except
// that this function makes a distinction between narrow/half-width and
// wide/full-width characters also. Wide/full-width characters always
// stand *upright*, that is, they always take the base gravity,
// whereas narrow/full-width characters are always rotated in vertical
// context.
// 
// If @base_gravity is %PANGO_GRAVITY_AUTO, it is first replaced with the
// preferred gravity of @script.
func GravityGetForScriptAndWidth(script Script, wide bool, baseGravity Gravity, hint GravityHint) Gravity {
	var carg1 C.PangoScript      // in, none, casted
	var carg2 C.gboolean         // in
	var carg3 C.PangoGravity     // in, none, casted
	var carg4 C.PangoGravityHint // in, none, casted
	var cret  C.PangoGravity     // return, none, casted

	carg1 = C.PangoScript(script)
	if wide {
		carg2 = C.TRUE
	}
	carg3 = C.PangoGravity(baseGravity)
	carg4 = C.PangoGravityHint(hint)

	cret = C.pango_gravity_get_for_script_and_width(carg1, carg2, carg3, carg4)
	runtime.KeepAlive(script)
	runtime.KeepAlive(wide)
	runtime.KeepAlive(baseGravity)
	runtime.KeepAlive(hint)

	var goret Gravity

	goret = Gravity(cret)

	return goret
}

// GravityToRotation wraps pango_gravity_to_rotation
// 
// The function takes the following parameters:
// 
// 	- gravity Gravity: gravity to query, should not be %PANGO_GRAVITY_AUTO 
// 
// The function returns the following values:
// 
// 	- goret float64 
//
// Converts a `PangoGravity` value to its natural rotation in radians.
// 
// Note that [method@Pango.Matrix.rotate] takes angle in degrees, not radians.
// So, to call [method@Pango.Matrix,rotate] with the output of this function
// you should multiply it by (180. / G_PI).
func GravityToRotation(gravity Gravity) float64 {
	var carg1 C.PangoGravity // in, none, casted
	var cret  C.double       // return, none, casted, casted C.gdouble

	carg1 = C.PangoGravity(gravity)

	cret = C.pango_gravity_to_rotation(carg1)
	runtime.KeepAlive(gravity)

	var goret float64

	goret = float64(cret)

	return goret
}

// GravityHint wraps PangoGravityHint
//
// `PangoGravityHint` defines how horizontal scripts should behave in a
// vertical context.
// 
// That is, English excerpts in a vertical paragraph for example.
// 
// See also [enum@Pango.Gravity]
type GravityHint C.int

const (
	// GravityHintNatural wraps PANGO_GRAVITY_HINT_NATURAL
	//
	// scripts will take their natural gravity based
	//   on the base gravity and the script.  This is the default.
	GravityHintNatural GravityHint = 0
	// GravityHintStrong wraps PANGO_GRAVITY_HINT_STRONG
	//
	// always use the base gravity set, regardless of
	//   the script.
	GravityHintStrong GravityHint = 1
	// GravityHintLine wraps PANGO_GRAVITY_HINT_LINE
	//
	// for scripts not in their natural direction (eg.
	//   Latin in East gravity), choose per-script gravity such that every script
	//   respects the line progression. This means, Latin and Arabic will take
	//   opposite gravities and both flow top-to-bottom for example.
	GravityHintLine GravityHint = 2
)

func marshalGravityHint(p unsafe.Pointer) (any, error) {
	return GravityHint(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = GravityHint(0)

func (e GravityHint) GoValueType() gobject.Type {
	return TypeGravityHint
}

func (e GravityHint) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e GravityHint) String() string {
	switch e {
		case GravityHintLine: return "GravityHintLine"
		case GravityHintNatural: return "GravityHintNatural"
		case GravityHintStrong: return "GravityHintStrong"
		default: return fmt.Sprintf("GravityHint(%d)", e)
	}
}

// LayoutDeserializeError wraps PangoLayoutDeserializeError
//
// Errors that can be returned by [func@Pango.Layout.deserialize].
type LayoutDeserializeError C.int

const (
	// LayoutDeserializeInvalid wraps PANGO_LAYOUT_DESERIALIZE_INVALID
	//
	// Unspecified error
	LayoutDeserializeInvalid LayoutDeserializeError = 0
	// LayoutDeserializeInvalidValue wraps PANGO_LAYOUT_DESERIALIZE_INVALID_VALUE
	//
	// A JSon value could not be
	//   interpreted
	LayoutDeserializeInvalidValue LayoutDeserializeError = 1
	// LayoutDeserializeMissingValue wraps PANGO_LAYOUT_DESERIALIZE_MISSING_VALUE
	//
	// A required JSon member was
	//   not found
	LayoutDeserializeMissingValue LayoutDeserializeError = 2
)

func marshalLayoutDeserializeError(p unsafe.Pointer) (any, error) {
	return LayoutDeserializeError(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = LayoutDeserializeError(0)

func (e LayoutDeserializeError) GoValueType() gobject.Type {
	return TypeLayoutDeserializeError
}

func (e LayoutDeserializeError) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e LayoutDeserializeError) String() string {
	switch e {
		case LayoutDeserializeInvalid: return "LayoutDeserializeInvalid"
		case LayoutDeserializeInvalidValue: return "LayoutDeserializeInvalidValue"
		case LayoutDeserializeMissingValue: return "LayoutDeserializeMissingValue"
		default: return fmt.Sprintf("LayoutDeserializeError(%d)", e)
	}
}

// LayoutDeserializeErrorQuark wraps pango_layout_deserialize_error_quark
// 
// The function returns the following values:
// 
// 	- goret glib.Quark 
func LayoutDeserializeErrorQuark() glib.Quark {
	var cret C.GQuark // return, none, casted, alias

	cret = C.pango_layout_deserialize_error_quark()

	var goret glib.Quark

	goret = glib.Quark(cret)

	return goret
}

// Overline wraps PangoOverline
//
// The `PangoOverline` enumeration is used to specify whether text
// should be overlined, and if so, the type of line.
type Overline C.int

const (
	// OverlineNone wraps PANGO_OVERLINE_NONE
	//
	// no overline should be drawn
	OverlineNone Overline = 0
	// OverlineSingle wraps PANGO_OVERLINE_SINGLE
	//
	// Draw a single line above the ink
	//   extents of the text being underlined.
	OverlineSingle Overline = 1
)

func marshalOverline(p unsafe.Pointer) (any, error) {
	return Overline(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Overline(0)

func (e Overline) GoValueType() gobject.Type {
	return TypeOverline
}

func (e Overline) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Overline) String() string {
	switch e {
		case OverlineNone: return "OverlineNone"
		case OverlineSingle: return "OverlineSingle"
		default: return fmt.Sprintf("Overline(%d)", e)
	}
}

// RenderPart wraps PangoRenderPart
//
// `PangoRenderPart` defines different items to render for such
// purposes as setting colors.
type RenderPart C.int

const (
	// RenderPartForeground wraps PANGO_RENDER_PART_FOREGROUND
	//
	// the text itself
	RenderPartForeground RenderPart = 0
	// RenderPartBackground wraps PANGO_RENDER_PART_BACKGROUND
	//
	// the area behind the text
	RenderPartBackground RenderPart = 1
	// RenderPartUnderline wraps PANGO_RENDER_PART_UNDERLINE
	//
	// underlines
	RenderPartUnderline RenderPart = 2
	// RenderPartStrikethrough wraps PANGO_RENDER_PART_STRIKETHROUGH
	//
	// strikethrough lines
	RenderPartStrikethrough RenderPart = 3
	// RenderPartOverline wraps PANGO_RENDER_PART_OVERLINE
	//
	// overlines
	RenderPartOverline RenderPart = 4
)

func marshalRenderPart(p unsafe.Pointer) (any, error) {
	return RenderPart(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = RenderPart(0)

func (e RenderPart) GoValueType() gobject.Type {
	return TypeRenderPart
}

func (e RenderPart) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e RenderPart) String() string {
	switch e {
		case RenderPartBackground: return "RenderPartBackground"
		case RenderPartForeground: return "RenderPartForeground"
		case RenderPartOverline: return "RenderPartOverline"
		case RenderPartStrikethrough: return "RenderPartStrikethrough"
		case RenderPartUnderline: return "RenderPartUnderline"
		default: return fmt.Sprintf("RenderPart(%d)", e)
	}
}

// Script wraps PangoScript
//
// The `PangoScript` enumeration identifies different writing
// systems.
// 
// The values correspond to the names as defined in the Unicode standard. See
// [Unicode Standard Annex 24: Script names](http://www.unicode.org/reports/tr24/)
// 
// Note that this enumeration is deprecated and will not be updated to include values
// in newer versions of the Unicode standard. Applications should use the
// [enum@GLib.UnicodeScript] enumeration instead,
// whose values are interchangeable with `PangoScript`.
type Script C.int

const (
	// ScriptInvalidCode wraps PANGO_SCRIPT_INVALID_CODE
	//
	// a value never returned from pango_script_for_unichar()
	ScriptInvalidCode Script = -1
	// ScriptCommon wraps PANGO_SCRIPT_COMMON
	//
	// a character used by multiple different scripts
	ScriptCommon Script = 0
	// ScriptInherited wraps PANGO_SCRIPT_INHERITED
	//
	// a mark glyph that takes its script from the
	// base glyph to which it is attached
	ScriptInherited Script = 1
	// ScriptArabic wraps PANGO_SCRIPT_ARABIC
	//
	// Arabic
	ScriptArabic Script = 2
	// ScriptArmenian wraps PANGO_SCRIPT_ARMENIAN
	//
	// Armenian
	ScriptArmenian Script = 3
	// ScriptBengali wraps PANGO_SCRIPT_BENGALI
	//
	// Bengali
	ScriptBengali Script = 4
	// ScriptBopomofo wraps PANGO_SCRIPT_BOPOMOFO
	//
	// Bopomofo
	ScriptBopomofo Script = 5
	// ScriptCherokee wraps PANGO_SCRIPT_CHEROKEE
	//
	// Cherokee
	ScriptCherokee Script = 6
	// ScriptCoptic wraps PANGO_SCRIPT_COPTIC
	//
	// Coptic
	ScriptCoptic Script = 7
	// ScriptCyrillic wraps PANGO_SCRIPT_CYRILLIC
	//
	// Cyrillic
	ScriptCyrillic Script = 8
	// ScriptDeseret wraps PANGO_SCRIPT_DESERET
	//
	// Deseret
	ScriptDeseret Script = 9
	// ScriptDevanagari wraps PANGO_SCRIPT_DEVANAGARI
	//
	// Devanagari
	ScriptDevanagari Script = 10
	// ScriptEthiopic wraps PANGO_SCRIPT_ETHIOPIC
	//
	// Ethiopic
	ScriptEthiopic Script = 11
	// ScriptGeorgian wraps PANGO_SCRIPT_GEORGIAN
	//
	// Georgian
	ScriptGeorgian Script = 12
	// ScriptGothic wraps PANGO_SCRIPT_GOTHIC
	//
	// Gothic
	ScriptGothic Script = 13
	// ScriptGreek wraps PANGO_SCRIPT_GREEK
	//
	// Greek
	ScriptGreek Script = 14
	// ScriptGujarati wraps PANGO_SCRIPT_GUJARATI
	//
	// Gujarati
	ScriptGujarati Script = 15
	// ScriptGurmukhi wraps PANGO_SCRIPT_GURMUKHI
	//
	// Gurmukhi
	ScriptGurmukhi Script = 16
	// ScriptHan wraps PANGO_SCRIPT_HAN
	//
	// Han
	ScriptHan Script = 17
	// ScriptHangul wraps PANGO_SCRIPT_HANGUL
	//
	// Hangul
	ScriptHangul Script = 18
	// ScriptHebrew wraps PANGO_SCRIPT_HEBREW
	//
	// Hebrew
	ScriptHebrew Script = 19
	// ScriptHiragana wraps PANGO_SCRIPT_HIRAGANA
	//
	// Hiragana
	ScriptHiragana Script = 20
	// ScriptKannada wraps PANGO_SCRIPT_KANNADA
	//
	// Kannada
	ScriptKannada Script = 21
	// ScriptKatakana wraps PANGO_SCRIPT_KATAKANA
	//
	// Katakana
	ScriptKatakana Script = 22
	// ScriptKhmer wraps PANGO_SCRIPT_KHMER
	//
	// Khmer
	ScriptKhmer Script = 23
	// ScriptLao wraps PANGO_SCRIPT_LAO
	//
	// Lao
	ScriptLao Script = 24
	// ScriptLatin wraps PANGO_SCRIPT_LATIN
	//
	// Latin
	ScriptLatin Script = 25
	// ScriptMalayalam wraps PANGO_SCRIPT_MALAYALAM
	//
	// Malayalam
	ScriptMalayalam Script = 26
	// ScriptMongolian wraps PANGO_SCRIPT_MONGOLIAN
	//
	// Mongolian
	ScriptMongolian Script = 27
	// ScriptMyanmar wraps PANGO_SCRIPT_MYANMAR
	//
	// Myanmar
	ScriptMyanmar Script = 28
	// ScriptOgham wraps PANGO_SCRIPT_OGHAM
	//
	// Ogham
	ScriptOgham Script = 29
	// ScriptOldItalic wraps PANGO_SCRIPT_OLD_ITALIC
	//
	// Old Italic
	ScriptOldItalic Script = 30
	// ScriptOriya wraps PANGO_SCRIPT_ORIYA
	//
	// Oriya
	ScriptOriya Script = 31
	// ScriptRunic wraps PANGO_SCRIPT_RUNIC
	//
	// Runic
	ScriptRunic Script = 32
	// ScriptSinhala wraps PANGO_SCRIPT_SINHALA
	//
	// Sinhala
	ScriptSinhala Script = 33
	// ScriptSyriac wraps PANGO_SCRIPT_SYRIAC
	//
	// Syriac
	ScriptSyriac Script = 34
	// ScriptTamil wraps PANGO_SCRIPT_TAMIL
	//
	// Tamil
	ScriptTamil Script = 35
	// ScriptTelugu wraps PANGO_SCRIPT_TELUGU
	//
	// Telugu
	ScriptTelugu Script = 36
	// ScriptThaana wraps PANGO_SCRIPT_THAANA
	//
	// Thaana
	ScriptThaana Script = 37
	// ScriptThai wraps PANGO_SCRIPT_THAI
	//
	// Thai
	ScriptThai Script = 38
	// ScriptTibetan wraps PANGO_SCRIPT_TIBETAN
	//
	// Tibetan
	ScriptTibetan Script = 39
	// ScriptCanadianAboriginal wraps PANGO_SCRIPT_CANADIAN_ABORIGINAL
	//
	// Canadian Aboriginal
	ScriptCanadianAboriginal Script = 40
	// ScriptYi wraps PANGO_SCRIPT_YI
	//
	// Yi
	ScriptYi Script = 41
	// ScriptTagalog wraps PANGO_SCRIPT_TAGALOG
	//
	// Tagalog
	ScriptTagalog Script = 42
	// ScriptHanunoo wraps PANGO_SCRIPT_HANUNOO
	//
	// Hanunoo
	ScriptHanunoo Script = 43
	// ScriptBuhid wraps PANGO_SCRIPT_BUHID
	//
	// Buhid
	ScriptBuhid Script = 44
	// ScriptTagbanwa wraps PANGO_SCRIPT_TAGBANWA
	//
	// Tagbanwa
	ScriptTagbanwa Script = 45
	// ScriptBraille wraps PANGO_SCRIPT_BRAILLE
	//
	// Braille
	ScriptBraille Script = 46
	// ScriptCypriot wraps PANGO_SCRIPT_CYPRIOT
	//
	// Cypriot
	ScriptCypriot Script = 47
	// ScriptLimbu wraps PANGO_SCRIPT_LIMBU
	//
	// Limbu
	ScriptLimbu Script = 48
	// ScriptOsmanya wraps PANGO_SCRIPT_OSMANYA
	//
	// Osmanya
	ScriptOsmanya Script = 49
	// ScriptShavian wraps PANGO_SCRIPT_SHAVIAN
	//
	// Shavian
	ScriptShavian Script = 50
	// ScriptLinearB wraps PANGO_SCRIPT_LINEAR_B
	//
	// Linear B
	ScriptLinearB Script = 51
	// ScriptTaiLe wraps PANGO_SCRIPT_TAI_LE
	//
	// Tai Le
	ScriptTaiLe Script = 52
	// ScriptUgaritic wraps PANGO_SCRIPT_UGARITIC
	//
	// Ugaritic
	ScriptUgaritic Script = 53
	// ScriptNewTaiLue wraps PANGO_SCRIPT_NEW_TAI_LUE
	//
	// New Tai Lue. Since 1.10
	ScriptNewTaiLue Script = 54
	// ScriptBuginese wraps PANGO_SCRIPT_BUGINESE
	//
	// Buginese. Since 1.10
	ScriptBuginese Script = 55
	// ScriptGlagolitic wraps PANGO_SCRIPT_GLAGOLITIC
	//
	// Glagolitic. Since 1.10
	ScriptGlagolitic Script = 56
	// ScriptTifinagh wraps PANGO_SCRIPT_TIFINAGH
	//
	// Tifinagh. Since 1.10
	ScriptTifinagh Script = 57
	// ScriptSylotiNagri wraps PANGO_SCRIPT_SYLOTI_NAGRI
	//
	// Syloti Nagri. Since 1.10
	ScriptSylotiNagri Script = 58
	// ScriptOldPersian wraps PANGO_SCRIPT_OLD_PERSIAN
	//
	// Old Persian. Since 1.10
	ScriptOldPersian Script = 59
	// ScriptKharoshthi wraps PANGO_SCRIPT_KHAROSHTHI
	//
	// Kharoshthi. Since 1.10
	ScriptKharoshthi Script = 60
	// ScriptUnknown wraps PANGO_SCRIPT_UNKNOWN
	//
	// an unassigned code point. Since 1.14
	ScriptUnknown Script = 61
	// ScriptBalinese wraps PANGO_SCRIPT_BALINESE
	//
	// Balinese. Since 1.14
	ScriptBalinese Script = 62
	// ScriptCuneiform wraps PANGO_SCRIPT_CUNEIFORM
	//
	// Cuneiform. Since 1.14
	ScriptCuneiform Script = 63
	// ScriptPhoenician wraps PANGO_SCRIPT_PHOENICIAN
	//
	// Phoenician. Since 1.14
	ScriptPhoenician Script = 64
	// ScriptPhagsPa wraps PANGO_SCRIPT_PHAGS_PA
	//
	// Phags-pa. Since 1.14
	ScriptPhagsPa Script = 65
	// ScriptNko wraps PANGO_SCRIPT_NKO
	//
	// N'Ko. Since 1.14
	ScriptNko Script = 66
	// ScriptKayahLi wraps PANGO_SCRIPT_KAYAH_LI
	//
	// Kayah Li. Since 1.20.1
	ScriptKayahLi Script = 67
	// ScriptLepcha wraps PANGO_SCRIPT_LEPCHA
	//
	// Lepcha. Since 1.20.1
	ScriptLepcha Script = 68
	// ScriptRejang wraps PANGO_SCRIPT_REJANG
	//
	// Rejang. Since 1.20.1
	ScriptRejang Script = 69
	// ScriptSundanese wraps PANGO_SCRIPT_SUNDANESE
	//
	// Sundanese. Since 1.20.1
	ScriptSundanese Script = 70
	// ScriptSaurashtra wraps PANGO_SCRIPT_SAURASHTRA
	//
	// Saurashtra. Since 1.20.1
	ScriptSaurashtra Script = 71
	// ScriptCham wraps PANGO_SCRIPT_CHAM
	//
	// Cham. Since 1.20.1
	ScriptCham Script = 72
	// ScriptOlChiki wraps PANGO_SCRIPT_OL_CHIKI
	//
	// Ol Chiki. Since 1.20.1
	ScriptOlChiki Script = 73
	// ScriptVai wraps PANGO_SCRIPT_VAI
	//
	// Vai. Since 1.20.1
	ScriptVai Script = 74
	// ScriptCarian wraps PANGO_SCRIPT_CARIAN
	//
	// Carian. Since 1.20.1
	ScriptCarian Script = 75
	// ScriptLycian wraps PANGO_SCRIPT_LYCIAN
	//
	// Lycian. Since 1.20.1
	ScriptLycian Script = 76
	// ScriptLydian wraps PANGO_SCRIPT_LYDIAN
	//
	// Lydian. Since 1.20.1
	ScriptLydian Script = 77
	// ScriptBatak wraps PANGO_SCRIPT_BATAK
	//
	// Batak. Since 1.32
	ScriptBatak Script = 78
	// ScriptBrahmi wraps PANGO_SCRIPT_BRAHMI
	//
	// Brahmi. Since 1.32
	ScriptBrahmi Script = 79
	// ScriptMandaic wraps PANGO_SCRIPT_MANDAIC
	//
	// Mandaic. Since 1.32
	ScriptMandaic Script = 80
	// ScriptChakma wraps PANGO_SCRIPT_CHAKMA
	//
	// Chakma. Since: 1.32
	ScriptChakma Script = 81
	// ScriptMeroiticCursive wraps PANGO_SCRIPT_MEROITIC_CURSIVE
	//
	// Meroitic Cursive. Since: 1.32
	ScriptMeroiticCursive Script = 82
	// ScriptMeroiticHieroglyphs wraps PANGO_SCRIPT_MEROITIC_HIEROGLYPHS
	//
	// Meroitic Hieroglyphs. Since: 1.32
	ScriptMeroiticHieroglyphs Script = 83
	// ScriptMiao wraps PANGO_SCRIPT_MIAO
	//
	// Miao. Since: 1.32
	ScriptMiao Script = 84
	// ScriptSharada wraps PANGO_SCRIPT_SHARADA
	//
	// Sharada. Since: 1.32
	ScriptSharada Script = 85
	// ScriptSoraSompeng wraps PANGO_SCRIPT_SORA_SOMPENG
	//
	// Sora Sompeng. Since: 1.32
	ScriptSoraSompeng Script = 86
	// ScriptTakri wraps PANGO_SCRIPT_TAKRI
	//
	// Takri. Since: 1.32
	ScriptTakri Script = 87
	// ScriptBassaVah wraps PANGO_SCRIPT_BASSA_VAH
	//
	// Bassa. Since: 1.40
	ScriptBassaVah Script = 88
	// ScriptCaucasianAlbanian wraps PANGO_SCRIPT_CAUCASIAN_ALBANIAN
	//
	// Caucasian Albanian. Since: 1.40
	ScriptCaucasianAlbanian Script = 89
	// ScriptDuployan wraps PANGO_SCRIPT_DUPLOYAN
	//
	// Duployan. Since: 1.40
	ScriptDuployan Script = 90
	// ScriptElbasan wraps PANGO_SCRIPT_ELBASAN
	//
	// Elbasan. Since: 1.40
	ScriptElbasan Script = 91
	// ScriptGrantha wraps PANGO_SCRIPT_GRANTHA
	//
	// Grantha. Since: 1.40
	ScriptGrantha Script = 92
	// ScriptKhojki wraps PANGO_SCRIPT_KHOJKI
	//
	// Kjohki. Since: 1.40
	ScriptKhojki Script = 93
	// ScriptKhudawadi wraps PANGO_SCRIPT_KHUDAWADI
	//
	// Khudawadi, Sindhi. Since: 1.40
	ScriptKhudawadi Script = 94
	// ScriptLinearA wraps PANGO_SCRIPT_LINEAR_A
	//
	// Linear A. Since: 1.40
	ScriptLinearA Script = 95
	// ScriptMahajani wraps PANGO_SCRIPT_MAHAJANI
	//
	// Mahajani. Since: 1.40
	ScriptMahajani Script = 96
	// ScriptManichaean wraps PANGO_SCRIPT_MANICHAEAN
	//
	// Manichaean. Since: 1.40
	ScriptManichaean Script = 97
	// ScriptMendeKikakui wraps PANGO_SCRIPT_MENDE_KIKAKUI
	//
	// Mende Kikakui. Since: 1.40
	ScriptMendeKikakui Script = 98
	// ScriptModi wraps PANGO_SCRIPT_MODI
	//
	// Modi. Since: 1.40
	ScriptModi Script = 99
	// ScriptMro wraps PANGO_SCRIPT_MRO
	//
	// Mro. Since: 1.40
	ScriptMro Script = 100
	// ScriptNabataean wraps PANGO_SCRIPT_NABATAEAN
	//
	// Nabataean. Since: 1.40
	ScriptNabataean Script = 101
	// ScriptOldNorthArabian wraps PANGO_SCRIPT_OLD_NORTH_ARABIAN
	//
	// Old North Arabian. Since: 1.40
	ScriptOldNorthArabian Script = 102
	// ScriptOldPermic wraps PANGO_SCRIPT_OLD_PERMIC
	//
	// Old Permic. Since: 1.40
	ScriptOldPermic Script = 103
	// ScriptPahawhHmong wraps PANGO_SCRIPT_PAHAWH_HMONG
	//
	// Pahawh Hmong. Since: 1.40
	ScriptPahawhHmong Script = 104
	// ScriptPalmyrene wraps PANGO_SCRIPT_PALMYRENE
	//
	// Palmyrene. Since: 1.40
	ScriptPalmyrene Script = 105
	// ScriptPauCinHau wraps PANGO_SCRIPT_PAU_CIN_HAU
	//
	// Pau Cin Hau. Since: 1.40
	ScriptPauCinHau Script = 106
	// ScriptPsalterPahlavi wraps PANGO_SCRIPT_PSALTER_PAHLAVI
	//
	// Psalter Pahlavi. Since: 1.40
	ScriptPsalterPahlavi Script = 107
	// ScriptSiddham wraps PANGO_SCRIPT_SIDDHAM
	//
	// Siddham. Since: 1.40
	ScriptSiddham Script = 108
	// ScriptTirhuta wraps PANGO_SCRIPT_TIRHUTA
	//
	// Tirhuta. Since: 1.40
	ScriptTirhuta Script = 109
	// ScriptWarangCiti wraps PANGO_SCRIPT_WARANG_CITI
	//
	// Warang Citi. Since: 1.40
	ScriptWarangCiti Script = 110
	// ScriptAhom wraps PANGO_SCRIPT_AHOM
	//
	// Ahom. Since: 1.40
	ScriptAhom Script = 111
	// ScriptAnatolianHieroglyphs wraps PANGO_SCRIPT_ANATOLIAN_HIEROGLYPHS
	//
	// Anatolian Hieroglyphs. Since: 1.40
	ScriptAnatolianHieroglyphs Script = 112
	// ScriptHatran wraps PANGO_SCRIPT_HATRAN
	//
	// Hatran. Since: 1.40
	ScriptHatran Script = 113
	// ScriptMultani wraps PANGO_SCRIPT_MULTANI
	//
	// Multani. Since: 1.40
	ScriptMultani Script = 114
	// ScriptOldHungarian wraps PANGO_SCRIPT_OLD_HUNGARIAN
	//
	// Old Hungarian. Since: 1.40
	ScriptOldHungarian Script = 115
	// ScriptSignwriting wraps PANGO_SCRIPT_SIGNWRITING
	//
	// Signwriting. Since: 1.40
	ScriptSignwriting Script = 116
)

func marshalScript(p unsafe.Pointer) (any, error) {
	return Script(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Script(0)

func (e Script) GoValueType() gobject.Type {
	return TypeScript
}

func (e Script) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Script) String() string {
	switch e {
		case ScriptAhom: return "ScriptAhom"
		case ScriptAnatolianHieroglyphs: return "ScriptAnatolianHieroglyphs"
		case ScriptArabic: return "ScriptArabic"
		case ScriptArmenian: return "ScriptArmenian"
		case ScriptBalinese: return "ScriptBalinese"
		case ScriptBassaVah: return "ScriptBassaVah"
		case ScriptBatak: return "ScriptBatak"
		case ScriptBengali: return "ScriptBengali"
		case ScriptBopomofo: return "ScriptBopomofo"
		case ScriptBrahmi: return "ScriptBrahmi"
		case ScriptBraille: return "ScriptBraille"
		case ScriptBuginese: return "ScriptBuginese"
		case ScriptBuhid: return "ScriptBuhid"
		case ScriptCanadianAboriginal: return "ScriptCanadianAboriginal"
		case ScriptCarian: return "ScriptCarian"
		case ScriptCaucasianAlbanian: return "ScriptCaucasianAlbanian"
		case ScriptChakma: return "ScriptChakma"
		case ScriptCham: return "ScriptCham"
		case ScriptCherokee: return "ScriptCherokee"
		case ScriptCommon: return "ScriptCommon"
		case ScriptCoptic: return "ScriptCoptic"
		case ScriptCuneiform: return "ScriptCuneiform"
		case ScriptCypriot: return "ScriptCypriot"
		case ScriptCyrillic: return "ScriptCyrillic"
		case ScriptDeseret: return "ScriptDeseret"
		case ScriptDevanagari: return "ScriptDevanagari"
		case ScriptDuployan: return "ScriptDuployan"
		case ScriptElbasan: return "ScriptElbasan"
		case ScriptEthiopic: return "ScriptEthiopic"
		case ScriptGeorgian: return "ScriptGeorgian"
		case ScriptGlagolitic: return "ScriptGlagolitic"
		case ScriptGothic: return "ScriptGothic"
		case ScriptGrantha: return "ScriptGrantha"
		case ScriptGreek: return "ScriptGreek"
		case ScriptGujarati: return "ScriptGujarati"
		case ScriptGurmukhi: return "ScriptGurmukhi"
		case ScriptHan: return "ScriptHan"
		case ScriptHangul: return "ScriptHangul"
		case ScriptHanunoo: return "ScriptHanunoo"
		case ScriptHatran: return "ScriptHatran"
		case ScriptHebrew: return "ScriptHebrew"
		case ScriptHiragana: return "ScriptHiragana"
		case ScriptInherited: return "ScriptInherited"
		case ScriptInvalidCode: return "ScriptInvalidCode"
		case ScriptKannada: return "ScriptKannada"
		case ScriptKatakana: return "ScriptKatakana"
		case ScriptKayahLi: return "ScriptKayahLi"
		case ScriptKharoshthi: return "ScriptKharoshthi"
		case ScriptKhmer: return "ScriptKhmer"
		case ScriptKhojki: return "ScriptKhojki"
		case ScriptKhudawadi: return "ScriptKhudawadi"
		case ScriptLao: return "ScriptLao"
		case ScriptLatin: return "ScriptLatin"
		case ScriptLepcha: return "ScriptLepcha"
		case ScriptLimbu: return "ScriptLimbu"
		case ScriptLinearA: return "ScriptLinearA"
		case ScriptLinearB: return "ScriptLinearB"
		case ScriptLycian: return "ScriptLycian"
		case ScriptLydian: return "ScriptLydian"
		case ScriptMahajani: return "ScriptMahajani"
		case ScriptMalayalam: return "ScriptMalayalam"
		case ScriptMandaic: return "ScriptMandaic"
		case ScriptManichaean: return "ScriptManichaean"
		case ScriptMendeKikakui: return "ScriptMendeKikakui"
		case ScriptMeroiticCursive: return "ScriptMeroiticCursive"
		case ScriptMeroiticHieroglyphs: return "ScriptMeroiticHieroglyphs"
		case ScriptMiao: return "ScriptMiao"
		case ScriptModi: return "ScriptModi"
		case ScriptMongolian: return "ScriptMongolian"
		case ScriptMro: return "ScriptMro"
		case ScriptMultani: return "ScriptMultani"
		case ScriptMyanmar: return "ScriptMyanmar"
		case ScriptNabataean: return "ScriptNabataean"
		case ScriptNewTaiLue: return "ScriptNewTaiLue"
		case ScriptNko: return "ScriptNko"
		case ScriptOgham: return "ScriptOgham"
		case ScriptOlChiki: return "ScriptOlChiki"
		case ScriptOldHungarian: return "ScriptOldHungarian"
		case ScriptOldItalic: return "ScriptOldItalic"
		case ScriptOldNorthArabian: return "ScriptOldNorthArabian"
		case ScriptOldPermic: return "ScriptOldPermic"
		case ScriptOldPersian: return "ScriptOldPersian"
		case ScriptOriya: return "ScriptOriya"
		case ScriptOsmanya: return "ScriptOsmanya"
		case ScriptPahawhHmong: return "ScriptPahawhHmong"
		case ScriptPalmyrene: return "ScriptPalmyrene"
		case ScriptPauCinHau: return "ScriptPauCinHau"
		case ScriptPhagsPa: return "ScriptPhagsPa"
		case ScriptPhoenician: return "ScriptPhoenician"
		case ScriptPsalterPahlavi: return "ScriptPsalterPahlavi"
		case ScriptRejang: return "ScriptRejang"
		case ScriptRunic: return "ScriptRunic"
		case ScriptSaurashtra: return "ScriptSaurashtra"
		case ScriptSharada: return "ScriptSharada"
		case ScriptShavian: return "ScriptShavian"
		case ScriptSiddham: return "ScriptSiddham"
		case ScriptSignwriting: return "ScriptSignwriting"
		case ScriptSinhala: return "ScriptSinhala"
		case ScriptSoraSompeng: return "ScriptSoraSompeng"
		case ScriptSundanese: return "ScriptSundanese"
		case ScriptSylotiNagri: return "ScriptSylotiNagri"
		case ScriptSyriac: return "ScriptSyriac"
		case ScriptTagalog: return "ScriptTagalog"
		case ScriptTagbanwa: return "ScriptTagbanwa"
		case ScriptTaiLe: return "ScriptTaiLe"
		case ScriptTakri: return "ScriptTakri"
		case ScriptTamil: return "ScriptTamil"
		case ScriptTelugu: return "ScriptTelugu"
		case ScriptThaana: return "ScriptThaana"
		case ScriptThai: return "ScriptThai"
		case ScriptTibetan: return "ScriptTibetan"
		case ScriptTifinagh: return "ScriptTifinagh"
		case ScriptTirhuta: return "ScriptTirhuta"
		case ScriptUgaritic: return "ScriptUgaritic"
		case ScriptUnknown: return "ScriptUnknown"
		case ScriptVai: return "ScriptVai"
		case ScriptWarangCiti: return "ScriptWarangCiti"
		case ScriptYi: return "ScriptYi"
		default: return fmt.Sprintf("Script(%d)", e)
	}
}

// ScriptForUnichar wraps pango_script_for_unichar
// 
// The function takes the following parameters:
// 
// 	- ch uint32: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret Script 
//
// Looks up the script for a particular character.
// 
// The script of a character is defined by
// [Unicode Standard Annex 24: Script names](http://www.unicode.org/reports/tr24/).
// 
// No check is made for @ch being a valid Unicode character; if you pass
// in invalid character, the result is undefined.
// 
// Note that while the return type of this function is declared
// as `PangoScript`, as of Pango 1.18, this function simply returns
// the return value of [func@GLib.unichar_get_script]. Callers must be
// prepared to handle unknown values.
//
// Deprecated: (since 1.44.0) Use g_unichar_get_script()
func ScriptForUnichar(ch uint32) Script {
	var carg1 C.gunichar    // in, none, casted
	var cret  C.PangoScript // return, none, casted

	carg1 = C.gunichar(ch)

	cret = C.pango_script_for_unichar(carg1)
	runtime.KeepAlive(ch)

	var goret Script

	goret = Script(cret)

	return goret
}

// ScriptGetSampleLanguage wraps pango_script_get_sample_language
// 
// The function takes the following parameters:
// 
// 	- script Script: a `PangoScript` 
// 
// The function returns the following values:
// 
// 	- goret *Language (nullable) 
//
// Finds a language tag that is reasonably representative of @script.
// 
// The language will usually be the most widely spoken or used language
// written in that script: for instance, the sample language for
// %PANGO_SCRIPT_CYRILLIC is ru (Russian), the sample language for
// %PANGO_SCRIPT_ARABIC is ar.
// 
// For some scripts, no sample language will be returned because
// there is no language that is sufficiently representative. The
// best example of this is %PANGO_SCRIPT_HAN, where various different
// variants of written Chinese, Japanese, and Korean all use
// significantly different sets of Han characters and forms
// of shared characters. No sample language can be provided
// for many historical scripts as well.
// 
// As of 1.18, this function checks the environment variables
// `PANGO_LANGUAGE` and `LANGUAGE` (checked in that order) first.
// If one of them is set, it is parsed as a list of language tags
// separated by colons or other separators. This function
// will return the first language in the parsed list that Pango
// believes may use @script for writing. This last predicate
// is tested using [method@Pango.Language.includes_script]. This can
// be used to control Pango's font selection for non-primary
// languages. For example, a `PANGO_LANGUAGE` enviroment variable
// set to "en:fa" makes Pango choose fonts suitable for Persian (fa)
// instead of Arabic (ar) when a segment of Arabic text is found
// in an otherwise non-Arabic text. The same trick can be used to
// choose a default language for %PANGO_SCRIPT_HAN when setting
// context language is not feasible.
func ScriptGetSampleLanguage(script Script) *Language {
	var carg1 C.PangoScript    // in, none, casted
	var cret  *C.PangoLanguage // return, full, converted, nullable

	carg1 = C.PangoScript(script)

	cret = C.pango_script_get_sample_language(carg1)
	runtime.KeepAlive(script)

	var goret *Language

	if cret != nil {
		goret = UnsafeLanguageFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Stretch wraps PangoStretch
//
// An enumeration specifying the width of the font relative to other designs
// within a family.
type Stretch C.int

const (
	// StretchUltraCondensed wraps PANGO_STRETCH_ULTRA_CONDENSED
	//
	// ultra condensed width
	StretchUltraCondensed Stretch = 0
	// StretchExtraCondensed wraps PANGO_STRETCH_EXTRA_CONDENSED
	//
	// extra condensed width
	StretchExtraCondensed Stretch = 1
	// StretchCondensed wraps PANGO_STRETCH_CONDENSED
	//
	// condensed width
	StretchCondensed Stretch = 2
	// StretchSemiCondensed wraps PANGO_STRETCH_SEMI_CONDENSED
	//
	// semi condensed width
	StretchSemiCondensed Stretch = 3
	// StretchNormal wraps PANGO_STRETCH_NORMAL
	//
	// the normal width
	StretchNormal Stretch = 4
	// StretchSemiExpanded wraps PANGO_STRETCH_SEMI_EXPANDED
	//
	// semi expanded width
	StretchSemiExpanded Stretch = 5
	// StretchExpanded wraps PANGO_STRETCH_EXPANDED
	//
	// expanded width
	StretchExpanded Stretch = 6
	// StretchExtraExpanded wraps PANGO_STRETCH_EXTRA_EXPANDED
	//
	// extra expanded width
	StretchExtraExpanded Stretch = 7
	// StretchUltraExpanded wraps PANGO_STRETCH_ULTRA_EXPANDED
	//
	// ultra expanded width
	StretchUltraExpanded Stretch = 8
)

func marshalStretch(p unsafe.Pointer) (any, error) {
	return Stretch(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Stretch(0)

func (e Stretch) GoValueType() gobject.Type {
	return TypeStretch
}

func (e Stretch) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Stretch) String() string {
	switch e {
		case StretchCondensed: return "StretchCondensed"
		case StretchExpanded: return "StretchExpanded"
		case StretchExtraCondensed: return "StretchExtraCondensed"
		case StretchExtraExpanded: return "StretchExtraExpanded"
		case StretchNormal: return "StretchNormal"
		case StretchSemiCondensed: return "StretchSemiCondensed"
		case StretchSemiExpanded: return "StretchSemiExpanded"
		case StretchUltraCondensed: return "StretchUltraCondensed"
		case StretchUltraExpanded: return "StretchUltraExpanded"
		default: return fmt.Sprintf("Stretch(%d)", e)
	}
}

// Style wraps PangoStyle
//
// An enumeration specifying the various slant styles possible for a font.
type Style C.int

const (
	// StyleNormal wraps PANGO_STYLE_NORMAL
	//
	// the font is upright.
	StyleNormal Style = 0
	// StyleOblique wraps PANGO_STYLE_OBLIQUE
	//
	// the font is slanted, but in a roman style.
	StyleOblique Style = 1
	// StyleItalic wraps PANGO_STYLE_ITALIC
	//
	// the font is slanted in an italic style.
	StyleItalic Style = 2
)

func marshalStyle(p unsafe.Pointer) (any, error) {
	return Style(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Style(0)

func (e Style) GoValueType() gobject.Type {
	return TypeStyle
}

func (e Style) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Style) String() string {
	switch e {
		case StyleItalic: return "StyleItalic"
		case StyleNormal: return "StyleNormal"
		case StyleOblique: return "StyleOblique"
		default: return fmt.Sprintf("Style(%d)", e)
	}
}

// TabAlign wraps PangoTabAlign
//
// `PangoTabAlign` specifies where the text appears relative to the tab stop
// position.
type TabAlign C.int

const (
	// TabLeft wraps PANGO_TAB_LEFT
	//
	// the text appears to the right of the tab stop position
	TabLeft TabAlign = 0
	// TabRight wraps PANGO_TAB_RIGHT
	//
	// the text appears to the left of the tab stop position
	//   until the available space is filled. Since: 1.50
	TabRight TabAlign = 1
	// TabCenter wraps PANGO_TAB_CENTER
	//
	// the text is centered at the tab stop position
	//   until the available space is filled. Since: 1.50
	TabCenter TabAlign = 2
	// TabDecimal wraps PANGO_TAB_DECIMAL
	//
	// text before the first occurrence of the decimal point
	//   character appears to the left of the tab stop position (until the available
	//   space is filled), the rest to the right. Since: 1.50
	TabDecimal TabAlign = 3
)

func marshalTabAlign(p unsafe.Pointer) (any, error) {
	return TabAlign(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = TabAlign(0)

func (e TabAlign) GoValueType() gobject.Type {
	return TypeTabAlign
}

func (e TabAlign) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e TabAlign) String() string {
	switch e {
		case TabCenter: return "TabCenter"
		case TabDecimal: return "TabDecimal"
		case TabLeft: return "TabLeft"
		case TabRight: return "TabRight"
		default: return fmt.Sprintf("TabAlign(%d)", e)
	}
}

// TextTransform wraps PangoTextTransform
//
// An enumeration that affects how Pango treats characters during shaping.
type TextTransform C.int

const (
	// TextTransformNone wraps PANGO_TEXT_TRANSFORM_NONE
	//
	// Leave text unchanged
	TextTransformNone TextTransform = 0
	// TextTransformLowercase wraps PANGO_TEXT_TRANSFORM_LOWERCASE
	//
	// Display letters and numbers as lowercase
	TextTransformLowercase TextTransform = 1
	// TextTransformUppercase wraps PANGO_TEXT_TRANSFORM_UPPERCASE
	//
	// Display letters and numbers as uppercase
	TextTransformUppercase TextTransform = 2
	// TextTransformCapitalize wraps PANGO_TEXT_TRANSFORM_CAPITALIZE
	//
	// Display the first character of a word
	//   in titlecase
	TextTransformCapitalize TextTransform = 3
)

func marshalTextTransform(p unsafe.Pointer) (any, error) {
	return TextTransform(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = TextTransform(0)

func (e TextTransform) GoValueType() gobject.Type {
	return TypeTextTransform
}

func (e TextTransform) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e TextTransform) String() string {
	switch e {
		case TextTransformCapitalize: return "TextTransformCapitalize"
		case TextTransformLowercase: return "TextTransformLowercase"
		case TextTransformNone: return "TextTransformNone"
		case TextTransformUppercase: return "TextTransformUppercase"
		default: return fmt.Sprintf("TextTransform(%d)", e)
	}
}

// Underline wraps PangoUnderline
//
// The `PangoUnderline` enumeration is used to specify whether text
// should be underlined, and if so, the type of underlining.
type Underline C.int

const (
	// UnderlineNone wraps PANGO_UNDERLINE_NONE
	//
	// no underline should be drawn
	UnderlineNone Underline = 0
	// UnderlineSingle wraps PANGO_UNDERLINE_SINGLE
	//
	// a single underline should be drawn
	UnderlineSingle Underline = 1
	// UnderlineDouble wraps PANGO_UNDERLINE_DOUBLE
	//
	// a double underline should be drawn
	UnderlineDouble Underline = 2
	// UnderlineLow wraps PANGO_UNDERLINE_LOW
	//
	// a single underline should be drawn at a
	//   position beneath the ink extents of the text being
	//   underlined. This should be used only for underlining
	//   single characters, such as for keyboard accelerators.
	//   %PANGO_UNDERLINE_SINGLE should be used for extended
	//   portions of text.
	UnderlineLow Underline = 3
	// UnderlineError wraps PANGO_UNDERLINE_ERROR
	//
	// an underline indicating an error should
	//   be drawn below. The exact style of rendering is up to the
	//   `PangoRenderer` in use, but typical styles include wavy
	//   or dotted lines.
	//   This underline is typically used to indicate an error such
	//   as a possible mispelling; in some cases a contrasting color
	//   may automatically be used. This type of underlining is
	//   available since Pango 1.4.
	UnderlineError Underline = 4
	// UnderlineSingleLine wraps PANGO_UNDERLINE_SINGLE_LINE
	//
	// Like @PANGO_UNDERLINE_SINGLE, but
	//   drawn continuously across multiple runs. This type
	//   of underlining is available since Pango 1.46.
	UnderlineSingleLine Underline = 5
	// UnderlineDoubleLine wraps PANGO_UNDERLINE_DOUBLE_LINE
	//
	// Like @PANGO_UNDERLINE_DOUBLE, but
	//   drawn continuously across multiple runs. This type
	//   of underlining is available since Pango 1.46.
	UnderlineDoubleLine Underline = 6
	// UnderlineErrorLine wraps PANGO_UNDERLINE_ERROR_LINE
	//
	// Like @PANGO_UNDERLINE_ERROR, but
	//   drawn continuously across multiple runs. This type
	//   of underlining is available since Pango 1.46.
	UnderlineErrorLine Underline = 7
)

func marshalUnderline(p unsafe.Pointer) (any, error) {
	return Underline(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Underline(0)

func (e Underline) GoValueType() gobject.Type {
	return TypeUnderline
}

func (e Underline) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Underline) String() string {
	switch e {
		case UnderlineDouble: return "UnderlineDouble"
		case UnderlineDoubleLine: return "UnderlineDoubleLine"
		case UnderlineError: return "UnderlineError"
		case UnderlineErrorLine: return "UnderlineErrorLine"
		case UnderlineLow: return "UnderlineLow"
		case UnderlineNone: return "UnderlineNone"
		case UnderlineSingle: return "UnderlineSingle"
		case UnderlineSingleLine: return "UnderlineSingleLine"
		default: return fmt.Sprintf("Underline(%d)", e)
	}
}

// Variant wraps PangoVariant
//
// An enumeration specifying capitalization variant of the font.
type Variant C.int

const (
	// VariantNormal wraps PANGO_VARIANT_NORMAL
	//
	// A normal font.
	VariantNormal Variant = 0
	// VariantSmallCaps wraps PANGO_VARIANT_SMALL_CAPS
	//
	// A font with the lower case characters
	//   replaced by smaller variants of the capital characters.
	VariantSmallCaps Variant = 1
	// VariantAllSmallCaps wraps PANGO_VARIANT_ALL_SMALL_CAPS
	//
	// A font with all characters
	//   replaced by smaller variants of the capital characters. Since: 1.50
	VariantAllSmallCaps Variant = 2
	// VariantPetiteCaps wraps PANGO_VARIANT_PETITE_CAPS
	//
	// A font with the lower case characters
	//   replaced by smaller variants of the capital characters.
	//   Petite Caps can be even smaller than Small Caps. Since: 1.50
	VariantPetiteCaps Variant = 3
	// VariantAllPetiteCaps wraps PANGO_VARIANT_ALL_PETITE_CAPS
	//
	// A font with all characters
	//   replaced by smaller variants of the capital characters.
	//   Petite Caps can be even smaller than Small Caps. Since: 1.50
	VariantAllPetiteCaps Variant = 4
	// VariantUnicase wraps PANGO_VARIANT_UNICASE
	//
	// A font with the upper case characters
	//   replaced by smaller variants of the capital letters. Since: 1.50
	VariantUnicase Variant = 5
	// VariantTitleCaps wraps PANGO_VARIANT_TITLE_CAPS
	//
	// A font with capital letters that
	//   are more suitable for all-uppercase titles. Since: 1.50
	VariantTitleCaps Variant = 6
)

func marshalVariant(p unsafe.Pointer) (any, error) {
	return Variant(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Variant(0)

func (e Variant) GoValueType() gobject.Type {
	return TypeVariant
}

func (e Variant) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Variant) String() string {
	switch e {
		case VariantAllPetiteCaps: return "VariantAllPetiteCaps"
		case VariantAllSmallCaps: return "VariantAllSmallCaps"
		case VariantNormal: return "VariantNormal"
		case VariantPetiteCaps: return "VariantPetiteCaps"
		case VariantSmallCaps: return "VariantSmallCaps"
		case VariantTitleCaps: return "VariantTitleCaps"
		case VariantUnicase: return "VariantUnicase"
		default: return fmt.Sprintf("Variant(%d)", e)
	}
}

// Weight wraps PangoWeight
//
// An enumeration specifying the weight (boldness) of a font.
// 
// Weight is specified as a numeric value ranging from 100 to 1000.
// This enumeration simply provides some common, predefined values.
type Weight C.int

const (
	// WeightThin wraps PANGO_WEIGHT_THIN
	//
	// the thin weight (= 100) Since: 1.24
	WeightThin Weight = 100
	// WeightUltralight wraps PANGO_WEIGHT_ULTRALIGHT
	//
	// the ultralight weight (= 200)
	WeightUltralight Weight = 200
	// WeightLight wraps PANGO_WEIGHT_LIGHT
	//
	// the light weight (= 300)
	WeightLight Weight = 300
	// WeightSemilight wraps PANGO_WEIGHT_SEMILIGHT
	//
	// the semilight weight (= 350) Since: 1.36.7
	WeightSemilight Weight = 350
	// WeightBook wraps PANGO_WEIGHT_BOOK
	//
	// the book weight (= 380) Since: 1.24)
	WeightBook Weight = 380
	// WeightNormal wraps PANGO_WEIGHT_NORMAL
	//
	// the default weight (= 400)
	WeightNormal Weight = 400
	// WeightMedium wraps PANGO_WEIGHT_MEDIUM
	//
	// the medium weight (= 500) Since: 1.24
	WeightMedium Weight = 500
	// WeightSemibold wraps PANGO_WEIGHT_SEMIBOLD
	//
	// the semibold weight (= 600)
	WeightSemibold Weight = 600
	// WeightBold wraps PANGO_WEIGHT_BOLD
	//
	// the bold weight (= 700)
	WeightBold Weight = 700
	// WeightUltrabold wraps PANGO_WEIGHT_ULTRABOLD
	//
	// the ultrabold weight (= 800)
	WeightUltrabold Weight = 800
	// WeightHeavy wraps PANGO_WEIGHT_HEAVY
	//
	// the heavy weight (= 900)
	WeightHeavy Weight = 900
	// WeightUltraheavy wraps PANGO_WEIGHT_ULTRAHEAVY
	//
	// the ultraheavy weight (= 1000) Since: 1.24
	WeightUltraheavy Weight = 1000
)

func marshalWeight(p unsafe.Pointer) (any, error) {
	return Weight(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = Weight(0)

func (e Weight) GoValueType() gobject.Type {
	return TypeWeight
}

func (e Weight) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e Weight) String() string {
	switch e {
		case WeightBold: return "WeightBold"
		case WeightBook: return "WeightBook"
		case WeightHeavy: return "WeightHeavy"
		case WeightLight: return "WeightLight"
		case WeightMedium: return "WeightMedium"
		case WeightNormal: return "WeightNormal"
		case WeightSemibold: return "WeightSemibold"
		case WeightSemilight: return "WeightSemilight"
		case WeightThin: return "WeightThin"
		case WeightUltrabold: return "WeightUltrabold"
		case WeightUltraheavy: return "WeightUltraheavy"
		case WeightUltralight: return "WeightUltralight"
		default: return fmt.Sprintf("Weight(%d)", e)
	}
}

// WrapMode wraps PangoWrapMode
//
// `PangoWrapMode` describes how to wrap the lines of a `PangoLayout`
// to the desired width.
// 
// For @PANGO_WRAP_WORD, Pango uses break opportunities that are determined
// by the Unicode line breaking algorithm. For @PANGO_WRAP_CHAR, Pango allows
// breaking at grapheme boundaries that are determined by the Unicode text
// segmentation algorithm.
type WrapMode C.int

const (
	// WrapWord wraps PANGO_WRAP_WORD
	//
	// wrap lines at word boundaries.
	WrapWord WrapMode = 0
	// WrapChar wraps PANGO_WRAP_CHAR
	//
	// wrap lines at character boundaries.
	WrapChar WrapMode = 1
	// WrapWordChar wraps PANGO_WRAP_WORD_CHAR
	//
	// wrap lines at word boundaries, but fall back to
	//   character boundaries if there is not enough space for a full word.
	WrapWordChar WrapMode = 2
	// WrapNone wraps PANGO_WRAP_NONE
	//
	// do not wrap.
	WrapNone WrapMode = 3
)

func marshalWrapMode(p unsafe.Pointer) (any, error) {
	return WrapMode(gobject.ValueFromNative(p).Enum()), nil
}

var _ gobject.GoValueInitializer = WrapMode(0)

func (e WrapMode) GoValueType() gobject.Type {
	return TypeWrapMode
}

func (e WrapMode) SetGoValue(v *gobject.Value) {
	v.SetEnum(int(e))
}

func (e WrapMode) String() string {
	switch e {
		case WrapChar: return "WrapChar"
		case WrapNone: return "WrapNone"
		case WrapWord: return "WrapWord"
		case WrapWordChar: return "WrapWordChar"
		default: return fmt.Sprintf("WrapMode(%d)", e)
	}
}

// FontMask wraps PangoFontMask
//
// The bits in a `PangoFontMask` correspond to the set fields in a
// `PangoFontDescription`.
type FontMask C.gint

const (
	// FontMaskFamily wraps PANGO_FONT_MASK_FAMILY
	//
	// the font family is specified.
	FontMaskFamily FontMask = 1
	// FontMaskStyle wraps PANGO_FONT_MASK_STYLE
	//
	// the font style is specified.
	FontMaskStyle FontMask = 2
	// FontMaskVariant wraps PANGO_FONT_MASK_VARIANT
	//
	// the font variant is specified.
	FontMaskVariant FontMask = 4
	// FontMaskWeight wraps PANGO_FONT_MASK_WEIGHT
	//
	// the font weight is specified.
	FontMaskWeight FontMask = 8
	// FontMaskStretch wraps PANGO_FONT_MASK_STRETCH
	//
	// the font stretch is specified.
	FontMaskStretch FontMask = 16
	// FontMaskSize wraps PANGO_FONT_MASK_SIZE
	//
	// the font size is specified.
	FontMaskSize FontMask = 32
	// FontMaskGravity wraps PANGO_FONT_MASK_GRAVITY
	//
	// The font gravity is specified.
	FontMaskGravity FontMask = 64
	// FontMaskVariations wraps PANGO_FONT_MASK_VARIATIONS
	//
	// OpenType font variations are specified.
	FontMaskVariations FontMask = 128
	// FontMaskFeatures wraps PANGO_FONT_MASK_FEATURES
	//
	// OpenType font features are specified.
	FontMaskFeatures FontMask = 256
)

func marshalFontMask(p unsafe.Pointer) (any, error) {
	return FontMask(gobject.ValueFromNative(p).Flags()), nil
}
// Has returns true if f contains other
func (f FontMask) Has(other FontMask) bool {
	return (f & other) == other
}

var _ gobject.GoValueInitializer = FontMask(0)

func (f FontMask) GoValueType() gobject.Type {
	return TypeFontMask
}

func (f FontMask) SetGoValue(v *gobject.Value) {
	v.SetFlags(int(f))
}

func (f FontMask) String() string {
	if f == 0 {
		return "FontMask(0)"
	}

	var parts []string
	if (f & FontMaskFamily) != 0 {
		parts = append(parts, "FontMaskFamily")
	}
	if (f & FontMaskStyle) != 0 {
		parts = append(parts, "FontMaskStyle")
	}
	if (f & FontMaskVariant) != 0 {
		parts = append(parts, "FontMaskVariant")
	}
	if (f & FontMaskWeight) != 0 {
		parts = append(parts, "FontMaskWeight")
	}
	if (f & FontMaskStretch) != 0 {
		parts = append(parts, "FontMaskStretch")
	}
	if (f & FontMaskSize) != 0 {
		parts = append(parts, "FontMaskSize")
	}
	if (f & FontMaskGravity) != 0 {
		parts = append(parts, "FontMaskGravity")
	}
	if (f & FontMaskVariations) != 0 {
		parts = append(parts, "FontMaskVariations")
	}
	if (f & FontMaskFeatures) != 0 {
		parts = append(parts, "FontMaskFeatures")
	}
	return "FontMask(" + strings.Join(parts, "|") + ")"
}

// LayoutDeserializeFlags wraps PangoLayoutDeserializeFlags
//
// Flags that influence the behavior of [func@Pango.Layout.deserialize].
// 
// New members may be added to this enumeration over time.
type LayoutDeserializeFlags C.gint

const (
	// LayoutDeserializeDefault wraps PANGO_LAYOUT_DESERIALIZE_DEFAULT
	//
	// Default behavior
	LayoutDeserializeDefault LayoutDeserializeFlags = 0
	// LayoutDeserializeContext wraps PANGO_LAYOUT_DESERIALIZE_CONTEXT
	//
	// Apply context information
	//   from the serialization to the `PangoContext`
	LayoutDeserializeContext LayoutDeserializeFlags = 1
)

func marshalLayoutDeserializeFlags(p unsafe.Pointer) (any, error) {
	return LayoutDeserializeFlags(gobject.ValueFromNative(p).Flags()), nil
}
// Has returns true if l contains other
func (l LayoutDeserializeFlags) Has(other LayoutDeserializeFlags) bool {
	return (l & other) == other
}

var _ gobject.GoValueInitializer = LayoutDeserializeFlags(0)

func (f LayoutDeserializeFlags) GoValueType() gobject.Type {
	return TypeLayoutDeserializeFlags
}

func (f LayoutDeserializeFlags) SetGoValue(v *gobject.Value) {
	v.SetFlags(int(f))
}

func (f LayoutDeserializeFlags) String() string {
	if f == 0 {
		return "LayoutDeserializeFlags(0)"
	}

	var parts []string
	if (f & LayoutDeserializeDefault) != 0 {
		parts = append(parts, "LayoutDeserializeDefault")
	}
	if (f & LayoutDeserializeContext) != 0 {
		parts = append(parts, "LayoutDeserializeContext")
	}
	return "LayoutDeserializeFlags(" + strings.Join(parts, "|") + ")"
}

// LayoutSerializeFlags wraps PangoLayoutSerializeFlags
//
// Flags that influence the behavior of [method@Pango.Layout.serialize].
// 
// New members may be added to this enumeration over time.
type LayoutSerializeFlags C.gint

const (
	// LayoutSerializeDefault wraps PANGO_LAYOUT_SERIALIZE_DEFAULT
	//
	// Default behavior
	LayoutSerializeDefault LayoutSerializeFlags = 0
	// LayoutSerializeContext wraps PANGO_LAYOUT_SERIALIZE_CONTEXT
	//
	// Include context information
	LayoutSerializeContext LayoutSerializeFlags = 1
	// LayoutSerializeOutput wraps PANGO_LAYOUT_SERIALIZE_OUTPUT
	//
	// Include information about the formatted output
	LayoutSerializeOutput LayoutSerializeFlags = 2
)

func marshalLayoutSerializeFlags(p unsafe.Pointer) (any, error) {
	return LayoutSerializeFlags(gobject.ValueFromNative(p).Flags()), nil
}
// Has returns true if l contains other
func (l LayoutSerializeFlags) Has(other LayoutSerializeFlags) bool {
	return (l & other) == other
}

var _ gobject.GoValueInitializer = LayoutSerializeFlags(0)

func (f LayoutSerializeFlags) GoValueType() gobject.Type {
	return TypeLayoutSerializeFlags
}

func (f LayoutSerializeFlags) SetGoValue(v *gobject.Value) {
	v.SetFlags(int(f))
}

func (f LayoutSerializeFlags) String() string {
	if f == 0 {
		return "LayoutSerializeFlags(0)"
	}

	var parts []string
	if (f & LayoutSerializeDefault) != 0 {
		parts = append(parts, "LayoutSerializeDefault")
	}
	if (f & LayoutSerializeContext) != 0 {
		parts = append(parts, "LayoutSerializeContext")
	}
	if (f & LayoutSerializeOutput) != 0 {
		parts = append(parts, "LayoutSerializeOutput")
	}
	return "LayoutSerializeFlags(" + strings.Join(parts, "|") + ")"
}

// ShapeFlags wraps PangoShapeFlags
//
// Flags influencing the shaping process.
// 
// `PangoShapeFlags` can be passed to [func@Pango.shape_with_flags].
type ShapeFlags C.gint

const (
	// ShapeNone wraps PANGO_SHAPE_NONE
	//
	// Default value
	ShapeNone ShapeFlags = 0
	// ShapeRoundPositions wraps PANGO_SHAPE_ROUND_POSITIONS
	//
	// Round glyph positions and widths to whole device units
	//   This option should be set if the target renderer can't do subpixel positioning of glyphs
	ShapeRoundPositions ShapeFlags = 1
)

func marshalShapeFlags(p unsafe.Pointer) (any, error) {
	return ShapeFlags(gobject.ValueFromNative(p).Flags()), nil
}
// Has returns true if s contains other
func (s ShapeFlags) Has(other ShapeFlags) bool {
	return (s & other) == other
}

var _ gobject.GoValueInitializer = ShapeFlags(0)

func (f ShapeFlags) GoValueType() gobject.Type {
	return TypeShapeFlags
}

func (f ShapeFlags) SetGoValue(v *gobject.Value) {
	v.SetFlags(int(f))
}

func (f ShapeFlags) String() string {
	if f == 0 {
		return "ShapeFlags(0)"
	}

	var parts []string
	if (f & ShapeNone) != 0 {
		parts = append(parts, "ShapeNone")
	}
	if (f & ShapeRoundPositions) != 0 {
		parts = append(parts, "ShapeRoundPositions")
	}
	return "ShapeFlags(" + strings.Join(parts, "|") + ")"
}

// ShowFlags wraps PangoShowFlags
//
// These flags affect how Pango treats characters that are normally
// not visible in the output.
type ShowFlags C.gint

const (
	// ShowNone wraps PANGO_SHOW_NONE
	//
	// No special treatment for invisible characters
	ShowNone ShowFlags = 0
	// ShowSpaces wraps PANGO_SHOW_SPACES
	//
	// Render spaces, tabs and newlines visibly
	ShowSpaces ShowFlags = 1
	// ShowLineBreaks wraps PANGO_SHOW_LINE_BREAKS
	//
	// Render line breaks visibly
	ShowLineBreaks ShowFlags = 2
	// ShowIgnorables wraps PANGO_SHOW_IGNORABLES
	//
	// Render default-ignorable Unicode
	//   characters visibly
	ShowIgnorables ShowFlags = 4
)

func marshalShowFlags(p unsafe.Pointer) (any, error) {
	return ShowFlags(gobject.ValueFromNative(p).Flags()), nil
}
// Has returns true if s contains other
func (s ShowFlags) Has(other ShowFlags) bool {
	return (s & other) == other
}

var _ gobject.GoValueInitializer = ShowFlags(0)

func (f ShowFlags) GoValueType() gobject.Type {
	return TypeShowFlags
}

func (f ShowFlags) SetGoValue(v *gobject.Value) {
	v.SetFlags(int(f))
}

func (f ShowFlags) String() string {
	if f == 0 {
		return "ShowFlags(0)"
	}

	var parts []string
	if (f & ShowNone) != 0 {
		parts = append(parts, "ShowNone")
	}
	if (f & ShowSpaces) != 0 {
		parts = append(parts, "ShowSpaces")
	}
	if (f & ShowLineBreaks) != 0 {
		parts = append(parts, "ShowLineBreaks")
	}
	if (f & ShowIgnorables) != 0 {
		parts = append(parts, "ShowIgnorables")
	}
	return "ShowFlags(" + strings.Join(parts, "|") + ")"
}

// AttrFilterFunc wraps PangoAttrFilterFunc
// 
// The function takes the following parameters:
// 
// 	- attribute *Attribute: a Pango attribute 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Type of a function filtering a list of attributes.
type AttrFilterFunc func(attribute *Attribute) (goret bool)

// FontsetForEachFunc wraps PangoFontsetForeachFunc
// 
// The function takes the following parameters:
// 
// 	- fontset Fontset: a `PangoFontset` 
// 	- font Font: a font from @fontset 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Callback used when enumerating fonts in a fontset.
// 
// See [method@Pango.Fontset.foreach].
type FontsetForEachFunc func(fontset Fontset, font Font) (goret bool)

// NewAttrAllowBreaks wraps pango_attr_allow_breaks_new
// 
// The function takes the following parameters:
// 
// 	- allowBreaks bool: %TRUE if we line breaks are allowed 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new allow-breaks attribute.
// 
// If breaks are disabled, the range will be kept in a
// single run, as far as possible.
func NewAttrAllowBreaks(allowBreaks bool) *Attribute {
	var carg1 C.gboolean        // in
	var cret  *C.PangoAttribute // return, full, converted

	if allowBreaks {
		carg1 = C.TRUE
	}

	cret = C.pango_attr_allow_breaks_new(carg1)
	runtime.KeepAlive(allowBreaks)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrBackgroundAlpha wraps pango_attr_background_alpha_new
// 
// The function takes the following parameters:
// 
// 	- alpha uint16: the alpha value, between 1 and 65536 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new background alpha attribute.
func NewAttrBackgroundAlpha(alpha uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(alpha)

	cret = C.pango_attr_background_alpha_new(carg1)
	runtime.KeepAlive(alpha)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrBackground wraps pango_attr_background_new
// 
// The function takes the following parameters:
// 
// 	- red uint16: the red value (ranging from 0 to 65535) 
// 	- green uint16: the green value 
// 	- blue uint16: the blue value 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new background color attribute.
func NewAttrBackground(red uint16, green uint16, blue uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var carg2 C.guint16         // in, none, casted
	var carg3 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(red)
	carg2 = C.guint16(green)
	carg3 = C.guint16(blue)

	cret = C.pango_attr_background_new(carg1, carg2, carg3)
	runtime.KeepAlive(red)
	runtime.KeepAlive(green)
	runtime.KeepAlive(blue)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrBaselineShift wraps pango_attr_baseline_shift_new
// 
// The function takes the following parameters:
// 
// 	- shift int32: either a `PangoBaselineShift` enumeration value or an absolute value (&gt; 1024)
//   in Pango units, relative to the baseline of the previous run.
//   Positive values displace the text upwards. 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new baseline displacement attribute.
// 
// The effect of this attribute is to shift the baseline of a run,
// relative to the run of preceding run.
// 
// &lt;picture&gt;
//   &lt;source srcset="baseline-shift-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Baseline Shift" src="baseline-shift-light.png"&gt;
// &lt;/picture&gt;
func NewAttrBaselineShift(shift int32) *Attribute {
	var carg1 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.int(shift)

	cret = C.pango_attr_baseline_shift_new(carg1)
	runtime.KeepAlive(shift)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrFallback wraps pango_attr_fallback_new
// 
// The function takes the following parameters:
// 
// 	- enableFallback bool: %TRUE if we should fall back on other fonts
//   for characters the active font is missing 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font fallback attribute.
// 
// If fallback is disabled, characters will only be
// used from the closest matching font on the system.
// No fallback will be done to other fonts on the system
// that might contain the characters in the text.
func NewAttrFallback(enableFallback bool) *Attribute {
	var carg1 C.gboolean        // in
	var cret  *C.PangoAttribute // return, full, converted

	if enableFallback {
		carg1 = C.TRUE
	}

	cret = C.pango_attr_fallback_new(carg1)
	runtime.KeepAlive(enableFallback)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrFamily wraps pango_attr_family_new
// 
// The function takes the following parameters:
// 
// 	- family string: the family or comma-separated list of families 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font family attribute.
func NewAttrFamily(family string) *Attribute {
	var carg1 *C.char           // in, none, string, casted *C.gchar
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = (*C.char)(unsafe.Pointer(C.CString(family)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_attr_family_new(carg1)
	runtime.KeepAlive(family)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrFontScale wraps pango_attr_font_scale_new
// 
// The function takes the following parameters:
// 
// 	- scale FontScale: a `PangoFontScale` value, which indicates font size change relative
//   to the size of the previous run. 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font scale attribute.
// 
// The effect of this attribute is to change the font size of a run,
// relative to the size of preceding run.
func NewAttrFontScale(scale FontScale) *Attribute {
	var carg1 C.PangoFontScale  // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoFontScale(scale)

	cret = C.pango_attr_font_scale_new(carg1)
	runtime.KeepAlive(scale)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrForegroundAlpha wraps pango_attr_foreground_alpha_new
// 
// The function takes the following parameters:
// 
// 	- alpha uint16: the alpha value, between 1 and 65536 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new foreground alpha attribute.
func NewAttrForegroundAlpha(alpha uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(alpha)

	cret = C.pango_attr_foreground_alpha_new(carg1)
	runtime.KeepAlive(alpha)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrForeground wraps pango_attr_foreground_new
// 
// The function takes the following parameters:
// 
// 	- red uint16: the red value (ranging from 0 to 65535) 
// 	- green uint16: the green value 
// 	- blue uint16: the blue value 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new foreground color attribute.
func NewAttrForeground(red uint16, green uint16, blue uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var carg2 C.guint16         // in, none, casted
	var carg3 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(red)
	carg2 = C.guint16(green)
	carg3 = C.guint16(blue)

	cret = C.pango_attr_foreground_new(carg1, carg2, carg3)
	runtime.KeepAlive(red)
	runtime.KeepAlive(green)
	runtime.KeepAlive(blue)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrGravityHint wraps pango_attr_gravity_hint_new
// 
// The function takes the following parameters:
// 
// 	- hint GravityHint: the gravity hint value 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new gravity hint attribute.
func NewAttrGravityHint(hint GravityHint) *Attribute {
	var carg1 C.PangoGravityHint // in, none, casted
	var cret  *C.PangoAttribute  // return, full, converted

	carg1 = C.PangoGravityHint(hint)

	cret = C.pango_attr_gravity_hint_new(carg1)
	runtime.KeepAlive(hint)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrGravity wraps pango_attr_gravity_new
// 
// The function takes the following parameters:
// 
// 	- gravity Gravity: the gravity value; should not be %PANGO_GRAVITY_AUTO 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new gravity attribute.
func NewAttrGravity(gravity Gravity) *Attribute {
	var carg1 C.PangoGravity    // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoGravity(gravity)

	cret = C.pango_attr_gravity_new(carg1)
	runtime.KeepAlive(gravity)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrInsertHyphens wraps pango_attr_insert_hyphens_new
// 
// The function takes the following parameters:
// 
// 	- insertHyphens bool: %TRUE if hyphens should be inserted 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new insert-hyphens attribute.
// 
// Pango will insert hyphens when breaking lines in
// the middle of a word. This attribute can be used
// to suppress the hyphen.
func NewAttrInsertHyphens(insertHyphens bool) *Attribute {
	var carg1 C.gboolean        // in
	var cret  *C.PangoAttribute // return, full, converted

	if insertHyphens {
		carg1 = C.TRUE
	}

	cret = C.pango_attr_insert_hyphens_new(carg1)
	runtime.KeepAlive(insertHyphens)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrLetterSpacing wraps pango_attr_letter_spacing_new
// 
// The function takes the following parameters:
// 
// 	- letterSpacing int32: amount of extra space to add between
//   graphemes of the text, in Pango units 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new letter-spacing attribute.
func NewAttrLetterSpacing(letterSpacing int32) *Attribute {
	var carg1 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.int(letterSpacing)

	cret = C.pango_attr_letter_spacing_new(carg1)
	runtime.KeepAlive(letterSpacing)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrLineHeight wraps pango_attr_line_height_new
// 
// The function takes the following parameters:
// 
// 	- factor float64: the scaling factor to apply to the logical height 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Modify the height of logical line extents by a factor.
// 
// This affects the values returned by
// [method@Pango.LayoutLine.get_extents],
// [method@Pango.LayoutLine.get_pixel_extents] and
// [method@Pango.LayoutIter.get_line_extents].
func NewAttrLineHeight(factor float64) *Attribute {
	var carg1 C.double          // in, none, casted, casted C.gdouble
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.double(factor)

	cret = C.pango_attr_line_height_new(carg1)
	runtime.KeepAlive(factor)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrLineHeightNewAbsolute wraps pango_attr_line_height_new_absolute
// 
// The function takes the following parameters:
// 
// 	- height int32: the line height, in %PANGO_SCALE-ths of a point 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Override the height of logical line extents to be @height.
// 
// This affects the values returned by
// [method@Pango.LayoutLine.get_extents],
// [method@Pango.LayoutLine.get_pixel_extents] and
// [method@Pango.LayoutIter.get_line_extents].
func AttrLineHeightNewAbsolute(height int32) *Attribute {
	var carg1 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.int(height)

	cret = C.pango_attr_line_height_new_absolute(carg1)
	runtime.KeepAlive(height)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrOverlineColor wraps pango_attr_overline_color_new
// 
// The function takes the following parameters:
// 
// 	- red uint16: the red value (ranging from 0 to 65535) 
// 	- green uint16: the green value 
// 	- blue uint16: the blue value 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new overline color attribute.
// 
// This attribute modifies the color of overlines.
// If not set, overlines will use the foreground color.
func NewAttrOverlineColor(red uint16, green uint16, blue uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var carg2 C.guint16         // in, none, casted
	var carg3 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(red)
	carg2 = C.guint16(green)
	carg3 = C.guint16(blue)

	cret = C.pango_attr_overline_color_new(carg1, carg2, carg3)
	runtime.KeepAlive(red)
	runtime.KeepAlive(green)
	runtime.KeepAlive(blue)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrOverline wraps pango_attr_overline_new
// 
// The function takes the following parameters:
// 
// 	- overline Overline: the overline style 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new overline-style attribute.
func NewAttrOverline(overline Overline) *Attribute {
	var carg1 C.PangoOverline   // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoOverline(overline)

	cret = C.pango_attr_overline_new(carg1)
	runtime.KeepAlive(overline)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrRise wraps pango_attr_rise_new
// 
// The function takes the following parameters:
// 
// 	- rise int32: the amount that the text should be displaced vertically,
//   in Pango units. Positive values displace the text upwards. 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new baseline displacement attribute.
func NewAttrRise(rise int32) *Attribute {
	var carg1 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.int(rise)

	cret = C.pango_attr_rise_new(carg1)
	runtime.KeepAlive(rise)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrScale wraps pango_attr_scale_new
// 
// The function takes the following parameters:
// 
// 	- scaleFactor float64: factor to scale the font 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font size scale attribute.
// 
// The base font for the affected text will have
// its size multiplied by @scale_factor.
func NewAttrScale(scaleFactor float64) *Attribute {
	var carg1 C.double          // in, none, casted, casted C.gdouble
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.double(scaleFactor)

	cret = C.pango_attr_scale_new(carg1)
	runtime.KeepAlive(scaleFactor)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrSentence wraps pango_attr_sentence_new
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Marks the range of the attribute as a single sentence.
// 
// Note that this may require adjustments to word and
// sentence classification around the range.
func NewAttrSentence() *Attribute {
	var cret *C.PangoAttribute // return, full, converted

	cret = C.pango_attr_sentence_new()

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrShow wraps pango_attr_show_new
// 
// The function takes the following parameters:
// 
// 	- flags ShowFlags: `PangoShowFlags` to apply 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new attribute that influences how invisible
// characters are rendered.
func NewAttrShow(flags ShowFlags) *Attribute {
	var carg1 C.PangoShowFlags  // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoShowFlags(flags)

	cret = C.pango_attr_show_new(carg1)
	runtime.KeepAlive(flags)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrStretch wraps pango_attr_stretch_new
// 
// The function takes the following parameters:
// 
// 	- stretch Stretch: the stretch 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font stretch attribute.
func NewAttrStretch(stretch Stretch) *Attribute {
	var carg1 C.PangoStretch    // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoStretch(stretch)

	cret = C.pango_attr_stretch_new(carg1)
	runtime.KeepAlive(stretch)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrStrikethroughColor wraps pango_attr_strikethrough_color_new
// 
// The function takes the following parameters:
// 
// 	- red uint16: the red value (ranging from 0 to 65535) 
// 	- green uint16: the green value 
// 	- blue uint16: the blue value 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new strikethrough color attribute.
// 
// This attribute modifies the color of strikethrough lines.
// If not set, strikethrough lines will use the foreground color.
func NewAttrStrikethroughColor(red uint16, green uint16, blue uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var carg2 C.guint16         // in, none, casted
	var carg3 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(red)
	carg2 = C.guint16(green)
	carg3 = C.guint16(blue)

	cret = C.pango_attr_strikethrough_color_new(carg1, carg2, carg3)
	runtime.KeepAlive(red)
	runtime.KeepAlive(green)
	runtime.KeepAlive(blue)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrStrikethrough wraps pango_attr_strikethrough_new
// 
// The function takes the following parameters:
// 
// 	- strikethrough bool: %TRUE if the text should be struck-through 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new strike-through attribute.
func NewAttrStrikethrough(strikethrough bool) *Attribute {
	var carg1 C.gboolean        // in
	var cret  *C.PangoAttribute // return, full, converted

	if strikethrough {
		carg1 = C.TRUE
	}

	cret = C.pango_attr_strikethrough_new(carg1)
	runtime.KeepAlive(strikethrough)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrStyle wraps pango_attr_style_new
// 
// The function takes the following parameters:
// 
// 	- style Style: the slant style 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font slant style attribute.
func NewAttrStyle(style Style) *Attribute {
	var carg1 C.PangoStyle      // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoStyle(style)

	cret = C.pango_attr_style_new(carg1)
	runtime.KeepAlive(style)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrTextTransform wraps pango_attr_text_transform_new
// 
// The function takes the following parameters:
// 
// 	- transform TextTransform: `PangoTextTransform` to apply 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new attribute that influences how characters
// are transformed during shaping.
func NewAttrTextTransform(transform TextTransform) *Attribute {
	var carg1 C.PangoTextTransform // in, none, casted
	var cret  *C.PangoAttribute    // return, full, converted

	carg1 = C.PangoTextTransform(transform)

	cret = C.pango_attr_text_transform_new(carg1)
	runtime.KeepAlive(transform)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrUnderlineColor wraps pango_attr_underline_color_new
// 
// The function takes the following parameters:
// 
// 	- red uint16: the red value (ranging from 0 to 65535) 
// 	- green uint16: the green value 
// 	- blue uint16: the blue value 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new underline color attribute.
// 
// This attribute modifies the color of underlines.
// If not set, underlines will use the foreground color.
func NewAttrUnderlineColor(red uint16, green uint16, blue uint16) *Attribute {
	var carg1 C.guint16         // in, none, casted
	var carg2 C.guint16         // in, none, casted
	var carg3 C.guint16         // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.guint16(red)
	carg2 = C.guint16(green)
	carg3 = C.guint16(blue)

	cret = C.pango_attr_underline_color_new(carg1, carg2, carg3)
	runtime.KeepAlive(red)
	runtime.KeepAlive(green)
	runtime.KeepAlive(blue)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrUnderline wraps pango_attr_underline_new
// 
// The function takes the following parameters:
// 
// 	- underline Underline: the underline style 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new underline-style attribute.
func NewAttrUnderline(underline Underline) *Attribute {
	var carg1 C.PangoUnderline  // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoUnderline(underline)

	cret = C.pango_attr_underline_new(carg1)
	runtime.KeepAlive(underline)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrVariant wraps pango_attr_variant_new
// 
// The function takes the following parameters:
// 
// 	- variant Variant: the variant 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font variant attribute (normal or small caps).
func NewAttrVariant(variant Variant) *Attribute {
	var carg1 C.PangoVariant    // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoVariant(variant)

	cret = C.pango_attr_variant_new(carg1)
	runtime.KeepAlive(variant)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrWeight wraps pango_attr_weight_new
// 
// The function takes the following parameters:
// 
// 	- weight Weight: the weight 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font weight attribute.
func NewAttrWeight(weight Weight) *Attribute {
	var carg1 C.PangoWeight     // in, none, casted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.PangoWeight(weight)

	cret = C.pango_attr_weight_new(carg1)
	runtime.KeepAlive(weight)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrWord wraps pango_attr_word_new
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Marks the range of the attribute as a single word.
// 
// Note that this may require adjustments to word and
// sentence classification around the range.
func NewAttrWord() *Attribute {
	var cret *C.PangoAttribute // return, full, converted

	cret = C.pango_attr_word_new()

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ExtentsToPixels wraps pango_extents_to_pixels
// 
// The function takes the following parameters:
// 
// 	- inclusive *Rectangle (nullable): rectangle to round to pixels inclusively 
// 	- nearest *Rectangle (nullable): rectangle to round to nearest pixels 
//
// Converts extents from Pango units to device units.
// 
// The conversion is done by dividing by the %PANGO_SCALE factor and
// performing rounding.
// 
// The @inclusive rectangle is converted by flooring the x/y coordinates
// and extending width/height, such that the final rectangle completely
// includes the original rectangle.
// 
// The @nearest rectangle is converted by rounding the coordinates
// of the rectangle to the nearest device unit (pixel).
// 
// The rule to which argument to use is: if you want the resulting device-space
// rectangle to completely contain the original rectangle, pass it in as
// @inclusive. If you want two touching-but-not-overlapping rectangles stay
// touching-but-not-overlapping after rounding to device units, pass them in
// as @nearest.
func ExtentsToPixels(inclusive *Rectangle, nearest *Rectangle) {
	var carg1 *C.PangoRectangle // in, none, converted, nullable
	var carg2 *C.PangoRectangle // in, none, converted, nullable

	if inclusive != nil {
		carg1 = (*C.PangoRectangle)(UnsafeRectangleToGlibNone(inclusive))
	}
	if nearest != nil {
		carg2 = (*C.PangoRectangle)(UnsafeRectangleToGlibNone(nearest))
	}

	C.pango_extents_to_pixels(carg1, carg2)
	runtime.KeepAlive(inclusive)
	runtime.KeepAlive(nearest)
}

// FindBaseDir wraps pango_find_base_dir
// 
// The function takes the following parameters:
// 
// 	- text string: the text to process. Must be valid UTF-8 
// 	- length int32: length of @text in bytes (may be -1 if @text is nul-terminated) 
// 
// The function returns the following values:
// 
// 	- goret Direction 
//
// Searches a string the first character that has a strong
// direction, according to the Unicode bidirectional algorithm.
func FindBaseDir(text string, length int32) Direction {
	var carg1 *C.gchar         // in, none, string
	var carg2 C.gint           // in, none, casted
	var cret  C.PangoDirection // return, none, casted

	carg1 = (*C.gchar)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.gint(length)

	cret = C.pango_find_base_dir(carg1, carg2)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)

	var goret Direction

	goret = Direction(cret)

	return goret
}

// FindParagraphBoundary wraps pango_find_paragraph_boundary
// 
// The function takes the following parameters:
// 
// 	- text string: UTF-8 text 
// 	- length int32: length of @text in bytes, or -1 if nul-terminated 
// 
// The function returns the following values:
// 
// 	- paragraphDelimiterIndex int32: return location for index of
//   delimiter 
// 	- nextParagraphStart int32: return location for start of next
//   paragraph 
//
// Locates a paragraph boundary in @text.
// 
// A boundary is caused by delimiter characters, such as
// a newline, carriage return, carriage return-newline pair,
// or Unicode paragraph separator character.
// 
// The index of the run of delimiters is returned in
// @paragraph_delimiter_index. The index of the start of the
// next paragraph (index after all delimiters) is stored n
// @next_paragraph_start.
// 
// If no delimiters are found, both @paragraph_delimiter_index
// and @next_paragraph_start are filled with the length of @text
// (an index one off the end).
func FindParagraphBoundary(text string, length int32) (int32, int32) {
	var carg1 *C.char // in, none, string, casted *C.gchar
	var carg2 C.int   // in, none, casted, casted C.gint
	var carg3 C.int   // out, full, casted, casted C.gint
	var carg4 C.int   // out, full, casted, casted C.gint

	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)

	C.pango_find_paragraph_boundary(carg1, carg2, &carg3, &carg4)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)

	var paragraphDelimiterIndex int32
	var nextParagraphStart      int32

	paragraphDelimiterIndex = int32(carg3)
	nextParagraphStart = int32(carg4)

	return paragraphDelimiterIndex, nextParagraphStart
}

// GetMirrorChar wraps pango_get_mirror_char
// 
// The function takes the following parameters:
// 
// 	- ch uint32: a Unicode character 
// 
// The function returns the following values:
// 
// 	- mirroredCh uint32: location to store the mirrored character 
// 	- goret bool 
//
// Returns the mirrored character of a Unicode character.
// 
// Mirror characters are determined by the Unicode mirrored property.
//
// Deprecated: (since 1.30.0) Use [func@GLib.unichar_get_mirror_char] instead;
//   the docs for that function provide full details.
func GetMirrorChar(ch uint32) (uint32, bool) {
	var carg1 C.gunichar // in, none, casted
	var carg2 C.gunichar // out, full, casted
	var cret  C.gboolean // return

	carg1 = C.gunichar(ch)

	cret = C.pango_get_mirror_char(carg1, &carg2)
	runtime.KeepAlive(ch)

	var mirroredCh uint32
	var goret      bool

	mirroredCh = uint32(carg2)
	if cret != 0 {
		goret = true
	}

	return mirroredCh, goret
}

// IsZeroWidth wraps pango_is_zero_width
// 
// The function takes the following parameters:
// 
// 	- ch uint32: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Checks if a character that should not be normally rendered.
// 
// This includes all Unicode characters with "ZERO WIDTH" in their name,
// as well as *bidi* formatting characters, and a few other ones.
// 
// This is totally different from [func@GLib.unichar_iszerowidth] and is at best misnamed.
func IsZeroWidth(ch uint32) bool {
	var carg1 C.gunichar // in, none, casted
	var cret  C.gboolean // return

	carg1 = C.gunichar(ch)

	cret = C.pango_is_zero_width(carg1)
	runtime.KeepAlive(ch)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Itemize wraps pango_itemize
// 
// The function takes the following parameters:
// 
// 	- _context Context: a structure holding information that affects
//   the itemization process. 
// 	- text string: the text to itemize. Must be valid UTF-8 
// 	- startIndex int32: first byte in @text to process 
// 	- length int32: the number of bytes (not characters) to process
//   after @start_index. This must be &gt;= 0. 
// 	- attrs *AttrList: the set of attributes that apply to @text. 
// 	- cachedIter *AttrIterator (nullable): Cached attribute iterator 
// 
// The function returns the following values:
// 
// 	- goret []*Item 
//
// Breaks a piece of text into segments with consistent directional
// level and font.
// 
// Each byte of @text will be contained in exactly one of the items in the
// returned list; the generated list of items will be in logical order (the
// start offsets of the items are ascending).
// 
// @cached_iter should be an iterator over @attrs currently positioned
// at a range before or containing @start_index; @cached_iter will be
// advanced to the range covering the position just after
// @start_index + @length. (i.e. if itemizing in a loop, just keep passing
// in the same @cached_iter).
func Itemize(_context Context, text string, startIndex int32, length int32, attrs *AttrList, cachedIter *AttrIterator) []*Item {
	var carg1 *C.PangoContext      // in, none, converted
	var carg2 *C.char              // in, none, string, casted *C.gchar
	var carg3 C.int                // in, none, casted, casted C.gint
	var carg4 C.int                // in, none, casted, casted C.gint
	var carg5 *C.PangoAttrList     // in, none, converted
	var carg6 *C.PangoAttrIterator // in, none, converted, nullable
	var cret  *C.GList             // container, transfer: full

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg2))
	carg3 = C.int(startIndex)
	carg4 = C.int(length)
	carg5 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(attrs))
	if cachedIter != nil {
		carg6 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(cachedIter))
	}

	cret = C.pango_itemize(carg1, carg2, carg3, carg4, carg5, carg6)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(text)
	runtime.KeepAlive(startIndex)
	runtime.KeepAlive(length)
	runtime.KeepAlive(attrs)
	runtime.KeepAlive(cachedIter)

	var goret []*Item

	goret = glib.UnsafeListFromGlibFull(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *Item {
			var dst *Item // converted
			dst = UnsafeItemFromGlibFull(v)
			return dst
		},
	)

	return goret
}

// ItemizeWithBaseDir wraps pango_itemize_with_base_dir
// 
// The function takes the following parameters:
// 
// 	- _context Context: a structure holding information that affects
//   the itemization process. 
// 	- baseDir Direction: base direction to use for bidirectional processing 
// 	- text string: the text to itemize. 
// 	- startIndex int32: first byte in @text to process 
// 	- length int32: the number of bytes (not characters) to process
//   after @start_index. This must be &gt;= 0. 
// 	- attrs *AttrList: the set of attributes that apply to @text. 
// 	- cachedIter *AttrIterator (nullable): Cached attribute iterator 
// 
// The function returns the following values:
// 
// 	- goret []*Item 
//
// Like `pango_itemize()`, but with an explicitly specified base direction.
// 
// The base direction is used when computing bidirectional levels.
// [func@itemize] gets the base direction from the `PangoContext`
// (see [method@Pango.Context.set_base_dir]).
func ItemizeWithBaseDir(_context Context, baseDir Direction, text string, startIndex int32, length int32, attrs *AttrList, cachedIter *AttrIterator) []*Item {
	var carg1 *C.PangoContext      // in, none, converted
	var carg2 C.PangoDirection     // in, none, casted
	var carg3 *C.char              // in, none, string, casted *C.gchar
	var carg4 C.int                // in, none, casted, casted C.gint
	var carg5 C.int                // in, none, casted, casted C.gint
	var carg6 *C.PangoAttrList     // in, none, converted
	var carg7 *C.PangoAttrIterator // in, none, converted, nullable
	var cret  *C.GList             // container, transfer: full

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = C.PangoDirection(baseDir)
	carg3 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg3))
	carg4 = C.int(startIndex)
	carg5 = C.int(length)
	carg6 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(attrs))
	if cachedIter != nil {
		carg7 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(cachedIter))
	}

	cret = C.pango_itemize_with_base_dir(carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(baseDir)
	runtime.KeepAlive(text)
	runtime.KeepAlive(startIndex)
	runtime.KeepAlive(length)
	runtime.KeepAlive(attrs)
	runtime.KeepAlive(cachedIter)

	var goret []*Item

	goret = glib.UnsafeListFromGlibFull(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *Item {
			var dst *Item // converted
			dst = UnsafeItemFromGlibFull(v)
			return dst
		},
	)

	return goret
}

// MarkupParserFinish wraps pango_markup_parser_finish
// 
// The function takes the following parameters:
// 
// 	- _context *glib.MarkupParseContext: A valid parse context that was returned from [func@markup_parser_new] 
// 
// The function returns the following values:
// 
// 	- attrList *AttrList: address of return location for a `PangoAttrList` 
// 	- text string: address of return location for text with tags stripped 
// 	- accelChar uint32: address of return location for accelerator char 
// 	- goret bool 
// 	- _goerr error (nullable): an error 
//
// Finishes parsing markup.
// 
// After feeding a Pango markup parser some data with [method@GLib.MarkupParseContext.parse],
// use this function to get the list of attributes and text out of the
// markup. This function will not free @context, use [method@GLib.MarkupParseContext.free]
// to do so.
func MarkupParserFinish(_context *glib.MarkupParseContext) (*AttrList, string, uint32, bool, error) {
	var carg1 *C.GMarkupParseContext // in, none, converted
	var carg2 *C.PangoAttrList       // out, full, converted
	var carg3 *C.char                // out, full, string, casted *C.gchar
	var carg4 C.gunichar             // out, full, casted
	var cret  C.gboolean             // return
	var _cerr *C.GError              // out, full, converted, nullable

	carg1 = (*C.GMarkupParseContext)(glib.UnsafeMarkupParseContextToGlibNone(_context))

	cret = C.pango_markup_parser_finish(carg1, &carg2, &carg3, &carg4, &_cerr)
	runtime.KeepAlive(_context)

	var attrList  *AttrList
	var text      string
	var accelChar uint32
	var goret     bool
	var _goerr    error

	attrList = UnsafeAttrListFromGlibFull(unsafe.Pointer(carg2))
	text = C.GoString((*C.char)(unsafe.Pointer(carg3)))
	defer C.free(unsafe.Pointer(carg3))
	accelChar = uint32(carg4)
	if cret != 0 {
		goret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return attrList, text, accelChar, goret, _goerr
}

// NewMarkupParser wraps pango_markup_parser_new
// 
// The function takes the following parameters:
// 
// 	- accelMarker uint32: character that precedes an accelerator, or 0 for none 
// 
// The function returns the following values:
// 
// 	- goret *glib.MarkupParseContext 
//
// Incrementally parses marked-up text to create a plain-text string
// and an attribute list.
// 
// See the [Pango Markup](pango_markup.html) docs for details about the
// supported markup.
// 
// If @accel_marker is nonzero, the given character will mark the
// character following it as an accelerator. For example, @accel_marker
// might be an ampersand or underscore. All characters marked
// as an accelerator will receive a %PANGO_UNDERLINE_LOW attribute,
// and the first character so marked will be returned in @accel_char,
// when calling [func@markup_parser_finish]. Two @accel_marker characters
// following each other produce a single literal @accel_marker character.
// 
// To feed markup to the parser, use [method@GLib.MarkupParseContext.parse]
// on the returned [struct@GLib.MarkupParseContext]. When done with feeding markup
// to the parser, use [func@markup_parser_finish] to get the data out
// of it, and then use [method@GLib.MarkupParseContext.free] to free it.
// 
// This function is designed for applications that read Pango markup
// from streams. To simply parse a string containing Pango markup,
// the [func@Pango.parse_markup] API is recommended instead.
func NewMarkupParser(accelMarker uint32) *glib.MarkupParseContext {
	var carg1 C.gunichar             // in, none, casted
	var cret  *C.GMarkupParseContext // return, none, converted

	carg1 = C.gunichar(accelMarker)

	cret = C.pango_markup_parser_new(carg1)
	runtime.KeepAlive(accelMarker)

	var goret *glib.MarkupParseContext

	goret = glib.UnsafeMarkupParseContextFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// ParseEnum wraps pango_parse_enum
// 
// The function takes the following parameters:
// 
// 	- typ gobject.Type: enum type to parse, eg. %PANGO_TYPE_ELLIPSIZE_MODE 
// 	- str string (nullable): string to parse 
// 	- warn bool: if %TRUE, issue a g_warning() on bad input 
// 
// The function returns the following values:
// 
// 	- value int32: integer to store the result in 
// 	- possibleValues string: place to store list of possible
//   values on failure 
// 	- goret bool 
//
// Parses an enum type and stores the result in @value.
// 
// If @str does not match the nick name of any of the possible values
// for the enum and is not an integer, %FALSE is returned, a warning
// is issued if @warn is %TRUE, and a string representing the list of
// possible values is stored in @possible_values. The list is
// slash-separated, eg. "none/start/middle/end".
// 
// If failed and @possible_values is not %NULL, returned string should
// be freed using g_free().
//
// Deprecated: (since 1.38.0) 
func ParseEnum(typ gobject.Type, str string, warn bool) (int32, string, bool) {
	var carg1 C.GType    // in, none, casted, alias
	var carg2 *C.char    // in, none, string, nullable-string
	var carg4 C.gboolean // in
	var carg3 C.int      // out, full, casted, casted C.gint
	var carg5 *C.char    // out, full, string, casted *C.gchar
	var cret  C.gboolean // return

	carg1 = C.GType(typ)
	if str != "" {
		carg2 = (*C.char)(unsafe.Pointer(C.CString(str)))
		defer C.free(unsafe.Pointer(carg2))
	}
	if warn {
		carg4 = C.TRUE
	}

	cret = C.pango_parse_enum(carg1, carg2, &carg3, carg4, &carg5)
	runtime.KeepAlive(typ)
	runtime.KeepAlive(str)
	runtime.KeepAlive(warn)

	var value          int32
	var possibleValues string
	var goret          bool

	value = int32(carg3)
	possibleValues = C.GoString((*C.char)(unsafe.Pointer(carg5)))
	defer C.free(unsafe.Pointer(carg5))
	if cret != 0 {
		goret = true
	}

	return value, possibleValues, goret
}

// ParseMarkup wraps pango_parse_markup
// 
// The function takes the following parameters:
// 
// 	- markupText string: markup to parse (see the [Pango Markup](pango_markup.html) docs) 
// 	- length int32: length of @markup_text, or -1 if nul-terminated 
// 	- accelMarker uint32: character that precedes an accelerator, or 0 for none 
// 
// The function returns the following values:
// 
// 	- attrList *AttrList: address of return location for a `PangoAttrList` 
// 	- text string: address of return location for text with tags stripped 
// 	- accelChar uint32: address of return location for accelerator char 
// 	- goret bool 
// 	- _goerr error (nullable): an error 
//
// Parses marked-up text to create a plain-text string and an attribute list.
// 
// See the [Pango Markup](pango_markup.html) docs for details about the
// supported markup.
// 
// If @accel_marker is nonzero, the given character will mark the
// character following it as an accelerator. For example, @accel_marker
// might be an ampersand or underscore. All characters marked
// as an accelerator will receive a %PANGO_UNDERLINE_LOW attribute,
// and the first character so marked will be returned in @accel_char.
// Two @accel_marker characters following each other produce a single
// literal @accel_marker character.
// 
// To parse a stream of pango markup incrementally, use [func@markup_parser_new].
// 
// If any error happens, none of the output arguments are touched except
// for @error.
func ParseMarkup(markupText string, length int32, accelMarker uint32) (*AttrList, string, uint32, bool, error) {
	var carg1 *C.char          // in, none, string, casted *C.gchar
	var carg2 C.int            // in, none, casted, casted C.gint
	var carg3 C.gunichar       // in, none, casted
	var carg4 *C.PangoAttrList // out, full, converted
	var carg5 *C.char          // out, full, string, casted *C.gchar
	var carg6 C.gunichar       // out, full, casted
	var cret  C.gboolean       // return
	var _cerr *C.GError        // out, full, converted, nullable

	carg1 = (*C.char)(unsafe.Pointer(C.CString(markupText)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)
	carg3 = C.gunichar(accelMarker)

	cret = C.pango_parse_markup(carg1, carg2, carg3, &carg4, &carg5, &carg6, &_cerr)
	runtime.KeepAlive(markupText)
	runtime.KeepAlive(length)
	runtime.KeepAlive(accelMarker)

	var attrList  *AttrList
	var text      string
	var accelChar uint32
	var goret     bool
	var _goerr    error

	attrList = UnsafeAttrListFromGlibFull(unsafe.Pointer(carg4))
	text = C.GoString((*C.char)(unsafe.Pointer(carg5)))
	defer C.free(unsafe.Pointer(carg5))
	accelChar = uint32(carg6)
	if cret != 0 {
		goret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return attrList, text, accelChar, goret, _goerr
}

// ParseStretch wraps pango_parse_stretch
// 
// The function takes the following parameters:
// 
// 	- str string: a string to parse. 
// 	- warn bool: if %TRUE, issue a g_warning() on bad input. 
// 
// The function returns the following values:
// 
// 	- stretch Stretch: a `PangoStretch` to store the result in. 
// 	- goret bool 
//
// Parses a font stretch.
// 
// The allowed values are
// "ultra_condensed", "extra_condensed", "condensed",
// "semi_condensed", "normal", "semi_expanded", "expanded",
// "extra_expanded" and "ultra_expanded". Case variations are
// ignored and the '_' characters may be omitted.
func ParseStretch(str string, warn bool) (Stretch, bool) {
	var carg1 *C.char        // in, none, string, casted *C.gchar
	var carg3 C.gboolean     // in
	var carg2 C.PangoStretch // out, full, casted
	var cret  C.gboolean     // return

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))
	if warn {
		carg3 = C.TRUE
	}

	cret = C.pango_parse_stretch(carg1, &carg2, carg3)
	runtime.KeepAlive(str)
	runtime.KeepAlive(warn)

	var stretch Stretch
	var goret   bool

	stretch = Stretch(carg2)
	if cret != 0 {
		goret = true
	}

	return stretch, goret
}

// ParseStyle wraps pango_parse_style
// 
// The function takes the following parameters:
// 
// 	- str string: a string to parse. 
// 	- warn bool: if %TRUE, issue a g_warning() on bad input. 
// 
// The function returns the following values:
// 
// 	- style Style: a `PangoStyle` to store the result in. 
// 	- goret bool 
//
// Parses a font style.
// 
// The allowed values are "normal", "italic" and "oblique", case
// variations being
// ignored.
func ParseStyle(str string, warn bool) (Style, bool) {
	var carg1 *C.char      // in, none, string, casted *C.gchar
	var carg3 C.gboolean   // in
	var carg2 C.PangoStyle // out, full, casted
	var cret  C.gboolean   // return

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))
	if warn {
		carg3 = C.TRUE
	}

	cret = C.pango_parse_style(carg1, &carg2, carg3)
	runtime.KeepAlive(str)
	runtime.KeepAlive(warn)

	var style Style
	var goret bool

	style = Style(carg2)
	if cret != 0 {
		goret = true
	}

	return style, goret
}

// ParseVariant wraps pango_parse_variant
// 
// The function takes the following parameters:
// 
// 	- str string: a string to parse. 
// 	- warn bool: if %TRUE, issue a g_warning() on bad input. 
// 
// The function returns the following values:
// 
// 	- variant Variant: a `PangoVariant` to store the result in. 
// 	- goret bool 
//
// Parses a font variant.
// 
// The allowed values are "normal", "small-caps", "all-small-caps",
// "petite-caps", "all-petite-caps", "unicase" and "title-caps",
// case variations being ignored.
func ParseVariant(str string, warn bool) (Variant, bool) {
	var carg1 *C.char        // in, none, string, casted *C.gchar
	var carg3 C.gboolean     // in
	var carg2 C.PangoVariant // out, full, casted
	var cret  C.gboolean     // return

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))
	if warn {
		carg3 = C.TRUE
	}

	cret = C.pango_parse_variant(carg1, &carg2, carg3)
	runtime.KeepAlive(str)
	runtime.KeepAlive(warn)

	var variant Variant
	var goret   bool

	variant = Variant(carg2)
	if cret != 0 {
		goret = true
	}

	return variant, goret
}

// ParseWeight wraps pango_parse_weight
// 
// The function takes the following parameters:
// 
// 	- str string: a string to parse. 
// 	- warn bool: if %TRUE, issue a g_warning() on bad input. 
// 
// The function returns the following values:
// 
// 	- weight Weight: a `PangoWeight` to store the result in. 
// 	- goret bool 
//
// Parses a font weight.
// 
// The allowed values are "heavy",
// "ultrabold", "bold", "normal", "light", "ultraleight"
// and integers. Case variations are ignored.
func ParseWeight(str string, warn bool) (Weight, bool) {
	var carg1 *C.char       // in, none, string, casted *C.gchar
	var carg3 C.gboolean    // in
	var carg2 C.PangoWeight // out, full, casted
	var cret  C.gboolean    // return

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))
	if warn {
		carg3 = C.TRUE
	}

	cret = C.pango_parse_weight(carg1, &carg2, carg3)
	runtime.KeepAlive(str)
	runtime.KeepAlive(warn)

	var weight Weight
	var goret  bool

	weight = Weight(carg2)
	if cret != 0 {
		goret = true
	}

	return weight, goret
}

// Shape wraps pango_shape
// 
// The function takes the following parameters:
// 
// 	- text string: the text to process 
// 	- length int32: the length (in bytes) of @text 
// 	- analysis *Analysis: `PangoAnalysis` structure from [func@Pango.itemize] 
// 
// The function returns the following values:
// 
// 	- glyphs GlyphString: glyph string in which to store results 
//
// Convert the characters in @text into glyphs.
// 
// Given a segment of text and the corresponding `PangoAnalysis` structure
// returned from [func@Pango.itemize], convert the characters into glyphs. You
// may also pass in only a substring of the item from [func@Pango.itemize].
// 
// It is recommended that you use [func@Pango.shape_full] instead, since
// that API allows for shaping interaction happening across text item
// boundaries.
// 
// Some aspects of hyphen insertion and text transformation (in particular,
// capitalization) require log attrs, and thus can only be handled by
// [func@Pango.shape_item].
// 
// Note that the extra attributes in the @analyis that is returned from
// [func@Pango.itemize] have indices that are relative to the entire paragraph,
// so you need to subtract the item offset from their indices before
// calling [func@Pango.shape].
func Shape(text string, length int32, analysis *Analysis) GlyphString {
	var carg1 *C.char            // in, none, string, casted *C.gchar
	var carg2 C.int              // in, none, casted, casted C.gint
	var carg3 *C.PangoAnalysis   // in, none, converted
	var carg4 C.PangoGlyphString // out, transfer: none, C Pointers: 0, Name: GlyphString, caller-allocates

	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)
	carg3 = (*C.PangoAnalysis)(UnsafeAnalysisToGlibNone(analysis))

	C.pango_shape(carg1, carg2, carg3, &carg4)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)
	runtime.KeepAlive(analysis)

	var glyphs GlyphString

	_ = glyphs
	_ = carg4
	panic("unimplemented conversion of GlyphString (PangoGlyphString)")

	return glyphs
}

// ShapeFull wraps pango_shape_full
// 
// The function takes the following parameters:
// 
// 	- itemText string: valid UTF-8 text to shape. 
// 	- itemLength int32: the length (in bytes) of @item_text. -1 means nul-terminated text. 
// 	- paragraphText string (nullable): text of the paragraph (see details). 
// 	- paragraphLength int32: the length (in bytes) of @paragraph_text. -1 means nul-terminated text. 
// 	- analysis *Analysis: `PangoAnalysis` structure from [func@Pango.itemize]. 
// 
// The function returns the following values:
// 
// 	- glyphs GlyphString: glyph string in which to store results. 
//
// Convert the characters in @text into glyphs.
// 
// Given a segment of text and the corresponding `PangoAnalysis` structure
// returned from [func@Pango.itemize], convert the characters into glyphs.
// You may also pass in only a substring of the item from [func@Pango.itemize].
// 
// This is similar to [func@Pango.shape], except it also can optionally take
// the full paragraph text as input, which will then be used to perform
// certain cross-item shaping interactions. If you have access to the broader
// text of which @item_text is part of, provide the broader text as
// @paragraph_text. If @paragraph_text is %NULL, item text is used instead.
// 
// Some aspects of hyphen insertion and text transformation (in particular,
// capitalization) require log attrs, and thus can only be handled by
// [func@Pango.shape_item].
// 
// Note that the extra attributes in the @analyis that is returned from
// [func@Pango.itemize] have indices that are relative to the entire paragraph,
// so you do not pass the full paragraph text as @paragraph_text, you need
// to subtract the item offset from their indices before calling
// [func@Pango.shape_full].
func ShapeFull(itemText string, itemLength int32, paragraphText string, paragraphLength int32, analysis *Analysis) GlyphString {
	var carg1 *C.char            // in, none, string, casted *C.gchar
	var carg2 C.int              // in, none, casted, casted C.gint
	var carg3 *C.char            // in, none, string, nullable-string
	var carg4 C.int              // in, none, casted, casted C.gint
	var carg5 *C.PangoAnalysis   // in, none, converted
	var carg6 C.PangoGlyphString // out, transfer: none, C Pointers: 0, Name: GlyphString, caller-allocates

	carg1 = (*C.char)(unsafe.Pointer(C.CString(itemText)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(itemLength)
	if paragraphText != "" {
		carg3 = (*C.char)(unsafe.Pointer(C.CString(paragraphText)))
		defer C.free(unsafe.Pointer(carg3))
	}
	carg4 = C.int(paragraphLength)
	carg5 = (*C.PangoAnalysis)(UnsafeAnalysisToGlibNone(analysis))

	C.pango_shape_full(carg1, carg2, carg3, carg4, carg5, &carg6)
	runtime.KeepAlive(itemText)
	runtime.KeepAlive(itemLength)
	runtime.KeepAlive(paragraphText)
	runtime.KeepAlive(paragraphLength)
	runtime.KeepAlive(analysis)

	var glyphs GlyphString

	_ = glyphs
	_ = carg6
	panic("unimplemented conversion of GlyphString (PangoGlyphString)")

	return glyphs
}

// ShapeItem wraps pango_shape_item
// 
// The function takes the following parameters:
// 
// 	- item *Item: `PangoItem` to shape 
// 	- paragraphText string (nullable): text of the paragraph (see details). 
// 	- paragraphLength int32: the length (in bytes) of @paragraph_text.
//     -1 means nul-terminated text. 
// 	- logAttrs *LogAttr (nullable): array of `PangoLogAttr` for @item 
// 	- flags ShapeFlags: flags influencing the shaping process 
// 
// The function returns the following values:
// 
// 	- glyphs GlyphString: glyph string in which to store results 
//
// Convert the characters in @item into glyphs.
// 
// This is similar to [func@Pango.shape_with_flags], except it takes a
// `PangoItem` instead of separate @item_text and @analysis arguments.
// 
// It also takes @log_attrs, which are needed for implementing some aspects
// of hyphen insertion and text transforms (in particular, capitalization).
// 
// Note that the extra attributes in the @analyis that is returned from
// [func@Pango.itemize] have indices that are relative to the entire paragraph,
// so you do not pass the full paragraph text as @paragraph_text, you need
// to subtract the item offset from their indices before calling
// [func@Pango.shape_with_flags].
func ShapeItem(item *Item, paragraphText string, paragraphLength int32, logAttrs *LogAttr, flags ShapeFlags) GlyphString {
	var carg1 *C.PangoItem       // in, none, converted
	var carg2 *C.char            // in, none, string, nullable-string
	var carg3 C.int              // in, none, casted, casted C.gint
	var carg4 *C.PangoLogAttr    // in, none, converted, nullable
	var carg6 C.PangoShapeFlags  // in, none, casted
	var carg5 C.PangoGlyphString // out, transfer: none, C Pointers: 0, Name: GlyphString, caller-allocates

	carg1 = (*C.PangoItem)(UnsafeItemToGlibNone(item))
	if paragraphText != "" {
		carg2 = (*C.char)(unsafe.Pointer(C.CString(paragraphText)))
		defer C.free(unsafe.Pointer(carg2))
	}
	carg3 = C.int(paragraphLength)
	if logAttrs != nil {
		carg4 = (*C.PangoLogAttr)(UnsafeLogAttrToGlibNone(logAttrs))
	}
	carg6 = C.PangoShapeFlags(flags)

	C.pango_shape_item(carg1, carg2, carg3, carg4, &carg5, carg6)
	runtime.KeepAlive(item)
	runtime.KeepAlive(paragraphText)
	runtime.KeepAlive(paragraphLength)
	runtime.KeepAlive(logAttrs)
	runtime.KeepAlive(flags)

	var glyphs GlyphString

	_ = glyphs
	_ = carg5
	panic("unimplemented conversion of GlyphString (PangoGlyphString)")

	return glyphs
}

// ShapeWithFlags wraps pango_shape_with_flags
// 
// The function takes the following parameters:
// 
// 	- itemText string: valid UTF-8 text to shape 
// 	- itemLength int32: the length (in bytes) of @item_text.
//     -1 means nul-terminated text. 
// 	- paragraphText string (nullable): text of the paragraph (see details). 
// 	- paragraphLength int32: the length (in bytes) of @paragraph_text.
//     -1 means nul-terminated text. 
// 	- analysis *Analysis: `PangoAnalysis` structure from [func@Pango.itemize] 
// 	- flags ShapeFlags: flags influencing the shaping process 
// 
// The function returns the following values:
// 
// 	- glyphs GlyphString: glyph string in which to store results 
//
// Convert the characters in @text into glyphs.
// 
// Given a segment of text and the corresponding `PangoAnalysis` structure
// returned from [func@Pango.itemize], convert the characters into glyphs.
// You may also pass in only a substring of the item from [func@Pango.itemize].
// 
// This is similar to [func@Pango.shape_full], except it also takes flags
// that can influence the shaping process.
// 
// Some aspects of hyphen insertion and text transformation (in particular,
// capitalization) require log attrs, and thus can only be handled by
// [func@Pango.shape_item].
// 
// Note that the extra attributes in the @analyis that is returned from
// [func@Pango.itemize] have indices that are relative to the entire paragraph,
// so you do not pass the full paragraph text as @paragraph_text, you need
// to subtract the item offset from their indices before calling
// [func@Pango.shape_with_flags].
func ShapeWithFlags(itemText string, itemLength int32, paragraphText string, paragraphLength int32, analysis *Analysis, flags ShapeFlags) GlyphString {
	var carg1 *C.char            // in, none, string, casted *C.gchar
	var carg2 C.int              // in, none, casted, casted C.gint
	var carg3 *C.char            // in, none, string, nullable-string
	var carg4 C.int              // in, none, casted, casted C.gint
	var carg5 *C.PangoAnalysis   // in, none, converted
	var carg7 C.PangoShapeFlags  // in, none, casted
	var carg6 C.PangoGlyphString // out, transfer: none, C Pointers: 0, Name: GlyphString, caller-allocates

	carg1 = (*C.char)(unsafe.Pointer(C.CString(itemText)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(itemLength)
	if paragraphText != "" {
		carg3 = (*C.char)(unsafe.Pointer(C.CString(paragraphText)))
		defer C.free(unsafe.Pointer(carg3))
	}
	carg4 = C.int(paragraphLength)
	carg5 = (*C.PangoAnalysis)(UnsafeAnalysisToGlibNone(analysis))
	carg7 = C.PangoShapeFlags(flags)

	C.pango_shape_with_flags(carg1, carg2, carg3, carg4, carg5, &carg6, carg7)
	runtime.KeepAlive(itemText)
	runtime.KeepAlive(itemLength)
	runtime.KeepAlive(paragraphText)
	runtime.KeepAlive(paragraphLength)
	runtime.KeepAlive(analysis)
	runtime.KeepAlive(flags)

	var glyphs GlyphString

	_ = glyphs
	_ = carg6
	panic("unimplemented conversion of GlyphString (PangoGlyphString)")

	return glyphs
}

// SplitFileList wraps pango_split_file_list
// 
// The function takes the following parameters:
// 
// 	- str string: a %G_SEARCHPATH_SEPARATOR separated list of filenames 
// 
// The function returns the following values:
// 
// 	- goret []string 
//
// Splits a %G_SEARCHPATH_SEPARATOR-separated list of files, stripping
// white space and substituting ~/ with $HOME/.
//
// Deprecated: (since 1.38.0) 
func SplitFileList(str string) []string {
	var carg1 *C.char  // in, none, string, casted *C.gchar
	var cret  **C.char // return, transfer: full, C Pointers: 2, Name: array[utf8], scope: , array (inner: *typesystem.StringPrimitive, zero-terminated)

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_split_file_list(carg1)
	runtime.KeepAlive(str)

	var goret []string

	_ = goret
	_ = cret
	panic("unimplemented conversion of []string (char**)")

	return goret
}

// TrimString wraps pango_trim_string
// 
// The function takes the following parameters:
// 
// 	- str string: a string 
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Trims leading and trailing whitespace from a string.
//
// Deprecated: (since 1.38.0) 
func TrimString(str string) string {
	var carg1 *C.char // in, none, string, casted *C.gchar
	var cret  *C.char // return, full, string, casted *C.gchar

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_trim_string(carg1)
	runtime.KeepAlive(str)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return goret
}

// UnicharDirection wraps pango_unichar_direction
// 
// The function takes the following parameters:
// 
// 	- ch uint32: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret Direction 
//
// Determines the inherent direction of a character.
// 
// The inherent direction is either `PANGO_DIRECTION_LTR`, `PANGO_DIRECTION_RTL`,
// or `PANGO_DIRECTION_NEUTRAL`.
// 
// This function is useful to categorize characters into left-to-right
// letters, right-to-left letters, and everything else. If full Unicode
// bidirectional type of a character is needed, [func@Pango.BidiType.for_unichar]
// can be used instead.
func UnicharDirection(ch uint32) Direction {
	var carg1 C.gunichar       // in, none, casted
	var cret  C.PangoDirection // return, none, casted

	carg1 = C.gunichar(ch)

	cret = C.pango_unichar_direction(carg1)
	runtime.KeepAlive(ch)

	var goret Direction

	goret = Direction(cret)

	return goret
}

// UnitsFromDouble wraps pango_units_from_double
// 
// The function takes the following parameters:
// 
// 	- d float64: double floating-point value 
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Converts a floating-point number to Pango units.
// 
// The conversion is done by multiplying @d by %PANGO_SCALE and
// rounding the result to nearest integer.
func UnitsFromDouble(d float64) int32 {
	var carg1 C.double // in, none, casted, casted C.gdouble
	var cret  C.int    // return, none, casted, casted C.gint

	carg1 = C.double(d)

	cret = C.pango_units_from_double(carg1)
	runtime.KeepAlive(d)

	var goret int32

	goret = int32(cret)

	return goret
}

// UnitsToDouble wraps pango_units_to_double
// 
// The function takes the following parameters:
// 
// 	- i int32: value in Pango units 
// 
// The function returns the following values:
// 
// 	- goret float64 
//
// Converts a number in Pango units to floating-point.
// 
// The conversion is done by dividing @i by %PANGO_SCALE.
func UnitsToDouble(i int32) float64 {
	var carg1 C.int    // in, none, casted, casted C.gint
	var cret  C.double // return, none, casted, casted C.gdouble

	carg1 = C.int(i)

	cret = C.pango_units_to_double(carg1)
	runtime.KeepAlive(i)

	var goret float64

	goret = float64(cret)

	return goret
}

// Version wraps pango_version
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Returns the encoded version of Pango available at run-time.
// 
// This is similar to the macro %PANGO_VERSION except that the macro
// returns the encoded version available at compile-time. A version
// number can be encoded into an integer using PANGO_VERSION_ENCODE().
func Version() int32 {
	var cret C.int // return, none, casted, casted C.gint

	cret = C.pango_version()

	var goret int32

	goret = int32(cret)

	return goret
}

// VersionCheck wraps pango_version_check
// 
// The function takes the following parameters:
// 
// 	- requiredMajor int32: the required major version 
// 	- requiredMinor int32: the required minor version 
// 	- requiredMicro int32: the required major version 
// 
// The function returns the following values:
// 
// 	- goret string (nullable) 
//
// Checks that the Pango library in use is compatible with the
// given version.
// 
// Generally you would pass in the constants %PANGO_VERSION_MAJOR,
// %PANGO_VERSION_MINOR, %PANGO_VERSION_MICRO as the three arguments
// to this function; that produces a check that the library in use at
// run-time is compatible with the version of Pango the application or
// module was compiled against.
// 
// Compatibility is defined by two things: first the version
// of the running library is newer than the version
// @required_major.required_minor.@required_micro. Second
// the running library must be binary compatible with the
// version @required_major.required_minor.@required_micro
// (same major version.)
// 
// For compile-time version checking use PANGO_VERSION_CHECK().
func VersionCheck(requiredMajor int32, requiredMinor int32, requiredMicro int32) string {
	var carg1 C.int   // in, none, casted, casted C.gint
	var carg2 C.int   // in, none, casted, casted C.gint
	var carg3 C.int   // in, none, casted, casted C.gint
	var cret  *C.char // return, none, string, casted *C.gchar, nullable

	carg1 = C.int(requiredMajor)
	carg2 = C.int(requiredMinor)
	carg3 = C.int(requiredMicro)

	cret = C.pango_version_check(carg1, carg2, carg3)
	runtime.KeepAlive(requiredMajor)
	runtime.KeepAlive(requiredMinor)
	runtime.KeepAlive(requiredMicro)

	var goret string

	if cret != nil {
		goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	}

	return goret
}

// VersionString wraps pango_version_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Returns the version of Pango available at run-time.
// 
// This is similar to the macro %PANGO_VERSION_STRING except that the
// macro returns the version available at compile-time.
func VersionString() string {
	var cret *C.char // return, none, string, casted *C.gchar

	cret = C.pango_version_string()

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// ContextInstance is the instance type used by all types extending PangoContext. It is used internally by the bindings. Users should use the interface [Context] instead.
type ContextInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ Context = (*ContextInstance)(nil)

// Context wraps PangoContext
//
// A `PangoContext` stores global information used to control the
// itemization process.
// 
// The information stored by `PangoContext` includes the fontmap used
// to look up fonts, and default values such as the default language,
// default gravity, or default font.
// 
// To obtain a `PangoContext`, use [method@Pango.FontMap.create_context].
type Context interface {
	gobject.Object
	upcastToPangoContext() *ContextInstance

	// Changed wraps pango_context_changed
	//
	// Forces a change in the context, which will cause any `PangoLayout`
	// using this context to re-layout.
	// 
	// This function is only useful when implementing a new backend
	// for Pango, something applications won't do. Backends should
	// call this function if they have attached extra data to the context
	// and such data is changed.
	Changed()
	// GetBaseDir wraps pango_context_get_base_dir
	// 
	// The function returns the following values:
	// 
	// 	- goret Direction 
	//
	// Retrieves the base direction for the context.
	// 
	// See [method@Pango.Context.set_base_dir].
	GetBaseDir() Direction
	// GetBaseGravity wraps pango_context_get_base_gravity
	// 
	// The function returns the following values:
	// 
	// 	- goret Gravity 
	//
	// Retrieves the base gravity for the context.
	// 
	// See [method@Pango.Context.set_base_gravity].
	GetBaseGravity() Gravity
	// GetFontDescription wraps pango_context_get_font_description
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontDescription (nullable) 
	//
	// Retrieve the default font description for the context.
	GetFontDescription() *FontDescription
	// GetFontMap wraps pango_context_get_font_map
	// 
	// The function returns the following values:
	// 
	// 	- goret FontMap (nullable) 
	//
	// Gets the `PangoFontMap` used to look up fonts for this context.
	GetFontMap() FontMap
	// GetGravity wraps pango_context_get_gravity
	// 
	// The function returns the following values:
	// 
	// 	- goret Gravity 
	//
	// Retrieves the gravity for the context.
	// 
	// This is similar to [method@Pango.Context.get_base_gravity],
	// except for when the base gravity is %PANGO_GRAVITY_AUTO for
	// which [func@Pango.Gravity.get_for_matrix] is used to return the
	// gravity from the current context matrix.
	GetGravity() Gravity
	// GetGravityHint wraps pango_context_get_gravity_hint
	// 
	// The function returns the following values:
	// 
	// 	- goret GravityHint 
	//
	// Retrieves the gravity hint for the context.
	// 
	// See [method@Pango.Context.set_gravity_hint] for details.
	GetGravityHint() GravityHint
	// GetLanguage wraps pango_context_get_language
	// 
	// The function returns the following values:
	// 
	// 	- goret *Language 
	//
	// Retrieves the global language tag for the context.
	GetLanguage() *Language
	// GetMatrix wraps pango_context_get_matrix
	// 
	// The function returns the following values:
	// 
	// 	- goret *Matrix (nullable) 
	//
	// Gets the transformation matrix that will be applied when
	// rendering with this context.
	// 
	// See [method@Pango.Context.set_matrix].
	GetMatrix() *Matrix
	// GetMetrics wraps pango_context_get_metrics
	// 
	// The function takes the following parameters:
	// 
	// 	- desc *FontDescription (nullable): a `PangoFontDescription` structure. %NULL means that the
	//   font description from the context will be used. 
	// 	- language *Language (nullable): language tag used to determine which script to get
	//   the metrics for. %NULL means that the language tag from the context
	//   will be used. If no language tag is set on the context, metrics
	//   for the default language (as determined by [func@Pango.Language.get_default]
	//   will be returned. 
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Get overall metric information for a particular font description.
	// 
	// Since the metrics may be substantially different for different scripts,
	// a language tag can be provided to indicate that the metrics should be
	// retrieved that correspond to the script(s) used by that language.
	// 
	// The `PangoFontDescription` is interpreted in the same way as by [func@itemize],
	// and the family name may be a comma separated list of names. If characters
	// from multiple of these families would be used to render the string, then
	// the returned fonts would be a composite of the metrics for the fonts loaded
	// for the individual families.
	GetMetrics(*FontDescription, *Language) *FontMetrics
	// GetRoundGlyphPositions wraps pango_context_get_round_glyph_positions
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Returns whether font rendering with this context should
	// round glyph positions and widths.
	GetRoundGlyphPositions() bool
	// GetSerial wraps pango_context_get_serial
	// 
	// The function returns the following values:
	// 
	// 	- goret uint 
	//
	// Returns the current serial number of @context.
	// 
	// The serial number is initialized to an small number larger than zero
	// when a new context is created and is increased whenever the context
	// is changed using any of the setter functions, or the `PangoFontMap` it
	// uses to find fonts has changed. The serial may wrap, but will never
	// have the value 0. Since it can wrap, never compare it with "less than",
	// always use "not equals".
	// 
	// This can be used to automatically detect changes to a `PangoContext`,
	// and is only useful when implementing objects that need update when their
	// `PangoContext` changes, like `PangoLayout`.
	GetSerial() uint
	// LoadFont wraps pango_context_load_font
	// 
	// The function takes the following parameters:
	// 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font (nullable) 
	//
	// Loads the font in one of the fontmaps in the context
	// that is the closest match for @desc.
	LoadFont(*FontDescription) Font
	// LoadFontset wraps pango_context_load_fontset
	// 
	// The function takes the following parameters:
	// 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the fonts to load 
	// 	- language *Language: a `PangoLanguage` the fonts will be used for 
	// 
	// The function returns the following values:
	// 
	// 	- goret Fontset (nullable) 
	//
	// Load a set of fonts in the context that can be used to render
	// a font matching @desc.
	LoadFontset(*FontDescription, *Language) Fontset
	// SetBaseDir wraps pango_context_set_base_dir
	// 
	// The function takes the following parameters:
	// 
	// 	- direction Direction: the new base direction 
	//
	// Sets the base direction for the context.
	// 
	// The base direction is used in applying the Unicode bidirectional
	// algorithm; if the @direction is %PANGO_DIRECTION_LTR or
	// %PANGO_DIRECTION_RTL, then the value will be used as the paragraph
	// direction in the Unicode bidirectional algorithm. A value of
	// %PANGO_DIRECTION_WEAK_LTR or %PANGO_DIRECTION_WEAK_RTL is used only
	// for paragraphs that do not contain any strong characters themselves.
	SetBaseDir(Direction)
	// SetBaseGravity wraps pango_context_set_base_gravity
	// 
	// The function takes the following parameters:
	// 
	// 	- gravity Gravity: the new base gravity 
	//
	// Sets the base gravity for the context.
	// 
	// The base gravity is used in laying vertical text out.
	SetBaseGravity(Gravity)
	// SetFontDescription wraps pango_context_set_font_description
	// 
	// The function takes the following parameters:
	// 
	// 	- desc *FontDescription (nullable): the new pango font description 
	//
	// Set the default font description for the context
	SetFontDescription(*FontDescription)
	// SetFontMap wraps pango_context_set_font_map
	// 
	// The function takes the following parameters:
	// 
	// 	- fontMap FontMap (nullable): the `PangoFontMap` to set. 
	//
	// Sets the font map to be searched when fonts are looked-up
	// in this context.
	// 
	// This is only for internal use by Pango backends, a `PangoContext`
	// obtained via one of the recommended methods should already have a
	// suitable font map.
	SetFontMap(FontMap)
	// SetGravityHint wraps pango_context_set_gravity_hint
	// 
	// The function takes the following parameters:
	// 
	// 	- hint GravityHint: the new gravity hint 
	//
	// Sets the gravity hint for the context.
	// 
	// The gravity hint is used in laying vertical text out, and
	// is only relevant if gravity of the context as returned by
	// [method@Pango.Context.get_gravity] is set to %PANGO_GRAVITY_EAST
	// or %PANGO_GRAVITY_WEST.
	SetGravityHint(GravityHint)
	// SetLanguage wraps pango_context_set_language
	// 
	// The function takes the following parameters:
	// 
	// 	- language *Language (nullable): the new language tag. 
	//
	// Sets the global language tag for the context.
	// 
	// The default language for the locale of the running process
	// can be found using [func@Pango.Language.get_default].
	SetLanguage(*Language)
	// SetMatrix wraps pango_context_set_matrix
	// 
	// The function takes the following parameters:
	// 
	// 	- matrix *Matrix (nullable): a `PangoMatrix`, or %NULL to unset any existing
	// matrix. (No matrix set is the same as setting the identity matrix.) 
	//
	// Sets the transformation matrix that will be applied when rendering
	// with this context.
	// 
	// Note that reported metrics are in the user space coordinates before
	// the application of the matrix, not device-space coordinates after the
	// application of the matrix. So, they don't scale with the matrix, though
	// they may change slightly for different matrices, depending on how the
	// text is fit to the pixel grid.
	SetMatrix(*Matrix)
	// SetRoundGlyphPositions wraps pango_context_set_round_glyph_positions
	// 
	// The function takes the following parameters:
	// 
	// 	- roundPositions bool: whether to round glyph positions 
	//
	// Sets whether font rendering with this context should
	// round glyph positions and widths to integral positions,
	// in device units.
	// 
	// This is useful when the renderer can't handle subpixel
	// positioning of glyphs.
	// 
	// The default value is to round glyph positions, to remain
	// compatible with previous Pango behavior.
	SetRoundGlyphPositions(bool)
}

func unsafeWrapContext(base *gobject.ObjectInstance) *ContextInstance {
	return &ContextInstance{
		ObjectInstance: *base,
	}
}

func marshalContextInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapContext(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeContextFromGlibNone is used to convert raw PangoContext pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeContextFromGlibNone(c unsafe.Pointer) Context {
	return gobject.UnsafeObjectFromGlibNone(c).(Context)
}

// UnsafeContextFromGlibFull is used to convert raw PangoContext pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeContextFromGlibFull(c unsafe.Pointer) Context {
	return gobject.UnsafeObjectFromGlibFull(c).(Context)
}

// UnsafeContextFromGlibBorrow is used to convert raw PangoContext pointers to go without touching any references. This is used by the bindings internally.
func UnsafeContextFromGlibBorrow(c unsafe.Pointer) Context {
	return gobject.UnsafeObjectFromGlibBorrow(c).(Context)
}

func (c *ContextInstance) upcastToPangoContext() *ContextInstance {
	return c
}

// UnsafeContextToGlibNone is used to convert the instance to it's C value PangoContext. This is used by the bindings internally.
func UnsafeContextToGlibNone(c Context) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeContextToGlibFull is used to convert the instance to it's C value PangoContext, while removeing the finalizer. This is used by the bindings internally.
func UnsafeContextToGlibFull(c Context) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// NewContext wraps pango_context_new
// 
// The function returns the following values:
// 
// 	- goret Context 
//
// Creates a new `PangoContext` initialized to default values.
// 
// This function is not particularly useful as it should always
// be followed by a [method@Pango.Context.set_font_map] call, and the
// function [method@Pango.FontMap.create_context] does these two steps
// together and hence users are recommended to use that.
// 
// If you are using Pango as part of a higher-level system,
// that system may have it's own way of create a `PangoContext`.
// For instance, the GTK toolkit has, among others,
// `gtk_widget_get_pango_context()`. Use those instead.
func NewContext() Context {
	var cret *C.PangoContext // return, full, converted

	cret = C.pango_context_new()

	var goret Context

	goret = UnsafeContextFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Changed wraps pango_context_changed
//
// Forces a change in the context, which will cause any `PangoLayout`
// using this context to re-layout.
// 
// This function is only useful when implementing a new backend
// for Pango, something applications won't do. Backends should
// call this function if they have attached extra data to the context
// and such data is changed.
func (_context *ContextInstance) Changed() {
	var carg0 *C.PangoContext // in, none, converted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	C.pango_context_changed(carg0)
	runtime.KeepAlive(_context)
}

// GetBaseDir wraps pango_context_get_base_dir
// 
// The function returns the following values:
// 
// 	- goret Direction 
//
// Retrieves the base direction for the context.
// 
// See [method@Pango.Context.set_base_dir].
func (_context *ContextInstance) GetBaseDir() Direction {
	var carg0 *C.PangoContext  // in, none, converted
	var cret  C.PangoDirection // return, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_base_dir(carg0)
	runtime.KeepAlive(_context)

	var goret Direction

	goret = Direction(cret)

	return goret
}

// GetBaseGravity wraps pango_context_get_base_gravity
// 
// The function returns the following values:
// 
// 	- goret Gravity 
//
// Retrieves the base gravity for the context.
// 
// See [method@Pango.Context.set_base_gravity].
func (_context *ContextInstance) GetBaseGravity() Gravity {
	var carg0 *C.PangoContext // in, none, converted
	var cret  C.PangoGravity  // return, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_base_gravity(carg0)
	runtime.KeepAlive(_context)

	var goret Gravity

	goret = Gravity(cret)

	return goret
}

// GetFontDescription wraps pango_context_get_font_description
// 
// The function returns the following values:
// 
// 	- goret *FontDescription (nullable) 
//
// Retrieve the default font description for the context.
func (_context *ContextInstance) GetFontDescription() *FontDescription {
	var carg0 *C.PangoContext         // in, none, converted
	var cret  *C.PangoFontDescription // return, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_font_description(carg0)
	runtime.KeepAlive(_context)

	var goret *FontDescription

	if cret != nil {
		goret = UnsafeFontDescriptionFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetFontMap wraps pango_context_get_font_map
// 
// The function returns the following values:
// 
// 	- goret FontMap (nullable) 
//
// Gets the `PangoFontMap` used to look up fonts for this context.
func (_context *ContextInstance) GetFontMap() FontMap {
	var carg0 *C.PangoContext // in, none, converted
	var cret  *C.PangoFontMap // return, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_font_map(carg0)
	runtime.KeepAlive(_context)

	var goret FontMap

	if cret != nil {
		goret = UnsafeFontMapFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetGravity wraps pango_context_get_gravity
// 
// The function returns the following values:
// 
// 	- goret Gravity 
//
// Retrieves the gravity for the context.
// 
// This is similar to [method@Pango.Context.get_base_gravity],
// except for when the base gravity is %PANGO_GRAVITY_AUTO for
// which [func@Pango.Gravity.get_for_matrix] is used to return the
// gravity from the current context matrix.
func (_context *ContextInstance) GetGravity() Gravity {
	var carg0 *C.PangoContext // in, none, converted
	var cret  C.PangoGravity  // return, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_gravity(carg0)
	runtime.KeepAlive(_context)

	var goret Gravity

	goret = Gravity(cret)

	return goret
}

// GetGravityHint wraps pango_context_get_gravity_hint
// 
// The function returns the following values:
// 
// 	- goret GravityHint 
//
// Retrieves the gravity hint for the context.
// 
// See [method@Pango.Context.set_gravity_hint] for details.
func (_context *ContextInstance) GetGravityHint() GravityHint {
	var carg0 *C.PangoContext    // in, none, converted
	var cret  C.PangoGravityHint // return, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_gravity_hint(carg0)
	runtime.KeepAlive(_context)

	var goret GravityHint

	goret = GravityHint(cret)

	return goret
}

// GetLanguage wraps pango_context_get_language
// 
// The function returns the following values:
// 
// 	- goret *Language 
//
// Retrieves the global language tag for the context.
func (_context *ContextInstance) GetLanguage() *Language {
	var carg0 *C.PangoContext  // in, none, converted
	var cret  *C.PangoLanguage // return, none, converted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_language(carg0)
	runtime.KeepAlive(_context)

	var goret *Language

	goret = UnsafeLanguageFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// GetMatrix wraps pango_context_get_matrix
// 
// The function returns the following values:
// 
// 	- goret *Matrix (nullable) 
//
// Gets the transformation matrix that will be applied when
// rendering with this context.
// 
// See [method@Pango.Context.set_matrix].
func (_context *ContextInstance) GetMatrix() *Matrix {
	var carg0 *C.PangoContext // in, none, converted
	var cret  *C.PangoMatrix  // return, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_matrix(carg0)
	runtime.KeepAlive(_context)

	var goret *Matrix

	if cret != nil {
		goret = UnsafeMatrixFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetMetrics wraps pango_context_get_metrics
// 
// The function takes the following parameters:
// 
// 	- desc *FontDescription (nullable): a `PangoFontDescription` structure. %NULL means that the
//   font description from the context will be used. 
// 	- language *Language (nullable): language tag used to determine which script to get
//   the metrics for. %NULL means that the language tag from the context
//   will be used. If no language tag is set on the context, metrics
//   for the default language (as determined by [func@Pango.Language.get_default]
//   will be returned. 
// 
// The function returns the following values:
// 
// 	- goret *FontMetrics 
//
// Get overall metric information for a particular font description.
// 
// Since the metrics may be substantially different for different scripts,
// a language tag can be provided to indicate that the metrics should be
// retrieved that correspond to the script(s) used by that language.
// 
// The `PangoFontDescription` is interpreted in the same way as by [func@itemize],
// and the family name may be a comma separated list of names. If characters
// from multiple of these families would be used to render the string, then
// the returned fonts would be a composite of the metrics for the fonts loaded
// for the individual families.
func (_context *ContextInstance) GetMetrics(desc *FontDescription, language *Language) *FontMetrics {
	var carg0 *C.PangoContext         // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted, nullable
	var carg2 *C.PangoLanguage        // in, none, converted, nullable
	var cret  *C.PangoFontMetrics     // return, full, converted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	if desc != nil {
		carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	}
	if language != nil {
		carg2 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))
	}

	cret = C.pango_context_get_metrics(carg0, carg1, carg2)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(language)

	var goret *FontMetrics

	goret = UnsafeFontMetricsFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetRoundGlyphPositions wraps pango_context_get_round_glyph_positions
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Returns whether font rendering with this context should
// round glyph positions and widths.
func (_context *ContextInstance) GetRoundGlyphPositions() bool {
	var carg0 *C.PangoContext // in, none, converted
	var cret  C.gboolean      // return

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_round_glyph_positions(carg0)
	runtime.KeepAlive(_context)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetSerial wraps pango_context_get_serial
// 
// The function returns the following values:
// 
// 	- goret uint 
//
// Returns the current serial number of @context.
// 
// The serial number is initialized to an small number larger than zero
// when a new context is created and is increased whenever the context
// is changed using any of the setter functions, or the `PangoFontMap` it
// uses to find fonts has changed. The serial may wrap, but will never
// have the value 0. Since it can wrap, never compare it with "less than",
// always use "not equals".
// 
// This can be used to automatically detect changes to a `PangoContext`,
// and is only useful when implementing objects that need update when their
// `PangoContext` changes, like `PangoLayout`.
func (_context *ContextInstance) GetSerial() uint {
	var carg0 *C.PangoContext // in, none, converted
	var cret  C.guint         // return, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_context_get_serial(carg0)
	runtime.KeepAlive(_context)

	var goret uint

	goret = uint(cret)

	return goret
}

// LoadFont wraps pango_context_load_font
// 
// The function takes the following parameters:
// 
// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
// 
// The function returns the following values:
// 
// 	- goret Font (nullable) 
//
// Loads the font in one of the fontmaps in the context
// that is the closest match for @desc.
func (_context *ContextInstance) LoadFont(desc *FontDescription) Font {
	var carg0 *C.PangoContext         // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted
	var cret  *C.PangoFont            // return, full, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_context_load_font(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)

	var goret Font

	if cret != nil {
		goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// LoadFontset wraps pango_context_load_fontset
// 
// The function takes the following parameters:
// 
// 	- desc *FontDescription: a `PangoFontDescription` describing the fonts to load 
// 	- language *Language: a `PangoLanguage` the fonts will be used for 
// 
// The function returns the following values:
// 
// 	- goret Fontset (nullable) 
//
// Load a set of fonts in the context that can be used to render
// a font matching @desc.
func (_context *ContextInstance) LoadFontset(desc *FontDescription, language *Language) Fontset {
	var carg0 *C.PangoContext         // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted
	var carg2 *C.PangoLanguage        // in, none, converted
	var cret  *C.PangoFontset         // return, full, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg2 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_context_load_fontset(carg0, carg1, carg2)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(language)

	var goret Fontset

	if cret != nil {
		goret = UnsafeFontsetFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// SetBaseDir wraps pango_context_set_base_dir
// 
// The function takes the following parameters:
// 
// 	- direction Direction: the new base direction 
//
// Sets the base direction for the context.
// 
// The base direction is used in applying the Unicode bidirectional
// algorithm; if the @direction is %PANGO_DIRECTION_LTR or
// %PANGO_DIRECTION_RTL, then the value will be used as the paragraph
// direction in the Unicode bidirectional algorithm. A value of
// %PANGO_DIRECTION_WEAK_LTR or %PANGO_DIRECTION_WEAK_RTL is used only
// for paragraphs that do not contain any strong characters themselves.
func (_context *ContextInstance) SetBaseDir(direction Direction) {
	var carg0 *C.PangoContext  // in, none, converted
	var carg1 C.PangoDirection // in, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg1 = C.PangoDirection(direction)

	C.pango_context_set_base_dir(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(direction)
}

// SetBaseGravity wraps pango_context_set_base_gravity
// 
// The function takes the following parameters:
// 
// 	- gravity Gravity: the new base gravity 
//
// Sets the base gravity for the context.
// 
// The base gravity is used in laying vertical text out.
func (_context *ContextInstance) SetBaseGravity(gravity Gravity) {
	var carg0 *C.PangoContext // in, none, converted
	var carg1 C.PangoGravity  // in, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg1 = C.PangoGravity(gravity)

	C.pango_context_set_base_gravity(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(gravity)
}

// SetFontDescription wraps pango_context_set_font_description
// 
// The function takes the following parameters:
// 
// 	- desc *FontDescription (nullable): the new pango font description 
//
// Set the default font description for the context
func (_context *ContextInstance) SetFontDescription(desc *FontDescription) {
	var carg0 *C.PangoContext         // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	if desc != nil {
		carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	}

	C.pango_context_set_font_description(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)
}

// SetFontMap wraps pango_context_set_font_map
// 
// The function takes the following parameters:
// 
// 	- fontMap FontMap (nullable): the `PangoFontMap` to set. 
//
// Sets the font map to be searched when fonts are looked-up
// in this context.
// 
// This is only for internal use by Pango backends, a `PangoContext`
// obtained via one of the recommended methods should already have a
// suitable font map.
func (_context *ContextInstance) SetFontMap(fontMap FontMap) {
	var carg0 *C.PangoContext // in, none, converted
	var carg1 *C.PangoFontMap // in, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	if fontMap != nil {
		carg1 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontMap))
	}

	C.pango_context_set_font_map(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(fontMap)
}

// SetGravityHint wraps pango_context_set_gravity_hint
// 
// The function takes the following parameters:
// 
// 	- hint GravityHint: the new gravity hint 
//
// Sets the gravity hint for the context.
// 
// The gravity hint is used in laying vertical text out, and
// is only relevant if gravity of the context as returned by
// [method@Pango.Context.get_gravity] is set to %PANGO_GRAVITY_EAST
// or %PANGO_GRAVITY_WEST.
func (_context *ContextInstance) SetGravityHint(hint GravityHint) {
	var carg0 *C.PangoContext    // in, none, converted
	var carg1 C.PangoGravityHint // in, none, casted

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg1 = C.PangoGravityHint(hint)

	C.pango_context_set_gravity_hint(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(hint)
}

// SetLanguage wraps pango_context_set_language
// 
// The function takes the following parameters:
// 
// 	- language *Language (nullable): the new language tag. 
//
// Sets the global language tag for the context.
// 
// The default language for the locale of the running process
// can be found using [func@Pango.Language.get_default].
func (_context *ContextInstance) SetLanguage(language *Language) {
	var carg0 *C.PangoContext  // in, none, converted
	var carg1 *C.PangoLanguage // in, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	if language != nil {
		carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))
	}

	C.pango_context_set_language(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(language)
}

// SetMatrix wraps pango_context_set_matrix
// 
// The function takes the following parameters:
// 
// 	- matrix *Matrix (nullable): a `PangoMatrix`, or %NULL to unset any existing
// matrix. (No matrix set is the same as setting the identity matrix.) 
//
// Sets the transformation matrix that will be applied when rendering
// with this context.
// 
// Note that reported metrics are in the user space coordinates before
// the application of the matrix, not device-space coordinates after the
// application of the matrix. So, they don't scale with the matrix, though
// they may change slightly for different matrices, depending on how the
// text is fit to the pixel grid.
func (_context *ContextInstance) SetMatrix(matrix *Matrix) {
	var carg0 *C.PangoContext // in, none, converted
	var carg1 *C.PangoMatrix  // in, none, converted, nullable

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	if matrix != nil {
		carg1 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	}

	C.pango_context_set_matrix(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(matrix)
}

// SetRoundGlyphPositions wraps pango_context_set_round_glyph_positions
// 
// The function takes the following parameters:
// 
// 	- roundPositions bool: whether to round glyph positions 
//
// Sets whether font rendering with this context should
// round glyph positions and widths to integral positions,
// in device units.
// 
// This is useful when the renderer can't handle subpixel
// positioning of glyphs.
// 
// The default value is to round glyph positions, to remain
// compatible with previous Pango behavior.
func (_context *ContextInstance) SetRoundGlyphPositions(roundPositions bool) {
	var carg0 *C.PangoContext // in, none, converted
	var carg1 C.gboolean      // in

	carg0 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	if roundPositions {
		carg1 = C.TRUE
	}

	C.pango_context_set_round_glyph_positions(carg0, carg1)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(roundPositions)
}

// CoverageInstance is the instance type used by all types extending PangoCoverage. It is used internally by the bindings. Users should use the interface [Coverage] instead.
type CoverageInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ Coverage = (*CoverageInstance)(nil)

// Coverage wraps PangoCoverage
//
// A `PangoCoverage` structure is a map from Unicode characters
// to [enum@Pango.CoverageLevel] values.
// 
// It is often necessary in Pango to determine if a particular
// font can represent a particular character, and also how well
// it can represent that character. The `PangoCoverage` is a data
// structure that is used to represent that information. It is an
// opaque structure with no public fields.
type Coverage interface {
	gobject.Object
	upcastToPangoCoverage() *CoverageInstance

	// Copy wraps pango_coverage_copy
	// 
	// The function returns the following values:
	// 
	// 	- goret Coverage 
	//
	// Copy an existing `PangoCoverage`.
	Copy() Coverage
	// Get wraps pango_coverage_get
	// 
	// The function takes the following parameters:
	// 
	// 	- index_ int32: the index to check 
	// 
	// The function returns the following values:
	// 
	// 	- goret CoverageLevel 
	//
	// Determine whether a particular index is covered by @coverage.
	Get(int32) CoverageLevel
	// Max wraps pango_coverage_max
	// 
	// The function takes the following parameters:
	// 
	// 	- other Coverage: another `PangoCoverage` 
	//
	// Set the coverage for each index in @coverage to be the max (better)
	// value of the current coverage for the index and the coverage for
	// the corresponding index in @other.
	//
	// Deprecated: (since 1.44.0) This function does nothing
	Max(Coverage)
	// Set wraps pango_coverage_set
	// 
	// The function takes the following parameters:
	// 
	// 	- index_ int32: the index to modify 
	// 	- level CoverageLevel: the new level for @index_ 
	//
	// Modify a particular index within @coverage
	Set(int32, CoverageLevel)
	// ToBytes wraps pango_coverage_to_bytes
	// 
	// The function returns the following values:
	// 
	// 	- bytes []uint8: 
	//   location to store result (must be freed with g_free()) 
	// 	- nBytes int32: location to store size of result 
	//
	// Convert a `PangoCoverage` structure into a flat binary format.
	//
	// Deprecated: (since 1.44.0) This returns %NULL
	ToBytes() ([]uint8)
}

func unsafeWrapCoverage(base *gobject.ObjectInstance) *CoverageInstance {
	return &CoverageInstance{
		ObjectInstance: *base,
	}
}

func marshalCoverageInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapCoverage(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeCoverageFromGlibNone is used to convert raw PangoCoverage pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeCoverageFromGlibNone(c unsafe.Pointer) Coverage {
	return gobject.UnsafeObjectFromGlibNone(c).(Coverage)
}

// UnsafeCoverageFromGlibFull is used to convert raw PangoCoverage pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeCoverageFromGlibFull(c unsafe.Pointer) Coverage {
	return gobject.UnsafeObjectFromGlibFull(c).(Coverage)
}

// UnsafeCoverageFromGlibBorrow is used to convert raw PangoCoverage pointers to go without touching any references. This is used by the bindings internally.
func UnsafeCoverageFromGlibBorrow(c unsafe.Pointer) Coverage {
	return gobject.UnsafeObjectFromGlibBorrow(c).(Coverage)
}

func (c *CoverageInstance) upcastToPangoCoverage() *CoverageInstance {
	return c
}

// UnsafeCoverageToGlibNone is used to convert the instance to it's C value PangoCoverage. This is used by the bindings internally.
func UnsafeCoverageToGlibNone(c Coverage) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeCoverageToGlibFull is used to convert the instance to it's C value PangoCoverage, while removeing the finalizer. This is used by the bindings internally.
func UnsafeCoverageToGlibFull(c Coverage) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// NewCoverage wraps pango_coverage_new
// 
// The function returns the following values:
// 
// 	- goret Coverage 
//
// Create a new `PangoCoverage`
func NewCoverage() Coverage {
	var cret *C.PangoCoverage // return, full, converted

	cret = C.pango_coverage_new()

	var goret Coverage

	goret = UnsafeCoverageFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// CoverageFromBytes wraps pango_coverage_from_bytes
// 
// The function takes the following parameters:
// 
// 	- bytes []uint8: binary data
//   representing a `PangoCoverage` 
// 
// The function returns the following values:
// 
// 	- goret Coverage (nullable) 
//
// Convert data generated from [method@Pango.Coverage.to_bytes]
// back to a `PangoCoverage`.
//
// Deprecated: (since 1.44.0) This returns %NULL
func CoverageFromBytes(bytes []uint8) Coverage {
	var carg1 *C.guchar        // in, transfer: none, C Pointers: 1, Name: array[guint8], array (inner: *typesystem.CastablePrimitive, length-by: carg2)
	var carg2 C.int            // implicit
	var cret  *C.PangoCoverage // return, full, converted, nullable

	_ = bytes
	_ = carg1
	_ = carg2
	panic("unimplemented conversion of []uint8 (guchar*)")

	cret = C.pango_coverage_from_bytes(carg1, carg2)
	runtime.KeepAlive(bytes)

	var goret Coverage

	if cret != nil {
		goret = UnsafeCoverageFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Copy wraps pango_coverage_copy
// 
// The function returns the following values:
// 
// 	- goret Coverage 
//
// Copy an existing `PangoCoverage`.
func (coverage *CoverageInstance) Copy() Coverage {
	var carg0 *C.PangoCoverage // in, none, converted
	var cret  *C.PangoCoverage // return, full, converted

	carg0 = (*C.PangoCoverage)(UnsafeCoverageToGlibNone(coverage))

	cret = C.pango_coverage_copy(carg0)
	runtime.KeepAlive(coverage)

	var goret Coverage

	goret = UnsafeCoverageFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Get wraps pango_coverage_get
// 
// The function takes the following parameters:
// 
// 	- index_ int32: the index to check 
// 
// The function returns the following values:
// 
// 	- goret CoverageLevel 
//
// Determine whether a particular index is covered by @coverage.
func (coverage *CoverageInstance) Get(index_ int32) CoverageLevel {
	var carg0 *C.PangoCoverage     // in, none, converted
	var carg1 C.int                // in, none, casted, casted C.gint
	var cret  C.PangoCoverageLevel // return, none, casted

	carg0 = (*C.PangoCoverage)(UnsafeCoverageToGlibNone(coverage))
	carg1 = C.int(index_)

	cret = C.pango_coverage_get(carg0, carg1)
	runtime.KeepAlive(coverage)
	runtime.KeepAlive(index_)

	var goret CoverageLevel

	goret = CoverageLevel(cret)

	return goret
}

// Max wraps pango_coverage_max
// 
// The function takes the following parameters:
// 
// 	- other Coverage: another `PangoCoverage` 
//
// Set the coverage for each index in @coverage to be the max (better)
// value of the current coverage for the index and the coverage for
// the corresponding index in @other.
//
// Deprecated: (since 1.44.0) This function does nothing
func (coverage *CoverageInstance) Max(other Coverage) {
	var carg0 *C.PangoCoverage // in, none, converted
	var carg1 *C.PangoCoverage // in, none, converted

	carg0 = (*C.PangoCoverage)(UnsafeCoverageToGlibNone(coverage))
	carg1 = (*C.PangoCoverage)(UnsafeCoverageToGlibNone(other))

	C.pango_coverage_max(carg0, carg1)
	runtime.KeepAlive(coverage)
	runtime.KeepAlive(other)
}

// Set wraps pango_coverage_set
// 
// The function takes the following parameters:
// 
// 	- index_ int32: the index to modify 
// 	- level CoverageLevel: the new level for @index_ 
//
// Modify a particular index within @coverage
func (coverage *CoverageInstance) Set(index_ int32, level CoverageLevel) {
	var carg0 *C.PangoCoverage     // in, none, converted
	var carg1 C.int                // in, none, casted, casted C.gint
	var carg2 C.PangoCoverageLevel // in, none, casted

	carg0 = (*C.PangoCoverage)(UnsafeCoverageToGlibNone(coverage))
	carg1 = C.int(index_)
	carg2 = C.PangoCoverageLevel(level)

	C.pango_coverage_set(carg0, carg1, carg2)
	runtime.KeepAlive(coverage)
	runtime.KeepAlive(index_)
	runtime.KeepAlive(level)
}

// ToBytes wraps pango_coverage_to_bytes
// 
// The function returns the following values:
// 
// 	- bytes []uint8: 
//   location to store result (must be freed with g_free()) 
// 	- nBytes int32: location to store size of result 
//
// Convert a `PangoCoverage` structure into a flat binary format.
//
// Deprecated: (since 1.44.0) This returns %NULL
func (coverage *CoverageInstance) ToBytes() ([]uint8) {
	var carg0 *C.PangoCoverage // in, none, converted
	var carg1 *C.guchar        // out, transfer: full, C Pointers: 1, Name: array[guint8], array (inner: *typesystem.CastablePrimitive, length-by: carg2)
	var carg2 C.int            // implicit

	carg0 = (*C.PangoCoverage)(UnsafeCoverageToGlibNone(coverage))

	C.pango_coverage_to_bytes(carg0, &carg1, &carg2)
	runtime.KeepAlive(coverage)

	var bytes []uint8

	_ = bytes
	_ = carg1
	_ = carg2
	panic("unimplemented conversion of []uint8 (guchar*)")

	return bytes
}

// FontInstance is the instance type used by all types extending PangoFont. It is used internally by the bindings. Users should use the interface [Font] instead.
type FontInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ Font = (*FontInstance)(nil)

// Font wraps PangoFont
//
// A `PangoFont` is used to represent a font in a
// rendering-system-independent manner.
type Font interface {
	gobject.Object
	upcastToPangoFont() *FontInstance

	// Describe wraps pango_font_describe
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a description of the font, with font size set in points.
	// 
	// Use [method@Pango.Font.describe_with_absolute_size] if you want
	// the font size in device units.
	Describe() *FontDescription
	// DescribeWithAbsoluteSize wraps pango_font_describe_with_absolute_size
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a description of the font, with absolute font size set
	// in device units.
	// 
	// Use [method@Pango.Font.describe] if you want the font size in points.
	DescribeWithAbsoluteSize() *FontDescription
	// GetCoverage wraps pango_font_get_coverage
	// 
	// The function takes the following parameters:
	// 
	// 	- language *Language: the language tag 
	// 
	// The function returns the following values:
	// 
	// 	- goret Coverage 
	//
	// Computes the coverage map for a given font and language tag.
	GetCoverage(*Language) Coverage
	// GetFace wraps pango_font_get_face
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFace 
	//
	// Gets the `PangoFontFace` to which @font belongs.
	GetFace() FontFace
	// GetFontMap wraps pango_font_get_font_map
	// 
	// The function returns the following values:
	// 
	// 	- goret FontMap (nullable) 
	//
	// Gets the font map for which the font was created.
	// 
	// Note that the font maintains a *weak* reference to
	// the font map, so if all references to font map are
	// dropped, the font map will be finalized even if there
	// are fonts created with the font map that are still alive.
	// In that case this function will return %NULL.
	// 
	// It is the responsibility of the user to ensure that the
	// font map is kept alive. In most uses this is not an issue
	// as a `PangoContext` holds a reference to the font map.
	GetFontMap() FontMap
	// GetGlyphExtents wraps pango_font_get_glyph_extents
	// 
	// The function takes the following parameters:
	// 
	// 	- glyph Glyph: the glyph index 
	// 
	// The function returns the following values:
	// 
	// 	- inkRect Rectangle: rectangle used to store the extents of the glyph as drawn 
	// 	- logicalRect Rectangle: rectangle used to store the logical extents of the glyph 
	//
	// Gets the logical and ink extents of a glyph within a font.
	// 
	// The coordinate system for each rectangle has its origin at the
	// base line and horizontal origin of the character with increasing
	// coordinates extending to the right and down. The macros PANGO_ASCENT(),
	// PANGO_DESCENT(), PANGO_LBEARING(), and PANGO_RBEARING() can be used to convert
	// from the extents rectangle to more traditional font metrics. The units
	// of the rectangles are in 1/PANGO_SCALE of a device unit.
	// 
	// If @font is %NULL, this function gracefully sets some sane values in the
	// output variables and returns.
	GetGlyphExtents(Glyph) (Rectangle, Rectangle)
	// GetMetrics wraps pango_font_get_metrics
	// 
	// The function takes the following parameters:
	// 
	// 	- language *Language (nullable): language tag used to determine which script
	//   to get the metrics for, or %NULL to indicate to get the metrics for
	//   the entire font. 
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Gets overall metric information for a font.
	// 
	// Since the metrics may be substantially different for different scripts,
	// a language tag can be provided to indicate that the metrics should be
	// retrieved that correspond to the script(s) used by that language.
	// 
	// If @font is %NULL, this function gracefully sets some sane values in the
	// output variables and returns.
	GetMetrics(*Language) *FontMetrics
	// HasChar wraps pango_font_has_char
	// 
	// The function takes the following parameters:
	// 
	// 	- wc uint32: a Unicode character 
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Returns whether the font provides a glyph for this character.
	HasChar(uint32) bool
	// Serialize wraps pango_font_serialize
	// 
	// The function returns the following values:
	// 
	// 	- goret *glib.Bytes 
	//
	// Serializes the @font in a way that can be uniquely identified.
	// 
	// There are no guarantees about the format of the output across different
	// versions of Pango.
	// 
	// The intended use of this function is testing, benchmarking and debugging.
	// The format is not meant as a permanent storage format.
	// 
	// To recreate a font from its serialized form, use [func@Pango.Font.deserialize].
	Serialize() *glib.Bytes

	// chain up virtual methods:

	// ParentDescribe calls the default implementations of the describe virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a description of the font, with font size set in points.
	// 
	// Use [method@Pango.Font.describe_with_absolute_size] if you want
	// the font size in device units.
	ParentDescribe() *FontDescription
	// ParentDescribeAbsolute calls the default implementations of the describe_absolute virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	ParentDescribeAbsolute() *FontDescription
	// ParentGetCoverage calls the default implementations of the get_coverage virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- language *Language: the language tag 
	// 
	// The function returns the following values:
	// 
	// 	- goret Coverage 
	//
	// Computes the coverage map for a given font and language tag.
	ParentGetCoverage(language *Language) Coverage
	// ParentGetFontMap calls the default implementations of the get_font_map virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret FontMap (nullable) 
	//
	// Gets the font map for which the font was created.
	// 
	// Note that the font maintains a *weak* reference to
	// the font map, so if all references to font map are
	// dropped, the font map will be finalized even if there
	// are fonts created with the font map that are still alive.
	// In that case this function will return %NULL.
	// 
	// It is the responsibility of the user to ensure that the
	// font map is kept alive. In most uses this is not an issue
	// as a `PangoContext` holds a reference to the font map.
	ParentGetFontMap() FontMap
	// ParentGetGlyphExtents calls the default implementations of the get_glyph_extents virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- glyph Glyph: the glyph index 
	// 
	// The function returns the following values:
	// 
	// 	- inkRect Rectangle: rectangle used to store the extents of the glyph as drawn 
	// 	- logicalRect Rectangle: rectangle used to store the logical extents of the glyph 
	//
	// Gets the logical and ink extents of a glyph within a font.
	// 
	// The coordinate system for each rectangle has its origin at the
	// base line and horizontal origin of the character with increasing
	// coordinates extending to the right and down. The macros PANGO_ASCENT(),
	// PANGO_DESCENT(), PANGO_LBEARING(), and PANGO_RBEARING() can be used to convert
	// from the extents rectangle to more traditional font metrics. The units
	// of the rectangles are in 1/PANGO_SCALE of a device unit.
	// 
	// If @font is %NULL, this function gracefully sets some sane values in the
	// output variables and returns.
	ParentGetGlyphExtents(glyph Glyph) (Rectangle, Rectangle)
	// ParentGetMetrics calls the default implementations of the get_metrics virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- language *Language (nullable): language tag used to determine which script
	//   to get the metrics for, or %NULL to indicate to get the metrics for
	//   the entire font. 
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Gets overall metric information for a font.
	// 
	// Since the metrics may be substantially different for different scripts,
	// a language tag can be provided to indicate that the metrics should be
	// retrieved that correspond to the script(s) used by that language.
	// 
	// If @font is %NULL, this function gracefully sets some sane values in the
	// output variables and returns.
	ParentGetMetrics(language *Language) *FontMetrics
}

func unsafeWrapFont(base *gobject.ObjectInstance) *FontInstance {
	return &FontInstance{
		ObjectInstance: *base,
	}
}

func marshalFontInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapFont(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeFontFromGlibNone is used to convert raw PangoFont pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeFontFromGlibNone(c unsafe.Pointer) Font {
	return gobject.UnsafeObjectFromGlibNone(c).(Font)
}

// UnsafeFontFromGlibFull is used to convert raw PangoFont pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeFontFromGlibFull(c unsafe.Pointer) Font {
	return gobject.UnsafeObjectFromGlibFull(c).(Font)
}

// UnsafeFontFromGlibBorrow is used to convert raw PangoFont pointers to go without touching any references. This is used by the bindings internally.
func UnsafeFontFromGlibBorrow(c unsafe.Pointer) Font {
	return gobject.UnsafeObjectFromGlibBorrow(c).(Font)
}

func (f *FontInstance) upcastToPangoFont() *FontInstance {
	return f
}

// UnsafeFontToGlibNone is used to convert the instance to it's C value PangoFont. This is used by the bindings internally.
func UnsafeFontToGlibNone(c Font) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeFontToGlibFull is used to convert the instance to it's C value PangoFont, while removeing the finalizer. This is used by the bindings internally.
func UnsafeFontToGlibFull(c Font) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// FontDescriptionsFree wraps pango_font_descriptions_free
// 
// The function takes the following parameters:
// 
// 	- descs []*FontDescription (nullable): a pointer
//   to an array of `PangoFontDescription`, may be %NULL 
//
// Frees an array of font descriptions.
func FontDescriptionsFree(descs []*FontDescription) {
	var carg1 **C.PangoFontDescription // in, transfer: full, C Pointers: 2, Name: array[FontDescription], nullable, array (inner: *typesystem.Record, length-by: carg2)
	var carg2 C.int                    // implicit

	_ = descs
	_ = carg1
	_ = carg2
	panic("unimplemented conversion of []*FontDescription (PangoFontDescription**)")

	C.pango_font_descriptions_free(carg1, carg2)
	runtime.KeepAlive(descs)
}

// FontDeserialize wraps pango_font_deserialize
// 
// The function takes the following parameters:
// 
// 	- _context Context: a `PangoContext` 
// 	- bytes *glib.Bytes: the bytes containing the data 
// 
// The function returns the following values:
// 
// 	- goret Font (nullable) 
// 	- _goerr error (nullable): an error 
//
// Loads data previously created via [method@Pango.Font.serialize].
// 
// For a discussion of the supported format, see that function.
// 
// Note: to verify that the returned font is identical to
// the one that was serialized, you can compare @bytes to the
// result of serializing the font again.
func FontDeserialize(_context Context, bytes *glib.Bytes) (Font, error) {
	var carg1 *C.PangoContext // in, none, converted
	var carg2 *C.GBytes       // in, none, converted
	var cret  *C.PangoFont    // return, full, converted, nullable
	var _cerr *C.GError       // out, full, converted, nullable

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.GBytes)(glib.UnsafeBytesToGlibNone(bytes))

	cret = C.pango_font_deserialize(carg1, carg2, &_cerr)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(bytes)

	var goret  Font
	var _goerr error

	if cret != nil {
		goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return goret, _goerr
}

// Describe wraps pango_font_describe
// 
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Returns a description of the font, with font size set in points.
// 
// Use [method@Pango.Font.describe_with_absolute_size] if you want
// the font size in device units.
func (font *FontInstance) Describe() *FontDescription {
	var carg0 *C.PangoFont            // in, none, converted
	var cret  *C.PangoFontDescription // return, full, converted

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	cret = C.pango_font_describe(carg0)
	runtime.KeepAlive(font)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// DescribeWithAbsoluteSize wraps pango_font_describe_with_absolute_size
// 
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Returns a description of the font, with absolute font size set
// in device units.
// 
// Use [method@Pango.Font.describe] if you want the font size in points.
func (font *FontInstance) DescribeWithAbsoluteSize() *FontDescription {
	var carg0 *C.PangoFont            // in, none, converted
	var cret  *C.PangoFontDescription // return, full, converted

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	cret = C.pango_font_describe_with_absolute_size(carg0)
	runtime.KeepAlive(font)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetCoverage wraps pango_font_get_coverage
// 
// The function takes the following parameters:
// 
// 	- language *Language: the language tag 
// 
// The function returns the following values:
// 
// 	- goret Coverage 
//
// Computes the coverage map for a given font and language tag.
func (font *FontInstance) GetCoverage(language *Language) Coverage {
	var carg0 *C.PangoFont     // in, none, converted
	var carg1 *C.PangoLanguage // in, none, converted
	var cret  *C.PangoCoverage // return, full, converted

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_font_get_coverage(carg0, carg1)
	runtime.KeepAlive(font)
	runtime.KeepAlive(language)

	var goret Coverage

	goret = UnsafeCoverageFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetFace wraps pango_font_get_face
// 
// The function returns the following values:
// 
// 	- goret FontFace 
//
// Gets the `PangoFontFace` to which @font belongs.
func (font *FontInstance) GetFace() FontFace {
	var carg0 *C.PangoFont     // in, none, converted
	var cret  *C.PangoFontFace // return, none, converted

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	cret = C.pango_font_get_face(carg0)
	runtime.KeepAlive(font)

	var goret FontFace

	goret = UnsafeFontFaceFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// GetFontMap wraps pango_font_get_font_map
// 
// The function returns the following values:
// 
// 	- goret FontMap (nullable) 
//
// Gets the font map for which the font was created.
// 
// Note that the font maintains a *weak* reference to
// the font map, so if all references to font map are
// dropped, the font map will be finalized even if there
// are fonts created with the font map that are still alive.
// In that case this function will return %NULL.
// 
// It is the responsibility of the user to ensure that the
// font map is kept alive. In most uses this is not an issue
// as a `PangoContext` holds a reference to the font map.
func (font *FontInstance) GetFontMap() FontMap {
	var carg0 *C.PangoFont    // in, none, converted
	var cret  *C.PangoFontMap // return, none, converted, nullable

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	cret = C.pango_font_get_font_map(carg0)
	runtime.KeepAlive(font)

	var goret FontMap

	if cret != nil {
		goret = UnsafeFontMapFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetGlyphExtents wraps pango_font_get_glyph_extents
// 
// The function takes the following parameters:
// 
// 	- glyph Glyph: the glyph index 
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of the glyph as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical extents of the glyph 
//
// Gets the logical and ink extents of a glyph within a font.
// 
// The coordinate system for each rectangle has its origin at the
// base line and horizontal origin of the character with increasing
// coordinates extending to the right and down. The macros PANGO_ASCENT(),
// PANGO_DESCENT(), PANGO_LBEARING(), and PANGO_RBEARING() can be used to convert
// from the extents rectangle to more traditional font metrics. The units
// of the rectangles are in 1/PANGO_SCALE of a device unit.
// 
// If @font is %NULL, this function gracefully sets some sane values in the
// output variables and returns.
func (font *FontInstance) GetGlyphExtents(glyph Glyph) (Rectangle, Rectangle) {
	var carg0 *C.PangoFont     // in, none, converted
	var carg1 C.PangoGlyph     // in, none, casted, alias
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg3 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg1 = C.PangoGlyph(glyph)

	C.pango_font_get_glyph_extents(carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(font)
	runtime.KeepAlive(glyph)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg3
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetMetrics wraps pango_font_get_metrics
// 
// The function takes the following parameters:
// 
// 	- language *Language (nullable): language tag used to determine which script
//   to get the metrics for, or %NULL to indicate to get the metrics for
//   the entire font. 
// 
// The function returns the following values:
// 
// 	- goret *FontMetrics 
//
// Gets overall metric information for a font.
// 
// Since the metrics may be substantially different for different scripts,
// a language tag can be provided to indicate that the metrics should be
// retrieved that correspond to the script(s) used by that language.
// 
// If @font is %NULL, this function gracefully sets some sane values in the
// output variables and returns.
func (font *FontInstance) GetMetrics(language *Language) *FontMetrics {
	var carg0 *C.PangoFont        // in, none, converted
	var carg1 *C.PangoLanguage    // in, none, converted, nullable
	var cret  *C.PangoFontMetrics // return, full, converted

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	if language != nil {
		carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))
	}

	cret = C.pango_font_get_metrics(carg0, carg1)
	runtime.KeepAlive(font)
	runtime.KeepAlive(language)

	var goret *FontMetrics

	goret = UnsafeFontMetricsFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// HasChar wraps pango_font_has_char
// 
// The function takes the following parameters:
// 
// 	- wc uint32: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Returns whether the font provides a glyph for this character.
func (font *FontInstance) HasChar(wc uint32) bool {
	var carg0 *C.PangoFont // in, none, converted
	var carg1 C.gunichar   // in, none, casted
	var cret  C.gboolean   // return

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg1 = C.gunichar(wc)

	cret = C.pango_font_has_char(carg0, carg1)
	runtime.KeepAlive(font)
	runtime.KeepAlive(wc)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Serialize wraps pango_font_serialize
// 
// The function returns the following values:
// 
// 	- goret *glib.Bytes 
//
// Serializes the @font in a way that can be uniquely identified.
// 
// There are no guarantees about the format of the output across different
// versions of Pango.
// 
// The intended use of this function is testing, benchmarking and debugging.
// The format is not meant as a permanent storage format.
// 
// To recreate a font from its serialized form, use [func@Pango.Font.deserialize].
func (font *FontInstance) Serialize() *glib.Bytes {
	var carg0 *C.PangoFont // in, none, converted
	var cret  *C.GBytes    // return, full, converted

	carg0 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	cret = C.pango_font_serialize(carg0)
	runtime.KeepAlive(font)

	var goret *glib.Bytes

	goret = glib.UnsafeBytesFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// FontOverrides is the struct used to override the default implementation of virtual methods.
// it is generic over the extending instance type.
type FontOverrides[Instance Font] struct {
	// gobject.ObjectOverrides allows you to override virtual methods from the parent class gobject.Object
	gobject.ObjectOverrides[Instance]

	// Describe allows you to override the implementation of the virtual method describe.
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a description of the font, with font size set in points.
	// 
	// Use [method@Pango.Font.describe_with_absolute_size] if you want
	// the font size in device units.
	Describe func(Instance) *FontDescription
	// DescribeAbsolute allows you to override the implementation of the virtual method describe_absolute.
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	DescribeAbsolute func(Instance) *FontDescription
	// GetCoverage allows you to override the implementation of the virtual method get_coverage.
	// The function takes the following parameters:
	// 
	// 	- language *Language: the language tag 
	// 
	// The function returns the following values:
	// 
	// 	- goret Coverage 
	//
	// Computes the coverage map for a given font and language tag.
	GetCoverage func(Instance, *Language) Coverage
	// GetFontMap allows you to override the implementation of the virtual method get_font_map.
	// The function returns the following values:
	// 
	// 	- goret FontMap (nullable) 
	//
	// Gets the font map for which the font was created.
	// 
	// Note that the font maintains a *weak* reference to
	// the font map, so if all references to font map are
	// dropped, the font map will be finalized even if there
	// are fonts created with the font map that are still alive.
	// In that case this function will return %NULL.
	// 
	// It is the responsibility of the user to ensure that the
	// font map is kept alive. In most uses this is not an issue
	// as a `PangoContext` holds a reference to the font map.
	GetFontMap func(Instance) FontMap
	// GetGlyphExtents allows you to override the implementation of the virtual method get_glyph_extents.
	// The function takes the following parameters:
	// 
	// 	- glyph Glyph: the glyph index 
	// 
	// The function returns the following values:
	// 
	// 	- inkRect Rectangle: rectangle used to store the extents of the glyph as drawn 
	// 	- logicalRect Rectangle: rectangle used to store the logical extents of the glyph 
	//
	// Gets the logical and ink extents of a glyph within a font.
	// 
	// The coordinate system for each rectangle has its origin at the
	// base line and horizontal origin of the character with increasing
	// coordinates extending to the right and down. The macros PANGO_ASCENT(),
	// PANGO_DESCENT(), PANGO_LBEARING(), and PANGO_RBEARING() can be used to convert
	// from the extents rectangle to more traditional font metrics. The units
	// of the rectangles are in 1/PANGO_SCALE of a device unit.
	// 
	// If @font is %NULL, this function gracefully sets some sane values in the
	// output variables and returns.
	GetGlyphExtents func(Instance, Glyph) (Rectangle, Rectangle)
	// GetMetrics allows you to override the implementation of the virtual method get_metrics.
	// The function takes the following parameters:
	// 
	// 	- language *Language (nullable): language tag used to determine which script
	//   to get the metrics for, or %NULL to indicate to get the metrics for
	//   the entire font. 
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Gets overall metric information for a font.
	// 
	// Since the metrics may be substantially different for different scripts,
	// a language tag can be provided to indicate that the metrics should be
	// retrieved that correspond to the script(s) used by that language.
	// 
	// If @font is %NULL, this function gracefully sets some sane values in the
	// output variables and returns.
	GetMetrics func(Instance, *Language) *FontMetrics
}

// UnsafeApplyFontOverrides applies the overrides to init the gclass by setting the trampoline functions.
// This is used by the bindings internally and only exported for visibility to other bindings code.
func UnsafeApplyFontOverrides[Instance Font](gclass unsafe.Pointer, overrides FontOverrides[Instance]) {
	gobject.UnsafeApplyObjectOverrides(gclass, overrides.ObjectOverrides)

	pclass := (*C.PangoFontClass)(gclass)

	if overrides.Describe != nil {
		pclass.describe = (*[0]byte)(C._gotk4_pango1_Font_describe)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Font_describe",
			func(carg0 *C.PangoFont) (cret *C.PangoFontDescription) {
				var font  Instance         // go PangoFont subclass
				var goret *FontDescription // return, full, converted

				font = UnsafeFontFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.Describe(font)

				cret = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibFull(goret))

				return cret
			},
		)
	}

	if overrides.DescribeAbsolute != nil {
		pclass.describe_absolute = (*[0]byte)(C._gotk4_pango1_Font_describe_absolute)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Font_describe_absolute",
			func(carg0 *C.PangoFont) (cret *C.PangoFontDescription) {
				var font  Instance         // go PangoFont subclass
				var goret *FontDescription // return, full, converted

				font = UnsafeFontFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.DescribeAbsolute(font)

				cret = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibFull(goret))

				return cret
			},
		)
	}

	if overrides.GetCoverage != nil {
		pclass.get_coverage = (*[0]byte)(C._gotk4_pango1_Font_get_coverage)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Font_get_coverage",
			func(carg0 *C.PangoFont, carg1 *C.PangoLanguage) (cret *C.PangoCoverage) {
				var font     Instance  // go PangoFont subclass
				var language *Language // in, none, converted
				var goret    Coverage  // return, full, converted

				font = UnsafeFontFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				language = UnsafeLanguageFromGlibNone(unsafe.Pointer(carg1))

				goret = overrides.GetCoverage(font, language)

				cret = (*C.PangoCoverage)(UnsafeCoverageToGlibFull(goret))

				return cret
			},
		)
	}

	if overrides.GetFontMap != nil {
		pclass.get_font_map = (*[0]byte)(C._gotk4_pango1_Font_get_font_map)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Font_get_font_map",
			func(carg0 *C.PangoFont) (cret *C.PangoFontMap) {
				var font  Instance // go PangoFont subclass
				var goret FontMap  // return, none, converted, nullable

				font = UnsafeFontFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetFontMap(font)

				if goret != nil {
					cret = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(goret))
				}

				return cret
			},
		)
	}

	if overrides.GetGlyphExtents != nil {
		pclass.get_glyph_extents = (*[0]byte)(C._gotk4_pango1_Font_get_glyph_extents)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Font_get_glyph_extents",
			func(carg0 *C.PangoFont, carg1 C.PangoGlyph, carg2 *C.PangoRectangle, carg3 *C.PangoRectangle) {
				var font        Instance  // go PangoFont subclass
				var glyph       Glyph     // in, none, casted, alias
				var inkRect     Rectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
				var logicalRect Rectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

				font = UnsafeFontFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				glyph = Glyph(carg1)

				inkRect, logicalRect = overrides.GetGlyphExtents(font, glyph)

				_ = inkRect
				_ = carg2
				panic("unimplemented conversion of Rectangle (PangoRectangle)")
				_ = logicalRect
				_ = carg3
				panic("unimplemented conversion of Rectangle (PangoRectangle)")
			},
		)
	}

	if overrides.GetMetrics != nil {
		pclass.get_metrics = (*[0]byte)(C._gotk4_pango1_Font_get_metrics)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Font_get_metrics",
			func(carg0 *C.PangoFont, carg1 *C.PangoLanguage) (cret *C.PangoFontMetrics) {
				var font     Instance     // go PangoFont subclass
				var language *Language    // in, none, converted, nullable
				var goret    *FontMetrics // return, full, converted

				font = UnsafeFontFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				if carg1 != nil {
					language = UnsafeLanguageFromGlibNone(unsafe.Pointer(carg1))
				}

				goret = overrides.GetMetrics(font, language)

				cret = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibFull(goret))

				return cret
			},
		)
	}
}

// ParentDescribe calls the default implementations of the describe virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Returns a description of the font, with font size set in points.
// 
// Use [method@Pango.Font.describe_with_absolute_size] if you want
// the font size in device units.
func (font *FontInstance) ParentDescribe() *FontDescription {
	var carg0 *C.PangoFont
	var cret  *C.PangoFontDescription // return, full, converted

	parentclass := (*C.PangoFontClass)(classdata.PeekParentClass(UnsafeFontToGlibNone(font)))

	cret = C._gotk4_pango1_Font_virtual_describe(unsafe.Pointer(parentclass.describe), carg0)
	runtime.KeepAlive(font)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ParentDescribeAbsolute calls the default implementations of the describe_absolute virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret *FontDescription 
func (font *FontInstance) ParentDescribeAbsolute() *FontDescription {
	var carg0 *C.PangoFont
	var cret  *C.PangoFontDescription // return, full, converted

	parentclass := (*C.PangoFontClass)(classdata.PeekParentClass(UnsafeFontToGlibNone(font)))

	cret = C._gotk4_pango1_Font_virtual_describe_absolute(unsafe.Pointer(parentclass.describe_absolute), carg0)
	runtime.KeepAlive(font)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ParentGetCoverage calls the default implementations of the get_coverage virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- language *Language: the language tag 
// 
// The function returns the following values:
// 
// 	- goret Coverage 
//
// Computes the coverage map for a given font and language tag.
func (font *FontInstance) ParentGetCoverage(language *Language) Coverage {
	var carg0 *C.PangoFont
	var carg1 *C.PangoLanguage // in, none, converted
	var cret  *C.PangoCoverage // return, full, converted

	parentclass := (*C.PangoFontClass)(classdata.PeekParentClass(UnsafeFontToGlibNone(font)))

	carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C._gotk4_pango1_Font_virtual_get_coverage(unsafe.Pointer(parentclass.get_coverage), carg0, carg1)
	runtime.KeepAlive(font)
	runtime.KeepAlive(language)

	var goret Coverage

	goret = UnsafeCoverageFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ParentGetFontMap calls the default implementations of the get_font_map virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret FontMap (nullable) 
//
// Gets the font map for which the font was created.
// 
// Note that the font maintains a *weak* reference to
// the font map, so if all references to font map are
// dropped, the font map will be finalized even if there
// are fonts created with the font map that are still alive.
// In that case this function will return %NULL.
// 
// It is the responsibility of the user to ensure that the
// font map is kept alive. In most uses this is not an issue
// as a `PangoContext` holds a reference to the font map.
func (font *FontInstance) ParentGetFontMap() FontMap {
	var carg0 *C.PangoFont
	var cret  *C.PangoFontMap // return, none, converted, nullable

	parentclass := (*C.PangoFontClass)(classdata.PeekParentClass(UnsafeFontToGlibNone(font)))

	cret = C._gotk4_pango1_Font_virtual_get_font_map(unsafe.Pointer(parentclass.get_font_map), carg0)
	runtime.KeepAlive(font)

	var goret FontMap

	if cret != nil {
		goret = UnsafeFontMapFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// ParentGetGlyphExtents calls the default implementations of the get_glyph_extents virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- glyph Glyph: the glyph index 
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of the glyph as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical extents of the glyph 
//
// Gets the logical and ink extents of a glyph within a font.
// 
// The coordinate system for each rectangle has its origin at the
// base line and horizontal origin of the character with increasing
// coordinates extending to the right and down. The macros PANGO_ASCENT(),
// PANGO_DESCENT(), PANGO_LBEARING(), and PANGO_RBEARING() can be used to convert
// from the extents rectangle to more traditional font metrics. The units
// of the rectangles are in 1/PANGO_SCALE of a device unit.
// 
// If @font is %NULL, this function gracefully sets some sane values in the
// output variables and returns.
func (font *FontInstance) ParentGetGlyphExtents(glyph Glyph) (Rectangle, Rectangle) {
	var carg0 *C.PangoFont
	var carg1 C.PangoGlyph     // in, none, casted, alias
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg3 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	parentclass := (*C.PangoFontClass)(classdata.PeekParentClass(UnsafeFontToGlibNone(font)))

	carg1 = C.PangoGlyph(glyph)

	C._gotk4_pango1_Font_virtual_get_glyph_extents(unsafe.Pointer(parentclass.get_glyph_extents), carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(font)
	runtime.KeepAlive(glyph)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg3
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// ParentGetMetrics calls the default implementations of the get_metrics virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- language *Language (nullable): language tag used to determine which script
//   to get the metrics for, or %NULL to indicate to get the metrics for
//   the entire font. 
// 
// The function returns the following values:
// 
// 	- goret *FontMetrics 
//
// Gets overall metric information for a font.
// 
// Since the metrics may be substantially different for different scripts,
// a language tag can be provided to indicate that the metrics should be
// retrieved that correspond to the script(s) used by that language.
// 
// If @font is %NULL, this function gracefully sets some sane values in the
// output variables and returns.
func (font *FontInstance) ParentGetMetrics(language *Language) *FontMetrics {
	var carg0 *C.PangoFont
	var carg1 *C.PangoLanguage    // in, none, converted, nullable
	var cret  *C.PangoFontMetrics // return, full, converted

	parentclass := (*C.PangoFontClass)(classdata.PeekParentClass(UnsafeFontToGlibNone(font)))

	if language != nil {
		carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))
	}

	cret = C._gotk4_pango1_Font_virtual_get_metrics(unsafe.Pointer(parentclass.get_metrics), carg0, carg1)
	runtime.KeepAlive(font)
	runtime.KeepAlive(language)

	var goret *FontMetrics

	goret = UnsafeFontMetricsFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// RegisterFontSubClass is used to register a go subclass of PangoFont. For this to work safely please implement the
// virtual methods required by the implementation.
func RegisterFontSubClass[InstanceT Font](
		name string,
		classInit func(class *FontClass),
		constructor func() InstanceT,
		overrides FontOverrides[InstanceT],
		signals map[string]gobject.SignalDefinition,
		interfaceInits ...gobject.SubClassInterfaceInit[InstanceT],
) gobject.Type {
	return gobject.UnsafeRegisterSubClass(
		name,
		classInit,
		constructor,
		overrides,
		signals,
		TypeFont,
		UnsafeFontClassFromGlibBorrow,
		UnsafeApplyFontOverrides,
		func (obj *gobject.ObjectInstance) gobject.Object {
			return unsafeWrapFont(obj)
		},
		interfaceInits...,
	)
}

// FontFaceInstance is the instance type used by all types extending PangoFontFace. It is used internally by the bindings. Users should use the interface [FontFace] instead.
type FontFaceInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ FontFace = (*FontFaceInstance)(nil)

// FontFace wraps PangoFontFace
//
// A `PangoFontFace` is used to represent a group of fonts with
// the same family, slant, weight, and width, but varying sizes.
type FontFace interface {
	gobject.Object
	upcastToPangoFontFace() *FontFaceInstance

	// Describe wraps pango_font_face_describe
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a font description that matches the face.
	// 
	// The resulting font description will have the family, style,
	// variant, weight and stretch of the face, but its size field
	// will be unset.
	Describe() *FontDescription
	// GetFaceName wraps pango_font_face_get_face_name
	// 
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets a name representing the style of this face.
	// 
	// Note that a font family may contain multiple faces
	// with the same name (e.g. a variable and a non-variable
	// face for the same style).
	GetFaceName() string
	// GetFamily wraps pango_font_face_get_family
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFamily 
	//
	// Gets the `PangoFontFamily` that @face belongs to.
	GetFamily() FontFamily
	// IsSynthesized wraps pango_font_face_is_synthesized
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Returns whether a `PangoFontFace` is synthesized.
	// 
	// This will be the case if the underlying font rendering engine
	// creates this face from another face, by shearing, emboldening,
	// lightening or modifying it in some other way.
	IsSynthesized() bool

	// chain up virtual methods:

	// ParentDescribe calls the default implementations of the describe virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a font description that matches the face.
	// 
	// The resulting font description will have the family, style,
	// variant, weight and stretch of the face, but its size field
	// will be unset.
	ParentDescribe() *FontDescription
	// ParentGetFaceName calls the default implementations of the get_face_name virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets a name representing the style of this face.
	// 
	// Note that a font family may contain multiple faces
	// with the same name (e.g. a variable and a non-variable
	// face for the same style).
	ParentGetFaceName() string
	// ParentGetFamily calls the default implementations of the get_family virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret FontFamily 
	//
	// Gets the `PangoFontFamily` that @face belongs to.
	ParentGetFamily() FontFamily
	// ParentIsSynthesized calls the default implementations of the is_synthesized virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Returns whether a `PangoFontFace` is synthesized.
	// 
	// This will be the case if the underlying font rendering engine
	// creates this face from another face, by shearing, emboldening,
	// lightening or modifying it in some other way.
	ParentIsSynthesized() bool
}

func unsafeWrapFontFace(base *gobject.ObjectInstance) *FontFaceInstance {
	return &FontFaceInstance{
		ObjectInstance: *base,
	}
}

func marshalFontFaceInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapFontFace(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeFontFaceFromGlibNone is used to convert raw PangoFontFace pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeFontFaceFromGlibNone(c unsafe.Pointer) FontFace {
	return gobject.UnsafeObjectFromGlibNone(c).(FontFace)
}

// UnsafeFontFaceFromGlibFull is used to convert raw PangoFontFace pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeFontFaceFromGlibFull(c unsafe.Pointer) FontFace {
	return gobject.UnsafeObjectFromGlibFull(c).(FontFace)
}

// UnsafeFontFaceFromGlibBorrow is used to convert raw PangoFontFace pointers to go without touching any references. This is used by the bindings internally.
func UnsafeFontFaceFromGlibBorrow(c unsafe.Pointer) FontFace {
	return gobject.UnsafeObjectFromGlibBorrow(c).(FontFace)
}

func (f *FontFaceInstance) upcastToPangoFontFace() *FontFaceInstance {
	return f
}

// UnsafeFontFaceToGlibNone is used to convert the instance to it's C value PangoFontFace. This is used by the bindings internally.
func UnsafeFontFaceToGlibNone(c FontFace) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeFontFaceToGlibFull is used to convert the instance to it's C value PangoFontFace, while removeing the finalizer. This is used by the bindings internally.
func UnsafeFontFaceToGlibFull(c FontFace) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// Describe wraps pango_font_face_describe
// 
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Returns a font description that matches the face.
// 
// The resulting font description will have the family, style,
// variant, weight and stretch of the face, but its size field
// will be unset.
func (face *FontFaceInstance) Describe() *FontDescription {
	var carg0 *C.PangoFontFace        // in, none, converted
	var cret  *C.PangoFontDescription // return, full, converted

	carg0 = (*C.PangoFontFace)(UnsafeFontFaceToGlibNone(face))

	cret = C.pango_font_face_describe(carg0)
	runtime.KeepAlive(face)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetFaceName wraps pango_font_face_get_face_name
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Gets a name representing the style of this face.
// 
// Note that a font family may contain multiple faces
// with the same name (e.g. a variable and a non-variable
// face for the same style).
func (face *FontFaceInstance) GetFaceName() string {
	var carg0 *C.PangoFontFace // in, none, converted
	var cret  *C.char          // return, none, string, casted *C.gchar

	carg0 = (*C.PangoFontFace)(UnsafeFontFaceToGlibNone(face))

	cret = C.pango_font_face_get_face_name(carg0)
	runtime.KeepAlive(face)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// GetFamily wraps pango_font_face_get_family
// 
// The function returns the following values:
// 
// 	- goret FontFamily 
//
// Gets the `PangoFontFamily` that @face belongs to.
func (face *FontFaceInstance) GetFamily() FontFamily {
	var carg0 *C.PangoFontFace   // in, none, converted
	var cret  *C.PangoFontFamily // return, none, converted

	carg0 = (*C.PangoFontFace)(UnsafeFontFaceToGlibNone(face))

	cret = C.pango_font_face_get_family(carg0)
	runtime.KeepAlive(face)

	var goret FontFamily

	goret = UnsafeFontFamilyFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// IsSynthesized wraps pango_font_face_is_synthesized
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Returns whether a `PangoFontFace` is synthesized.
// 
// This will be the case if the underlying font rendering engine
// creates this face from another face, by shearing, emboldening,
// lightening or modifying it in some other way.
func (face *FontFaceInstance) IsSynthesized() bool {
	var carg0 *C.PangoFontFace // in, none, converted
	var cret  C.gboolean       // return

	carg0 = (*C.PangoFontFace)(UnsafeFontFaceToGlibNone(face))

	cret = C.pango_font_face_is_synthesized(carg0)
	runtime.KeepAlive(face)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// FontFaceOverrides is the struct used to override the default implementation of virtual methods.
// it is generic over the extending instance type.
type FontFaceOverrides[Instance FontFace] struct {
	// gobject.ObjectOverrides allows you to override virtual methods from the parent class gobject.Object
	gobject.ObjectOverrides[Instance]

	// Describe allows you to override the implementation of the virtual method describe.
	// The function returns the following values:
	// 
	// 	- goret *FontDescription 
	//
	// Returns a font description that matches the face.
	// 
	// The resulting font description will have the family, style,
	// variant, weight and stretch of the face, but its size field
	// will be unset.
	Describe func(Instance) *FontDescription
	// GetFaceName allows you to override the implementation of the virtual method get_face_name.
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets a name representing the style of this face.
	// 
	// Note that a font family may contain multiple faces
	// with the same name (e.g. a variable and a non-variable
	// face for the same style).
	GetFaceName func(Instance) string
	// GetFamily allows you to override the implementation of the virtual method get_family.
	// The function returns the following values:
	// 
	// 	- goret FontFamily 
	//
	// Gets the `PangoFontFamily` that @face belongs to.
	GetFamily func(Instance) FontFamily
	// IsSynthesized allows you to override the implementation of the virtual method is_synthesized.
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Returns whether a `PangoFontFace` is synthesized.
	// 
	// This will be the case if the underlying font rendering engine
	// creates this face from another face, by shearing, emboldening,
	// lightening or modifying it in some other way.
	IsSynthesized func(Instance) bool
}

// UnsafeApplyFontFaceOverrides applies the overrides to init the gclass by setting the trampoline functions.
// This is used by the bindings internally and only exported for visibility to other bindings code.
func UnsafeApplyFontFaceOverrides[Instance FontFace](gclass unsafe.Pointer, overrides FontFaceOverrides[Instance]) {
	gobject.UnsafeApplyObjectOverrides(gclass, overrides.ObjectOverrides)

	pclass := (*C.PangoFontFaceClass)(gclass)

	if overrides.Describe != nil {
		pclass.describe = (*[0]byte)(C._gotk4_pango1_FontFace_describe)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFace_describe",
			func(carg0 *C.PangoFontFace) (cret *C.PangoFontDescription) {
				var face  Instance         // go PangoFontFace subclass
				var goret *FontDescription // return, full, converted

				face = UnsafeFontFaceFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.Describe(face)

				cret = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibFull(goret))

				return cret
			},
		)
	}

	if overrides.GetFaceName != nil {
		pclass.get_face_name = (*[0]byte)(C._gotk4_pango1_FontFace_get_face_name)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFace_get_face_name",
			func(carg0 *C.PangoFontFace) (cret *C.char) {
				var face  Instance // go PangoFontFace subclass
				var goret string   // return, none, string, casted *C.gchar

				face = UnsafeFontFaceFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetFaceName(face)

				cret = (*C.char)(unsafe.Pointer(C.CString(goret)))
				defer C.free(unsafe.Pointer(cret))

				return cret
			},
		)
	}

	if overrides.GetFamily != nil {
		pclass.get_family = (*[0]byte)(C._gotk4_pango1_FontFace_get_family)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFace_get_family",
			func(carg0 *C.PangoFontFace) (cret *C.PangoFontFamily) {
				var face  Instance   // go PangoFontFace subclass
				var goret FontFamily // return, none, converted

				face = UnsafeFontFaceFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetFamily(face)

				cret = (*C.PangoFontFamily)(UnsafeFontFamilyToGlibNone(goret))

				return cret
			},
		)
	}

	if overrides.IsSynthesized != nil {
		pclass.is_synthesized = (*[0]byte)(C._gotk4_pango1_FontFace_is_synthesized)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFace_is_synthesized",
			func(carg0 *C.PangoFontFace) (cret C.gboolean) {
				var face  Instance // go PangoFontFace subclass
				var goret bool     // return

				face = UnsafeFontFaceFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.IsSynthesized(face)

				if goret {
					cret = C.TRUE
				}

				return cret
			},
		)
	}
}

// ParentDescribe calls the default implementations of the describe virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Returns a font description that matches the face.
// 
// The resulting font description will have the family, style,
// variant, weight and stretch of the face, but its size field
// will be unset.
func (face *FontFaceInstance) ParentDescribe() *FontDescription {
	var carg0 *C.PangoFontFace
	var cret  *C.PangoFontDescription // return, full, converted

	parentclass := (*C.PangoFontFaceClass)(classdata.PeekParentClass(UnsafeFontFaceToGlibNone(face)))

	cret = C._gotk4_pango1_FontFace_virtual_describe(unsafe.Pointer(parentclass.describe), carg0)
	runtime.KeepAlive(face)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ParentGetFaceName calls the default implementations of the get_face_name virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret string 
//
// Gets a name representing the style of this face.
// 
// Note that a font family may contain multiple faces
// with the same name (e.g. a variable and a non-variable
// face for the same style).
func (face *FontFaceInstance) ParentGetFaceName() string {
	var carg0 *C.PangoFontFace
	var cret  *C.char // return, none, string, casted *C.gchar

	parentclass := (*C.PangoFontFaceClass)(classdata.PeekParentClass(UnsafeFontFaceToGlibNone(face)))

	cret = C._gotk4_pango1_FontFace_virtual_get_face_name(unsafe.Pointer(parentclass.get_face_name), carg0)
	runtime.KeepAlive(face)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// ParentGetFamily calls the default implementations of the get_family virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret FontFamily 
//
// Gets the `PangoFontFamily` that @face belongs to.
func (face *FontFaceInstance) ParentGetFamily() FontFamily {
	var carg0 *C.PangoFontFace
	var cret  *C.PangoFontFamily // return, none, converted

	parentclass := (*C.PangoFontFaceClass)(classdata.PeekParentClass(UnsafeFontFaceToGlibNone(face)))

	cret = C._gotk4_pango1_FontFace_virtual_get_family(unsafe.Pointer(parentclass.get_family), carg0)
	runtime.KeepAlive(face)

	var goret FontFamily

	goret = UnsafeFontFamilyFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// ParentIsSynthesized calls the default implementations of the is_synthesized virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret bool 
//
// Returns whether a `PangoFontFace` is synthesized.
// 
// This will be the case if the underlying font rendering engine
// creates this face from another face, by shearing, emboldening,
// lightening or modifying it in some other way.
func (face *FontFaceInstance) ParentIsSynthesized() bool {
	var carg0 *C.PangoFontFace
	var cret  C.gboolean // return

	parentclass := (*C.PangoFontFaceClass)(classdata.PeekParentClass(UnsafeFontFaceToGlibNone(face)))

	cret = C._gotk4_pango1_FontFace_virtual_is_synthesized(unsafe.Pointer(parentclass.is_synthesized), carg0)
	runtime.KeepAlive(face)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// RegisterFontFaceSubClass is used to register a go subclass of PangoFontFace. For this to work safely please implement the
// virtual methods required by the implementation.
func RegisterFontFaceSubClass[InstanceT FontFace](
		name string,
		classInit func(class *FontFaceClass),
		constructor func() InstanceT,
		overrides FontFaceOverrides[InstanceT],
		signals map[string]gobject.SignalDefinition,
		interfaceInits ...gobject.SubClassInterfaceInit[InstanceT],
) gobject.Type {
	return gobject.UnsafeRegisterSubClass(
		name,
		classInit,
		constructor,
		overrides,
		signals,
		TypeFontFace,
		UnsafeFontFaceClassFromGlibBorrow,
		UnsafeApplyFontFaceOverrides,
		func (obj *gobject.ObjectInstance) gobject.Object {
			return unsafeWrapFontFace(obj)
		},
		interfaceInits...,
	)
}

// FontFamilyInstance is the instance type used by all types extending PangoFontFamily. It is used internally by the bindings. Users should use the interface [FontFamily] instead.
type FontFamilyInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
	// implemented interfaces:
	gio.ListModelInstance
}

var _ FontFamily = (*FontFamilyInstance)(nil)

// FontFamily wraps PangoFontFamily
//
// A `PangoFontFamily` is used to represent a family of related
// font faces.
// 
// The font faces in a family share a common design, but differ in
// slant, weight, width or other aspects.
type FontFamily interface {
	gobject.Object
	gio.ListModel
	upcastToPangoFontFamily() *FontFamilyInstance

	// GetFace wraps pango_font_family_get_face
	// 
	// The function takes the following parameters:
	// 
	// 	- name string (nullable): the name of a face. If the name is %NULL,
	//   the family's default face (fontconfig calls it "Regular")
	//   will be returned. 
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFace (nullable) 
	//
	// Gets the `PangoFontFace` of @family with the given name.
	GetFace(string) FontFace
	// GetName wraps pango_font_family_get_name
	// 
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets the name of the family.
	// 
	// The name is unique among all fonts for the font backend and can
	// be used in a `PangoFontDescription` to specify that a face from
	// this family is desired.
	GetName() string
	// IsMonospace wraps pango_font_family_is_monospace
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// A monospace font is a font designed for text display where the the
	// characters form a regular grid.
	// 
	// For Western languages this would
	// mean that the advance width of all characters are the same, but
	// this categorization also includes Asian fonts which include
	// double-width characters: characters that occupy two grid cells.
	// g_unichar_iswide() returns a result that indicates whether a
	// character is typically double-width in a monospace font.
	// 
	// The best way to find out the grid-cell size is to call
	// [method@Pango.FontMetrics.get_approximate_digit_width], since the
	// results of [method@Pango.FontMetrics.get_approximate_char_width] may
	// be affected by double-width characters.
	IsMonospace() bool
	// IsVariable wraps pango_font_family_is_variable
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// A variable font is a font which has axes that can be modified to
	// produce different faces.
	// 
	// Such axes are also known as _variations_; see
	// [method@Pango.FontDescription.set_variations] for more information.
	IsVariable() bool

	// chain up virtual methods:

	// ParentGetFace calls the default implementations of the get_face virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- name string (nullable): the name of a face. If the name is %NULL,
	//   the family's default face (fontconfig calls it "Regular")
	//   will be returned. 
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFace (nullable) 
	//
	// Gets the `PangoFontFace` of @family with the given name.
	ParentGetFace(name string) FontFace
	// ParentGetName calls the default implementations of the get_name virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets the name of the family.
	// 
	// The name is unique among all fonts for the font backend and can
	// be used in a `PangoFontDescription` to specify that a face from
	// this family is desired.
	ParentGetName() string
	// ParentIsMonospace calls the default implementations of the is_monospace virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// A monospace font is a font designed for text display where the the
	// characters form a regular grid.
	// 
	// For Western languages this would
	// mean that the advance width of all characters are the same, but
	// this categorization also includes Asian fonts which include
	// double-width characters: characters that occupy two grid cells.
	// g_unichar_iswide() returns a result that indicates whether a
	// character is typically double-width in a monospace font.
	// 
	// The best way to find out the grid-cell size is to call
	// [method@Pango.FontMetrics.get_approximate_digit_width], since the
	// results of [method@Pango.FontMetrics.get_approximate_char_width] may
	// be affected by double-width characters.
	ParentIsMonospace() bool
	// ParentIsVariable calls the default implementations of the is_variable virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// A variable font is a font which has axes that can be modified to
	// produce different faces.
	// 
	// Such axes are also known as _variations_; see
	// [method@Pango.FontDescription.set_variations] for more information.
	ParentIsVariable() bool
}

func unsafeWrapFontFamily(base *gobject.ObjectInstance) *FontFamilyInstance {
	return &FontFamilyInstance{
		ObjectInstance: *base,
		ListModelInstance: gio.ListModelInstance{
			ObjectInstance: *base,
		},
	}
}

func marshalFontFamilyInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapFontFamily(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeFontFamilyFromGlibNone is used to convert raw PangoFontFamily pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeFontFamilyFromGlibNone(c unsafe.Pointer) FontFamily {
	return gobject.UnsafeObjectFromGlibNone(c).(FontFamily)
}

// UnsafeFontFamilyFromGlibFull is used to convert raw PangoFontFamily pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeFontFamilyFromGlibFull(c unsafe.Pointer) FontFamily {
	return gobject.UnsafeObjectFromGlibFull(c).(FontFamily)
}

// UnsafeFontFamilyFromGlibBorrow is used to convert raw PangoFontFamily pointers to go without touching any references. This is used by the bindings internally.
func UnsafeFontFamilyFromGlibBorrow(c unsafe.Pointer) FontFamily {
	return gobject.UnsafeObjectFromGlibBorrow(c).(FontFamily)
}

func (f *FontFamilyInstance) upcastToPangoFontFamily() *FontFamilyInstance {
	return f
}

// UnsafeFontFamilyToGlibNone is used to convert the instance to it's C value PangoFontFamily. This is used by the bindings internally.
func UnsafeFontFamilyToGlibNone(c FontFamily) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeFontFamilyToGlibFull is used to convert the instance to it's C value PangoFontFamily, while removeing the finalizer. This is used by the bindings internally.
func UnsafeFontFamilyToGlibFull(c FontFamily) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// GetFace wraps pango_font_family_get_face
// 
// The function takes the following parameters:
// 
// 	- name string (nullable): the name of a face. If the name is %NULL,
//   the family's default face (fontconfig calls it "Regular")
//   will be returned. 
// 
// The function returns the following values:
// 
// 	- goret FontFace (nullable) 
//
// Gets the `PangoFontFace` of @family with the given name.
func (family *FontFamilyInstance) GetFace(name string) FontFace {
	var carg0 *C.PangoFontFamily // in, none, converted
	var carg1 *C.char            // in, none, string, nullable-string
	var cret  *C.PangoFontFace   // return, none, converted, nullable

	carg0 = (*C.PangoFontFamily)(UnsafeFontFamilyToGlibNone(family))
	if name != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(name)))
		defer C.free(unsafe.Pointer(carg1))
	}

	cret = C.pango_font_family_get_face(carg0, carg1)
	runtime.KeepAlive(family)
	runtime.KeepAlive(name)

	var goret FontFace

	if cret != nil {
		goret = UnsafeFontFaceFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetName wraps pango_font_family_get_name
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Gets the name of the family.
// 
// The name is unique among all fonts for the font backend and can
// be used in a `PangoFontDescription` to specify that a face from
// this family is desired.
func (family *FontFamilyInstance) GetName() string {
	var carg0 *C.PangoFontFamily // in, none, converted
	var cret  *C.char            // return, none, string, casted *C.gchar

	carg0 = (*C.PangoFontFamily)(UnsafeFontFamilyToGlibNone(family))

	cret = C.pango_font_family_get_name(carg0)
	runtime.KeepAlive(family)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// IsMonospace wraps pango_font_family_is_monospace
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// A monospace font is a font designed for text display where the the
// characters form a regular grid.
// 
// For Western languages this would
// mean that the advance width of all characters are the same, but
// this categorization also includes Asian fonts which include
// double-width characters: characters that occupy two grid cells.
// g_unichar_iswide() returns a result that indicates whether a
// character is typically double-width in a monospace font.
// 
// The best way to find out the grid-cell size is to call
// [method@Pango.FontMetrics.get_approximate_digit_width], since the
// results of [method@Pango.FontMetrics.get_approximate_char_width] may
// be affected by double-width characters.
func (family *FontFamilyInstance) IsMonospace() bool {
	var carg0 *C.PangoFontFamily // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoFontFamily)(UnsafeFontFamilyToGlibNone(family))

	cret = C.pango_font_family_is_monospace(carg0)
	runtime.KeepAlive(family)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// IsVariable wraps pango_font_family_is_variable
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// A variable font is a font which has axes that can be modified to
// produce different faces.
// 
// Such axes are also known as _variations_; see
// [method@Pango.FontDescription.set_variations] for more information.
func (family *FontFamilyInstance) IsVariable() bool {
	var carg0 *C.PangoFontFamily // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoFontFamily)(UnsafeFontFamilyToGlibNone(family))

	cret = C.pango_font_family_is_variable(carg0)
	runtime.KeepAlive(family)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// FontFamilyOverrides is the struct used to override the default implementation of virtual methods.
// it is generic over the extending instance type.
type FontFamilyOverrides[Instance FontFamily] struct {
	// gobject.ObjectOverrides allows you to override virtual methods from the parent class gobject.Object
	gobject.ObjectOverrides[Instance]

	// GetFace allows you to override the implementation of the virtual method get_face.
	// The function takes the following parameters:
	// 
	// 	- name string (nullable): the name of a face. If the name is %NULL,
	//   the family's default face (fontconfig calls it "Regular")
	//   will be returned. 
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFace (nullable) 
	//
	// Gets the `PangoFontFace` of @family with the given name.
	GetFace func(Instance, string) FontFace
	// GetName allows you to override the implementation of the virtual method get_name.
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets the name of the family.
	// 
	// The name is unique among all fonts for the font backend and can
	// be used in a `PangoFontDescription` to specify that a face from
	// this family is desired.
	GetName func(Instance) string
	// IsMonospace allows you to override the implementation of the virtual method is_monospace.
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// A monospace font is a font designed for text display where the the
	// characters form a regular grid.
	// 
	// For Western languages this would
	// mean that the advance width of all characters are the same, but
	// this categorization also includes Asian fonts which include
	// double-width characters: characters that occupy two grid cells.
	// g_unichar_iswide() returns a result that indicates whether a
	// character is typically double-width in a monospace font.
	// 
	// The best way to find out the grid-cell size is to call
	// [method@Pango.FontMetrics.get_approximate_digit_width], since the
	// results of [method@Pango.FontMetrics.get_approximate_char_width] may
	// be affected by double-width characters.
	IsMonospace func(Instance) bool
	// IsVariable allows you to override the implementation of the virtual method is_variable.
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// A variable font is a font which has axes that can be modified to
	// produce different faces.
	// 
	// Such axes are also known as _variations_; see
	// [method@Pango.FontDescription.set_variations] for more information.
	IsVariable func(Instance) bool
}

// UnsafeApplyFontFamilyOverrides applies the overrides to init the gclass by setting the trampoline functions.
// This is used by the bindings internally and only exported for visibility to other bindings code.
func UnsafeApplyFontFamilyOverrides[Instance FontFamily](gclass unsafe.Pointer, overrides FontFamilyOverrides[Instance]) {
	gobject.UnsafeApplyObjectOverrides(gclass, overrides.ObjectOverrides)

	pclass := (*C.PangoFontFamilyClass)(gclass)

	if overrides.GetFace != nil {
		pclass.get_face = (*[0]byte)(C._gotk4_pango1_FontFamily_get_face)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFamily_get_face",
			func(carg0 *C.PangoFontFamily, carg1 *C.char) (cret *C.PangoFontFace) {
				var family Instance // go PangoFontFamily subclass
				var name   string   // in, none, string, casted *C.gchar, nullable
				var goret  FontFace // return, none, converted, nullable

				family = UnsafeFontFamilyFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				if carg1 != nil {
					name = C.GoString((*C.char)(unsafe.Pointer(carg1)))
				}

				goret = overrides.GetFace(family, name)

				if goret != nil {
					cret = (*C.PangoFontFace)(UnsafeFontFaceToGlibNone(goret))
				}

				return cret
			},
		)
	}

	if overrides.GetName != nil {
		pclass.get_name = (*[0]byte)(C._gotk4_pango1_FontFamily_get_name)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFamily_get_name",
			func(carg0 *C.PangoFontFamily) (cret *C.char) {
				var family Instance // go PangoFontFamily subclass
				var goret  string   // return, none, string, casted *C.gchar

				family = UnsafeFontFamilyFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetName(family)

				cret = (*C.char)(unsafe.Pointer(C.CString(goret)))
				defer C.free(unsafe.Pointer(cret))

				return cret
			},
		)
	}

	if overrides.IsMonospace != nil {
		pclass.is_monospace = (*[0]byte)(C._gotk4_pango1_FontFamily_is_monospace)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFamily_is_monospace",
			func(carg0 *C.PangoFontFamily) (cret C.gboolean) {
				var family Instance // go PangoFontFamily subclass
				var goret  bool     // return

				family = UnsafeFontFamilyFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.IsMonospace(family)

				if goret {
					cret = C.TRUE
				}

				return cret
			},
		)
	}

	if overrides.IsVariable != nil {
		pclass.is_variable = (*[0]byte)(C._gotk4_pango1_FontFamily_is_variable)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontFamily_is_variable",
			func(carg0 *C.PangoFontFamily) (cret C.gboolean) {
				var family Instance // go PangoFontFamily subclass
				var goret  bool     // return

				family = UnsafeFontFamilyFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.IsVariable(family)

				if goret {
					cret = C.TRUE
				}

				return cret
			},
		)
	}
}

// ParentGetFace calls the default implementations of the get_face virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- name string (nullable): the name of a face. If the name is %NULL,
//   the family's default face (fontconfig calls it "Regular")
//   will be returned. 
// 
// The function returns the following values:
// 
// 	- goret FontFace (nullable) 
//
// Gets the `PangoFontFace` of @family with the given name.
func (family *FontFamilyInstance) ParentGetFace(name string) FontFace {
	var carg0 *C.PangoFontFamily
	var carg1 *C.char          // in, none, string, nullable-string
	var cret  *C.PangoFontFace // return, none, converted, nullable

	parentclass := (*C.PangoFontFamilyClass)(classdata.PeekParentClass(UnsafeFontFamilyToGlibNone(family)))

	if name != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(name)))
		defer C.free(unsafe.Pointer(carg1))
	}

	cret = C._gotk4_pango1_FontFamily_virtual_get_face(unsafe.Pointer(parentclass.get_face), carg0, carg1)
	runtime.KeepAlive(family)
	runtime.KeepAlive(name)

	var goret FontFace

	if cret != nil {
		goret = UnsafeFontFaceFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// ParentGetName calls the default implementations of the get_name virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret string 
//
// Gets the name of the family.
// 
// The name is unique among all fonts for the font backend and can
// be used in a `PangoFontDescription` to specify that a face from
// this family is desired.
func (family *FontFamilyInstance) ParentGetName() string {
	var carg0 *C.PangoFontFamily
	var cret  *C.char // return, none, string, casted *C.gchar

	parentclass := (*C.PangoFontFamilyClass)(classdata.PeekParentClass(UnsafeFontFamilyToGlibNone(family)))

	cret = C._gotk4_pango1_FontFamily_virtual_get_name(unsafe.Pointer(parentclass.get_name), carg0)
	runtime.KeepAlive(family)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// ParentIsMonospace calls the default implementations of the is_monospace virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret bool 
//
// A monospace font is a font designed for text display where the the
// characters form a regular grid.
// 
// For Western languages this would
// mean that the advance width of all characters are the same, but
// this categorization also includes Asian fonts which include
// double-width characters: characters that occupy two grid cells.
// g_unichar_iswide() returns a result that indicates whether a
// character is typically double-width in a monospace font.
// 
// The best way to find out the grid-cell size is to call
// [method@Pango.FontMetrics.get_approximate_digit_width], since the
// results of [method@Pango.FontMetrics.get_approximate_char_width] may
// be affected by double-width characters.
func (family *FontFamilyInstance) ParentIsMonospace() bool {
	var carg0 *C.PangoFontFamily
	var cret  C.gboolean // return

	parentclass := (*C.PangoFontFamilyClass)(classdata.PeekParentClass(UnsafeFontFamilyToGlibNone(family)))

	cret = C._gotk4_pango1_FontFamily_virtual_is_monospace(unsafe.Pointer(parentclass.is_monospace), carg0)
	runtime.KeepAlive(family)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// ParentIsVariable calls the default implementations of the is_variable virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret bool 
//
// A variable font is a font which has axes that can be modified to
// produce different faces.
// 
// Such axes are also known as _variations_; see
// [method@Pango.FontDescription.set_variations] for more information.
func (family *FontFamilyInstance) ParentIsVariable() bool {
	var carg0 *C.PangoFontFamily
	var cret  C.gboolean // return

	parentclass := (*C.PangoFontFamilyClass)(classdata.PeekParentClass(UnsafeFontFamilyToGlibNone(family)))

	cret = C._gotk4_pango1_FontFamily_virtual_is_variable(unsafe.Pointer(parentclass.is_variable), carg0)
	runtime.KeepAlive(family)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// RegisterFontFamilySubClass is used to register a go subclass of PangoFontFamily. For this to work safely please implement the
// virtual methods required by the implementation.
func RegisterFontFamilySubClass[InstanceT FontFamily](
		name string,
		classInit func(class *FontFamilyClass),
		constructor func() InstanceT,
		overrides FontFamilyOverrides[InstanceT],
		signals map[string]gobject.SignalDefinition,
		interfaceInits ...gobject.SubClassInterfaceInit[InstanceT],
) gobject.Type {
	return gobject.UnsafeRegisterSubClass(
		name,
		classInit,
		constructor,
		overrides,
		signals,
		TypeFontFamily,
		UnsafeFontFamilyClassFromGlibBorrow,
		UnsafeApplyFontFamilyOverrides,
		func (obj *gobject.ObjectInstance) gobject.Object {
			return unsafeWrapFontFamily(obj)
		},
		interfaceInits...,
	)
}

// FontMapInstance is the instance type used by all types extending PangoFontMap. It is used internally by the bindings. Users should use the interface [FontMap] instead.
type FontMapInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
	// implemented interfaces:
	gio.ListModelInstance
}

var _ FontMap = (*FontMapInstance)(nil)

// FontMap wraps PangoFontMap
//
// A `PangoFontMap` represents the set of fonts available for a
// particular rendering system.
// 
// This is a virtual object with implementations being specific to
// particular rendering systems.
type FontMap interface {
	gobject.Object
	gio.ListModel
	upcastToPangoFontMap() *FontMapInstance

	// AddFontFile wraps pango_font_map_add_font_file
	// 
	// The function takes the following parameters:
	// 
	// 	- filename string: Path to the font file 
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	// 	- _goerr error (nullable): an error 
	//
	// Loads a font file with one or more fonts into the `PangoFontMap`.
	// 
	// The added fonts will take precedence over preexisting
	// fonts with the same name.
	AddFontFile(string) (bool, error)
	// Changed wraps pango_font_map_changed
	//
	// Forces a change in the context, which will cause any `PangoContext`
	// using this fontmap to change.
	// 
	// This function is only useful when implementing a new backend
	// for Pango, something applications won't do. Backends should
	// call this function if they have attached extra data to the
	// context and such data is changed.
	Changed()
	// CreateContext wraps pango_font_map_create_context
	// 
	// The function returns the following values:
	// 
	// 	- goret Context 
	//
	// Creates a `PangoContext` connected to @fontmap.
	// 
	// This is equivalent to [ctor@Pango.Context.new] followed by
	// [method@Pango.Context.set_font_map].
	// 
	// If you are using Pango as part of a higher-level system,
	// that system may have it's own way of create a `PangoContext`.
	// For instance, the GTK toolkit has, among others,
	// gtk_widget_get_pango_context(). Use those instead.
	CreateContext() Context
	// GetFamily wraps pango_font_map_get_family
	// 
	// The function takes the following parameters:
	// 
	// 	- name string: a family name 
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFamily 
	//
	// Gets a font family by name.
	GetFamily(string) FontFamily
	// GetSerial wraps pango_font_map_get_serial
	// 
	// The function returns the following values:
	// 
	// 	- goret uint 
	//
	// Returns the current serial number of @fontmap.
	// 
	// The serial number is initialized to an small number larger than zero
	// when a new fontmap is created and is increased whenever the fontmap
	// is changed. It may wrap, but will never have the value 0. Since it can
	// wrap, never compare it with "less than", always use "not equals".
	// 
	// The fontmap can only be changed using backend-specific API, like changing
	// fontmap resolution.
	// 
	// This can be used to automatically detect changes to a `PangoFontMap`,
	// like in `PangoContext`.
	GetSerial() uint
	// LoadFont wraps pango_font_map_load_font
	// 
	// The function takes the following parameters:
	// 
	// 	- _context Context: the `PangoContext` the font will be used with 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font (nullable) 
	//
	// Load the font in the fontmap that is the closest match for @desc.
	LoadFont(Context, *FontDescription) Font
	// LoadFontset wraps pango_font_map_load_fontset
	// 
	// The function takes the following parameters:
	// 
	// 	- _context Context: the `PangoContext` the font will be used with 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 	- language *Language: a `PangoLanguage` the fonts will be used for 
	// 
	// The function returns the following values:
	// 
	// 	- goret Fontset (nullable) 
	//
	// Load a set of fonts in the fontmap that can be used to render
	// a font matching @desc.
	LoadFontset(Context, *FontDescription, *Language) Fontset
	// ReloadFont wraps pango_font_map_reload_font
	// 
	// The function takes the following parameters:
	// 
	// 	- font Font: a font in @fontmap 
	// 	- scale float64: the scale factor to apply 
	// 	- _context Context (nullable): a `PangoContext` 
	// 	- variations string (nullable): font variations to use 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font 
	//
	// Returns a new font that is like @font, except that it is scaled
	// by @scale, its backend-dependent configuration (e.g. cairo font options)
	// is replaced by the one in @context, and its variations are replaced
	// by @variations.
	// 
	// Note that the scaling here is meant to be linear, so this
	// scaling can be used to render a font on a hi-dpi display
	// without changing its optical size.
	ReloadFont(Font, float64, Context, string) Font

	// chain up virtual methods:

	// ParentChanged calls the default implementations of the changed virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	//
	// Forces a change in the context, which will cause any `PangoContext`
	// using this fontmap to change.
	// 
	// This function is only useful when implementing a new backend
	// for Pango, something applications won't do. Backends should
	// call this function if they have attached extra data to the
	// context and such data is changed.
	ParentChanged()
	// ParentGetFamily calls the default implementations of the get_family virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- name string: a family name 
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFamily 
	//
	// Gets a font family by name.
	ParentGetFamily(name string) FontFamily
	// ParentGetSerial calls the default implementations of the get_serial virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret uint 
	//
	// Returns the current serial number of @fontmap.
	// 
	// The serial number is initialized to an small number larger than zero
	// when a new fontmap is created and is increased whenever the fontmap
	// is changed. It may wrap, but will never have the value 0. Since it can
	// wrap, never compare it with "less than", always use "not equals".
	// 
	// The fontmap can only be changed using backend-specific API, like changing
	// fontmap resolution.
	// 
	// This can be used to automatically detect changes to a `PangoFontMap`,
	// like in `PangoContext`.
	ParentGetSerial() uint
	// ParentLoadFont calls the default implementations of the load_font virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- _context Context: the `PangoContext` the font will be used with 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font (nullable) 
	//
	// Load the font in the fontmap that is the closest match for @desc.
	ParentLoadFont(_context Context, desc *FontDescription) Font
	// ParentLoadFontset calls the default implementations of the load_fontset virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- _context Context: the `PangoContext` the font will be used with 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 	- language *Language: a `PangoLanguage` the fonts will be used for 
	// 
	// The function returns the following values:
	// 
	// 	- goret Fontset (nullable) 
	//
	// Load a set of fonts in the fontmap that can be used to render
	// a font matching @desc.
	ParentLoadFontset(_context Context, desc *FontDescription, language *Language) Fontset
}

func unsafeWrapFontMap(base *gobject.ObjectInstance) *FontMapInstance {
	return &FontMapInstance{
		ObjectInstance: *base,
		ListModelInstance: gio.ListModelInstance{
			ObjectInstance: *base,
		},
	}
}

func marshalFontMapInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapFontMap(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeFontMapFromGlibNone is used to convert raw PangoFontMap pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeFontMapFromGlibNone(c unsafe.Pointer) FontMap {
	return gobject.UnsafeObjectFromGlibNone(c).(FontMap)
}

// UnsafeFontMapFromGlibFull is used to convert raw PangoFontMap pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeFontMapFromGlibFull(c unsafe.Pointer) FontMap {
	return gobject.UnsafeObjectFromGlibFull(c).(FontMap)
}

// UnsafeFontMapFromGlibBorrow is used to convert raw PangoFontMap pointers to go without touching any references. This is used by the bindings internally.
func UnsafeFontMapFromGlibBorrow(c unsafe.Pointer) FontMap {
	return gobject.UnsafeObjectFromGlibBorrow(c).(FontMap)
}

func (f *FontMapInstance) upcastToPangoFontMap() *FontMapInstance {
	return f
}

// UnsafeFontMapToGlibNone is used to convert the instance to it's C value PangoFontMap. This is used by the bindings internally.
func UnsafeFontMapToGlibNone(c FontMap) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeFontMapToGlibFull is used to convert the instance to it's C value PangoFontMap, while removeing the finalizer. This is used by the bindings internally.
func UnsafeFontMapToGlibFull(c FontMap) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// AddFontFile wraps pango_font_map_add_font_file
// 
// The function takes the following parameters:
// 
// 	- filename string: Path to the font file 
// 
// The function returns the following values:
// 
// 	- goret bool 
// 	- _goerr error (nullable): an error 
//
// Loads a font file with one or more fonts into the `PangoFontMap`.
// 
// The added fonts will take precedence over preexisting
// fonts with the same name.
func (fontmap *FontMapInstance) AddFontFile(filename string) (bool, error) {
	var carg0 *C.PangoFontMap // in, none, converted
	var carg1 *C.char         // in, none, string, casted *C.gchar
	var cret  C.gboolean      // return
	var _cerr *C.GError       // out, full, converted, nullable

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(filename)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_font_map_add_font_file(carg0, carg1, &_cerr)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(filename)

	var goret  bool
	var _goerr error

	if cret != 0 {
		goret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return goret, _goerr
}

// Changed wraps pango_font_map_changed
//
// Forces a change in the context, which will cause any `PangoContext`
// using this fontmap to change.
// 
// This function is only useful when implementing a new backend
// for Pango, something applications won't do. Backends should
// call this function if they have attached extra data to the
// context and such data is changed.
func (fontmap *FontMapInstance) Changed() {
	var carg0 *C.PangoFontMap // in, none, converted

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))

	C.pango_font_map_changed(carg0)
	runtime.KeepAlive(fontmap)
}

// CreateContext wraps pango_font_map_create_context
// 
// The function returns the following values:
// 
// 	- goret Context 
//
// Creates a `PangoContext` connected to @fontmap.
// 
// This is equivalent to [ctor@Pango.Context.new] followed by
// [method@Pango.Context.set_font_map].
// 
// If you are using Pango as part of a higher-level system,
// that system may have it's own way of create a `PangoContext`.
// For instance, the GTK toolkit has, among others,
// gtk_widget_get_pango_context(). Use those instead.
func (fontmap *FontMapInstance) CreateContext() Context {
	var carg0 *C.PangoFontMap // in, none, converted
	var cret  *C.PangoContext // return, full, converted

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))

	cret = C.pango_font_map_create_context(carg0)
	runtime.KeepAlive(fontmap)

	var goret Context

	goret = UnsafeContextFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetFamily wraps pango_font_map_get_family
// 
// The function takes the following parameters:
// 
// 	- name string: a family name 
// 
// The function returns the following values:
// 
// 	- goret FontFamily 
//
// Gets a font family by name.
func (fontmap *FontMapInstance) GetFamily(name string) FontFamily {
	var carg0 *C.PangoFontMap    // in, none, converted
	var carg1 *C.char            // in, none, string, casted *C.gchar
	var cret  *C.PangoFontFamily // return, none, converted

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(name)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_font_map_get_family(carg0, carg1)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(name)

	var goret FontFamily

	goret = UnsafeFontFamilyFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// GetSerial wraps pango_font_map_get_serial
// 
// The function returns the following values:
// 
// 	- goret uint 
//
// Returns the current serial number of @fontmap.
// 
// The serial number is initialized to an small number larger than zero
// when a new fontmap is created and is increased whenever the fontmap
// is changed. It may wrap, but will never have the value 0. Since it can
// wrap, never compare it with "less than", always use "not equals".
// 
// The fontmap can only be changed using backend-specific API, like changing
// fontmap resolution.
// 
// This can be used to automatically detect changes to a `PangoFontMap`,
// like in `PangoContext`.
func (fontmap *FontMapInstance) GetSerial() uint {
	var carg0 *C.PangoFontMap // in, none, converted
	var cret  C.guint         // return, none, casted

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))

	cret = C.pango_font_map_get_serial(carg0)
	runtime.KeepAlive(fontmap)

	var goret uint

	goret = uint(cret)

	return goret
}

// LoadFont wraps pango_font_map_load_font
// 
// The function takes the following parameters:
// 
// 	- _context Context: the `PangoContext` the font will be used with 
// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
// 
// The function returns the following values:
// 
// 	- goret Font (nullable) 
//
// Load the font in the fontmap that is the closest match for @desc.
func (fontmap *FontMapInstance) LoadFont(_context Context, desc *FontDescription) Font {
	var carg0 *C.PangoFontMap         // in, none, converted
	var carg1 *C.PangoContext         // in, none, converted
	var carg2 *C.PangoFontDescription // in, none, converted
	var cret  *C.PangoFont            // return, full, converted, nullable

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))
	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_map_load_font(carg0, carg1, carg2)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)

	var goret Font

	if cret != nil {
		goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// LoadFontset wraps pango_font_map_load_fontset
// 
// The function takes the following parameters:
// 
// 	- _context Context: the `PangoContext` the font will be used with 
// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
// 	- language *Language: a `PangoLanguage` the fonts will be used for 
// 
// The function returns the following values:
// 
// 	- goret Fontset (nullable) 
//
// Load a set of fonts in the fontmap that can be used to render
// a font matching @desc.
func (fontmap *FontMapInstance) LoadFontset(_context Context, desc *FontDescription, language *Language) Fontset {
	var carg0 *C.PangoFontMap         // in, none, converted
	var carg1 *C.PangoContext         // in, none, converted
	var carg2 *C.PangoFontDescription // in, none, converted
	var carg3 *C.PangoLanguage        // in, none, converted
	var cret  *C.PangoFontset         // return, full, converted, nullable

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))
	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg3 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_font_map_load_fontset(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(language)

	var goret Fontset

	if cret != nil {
		goret = UnsafeFontsetFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// ReloadFont wraps pango_font_map_reload_font
// 
// The function takes the following parameters:
// 
// 	- font Font: a font in @fontmap 
// 	- scale float64: the scale factor to apply 
// 	- _context Context (nullable): a `PangoContext` 
// 	- variations string (nullable): font variations to use 
// 
// The function returns the following values:
// 
// 	- goret Font 
//
// Returns a new font that is like @font, except that it is scaled
// by @scale, its backend-dependent configuration (e.g. cairo font options)
// is replaced by the one in @context, and its variations are replaced
// by @variations.
// 
// Note that the scaling here is meant to be linear, so this
// scaling can be used to render a font on a hi-dpi display
// without changing its optical size.
func (fontmap *FontMapInstance) ReloadFont(font Font, scale float64, _context Context, variations string) Font {
	var carg0 *C.PangoFontMap // in, none, converted
	var carg1 *C.PangoFont    // in, none, converted
	var carg2 C.double        // in, none, casted, casted C.gdouble
	var carg3 *C.PangoContext // in, none, converted, nullable
	var carg4 *C.char         // in, none, string, nullable-string
	var cret  *C.PangoFont    // return, full, converted

	carg0 = (*C.PangoFontMap)(UnsafeFontMapToGlibNone(fontmap))
	carg1 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg2 = C.double(scale)
	if _context != nil {
		carg3 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	}
	if variations != "" {
		carg4 = (*C.char)(unsafe.Pointer(C.CString(variations)))
		defer C.free(unsafe.Pointer(carg4))
	}

	cret = C.pango_font_map_reload_font(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(font)
	runtime.KeepAlive(scale)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(variations)

	var goret Font

	goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// FontMapOverrides is the struct used to override the default implementation of virtual methods.
// it is generic over the extending instance type.
type FontMapOverrides[Instance FontMap] struct {
	// gobject.ObjectOverrides allows you to override virtual methods from the parent class gobject.Object
	gobject.ObjectOverrides[Instance]

	// Changed allows you to override the implementation of the virtual method changed.
	//
	// Forces a change in the context, which will cause any `PangoContext`
	// using this fontmap to change.
	// 
	// This function is only useful when implementing a new backend
	// for Pango, something applications won't do. Backends should
	// call this function if they have attached extra data to the
	// context and such data is changed.
	Changed func(Instance)
	// GetFamily allows you to override the implementation of the virtual method get_family.
	// The function takes the following parameters:
	// 
	// 	- name string: a family name 
	// 
	// The function returns the following values:
	// 
	// 	- goret FontFamily 
	//
	// Gets a font family by name.
	GetFamily func(Instance, string) FontFamily
	// GetSerial allows you to override the implementation of the virtual method get_serial.
	// The function returns the following values:
	// 
	// 	- goret uint 
	//
	// Returns the current serial number of @fontmap.
	// 
	// The serial number is initialized to an small number larger than zero
	// when a new fontmap is created and is increased whenever the fontmap
	// is changed. It may wrap, but will never have the value 0. Since it can
	// wrap, never compare it with "less than", always use "not equals".
	// 
	// The fontmap can only be changed using backend-specific API, like changing
	// fontmap resolution.
	// 
	// This can be used to automatically detect changes to a `PangoFontMap`,
	// like in `PangoContext`.
	GetSerial func(Instance) uint
	// LoadFont allows you to override the implementation of the virtual method load_font.
	// The function takes the following parameters:
	// 
	// 	- _context Context: the `PangoContext` the font will be used with 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font (nullable) 
	//
	// Load the font in the fontmap that is the closest match for @desc.
	LoadFont func(Instance, Context, *FontDescription) Font
	// LoadFontset allows you to override the implementation of the virtual method load_fontset.
	// The function takes the following parameters:
	// 
	// 	- _context Context: the `PangoContext` the font will be used with 
	// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
	// 	- language *Language: a `PangoLanguage` the fonts will be used for 
	// 
	// The function returns the following values:
	// 
	// 	- goret Fontset (nullable) 
	//
	// Load a set of fonts in the fontmap that can be used to render
	// a font matching @desc.
	LoadFontset func(Instance, Context, *FontDescription, *Language) Fontset
}

// UnsafeApplyFontMapOverrides applies the overrides to init the gclass by setting the trampoline functions.
// This is used by the bindings internally and only exported for visibility to other bindings code.
func UnsafeApplyFontMapOverrides[Instance FontMap](gclass unsafe.Pointer, overrides FontMapOverrides[Instance]) {
	gobject.UnsafeApplyObjectOverrides(gclass, overrides.ObjectOverrides)

	pclass := (*C.PangoFontMapClass)(gclass)

	if overrides.Changed != nil {
		pclass.changed = (*[0]byte)(C._gotk4_pango1_FontMap_changed)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontMap_changed",
			func(carg0 *C.PangoFontMap) {
				var fontmap Instance // go PangoFontMap subclass

				fontmap = UnsafeFontMapFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				overrides.Changed(fontmap)
			},
		)
	}

	if overrides.GetFamily != nil {
		pclass.get_family = (*[0]byte)(C._gotk4_pango1_FontMap_get_family)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontMap_get_family",
			func(carg0 *C.PangoFontMap, carg1 *C.char) (cret *C.PangoFontFamily) {
				var fontmap Instance   // go PangoFontMap subclass
				var name    string     // in, none, string, casted *C.gchar
				var goret   FontFamily // return, none, converted

				fontmap = UnsafeFontMapFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				name = C.GoString((*C.char)(unsafe.Pointer(carg1)))

				goret = overrides.GetFamily(fontmap, name)

				cret = (*C.PangoFontFamily)(UnsafeFontFamilyToGlibNone(goret))

				return cret
			},
		)
	}

	if overrides.GetSerial != nil {
		pclass.get_serial = (*[0]byte)(C._gotk4_pango1_FontMap_get_serial)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontMap_get_serial",
			func(carg0 *C.PangoFontMap) (cret C.guint) {
				var fontmap Instance // go PangoFontMap subclass
				var goret   uint     // return, none, casted

				fontmap = UnsafeFontMapFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetSerial(fontmap)

				cret = C.guint(goret)

				return cret
			},
		)
	}

	if overrides.LoadFont != nil {
		pclass.load_font = (*[0]byte)(C._gotk4_pango1_FontMap_load_font)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontMap_load_font",
			func(carg0 *C.PangoFontMap, carg1 *C.PangoContext, carg2 *C.PangoFontDescription) (cret *C.PangoFont) {
				var fontmap  Instance         // go PangoFontMap subclass
				var _context Context          // in, none, converted
				var desc     *FontDescription // in, none, converted
				var goret    Font             // return, full, converted, nullable

				fontmap = UnsafeFontMapFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				_context = UnsafeContextFromGlibNone(unsafe.Pointer(carg1))
				desc = UnsafeFontDescriptionFromGlibNone(unsafe.Pointer(carg2))

				goret = overrides.LoadFont(fontmap, _context, desc)

				if goret != nil {
					cret = (*C.PangoFont)(UnsafeFontToGlibFull(goret))
				}

				return cret
			},
		)
	}

	if overrides.LoadFontset != nil {
		pclass.load_fontset = (*[0]byte)(C._gotk4_pango1_FontMap_load_fontset)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_FontMap_load_fontset",
			func(carg0 *C.PangoFontMap, carg1 *C.PangoContext, carg2 *C.PangoFontDescription, carg3 *C.PangoLanguage) (cret *C.PangoFontset) {
				var fontmap  Instance         // go PangoFontMap subclass
				var _context Context          // in, none, converted
				var desc     *FontDescription // in, none, converted
				var language *Language        // in, none, converted
				var goret    Fontset          // return, full, converted, nullable

				fontmap = UnsafeFontMapFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				_context = UnsafeContextFromGlibNone(unsafe.Pointer(carg1))
				desc = UnsafeFontDescriptionFromGlibNone(unsafe.Pointer(carg2))
				language = UnsafeLanguageFromGlibNone(unsafe.Pointer(carg3))

				goret = overrides.LoadFontset(fontmap, _context, desc, language)

				if goret != nil {
					cret = (*C.PangoFontset)(UnsafeFontsetToGlibFull(goret))
				}

				return cret
			},
		)
	}
}

// ParentChanged calls the default implementations of the changed virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
//
// Forces a change in the context, which will cause any `PangoContext`
// using this fontmap to change.
// 
// This function is only useful when implementing a new backend
// for Pango, something applications won't do. Backends should
// call this function if they have attached extra data to the
// context and such data is changed.
func (fontmap *FontMapInstance) ParentChanged() {
	var carg0 *C.PangoFontMap

	parentclass := (*C.PangoFontMapClass)(classdata.PeekParentClass(UnsafeFontMapToGlibNone(fontmap)))

	C._gotk4_pango1_FontMap_virtual_changed(unsafe.Pointer(parentclass.changed), carg0)
	runtime.KeepAlive(fontmap)
}

// ParentGetFamily calls the default implementations of the get_family virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- name string: a family name 
// 
// The function returns the following values:
// 
// 	- goret FontFamily 
//
// Gets a font family by name.
func (fontmap *FontMapInstance) ParentGetFamily(name string) FontFamily {
	var carg0 *C.PangoFontMap
	var carg1 *C.char            // in, none, string, casted *C.gchar
	var cret  *C.PangoFontFamily // return, none, converted

	parentclass := (*C.PangoFontMapClass)(classdata.PeekParentClass(UnsafeFontMapToGlibNone(fontmap)))

	carg1 = (*C.char)(unsafe.Pointer(C.CString(name)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C._gotk4_pango1_FontMap_virtual_get_family(unsafe.Pointer(parentclass.get_family), carg0, carg1)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(name)

	var goret FontFamily

	goret = UnsafeFontFamilyFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// ParentGetSerial calls the default implementations of the get_serial virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret uint 
//
// Returns the current serial number of @fontmap.
// 
// The serial number is initialized to an small number larger than zero
// when a new fontmap is created and is increased whenever the fontmap
// is changed. It may wrap, but will never have the value 0. Since it can
// wrap, never compare it with "less than", always use "not equals".
// 
// The fontmap can only be changed using backend-specific API, like changing
// fontmap resolution.
// 
// This can be used to automatically detect changes to a `PangoFontMap`,
// like in `PangoContext`.
func (fontmap *FontMapInstance) ParentGetSerial() uint {
	var carg0 *C.PangoFontMap
	var cret  C.guint // return, none, casted

	parentclass := (*C.PangoFontMapClass)(classdata.PeekParentClass(UnsafeFontMapToGlibNone(fontmap)))

	cret = C._gotk4_pango1_FontMap_virtual_get_serial(unsafe.Pointer(parentclass.get_serial), carg0)
	runtime.KeepAlive(fontmap)

	var goret uint

	goret = uint(cret)

	return goret
}

// ParentLoadFont calls the default implementations of the load_font virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- _context Context: the `PangoContext` the font will be used with 
// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
// 
// The function returns the following values:
// 
// 	- goret Font (nullable) 
//
// Load the font in the fontmap that is the closest match for @desc.
func (fontmap *FontMapInstance) ParentLoadFont(_context Context, desc *FontDescription) Font {
	var carg0 *C.PangoFontMap
	var carg1 *C.PangoContext         // in, none, converted
	var carg2 *C.PangoFontDescription // in, none, converted
	var cret  *C.PangoFont            // return, full, converted, nullable

	parentclass := (*C.PangoFontMapClass)(classdata.PeekParentClass(UnsafeFontMapToGlibNone(fontmap)))

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C._gotk4_pango1_FontMap_virtual_load_font(unsafe.Pointer(parentclass.load_font), carg0, carg1, carg2)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)

	var goret Font

	if cret != nil {
		goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// ParentLoadFontset calls the default implementations of the load_fontset virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- _context Context: the `PangoContext` the font will be used with 
// 	- desc *FontDescription: a `PangoFontDescription` describing the font to load 
// 	- language *Language: a `PangoLanguage` the fonts will be used for 
// 
// The function returns the following values:
// 
// 	- goret Fontset (nullable) 
//
// Load a set of fonts in the fontmap that can be used to render
// a font matching @desc.
func (fontmap *FontMapInstance) ParentLoadFontset(_context Context, desc *FontDescription, language *Language) Fontset {
	var carg0 *C.PangoFontMap
	var carg1 *C.PangoContext         // in, none, converted
	var carg2 *C.PangoFontDescription // in, none, converted
	var carg3 *C.PangoLanguage        // in, none, converted
	var cret  *C.PangoFontset         // return, full, converted, nullable

	parentclass := (*C.PangoFontMapClass)(classdata.PeekParentClass(UnsafeFontMapToGlibNone(fontmap)))

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg3 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C._gotk4_pango1_FontMap_virtual_load_fontset(unsafe.Pointer(parentclass.load_fontset), carg0, carg1, carg2, carg3)
	runtime.KeepAlive(fontmap)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(language)

	var goret Fontset

	if cret != nil {
		goret = UnsafeFontsetFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// RegisterFontMapSubClass is used to register a go subclass of PangoFontMap. For this to work safely please implement the
// virtual methods required by the implementation.
func RegisterFontMapSubClass[InstanceT FontMap](
		name string,
		classInit func(class *FontMapClass),
		constructor func() InstanceT,
		overrides FontMapOverrides[InstanceT],
		signals map[string]gobject.SignalDefinition,
		interfaceInits ...gobject.SubClassInterfaceInit[InstanceT],
) gobject.Type {
	return gobject.UnsafeRegisterSubClass(
		name,
		classInit,
		constructor,
		overrides,
		signals,
		TypeFontMap,
		UnsafeFontMapClassFromGlibBorrow,
		UnsafeApplyFontMapOverrides,
		func (obj *gobject.ObjectInstance) gobject.Object {
			return unsafeWrapFontMap(obj)
		},
		interfaceInits...,
	)
}

// FontsetInstance is the instance type used by all types extending PangoFontset. It is used internally by the bindings. Users should use the interface [Fontset] instead.
type FontsetInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ Fontset = (*FontsetInstance)(nil)

// Fontset wraps PangoFontset
//
// A `PangoFontset` represents a set of `PangoFont` to use when rendering text.
// 
// A `PangoFontset` is the result of resolving a `PangoFontDescription`
// against a particular `PangoContext`. It has operations for finding the
// component font for a particular Unicode character, and for finding a
// composite set of metrics for the entire fontset.
type Fontset interface {
	gobject.Object
	upcastToPangoFontset() *FontsetInstance

	// ForEach wraps pango_fontset_foreach
	// 
	// The function takes the following parameters:
	// 
	// 	- fn FontsetForEachFunc: Callback function 
	//
	// Iterates through all the fonts in a fontset, calling @func for
	// each one.
	// 
	// If @func returns %TRUE, that stops the iteration.
	ForEach(FontsetForEachFunc)
	// GetFont wraps pango_fontset_get_font
	// 
	// The function takes the following parameters:
	// 
	// 	- wc uint: a Unicode character 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font 
	//
	// Returns the font in the fontset that contains the best
	// glyph for a Unicode character.
	GetFont(uint) Font
	// GetMetrics wraps pango_fontset_get_metrics
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Get overall metric information for the fonts in the fontset.
	GetMetrics() *FontMetrics

	// chain up virtual methods:

	// ParentForEach calls the default implementations of the foreach virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- fn FontsetForEachFunc: Callback function 
	//
	// Iterates through all the fonts in a fontset, calling @func for
	// each one.
	// 
	// If @func returns %TRUE, that stops the iteration.
	ParentForEach(fn FontsetForEachFunc)
	// ParentGetFont calls the default implementations of the get_font virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- wc uint: a Unicode character 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font 
	//
	// Returns the font in the fontset that contains the best
	// glyph for a Unicode character.
	ParentGetFont(wc uint) Font
	// ParentGetLanguage calls the default implementations of the get_language virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret *Language 
	//
	// a function to get the language of the fontset.
	ParentGetLanguage() *Language
	// ParentGetMetrics calls the default implementations of the get_metrics virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Get overall metric information for the fonts in the fontset.
	ParentGetMetrics() *FontMetrics
}

func unsafeWrapFontset(base *gobject.ObjectInstance) *FontsetInstance {
	return &FontsetInstance{
		ObjectInstance: *base,
	}
}

func marshalFontsetInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapFontset(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeFontsetFromGlibNone is used to convert raw PangoFontset pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeFontsetFromGlibNone(c unsafe.Pointer) Fontset {
	return gobject.UnsafeObjectFromGlibNone(c).(Fontset)
}

// UnsafeFontsetFromGlibFull is used to convert raw PangoFontset pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeFontsetFromGlibFull(c unsafe.Pointer) Fontset {
	return gobject.UnsafeObjectFromGlibFull(c).(Fontset)
}

// UnsafeFontsetFromGlibBorrow is used to convert raw PangoFontset pointers to go without touching any references. This is used by the bindings internally.
func UnsafeFontsetFromGlibBorrow(c unsafe.Pointer) Fontset {
	return gobject.UnsafeObjectFromGlibBorrow(c).(Fontset)
}

func (f *FontsetInstance) upcastToPangoFontset() *FontsetInstance {
	return f
}

// UnsafeFontsetToGlibNone is used to convert the instance to it's C value PangoFontset. This is used by the bindings internally.
func UnsafeFontsetToGlibNone(c Fontset) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeFontsetToGlibFull is used to convert the instance to it's C value PangoFontset, while removeing the finalizer. This is used by the bindings internally.
func UnsafeFontsetToGlibFull(c Fontset) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// ForEach wraps pango_fontset_foreach
// 
// The function takes the following parameters:
// 
// 	- fn FontsetForEachFunc: Callback function 
//
// Iterates through all the fonts in a fontset, calling @func for
// each one.
// 
// If @func returns %TRUE, that stops the iteration.
func (fontset *FontsetInstance) ForEach(fn FontsetForEachFunc) {
	var carg0 *C.PangoFontset           // in, none, converted
	var carg1 C.PangoFontsetForeachFunc // callback, scope: call, closure: carg2
	var carg2 C.gpointer                // implicit

	carg0 = (*C.PangoFontset)(UnsafeFontsetToGlibNone(fontset))
	carg1 = (*[0]byte)(C._gotk4_pango1_FontsetForEachFunc)
	carg2 = C.gpointer(userdata.Register(fn))
	defer userdata.Delete(unsafe.Pointer(carg2))

	C.pango_fontset_foreach(carg0, carg1, carg2)
	runtime.KeepAlive(fontset)
	runtime.KeepAlive(fn)
}

// GetFont wraps pango_fontset_get_font
// 
// The function takes the following parameters:
// 
// 	- wc uint: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret Font 
//
// Returns the font in the fontset that contains the best
// glyph for a Unicode character.
func (fontset *FontsetInstance) GetFont(wc uint) Font {
	var carg0 *C.PangoFontset // in, none, converted
	var carg1 C.guint         // in, none, casted
	var cret  *C.PangoFont    // return, full, converted

	carg0 = (*C.PangoFontset)(UnsafeFontsetToGlibNone(fontset))
	carg1 = C.guint(wc)

	cret = C.pango_fontset_get_font(carg0, carg1)
	runtime.KeepAlive(fontset)
	runtime.KeepAlive(wc)

	var goret Font

	goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetMetrics wraps pango_fontset_get_metrics
// 
// The function returns the following values:
// 
// 	- goret *FontMetrics 
//
// Get overall metric information for the fonts in the fontset.
func (fontset *FontsetInstance) GetMetrics() *FontMetrics {
	var carg0 *C.PangoFontset     // in, none, converted
	var cret  *C.PangoFontMetrics // return, full, converted

	carg0 = (*C.PangoFontset)(UnsafeFontsetToGlibNone(fontset))

	cret = C.pango_fontset_get_metrics(carg0)
	runtime.KeepAlive(fontset)

	var goret *FontMetrics

	goret = UnsafeFontMetricsFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// FontsetOverrides is the struct used to override the default implementation of virtual methods.
// it is generic over the extending instance type.
type FontsetOverrides[Instance Fontset] struct {
	// gobject.ObjectOverrides allows you to override virtual methods from the parent class gobject.Object
	gobject.ObjectOverrides[Instance]

	// ForEach allows you to override the implementation of the virtual method foreach.
	// The function takes the following parameters:
	// 
	// 	- fn FontsetForEachFunc: Callback function 
	//
	// Iterates through all the fonts in a fontset, calling @func for
	// each one.
	// 
	// If @func returns %TRUE, that stops the iteration.
	ForEach func(Instance, FontsetForEachFunc)
	// GetFont allows you to override the implementation of the virtual method get_font.
	// The function takes the following parameters:
	// 
	// 	- wc uint: a Unicode character 
	// 
	// The function returns the following values:
	// 
	// 	- goret Font 
	//
	// Returns the font in the fontset that contains the best
	// glyph for a Unicode character.
	GetFont func(Instance, uint) Font
	// GetLanguage allows you to override the implementation of the virtual method get_language.
	// The function returns the following values:
	// 
	// 	- goret *Language 
	//
	// a function to get the language of the fontset.
	GetLanguage func(Instance) *Language
	// GetMetrics allows you to override the implementation of the virtual method get_metrics.
	// The function returns the following values:
	// 
	// 	- goret *FontMetrics 
	//
	// Get overall metric information for the fonts in the fontset.
	GetMetrics func(Instance) *FontMetrics
}

// UnsafeApplyFontsetOverrides applies the overrides to init the gclass by setting the trampoline functions.
// This is used by the bindings internally and only exported for visibility to other bindings code.
func UnsafeApplyFontsetOverrides[Instance Fontset](gclass unsafe.Pointer, overrides FontsetOverrides[Instance]) {
	gobject.UnsafeApplyObjectOverrides(gclass, overrides.ObjectOverrides)

	pclass := (*C.PangoFontsetClass)(gclass)

	if overrides.ForEach != nil {
		pclass.foreach = (*[0]byte)(C._gotk4_pango1_Fontset_foreach)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Fontset_foreach",
			func(carg0 *C.PangoFontset, carg1 C.PangoFontsetForeachFunc, carg2 C.gpointer) {
				var fontset Instance           // go PangoFontset subclass
				var fn      FontsetForEachFunc // in, transfer: none, C Pointers: 0, Name: FontsetForeachFunc, closure: carg2

				fontset = UnsafeFontsetFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				_ = fn
				_ = carg1
				_ = carg2
				panic("unimplemented conversion of FontsetForEachFunc (PangoFontsetForeachFunc)")

				overrides.ForEach(fontset, fn)
			},
		)
	}

	if overrides.GetFont != nil {
		pclass.get_font = (*[0]byte)(C._gotk4_pango1_Fontset_get_font)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Fontset_get_font",
			func(carg0 *C.PangoFontset, carg1 C.guint) (cret *C.PangoFont) {
				var fontset Instance // go PangoFontset subclass
				var wc      uint     // in, none, casted
				var goret   Font     // return, full, converted

				fontset = UnsafeFontsetFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				wc = uint(carg1)

				goret = overrides.GetFont(fontset, wc)

				cret = (*C.PangoFont)(UnsafeFontToGlibFull(goret))

				return cret
			},
		)
	}

	if overrides.GetLanguage != nil {
		pclass.get_language = (*[0]byte)(C._gotk4_pango1_Fontset_get_language)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Fontset_get_language",
			func(carg0 *C.PangoFontset) (cret *C.PangoLanguage) {
				var fontset Instance  // go PangoFontset subclass
				var goret   *Language // return, full, converted

				fontset = UnsafeFontsetFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetLanguage(fontset)

				cret = (*C.PangoLanguage)(UnsafeLanguageToGlibFull(goret))

				return cret
			},
		)
	}

	if overrides.GetMetrics != nil {
		pclass.get_metrics = (*[0]byte)(C._gotk4_pango1_Fontset_get_metrics)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Fontset_get_metrics",
			func(carg0 *C.PangoFontset) (cret *C.PangoFontMetrics) {
				var fontset Instance     // go PangoFontset subclass
				var goret   *FontMetrics // return, full, converted

				fontset = UnsafeFontsetFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				goret = overrides.GetMetrics(fontset)

				cret = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibFull(goret))

				return cret
			},
		)
	}
}

// ParentForEach calls the default implementations of the foreach virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- fn FontsetForEachFunc: Callback function 
//
// Iterates through all the fonts in a fontset, calling @func for
// each one.
// 
// If @func returns %TRUE, that stops the iteration.
func (fontset *FontsetInstance) ParentForEach(fn FontsetForEachFunc) {
	var carg0 *C.PangoFontset
	var carg1 C.PangoFontsetForeachFunc // callback, scope: call, closure: carg2
	var carg2 C.gpointer                // implicit

	parentclass := (*C.PangoFontsetClass)(classdata.PeekParentClass(UnsafeFontsetToGlibNone(fontset)))

	carg1 = (*[0]byte)(C._gotk4_pango1_FontsetForEachFunc)
	carg2 = C.gpointer(userdata.Register(fn))
	defer userdata.Delete(unsafe.Pointer(carg2))

	C._gotk4_pango1_Fontset_virtual_foreach(unsafe.Pointer(parentclass.foreach), carg0, carg1, carg2)
	runtime.KeepAlive(fontset)
	runtime.KeepAlive(fn)
}

// ParentGetFont calls the default implementations of the get_font virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- wc uint: a Unicode character 
// 
// The function returns the following values:
// 
// 	- goret Font 
//
// Returns the font in the fontset that contains the best
// glyph for a Unicode character.
func (fontset *FontsetInstance) ParentGetFont(wc uint) Font {
	var carg0 *C.PangoFontset
	var carg1 C.guint      // in, none, casted
	var cret  *C.PangoFont // return, full, converted

	parentclass := (*C.PangoFontsetClass)(classdata.PeekParentClass(UnsafeFontsetToGlibNone(fontset)))

	carg1 = C.guint(wc)

	cret = C._gotk4_pango1_Fontset_virtual_get_font(unsafe.Pointer(parentclass.get_font), carg0, carg1)
	runtime.KeepAlive(fontset)
	runtime.KeepAlive(wc)

	var goret Font

	goret = UnsafeFontFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ParentGetLanguage calls the default implementations of the get_language virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret *Language 
//
// a function to get the language of the fontset.
func (fontset *FontsetInstance) ParentGetLanguage() *Language {
	var carg0 *C.PangoFontset
	var cret  *C.PangoLanguage // return, full, converted

	parentclass := (*C.PangoFontsetClass)(classdata.PeekParentClass(UnsafeFontsetToGlibNone(fontset)))

	cret = C._gotk4_pango1_Fontset_virtual_get_language(unsafe.Pointer(parentclass.get_language), carg0)
	runtime.KeepAlive(fontset)

	var goret *Language

	goret = UnsafeLanguageFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ParentGetMetrics calls the default implementations of the get_metrics virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function returns the following values:
// 
// 	- goret *FontMetrics 
//
// Get overall metric information for the fonts in the fontset.
func (fontset *FontsetInstance) ParentGetMetrics() *FontMetrics {
	var carg0 *C.PangoFontset
	var cret  *C.PangoFontMetrics // return, full, converted

	parentclass := (*C.PangoFontsetClass)(classdata.PeekParentClass(UnsafeFontsetToGlibNone(fontset)))

	cret = C._gotk4_pango1_Fontset_virtual_get_metrics(unsafe.Pointer(parentclass.get_metrics), carg0)
	runtime.KeepAlive(fontset)

	var goret *FontMetrics

	goret = UnsafeFontMetricsFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// RegisterFontsetSubClass is used to register a go subclass of PangoFontset. For this to work safely please implement the
// virtual methods required by the implementation.
func RegisterFontsetSubClass[InstanceT Fontset](
		name string,
		classInit func(class *FontsetClass),
		constructor func() InstanceT,
		overrides FontsetOverrides[InstanceT],
		signals map[string]gobject.SignalDefinition,
		interfaceInits ...gobject.SubClassInterfaceInit[InstanceT],
) gobject.Type {
	return gobject.UnsafeRegisterSubClass(
		name,
		classInit,
		constructor,
		overrides,
		signals,
		TypeFontset,
		UnsafeFontsetClassFromGlibBorrow,
		UnsafeApplyFontsetOverrides,
		func (obj *gobject.ObjectInstance) gobject.Object {
			return unsafeWrapFontset(obj)
		},
		interfaceInits...,
	)
}

// FontsetSimpleInstance is the instance type used by all types extending PangoFontsetSimple. It is used internally by the bindings. Users should use the interface [FontsetSimple] instead.
type FontsetSimpleInstance struct {
	_ [0]func() // equal guard
	FontsetInstance
}

var _ FontsetSimple = (*FontsetSimpleInstance)(nil)

// FontsetSimple wraps PangoFontsetSimple
//
// `PangoFontsetSimple` is a implementation of the abstract
// `PangoFontset` base class as an array of fonts.
// 
// When creating a `PangoFontsetSimple`, you have to provide
// the array of fonts that make up the fontset.
type FontsetSimple interface {
	Fontset
	upcastToPangoFontsetSimple() *FontsetSimpleInstance

	// Append wraps pango_fontset_simple_append
	// 
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont`. 
	//
	// Adds a font to the fontset.
	// 
	// The fontset takes ownership of @font.
	Append(Font)
	// Size wraps pango_fontset_simple_size
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Returns the number of fonts in the fontset.
	Size() int32
}

func unsafeWrapFontsetSimple(base *gobject.ObjectInstance) *FontsetSimpleInstance {
	return &FontsetSimpleInstance{
		FontsetInstance: FontsetInstance{
			ObjectInstance: *base,
		},
	}
}

func marshalFontsetSimpleInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapFontsetSimple(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeFontsetSimpleFromGlibNone is used to convert raw PangoFontsetSimple pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeFontsetSimpleFromGlibNone(c unsafe.Pointer) FontsetSimple {
	return gobject.UnsafeObjectFromGlibNone(c).(FontsetSimple)
}

// UnsafeFontsetSimpleFromGlibFull is used to convert raw PangoFontsetSimple pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeFontsetSimpleFromGlibFull(c unsafe.Pointer) FontsetSimple {
	return gobject.UnsafeObjectFromGlibFull(c).(FontsetSimple)
}

// UnsafeFontsetSimpleFromGlibBorrow is used to convert raw PangoFontsetSimple pointers to go without touching any references. This is used by the bindings internally.
func UnsafeFontsetSimpleFromGlibBorrow(c unsafe.Pointer) FontsetSimple {
	return gobject.UnsafeObjectFromGlibBorrow(c).(FontsetSimple)
}

func (f *FontsetSimpleInstance) upcastToPangoFontsetSimple() *FontsetSimpleInstance {
	return f
}

// UnsafeFontsetSimpleToGlibNone is used to convert the instance to it's C value PangoFontsetSimple. This is used by the bindings internally.
func UnsafeFontsetSimpleToGlibNone(c FontsetSimple) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeFontsetSimpleToGlibFull is used to convert the instance to it's C value PangoFontsetSimple, while removeing the finalizer. This is used by the bindings internally.
func UnsafeFontsetSimpleToGlibFull(c FontsetSimple) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// NewFontsetSimple wraps pango_fontset_simple_new
// 
// The function takes the following parameters:
// 
// 	- language *Language: a `PangoLanguage` tag 
// 
// The function returns the following values:
// 
// 	- goret FontsetSimple 
//
// Creates a new `PangoFontsetSimple` for the given language.
func NewFontsetSimple(language *Language) FontsetSimple {
	var carg1 *C.PangoLanguage      // in, none, converted
	var cret  *C.PangoFontsetSimple // return, full, converted

	carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_fontset_simple_new(carg1)
	runtime.KeepAlive(language)

	var goret FontsetSimple

	goret = UnsafeFontsetSimpleFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Append wraps pango_fontset_simple_append
// 
// The function takes the following parameters:
// 
// 	- font Font: a `PangoFont`. 
//
// Adds a font to the fontset.
// 
// The fontset takes ownership of @font.
func (fontset *FontsetSimpleInstance) Append(font Font) {
	var carg0 *C.PangoFontsetSimple // in, none, converted
	var carg1 *C.PangoFont          // in, full, converted

	carg0 = (*C.PangoFontsetSimple)(UnsafeFontsetSimpleToGlibNone(fontset))
	carg1 = (*C.PangoFont)(UnsafeFontToGlibFull(font))

	C.pango_fontset_simple_append(carg0, carg1)
	runtime.KeepAlive(fontset)
	runtime.KeepAlive(font)
}

// Size wraps pango_fontset_simple_size
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Returns the number of fonts in the fontset.
func (fontset *FontsetSimpleInstance) Size() int32 {
	var carg0 *C.PangoFontsetSimple // in, none, converted
	var cret  C.int                 // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontsetSimple)(UnsafeFontsetSimpleToGlibNone(fontset))

	cret = C.pango_fontset_simple_size(carg0)
	runtime.KeepAlive(fontset)

	var goret int32

	goret = int32(cret)

	return goret
}

// LayoutInstance is the instance type used by all types extending PangoLayout. It is used internally by the bindings. Users should use the interface [Layout] instead.
type LayoutInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ Layout = (*LayoutInstance)(nil)

// Layout wraps PangoLayout
//
// A `PangoLayout` structure represents an entire paragraph of text.
// 
// While complete access to the layout capabilities of Pango is provided
// using the detailed interfaces for itemization and shaping, using
// that functionality directly involves writing a fairly large amount
// of code. `PangoLayout` provides a high-level driver for formatting
// entire paragraphs of text at once. This includes paragraph-level
// functionality such as line breaking, justification, alignment and
// ellipsization.
// 
// A `PangoLayout` is initialized with a `PangoContext`, UTF-8 string
// and set of attributes for that string. Once that is done, the set of
// formatted lines can be extracted from the object, the layout can be
// rendered, and conversion between logical character positions within
// the layout's text, and the physical position of the resulting glyphs
// can be made.
// 
// There are a number of parameters to adjust the formatting of a
// `PangoLayout`. The following image shows adjustable parameters
// (on the left) and font metrics (on the right):
// 
// &lt;picture&gt;
//   &lt;source srcset="layout-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Pango Layout Parameters" src="layout-light.png"&gt;
// &lt;/picture&gt;
// 
// The following images demonstrate the effect of alignment and
// justification on the layout of text:
// 
// | | |
// | --- | --- |
// | ![align=left](align-left.png) | ![align=left, justify](align-left-justify.png) |
// | ![align=center](align-center.png) | ![align=center, justify](align-center-justify.png) |
// | ![align=right](align-right.png) | ![align=right, justify](align-right-justify.png) |
// 
// 
// It is possible, as well, to ignore the 2-D setup,
// and simply treat the results of a `PangoLayout` as a list of lines.
type Layout interface {
	gobject.Object
	upcastToPangoLayout() *LayoutInstance

	// ContextChanged wraps pango_layout_context_changed
	//
	// Forces recomputation of any state in the `PangoLayout` that
	// might depend on the layout's context.
	// 
	// This function should be called if you make changes to the context
	// subsequent to creating the layout.
	ContextChanged()
	// Copy wraps pango_layout_copy
	// 
	// The function returns the following values:
	// 
	// 	- goret Layout 
	//
	// Creates a deep copy-by-value of the layout.
	// 
	// The attribute list, tab array, and text from the original layout
	// are all copied by value.
	Copy() Layout
	// GetAlignment wraps pango_layout_get_alignment
	// 
	// The function returns the following values:
	// 
	// 	- goret Alignment 
	//
	// Gets the alignment for the layout: how partial lines are
	// positioned within the horizontal space available.
	GetAlignment() Alignment
	// GetAttributes wraps pango_layout_get_attributes
	// 
	// The function returns the following values:
	// 
	// 	- goret *AttrList (nullable) 
	//
	// Gets the attribute list for the layout, if any.
	GetAttributes() *AttrList
	// GetAutoDir wraps pango_layout_get_auto_dir
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Gets whether to calculate the base direction for the layout
	// according to its contents.
	// 
	// See [method@Pango.Layout.set_auto_dir].
	GetAutoDir() bool
	// GetBaseline wraps pango_layout_get_baseline
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Gets the Y position of baseline of the first line in @layout.
	GetBaseline() int32
	// GetCaretPos wraps pango_layout_get_caret_pos
	// 
	// The function takes the following parameters:
	// 
	// 	- index_ int32: the byte index of the cursor 
	// 
	// The function returns the following values:
	// 
	// 	- strongPos Rectangle: location to store the strong cursor position 
	// 	- weakPos Rectangle: location to store the weak cursor position 
	//
	// Given an index within a layout, determines the positions that of the
	// strong and weak cursors if the insertion point is at that index.
	// 
	// This is a variant of [method@Pango.Layout.get_cursor_pos] that applies
	// font metric information about caret slope and offset to the positions
	// it returns.
	// 
	// &lt;picture&gt;
	//   &lt;source srcset="caret-metrics-dark.png" media="(prefers-color-scheme: dark)"&gt;
	//   &lt;img alt="Caret metrics" src="caret-metrics-light.png"&gt;
	// &lt;/picture&gt;
	GetCaretPos(int32) (Rectangle, Rectangle)
	// GetCharacterCount wraps pango_layout_get_character_count
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Returns the number of Unicode characters in the
	// the text of @layout.
	GetCharacterCount() int32
	// GetContext wraps pango_layout_get_context
	// 
	// The function returns the following values:
	// 
	// 	- goret Context 
	//
	// Retrieves the `PangoContext` used for this layout.
	GetContext() Context
	// GetCursorPos wraps pango_layout_get_cursor_pos
	// 
	// The function takes the following parameters:
	// 
	// 	- index_ int32: the byte index of the cursor 
	// 
	// The function returns the following values:
	// 
	// 	- strongPos Rectangle: location to store the strong cursor position 
	// 	- weakPos Rectangle: location to store the weak cursor position 
	//
	// Given an index within a layout, determines the positions that of the
	// strong and weak cursors if the insertion point is at that index.
	// 
	// The position of each cursor is stored as a zero-width rectangle
	// with the height of the run extents.
	// 
	// &lt;picture&gt;
	//   &lt;source srcset="cursor-positions-dark.png" media="(prefers-color-scheme: dark)"&gt;
	//   &lt;img alt="Cursor positions" src="cursor-positions-light.png"&gt;
	// &lt;/picture&gt;
	// 
	// The strong cursor location is the location where characters of the
	// directionality equal to the base direction of the layout are inserted.
	// The weak cursor location is the location where characters of the
	// directionality opposite to the base direction of the layout are inserted.
	// 
	// The following example shows text with both a strong and a weak cursor.
	// 
	// &lt;picture&gt;
	//   &lt;source srcset="split-cursor-dark.png" media="(prefers-color-scheme: dark)"&gt;
	//   &lt;img alt="Strong and weak cursors" src="split-cursor-light.png"&gt;
	// &lt;/picture&gt;
	// 
	// The strong cursor has a little arrow pointing to the right, the weak
	// cursor to the left. Typing a 'c' in this situation will insert the
	// character after the 'b', and typing another Hebrew character, like 'ג',
	// will insert it at the end.
	GetCursorPos(int32) (Rectangle, Rectangle)
	// GetDirection wraps pango_layout_get_direction
	// 
	// The function takes the following parameters:
	// 
	// 	- index int32: the byte index of the char 
	// 
	// The function returns the following values:
	// 
	// 	- goret Direction 
	//
	// Gets the text direction at the given character position in @layout.
	GetDirection(int32) Direction
	// GetEllipsize wraps pango_layout_get_ellipsize
	// 
	// The function returns the following values:
	// 
	// 	- goret EllipsizeMode 
	//
	// Gets the type of ellipsization being performed for @layout.
	// 
	// See [method@Pango.Layout.set_ellipsize].
	// 
	// Use [method@Pango.Layout.is_ellipsized] to query whether any
	// paragraphs were actually ellipsized.
	GetEllipsize() EllipsizeMode
	// GetExtents wraps pango_layout_get_extents
	// 
	// The function returns the following values:
	// 
	// 	- inkRect Rectangle: rectangle used to store the extents of the
	//   layout as drawn 
	// 	- logicalRect Rectangle: rectangle used to store the logical
	//   extents of the layout 
	//
	// Computes the logical and ink extents of @layout.
	// 
	// Logical extents are usually what you want for positioning things. Note
	// that both extents may have non-zero x and y. You may want to use those
	// to offset where you render the layout. Not doing that is a very typical
	// bug that shows up as right-to-left layouts not being correctly positioned
	// in a layout with a set width.
	// 
	// The extents are given in layout coordinates and in Pango units; layout
	// coordinates begin at the top left corner of the layout.
	GetExtents() (Rectangle, Rectangle)
	// GetFontDescription wraps pango_layout_get_font_description
	// 
	// The function returns the following values:
	// 
	// 	- goret *FontDescription (nullable) 
	//
	// Gets the font description for the layout, if any.
	GetFontDescription() *FontDescription
	// GetHeight wraps pango_layout_get_height
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Gets the height of layout used for ellipsization.
	// 
	// See [method@Pango.Layout.set_height] for details.
	GetHeight() int32
	// GetIndent wraps pango_layout_get_indent
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Gets the paragraph indent width in Pango units.
	// 
	// A negative value indicates a hanging indentation.
	GetIndent() int32
	// GetIter wraps pango_layout_get_iter
	// 
	// The function returns the following values:
	// 
	// 	- goret *LayoutIter 
	//
	// Returns an iterator to iterate over the visual extents of the layout.
	GetIter() *LayoutIter
	// GetJustify wraps pango_layout_get_justify
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Gets whether each complete line should be stretched to fill the entire
	// width of the layout.
	GetJustify() bool
	// GetJustifyLastLine wraps pango_layout_get_justify_last_line
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Gets whether the last line should be stretched
	// to fill the entire width of the layout.
	GetJustifyLastLine() bool
	// GetLine wraps pango_layout_get_line
	// 
	// The function takes the following parameters:
	// 
	// 	- line int32: the index of a line, which must be between 0 and
	//   `pango_layout_get_line_count(layout) - 1`, inclusive. 
	// 
	// The function returns the following values:
	// 
	// 	- goret *LayoutLine (nullable) 
	//
	// Retrieves a particular line from a `PangoLayout`.
	// 
	// Use the faster [method@Pango.Layout.get_line_readonly] if you do not
	// plan to modify the contents of the line (glyphs, glyph widths, etc.).
	GetLine(int32) *LayoutLine
	// GetLineCount wraps pango_layout_get_line_count
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Retrieves the count of lines for the @layout.
	GetLineCount() int32
	// GetLineReadonly wraps pango_layout_get_line_readonly
	// 
	// The function takes the following parameters:
	// 
	// 	- line int32: the index of a line, which must be between 0 and
	//   `pango_layout_get_line_count(layout) - 1`, inclusive. 
	// 
	// The function returns the following values:
	// 
	// 	- goret *LayoutLine (nullable) 
	//
	// Retrieves a particular line from a `PangoLayout`.
	// 
	// This is a faster alternative to [method@Pango.Layout.get_line],
	// but the user is not expected to modify the contents of the line
	// (glyphs, glyph widths, etc.).
	GetLineReadonly(int32) *LayoutLine
	// GetLineSpacing wraps pango_layout_get_line_spacing
	// 
	// The function returns the following values:
	// 
	// 	- goret float32 
	//
	// Gets the line spacing factor of @layout.
	// 
	// See [method@Pango.Layout.set_line_spacing].
	GetLineSpacing() float32
	// GetLines wraps pango_layout_get_lines
	// 
	// The function returns the following values:
	// 
	// 	- goret []*LayoutLine 
	//
	// Returns the lines of the @layout as a list.
	// 
	// Use the faster [method@Pango.Layout.get_lines_readonly] if you do not
	// plan to modify the contents of the lines (glyphs, glyph widths, etc.).
	GetLines() []*LayoutLine
	// GetLinesReadonly wraps pango_layout_get_lines_readonly
	// 
	// The function returns the following values:
	// 
	// 	- goret []*LayoutLine 
	//
	// Returns the lines of the @layout as a list.
	// 
	// This is a faster alternative to [method@Pango.Layout.get_lines],
	// but the user is not expected to modify the contents of the lines
	// (glyphs, glyph widths, etc.).
	GetLinesReadonly() []*LayoutLine
	// GetLogAttrsReadonly wraps pango_layout_get_log_attrs_readonly
	// 
	// The function returns the following values:
	// 
	// 	- nAttrs int32: location to store the number of the attributes in
	//   the array 
	// 	- goret []LogAttr 
	//
	// Retrieves an array of logical attributes for each character in
	// the @layout.
	// 
	// This is a faster alternative to [method@Pango.Layout.get_log_attrs].
	// The returned array is part of @layout and must not be modified.
	// Modifying the layout will invalidate the returned array.
	// 
	// The number of attributes returned in @n_attrs will be one more
	// than the total number of characters in the layout, since there
	// need to be attributes corresponding to both the position before
	// the first character and the position after the last character.
	GetLogAttrsReadonly() (int32, []LogAttr)
	// GetPixelExtents wraps pango_layout_get_pixel_extents
	// 
	// The function returns the following values:
	// 
	// 	- inkRect Rectangle: rectangle used to store the extents of the
	//   layout as drawn 
	// 	- logicalRect Rectangle: rectangle used to store the logical
	//   extents of the layout 
	//
	// Computes the logical and ink extents of @layout in device units.
	// 
	// This function just calls [method@Pango.Layout.get_extents] followed by
	// two [func@extents_to_pixels] calls, rounding @ink_rect and @logical_rect
	// such that the rounded rectangles fully contain the unrounded one (that is,
	// passes them as first argument to [func@Pango.extents_to_pixels]).
	GetPixelExtents() (Rectangle, Rectangle)
	// GetPixelSize wraps pango_layout_get_pixel_size
	// 
	// The function returns the following values:
	// 
	// 	- width int32: location to store the logical width 
	// 	- height int32: location to store the logical height 
	//
	// Determines the logical width and height of a `PangoLayout` in device
	// units.
	// 
	// [method@Pango.Layout.get_size] returns the width and height
	// scaled by %PANGO_SCALE. This is simply a convenience function
	// around [method@Pango.Layout.get_pixel_extents].
	GetPixelSize() (int32, int32)
	// GetSerial wraps pango_layout_get_serial
	// 
	// The function returns the following values:
	// 
	// 	- goret uint 
	//
	// Returns the current serial number of @layout.
	// 
	// The serial number is initialized to an small number larger than zero
	// when a new layout is created and is increased whenever the layout is
	// changed using any of the setter functions, or the `PangoContext` it
	// uses has changed. The serial may wrap, but will never have the value 0.
	// Since it can wrap, never compare it with "less than", always use "not equals".
	// 
	// This can be used to automatically detect changes to a `PangoLayout`,
	// and is useful for example to decide whether a layout needs redrawing.
	// To force the serial to be increased, use
	// [method@Pango.Layout.context_changed].
	GetSerial() uint
	// GetSingleParagraphMode wraps pango_layout_get_single_paragraph_mode
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Obtains whether @layout is in single paragraph mode.
	// 
	// See [method@Pango.Layout.set_single_paragraph_mode].
	GetSingleParagraphMode() bool
	// GetSize wraps pango_layout_get_size
	// 
	// The function returns the following values:
	// 
	// 	- width int32: location to store the logical width 
	// 	- height int32: location to store the logical height 
	//
	// Determines the logical width and height of a `PangoLayout` in Pango
	// units.
	// 
	// This is simply a convenience function around [method@Pango.Layout.get_extents].
	GetSize() (int32, int32)
	// GetSpacing wraps pango_layout_get_spacing
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Gets the amount of spacing between the lines of the layout.
	GetSpacing() int32
	// GetTabs wraps pango_layout_get_tabs
	// 
	// The function returns the following values:
	// 
	// 	- goret *TabArray (nullable) 
	//
	// Gets the current `PangoTabArray` used by this layout.
	// 
	// If no `PangoTabArray` has been set, then the default tabs are
	// in use and %NULL is returned. Default tabs are every 8 spaces.
	// 
	// The return value should be freed with [method@Pango.TabArray.free].
	GetTabs() *TabArray
	// GetText wraps pango_layout_get_text
	// 
	// The function returns the following values:
	// 
	// 	- goret string 
	//
	// Gets the text in the layout.
	// 
	// The returned text should not be freed or modified.
	GetText() string
	// GetUnknownGlyphsCount wraps pango_layout_get_unknown_glyphs_count
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Counts the number of unknown glyphs in @layout.
	// 
	// This function can be used to determine if there are any fonts
	// available to render all characters in a certain string, or when
	// used in combination with %PANGO_ATTR_FALLBACK, to check if a
	// certain font supports all the characters in the string.
	GetUnknownGlyphsCount() int32
	// GetWidth wraps pango_layout_get_width
	// 
	// The function returns the following values:
	// 
	// 	- goret int32 
	//
	// Gets the width to which the lines of the `PangoLayout` should wrap.
	GetWidth() int32
	// GetWrap wraps pango_layout_get_wrap
	// 
	// The function returns the following values:
	// 
	// 	- goret WrapMode 
	//
	// Gets the wrap mode for the layout.
	// 
	// Use [method@Pango.Layout.is_wrapped] to query whether
	// any paragraphs were actually wrapped.
	GetWrap() WrapMode
	// IndexToLineX wraps pango_layout_index_to_line_x
	// 
	// The function takes the following parameters:
	// 
	// 	- index_ int32: the byte index of a grapheme within the layout 
	// 	- trailing bool: an integer indicating the edge of the grapheme to retrieve the
	//   position of. If &gt; 0, the trailing edge of the grapheme, if 0,
	//   the leading of the grapheme 
	// 
	// The function returns the following values:
	// 
	// 	- line int32: location to store resulting line index. (which will
	//   between 0 and pango_layout_get_line_count(layout) - 1) 
	// 	- xPos int32: location to store resulting position within line
	//   (%PANGO_SCALE units per device unit) 
	//
	// Converts from byte @index_ within the @layout to line and X position.
	// 
	// The X position is measured from the left edge of the line.
	IndexToLineX(int32, bool) (int32, int32)
	// IndexToPos wraps pango_layout_index_to_pos
	// 
	// The function takes the following parameters:
	// 
	// 	- index_ int32: byte index within @layout 
	// 
	// The function returns the following values:
	// 
	// 	- pos Rectangle: rectangle in which to store the position of the grapheme 
	//
	// Converts from an index within a `PangoLayout` to the onscreen position
	// corresponding to the grapheme at that index.
	// 
	// The returns is represented as rectangle. Note that `pos-&gt;x` is
	// always the leading edge of the grapheme and `pos-&gt;x + pos-&gt;width` the
	// trailing edge of the grapheme. If the directionality of the grapheme
	// is right-to-left, then `pos-&gt;width` will be negative.
	IndexToPos(int32) Rectangle
	// IsEllipsized wraps pango_layout_is_ellipsized
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Queries whether the layout had to ellipsize any paragraphs.
	// 
	// This returns %TRUE if the ellipsization mode for @layout
	// is not %PANGO_ELLIPSIZE_NONE, a positive width is set on @layout,
	// and there are paragraphs exceeding that width that have to be
	// ellipsized.
	IsEllipsized() bool
	// IsWrapped wraps pango_layout_is_wrapped
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	//
	// Queries whether the layout had to wrap any paragraphs.
	// 
	// This returns %TRUE if a positive width is set on @layout,
	// and there are paragraphs exceeding the layout width that have
	// to be wrapped.
	IsWrapped() bool
	// MoveCursorVisually wraps pango_layout_move_cursor_visually
	// 
	// The function takes the following parameters:
	// 
	// 	- strong bool: whether the moving cursor is the strong cursor or the
	//   weak cursor. The strong cursor is the cursor corresponding
	//   to text insertion in the base direction for the layout. 
	// 	- oldIndex int32: the byte index of the current cursor position 
	// 	- oldTrailing int32: if 0, the cursor was at the leading edge of the
	//   grapheme indicated by @old_index, if &gt; 0, the cursor
	//   was at the trailing edge. 
	// 	- direction int32: direction to move cursor. A negative
	//   value indicates motion to the left 
	// 
	// The function returns the following values:
	// 
	// 	- newIndex int32: location to store the new cursor byte index.
	//   A value of -1 indicates that the cursor has been moved off the
	//   beginning of the layout. A value of %G_MAXINT indicates that
	//   the cursor has been moved off the end of the layout. 
	// 	- newTrailing int32: number of characters to move forward from
	//   the location returned for @new_index to get the position where
	//   the cursor should be displayed. This allows distinguishing the
	//   position at the beginning of one line from the position at the
	//   end of the preceding line. @new_index is always on the line where
	//   the cursor should be displayed. 
	//
	// Computes a new cursor position from an old position and a direction.
	// 
	// If @direction is positive, then the new position will cause the strong
	// or weak cursor to be displayed one position to right of where it was
	// with the old cursor position. If @direction is negative, it will be
	// moved to the left.
	// 
	// In the presence of bidirectional text, the correspondence between
	// logical and visual order will depend on the direction of the current
	// run, and there may be jumps when the cursor is moved off of the end
	// of a run.
	// 
	// Motion here is in cursor positions, not in characters, so a single
	// call to this function may move the cursor over multiple characters
	// when multiple characters combine to form a single grapheme.
	MoveCursorVisually(bool, int32, int32, int32) (int32, int32)
	// Serialize wraps pango_layout_serialize
	// 
	// The function takes the following parameters:
	// 
	// 	- flags LayoutSerializeFlags: `PangoLayoutSerializeFlags` 
	// 
	// The function returns the following values:
	// 
	// 	- goret *glib.Bytes 
	//
	// Serializes the @layout for later deserialization via [func@Pango.Layout.deserialize].
	// 
	// There are no guarantees about the format of the output across different
	// versions of Pango and [func@Pango.Layout.deserialize] will reject data
	// that it cannot parse.
	// 
	// The intended use of this function is testing, benchmarking and debugging.
	// The format is not meant as a permanent storage format.
	Serialize(LayoutSerializeFlags) *glib.Bytes
	// SetAlignment wraps pango_layout_set_alignment
	// 
	// The function takes the following parameters:
	// 
	// 	- alignment Alignment: the alignment 
	//
	// Sets the alignment for the layout: how partial lines are
	// positioned within the horizontal space available.
	// 
	// The default alignment is %PANGO_ALIGN_LEFT.
	SetAlignment(Alignment)
	// SetAttributes wraps pango_layout_set_attributes
	// 
	// The function takes the following parameters:
	// 
	// 	- attrs *AttrList (nullable): a `PangoAttrList` 
	//
	// Sets the text attributes for a layout object.
	// 
	// References @attrs, so the caller can unref its reference.
	SetAttributes(*AttrList)
	// SetAutoDir wraps pango_layout_set_auto_dir
	// 
	// The function takes the following parameters:
	// 
	// 	- autoDir bool: if %TRUE, compute the bidirectional base direction
	//   from the layout's contents 
	//
	// Sets whether to calculate the base direction
	// for the layout according to its contents.
	// 
	// When this flag is on (the default), then paragraphs in @layout that
	// begin with strong right-to-left characters (Arabic and Hebrew principally),
	// will have right-to-left layout, paragraphs with letters from other scripts
	// will have left-to-right layout. Paragraphs with only neutral characters
	// get their direction from the surrounding paragraphs.
	// 
	// When %FALSE, the choice between left-to-right and right-to-left
	// layout is done according to the base direction of the layout's
	// `PangoContext`. (See [method@Pango.Context.set_base_dir]).
	// 
	// When the auto-computed direction of a paragraph differs from the
	// base direction of the context, the interpretation of
	// %PANGO_ALIGN_LEFT and %PANGO_ALIGN_RIGHT are swapped.
	SetAutoDir(bool)
	// SetEllipsize wraps pango_layout_set_ellipsize
	// 
	// The function takes the following parameters:
	// 
	// 	- ellipsize EllipsizeMode: the new ellipsization mode for @layout 
	//
	// Sets the type of ellipsization being performed for @layout.
	// 
	// Depending on the ellipsization mode @ellipsize text is
	// removed from the start, middle, or end of text so they
	// fit within the width and height of layout set with
	// [method@Pango.Layout.set_width] and [method@Pango.Layout.set_height].
	// 
	// If the layout contains characters such as newlines that
	// force it to be layed out in multiple paragraphs, then whether
	// each paragraph is ellipsized separately or the entire layout
	// is ellipsized as a whole depends on the set height of the layout.
	// 
	// The default value is %PANGO_ELLIPSIZE_NONE.
	// 
	// See [method@Pango.Layout.set_height] for details.
	SetEllipsize(EllipsizeMode)
	// SetFontDescription wraps pango_layout_set_font_description
	// 
	// The function takes the following parameters:
	// 
	// 	- desc *FontDescription (nullable): the new `PangoFontDescription`
	//   to unset the current font description 
	//
	// Sets the default font description for the layout.
	// 
	// If no font description is set on the layout, the
	// font description from the layout's context is used.
	SetFontDescription(*FontDescription)
	// SetHeight wraps pango_layout_set_height
	// 
	// The function takes the following parameters:
	// 
	// 	- height int32: the desired height of the layout in Pango units if positive,
	//   or desired number of lines if negative. 
	//
	// Sets the height to which the `PangoLayout` should be ellipsized at.
	// 
	// There are two different behaviors, based on whether @height is positive
	// or negative.
	// 
	// If @height is positive, it will be the maximum height of the layout. Only
	// lines would be shown that would fit, and if there is any text omitted,
	// an ellipsis added. At least one line is included in each paragraph regardless
	// of how small the height value is. A value of zero will render exactly one
	// line for the entire layout.
	// 
	// If @height is negative, it will be the (negative of) maximum number of lines
	// per paragraph. That is, the total number of lines shown may well be more than
	// this value if the layout contains multiple paragraphs of text.
	// The default value of -1 means that the first line of each paragraph is ellipsized.
	// This behavior may be changed in the future to act per layout instead of per
	// paragraph. File a bug against pango at
	// [https://gitlab.gnome.org/gnome/pango](https://gitlab.gnome.org/gnome/pango)
	// if your code relies on this behavior.
	// 
	// Height setting only has effect if a positive width is set on
	// @layout and ellipsization mode of @layout is not %PANGO_ELLIPSIZE_NONE.
	// The behavior is undefined if a height other than -1 is set and
	// ellipsization mode is set to %PANGO_ELLIPSIZE_NONE, and may change in the
	// future.
	SetHeight(int32)
	// SetIndent wraps pango_layout_set_indent
	// 
	// The function takes the following parameters:
	// 
	// 	- indent int32: the amount by which to indent 
	//
	// Sets the width in Pango units to indent each paragraph.
	// 
	// A negative value of @indent will produce a hanging indentation.
	// That is, the first line will have the full width, and subsequent
	// lines will be indented by the absolute value of @indent.
	// 
	// The indent setting is ignored if layout alignment is set to
	// %PANGO_ALIGN_CENTER.
	// 
	// The default value is 0.
	SetIndent(int32)
	// SetJustify wraps pango_layout_set_justify
	// 
	// The function takes the following parameters:
	// 
	// 	- justify bool: whether the lines in the layout should be justified 
	//
	// Sets whether each complete line should be stretched to fill the
	// entire width of the layout.
	// 
	// Stretching is typically done by adding whitespace, but for some scripts
	// (such as Arabic), the justification may be done in more complex ways,
	// like extending the characters.
	// 
	// Note that this setting is not implemented and so is ignored in
	// Pango older than 1.18.
	// 
	// Note that tabs and justification conflict with each other:
	// Justification will move content away from its tab-aligned
	// positions.
	// 
	// The default value is %FALSE.
	// 
	// Also see [method@Pango.Layout.set_justify_last_line].
	SetJustify(bool)
	// SetJustifyLastLine wraps pango_layout_set_justify_last_line
	// 
	// The function takes the following parameters:
	// 
	// 	- justify bool: whether the last line in the layout should be justified 
	//
	// Sets whether the last line should be stretched to fill the
	// entire width of the layout.
	// 
	// This only has an effect if [method@Pango.Layout.set_justify] has
	// been called as well.
	// 
	// The default value is %FALSE.
	SetJustifyLastLine(bool)
	// SetLineSpacing wraps pango_layout_set_line_spacing
	// 
	// The function takes the following parameters:
	// 
	// 	- factor float32: the new line spacing factor 
	//
	// Sets a factor for line spacing.
	// 
	// Typical values are: 0, 1, 1.5, 2. The default values is 0.
	// 
	// If @factor is non-zero, lines are placed so that
	// 
	//     baseline2 = baseline1 + factor * height2
	// 
	// where height2 is the line height of the second line
	// (as determined by the font(s)). In this case, the spacing
	// set with [method@Pango.Layout.set_spacing] is ignored.
	// 
	// If @factor is zero (the default), spacing is applied as before.
	// 
	// Note: for semantics that are closer to the CSS line-height
	// property, see [func@Pango.attr_line_height_new].
	SetLineSpacing(float32)
	// SetMarkup wraps pango_layout_set_markup
	// 
	// The function takes the following parameters:
	// 
	// 	- markup string: marked-up text 
	// 	- length int32: length of marked-up text in bytes, or -1 if @markup is
	//   `NUL`-terminated 
	//
	// Sets the layout text and attribute list from marked-up text.
	// 
	// See [Pango Markup](pango_markup.html)).
	// 
	// Replaces the current text and attribute list.
	// 
	// This is the same as [method@Pango.Layout.set_markup_with_accel],
	// but the markup text isn't scanned for accelerators.
	SetMarkup(string, int32)
	// SetMarkupWithAccel wraps pango_layout_set_markup_with_accel
	// 
	// The function takes the following parameters:
	// 
	// 	- markup string: marked-up text (see [Pango Markup](pango_markup.html)) 
	// 	- length int32: length of marked-up text in bytes, or -1 if @markup is
	//   `NUL`-terminated 
	// 	- accelMarker uint32: marker for accelerators in the text 
	// 
	// The function returns the following values:
	// 
	// 	- accelChar uint32: return location
	//   for first located accelerator 
	//
	// Sets the layout text and attribute list from marked-up text.
	// 
	// See [Pango Markup](pango_markup.html)).
	// 
	// Replaces the current text and attribute list.
	// 
	// If @accel_marker is nonzero, the given character will mark the
	// character following it as an accelerator. For example, @accel_marker
	// might be an ampersand or underscore. All characters marked
	// as an accelerator will receive a %PANGO_UNDERLINE_LOW attribute,
	// and the first character so marked will be returned in @accel_char.
	// Two @accel_marker characters following each other produce a single
	// literal @accel_marker character.
	SetMarkupWithAccel(string, int32, uint32) uint32
	// SetSingleParagraphMode wraps pango_layout_set_single_paragraph_mode
	// 
	// The function takes the following parameters:
	// 
	// 	- setting bool: new setting 
	//
	// Sets the single paragraph mode of @layout.
	// 
	// If @setting is %TRUE, do not treat newlines and similar characters
	// as paragraph separators; instead, keep all text in a single paragraph,
	// and display a glyph for paragraph separator characters. Used when
	// you want to allow editing of newlines on a single text line.
	// 
	// The default value is %FALSE.
	SetSingleParagraphMode(bool)
	// SetSpacing wraps pango_layout_set_spacing
	// 
	// The function takes the following parameters:
	// 
	// 	- spacing int32: the amount of spacing 
	//
	// Sets the amount of spacing in Pango units between
	// the lines of the layout.
	// 
	// When placing lines with spacing, Pango arranges things so that
	// 
	//     line2.top = line1.bottom + spacing
	// 
	// The default value is 0.
	// 
	// Note: Since 1.44, Pango is using the line height (as determined
	// by the font) for placing lines when the line spacing factor is set
	// to a non-zero value with [method@Pango.Layout.set_line_spacing].
	// In that case, the @spacing set with this function is ignored.
	// 
	// Note: for semantics that are closer to the CSS line-height
	// property, see [func@Pango.attr_line_height_new].
	SetSpacing(int32)
	// SetTabs wraps pango_layout_set_tabs
	// 
	// The function takes the following parameters:
	// 
	// 	- tabs *TabArray (nullable): a `PangoTabArray` 
	//
	// Sets the tabs to use for @layout, overriding the default tabs.
	// 
	// `PangoLayout` will place content at the next tab position
	// whenever it meets a Tab character (U+0009).
	// 
	// By default, tabs are every 8 spaces. If @tabs is %NULL, the
	// default tabs are reinstated. @tabs is copied into the layout;
	// you must free your copy of @tabs yourself.
	// 
	// Note that tabs and justification conflict with each other:
	// Justification will move content away from its tab-aligned
	// positions. The same is true for alignments other than
	// %PANGO_ALIGN_LEFT.
	SetTabs(*TabArray)
	// SetText wraps pango_layout_set_text
	// 
	// The function takes the following parameters:
	// 
	// 	- text string: the text 
	// 	- length int32: maximum length of @text, in bytes. -1 indicates that
	//   the string is nul-terminated and the length should be calculated.
	//   The text will also be truncated on encountering a nul-termination
	//   even when @length is positive. 
	//
	// Sets the text of the layout.
	// 
	// This function validates @text and renders invalid UTF-8
	// with a placeholder glyph.
	// 
	// Note that if you have used [method@Pango.Layout.set_markup] or
	// [method@Pango.Layout.set_markup_with_accel] on @layout before, you
	// may want to call [method@Pango.Layout.set_attributes] to clear the
	// attributes set on the layout from the markup as this function does
	// not clear attributes.
	SetText(string, int32)
	// SetWidth wraps pango_layout_set_width
	// 
	// The function takes the following parameters:
	// 
	// 	- width int32: the desired width in Pango units, or -1 to indicate that no
	//   wrapping or ellipsization should be performed. 
	//
	// Sets the width to which the lines of the `PangoLayout` should wrap or
	// get ellipsized.
	// 
	// The default value is -1: no width set.
	SetWidth(int32)
	// SetWrap wraps pango_layout_set_wrap
	// 
	// The function takes the following parameters:
	// 
	// 	- wrap WrapMode: the wrap mode 
	//
	// Sets the wrap mode.
	// 
	// The wrap mode only has effect if a width is set on the layout
	// with [method@Pango.Layout.set_width]. To turn off wrapping,
	// set the width to -1.
	// 
	// The default value is %PANGO_WRAP_WORD.
	SetWrap(WrapMode)
	// WriteToFile wraps pango_layout_write_to_file
	// 
	// The function takes the following parameters:
	// 
	// 	- flags LayoutSerializeFlags: `PangoLayoutSerializeFlags` 
	// 	- filename string: the file to save it to 
	// 
	// The function returns the following values:
	// 
	// 	- goret bool 
	// 	- _goerr error (nullable): an error 
	//
	// A convenience method to serialize a layout to a file.
	// 
	// It is equivalent to calling [method@Pango.Layout.serialize]
	// followed by [func@GLib.file_set_contents].
	// 
	// See those two functions for details on the arguments.
	// 
	// It is mostly intended for use inside a debugger to quickly dump
	// a layout to a file for later inspection.
	WriteToFile(LayoutSerializeFlags, string) (bool, error)
	// XYToIndex wraps pango_layout_xy_to_index
	// 
	// The function takes the following parameters:
	// 
	// 	- x int32: the X offset (in Pango units) from the left edge of the layout 
	// 	- y int32: the Y offset (in Pango units) from the top edge of the layout 
	// 
	// The function returns the following values:
	// 
	// 	- index_ int32: location to store calculated byte index 
	// 	- trailing int32: location to store a integer indicating where
	//   in the grapheme the user clicked. It will either be zero, or the
	//   number of characters in the grapheme. 0 represents the leading edge
	//   of the grapheme. 
	// 	- goret bool 
	//
	// Converts from X and Y position within a layout to the byte index to the
	// character at that logical position.
	// 
	// If the Y position is not inside the layout, the closest position is
	// chosen (the position will be clamped inside the layout). If the X position
	// is not within the layout, then the start or the end of the line is
	// chosen as described for [method@Pango.LayoutLine.x_to_index]. If either
	// the X or Y positions were not inside the layout, then the function returns
	// %FALSE; on an exact hit, it returns %TRUE.
	XYToIndex(int32, int32) (int32, int32, bool)
}

func unsafeWrapLayout(base *gobject.ObjectInstance) *LayoutInstance {
	return &LayoutInstance{
		ObjectInstance: *base,
	}
}

func marshalLayoutInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapLayout(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeLayoutFromGlibNone is used to convert raw PangoLayout pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeLayoutFromGlibNone(c unsafe.Pointer) Layout {
	return gobject.UnsafeObjectFromGlibNone(c).(Layout)
}

// UnsafeLayoutFromGlibFull is used to convert raw PangoLayout pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeLayoutFromGlibFull(c unsafe.Pointer) Layout {
	return gobject.UnsafeObjectFromGlibFull(c).(Layout)
}

// UnsafeLayoutFromGlibBorrow is used to convert raw PangoLayout pointers to go without touching any references. This is used by the bindings internally.
func UnsafeLayoutFromGlibBorrow(c unsafe.Pointer) Layout {
	return gobject.UnsafeObjectFromGlibBorrow(c).(Layout)
}

func (l *LayoutInstance) upcastToPangoLayout() *LayoutInstance {
	return l
}

// UnsafeLayoutToGlibNone is used to convert the instance to it's C value PangoLayout. This is used by the bindings internally.
func UnsafeLayoutToGlibNone(c Layout) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeLayoutToGlibFull is used to convert the instance to it's C value PangoLayout, while removeing the finalizer. This is used by the bindings internally.
func UnsafeLayoutToGlibFull(c Layout) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// NewLayout wraps pango_layout_new
// 
// The function takes the following parameters:
// 
// 	- _context Context: a `PangoContext` 
// 
// The function returns the following values:
// 
// 	- goret Layout 
//
// Create a new `PangoLayout` object with attributes initialized to
// default values for a particular `PangoContext`.
func NewLayout(_context Context) Layout {
	var carg1 *C.PangoContext // in, none, converted
	var cret  *C.PangoLayout  // return, full, converted

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))

	cret = C.pango_layout_new(carg1)
	runtime.KeepAlive(_context)

	var goret Layout

	goret = UnsafeLayoutFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// LayoutDeserialize wraps pango_layout_deserialize
// 
// The function takes the following parameters:
// 
// 	- _context Context: a `PangoContext` 
// 	- bytes *glib.Bytes: the bytes containing the data 
// 	- flags LayoutDeserializeFlags: `PangoLayoutDeserializeFlags` 
// 
// The function returns the following values:
// 
// 	- goret Layout (nullable) 
// 	- _goerr error (nullable): an error 
//
// Loads data previously created via [method@Pango.Layout.serialize].
// 
// For a discussion of the supported format, see that function.
// 
// Note: to verify that the returned layout is identical to
// the one that was serialized, you can compare @bytes to the
// result of serializing the layout again.
func LayoutDeserialize(_context Context, bytes *glib.Bytes, flags LayoutDeserializeFlags) (Layout, error) {
	var carg1 *C.PangoContext               // in, none, converted
	var carg2 *C.GBytes                     // in, none, converted
	var carg3 C.PangoLayoutDeserializeFlags // in, none, casted
	var cret  *C.PangoLayout                // return, full, converted, nullable
	var _cerr *C.GError                     // out, full, converted, nullable

	carg1 = (*C.PangoContext)(UnsafeContextToGlibNone(_context))
	carg2 = (*C.GBytes)(glib.UnsafeBytesToGlibNone(bytes))
	carg3 = C.PangoLayoutDeserializeFlags(flags)

	cret = C.pango_layout_deserialize(carg1, carg2, carg3, &_cerr)
	runtime.KeepAlive(_context)
	runtime.KeepAlive(bytes)
	runtime.KeepAlive(flags)

	var goret  Layout
	var _goerr error

	if cret != nil {
		goret = UnsafeLayoutFromGlibFull(unsafe.Pointer(cret))
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return goret, _goerr
}

// ContextChanged wraps pango_layout_context_changed
//
// Forces recomputation of any state in the `PangoLayout` that
// might depend on the layout's context.
// 
// This function should be called if you make changes to the context
// subsequent to creating the layout.
func (layout *LayoutInstance) ContextChanged() {
	var carg0 *C.PangoLayout // in, none, converted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	C.pango_layout_context_changed(carg0)
	runtime.KeepAlive(layout)
}

// Copy wraps pango_layout_copy
// 
// The function returns the following values:
// 
// 	- goret Layout 
//
// Creates a deep copy-by-value of the layout.
// 
// The attribute list, tab array, and text from the original layout
// are all copied by value.
func (src *LayoutInstance) Copy() Layout {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  *C.PangoLayout // return, full, converted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(src))

	cret = C.pango_layout_copy(carg0)
	runtime.KeepAlive(src)

	var goret Layout

	goret = UnsafeLayoutFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetAlignment wraps pango_layout_get_alignment
// 
// The function returns the following values:
// 
// 	- goret Alignment 
//
// Gets the alignment for the layout: how partial lines are
// positioned within the horizontal space available.
func (layout *LayoutInstance) GetAlignment() Alignment {
	var carg0 *C.PangoLayout   // in, none, converted
	var cret  C.PangoAlignment // return, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_alignment(carg0)
	runtime.KeepAlive(layout)

	var goret Alignment

	goret = Alignment(cret)

	return goret
}

// GetAttributes wraps pango_layout_get_attributes
// 
// The function returns the following values:
// 
// 	- goret *AttrList (nullable) 
//
// Gets the attribute list for the layout, if any.
func (layout *LayoutInstance) GetAttributes() *AttrList {
	var carg0 *C.PangoLayout   // in, none, converted
	var cret  *C.PangoAttrList // return, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_attributes(carg0)
	runtime.KeepAlive(layout)

	var goret *AttrList

	if cret != nil {
		goret = UnsafeAttrListFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetAutoDir wraps pango_layout_get_auto_dir
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Gets whether to calculate the base direction for the layout
// according to its contents.
// 
// See [method@Pango.Layout.set_auto_dir].
func (layout *LayoutInstance) GetAutoDir() bool {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_auto_dir(carg0)
	runtime.KeepAlive(layout)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetBaseline wraps pango_layout_get_baseline
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the Y position of baseline of the first line in @layout.
func (layout *LayoutInstance) GetBaseline() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_baseline(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetCaretPos wraps pango_layout_get_caret_pos
// 
// The function takes the following parameters:
// 
// 	- index_ int32: the byte index of the cursor 
// 
// The function returns the following values:
// 
// 	- strongPos Rectangle: location to store the strong cursor position 
// 	- weakPos Rectangle: location to store the weak cursor position 
//
// Given an index within a layout, determines the positions that of the
// strong and weak cursors if the insertion point is at that index.
// 
// This is a variant of [method@Pango.Layout.get_cursor_pos] that applies
// font metric information about caret slope and offset to the positions
// it returns.
// 
// &lt;picture&gt;
//   &lt;source srcset="caret-metrics-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Caret metrics" src="caret-metrics-light.png"&gt;
// &lt;/picture&gt;
func (layout *LayoutInstance) GetCaretPos(index_ int32) (Rectangle, Rectangle) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg3 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(index_)

	C.pango_layout_get_caret_pos(carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(index_)

	var strongPos Rectangle
	var weakPos   Rectangle

	_ = strongPos
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = weakPos
	_ = carg3
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return strongPos, weakPos
}

// GetCharacterCount wraps pango_layout_get_character_count
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Returns the number of Unicode characters in the
// the text of @layout.
func (layout *LayoutInstance) GetCharacterCount() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gint         // return, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_character_count(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetContext wraps pango_layout_get_context
// 
// The function returns the following values:
// 
// 	- goret Context 
//
// Retrieves the `PangoContext` used for this layout.
func (layout *LayoutInstance) GetContext() Context {
	var carg0 *C.PangoLayout  // in, none, converted
	var cret  *C.PangoContext // return, none, converted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_context(carg0)
	runtime.KeepAlive(layout)

	var goret Context

	goret = UnsafeContextFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// GetCursorPos wraps pango_layout_get_cursor_pos
// 
// The function takes the following parameters:
// 
// 	- index_ int32: the byte index of the cursor 
// 
// The function returns the following values:
// 
// 	- strongPos Rectangle: location to store the strong cursor position 
// 	- weakPos Rectangle: location to store the weak cursor position 
//
// Given an index within a layout, determines the positions that of the
// strong and weak cursors if the insertion point is at that index.
// 
// The position of each cursor is stored as a zero-width rectangle
// with the height of the run extents.
// 
// &lt;picture&gt;
//   &lt;source srcset="cursor-positions-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Cursor positions" src="cursor-positions-light.png"&gt;
// &lt;/picture&gt;
// 
// The strong cursor location is the location where characters of the
// directionality equal to the base direction of the layout are inserted.
// The weak cursor location is the location where characters of the
// directionality opposite to the base direction of the layout are inserted.
// 
// The following example shows text with both a strong and a weak cursor.
// 
// &lt;picture&gt;
//   &lt;source srcset="split-cursor-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Strong and weak cursors" src="split-cursor-light.png"&gt;
// &lt;/picture&gt;
// 
// The strong cursor has a little arrow pointing to the right, the weak
// cursor to the left. Typing a 'c' in this situation will insert the
// character after the 'b', and typing another Hebrew character, like 'ג',
// will insert it at the end.
func (layout *LayoutInstance) GetCursorPos(index_ int32) (Rectangle, Rectangle) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg3 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(index_)

	C.pango_layout_get_cursor_pos(carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(index_)

	var strongPos Rectangle
	var weakPos   Rectangle

	_ = strongPos
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = weakPos
	_ = carg3
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return strongPos, weakPos
}

// GetDirection wraps pango_layout_get_direction
// 
// The function takes the following parameters:
// 
// 	- index int32: the byte index of the char 
// 
// The function returns the following values:
// 
// 	- goret Direction 
//
// Gets the text direction at the given character position in @layout.
func (layout *LayoutInstance) GetDirection(index int32) Direction {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var cret  C.PangoDirection // return, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(index)

	cret = C.pango_layout_get_direction(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(index)

	var goret Direction

	goret = Direction(cret)

	return goret
}

// GetEllipsize wraps pango_layout_get_ellipsize
// 
// The function returns the following values:
// 
// 	- goret EllipsizeMode 
//
// Gets the type of ellipsization being performed for @layout.
// 
// See [method@Pango.Layout.set_ellipsize].
// 
// Use [method@Pango.Layout.is_ellipsized] to query whether any
// paragraphs were actually ellipsized.
func (layout *LayoutInstance) GetEllipsize() EllipsizeMode {
	var carg0 *C.PangoLayout       // in, none, converted
	var cret  C.PangoEllipsizeMode // return, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_ellipsize(carg0)
	runtime.KeepAlive(layout)

	var goret EllipsizeMode

	goret = EllipsizeMode(cret)

	return goret
}

// GetExtents wraps pango_layout_get_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of the
//   layout as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical
//   extents of the layout 
//
// Computes the logical and ink extents of @layout.
// 
// Logical extents are usually what you want for positioning things. Note
// that both extents may have non-zero x and y. You may want to use those
// to offset where you render the layout. Not doing that is a very typical
// bug that shows up as right-to-left layouts not being correctly positioned
// in a layout with a set width.
// 
// The extents are given in layout coordinates and in Pango units; layout
// coordinates begin at the top left corner of the layout.
func (layout *LayoutInstance) GetExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	C.pango_layout_get_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(layout)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetFontDescription wraps pango_layout_get_font_description
// 
// The function returns the following values:
// 
// 	- goret *FontDescription (nullable) 
//
// Gets the font description for the layout, if any.
func (layout *LayoutInstance) GetFontDescription() *FontDescription {
	var carg0 *C.PangoLayout          // in, none, converted
	var cret  *C.PangoFontDescription // return, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_font_description(carg0)
	runtime.KeepAlive(layout)

	var goret *FontDescription

	if cret != nil {
		goret = UnsafeFontDescriptionFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetHeight wraps pango_layout_get_height
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the height of layout used for ellipsization.
// 
// See [method@Pango.Layout.set_height] for details.
func (layout *LayoutInstance) GetHeight() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_height(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetIndent wraps pango_layout_get_indent
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the paragraph indent width in Pango units.
// 
// A negative value indicates a hanging indentation.
func (layout *LayoutInstance) GetIndent() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_indent(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetIter wraps pango_layout_get_iter
// 
// The function returns the following values:
// 
// 	- goret *LayoutIter 
//
// Returns an iterator to iterate over the visual extents of the layout.
func (layout *LayoutInstance) GetIter() *LayoutIter {
	var carg0 *C.PangoLayout     // in, none, converted
	var cret  *C.PangoLayoutIter // return, full, converted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_iter(carg0)
	runtime.KeepAlive(layout)

	var goret *LayoutIter

	goret = UnsafeLayoutIterFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetJustify wraps pango_layout_get_justify
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Gets whether each complete line should be stretched to fill the entire
// width of the layout.
func (layout *LayoutInstance) GetJustify() bool {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_justify(carg0)
	runtime.KeepAlive(layout)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetJustifyLastLine wraps pango_layout_get_justify_last_line
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Gets whether the last line should be stretched
// to fill the entire width of the layout.
func (layout *LayoutInstance) GetJustifyLastLine() bool {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_justify_last_line(carg0)
	runtime.KeepAlive(layout)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetLine wraps pango_layout_get_line
// 
// The function takes the following parameters:
// 
// 	- line int32: the index of a line, which must be between 0 and
//   `pango_layout_get_line_count(layout) - 1`, inclusive. 
// 
// The function returns the following values:
// 
// 	- goret *LayoutLine (nullable) 
//
// Retrieves a particular line from a `PangoLayout`.
// 
// Use the faster [method@Pango.Layout.get_line_readonly] if you do not
// plan to modify the contents of the line (glyphs, glyph widths, etc.).
func (layout *LayoutInstance) GetLine(line int32) *LayoutLine {
	var carg0 *C.PangoLayout     // in, none, converted
	var carg1 C.int              // in, none, casted, casted C.gint
	var cret  *C.PangoLayoutLine // return, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(line)

	cret = C.pango_layout_get_line(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(line)

	var goret *LayoutLine

	if cret != nil {
		goret = UnsafeLayoutLineFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLineCount wraps pango_layout_get_line_count
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Retrieves the count of lines for the @layout.
func (layout *LayoutInstance) GetLineCount() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_line_count(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetLineReadonly wraps pango_layout_get_line_readonly
// 
// The function takes the following parameters:
// 
// 	- line int32: the index of a line, which must be between 0 and
//   `pango_layout_get_line_count(layout) - 1`, inclusive. 
// 
// The function returns the following values:
// 
// 	- goret *LayoutLine (nullable) 
//
// Retrieves a particular line from a `PangoLayout`.
// 
// This is a faster alternative to [method@Pango.Layout.get_line],
// but the user is not expected to modify the contents of the line
// (glyphs, glyph widths, etc.).
func (layout *LayoutInstance) GetLineReadonly(line int32) *LayoutLine {
	var carg0 *C.PangoLayout     // in, none, converted
	var carg1 C.int              // in, none, casted, casted C.gint
	var cret  *C.PangoLayoutLine // return, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(line)

	cret = C.pango_layout_get_line_readonly(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(line)

	var goret *LayoutLine

	if cret != nil {
		goret = UnsafeLayoutLineFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLineSpacing wraps pango_layout_get_line_spacing
// 
// The function returns the following values:
// 
// 	- goret float32 
//
// Gets the line spacing factor of @layout.
// 
// See [method@Pango.Layout.set_line_spacing].
func (layout *LayoutInstance) GetLineSpacing() float32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.float        // return, none, casted, casted C.gfloat

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_line_spacing(carg0)
	runtime.KeepAlive(layout)

	var goret float32

	goret = float32(cret)

	return goret
}

// GetLines wraps pango_layout_get_lines
// 
// The function returns the following values:
// 
// 	- goret []*LayoutLine 
//
// Returns the lines of the @layout as a list.
// 
// Use the faster [method@Pango.Layout.get_lines_readonly] if you do not
// plan to modify the contents of the lines (glyphs, glyph widths, etc.).
func (layout *LayoutInstance) GetLines() []*LayoutLine {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  *C.GSList      // container, transfer: none

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_lines(carg0)
	runtime.KeepAlive(layout)

	var goret []*LayoutLine

	goret = glib.UnsafeSListFromGlibNone(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *LayoutLine {
			var dst *LayoutLine // converted
			dst = UnsafeLayoutLineFromGlibNone(v)
			return dst
		},
	)

	return goret
}

// GetLinesReadonly wraps pango_layout_get_lines_readonly
// 
// The function returns the following values:
// 
// 	- goret []*LayoutLine 
//
// Returns the lines of the @layout as a list.
// 
// This is a faster alternative to [method@Pango.Layout.get_lines],
// but the user is not expected to modify the contents of the lines
// (glyphs, glyph widths, etc.).
func (layout *LayoutInstance) GetLinesReadonly() []*LayoutLine {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  *C.GSList      // container, transfer: none

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_lines_readonly(carg0)
	runtime.KeepAlive(layout)

	var goret []*LayoutLine

	goret = glib.UnsafeSListFromGlibNone(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *LayoutLine {
			var dst *LayoutLine // converted
			dst = UnsafeLayoutLineFromGlibNone(v)
			return dst
		},
	)

	return goret
}

// GetLogAttrsReadonly wraps pango_layout_get_log_attrs_readonly
// 
// The function returns the following values:
// 
// 	- nAttrs int32: location to store the number of the attributes in
//   the array 
// 	- goret []LogAttr 
//
// Retrieves an array of logical attributes for each character in
// the @layout.
// 
// This is a faster alternative to [method@Pango.Layout.get_log_attrs].
// The returned array is part of @layout and must not be modified.
// Modifying the layout will invalidate the returned array.
// 
// The number of attributes returned in @n_attrs will be one more
// than the total number of characters in the layout, since there
// need to be attributes corresponding to both the position before
// the first character and the position after the last character.
func (layout *LayoutInstance) GetLogAttrsReadonly() (int32, []LogAttr) {
	var carg0 *C.PangoLayout  // in, none, converted
	var carg1 C.gint          // out, full, casted
	var cret  *C.PangoLogAttr // return, transfer: none, C Pointers: 1, Name: array[LogAttr], scope: , array (inner: *typesystem.Record)

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_log_attrs_readonly(carg0, &carg1)
	runtime.KeepAlive(layout)

	var nAttrs int32
	var goret  []LogAttr

	nAttrs = int32(carg1)
	_ = goret
	_ = cret
	panic("unimplemented conversion of []LogAttr (const PangoLogAttr*)")

	return nAttrs, goret
}

// GetPixelExtents wraps pango_layout_get_pixel_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of the
//   layout as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical
//   extents of the layout 
//
// Computes the logical and ink extents of @layout in device units.
// 
// This function just calls [method@Pango.Layout.get_extents] followed by
// two [func@extents_to_pixels] calls, rounding @ink_rect and @logical_rect
// such that the rounded rectangles fully contain the unrounded one (that is,
// passes them as first argument to [func@Pango.extents_to_pixels]).
func (layout *LayoutInstance) GetPixelExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	C.pango_layout_get_pixel_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(layout)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetPixelSize wraps pango_layout_get_pixel_size
// 
// The function returns the following values:
// 
// 	- width int32: location to store the logical width 
// 	- height int32: location to store the logical height 
//
// Determines the logical width and height of a `PangoLayout` in device
// units.
// 
// [method@Pango.Layout.get_size] returns the width and height
// scaled by %PANGO_SCALE. This is simply a convenience function
// around [method@Pango.Layout.get_pixel_extents].
func (layout *LayoutInstance) GetPixelSize() (int32, int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // out, full, casted, casted C.gint
	var carg2 C.int          // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	C.pango_layout_get_pixel_size(carg0, &carg1, &carg2)
	runtime.KeepAlive(layout)

	var width  int32
	var height int32

	width = int32(carg1)
	height = int32(carg2)

	return width, height
}

// GetSerial wraps pango_layout_get_serial
// 
// The function returns the following values:
// 
// 	- goret uint 
//
// Returns the current serial number of @layout.
// 
// The serial number is initialized to an small number larger than zero
// when a new layout is created and is increased whenever the layout is
// changed using any of the setter functions, or the `PangoContext` it
// uses has changed. The serial may wrap, but will never have the value 0.
// Since it can wrap, never compare it with "less than", always use "not equals".
// 
// This can be used to automatically detect changes to a `PangoLayout`,
// and is useful for example to decide whether a layout needs redrawing.
// To force the serial to be increased, use
// [method@Pango.Layout.context_changed].
func (layout *LayoutInstance) GetSerial() uint {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.guint        // return, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_serial(carg0)
	runtime.KeepAlive(layout)

	var goret uint

	goret = uint(cret)

	return goret
}

// GetSingleParagraphMode wraps pango_layout_get_single_paragraph_mode
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Obtains whether @layout is in single paragraph mode.
// 
// See [method@Pango.Layout.set_single_paragraph_mode].
func (layout *LayoutInstance) GetSingleParagraphMode() bool {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_single_paragraph_mode(carg0)
	runtime.KeepAlive(layout)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetSize wraps pango_layout_get_size
// 
// The function returns the following values:
// 
// 	- width int32: location to store the logical width 
// 	- height int32: location to store the logical height 
//
// Determines the logical width and height of a `PangoLayout` in Pango
// units.
// 
// This is simply a convenience function around [method@Pango.Layout.get_extents].
func (layout *LayoutInstance) GetSize() (int32, int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // out, full, casted, casted C.gint
	var carg2 C.int          // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	C.pango_layout_get_size(carg0, &carg1, &carg2)
	runtime.KeepAlive(layout)

	var width  int32
	var height int32

	width = int32(carg1)
	height = int32(carg2)

	return width, height
}

// GetSpacing wraps pango_layout_get_spacing
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the amount of spacing between the lines of the layout.
func (layout *LayoutInstance) GetSpacing() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_spacing(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetTabs wraps pango_layout_get_tabs
// 
// The function returns the following values:
// 
// 	- goret *TabArray (nullable) 
//
// Gets the current `PangoTabArray` used by this layout.
// 
// If no `PangoTabArray` has been set, then the default tabs are
// in use and %NULL is returned. Default tabs are every 8 spaces.
// 
// The return value should be freed with [method@Pango.TabArray.free].
func (layout *LayoutInstance) GetTabs() *TabArray {
	var carg0 *C.PangoLayout   // in, none, converted
	var cret  *C.PangoTabArray // return, full, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_tabs(carg0)
	runtime.KeepAlive(layout)

	var goret *TabArray

	if cret != nil {
		goret = UnsafeTabArrayFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// GetText wraps pango_layout_get_text
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Gets the text in the layout.
// 
// The returned text should not be freed or modified.
func (layout *LayoutInstance) GetText() string {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  *C.char        // return, none, string, casted *C.gchar

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_text(carg0)
	runtime.KeepAlive(layout)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// GetUnknownGlyphsCount wraps pango_layout_get_unknown_glyphs_count
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Counts the number of unknown glyphs in @layout.
// 
// This function can be used to determine if there are any fonts
// available to render all characters in a certain string, or when
// used in combination with %PANGO_ATTR_FALLBACK, to check if a
// certain font supports all the characters in the string.
func (layout *LayoutInstance) GetUnknownGlyphsCount() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_unknown_glyphs_count(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetWidth wraps pango_layout_get_width
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the width to which the lines of the `PangoLayout` should wrap.
func (layout *LayoutInstance) GetWidth() int32 {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.int          // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_width(carg0)
	runtime.KeepAlive(layout)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetWrap wraps pango_layout_get_wrap
// 
// The function returns the following values:
// 
// 	- goret WrapMode 
//
// Gets the wrap mode for the layout.
// 
// Use [method@Pango.Layout.is_wrapped] to query whether
// any paragraphs were actually wrapped.
func (layout *LayoutInstance) GetWrap() WrapMode {
	var carg0 *C.PangoLayout  // in, none, converted
	var cret  C.PangoWrapMode // return, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_get_wrap(carg0)
	runtime.KeepAlive(layout)

	var goret WrapMode

	goret = WrapMode(cret)

	return goret
}

// IndexToLineX wraps pango_layout_index_to_line_x
// 
// The function takes the following parameters:
// 
// 	- index_ int32: the byte index of a grapheme within the layout 
// 	- trailing bool: an integer indicating the edge of the grapheme to retrieve the
//   position of. If &gt; 0, the trailing edge of the grapheme, if 0,
//   the leading of the grapheme 
// 
// The function returns the following values:
// 
// 	- line int32: location to store resulting line index. (which will
//   between 0 and pango_layout_get_line_count(layout) - 1) 
// 	- xPos int32: location to store resulting position within line
//   (%PANGO_SCALE units per device unit) 
//
// Converts from byte @index_ within the @layout to line and X position.
// 
// The X position is measured from the left edge of the line.
func (layout *LayoutInstance) IndexToLineX(index_ int32, trailing bool) (int32, int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // in, none, casted, casted C.gint
	var carg2 C.gboolean     // in
	var carg3 C.int          // out, full, casted, casted C.gint
	var carg4 C.int          // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(index_)
	if trailing {
		carg2 = C.TRUE
	}

	C.pango_layout_index_to_line_x(carg0, carg1, carg2, &carg3, &carg4)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(index_)
	runtime.KeepAlive(trailing)

	var line int32
	var xPos int32

	line = int32(carg3)
	xPos = int32(carg4)

	return line, xPos
}

// IndexToPos wraps pango_layout_index_to_pos
// 
// The function takes the following parameters:
// 
// 	- index_ int32: byte index within @layout 
// 
// The function returns the following values:
// 
// 	- pos Rectangle: rectangle in which to store the position of the grapheme 
//
// Converts from an index within a `PangoLayout` to the onscreen position
// corresponding to the grapheme at that index.
// 
// The returns is represented as rectangle. Note that `pos-&gt;x` is
// always the leading edge of the grapheme and `pos-&gt;x + pos-&gt;width` the
// trailing edge of the grapheme. If the directionality of the grapheme
// is right-to-left, then `pos-&gt;width` will be negative.
func (layout *LayoutInstance) IndexToPos(index_ int32) Rectangle {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var carg2 C.PangoRectangle // out, transfer: none, C Pointers: 0, Name: Rectangle, caller-allocates

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(index_)

	C.pango_layout_index_to_pos(carg0, carg1, &carg2)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(index_)

	var pos Rectangle

	_ = pos
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return pos
}

// IsEllipsized wraps pango_layout_is_ellipsized
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Queries whether the layout had to ellipsize any paragraphs.
// 
// This returns %TRUE if the ellipsization mode for @layout
// is not %PANGO_ELLIPSIZE_NONE, a positive width is set on @layout,
// and there are paragraphs exceeding that width that have to be
// ellipsized.
func (layout *LayoutInstance) IsEllipsized() bool {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_is_ellipsized(carg0)
	runtime.KeepAlive(layout)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// IsWrapped wraps pango_layout_is_wrapped
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Queries whether the layout had to wrap any paragraphs.
// 
// This returns %TRUE if a positive width is set on @layout,
// and there are paragraphs exceeding the layout width that have
// to be wrapped.
func (layout *LayoutInstance) IsWrapped() bool {
	var carg0 *C.PangoLayout // in, none, converted
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))

	cret = C.pango_layout_is_wrapped(carg0)
	runtime.KeepAlive(layout)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// MoveCursorVisually wraps pango_layout_move_cursor_visually
// 
// The function takes the following parameters:
// 
// 	- strong bool: whether the moving cursor is the strong cursor or the
//   weak cursor. The strong cursor is the cursor corresponding
//   to text insertion in the base direction for the layout. 
// 	- oldIndex int32: the byte index of the current cursor position 
// 	- oldTrailing int32: if 0, the cursor was at the leading edge of the
//   grapheme indicated by @old_index, if &gt; 0, the cursor
//   was at the trailing edge. 
// 	- direction int32: direction to move cursor. A negative
//   value indicates motion to the left 
// 
// The function returns the following values:
// 
// 	- newIndex int32: location to store the new cursor byte index.
//   A value of -1 indicates that the cursor has been moved off the
//   beginning of the layout. A value of %G_MAXINT indicates that
//   the cursor has been moved off the end of the layout. 
// 	- newTrailing int32: number of characters to move forward from
//   the location returned for @new_index to get the position where
//   the cursor should be displayed. This allows distinguishing the
//   position at the beginning of one line from the position at the
//   end of the preceding line. @new_index is always on the line where
//   the cursor should be displayed. 
//
// Computes a new cursor position from an old position and a direction.
// 
// If @direction is positive, then the new position will cause the strong
// or weak cursor to be displayed one position to right of where it was
// with the old cursor position. If @direction is negative, it will be
// moved to the left.
// 
// In the presence of bidirectional text, the correspondence between
// logical and visual order will depend on the direction of the current
// run, and there may be jumps when the cursor is moved off of the end
// of a run.
// 
// Motion here is in cursor positions, not in characters, so a single
// call to this function may move the cursor over multiple characters
// when multiple characters combine to form a single grapheme.
func (layout *LayoutInstance) MoveCursorVisually(strong bool, oldIndex int32, oldTrailing int32, direction int32) (int32, int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.gboolean     // in
	var carg2 C.int          // in, none, casted, casted C.gint
	var carg3 C.int          // in, none, casted, casted C.gint
	var carg4 C.int          // in, none, casted, casted C.gint
	var carg5 C.int          // out, full, casted, casted C.gint
	var carg6 C.int          // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if strong {
		carg1 = C.TRUE
	}
	carg2 = C.int(oldIndex)
	carg3 = C.int(oldTrailing)
	carg4 = C.int(direction)

	C.pango_layout_move_cursor_visually(carg0, carg1, carg2, carg3, carg4, &carg5, &carg6)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(strong)
	runtime.KeepAlive(oldIndex)
	runtime.KeepAlive(oldTrailing)
	runtime.KeepAlive(direction)

	var newIndex    int32
	var newTrailing int32

	newIndex = int32(carg5)
	newTrailing = int32(carg6)

	return newIndex, newTrailing
}

// Serialize wraps pango_layout_serialize
// 
// The function takes the following parameters:
// 
// 	- flags LayoutSerializeFlags: `PangoLayoutSerializeFlags` 
// 
// The function returns the following values:
// 
// 	- goret *glib.Bytes 
//
// Serializes the @layout for later deserialization via [func@Pango.Layout.deserialize].
// 
// There are no guarantees about the format of the output across different
// versions of Pango and [func@Pango.Layout.deserialize] will reject data
// that it cannot parse.
// 
// The intended use of this function is testing, benchmarking and debugging.
// The format is not meant as a permanent storage format.
func (layout *LayoutInstance) Serialize(flags LayoutSerializeFlags) *glib.Bytes {
	var carg0 *C.PangoLayout              // in, none, converted
	var carg1 C.PangoLayoutSerializeFlags // in, none, casted
	var cret  *C.GBytes                   // return, full, converted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.PangoLayoutSerializeFlags(flags)

	cret = C.pango_layout_serialize(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(flags)

	var goret *glib.Bytes

	goret = glib.UnsafeBytesFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// SetAlignment wraps pango_layout_set_alignment
// 
// The function takes the following parameters:
// 
// 	- alignment Alignment: the alignment 
//
// Sets the alignment for the layout: how partial lines are
// positioned within the horizontal space available.
// 
// The default alignment is %PANGO_ALIGN_LEFT.
func (layout *LayoutInstance) SetAlignment(alignment Alignment) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 C.PangoAlignment // in, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.PangoAlignment(alignment)

	C.pango_layout_set_alignment(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(alignment)
}

// SetAttributes wraps pango_layout_set_attributes
// 
// The function takes the following parameters:
// 
// 	- attrs *AttrList (nullable): a `PangoAttrList` 
//
// Sets the text attributes for a layout object.
// 
// References @attrs, so the caller can unref its reference.
func (layout *LayoutInstance) SetAttributes(attrs *AttrList) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 *C.PangoAttrList // in, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if attrs != nil {
		carg1 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(attrs))
	}

	C.pango_layout_set_attributes(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(attrs)
}

// SetAutoDir wraps pango_layout_set_auto_dir
// 
// The function takes the following parameters:
// 
// 	- autoDir bool: if %TRUE, compute the bidirectional base direction
//   from the layout's contents 
//
// Sets whether to calculate the base direction
// for the layout according to its contents.
// 
// When this flag is on (the default), then paragraphs in @layout that
// begin with strong right-to-left characters (Arabic and Hebrew principally),
// will have right-to-left layout, paragraphs with letters from other scripts
// will have left-to-right layout. Paragraphs with only neutral characters
// get their direction from the surrounding paragraphs.
// 
// When %FALSE, the choice between left-to-right and right-to-left
// layout is done according to the base direction of the layout's
// `PangoContext`. (See [method@Pango.Context.set_base_dir]).
// 
// When the auto-computed direction of a paragraph differs from the
// base direction of the context, the interpretation of
// %PANGO_ALIGN_LEFT and %PANGO_ALIGN_RIGHT are swapped.
func (layout *LayoutInstance) SetAutoDir(autoDir bool) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.gboolean     // in

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if autoDir {
		carg1 = C.TRUE
	}

	C.pango_layout_set_auto_dir(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(autoDir)
}

// SetEllipsize wraps pango_layout_set_ellipsize
// 
// The function takes the following parameters:
// 
// 	- ellipsize EllipsizeMode: the new ellipsization mode for @layout 
//
// Sets the type of ellipsization being performed for @layout.
// 
// Depending on the ellipsization mode @ellipsize text is
// removed from the start, middle, or end of text so they
// fit within the width and height of layout set with
// [method@Pango.Layout.set_width] and [method@Pango.Layout.set_height].
// 
// If the layout contains characters such as newlines that
// force it to be layed out in multiple paragraphs, then whether
// each paragraph is ellipsized separately or the entire layout
// is ellipsized as a whole depends on the set height of the layout.
// 
// The default value is %PANGO_ELLIPSIZE_NONE.
// 
// See [method@Pango.Layout.set_height] for details.
func (layout *LayoutInstance) SetEllipsize(ellipsize EllipsizeMode) {
	var carg0 *C.PangoLayout       // in, none, converted
	var carg1 C.PangoEllipsizeMode // in, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.PangoEllipsizeMode(ellipsize)

	C.pango_layout_set_ellipsize(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(ellipsize)
}

// SetFontDescription wraps pango_layout_set_font_description
// 
// The function takes the following parameters:
// 
// 	- desc *FontDescription (nullable): the new `PangoFontDescription`
//   to unset the current font description 
//
// Sets the default font description for the layout.
// 
// If no font description is set on the layout, the
// font description from the layout's context is used.
func (layout *LayoutInstance) SetFontDescription(desc *FontDescription) {
	var carg0 *C.PangoLayout          // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if desc != nil {
		carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	}

	C.pango_layout_set_font_description(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(desc)
}

// SetHeight wraps pango_layout_set_height
// 
// The function takes the following parameters:
// 
// 	- height int32: the desired height of the layout in Pango units if positive,
//   or desired number of lines if negative. 
//
// Sets the height to which the `PangoLayout` should be ellipsized at.
// 
// There are two different behaviors, based on whether @height is positive
// or negative.
// 
// If @height is positive, it will be the maximum height of the layout. Only
// lines would be shown that would fit, and if there is any text omitted,
// an ellipsis added. At least one line is included in each paragraph regardless
// of how small the height value is. A value of zero will render exactly one
// line for the entire layout.
// 
// If @height is negative, it will be the (negative of) maximum number of lines
// per paragraph. That is, the total number of lines shown may well be more than
// this value if the layout contains multiple paragraphs of text.
// The default value of -1 means that the first line of each paragraph is ellipsized.
// This behavior may be changed in the future to act per layout instead of per
// paragraph. File a bug against pango at
// [https://gitlab.gnome.org/gnome/pango](https://gitlab.gnome.org/gnome/pango)
// if your code relies on this behavior.
// 
// Height setting only has effect if a positive width is set on
// @layout and ellipsization mode of @layout is not %PANGO_ELLIPSIZE_NONE.
// The behavior is undefined if a height other than -1 is set and
// ellipsization mode is set to %PANGO_ELLIPSIZE_NONE, and may change in the
// future.
func (layout *LayoutInstance) SetHeight(height int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // in, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(height)

	C.pango_layout_set_height(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(height)
}

// SetIndent wraps pango_layout_set_indent
// 
// The function takes the following parameters:
// 
// 	- indent int32: the amount by which to indent 
//
// Sets the width in Pango units to indent each paragraph.
// 
// A negative value of @indent will produce a hanging indentation.
// That is, the first line will have the full width, and subsequent
// lines will be indented by the absolute value of @indent.
// 
// The indent setting is ignored if layout alignment is set to
// %PANGO_ALIGN_CENTER.
// 
// The default value is 0.
func (layout *LayoutInstance) SetIndent(indent int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // in, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(indent)

	C.pango_layout_set_indent(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(indent)
}

// SetJustify wraps pango_layout_set_justify
// 
// The function takes the following parameters:
// 
// 	- justify bool: whether the lines in the layout should be justified 
//
// Sets whether each complete line should be stretched to fill the
// entire width of the layout.
// 
// Stretching is typically done by adding whitespace, but for some scripts
// (such as Arabic), the justification may be done in more complex ways,
// like extending the characters.
// 
// Note that this setting is not implemented and so is ignored in
// Pango older than 1.18.
// 
// Note that tabs and justification conflict with each other:
// Justification will move content away from its tab-aligned
// positions.
// 
// The default value is %FALSE.
// 
// Also see [method@Pango.Layout.set_justify_last_line].
func (layout *LayoutInstance) SetJustify(justify bool) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.gboolean     // in

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if justify {
		carg1 = C.TRUE
	}

	C.pango_layout_set_justify(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(justify)
}

// SetJustifyLastLine wraps pango_layout_set_justify_last_line
// 
// The function takes the following parameters:
// 
// 	- justify bool: whether the last line in the layout should be justified 
//
// Sets whether the last line should be stretched to fill the
// entire width of the layout.
// 
// This only has an effect if [method@Pango.Layout.set_justify] has
// been called as well.
// 
// The default value is %FALSE.
func (layout *LayoutInstance) SetJustifyLastLine(justify bool) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.gboolean     // in

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if justify {
		carg1 = C.TRUE
	}

	C.pango_layout_set_justify_last_line(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(justify)
}

// SetLineSpacing wraps pango_layout_set_line_spacing
// 
// The function takes the following parameters:
// 
// 	- factor float32: the new line spacing factor 
//
// Sets a factor for line spacing.
// 
// Typical values are: 0, 1, 1.5, 2. The default values is 0.
// 
// If @factor is non-zero, lines are placed so that
// 
//     baseline2 = baseline1 + factor * height2
// 
// where height2 is the line height of the second line
// (as determined by the font(s)). In this case, the spacing
// set with [method@Pango.Layout.set_spacing] is ignored.
// 
// If @factor is zero (the default), spacing is applied as before.
// 
// Note: for semantics that are closer to the CSS line-height
// property, see [func@Pango.attr_line_height_new].
func (layout *LayoutInstance) SetLineSpacing(factor float32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.float        // in, none, casted, casted C.gfloat

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.float(factor)

	C.pango_layout_set_line_spacing(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(factor)
}

// SetMarkup wraps pango_layout_set_markup
// 
// The function takes the following parameters:
// 
// 	- markup string: marked-up text 
// 	- length int32: length of marked-up text in bytes, or -1 if @markup is
//   `NUL`-terminated 
//
// Sets the layout text and attribute list from marked-up text.
// 
// See [Pango Markup](pango_markup.html)).
// 
// Replaces the current text and attribute list.
// 
// This is the same as [method@Pango.Layout.set_markup_with_accel],
// but the markup text isn't scanned for accelerators.
func (layout *LayoutInstance) SetMarkup(markup string, length int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 *C.char        // in, none, string, casted *C.gchar
	var carg2 C.int          // in, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(markup)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)

	C.pango_layout_set_markup(carg0, carg1, carg2)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(markup)
	runtime.KeepAlive(length)
}

// SetMarkupWithAccel wraps pango_layout_set_markup_with_accel
// 
// The function takes the following parameters:
// 
// 	- markup string: marked-up text (see [Pango Markup](pango_markup.html)) 
// 	- length int32: length of marked-up text in bytes, or -1 if @markup is
//   `NUL`-terminated 
// 	- accelMarker uint32: marker for accelerators in the text 
// 
// The function returns the following values:
// 
// 	- accelChar uint32: return location
//   for first located accelerator 
//
// Sets the layout text and attribute list from marked-up text.
// 
// See [Pango Markup](pango_markup.html)).
// 
// Replaces the current text and attribute list.
// 
// If @accel_marker is nonzero, the given character will mark the
// character following it as an accelerator. For example, @accel_marker
// might be an ampersand or underscore. All characters marked
// as an accelerator will receive a %PANGO_UNDERLINE_LOW attribute,
// and the first character so marked will be returned in @accel_char.
// Two @accel_marker characters following each other produce a single
// literal @accel_marker character.
func (layout *LayoutInstance) SetMarkupWithAccel(markup string, length int32, accelMarker uint32) uint32 {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 *C.char        // in, none, string, casted *C.gchar
	var carg2 C.int          // in, none, casted, casted C.gint
	var carg3 C.gunichar     // in, none, casted
	var carg4 C.gunichar     // out, full, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(markup)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)
	carg3 = C.gunichar(accelMarker)

	C.pango_layout_set_markup_with_accel(carg0, carg1, carg2, carg3, &carg4)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(markup)
	runtime.KeepAlive(length)
	runtime.KeepAlive(accelMarker)

	var accelChar uint32

	accelChar = uint32(carg4)

	return accelChar
}

// SetSingleParagraphMode wraps pango_layout_set_single_paragraph_mode
// 
// The function takes the following parameters:
// 
// 	- setting bool: new setting 
//
// Sets the single paragraph mode of @layout.
// 
// If @setting is %TRUE, do not treat newlines and similar characters
// as paragraph separators; instead, keep all text in a single paragraph,
// and display a glyph for paragraph separator characters. Used when
// you want to allow editing of newlines on a single text line.
// 
// The default value is %FALSE.
func (layout *LayoutInstance) SetSingleParagraphMode(setting bool) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.gboolean     // in

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if setting {
		carg1 = C.TRUE
	}

	C.pango_layout_set_single_paragraph_mode(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(setting)
}

// SetSpacing wraps pango_layout_set_spacing
// 
// The function takes the following parameters:
// 
// 	- spacing int32: the amount of spacing 
//
// Sets the amount of spacing in Pango units between
// the lines of the layout.
// 
// When placing lines with spacing, Pango arranges things so that
// 
//     line2.top = line1.bottom + spacing
// 
// The default value is 0.
// 
// Note: Since 1.44, Pango is using the line height (as determined
// by the font) for placing lines when the line spacing factor is set
// to a non-zero value with [method@Pango.Layout.set_line_spacing].
// In that case, the @spacing set with this function is ignored.
// 
// Note: for semantics that are closer to the CSS line-height
// property, see [func@Pango.attr_line_height_new].
func (layout *LayoutInstance) SetSpacing(spacing int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // in, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(spacing)

	C.pango_layout_set_spacing(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(spacing)
}

// SetTabs wraps pango_layout_set_tabs
// 
// The function takes the following parameters:
// 
// 	- tabs *TabArray (nullable): a `PangoTabArray` 
//
// Sets the tabs to use for @layout, overriding the default tabs.
// 
// `PangoLayout` will place content at the next tab position
// whenever it meets a Tab character (U+0009).
// 
// By default, tabs are every 8 spaces. If @tabs is %NULL, the
// default tabs are reinstated. @tabs is copied into the layout;
// you must free your copy of @tabs yourself.
// 
// Note that tabs and justification conflict with each other:
// Justification will move content away from its tab-aligned
// positions. The same is true for alignments other than
// %PANGO_ALIGN_LEFT.
func (layout *LayoutInstance) SetTabs(tabs *TabArray) {
	var carg0 *C.PangoLayout   // in, none, converted
	var carg1 *C.PangoTabArray // in, none, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	if tabs != nil {
		carg1 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabs))
	}

	C.pango_layout_set_tabs(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(tabs)
}

// SetText wraps pango_layout_set_text
// 
// The function takes the following parameters:
// 
// 	- text string: the text 
// 	- length int32: maximum length of @text, in bytes. -1 indicates that
//   the string is nul-terminated and the length should be calculated.
//   The text will also be truncated on encountering a nul-termination
//   even when @length is positive. 
//
// Sets the text of the layout.
// 
// This function validates @text and renders invalid UTF-8
// with a placeholder glyph.
// 
// Note that if you have used [method@Pango.Layout.set_markup] or
// [method@Pango.Layout.set_markup_with_accel] on @layout before, you
// may want to call [method@Pango.Layout.set_attributes] to clear the
// attributes set on the layout from the markup as this function does
// not clear attributes.
func (layout *LayoutInstance) SetText(text string, length int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 *C.char        // in, none, string, casted *C.gchar
	var carg2 C.int          // in, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)

	C.pango_layout_set_text(carg0, carg1, carg2)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)
}

// SetWidth wraps pango_layout_set_width
// 
// The function takes the following parameters:
// 
// 	- width int32: the desired width in Pango units, or -1 to indicate that no
//   wrapping or ellipsization should be performed. 
//
// Sets the width to which the lines of the `PangoLayout` should wrap or
// get ellipsized.
// 
// The default value is -1: no width set.
func (layout *LayoutInstance) SetWidth(width int32) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // in, none, casted, casted C.gint

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(width)

	C.pango_layout_set_width(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(width)
}

// SetWrap wraps pango_layout_set_wrap
// 
// The function takes the following parameters:
// 
// 	- wrap WrapMode: the wrap mode 
//
// Sets the wrap mode.
// 
// The wrap mode only has effect if a width is set on the layout
// with [method@Pango.Layout.set_width]. To turn off wrapping,
// set the width to -1.
// 
// The default value is %PANGO_WRAP_WORD.
func (layout *LayoutInstance) SetWrap(wrap WrapMode) {
	var carg0 *C.PangoLayout  // in, none, converted
	var carg1 C.PangoWrapMode // in, none, casted

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.PangoWrapMode(wrap)

	C.pango_layout_set_wrap(carg0, carg1)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(wrap)
}

// WriteToFile wraps pango_layout_write_to_file
// 
// The function takes the following parameters:
// 
// 	- flags LayoutSerializeFlags: `PangoLayoutSerializeFlags` 
// 	- filename string: the file to save it to 
// 
// The function returns the following values:
// 
// 	- goret bool 
// 	- _goerr error (nullable): an error 
//
// A convenience method to serialize a layout to a file.
// 
// It is equivalent to calling [method@Pango.Layout.serialize]
// followed by [func@GLib.file_set_contents].
// 
// See those two functions for details on the arguments.
// 
// It is mostly intended for use inside a debugger to quickly dump
// a layout to a file for later inspection.
func (layout *LayoutInstance) WriteToFile(flags LayoutSerializeFlags, filename string) (bool, error) {
	var carg0 *C.PangoLayout              // in, none, converted
	var carg1 C.PangoLayoutSerializeFlags // in, none, casted
	var carg2 *C.char                     // in, none, string, casted *C.gchar
	var cret  C.gboolean                  // return
	var _cerr *C.GError                   // out, full, converted, nullable

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.PangoLayoutSerializeFlags(flags)
	carg2 = (*C.char)(unsafe.Pointer(C.CString(filename)))
	defer C.free(unsafe.Pointer(carg2))

	cret = C.pango_layout_write_to_file(carg0, carg1, carg2, &_cerr)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(flags)
	runtime.KeepAlive(filename)

	var goret  bool
	var _goerr error

	if cret != 0 {
		goret = true
	}
	if _cerr != nil {
		_goerr = glib.UnsafeErrorFromGlibFull(unsafe.Pointer(_cerr))
	}

	return goret, _goerr
}

// XYToIndex wraps pango_layout_xy_to_index
// 
// The function takes the following parameters:
// 
// 	- x int32: the X offset (in Pango units) from the left edge of the layout 
// 	- y int32: the Y offset (in Pango units) from the top edge of the layout 
// 
// The function returns the following values:
// 
// 	- index_ int32: location to store calculated byte index 
// 	- trailing int32: location to store a integer indicating where
//   in the grapheme the user clicked. It will either be zero, or the
//   number of characters in the grapheme. 0 represents the leading edge
//   of the grapheme. 
// 	- goret bool 
//
// Converts from X and Y position within a layout to the byte index to the
// character at that logical position.
// 
// If the Y position is not inside the layout, the closest position is
// chosen (the position will be clamped inside the layout). If the X position
// is not within the layout, then the start or the end of the line is
// chosen as described for [method@Pango.LayoutLine.x_to_index]. If either
// the X or Y positions were not inside the layout, then the function returns
// %FALSE; on an exact hit, it returns %TRUE.
func (layout *LayoutInstance) XYToIndex(x int32, y int32) (int32, int32, bool) {
	var carg0 *C.PangoLayout // in, none, converted
	var carg1 C.int          // in, none, casted, casted C.gint
	var carg2 C.int          // in, none, casted, casted C.gint
	var carg3 C.int          // out, full, casted, casted C.gint
	var carg4 C.int          // out, full, casted, casted C.gint
	var cret  C.gboolean     // return

	carg0 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg1 = C.int(x)
	carg2 = C.int(y)

	cret = C.pango_layout_xy_to_index(carg0, carg1, carg2, &carg3, &carg4)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)

	var index_   int32
	var trailing int32
	var goret    bool

	index_ = int32(carg3)
	trailing = int32(carg4)
	if cret != 0 {
		goret = true
	}

	return index_, trailing, goret
}

// RendererInstance is the instance type used by all types extending PangoRenderer. It is used internally by the bindings. Users should use the interface [Renderer] instead.
type RendererInstance struct {
	_ [0]func() // equal guard
	gobject.ObjectInstance
}

var _ Renderer = (*RendererInstance)(nil)

// Renderer wraps PangoRenderer
//
// `PangoRenderer` is a base class for objects that can render text
// provided as `PangoGlyphString` or `PangoLayout`.
// 
// By subclassing `PangoRenderer` and overriding operations such as
// @draw_glyphs and @draw_rectangle, renderers for particular font
// backends and destinations can be created.
type Renderer interface {
	gobject.Object
	upcastToPangoRenderer() *RendererInstance

	// Activate wraps pango_renderer_activate
	//
	// Does initial setup before rendering operations on @renderer.
	// 
	// [method@Pango.Renderer.deactivate] should be called when done drawing.
	// Calls such as [method@Pango.Renderer.draw_layout] automatically
	// activate the layout before drawing on it.
	// 
	// Calls to [method@Pango.Renderer.activate] and
	// [method@Pango.Renderer.deactivate] can be nested and the
	// renderer will only be initialized and deinitialized once.
	Activate()
	// Deactivate wraps pango_renderer_deactivate
	//
	// Cleans up after rendering operations on @renderer.
	// 
	// See docs for [method@Pango.Renderer.activate].
	Deactivate()
	// DrawErrorUnderline wraps pango_renderer_draw_error_underline
	// 
	// The function takes the following parameters:
	// 
	// 	- x int32: X coordinate of underline, in Pango units in user coordinate system 
	// 	- y int32: Y coordinate of underline, in Pango units in user coordinate system 
	// 	- width int32: width of underline, in Pango units in user coordinate system 
	// 	- height int32: height of underline, in Pango units in user coordinate system 
	//
	// Draw a squiggly line that approximately covers the given rectangle
	// in the style of an underline used to indicate a spelling error.
	// 
	// The width of the underline is rounded to an integer number
	// of up/down segments and the resulting rectangle is centered
	// in the original rectangle.
	// 
	// This should be called while @renderer is already active.
	// Use [method@Pango.Renderer.activate] to activate a renderer.
	DrawErrorUnderline(int32, int32, int32, int32)
	// DrawGlyph wraps pango_renderer_draw_glyph
	// 
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont` 
	// 	- glyph Glyph: the glyph index of a single glyph 
	// 	- x float64: X coordinate of left edge of baseline of glyph 
	// 	- y float64: Y coordinate of left edge of baseline of glyph 
	//
	// Draws a single glyph with coordinates in device space.
	DrawGlyph(Font, Glyph, float64, float64)
	// DrawGlyphItem wraps pango_renderer_draw_glyph_item
	// 
	// The function takes the following parameters:
	// 
	// 	- text string (nullable): the UTF-8 text that @glyph_item refers to 
	// 	- glyphItem *GlyphItem: a `PangoGlyphItem` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units 
	//
	// Draws the glyphs in @glyph_item with the specified `PangoRenderer`,
	// embedding the text associated with the glyphs in the output if the
	// output format supports it.
	// 
	// This is useful for rendering text in PDF.
	// 
	// Note that this method does not handle attributes in @glyph_item.
	// If you want colors, shapes and lines handled automatically according
	// to those attributes, you need to use pango_renderer_draw_layout_line()
	// or pango_renderer_draw_layout().
	// 
	// Note that @text is the start of the text for layout, which is then
	// indexed by `glyph_item-&gt;item-&gt;offset`.
	// 
	// If @text is %NULL, this simply calls [method@Pango.Renderer.draw_glyphs].
	// 
	// The default implementation of this method simply falls back to
	// [method@Pango.Renderer.draw_glyphs].
	DrawGlyphItem(string, *GlyphItem, int32, int32)
	// DrawGlyphs wraps pango_renderer_draw_glyphs
	// 
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont` 
	// 	- glyphs *GlyphString: a `PangoGlyphString` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	//
	// Draws the glyphs in @glyphs with the specified `PangoRenderer`.
	DrawGlyphs(Font, *GlyphString, int32, int32)
	// DrawLayout wraps pango_renderer_draw_layout
	// 
	// The function takes the following parameters:
	// 
	// 	- layout Layout: a `PangoLayout` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	//
	// Draws @layout with the specified `PangoRenderer`.
	// 
	// This is equivalent to drawing the lines of the layout, at their
	// respective positions relative to @x, @y.
	DrawLayout(Layout, int32, int32)
	// DrawLayoutLine wraps pango_renderer_draw_layout_line
	// 
	// The function takes the following parameters:
	// 
	// 	- line *LayoutLine: a `PangoLayoutLine` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	//
	// Draws @line with the specified `PangoRenderer`.
	// 
	// This draws the glyph items that make up the line, as well as
	// shapes, backgrounds and lines that are specified by the attributes
	// of those items.
	DrawLayoutLine(*LayoutLine, int32, int32)
	// DrawRectangle wraps pango_renderer_draw_rectangle
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: type of object this rectangle is part of 
	// 	- x int32: X position at which to draw rectangle, in user space coordinates
	//   in Pango units 
	// 	- y int32: Y position at which to draw rectangle, in user space coordinates
	//   in Pango units 
	// 	- width int32: width of rectangle in Pango units 
	// 	- height int32: height of rectangle in Pango units 
	//
	// Draws an axis-aligned rectangle in user space coordinates with the
	// specified `PangoRenderer`.
	// 
	// This should be called while @renderer is already active.
	// Use [method@Pango.Renderer.activate] to activate a renderer.
	DrawRectangle(RenderPart, int32, int32, int32, int32)
	// DrawTrapezoid wraps pango_renderer_draw_trapezoid
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: type of object this trapezoid is part of 
	// 	- y1 float64: Y coordinate of top of trapezoid 
	// 	- x11 float64: X coordinate of left end of top of trapezoid 
	// 	- x21 float64: X coordinate of right end of top of trapezoid 
	// 	- y2 float64: Y coordinate of bottom of trapezoid 
	// 	- x12 float64: X coordinate of left end of bottom of trapezoid 
	// 	- x22 float64: X coordinate of right end of bottom of trapezoid 
	//
	// Draws a trapezoid with the parallel sides aligned with the X axis
	// using the given `PangoRenderer`; coordinates are in device space.
	DrawTrapezoid(RenderPart, float64, float64, float64, float64, float64, float64)
	// GetAlpha wraps pango_renderer_get_alpha
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part to get the alpha for 
	// 
	// The function returns the following values:
	// 
	// 	- goret uint16 
	//
	// Gets the current alpha for the specified part.
	GetAlpha(RenderPart) uint16
	// GetColor wraps pango_renderer_get_color
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part to get the color for 
	// 
	// The function returns the following values:
	// 
	// 	- goret *Color (nullable) 
	//
	// Gets the current rendering color for the specified part.
	GetColor(RenderPart) *Color
	// GetLayout wraps pango_renderer_get_layout
	// 
	// The function returns the following values:
	// 
	// 	- goret Layout (nullable) 
	//
	// Gets the layout currently being rendered using @renderer.
	// 
	// Calling this function only makes sense from inside a subclass's
	// methods, like in its draw_shape vfunc, for example.
	// 
	// The returned layout should not be modified while still being
	// rendered.
	GetLayout() Layout
	// GetLayoutLine wraps pango_renderer_get_layout_line
	// 
	// The function returns the following values:
	// 
	// 	- goret *LayoutLine (nullable) 
	//
	// Gets the layout line currently being rendered using @renderer.
	// 
	// Calling this function only makes sense from inside a subclass's
	// methods, like in its draw_shape vfunc, for example.
	// 
	// The returned layout line should not be modified while still being
	// rendered.
	GetLayoutLine() *LayoutLine
	// GetMatrix wraps pango_renderer_get_matrix
	// 
	// The function returns the following values:
	// 
	// 	- goret *Matrix (nullable) 
	//
	// Gets the transformation matrix that will be applied when
	// rendering.
	// 
	// See [method@Pango.Renderer.set_matrix].
	GetMatrix() *Matrix
	// PartChanged wraps pango_renderer_part_changed
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part for which rendering has changed. 
	//
	// Informs Pango that the way that the rendering is done
	// for @part has changed.
	// 
	// This should be called if the rendering changes in a way that would
	// prevent multiple pieces being joined together into one drawing call.
	// For instance, if a subclass of `PangoRenderer` was to add a stipple
	// option for drawing underlines, it needs to call
	// 
	// ```
	// pango_renderer_part_changed (render, PANGO_RENDER_PART_UNDERLINE);
	// ```
	// 
	// When the stipple changes or underlines with different stipples
	// might be joined together. Pango automatically calls this for
	// changes to colors. (See [method@Pango.Renderer.set_color])
	PartChanged(RenderPart)
	// SetAlpha wraps pango_renderer_set_alpha
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part to set the alpha for 
	// 	- alpha uint16: an alpha value between 1 and 65536, or 0 to unset the alpha 
	//
	// Sets the alpha for part of the rendering.
	// 
	// Note that the alpha may only be used if a color is
	// specified for @part as well.
	SetAlpha(RenderPart, uint16)
	// SetColor wraps pango_renderer_set_color
	// 
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part to change the color of 
	// 	- color *Color (nullable): the new color or %NULL to unset the current color 
	//
	// Sets the color for part of the rendering.
	// 
	// Also see [method@Pango.Renderer.set_alpha].
	SetColor(RenderPart, *Color)
	// SetMatrix wraps pango_renderer_set_matrix
	// 
	// The function takes the following parameters:
	// 
	// 	- matrix *Matrix (nullable): a `PangoMatrix`, or %NULL to unset any existing matrix
	//  (No matrix set is the same as setting the identity matrix.) 
	//
	// Sets the transformation matrix that will be applied when rendering.
	SetMatrix(*Matrix)

	// chain up virtual methods:

	// ParentBegin calls the default implementations of the begin virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	//
	// Do renderer-specific initialization before drawing
	ParentBegin()
	// ParentDrawErrorUnderline calls the default implementations of the draw_error_underline virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- x int32: X coordinate of underline, in Pango units in user coordinate system 
	// 	- y int32: Y coordinate of underline, in Pango units in user coordinate system 
	// 	- width int32: width of underline, in Pango units in user coordinate system 
	// 	- height int32: height of underline, in Pango units in user coordinate system 
	//
	// Draw a squiggly line that approximately covers the given rectangle
	// in the style of an underline used to indicate a spelling error.
	// 
	// The width of the underline is rounded to an integer number
	// of up/down segments and the resulting rectangle is centered
	// in the original rectangle.
	// 
	// This should be called while @renderer is already active.
	// Use [method@Pango.Renderer.activate] to activate a renderer.
	ParentDrawErrorUnderline(x int32, y int32, width int32, height int32)
	// ParentDrawGlyph calls the default implementations of the draw_glyph virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont` 
	// 	- glyph Glyph: the glyph index of a single glyph 
	// 	- x float64: X coordinate of left edge of baseline of glyph 
	// 	- y float64: Y coordinate of left edge of baseline of glyph 
	//
	// Draws a single glyph with coordinates in device space.
	ParentDrawGlyph(font Font, glyph Glyph, x float64, y float64)
	// ParentDrawGlyphItem calls the default implementations of the draw_glyph_item virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- text string (nullable): the UTF-8 text that @glyph_item refers to 
	// 	- glyphItem *GlyphItem: a `PangoGlyphItem` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units 
	//
	// Draws the glyphs in @glyph_item with the specified `PangoRenderer`,
	// embedding the text associated with the glyphs in the output if the
	// output format supports it.
	// 
	// This is useful for rendering text in PDF.
	// 
	// Note that this method does not handle attributes in @glyph_item.
	// If you want colors, shapes and lines handled automatically according
	// to those attributes, you need to use pango_renderer_draw_layout_line()
	// or pango_renderer_draw_layout().
	// 
	// Note that @text is the start of the text for layout, which is then
	// indexed by `glyph_item-&gt;item-&gt;offset`.
	// 
	// If @text is %NULL, this simply calls [method@Pango.Renderer.draw_glyphs].
	// 
	// The default implementation of this method simply falls back to
	// [method@Pango.Renderer.draw_glyphs].
	ParentDrawGlyphItem(text string, glyphItem *GlyphItem, x int32, y int32)
	// ParentDrawGlyphs calls the default implementations of the draw_glyphs virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont` 
	// 	- glyphs *GlyphString: a `PangoGlyphString` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	//
	// Draws the glyphs in @glyphs with the specified `PangoRenderer`.
	ParentDrawGlyphs(font Font, glyphs *GlyphString, x int32, y int32)
	// ParentDrawRectangle calls the default implementations of the draw_rectangle virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: type of object this rectangle is part of 
	// 	- x int32: X position at which to draw rectangle, in user space coordinates
	//   in Pango units 
	// 	- y int32: Y position at which to draw rectangle, in user space coordinates
	//   in Pango units 
	// 	- width int32: width of rectangle in Pango units 
	// 	- height int32: height of rectangle in Pango units 
	//
	// Draws an axis-aligned rectangle in user space coordinates with the
	// specified `PangoRenderer`.
	// 
	// This should be called while @renderer is already active.
	// Use [method@Pango.Renderer.activate] to activate a renderer.
	ParentDrawRectangle(part RenderPart, x int32, y int32, width int32, height int32)
	// ParentDrawShape calls the default implementations of the draw_shape virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- attr *AttrShape 
	// 	- x int32 
	// 	- y int32 
	//
	// draw content for a glyph shaped with `PangoAttrShape`
	//   @x, @y are the coordinates of the left edge of the baseline,
	//   in user coordinates.
	ParentDrawShape(attr *AttrShape, x int32, y int32)
	// ParentDrawTrapezoid calls the default implementations of the draw_trapezoid virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: type of object this trapezoid is part of 
	// 	- y1 float64: Y coordinate of top of trapezoid 
	// 	- x11 float64: X coordinate of left end of top of trapezoid 
	// 	- x21 float64: X coordinate of right end of top of trapezoid 
	// 	- y2 float64: Y coordinate of bottom of trapezoid 
	// 	- x12 float64: X coordinate of left end of bottom of trapezoid 
	// 	- x22 float64: X coordinate of right end of bottom of trapezoid 
	//
	// Draws a trapezoid with the parallel sides aligned with the X axis
	// using the given `PangoRenderer`; coordinates are in device space.
	ParentDrawTrapezoid(part RenderPart, y1 float64, x11 float64, x21 float64, y2 float64, x12 float64, x22 float64)
	// ParentEnd calls the default implementations of the end virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	//
	// Do renderer-specific cleanup after drawing
	ParentEnd()
	// ParentPartChanged calls the default implementations of the part_changed virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part for which rendering has changed. 
	//
	// Informs Pango that the way that the rendering is done
	// for @part has changed.
	// 
	// This should be called if the rendering changes in a way that would
	// prevent multiple pieces being joined together into one drawing call.
	// For instance, if a subclass of `PangoRenderer` was to add a stipple
	// option for drawing underlines, it needs to call
	// 
	// ```
	// pango_renderer_part_changed (render, PANGO_RENDER_PART_UNDERLINE);
	// ```
	// 
	// When the stipple changes or underlines with different stipples
	// might be joined together. Pango automatically calls this for
	// changes to colors. (See [method@Pango.Renderer.set_color])
	ParentPartChanged(part RenderPart)
	// ParentPrepareRun calls the default implementations of the prepare_run virtual method.
	// This functions behavior is not defined when the parent does not implement the virtual method.
	// The function takes the following parameters:
	// 
	// 	- run *LayoutRun 
	//
	// updates the renderer for a new run
	ParentPrepareRun(run *LayoutRun)
}

func unsafeWrapRenderer(base *gobject.ObjectInstance) *RendererInstance {
	return &RendererInstance{
		ObjectInstance: *base,
	}
}

func marshalRendererInstance(p unsafe.Pointer) (any, error) {
	return unsafeWrapRenderer(gobject.ValueFromNative(p).Object()), nil
}

// UnsafeRendererFromGlibNone is used to convert raw PangoRenderer pointers to go while taking a reference and attaching a finalizer. This is used by the bindings internally.
func UnsafeRendererFromGlibNone(c unsafe.Pointer) Renderer {
	return gobject.UnsafeObjectFromGlibNone(c).(Renderer)
}

// UnsafeRendererFromGlibFull is used to convert raw PangoRenderer pointers to go while attaching a finalizer. This is used by the bindings internally.
func UnsafeRendererFromGlibFull(c unsafe.Pointer) Renderer {
	return gobject.UnsafeObjectFromGlibFull(c).(Renderer)
}

// UnsafeRendererFromGlibBorrow is used to convert raw PangoRenderer pointers to go without touching any references. This is used by the bindings internally.
func UnsafeRendererFromGlibBorrow(c unsafe.Pointer) Renderer {
	return gobject.UnsafeObjectFromGlibBorrow(c).(Renderer)
}

func (r *RendererInstance) upcastToPangoRenderer() *RendererInstance {
	return r
}

// UnsafeRendererToGlibNone is used to convert the instance to it's C value PangoRenderer. This is used by the bindings internally.
func UnsafeRendererToGlibNone(c Renderer) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibNone(c)
}

// UnsafeRendererToGlibFull is used to convert the instance to it's C value PangoRenderer, while removeing the finalizer. This is used by the bindings internally.
func UnsafeRendererToGlibFull(c Renderer) unsafe.Pointer {
	return gobject.UnsafeObjectToGlibFull(c)
}

// Activate wraps pango_renderer_activate
//
// Does initial setup before rendering operations on @renderer.
// 
// [method@Pango.Renderer.deactivate] should be called when done drawing.
// Calls such as [method@Pango.Renderer.draw_layout] automatically
// activate the layout before drawing on it.
// 
// Calls to [method@Pango.Renderer.activate] and
// [method@Pango.Renderer.deactivate] can be nested and the
// renderer will only be initialized and deinitialized once.
func (renderer *RendererInstance) Activate() {
	var carg0 *C.PangoRenderer // in, none, converted

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))

	C.pango_renderer_activate(carg0)
	runtime.KeepAlive(renderer)
}

// Deactivate wraps pango_renderer_deactivate
//
// Cleans up after rendering operations on @renderer.
// 
// See docs for [method@Pango.Renderer.activate].
func (renderer *RendererInstance) Deactivate() {
	var carg0 *C.PangoRenderer // in, none, converted

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))

	C.pango_renderer_deactivate(carg0)
	runtime.KeepAlive(renderer)
}

// DrawErrorUnderline wraps pango_renderer_draw_error_underline
// 
// The function takes the following parameters:
// 
// 	- x int32: X coordinate of underline, in Pango units in user coordinate system 
// 	- y int32: Y coordinate of underline, in Pango units in user coordinate system 
// 	- width int32: width of underline, in Pango units in user coordinate system 
// 	- height int32: height of underline, in Pango units in user coordinate system 
//
// Draw a squiggly line that approximately covers the given rectangle
// in the style of an underline used to indicate a spelling error.
// 
// The width of the underline is rounded to an integer number
// of up/down segments and the resulting rectangle is centered
// in the original rectangle.
// 
// This should be called while @renderer is already active.
// Use [method@Pango.Renderer.activate] to activate a renderer.
func (renderer *RendererInstance) DrawErrorUnderline(x int32, y int32, width int32, height int32) {
	var carg0 *C.PangoRenderer // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var carg2 C.int            // in, none, casted, casted C.gint
	var carg3 C.int            // in, none, casted, casted C.gint
	var carg4 C.int            // in, none, casted, casted C.gint

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.int(x)
	carg2 = C.int(y)
	carg3 = C.int(width)
	carg4 = C.int(height)

	C.pango_renderer_draw_error_underline(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
}

// DrawGlyph wraps pango_renderer_draw_glyph
// 
// The function takes the following parameters:
// 
// 	- font Font: a `PangoFont` 
// 	- glyph Glyph: the glyph index of a single glyph 
// 	- x float64: X coordinate of left edge of baseline of glyph 
// 	- y float64: Y coordinate of left edge of baseline of glyph 
//
// Draws a single glyph with coordinates in device space.
func (renderer *RendererInstance) DrawGlyph(font Font, glyph Glyph, x float64, y float64) {
	var carg0 *C.PangoRenderer // in, none, converted
	var carg1 *C.PangoFont     // in, none, converted
	var carg2 C.PangoGlyph     // in, none, casted, alias
	var carg3 C.double         // in, none, casted, casted C.gdouble
	var carg4 C.double         // in, none, casted, casted C.gdouble

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg2 = C.PangoGlyph(glyph)
	carg3 = C.double(x)
	carg4 = C.double(y)

	C.pango_renderer_draw_glyph(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(font)
	runtime.KeepAlive(glyph)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// DrawGlyphItem wraps pango_renderer_draw_glyph_item
// 
// The function takes the following parameters:
// 
// 	- text string (nullable): the UTF-8 text that @glyph_item refers to 
// 	- glyphItem *GlyphItem: a `PangoGlyphItem` 
// 	- x int32: X position of left edge of baseline, in user space coordinates
//   in Pango units 
// 	- y int32: Y position of left edge of baseline, in user space coordinates
//   in Pango units 
//
// Draws the glyphs in @glyph_item with the specified `PangoRenderer`,
// embedding the text associated with the glyphs in the output if the
// output format supports it.
// 
// This is useful for rendering text in PDF.
// 
// Note that this method does not handle attributes in @glyph_item.
// If you want colors, shapes and lines handled automatically according
// to those attributes, you need to use pango_renderer_draw_layout_line()
// or pango_renderer_draw_layout().
// 
// Note that @text is the start of the text for layout, which is then
// indexed by `glyph_item-&gt;item-&gt;offset`.
// 
// If @text is %NULL, this simply calls [method@Pango.Renderer.draw_glyphs].
// 
// The default implementation of this method simply falls back to
// [method@Pango.Renderer.draw_glyphs].
func (renderer *RendererInstance) DrawGlyphItem(text string, glyphItem *GlyphItem, x int32, y int32) {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 *C.char           // in, none, string, nullable-string
	var carg2 *C.PangoGlyphItem // in, none, converted
	var carg3 C.int             // in, none, casted, casted C.gint
	var carg4 C.int             // in, none, casted, casted C.gint

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	if text != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
		defer C.free(unsafe.Pointer(carg1))
	}
	carg2 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(glyphItem))
	carg3 = C.int(x)
	carg4 = C.int(y)

	C.pango_renderer_draw_glyph_item(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(text)
	runtime.KeepAlive(glyphItem)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// DrawGlyphs wraps pango_renderer_draw_glyphs
// 
// The function takes the following parameters:
// 
// 	- font Font: a `PangoFont` 
// 	- glyphs *GlyphString: a `PangoGlyphString` 
// 	- x int32: X position of left edge of baseline, in user space coordinates
//   in Pango units. 
// 	- y int32: Y position of left edge of baseline, in user space coordinates
//   in Pango units. 
//
// Draws the glyphs in @glyphs with the specified `PangoRenderer`.
func (renderer *RendererInstance) DrawGlyphs(font Font, glyphs *GlyphString, x int32, y int32) {
	var carg0 *C.PangoRenderer    // in, none, converted
	var carg1 *C.PangoFont        // in, none, converted
	var carg2 *C.PangoGlyphString // in, none, converted
	var carg3 C.int               // in, none, casted, casted C.gint
	var carg4 C.int               // in, none, casted, casted C.gint

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg2 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg3 = C.int(x)
	carg4 = C.int(y)

	C.pango_renderer_draw_glyphs(carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(font)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// DrawLayout wraps pango_renderer_draw_layout
// 
// The function takes the following parameters:
// 
// 	- layout Layout: a `PangoLayout` 
// 	- x int32: X position of left edge of baseline, in user space coordinates
//   in Pango units. 
// 	- y int32: Y position of left edge of baseline, in user space coordinates
//   in Pango units. 
//
// Draws @layout with the specified `PangoRenderer`.
// 
// This is equivalent to drawing the lines of the layout, at their
// respective positions relative to @x, @y.
func (renderer *RendererInstance) DrawLayout(layout Layout, x int32, y int32) {
	var carg0 *C.PangoRenderer // in, none, converted
	var carg1 *C.PangoLayout   // in, none, converted
	var carg2 C.int            // in, none, casted, casted C.gint
	var carg3 C.int            // in, none, casted, casted C.gint

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = (*C.PangoLayout)(UnsafeLayoutToGlibNone(layout))
	carg2 = C.int(x)
	carg3 = C.int(y)

	C.pango_renderer_draw_layout(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(layout)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// DrawLayoutLine wraps pango_renderer_draw_layout_line
// 
// The function takes the following parameters:
// 
// 	- line *LayoutLine: a `PangoLayoutLine` 
// 	- x int32: X position of left edge of baseline, in user space coordinates
//   in Pango units. 
// 	- y int32: Y position of left edge of baseline, in user space coordinates
//   in Pango units. 
//
// Draws @line with the specified `PangoRenderer`.
// 
// This draws the glyph items that make up the line, as well as
// shapes, backgrounds and lines that are specified by the attributes
// of those items.
func (renderer *RendererInstance) DrawLayoutLine(line *LayoutLine, x int32, y int32) {
	var carg0 *C.PangoRenderer   // in, none, converted
	var carg1 *C.PangoLayoutLine // in, none, converted
	var carg2 C.int              // in, none, casted, casted C.gint
	var carg3 C.int              // in, none, casted, casted C.gint

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))
	carg2 = C.int(x)
	carg3 = C.int(y)

	C.pango_renderer_draw_layout_line(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(line)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// DrawRectangle wraps pango_renderer_draw_rectangle
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: type of object this rectangle is part of 
// 	- x int32: X position at which to draw rectangle, in user space coordinates
//   in Pango units 
// 	- y int32: Y position at which to draw rectangle, in user space coordinates
//   in Pango units 
// 	- width int32: width of rectangle in Pango units 
// 	- height int32: height of rectangle in Pango units 
//
// Draws an axis-aligned rectangle in user space coordinates with the
// specified `PangoRenderer`.
// 
// This should be called while @renderer is already active.
// Use [method@Pango.Renderer.activate] to activate a renderer.
func (renderer *RendererInstance) DrawRectangle(part RenderPart, x int32, y int32, width int32, height int32) {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted
	var carg2 C.int             // in, none, casted, casted C.gint
	var carg3 C.int             // in, none, casted, casted C.gint
	var carg4 C.int             // in, none, casted, casted C.gint
	var carg5 C.int             // in, none, casted, casted C.gint

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)
	carg2 = C.int(x)
	carg3 = C.int(y)
	carg4 = C.int(width)
	carg5 = C.int(height)

	C.pango_renderer_draw_rectangle(carg0, carg1, carg2, carg3, carg4, carg5)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
}

// DrawTrapezoid wraps pango_renderer_draw_trapezoid
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: type of object this trapezoid is part of 
// 	- y1 float64: Y coordinate of top of trapezoid 
// 	- x11 float64: X coordinate of left end of top of trapezoid 
// 	- x21 float64: X coordinate of right end of top of trapezoid 
// 	- y2 float64: Y coordinate of bottom of trapezoid 
// 	- x12 float64: X coordinate of left end of bottom of trapezoid 
// 	- x22 float64: X coordinate of right end of bottom of trapezoid 
//
// Draws a trapezoid with the parallel sides aligned with the X axis
// using the given `PangoRenderer`; coordinates are in device space.
func (renderer *RendererInstance) DrawTrapezoid(part RenderPart, y1 float64, x11 float64, x21 float64, y2 float64, x12 float64, x22 float64) {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted
	var carg2 C.double          // in, none, casted, casted C.gdouble
	var carg3 C.double          // in, none, casted, casted C.gdouble
	var carg4 C.double          // in, none, casted, casted C.gdouble
	var carg5 C.double          // in, none, casted, casted C.gdouble
	var carg6 C.double          // in, none, casted, casted C.gdouble
	var carg7 C.double          // in, none, casted, casted C.gdouble

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)
	carg2 = C.double(y1)
	carg3 = C.double(x11)
	carg4 = C.double(x21)
	carg5 = C.double(y2)
	carg6 = C.double(x12)
	carg7 = C.double(x22)

	C.pango_renderer_draw_trapezoid(carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
	runtime.KeepAlive(y1)
	runtime.KeepAlive(x11)
	runtime.KeepAlive(x21)
	runtime.KeepAlive(y2)
	runtime.KeepAlive(x12)
	runtime.KeepAlive(x22)
}

// GetAlpha wraps pango_renderer_get_alpha
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: the part to get the alpha for 
// 
// The function returns the following values:
// 
// 	- goret uint16 
//
// Gets the current alpha for the specified part.
func (renderer *RendererInstance) GetAlpha(part RenderPart) uint16 {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted
	var cret  C.guint16         // return, none, casted

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)

	cret = C.pango_renderer_get_alpha(carg0, carg1)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)

	var goret uint16

	goret = uint16(cret)

	return goret
}

// GetColor wraps pango_renderer_get_color
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: the part to get the color for 
// 
// The function returns the following values:
// 
// 	- goret *Color (nullable) 
//
// Gets the current rendering color for the specified part.
func (renderer *RendererInstance) GetColor(part RenderPart) *Color {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted
	var cret  *C.PangoColor     // return, none, converted, nullable

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)

	cret = C.pango_renderer_get_color(carg0, carg1)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)

	var goret *Color

	if cret != nil {
		goret = UnsafeColorFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLayout wraps pango_renderer_get_layout
// 
// The function returns the following values:
// 
// 	- goret Layout (nullable) 
//
// Gets the layout currently being rendered using @renderer.
// 
// Calling this function only makes sense from inside a subclass's
// methods, like in its draw_shape vfunc, for example.
// 
// The returned layout should not be modified while still being
// rendered.
func (renderer *RendererInstance) GetLayout() Layout {
	var carg0 *C.PangoRenderer // in, none, converted
	var cret  *C.PangoLayout   // return, none, converted, nullable

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))

	cret = C.pango_renderer_get_layout(carg0)
	runtime.KeepAlive(renderer)

	var goret Layout

	if cret != nil {
		goret = UnsafeLayoutFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLayoutLine wraps pango_renderer_get_layout_line
// 
// The function returns the following values:
// 
// 	- goret *LayoutLine (nullable) 
//
// Gets the layout line currently being rendered using @renderer.
// 
// Calling this function only makes sense from inside a subclass's
// methods, like in its draw_shape vfunc, for example.
// 
// The returned layout line should not be modified while still being
// rendered.
func (renderer *RendererInstance) GetLayoutLine() *LayoutLine {
	var carg0 *C.PangoRenderer   // in, none, converted
	var cret  *C.PangoLayoutLine // return, none, converted, nullable

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))

	cret = C.pango_renderer_get_layout_line(carg0)
	runtime.KeepAlive(renderer)

	var goret *LayoutLine

	if cret != nil {
		goret = UnsafeLayoutLineFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetMatrix wraps pango_renderer_get_matrix
// 
// The function returns the following values:
// 
// 	- goret *Matrix (nullable) 
//
// Gets the transformation matrix that will be applied when
// rendering.
// 
// See [method@Pango.Renderer.set_matrix].
func (renderer *RendererInstance) GetMatrix() *Matrix {
	var carg0 *C.PangoRenderer // in, none, converted
	var cret  *C.PangoMatrix   // return, none, converted, nullable

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))

	cret = C.pango_renderer_get_matrix(carg0)
	runtime.KeepAlive(renderer)

	var goret *Matrix

	if cret != nil {
		goret = UnsafeMatrixFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// PartChanged wraps pango_renderer_part_changed
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: the part for which rendering has changed. 
//
// Informs Pango that the way that the rendering is done
// for @part has changed.
// 
// This should be called if the rendering changes in a way that would
// prevent multiple pieces being joined together into one drawing call.
// For instance, if a subclass of `PangoRenderer` was to add a stipple
// option for drawing underlines, it needs to call
// 
// ```
// pango_renderer_part_changed (render, PANGO_RENDER_PART_UNDERLINE);
// ```
// 
// When the stipple changes or underlines with different stipples
// might be joined together. Pango automatically calls this for
// changes to colors. (See [method@Pango.Renderer.set_color])
func (renderer *RendererInstance) PartChanged(part RenderPart) {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)

	C.pango_renderer_part_changed(carg0, carg1)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
}

// SetAlpha wraps pango_renderer_set_alpha
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: the part to set the alpha for 
// 	- alpha uint16: an alpha value between 1 and 65536, or 0 to unset the alpha 
//
// Sets the alpha for part of the rendering.
// 
// Note that the alpha may only be used if a color is
// specified for @part as well.
func (renderer *RendererInstance) SetAlpha(part RenderPart, alpha uint16) {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted
	var carg2 C.guint16         // in, none, casted

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)
	carg2 = C.guint16(alpha)

	C.pango_renderer_set_alpha(carg0, carg1, carg2)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
	runtime.KeepAlive(alpha)
}

// SetColor wraps pango_renderer_set_color
// 
// The function takes the following parameters:
// 
// 	- part RenderPart: the part to change the color of 
// 	- color *Color (nullable): the new color or %NULL to unset the current color 
//
// Sets the color for part of the rendering.
// 
// Also see [method@Pango.Renderer.set_alpha].
func (renderer *RendererInstance) SetColor(part RenderPart, color *Color) {
	var carg0 *C.PangoRenderer  // in, none, converted
	var carg1 C.PangoRenderPart // in, none, casted
	var carg2 *C.PangoColor     // in, none, converted, nullable

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	carg1 = C.PangoRenderPart(part)
	if color != nil {
		carg2 = (*C.PangoColor)(UnsafeColorToGlibNone(color))
	}

	C.pango_renderer_set_color(carg0, carg1, carg2)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
	runtime.KeepAlive(color)
}

// SetMatrix wraps pango_renderer_set_matrix
// 
// The function takes the following parameters:
// 
// 	- matrix *Matrix (nullable): a `PangoMatrix`, or %NULL to unset any existing matrix
//  (No matrix set is the same as setting the identity matrix.) 
//
// Sets the transformation matrix that will be applied when rendering.
func (renderer *RendererInstance) SetMatrix(matrix *Matrix) {
	var carg0 *C.PangoRenderer // in, none, converted
	var carg1 *C.PangoMatrix   // in, none, converted, nullable

	carg0 = (*C.PangoRenderer)(UnsafeRendererToGlibNone(renderer))
	if matrix != nil {
		carg1 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	}

	C.pango_renderer_set_matrix(carg0, carg1)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(matrix)
}

// RendererOverrides is the struct used to override the default implementation of virtual methods.
// it is generic over the extending instance type.
type RendererOverrides[Instance Renderer] struct {
	// gobject.ObjectOverrides allows you to override virtual methods from the parent class gobject.Object
	gobject.ObjectOverrides[Instance]

	// Begin allows you to override the implementation of the virtual method begin.
	//
	// Do renderer-specific initialization before drawing
	Begin func(Instance)
	// DrawErrorUnderline allows you to override the implementation of the virtual method draw_error_underline.
	// The function takes the following parameters:
	// 
	// 	- x int32: X coordinate of underline, in Pango units in user coordinate system 
	// 	- y int32: Y coordinate of underline, in Pango units in user coordinate system 
	// 	- width int32: width of underline, in Pango units in user coordinate system 
	// 	- height int32: height of underline, in Pango units in user coordinate system 
	//
	// Draw a squiggly line that approximately covers the given rectangle
	// in the style of an underline used to indicate a spelling error.
	// 
	// The width of the underline is rounded to an integer number
	// of up/down segments and the resulting rectangle is centered
	// in the original rectangle.
	// 
	// This should be called while @renderer is already active.
	// Use [method@Pango.Renderer.activate] to activate a renderer.
	DrawErrorUnderline func(Instance, int32, int32, int32, int32)
	// DrawGlyph allows you to override the implementation of the virtual method draw_glyph.
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont` 
	// 	- glyph Glyph: the glyph index of a single glyph 
	// 	- x float64: X coordinate of left edge of baseline of glyph 
	// 	- y float64: Y coordinate of left edge of baseline of glyph 
	//
	// Draws a single glyph with coordinates in device space.
	DrawGlyph func(Instance, Font, Glyph, float64, float64)
	// DrawGlyphItem allows you to override the implementation of the virtual method draw_glyph_item.
	// The function takes the following parameters:
	// 
	// 	- text string (nullable): the UTF-8 text that @glyph_item refers to 
	// 	- glyphItem *GlyphItem: a `PangoGlyphItem` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units 
	//
	// Draws the glyphs in @glyph_item with the specified `PangoRenderer`,
	// embedding the text associated with the glyphs in the output if the
	// output format supports it.
	// 
	// This is useful for rendering text in PDF.
	// 
	// Note that this method does not handle attributes in @glyph_item.
	// If you want colors, shapes and lines handled automatically according
	// to those attributes, you need to use pango_renderer_draw_layout_line()
	// or pango_renderer_draw_layout().
	// 
	// Note that @text is the start of the text for layout, which is then
	// indexed by `glyph_item-&gt;item-&gt;offset`.
	// 
	// If @text is %NULL, this simply calls [method@Pango.Renderer.draw_glyphs].
	// 
	// The default implementation of this method simply falls back to
	// [method@Pango.Renderer.draw_glyphs].
	DrawGlyphItem func(Instance, string, *GlyphItem, int32, int32)
	// DrawGlyphs allows you to override the implementation of the virtual method draw_glyphs.
	// The function takes the following parameters:
	// 
	// 	- font Font: a `PangoFont` 
	// 	- glyphs *GlyphString: a `PangoGlyphString` 
	// 	- x int32: X position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	// 	- y int32: Y position of left edge of baseline, in user space coordinates
	//   in Pango units. 
	//
	// Draws the glyphs in @glyphs with the specified `PangoRenderer`.
	DrawGlyphs func(Instance, Font, *GlyphString, int32, int32)
	// DrawRectangle allows you to override the implementation of the virtual method draw_rectangle.
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: type of object this rectangle is part of 
	// 	- x int32: X position at which to draw rectangle, in user space coordinates
	//   in Pango units 
	// 	- y int32: Y position at which to draw rectangle, in user space coordinates
	//   in Pango units 
	// 	- width int32: width of rectangle in Pango units 
	// 	- height int32: height of rectangle in Pango units 
	//
	// Draws an axis-aligned rectangle in user space coordinates with the
	// specified `PangoRenderer`.
	// 
	// This should be called while @renderer is already active.
	// Use [method@Pango.Renderer.activate] to activate a renderer.
	DrawRectangle func(Instance, RenderPart, int32, int32, int32, int32)
	// DrawShape allows you to override the implementation of the virtual method draw_shape.
	// The function takes the following parameters:
	// 
	// 	- attr *AttrShape 
	// 	- x int32 
	// 	- y int32 
	//
	// draw content for a glyph shaped with `PangoAttrShape`
	//   @x, @y are the coordinates of the left edge of the baseline,
	//   in user coordinates.
	DrawShape func(Instance, *AttrShape, int32, int32)
	// DrawTrapezoid allows you to override the implementation of the virtual method draw_trapezoid.
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: type of object this trapezoid is part of 
	// 	- y1 float64: Y coordinate of top of trapezoid 
	// 	- x11 float64: X coordinate of left end of top of trapezoid 
	// 	- x21 float64: X coordinate of right end of top of trapezoid 
	// 	- y2 float64: Y coordinate of bottom of trapezoid 
	// 	- x12 float64: X coordinate of left end of bottom of trapezoid 
	// 	- x22 float64: X coordinate of right end of bottom of trapezoid 
	//
	// Draws a trapezoid with the parallel sides aligned with the X axis
	// using the given `PangoRenderer`; coordinates are in device space.
	DrawTrapezoid func(Instance, RenderPart, float64, float64, float64, float64, float64, float64)
	// End allows you to override the implementation of the virtual method end.
	//
	// Do renderer-specific cleanup after drawing
	End func(Instance)
	// PartChanged allows you to override the implementation of the virtual method part_changed.
	// The function takes the following parameters:
	// 
	// 	- part RenderPart: the part for which rendering has changed. 
	//
	// Informs Pango that the way that the rendering is done
	// for @part has changed.
	// 
	// This should be called if the rendering changes in a way that would
	// prevent multiple pieces being joined together into one drawing call.
	// For instance, if a subclass of `PangoRenderer` was to add a stipple
	// option for drawing underlines, it needs to call
	// 
	// ```
	// pango_renderer_part_changed (render, PANGO_RENDER_PART_UNDERLINE);
	// ```
	// 
	// When the stipple changes or underlines with different stipples
	// might be joined together. Pango automatically calls this for
	// changes to colors. (See [method@Pango.Renderer.set_color])
	PartChanged func(Instance, RenderPart)
	// PrepareRun allows you to override the implementation of the virtual method prepare_run.
	// The function takes the following parameters:
	// 
	// 	- run *LayoutRun 
	//
	// updates the renderer for a new run
	PrepareRun func(Instance, *LayoutRun)
}

// UnsafeApplyRendererOverrides applies the overrides to init the gclass by setting the trampoline functions.
// This is used by the bindings internally and only exported for visibility to other bindings code.
func UnsafeApplyRendererOverrides[Instance Renderer](gclass unsafe.Pointer, overrides RendererOverrides[Instance]) {
	gobject.UnsafeApplyObjectOverrides(gclass, overrides.ObjectOverrides)

	pclass := (*C.PangoRendererClass)(gclass)

	if overrides.Begin != nil {
		pclass.begin = (*[0]byte)(C._gotk4_pango1_Renderer_begin)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_begin",
			func(carg0 *C.PangoRenderer) {
				var renderer Instance // go PangoRenderer subclass

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				overrides.Begin(renderer)
			},
		)
	}

	if overrides.DrawErrorUnderline != nil {
		pclass.draw_error_underline = (*[0]byte)(C._gotk4_pango1_Renderer_draw_error_underline)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_error_underline",
			func(carg0 *C.PangoRenderer, carg1 C.int, carg2 C.int, carg3 C.int, carg4 C.int) {
				var renderer Instance // go PangoRenderer subclass
				var x        int32    // in, none, casted, casted C.gint
				var y        int32    // in, none, casted, casted C.gint
				var width    int32    // in, none, casted, casted C.gint
				var height   int32    // in, none, casted, casted C.gint

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				x = int32(carg1)
				y = int32(carg2)
				width = int32(carg3)
				height = int32(carg4)

				overrides.DrawErrorUnderline(renderer, x, y, width, height)
			},
		)
	}

	if overrides.DrawGlyph != nil {
		pclass.draw_glyph = (*[0]byte)(C._gotk4_pango1_Renderer_draw_glyph)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_glyph",
			func(carg0 *C.PangoRenderer, carg1 *C.PangoFont, carg2 C.PangoGlyph, carg3 C.double, carg4 C.double) {
				var renderer Instance // go PangoRenderer subclass
				var font     Font     // in, none, converted
				var glyph    Glyph    // in, none, casted, alias
				var x        float64  // in, none, casted, casted C.gdouble
				var y        float64  // in, none, casted, casted C.gdouble

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				font = UnsafeFontFromGlibNone(unsafe.Pointer(carg1))
				glyph = Glyph(carg2)
				x = float64(carg3)
				y = float64(carg4)

				overrides.DrawGlyph(renderer, font, glyph, x, y)
			},
		)
	}

	if overrides.DrawGlyphItem != nil {
		pclass.draw_glyph_item = (*[0]byte)(C._gotk4_pango1_Renderer_draw_glyph_item)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_glyph_item",
			func(carg0 *C.PangoRenderer, carg1 *C.char, carg2 *C.PangoGlyphItem, carg3 C.int, carg4 C.int) {
				var renderer  Instance   // go PangoRenderer subclass
				var text      string     // in, none, string, casted *C.gchar, nullable
				var glyphItem *GlyphItem // in, none, converted
				var x         int32      // in, none, casted, casted C.gint
				var y         int32      // in, none, casted, casted C.gint

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				if carg1 != nil {
					text = C.GoString((*C.char)(unsafe.Pointer(carg1)))
				}
				glyphItem = UnsafeGlyphItemFromGlibNone(unsafe.Pointer(carg2))
				x = int32(carg3)
				y = int32(carg4)

				overrides.DrawGlyphItem(renderer, text, glyphItem, x, y)
			},
		)
	}

	if overrides.DrawGlyphs != nil {
		pclass.draw_glyphs = (*[0]byte)(C._gotk4_pango1_Renderer_draw_glyphs)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_glyphs",
			func(carg0 *C.PangoRenderer, carg1 *C.PangoFont, carg2 *C.PangoGlyphString, carg3 C.int, carg4 C.int) {
				var renderer Instance     // go PangoRenderer subclass
				var font     Font         // in, none, converted
				var glyphs   *GlyphString // in, none, converted
				var x        int32        // in, none, casted, casted C.gint
				var y        int32        // in, none, casted, casted C.gint

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				font = UnsafeFontFromGlibNone(unsafe.Pointer(carg1))
				glyphs = UnsafeGlyphStringFromGlibNone(unsafe.Pointer(carg2))
				x = int32(carg3)
				y = int32(carg4)

				overrides.DrawGlyphs(renderer, font, glyphs, x, y)
			},
		)
	}

	if overrides.DrawRectangle != nil {
		pclass.draw_rectangle = (*[0]byte)(C._gotk4_pango1_Renderer_draw_rectangle)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_rectangle",
			func(carg0 *C.PangoRenderer, carg1 C.PangoRenderPart, carg2 C.int, carg3 C.int, carg4 C.int, carg5 C.int) {
				var renderer Instance   // go PangoRenderer subclass
				var part     RenderPart // in, none, casted
				var x        int32      // in, none, casted, casted C.gint
				var y        int32      // in, none, casted, casted C.gint
				var width    int32      // in, none, casted, casted C.gint
				var height   int32      // in, none, casted, casted C.gint

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				part = RenderPart(carg1)
				x = int32(carg2)
				y = int32(carg3)
				width = int32(carg4)
				height = int32(carg5)

				overrides.DrawRectangle(renderer, part, x, y, width, height)
			},
		)
	}

	if overrides.DrawShape != nil {
		pclass.draw_shape = (*[0]byte)(C._gotk4_pango1_Renderer_draw_shape)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_shape",
			func(carg0 *C.PangoRenderer, carg1 *C.PangoAttrShape, carg2 C.int, carg3 C.int) {
				var renderer Instance   // go PangoRenderer subclass
				var attr     *AttrShape // in, none, converted
				var x        int32      // in, none, casted, casted C.gint
				var y        int32      // in, none, casted, casted C.gint

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				attr = UnsafeAttrShapeFromGlibNone(unsafe.Pointer(carg1))
				x = int32(carg2)
				y = int32(carg3)

				overrides.DrawShape(renderer, attr, x, y)
			},
		)
	}

	if overrides.DrawTrapezoid != nil {
		pclass.draw_trapezoid = (*[0]byte)(C._gotk4_pango1_Renderer_draw_trapezoid)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_draw_trapezoid",
			func(carg0 *C.PangoRenderer, carg1 C.PangoRenderPart, carg2 C.double, carg3 C.double, carg4 C.double, carg5 C.double, carg6 C.double, carg7 C.double) {
				var renderer Instance   // go PangoRenderer subclass
				var part     RenderPart // in, none, casted
				var y1       float64    // in, none, casted, casted C.gdouble
				var x11      float64    // in, none, casted, casted C.gdouble
				var x21      float64    // in, none, casted, casted C.gdouble
				var y2       float64    // in, none, casted, casted C.gdouble
				var x12      float64    // in, none, casted, casted C.gdouble
				var x22      float64    // in, none, casted, casted C.gdouble

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				part = RenderPart(carg1)
				y1 = float64(carg2)
				x11 = float64(carg3)
				x21 = float64(carg4)
				y2 = float64(carg5)
				x12 = float64(carg6)
				x22 = float64(carg7)

				overrides.DrawTrapezoid(renderer, part, y1, x11, x21, y2, x12, x22)
			},
		)
	}

	if overrides.End != nil {
		pclass.end = (*[0]byte)(C._gotk4_pango1_Renderer_end)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_end",
			func(carg0 *C.PangoRenderer) {
				var renderer Instance // go PangoRenderer subclass

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)

				overrides.End(renderer)
			},
		)
	}

	if overrides.PartChanged != nil {
		pclass.part_changed = (*[0]byte)(C._gotk4_pango1_Renderer_part_changed)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_part_changed",
			func(carg0 *C.PangoRenderer, carg1 C.PangoRenderPart) {
				var renderer Instance   // go PangoRenderer subclass
				var part     RenderPart // in, none, casted

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				part = RenderPart(carg1)

				overrides.PartChanged(renderer, part)
			},
		)
	}

	if overrides.PrepareRun != nil {
		pclass.prepare_run = (*[0]byte)(C._gotk4_pango1_Renderer_prepare_run)
		classdata.StoreVirtualMethod(
			unsafe.Pointer(pclass),
			"_gotk4_pango1_Renderer_prepare_run",
			func(carg0 *C.PangoRenderer, carg1 *C.PangoLayoutRun) {
				var renderer Instance   // go PangoRenderer subclass
				var run      *LayoutRun // in, transfer: none, C Pointers: 1, Name: LayoutRun

				renderer = UnsafeRendererFromGlibBorrow(unsafe.Pointer(carg0)).(Instance)
				_ = run
				_ = carg1
				panic("unimplemented conversion of *LayoutRun (PangoLayoutRun*)")

				overrides.PrepareRun(renderer, run)
			},
		)
	}
}

// ParentBegin calls the default implementations of the begin virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
//
// Do renderer-specific initialization before drawing
func (renderer *RendererInstance) ParentBegin() {
	var carg0 *C.PangoRenderer

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	C._gotk4_pango1_Renderer_virtual_begin(unsafe.Pointer(parentclass.begin), carg0)
	runtime.KeepAlive(renderer)
}

// ParentDrawErrorUnderline calls the default implementations of the draw_error_underline virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- x int32: X coordinate of underline, in Pango units in user coordinate system 
// 	- y int32: Y coordinate of underline, in Pango units in user coordinate system 
// 	- width int32: width of underline, in Pango units in user coordinate system 
// 	- height int32: height of underline, in Pango units in user coordinate system 
//
// Draw a squiggly line that approximately covers the given rectangle
// in the style of an underline used to indicate a spelling error.
// 
// The width of the underline is rounded to an integer number
// of up/down segments and the resulting rectangle is centered
// in the original rectangle.
// 
// This should be called while @renderer is already active.
// Use [method@Pango.Renderer.activate] to activate a renderer.
func (renderer *RendererInstance) ParentDrawErrorUnderline(x int32, y int32, width int32, height int32) {
	var carg0 *C.PangoRenderer
	var carg1 C.int // in, none, casted, casted C.gint
	var carg2 C.int // in, none, casted, casted C.gint
	var carg3 C.int // in, none, casted, casted C.gint
	var carg4 C.int // in, none, casted, casted C.gint

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = C.int(x)
	carg2 = C.int(y)
	carg3 = C.int(width)
	carg4 = C.int(height)

	C._gotk4_pango1_Renderer_virtual_draw_error_underline(unsafe.Pointer(parentclass.draw_error_underline), carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
}

// ParentDrawGlyph calls the default implementations of the draw_glyph virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- font Font: a `PangoFont` 
// 	- glyph Glyph: the glyph index of a single glyph 
// 	- x float64: X coordinate of left edge of baseline of glyph 
// 	- y float64: Y coordinate of left edge of baseline of glyph 
//
// Draws a single glyph with coordinates in device space.
func (renderer *RendererInstance) ParentDrawGlyph(font Font, glyph Glyph, x float64, y float64) {
	var carg0 *C.PangoRenderer
	var carg1 *C.PangoFont // in, none, converted
	var carg2 C.PangoGlyph // in, none, casted, alias
	var carg3 C.double     // in, none, casted, casted C.gdouble
	var carg4 C.double     // in, none, casted, casted C.gdouble

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg2 = C.PangoGlyph(glyph)
	carg3 = C.double(x)
	carg4 = C.double(y)

	C._gotk4_pango1_Renderer_virtual_draw_glyph(unsafe.Pointer(parentclass.draw_glyph), carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(font)
	runtime.KeepAlive(glyph)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// ParentDrawGlyphItem calls the default implementations of the draw_glyph_item virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- text string (nullable): the UTF-8 text that @glyph_item refers to 
// 	- glyphItem *GlyphItem: a `PangoGlyphItem` 
// 	- x int32: X position of left edge of baseline, in user space coordinates
//   in Pango units 
// 	- y int32: Y position of left edge of baseline, in user space coordinates
//   in Pango units 
//
// Draws the glyphs in @glyph_item with the specified `PangoRenderer`,
// embedding the text associated with the glyphs in the output if the
// output format supports it.
// 
// This is useful for rendering text in PDF.
// 
// Note that this method does not handle attributes in @glyph_item.
// If you want colors, shapes and lines handled automatically according
// to those attributes, you need to use pango_renderer_draw_layout_line()
// or pango_renderer_draw_layout().
// 
// Note that @text is the start of the text for layout, which is then
// indexed by `glyph_item-&gt;item-&gt;offset`.
// 
// If @text is %NULL, this simply calls [method@Pango.Renderer.draw_glyphs].
// 
// The default implementation of this method simply falls back to
// [method@Pango.Renderer.draw_glyphs].
func (renderer *RendererInstance) ParentDrawGlyphItem(text string, glyphItem *GlyphItem, x int32, y int32) {
	var carg0 *C.PangoRenderer
	var carg1 *C.char           // in, none, string, nullable-string
	var carg2 *C.PangoGlyphItem // in, none, converted
	var carg3 C.int             // in, none, casted, casted C.gint
	var carg4 C.int             // in, none, casted, casted C.gint

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	if text != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
		defer C.free(unsafe.Pointer(carg1))
	}
	carg2 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(glyphItem))
	carg3 = C.int(x)
	carg4 = C.int(y)

	C._gotk4_pango1_Renderer_virtual_draw_glyph_item(unsafe.Pointer(parentclass.draw_glyph_item), carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(text)
	runtime.KeepAlive(glyphItem)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// ParentDrawGlyphs calls the default implementations of the draw_glyphs virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- font Font: a `PangoFont` 
// 	- glyphs *GlyphString: a `PangoGlyphString` 
// 	- x int32: X position of left edge of baseline, in user space coordinates
//   in Pango units. 
// 	- y int32: Y position of left edge of baseline, in user space coordinates
//   in Pango units. 
//
// Draws the glyphs in @glyphs with the specified `PangoRenderer`.
func (renderer *RendererInstance) ParentDrawGlyphs(font Font, glyphs *GlyphString, x int32, y int32) {
	var carg0 *C.PangoRenderer
	var carg1 *C.PangoFont        // in, none, converted
	var carg2 *C.PangoGlyphString // in, none, converted
	var carg3 C.int               // in, none, casted, casted C.gint
	var carg4 C.int               // in, none, casted, casted C.gint

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = (*C.PangoFont)(UnsafeFontToGlibNone(font))
	carg2 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg3 = C.int(x)
	carg4 = C.int(y)

	C._gotk4_pango1_Renderer_virtual_draw_glyphs(unsafe.Pointer(parentclass.draw_glyphs), carg0, carg1, carg2, carg3, carg4)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(font)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// ParentDrawRectangle calls the default implementations of the draw_rectangle virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- part RenderPart: type of object this rectangle is part of 
// 	- x int32: X position at which to draw rectangle, in user space coordinates
//   in Pango units 
// 	- y int32: Y position at which to draw rectangle, in user space coordinates
//   in Pango units 
// 	- width int32: width of rectangle in Pango units 
// 	- height int32: height of rectangle in Pango units 
//
// Draws an axis-aligned rectangle in user space coordinates with the
// specified `PangoRenderer`.
// 
// This should be called while @renderer is already active.
// Use [method@Pango.Renderer.activate] to activate a renderer.
func (renderer *RendererInstance) ParentDrawRectangle(part RenderPart, x int32, y int32, width int32, height int32) {
	var carg0 *C.PangoRenderer
	var carg1 C.PangoRenderPart // in, none, casted
	var carg2 C.int             // in, none, casted, casted C.gint
	var carg3 C.int             // in, none, casted, casted C.gint
	var carg4 C.int             // in, none, casted, casted C.gint
	var carg5 C.int             // in, none, casted, casted C.gint

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = C.PangoRenderPart(part)
	carg2 = C.int(x)
	carg3 = C.int(y)
	carg4 = C.int(width)
	carg5 = C.int(height)

	C._gotk4_pango1_Renderer_virtual_draw_rectangle(unsafe.Pointer(parentclass.draw_rectangle), carg0, carg1, carg2, carg3, carg4, carg5)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
	runtime.KeepAlive(width)
	runtime.KeepAlive(height)
}

// ParentDrawShape calls the default implementations of the draw_shape virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- attr *AttrShape 
// 	- x int32 
// 	- y int32 
//
// draw content for a glyph shaped with `PangoAttrShape`
//   @x, @y are the coordinates of the left edge of the baseline,
//   in user coordinates.
func (renderer *RendererInstance) ParentDrawShape(attr *AttrShape, x int32, y int32) {
	var carg0 *C.PangoRenderer
	var carg1 *C.PangoAttrShape // in, none, converted
	var carg2 C.int             // in, none, casted, casted C.gint
	var carg3 C.int             // in, none, casted, casted C.gint

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = (*C.PangoAttrShape)(UnsafeAttrShapeToGlibNone(attr))
	carg2 = C.int(x)
	carg3 = C.int(y)

	C._gotk4_pango1_Renderer_virtual_draw_shape(unsafe.Pointer(parentclass.draw_shape), carg0, carg1, carg2, carg3)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(attr)
	runtime.KeepAlive(x)
	runtime.KeepAlive(y)
}

// ParentDrawTrapezoid calls the default implementations of the draw_trapezoid virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- part RenderPart: type of object this trapezoid is part of 
// 	- y1 float64: Y coordinate of top of trapezoid 
// 	- x11 float64: X coordinate of left end of top of trapezoid 
// 	- x21 float64: X coordinate of right end of top of trapezoid 
// 	- y2 float64: Y coordinate of bottom of trapezoid 
// 	- x12 float64: X coordinate of left end of bottom of trapezoid 
// 	- x22 float64: X coordinate of right end of bottom of trapezoid 
//
// Draws a trapezoid with the parallel sides aligned with the X axis
// using the given `PangoRenderer`; coordinates are in device space.
func (renderer *RendererInstance) ParentDrawTrapezoid(part RenderPart, y1 float64, x11 float64, x21 float64, y2 float64, x12 float64, x22 float64) {
	var carg0 *C.PangoRenderer
	var carg1 C.PangoRenderPart // in, none, casted
	var carg2 C.double          // in, none, casted, casted C.gdouble
	var carg3 C.double          // in, none, casted, casted C.gdouble
	var carg4 C.double          // in, none, casted, casted C.gdouble
	var carg5 C.double          // in, none, casted, casted C.gdouble
	var carg6 C.double          // in, none, casted, casted C.gdouble
	var carg7 C.double          // in, none, casted, casted C.gdouble

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = C.PangoRenderPart(part)
	carg2 = C.double(y1)
	carg3 = C.double(x11)
	carg4 = C.double(x21)
	carg5 = C.double(y2)
	carg6 = C.double(x12)
	carg7 = C.double(x22)

	C._gotk4_pango1_Renderer_virtual_draw_trapezoid(unsafe.Pointer(parentclass.draw_trapezoid), carg0, carg1, carg2, carg3, carg4, carg5, carg6, carg7)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
	runtime.KeepAlive(y1)
	runtime.KeepAlive(x11)
	runtime.KeepAlive(x21)
	runtime.KeepAlive(y2)
	runtime.KeepAlive(x12)
	runtime.KeepAlive(x22)
}

// ParentEnd calls the default implementations of the end virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
//
// Do renderer-specific cleanup after drawing
func (renderer *RendererInstance) ParentEnd() {
	var carg0 *C.PangoRenderer

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	C._gotk4_pango1_Renderer_virtual_end(unsafe.Pointer(parentclass.end), carg0)
	runtime.KeepAlive(renderer)
}

// ParentPartChanged calls the default implementations of the part_changed virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- part RenderPart: the part for which rendering has changed. 
//
// Informs Pango that the way that the rendering is done
// for @part has changed.
// 
// This should be called if the rendering changes in a way that would
// prevent multiple pieces being joined together into one drawing call.
// For instance, if a subclass of `PangoRenderer` was to add a stipple
// option for drawing underlines, it needs to call
// 
// ```
// pango_renderer_part_changed (render, PANGO_RENDER_PART_UNDERLINE);
// ```
// 
// When the stipple changes or underlines with different stipples
// might be joined together. Pango automatically calls this for
// changes to colors. (See [method@Pango.Renderer.set_color])
func (renderer *RendererInstance) ParentPartChanged(part RenderPart) {
	var carg0 *C.PangoRenderer
	var carg1 C.PangoRenderPart // in, none, casted

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	carg1 = C.PangoRenderPart(part)

	C._gotk4_pango1_Renderer_virtual_part_changed(unsafe.Pointer(parentclass.part_changed), carg0, carg1)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(part)
}

// ParentPrepareRun calls the default implementations of the prepare_run virtual method.
// This functions behavior is not defined when the parent does not implement the virtual method.
// The function takes the following parameters:
// 
// 	- run *LayoutRun 
//
// updates the renderer for a new run
func (renderer *RendererInstance) ParentPrepareRun(run *LayoutRun) {
	var carg0 *C.PangoRenderer
	var carg1 *C.PangoLayoutRun // in, transfer: none, C Pointers: 1, Name: LayoutRun

	parentclass := (*C.PangoRendererClass)(classdata.PeekParentClass(UnsafeRendererToGlibNone(renderer)))

	_ = run
	_ = carg1
	panic("unimplemented conversion of *LayoutRun (PangoLayoutRun*)")

	C._gotk4_pango1_Renderer_virtual_prepare_run(unsafe.Pointer(parentclass.prepare_run), carg0, carg1)
	runtime.KeepAlive(renderer)
	runtime.KeepAlive(run)
}

// RegisterRendererSubClass is used to register a go subclass of PangoRenderer. For this to work safely please implement the
// virtual methods required by the implementation.
func RegisterRendererSubClass[InstanceT Renderer](
		name string,
		classInit func(class *RendererClass),
		constructor func() InstanceT,
		overrides RendererOverrides[InstanceT],
		signals map[string]gobject.SignalDefinition,
		interfaceInits ...gobject.SubClassInterfaceInit[InstanceT],
) gobject.Type {
	return gobject.UnsafeRegisterSubClass(
		name,
		classInit,
		constructor,
		overrides,
		signals,
		TypeRenderer,
		UnsafeRendererClassFromGlibBorrow,
		UnsafeApplyRendererOverrides,
		func (obj *gobject.ObjectInstance) gobject.Object {
			return unsafeWrapRenderer(obj)
		},
		interfaceInits...,
	)
}

// Analysis wraps PangoAnalysis
//
// The `PangoAnalysis` structure stores information about
// the properties of a segment of text.
type Analysis struct {
	*analysis
}

// analysis is the struct that's finalized
type analysis struct {
	native *C.PangoAnalysis
}

// UnsafeAnalysisFromGlibBorrow is used to convert raw C.PangoAnalysis pointers to go. This is used by the bindings internally.
func UnsafeAnalysisFromGlibBorrow(p unsafe.Pointer) *Analysis {
	return &Analysis{&analysis{(*C.PangoAnalysis)(p)}}
}

// UnsafeAnalysisFromGlibNone is used to convert raw C.PangoAnalysis pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAnalysisFromGlibNone(p unsafe.Pointer) *Analysis {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAnalysisFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.analysis,
		func (intern *analysis) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAnalysisFromGlibFull is used to convert raw C.PangoAnalysis pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAnalysisFromGlibFull(p unsafe.Pointer) *Analysis {
	wrapped := UnsafeAnalysisFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.analysis,
		func (intern *analysis) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAnalysisFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Analysis] is expected to work anymore.
func UnsafeAnalysisFree(a *Analysis) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAnalysisToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAnalysisToGlibNone(a *Analysis) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAnalysisToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAnalysisToGlibFull(a *Analysis) unsafe.Pointer {
	runtime.SetFinalizer(a.analysis, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // Analysis is invalid from here on
	return _p
}

// AttrClass wraps PangoAttrClass
//
// The `PangoAttrClass` structure stores the type and operations for
// a particular type of attribute.
// 
// The functions in this structure should not be called directly. Instead,
// one should use the wrapper functions provided for `PangoAttribute`.
type AttrClass struct {
	*attrClass
}

// attrClass is the struct that's finalized
type attrClass struct {
	native *C.PangoAttrClass
}

// UnsafeAttrClassFromGlibBorrow is used to convert raw C.PangoAttrClass pointers to go. This is used by the bindings internally.
func UnsafeAttrClassFromGlibBorrow(p unsafe.Pointer) *AttrClass {
	return &AttrClass{&attrClass{(*C.PangoAttrClass)(p)}}
}

// UnsafeAttrClassFromGlibNone is used to convert raw C.PangoAttrClass pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrClassFromGlibNone(p unsafe.Pointer) *AttrClass {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrClassFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrClass,
		func (intern *attrClass) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrClassFromGlibFull is used to convert raw C.PangoAttrClass pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrClassFromGlibFull(p unsafe.Pointer) *AttrClass {
	wrapped := UnsafeAttrClassFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrClass,
		func (intern *attrClass) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrClass] is expected to work anymore.
func UnsafeAttrClassFree(a *AttrClass) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrClassToGlibNone(a *AttrClass) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrClassToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrClassToGlibFull(a *AttrClass) unsafe.Pointer {
	runtime.SetFinalizer(a.attrClass, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrClass is invalid from here on
	return _p
}

// AttrColor wraps PangoAttrColor
//
// The `PangoAttrColor` structure is used to represent attributes that
// are colors.
type AttrColor struct {
	*attrColor
}

// attrColor is the struct that's finalized
type attrColor struct {
	native *C.PangoAttrColor
}

// UnsafeAttrColorFromGlibBorrow is used to convert raw C.PangoAttrColor pointers to go. This is used by the bindings internally.
func UnsafeAttrColorFromGlibBorrow(p unsafe.Pointer) *AttrColor {
	return &AttrColor{&attrColor{(*C.PangoAttrColor)(p)}}
}

// UnsafeAttrColorFromGlibNone is used to convert raw C.PangoAttrColor pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrColorFromGlibNone(p unsafe.Pointer) *AttrColor {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrColorFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrColor,
		func (intern *attrColor) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrColorFromGlibFull is used to convert raw C.PangoAttrColor pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrColorFromGlibFull(p unsafe.Pointer) *AttrColor {
	wrapped := UnsafeAttrColorFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrColor,
		func (intern *attrColor) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrColorFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrColor] is expected to work anymore.
func UnsafeAttrColorFree(a *AttrColor) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrColorToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrColorToGlibNone(a *AttrColor) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrColorToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrColorToGlibFull(a *AttrColor) unsafe.Pointer {
	runtime.SetFinalizer(a.attrColor, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrColor is invalid from here on
	return _p
}

// AttrFloat wraps PangoAttrFloat
//
// The `PangoAttrFloat` structure is used to represent attributes with
// a float or double value.
type AttrFloat struct {
	*attrFloat
}

// attrFloat is the struct that's finalized
type attrFloat struct {
	native *C.PangoAttrFloat
}

// UnsafeAttrFloatFromGlibBorrow is used to convert raw C.PangoAttrFloat pointers to go. This is used by the bindings internally.
func UnsafeAttrFloatFromGlibBorrow(p unsafe.Pointer) *AttrFloat {
	return &AttrFloat{&attrFloat{(*C.PangoAttrFloat)(p)}}
}

// UnsafeAttrFloatFromGlibNone is used to convert raw C.PangoAttrFloat pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrFloatFromGlibNone(p unsafe.Pointer) *AttrFloat {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrFloatFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrFloat,
		func (intern *attrFloat) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrFloatFromGlibFull is used to convert raw C.PangoAttrFloat pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrFloatFromGlibFull(p unsafe.Pointer) *AttrFloat {
	wrapped := UnsafeAttrFloatFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrFloat,
		func (intern *attrFloat) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrFloatFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrFloat] is expected to work anymore.
func UnsafeAttrFloatFree(a *AttrFloat) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrFloatToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrFloatToGlibNone(a *AttrFloat) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrFloatToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrFloatToGlibFull(a *AttrFloat) unsafe.Pointer {
	runtime.SetFinalizer(a.attrFloat, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrFloat is invalid from here on
	return _p
}

// AttrFontDesc wraps PangoAttrFontDesc
//
// The `PangoAttrFontDesc` structure is used to store an attribute that
// sets all aspects of the font description at once.
type AttrFontDesc struct {
	*attrFontDesc
}

// attrFontDesc is the struct that's finalized
type attrFontDesc struct {
	native *C.PangoAttrFontDesc
}

// UnsafeAttrFontDescFromGlibBorrow is used to convert raw C.PangoAttrFontDesc pointers to go. This is used by the bindings internally.
func UnsafeAttrFontDescFromGlibBorrow(p unsafe.Pointer) *AttrFontDesc {
	return &AttrFontDesc{&attrFontDesc{(*C.PangoAttrFontDesc)(p)}}
}

// UnsafeAttrFontDescFromGlibNone is used to convert raw C.PangoAttrFontDesc pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrFontDescFromGlibNone(p unsafe.Pointer) *AttrFontDesc {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrFontDescFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrFontDesc,
		func (intern *attrFontDesc) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrFontDescFromGlibFull is used to convert raw C.PangoAttrFontDesc pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrFontDescFromGlibFull(p unsafe.Pointer) *AttrFontDesc {
	wrapped := UnsafeAttrFontDescFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrFontDesc,
		func (intern *attrFontDesc) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrFontDescFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrFontDesc] is expected to work anymore.
func UnsafeAttrFontDescFree(a *AttrFontDesc) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrFontDescToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrFontDescToGlibNone(a *AttrFontDesc) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrFontDescToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrFontDescToGlibFull(a *AttrFontDesc) unsafe.Pointer {
	runtime.SetFinalizer(a.attrFontDesc, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrFontDesc is invalid from here on
	return _p
}

// NewAttrFontDesc wraps pango_attr_font_desc_new
// 
// The function takes the following parameters:
// 
// 	- desc *FontDescription: the font description 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font description attribute.
// 
// This attribute allows setting family, style, weight, variant,
// stretch, and size simultaneously.
func NewAttrFontDesc(desc *FontDescription) *Attribute {
	var carg1 *C.PangoFontDescription // in, none, converted
	var cret  *C.PangoAttribute       // return, full, converted

	carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_attr_font_desc_new(carg1)
	runtime.KeepAlive(desc)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrFontFeatures wraps PangoAttrFontFeatures
//
// The `PangoAttrFontFeatures` structure is used to represent OpenType
// font features as an attribute.
type AttrFontFeatures struct {
	*attrFontFeatures
}

// attrFontFeatures is the struct that's finalized
type attrFontFeatures struct {
	native *C.PangoAttrFontFeatures
}

// UnsafeAttrFontFeaturesFromGlibBorrow is used to convert raw C.PangoAttrFontFeatures pointers to go. This is used by the bindings internally.
func UnsafeAttrFontFeaturesFromGlibBorrow(p unsafe.Pointer) *AttrFontFeatures {
	return &AttrFontFeatures{&attrFontFeatures{(*C.PangoAttrFontFeatures)(p)}}
}

// UnsafeAttrFontFeaturesFromGlibNone is used to convert raw C.PangoAttrFontFeatures pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrFontFeaturesFromGlibNone(p unsafe.Pointer) *AttrFontFeatures {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrFontFeaturesFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrFontFeatures,
		func (intern *attrFontFeatures) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrFontFeaturesFromGlibFull is used to convert raw C.PangoAttrFontFeatures pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrFontFeaturesFromGlibFull(p unsafe.Pointer) *AttrFontFeatures {
	wrapped := UnsafeAttrFontFeaturesFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrFontFeatures,
		func (intern *attrFontFeatures) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrFontFeaturesFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrFontFeatures] is expected to work anymore.
func UnsafeAttrFontFeaturesFree(a *AttrFontFeatures) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrFontFeaturesToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrFontFeaturesToGlibNone(a *AttrFontFeatures) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrFontFeaturesToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrFontFeaturesToGlibFull(a *AttrFontFeatures) unsafe.Pointer {
	runtime.SetFinalizer(a.attrFontFeatures, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrFontFeatures is invalid from here on
	return _p
}

// NewAttrFontFeatures wraps pango_attr_font_features_new
// 
// The function takes the following parameters:
// 
// 	- features string: a string with OpenType font features, with the syntax of the [CSS
// font-feature-settings property](https://www.w3.org/TR/css-fonts-4/#font-rend-desc) 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font features tag attribute.
// 
// You can use this attribute to select OpenType font features like small-caps,
// alternative glyphs, ligatures, etc. for fonts that support them.
func NewAttrFontFeatures(features string) *Attribute {
	var carg1 *C.char           // in, none, string, casted *C.gchar
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = (*C.char)(unsafe.Pointer(C.CString(features)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_attr_font_features_new(carg1)
	runtime.KeepAlive(features)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrInt wraps PangoAttrInt
//
// The `PangoAttrInt` structure is used to represent attributes with
// an integer or enumeration value.
type AttrInt struct {
	*attrInt
}

// attrInt is the struct that's finalized
type attrInt struct {
	native *C.PangoAttrInt
}

// UnsafeAttrIntFromGlibBorrow is used to convert raw C.PangoAttrInt pointers to go. This is used by the bindings internally.
func UnsafeAttrIntFromGlibBorrow(p unsafe.Pointer) *AttrInt {
	return &AttrInt{&attrInt{(*C.PangoAttrInt)(p)}}
}

// UnsafeAttrIntFromGlibNone is used to convert raw C.PangoAttrInt pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrIntFromGlibNone(p unsafe.Pointer) *AttrInt {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrIntFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrInt,
		func (intern *attrInt) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrIntFromGlibFull is used to convert raw C.PangoAttrInt pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrIntFromGlibFull(p unsafe.Pointer) *AttrInt {
	wrapped := UnsafeAttrIntFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrInt,
		func (intern *attrInt) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrIntFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrInt] is expected to work anymore.
func UnsafeAttrIntFree(a *AttrInt) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrIntToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrIntToGlibNone(a *AttrInt) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrIntToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrIntToGlibFull(a *AttrInt) unsafe.Pointer {
	runtime.SetFinalizer(a.attrInt, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrInt is invalid from here on
	return _p
}

// AttrIterator wraps PangoAttrIterator
//
// A `PangoAttrIterator` is used to iterate through a `PangoAttrList`.
// 
// A new iterator is created with [method@Pango.AttrList.get_iterator].
// Once the iterator is created, it can be advanced through the style
// changes in the text using [method@Pango.AttrIterator.next]. At each
// style change, the range of the current style segment and the attributes
// currently in effect can be queried.
type AttrIterator struct {
	*attrIterator
}

// attrIterator is the struct that's finalized
type attrIterator struct {
	native *C.PangoAttrIterator
}

var _ gobject.GoValueInitializer = (*AttrIterator)(nil)

func marshalAttrIterator(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeAttrIteratorFromGlibBorrow(b), nil
}

func (r *AttrIterator) GoValueType() gobject.Type {
	return TypeAttrIterator
}

func (r *AttrIterator) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeAttrIteratorFromGlibBorrow is used to convert raw C.PangoAttrIterator pointers to go. This is used by the bindings internally.
func UnsafeAttrIteratorFromGlibBorrow(p unsafe.Pointer) *AttrIterator {
	return &AttrIterator{&attrIterator{(*C.PangoAttrIterator)(p)}}
}

// UnsafeAttrIteratorFromGlibNone is used to convert raw C.PangoAttrIterator pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrIteratorFromGlibNone(p unsafe.Pointer) *AttrIterator {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrIteratorFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrIterator,
		func (intern *attrIterator) {
			C.pango_attr_iterator_destroy(intern.native)
		},
	)
	return wrapped
}

// UnsafeAttrIteratorFromGlibFull is used to convert raw C.PangoAttrIterator pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrIteratorFromGlibFull(p unsafe.Pointer) *AttrIterator {
	wrapped := UnsafeAttrIteratorFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrIterator,
		func (intern *attrIterator) {
			C.pango_attr_iterator_destroy(intern.native)
		},
	)
	return wrapped
}

// UnsafeAttrIteratorDestroy unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrIterator] is expected to work anymore.
func UnsafeAttrIteratorDestroy(a *AttrIterator) {
	C.pango_attr_iterator_destroy(a.native)
}

// UnsafeAttrIteratorToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrIteratorToGlibNone(a *AttrIterator) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrIteratorToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrIteratorToGlibFull(a *AttrIterator) unsafe.Pointer {
	runtime.SetFinalizer(a.attrIterator, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrIterator is invalid from here on
	return _p
}

// Copy wraps pango_attr_iterator_copy
// 
// The function returns the following values:
// 
// 	- goret *AttrIterator 
//
// Copy a `PangoAttrIterator`.
func (iterator *AttrIterator) Copy() *AttrIterator {
	var carg0 *C.PangoAttrIterator // in, none, converted
	var cret  *C.PangoAttrIterator // return, full, converted

	carg0 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(iterator))

	cret = C.pango_attr_iterator_copy(carg0)
	runtime.KeepAlive(iterator)

	var goret *AttrIterator

	goret = UnsafeAttrIteratorFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Get wraps pango_attr_iterator_get
// 
// The function takes the following parameters:
// 
// 	- typ AttrType: the type of attribute to find 
// 
// The function returns the following values:
// 
// 	- goret *Attribute (nullable) 
//
// Find the current attribute of a particular type
// at the iterator location.
// 
// When multiple attributes of the same type overlap,
// the attribute whose range starts closest to the
// current location is used.
func (iterator *AttrIterator) Get(typ AttrType) *Attribute {
	var carg0 *C.PangoAttrIterator // in, none, converted
	var carg1 C.PangoAttrType      // in, none, casted
	var cret  *C.PangoAttribute    // return, none, converted, nullable

	carg0 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(iterator))
	carg1 = C.PangoAttrType(typ)

	cret = C.pango_attr_iterator_get(carg0, carg1)
	runtime.KeepAlive(iterator)
	runtime.KeepAlive(typ)

	var goret *Attribute

	if cret != nil {
		goret = UnsafeAttributeFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetAttrs wraps pango_attr_iterator_get_attrs
// 
// The function returns the following values:
// 
// 	- goret []*Attribute 
//
// Gets a list of all attributes at the current position of the
// iterator.
func (iterator *AttrIterator) GetAttrs() []*Attribute {
	var carg0 *C.PangoAttrIterator // in, none, converted
	var cret  *C.GSList            // container, transfer: full

	carg0 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(iterator))

	cret = C.pango_attr_iterator_get_attrs(carg0)
	runtime.KeepAlive(iterator)

	var goret []*Attribute

	goret = glib.UnsafeSListFromGlibFull(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *Attribute {
			var dst *Attribute // converted
			dst = UnsafeAttributeFromGlibFull(v)
			return dst
		},
	)

	return goret
}

// Next wraps pango_attr_iterator_next
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Advance the iterator until the next change of style.
func (iterator *AttrIterator) Next() bool {
	var carg0 *C.PangoAttrIterator // in, none, converted
	var cret  C.gboolean           // return

	carg0 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(iterator))

	cret = C.pango_attr_iterator_next(carg0)
	runtime.KeepAlive(iterator)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Range wraps pango_attr_iterator_range
// 
// The function returns the following values:
// 
// 	- start int32: location to store the start of the range 
// 	- end int32: location to store the end of the range 
//
// Get the range of the current segment.
// 
// Note that the stored return values are signed, not unsigned
// like the values in `PangoAttribute`. To deal with this API
// oversight, stored return values that wouldn't fit into
// a signed integer are clamped to %G_MAXINT.
func (iterator *AttrIterator) Range() (int32, int32) {
	var carg0 *C.PangoAttrIterator // in, none, converted
	var carg1 C.int                // out, full, casted, casted C.gint
	var carg2 C.int                // out, full, casted, casted C.gint

	carg0 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(iterator))

	C.pango_attr_iterator_range(carg0, &carg1, &carg2)
	runtime.KeepAlive(iterator)

	var start int32
	var end   int32

	start = int32(carg1)
	end = int32(carg2)

	return start, end
}

// AttrLanguage wraps PangoAttrLanguage
//
// The `PangoAttrLanguage` structure is used to represent attributes that
// are languages.
type AttrLanguage struct {
	*attrLanguage
}

// attrLanguage is the struct that's finalized
type attrLanguage struct {
	native *C.PangoAttrLanguage
}

// UnsafeAttrLanguageFromGlibBorrow is used to convert raw C.PangoAttrLanguage pointers to go. This is used by the bindings internally.
func UnsafeAttrLanguageFromGlibBorrow(p unsafe.Pointer) *AttrLanguage {
	return &AttrLanguage{&attrLanguage{(*C.PangoAttrLanguage)(p)}}
}

// UnsafeAttrLanguageFromGlibNone is used to convert raw C.PangoAttrLanguage pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrLanguageFromGlibNone(p unsafe.Pointer) *AttrLanguage {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrLanguageFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrLanguage,
		func (intern *attrLanguage) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrLanguageFromGlibFull is used to convert raw C.PangoAttrLanguage pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrLanguageFromGlibFull(p unsafe.Pointer) *AttrLanguage {
	wrapped := UnsafeAttrLanguageFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrLanguage,
		func (intern *attrLanguage) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrLanguageFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrLanguage] is expected to work anymore.
func UnsafeAttrLanguageFree(a *AttrLanguage) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrLanguageToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrLanguageToGlibNone(a *AttrLanguage) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrLanguageToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrLanguageToGlibFull(a *AttrLanguage) unsafe.Pointer {
	runtime.SetFinalizer(a.attrLanguage, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrLanguage is invalid from here on
	return _p
}

// NewAttrLanguage wraps pango_attr_language_new
// 
// The function takes the following parameters:
// 
// 	- language *Language: language tag 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new language tag attribute.
func NewAttrLanguage(language *Language) *Attribute {
	var carg1 *C.PangoLanguage  // in, none, converted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_attr_language_new(carg1)
	runtime.KeepAlive(language)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrList wraps PangoAttrList
//
// A `PangoAttrList` represents a list of attributes that apply to a section
// of text.
// 
// The attributes in a `PangoAttrList` are, in general, allowed to overlap in
// an arbitrary fashion. However, if the attributes are manipulated only through
// [method@Pango.AttrList.change], the overlap between properties will meet
// stricter criteria.
// 
// Since the `PangoAttrList` structure is stored as a linear list, it is not
// suitable for storing attributes for large amounts of text. In general, you
// should not use a single `PangoAttrList` for more than one paragraph of text.
type AttrList struct {
	*attrList
}

// attrList is the struct that's finalized
type attrList struct {
	native *C.PangoAttrList
}

var _ gobject.GoValueInitializer = (*AttrList)(nil)

func marshalAttrList(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeAttrListFromGlibBorrow(b), nil
}

func (r *AttrList) GoValueType() gobject.Type {
	return TypeAttrList
}

func (r *AttrList) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeAttrListFromGlibBorrow is used to convert raw C.PangoAttrList pointers to go. This is used by the bindings internally.
func UnsafeAttrListFromGlibBorrow(p unsafe.Pointer) *AttrList {
	return &AttrList{&attrList{(*C.PangoAttrList)(p)}}
}

// UnsafeAttrListFromGlibNone is used to convert raw C.PangoAttrList pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrListFromGlibNone(p unsafe.Pointer) *AttrList {
	C.pango_attr_list_ref((*C.PangoAttrList)(p))
	wrapped := UnsafeAttrListFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrList,
		func (intern *attrList) {
			C.pango_attr_list_unref(intern.native)
		},
	)
	return wrapped
}

// UnsafeAttrListFromGlibFull is used to convert raw C.PangoAttrList pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrListFromGlibFull(p unsafe.Pointer) *AttrList {
	wrapped := UnsafeAttrListFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrList,
		func (intern *attrList) {
			C.pango_attr_list_unref(intern.native)
		},
	)
	return wrapped
}

// UnsafeAttrListRef increases the refcount on the underlying resource. This is used by the bindings internally.
// 
// When this is called without an associated call to [AttrList.UnsafeAttrListUnref], then [AttrList] will leak memory.
func UnsafeAttrListRef(a *AttrList) {
	C.pango_attr_list_ref(a.native)
}

// UnsafeAttrListUnref unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrList] is expected to work anymore.
func UnsafeAttrListUnref(a *AttrList) {
	C.pango_attr_list_unref(a.native)
}

// UnsafeAttrListToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrListToGlibNone(a *AttrList) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrListToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrListToGlibFull(a *AttrList) unsafe.Pointer {
	runtime.SetFinalizer(a.attrList, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrList is invalid from here on
	return _p
}

// NewAttrList wraps pango_attr_list_new
// 
// The function returns the following values:
// 
// 	- goret *AttrList 
//
// Create a new empty attribute list with a reference
// count of one.
func NewAttrList() *AttrList {
	var cret *C.PangoAttrList // return, full, converted

	cret = C.pango_attr_list_new()

	var goret *AttrList

	goret = UnsafeAttrListFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrListFromString wraps pango_attr_list_from_string
// 
// The function takes the following parameters:
// 
// 	- text string: a string 
// 
// The function returns the following values:
// 
// 	- goret *AttrList (nullable) 
//
// Deserializes a `PangoAttrList` from a string.
// 
// This is the counterpart to [method@Pango.AttrList.to_string].
// See that functions for details about the format.
func AttrListFromString(text string) *AttrList {
	var carg1 *C.char          // in, none, string, casted *C.gchar
	var cret  *C.PangoAttrList // return, full, converted, nullable

	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_attr_list_from_string(carg1)
	runtime.KeepAlive(text)

	var goret *AttrList

	if cret != nil {
		goret = UnsafeAttrListFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Change wraps pango_attr_list_change
// 
// The function takes the following parameters:
// 
// 	- attr *Attribute: the attribute to insert 
//
// Insert the given attribute into the `PangoAttrList`.
// 
// It will replace any attributes of the same type
// on that segment and be merged with any adjoining
// attributes that are identical.
// 
// This function is slower than [method@Pango.AttrList.insert]
// for creating an attribute list in order (potentially
// much slower for large lists). However,
// [method@Pango.AttrList.insert] is not suitable for
// continually changing a set of attributes since it
// never removes or combines existing attributes.
func (list *AttrList) Change(attr *Attribute) {
	var carg0 *C.PangoAttrList  // in, none, converted
	var carg1 *C.PangoAttribute // in, full, converted

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = (*C.PangoAttribute)(UnsafeAttributeToGlibFull(attr))

	C.pango_attr_list_change(carg0, carg1)
	runtime.KeepAlive(list)
	runtime.KeepAlive(attr)
}

// Copy wraps pango_attr_list_copy
// 
// The function returns the following values:
// 
// 	- goret *AttrList (nullable) 
//
// Copy @list and return an identical new list.
func (list *AttrList) Copy() *AttrList {
	var carg0 *C.PangoAttrList // in, none, converted
	var cret  *C.PangoAttrList // return, full, converted, nullable

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))

	cret = C.pango_attr_list_copy(carg0)
	runtime.KeepAlive(list)

	var goret *AttrList

	if cret != nil {
		goret = UnsafeAttrListFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Equal wraps pango_attr_list_equal
// 
// The function takes the following parameters:
// 
// 	- otherList *AttrList: the other `PangoAttrList` 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Checks whether @list and @other_list contain the same
// attributes and whether those attributes apply to the
// same ranges.
// 
// Beware that this will return wrong values if any list
// contains duplicates.
func (list *AttrList) Equal(otherList *AttrList) bool {
	var carg0 *C.PangoAttrList // in, none, converted
	var carg1 *C.PangoAttrList // in, none, converted
	var cret  C.gboolean       // return

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(otherList))

	cret = C.pango_attr_list_equal(carg0, carg1)
	runtime.KeepAlive(list)
	runtime.KeepAlive(otherList)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Filter wraps pango_attr_list_filter
// 
// The function takes the following parameters:
// 
// 	- fn AttrFilterFunc: callback function;
//   returns %TRUE if an attribute should be filtered out 
// 
// The function returns the following values:
// 
// 	- goret *AttrList (nullable) 
//
// Given a `PangoAttrList` and callback function, removes
// any elements of @list for which @func returns %TRUE and
// inserts them into a new list.
func (list *AttrList) Filter(fn AttrFilterFunc) *AttrList {
	var carg0 *C.PangoAttrList      // in, none, converted
	var carg1 C.PangoAttrFilterFunc // callback, scope: call, closure: carg2
	var carg2 C.gpointer            // implicit
	var cret  *C.PangoAttrList      // return, full, converted, nullable

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = (*[0]byte)(C._gotk4_pango1_AttrFilterFunc)
	carg2 = C.gpointer(userdata.Register(fn))
	defer userdata.Delete(unsafe.Pointer(carg2))

	cret = C.pango_attr_list_filter(carg0, carg1, carg2)
	runtime.KeepAlive(list)
	runtime.KeepAlive(fn)

	var goret *AttrList

	if cret != nil {
		goret = UnsafeAttrListFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// GetAttributes wraps pango_attr_list_get_attributes
// 
// The function returns the following values:
// 
// 	- goret []*Attribute 
//
// Gets a list of all attributes in @list.
func (list *AttrList) GetAttributes() []*Attribute {
	var carg0 *C.PangoAttrList // in, none, converted
	var cret  *C.GSList        // container, transfer: full

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))

	cret = C.pango_attr_list_get_attributes(carg0)
	runtime.KeepAlive(list)

	var goret []*Attribute

	goret = glib.UnsafeSListFromGlibFull(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *Attribute {
			var dst *Attribute // converted
			dst = UnsafeAttributeFromGlibFull(v)
			return dst
		},
	)

	return goret
}

// GetIterator wraps pango_attr_list_get_iterator
// 
// The function returns the following values:
// 
// 	- goret *AttrIterator 
//
// Create a iterator initialized to the beginning of the list.
// 
// @list must not be modified until this iterator is freed.
func (list *AttrList) GetIterator() *AttrIterator {
	var carg0 *C.PangoAttrList     // in, none, converted
	var cret  *C.PangoAttrIterator // return, full, converted

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))

	cret = C.pango_attr_list_get_iterator(carg0)
	runtime.KeepAlive(list)

	var goret *AttrIterator

	goret = UnsafeAttrIteratorFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Insert wraps pango_attr_list_insert
// 
// The function takes the following parameters:
// 
// 	- attr *Attribute: the attribute to insert 
//
// Insert the given attribute into the `PangoAttrList`.
// 
// It will be inserted after all other attributes with a
// matching @start_index.
func (list *AttrList) Insert(attr *Attribute) {
	var carg0 *C.PangoAttrList  // in, none, converted
	var carg1 *C.PangoAttribute // in, full, converted

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = (*C.PangoAttribute)(UnsafeAttributeToGlibFull(attr))

	C.pango_attr_list_insert(carg0, carg1)
	runtime.KeepAlive(list)
	runtime.KeepAlive(attr)
}

// InsertBefore wraps pango_attr_list_insert_before
// 
// The function takes the following parameters:
// 
// 	- attr *Attribute: the attribute to insert 
//
// Insert the given attribute into the `PangoAttrList`.
// 
// It will be inserted before all other attributes with a
// matching @start_index.
func (list *AttrList) InsertBefore(attr *Attribute) {
	var carg0 *C.PangoAttrList  // in, none, converted
	var carg1 *C.PangoAttribute // in, full, converted

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = (*C.PangoAttribute)(UnsafeAttributeToGlibFull(attr))

	C.pango_attr_list_insert_before(carg0, carg1)
	runtime.KeepAlive(list)
	runtime.KeepAlive(attr)
}

// Splice wraps pango_attr_list_splice
// 
// The function takes the following parameters:
// 
// 	- other *AttrList: another `PangoAttrList` 
// 	- pos int32: the position in @list at which to insert @other 
// 	- len int32: the length of the spliced segment. (Note that this
//   must be specified since the attributes in @other may only
//   be present at some subsection of this range) 
//
// This function opens up a hole in @list, fills it
// in with attributes from the left, and then merges
// @other on top of the hole.
// 
// This operation is equivalent to stretching every attribute
// that applies at position @pos in @list by an amount @len,
// and then calling [method@Pango.AttrList.change] with a copy
// of each attribute in @other in sequence (offset in position
// by @pos, and limited in length to @len).
// 
// This operation proves useful for, for instance, inserting
// a pre-edit string in the middle of an edit buffer.
// 
// For backwards compatibility, the function behaves differently
// when @len is 0. In this case, the attributes from @other are
// not imited to @len, and are just overlayed on top of @list.
// 
// This mode is useful for merging two lists of attributes together.
func (list *AttrList) Splice(other *AttrList, pos int32, len int32) {
	var carg0 *C.PangoAttrList // in, none, converted
	var carg1 *C.PangoAttrList // in, none, converted
	var carg2 C.int            // in, none, casted, casted C.gint
	var carg3 C.int            // in, none, casted, casted C.gint

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(other))
	carg2 = C.int(pos)
	carg3 = C.int(len)

	C.pango_attr_list_splice(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(list)
	runtime.KeepAlive(other)
	runtime.KeepAlive(pos)
	runtime.KeepAlive(len)
}

// ToString wraps pango_attr_list_to_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Serializes a `PangoAttrList` to a string.
// 
// In the resulting string, serialized attributes are separated by newlines or commas.
// Individual attributes are serialized to a string of the form
// 
//     [START END] TYPE VALUE
// 
// Where START and END are the indices (with -1 being accepted in place
// of MAXUINT), TYPE is the nickname of the attribute value type, e.g.
// _weight_ or _stretch_, and the value is serialized according to its type:
// 
// Optionally, START and END can be omitted to indicate unlimited extent.
// 
// - enum values as nick or numeric value
// - boolean values as _true_ or _false_
// - integers and floats as numbers
// - strings as string, optionally quoted
// - font features as quoted string
// - PangoLanguage as string
// - PangoFontDescription as serialized by [method@Pango.FontDescription.to_string], quoted
// - PangoColor as serialized by [method@Pango.Color.to_string]
// 
// Examples:
// 
//     0 10 foreground red, 5 15 weight bold, 0 200 font-desc "Sans 10"
// 
//     0 -1 weight 700
//     0 100 family Times
// 
//     weight bold
// 
// To parse the returned value, use [func@Pango.AttrList.from_string].
// 
// Note that shape attributes can not be serialized.
func (list *AttrList) ToString() string {
	var carg0 *C.PangoAttrList // in, none, converted
	var cret  *C.char          // return, full, string, casted *C.gchar

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))

	cret = C.pango_attr_list_to_string(carg0)
	runtime.KeepAlive(list)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return goret
}

// Update wraps pango_attr_list_update
// 
// The function takes the following parameters:
// 
// 	- pos int32: the position of the change 
// 	- remove int32: the number of removed bytes 
// 	- add int32: the number of added bytes 
//
// Update indices of attributes in @list for a change in the
// text they refer to.
// 
// The change that this function applies is removing @remove
// bytes at position @pos and inserting @add bytes instead.
// 
// Attributes that fall entirely in the (@pos, @pos + @remove)
// range are removed.
// 
// Attributes that start or end inside the (@pos, @pos + @remove)
// range are shortened to reflect the removal.
// 
// Attributes start and end positions are updated if they are
// behind @pos + @remove.
func (list *AttrList) Update(pos int32, remove int32, add int32) {
	var carg0 *C.PangoAttrList // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var carg2 C.int            // in, none, casted, casted C.gint
	var carg3 C.int            // in, none, casted, casted C.gint

	carg0 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))
	carg1 = C.int(pos)
	carg2 = C.int(remove)
	carg3 = C.int(add)

	C.pango_attr_list_update(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(list)
	runtime.KeepAlive(pos)
	runtime.KeepAlive(remove)
	runtime.KeepAlive(add)
}

// AttrShape wraps PangoAttrShape
//
// The `PangoAttrShape` structure is used to represent attributes which
// impose shape restrictions.
type AttrShape struct {
	*attrShape
}

// attrShape is the struct that's finalized
type attrShape struct {
	native *C.PangoAttrShape
}

// UnsafeAttrShapeFromGlibBorrow is used to convert raw C.PangoAttrShape pointers to go. This is used by the bindings internally.
func UnsafeAttrShapeFromGlibBorrow(p unsafe.Pointer) *AttrShape {
	return &AttrShape{&attrShape{(*C.PangoAttrShape)(p)}}
}

// UnsafeAttrShapeFromGlibNone is used to convert raw C.PangoAttrShape pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrShapeFromGlibNone(p unsafe.Pointer) *AttrShape {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrShapeFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrShape,
		func (intern *attrShape) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrShapeFromGlibFull is used to convert raw C.PangoAttrShape pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrShapeFromGlibFull(p unsafe.Pointer) *AttrShape {
	wrapped := UnsafeAttrShapeFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrShape,
		func (intern *attrShape) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrShapeFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrShape] is expected to work anymore.
func UnsafeAttrShapeFree(a *AttrShape) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrShapeToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrShapeToGlibNone(a *AttrShape) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrShapeToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrShapeToGlibFull(a *AttrShape) unsafe.Pointer {
	runtime.SetFinalizer(a.attrShape, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrShape is invalid from here on
	return _p
}

// NewAttrShape wraps pango_attr_shape_new
// 
// The function takes the following parameters:
// 
// 	- inkRect *Rectangle: ink rectangle to assign to each character 
// 	- logicalRect *Rectangle: logical rectangle to assign to each character 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new shape attribute.
// 
// A shape is used to impose a particular ink and logical
// rectangle on the result of shaping a particular glyph.
// This might be used, for instance, for embedding a picture
// or a widget inside a `PangoLayout`.
func NewAttrShape(inkRect *Rectangle, logicalRect *Rectangle) *Attribute {
	var carg1 *C.PangoRectangle // in, none, converted
	var carg2 *C.PangoRectangle // in, none, converted
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = (*C.PangoRectangle)(UnsafeRectangleToGlibNone(inkRect))
	carg2 = (*C.PangoRectangle)(UnsafeRectangleToGlibNone(logicalRect))

	cret = C.pango_attr_shape_new(carg1, carg2)
	runtime.KeepAlive(inkRect)
	runtime.KeepAlive(logicalRect)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrSize wraps PangoAttrSize
//
// The `PangoAttrSize` structure is used to represent attributes which
// set font size.
type AttrSize struct {
	*attrSize
}

// attrSize is the struct that's finalized
type attrSize struct {
	native *C.PangoAttrSize
}

// UnsafeAttrSizeFromGlibBorrow is used to convert raw C.PangoAttrSize pointers to go. This is used by the bindings internally.
func UnsafeAttrSizeFromGlibBorrow(p unsafe.Pointer) *AttrSize {
	return &AttrSize{&attrSize{(*C.PangoAttrSize)(p)}}
}

// UnsafeAttrSizeFromGlibNone is used to convert raw C.PangoAttrSize pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrSizeFromGlibNone(p unsafe.Pointer) *AttrSize {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrSizeFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrSize,
		func (intern *attrSize) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrSizeFromGlibFull is used to convert raw C.PangoAttrSize pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrSizeFromGlibFull(p unsafe.Pointer) *AttrSize {
	wrapped := UnsafeAttrSizeFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrSize,
		func (intern *attrSize) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrSizeFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrSize] is expected to work anymore.
func UnsafeAttrSizeFree(a *AttrSize) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrSizeToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrSizeToGlibNone(a *AttrSize) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrSizeToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrSizeToGlibFull(a *AttrSize) unsafe.Pointer {
	runtime.SetFinalizer(a.attrSize, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrSize is invalid from here on
	return _p
}

// NewAttrSize wraps pango_attr_size_new
// 
// The function takes the following parameters:
// 
// 	- size int32: the font size, in %PANGO_SCALE-ths of a point 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font-size attribute in fractional points.
func NewAttrSize(size int32) *Attribute {
	var carg1 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.int(size)

	cret = C.pango_attr_size_new(carg1)
	runtime.KeepAlive(size)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// NewAttrSizeAbsolute wraps pango_attr_size_new_absolute
// 
// The function takes the following parameters:
// 
// 	- size int32: the font size, in %PANGO_SCALE-ths of a device unit 
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Create a new font-size attribute in device units.
func NewAttrSizeAbsolute(size int32) *Attribute {
	var carg1 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoAttribute // return, full, converted

	carg1 = C.int(size)

	cret = C.pango_attr_size_new_absolute(carg1)
	runtime.KeepAlive(size)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// AttrString wraps PangoAttrString
//
// The `PangoAttrString` structure is used to represent attributes with
// a string value.
type AttrString struct {
	*attrString
}

// attrString is the struct that's finalized
type attrString struct {
	native *C.PangoAttrString
}

// UnsafeAttrStringFromGlibBorrow is used to convert raw C.PangoAttrString pointers to go. This is used by the bindings internally.
func UnsafeAttrStringFromGlibBorrow(p unsafe.Pointer) *AttrString {
	return &AttrString{&attrString{(*C.PangoAttrString)(p)}}
}

// UnsafeAttrStringFromGlibNone is used to convert raw C.PangoAttrString pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttrStringFromGlibNone(p unsafe.Pointer) *AttrString {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttrStringFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrString,
		func (intern *attrString) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrStringFromGlibFull is used to convert raw C.PangoAttrString pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttrStringFromGlibFull(p unsafe.Pointer) *AttrString {
	wrapped := UnsafeAttrStringFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attrString,
		func (intern *attrString) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeAttrStringFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [AttrString] is expected to work anymore.
func UnsafeAttrStringFree(a *AttrString) {
	C.free(unsafe.Pointer(a.native))
}

// UnsafeAttrStringToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttrStringToGlibNone(a *AttrString) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttrStringToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttrStringToGlibFull(a *AttrString) unsafe.Pointer {
	runtime.SetFinalizer(a.attrString, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // AttrString is invalid from here on
	return _p
}

// Attribute wraps PangoAttribute
//
// The `PangoAttribute` structure represents the common portions of all
// attributes.
// 
// Particular types of attributes include this structure as their initial
// portion. The common portion of the attribute holds the range to which
// the value in the type-specific part of the attribute applies and should
// be initialized using [method@Pango.Attribute.init]. By default, an attribute
// will have an all-inclusive range of [0,%G_MAXUINT].
type Attribute struct {
	*attribute
}

// attribute is the struct that's finalized
type attribute struct {
	native *C.PangoAttribute
}

var _ gobject.GoValueInitializer = (*Attribute)(nil)

func marshalAttribute(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeAttributeFromGlibBorrow(b), nil
}

func (r *Attribute) GoValueType() gobject.Type {
	return TypeAttribute
}

func (r *Attribute) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeAttributeFromGlibBorrow is used to convert raw C.PangoAttribute pointers to go. This is used by the bindings internally.
func UnsafeAttributeFromGlibBorrow(p unsafe.Pointer) *Attribute {
	return &Attribute{&attribute{(*C.PangoAttribute)(p)}}
}

// UnsafeAttributeFromGlibNone is used to convert raw C.PangoAttribute pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeAttributeFromGlibNone(p unsafe.Pointer) *Attribute {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeAttributeFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attribute,
		func (intern *attribute) {
			C.pango_attribute_destroy(intern.native)
		},
	)
	return wrapped
}

// UnsafeAttributeFromGlibFull is used to convert raw C.PangoAttribute pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeAttributeFromGlibFull(p unsafe.Pointer) *Attribute {
	wrapped := UnsafeAttributeFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.attribute,
		func (intern *attribute) {
			C.pango_attribute_destroy(intern.native)
		},
	)
	return wrapped
}

// UnsafeAttributeDestroy unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Attribute] is expected to work anymore.
func UnsafeAttributeDestroy(a *Attribute) {
	C.pango_attribute_destroy(a.native)
}

// UnsafeAttributeToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeAttributeToGlibNone(a *Attribute) unsafe.Pointer {
	return unsafe.Pointer(a.native)
}

// UnsafeAttributeToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeAttributeToGlibFull(a *Attribute) unsafe.Pointer {
	runtime.SetFinalizer(a.attribute, nil)
	_p := unsafe.Pointer(a.native)
	a.native = nil // Attribute is invalid from here on
	return _p
}

// AsColor wraps pango_attribute_as_color
// 
// The function returns the following values:
// 
// 	- goret *AttrColor (nullable) 
//
// Returns the attribute cast to `PangoAttrColor`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsColor() *AttrColor {
	var carg0 *C.PangoAttribute // in, none, converted
	var cret  *C.PangoAttrColor // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_color(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrColor

	if cret != nil {
		goret = UnsafeAttrColorFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsFloat wraps pango_attribute_as_float
// 
// The function returns the following values:
// 
// 	- goret *AttrFloat (nullable) 
//
// Returns the attribute cast to `PangoAttrFloat`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsFloat() *AttrFloat {
	var carg0 *C.PangoAttribute // in, none, converted
	var cret  *C.PangoAttrFloat // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_float(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrFloat

	if cret != nil {
		goret = UnsafeAttrFloatFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsFontDesc wraps pango_attribute_as_font_desc
// 
// The function returns the following values:
// 
// 	- goret *AttrFontDesc (nullable) 
//
// Returns the attribute cast to `PangoAttrFontDesc`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsFontDesc() *AttrFontDesc {
	var carg0 *C.PangoAttribute    // in, none, converted
	var cret  *C.PangoAttrFontDesc // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_font_desc(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrFontDesc

	if cret != nil {
		goret = UnsafeAttrFontDescFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsFontFeatures wraps pango_attribute_as_font_features
// 
// The function returns the following values:
// 
// 	- goret *AttrFontFeatures (nullable) 
//
// Returns the attribute cast to `PangoAttrFontFeatures`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsFontFeatures() *AttrFontFeatures {
	var carg0 *C.PangoAttribute        // in, none, converted
	var cret  *C.PangoAttrFontFeatures // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_font_features(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrFontFeatures

	if cret != nil {
		goret = UnsafeAttrFontFeaturesFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsInt wraps pango_attribute_as_int
// 
// The function returns the following values:
// 
// 	- goret *AttrInt (nullable) 
//
// Returns the attribute cast to `PangoAttrInt`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsInt() *AttrInt {
	var carg0 *C.PangoAttribute // in, none, converted
	var cret  *C.PangoAttrInt   // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_int(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrInt

	if cret != nil {
		goret = UnsafeAttrIntFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsLanguage wraps pango_attribute_as_language
// 
// The function returns the following values:
// 
// 	- goret *AttrLanguage (nullable) 
//
// Returns the attribute cast to `PangoAttrLanguage`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsLanguage() *AttrLanguage {
	var carg0 *C.PangoAttribute    // in, none, converted
	var cret  *C.PangoAttrLanguage // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_language(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrLanguage

	if cret != nil {
		goret = UnsafeAttrLanguageFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsShape wraps pango_attribute_as_shape
// 
// The function returns the following values:
// 
// 	- goret *AttrShape (nullable) 
//
// Returns the attribute cast to `PangoAttrShape`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsShape() *AttrShape {
	var carg0 *C.PangoAttribute // in, none, converted
	var cret  *C.PangoAttrShape // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_shape(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrShape

	if cret != nil {
		goret = UnsafeAttrShapeFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsSize wraps pango_attribute_as_size
// 
// The function returns the following values:
// 
// 	- goret *AttrSize (nullable) 
//
// Returns the attribute cast to `PangoAttrSize`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsSize() *AttrSize {
	var carg0 *C.PangoAttribute // in, none, converted
	var cret  *C.PangoAttrSize  // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_size(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrSize

	if cret != nil {
		goret = UnsafeAttrSizeFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// AsString wraps pango_attribute_as_string
// 
// The function returns the following values:
// 
// 	- goret *AttrString (nullable) 
//
// Returns the attribute cast to `PangoAttrString`.
// 
// This is mainly useful for language bindings.
func (attr *Attribute) AsString() *AttrString {
	var carg0 *C.PangoAttribute  // in, none, converted
	var cret  *C.PangoAttrString // return, none, converted, nullable

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_as_string(carg0)
	runtime.KeepAlive(attr)

	var goret *AttrString

	if cret != nil {
		goret = UnsafeAttrStringFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// Copy wraps pango_attribute_copy
// 
// The function returns the following values:
// 
// 	- goret *Attribute 
//
// Make a copy of an attribute.
func (attr *Attribute) Copy() *Attribute {
	var carg0 *C.PangoAttribute // in, none, converted
	var cret  *C.PangoAttribute // return, full, converted

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))

	cret = C.pango_attribute_copy(carg0)
	runtime.KeepAlive(attr)

	var goret *Attribute

	goret = UnsafeAttributeFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Equal wraps pango_attribute_equal
// 
// The function takes the following parameters:
// 
// 	- attr2 *Attribute: another `PangoAttribute` 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Compare two attributes for equality.
// 
// This compares only the actual value of the two
// attributes and not the ranges that the attributes
// apply to.
func (attr1 *Attribute) Equal(attr2 *Attribute) bool {
	var carg0 *C.PangoAttribute // in, none, converted
	var carg1 *C.PangoAttribute // in, none, converted
	var cret  C.gboolean        // return

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr1))
	carg1 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr2))

	cret = C.pango_attribute_equal(carg0, carg1)
	runtime.KeepAlive(attr1)
	runtime.KeepAlive(attr2)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Init wraps pango_attribute_init
// 
// The function takes the following parameters:
// 
// 	- klass *AttrClass: a `PangoAttrClass` 
//
// Initializes @attr's klass to @klass, it's start_index to
// %PANGO_ATTR_INDEX_FROM_TEXT_BEGINNING and end_index to
// %PANGO_ATTR_INDEX_TO_TEXT_END such that the attribute applies
// to the entire text by default.
func (attr *Attribute) Init(klass *AttrClass) {
	var carg0 *C.PangoAttribute // in, none, converted
	var carg1 *C.PangoAttrClass // in, none, converted

	carg0 = (*C.PangoAttribute)(UnsafeAttributeToGlibNone(attr))
	carg1 = (*C.PangoAttrClass)(UnsafeAttrClassToGlibNone(klass))

	C.pango_attribute_init(carg0, carg1)
	runtime.KeepAlive(attr)
	runtime.KeepAlive(klass)
}

// Color wraps PangoColor
//
// The `PangoColor` structure is used to
// represent a color in an uncalibrated RGB color-space.
type Color struct {
	*color
}

// color is the struct that's finalized
type color struct {
	native *C.PangoColor
}

var _ gobject.GoValueInitializer = (*Color)(nil)

func marshalColor(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeColorFromGlibBorrow(b), nil
}

func (r *Color) GoValueType() gobject.Type {
	return TypeColor
}

func (r *Color) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeColorFromGlibBorrow is used to convert raw C.PangoColor pointers to go. This is used by the bindings internally.
func UnsafeColorFromGlibBorrow(p unsafe.Pointer) *Color {
	return &Color{&color{(*C.PangoColor)(p)}}
}

// UnsafeColorFromGlibNone is used to convert raw C.PangoColor pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeColorFromGlibNone(p unsafe.Pointer) *Color {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeColorFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.color,
		func (intern *color) {
			C.pango_color_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeColorFromGlibFull is used to convert raw C.PangoColor pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeColorFromGlibFull(p unsafe.Pointer) *Color {
	wrapped := UnsafeColorFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.color,
		func (intern *color) {
			C.pango_color_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeColorFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Color] is expected to work anymore.
func UnsafeColorFree(c *Color) {
	C.pango_color_free(c.native)
}

// UnsafeColorToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeColorToGlibNone(c *Color) unsafe.Pointer {
	return unsafe.Pointer(c.native)
}

// UnsafeColorToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeColorToGlibFull(c *Color) unsafe.Pointer {
	runtime.SetFinalizer(c.color, nil)
	_p := unsafe.Pointer(c.native)
	c.native = nil // Color is invalid from here on
	return _p
}

// Copy wraps pango_color_copy
// 
// The function returns the following values:
// 
// 	- goret *Color (nullable) 
//
// Creates a copy of @src.
// 
// The copy should be freed with [method@Pango.Color.free].
// Primarily used by language bindings, not that useful
// otherwise (since colors can just be copied by assignment
// in C).
func (src *Color) Copy() *Color {
	var carg0 *C.PangoColor // in, none, converted
	var cret  *C.PangoColor // return, full, converted, nullable

	carg0 = (*C.PangoColor)(UnsafeColorToGlibNone(src))

	cret = C.pango_color_copy(carg0)
	runtime.KeepAlive(src)

	var goret *Color

	if cret != nil {
		goret = UnsafeColorFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Parse wraps pango_color_parse
// 
// The function takes the following parameters:
// 
// 	- spec string: a string specifying the new color 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Fill in the fields of a color from a string specification.
// 
// The string can either one of a large set of standard names.
// (Taken from the CSS Color [specification](https://www.w3.org/TR/css-color-4/#named-colors),
// or it can be a value in the form `#rgb`, `#rrggbb`,
// `#rrrgggbbb` or `#rrrrggggbbbb`, where `r`, `g` and `b`
// are hex digits of the red, green, and blue components
// of the color, respectively. (White in the four forms is
// `#fff`, `#ffffff`, `#fffffffff` and `#ffffffffffff`.)
func (color *Color) Parse(spec string) bool {
	var carg0 *C.PangoColor // in, none, converted
	var carg1 *C.char       // in, none, string, casted *C.gchar
	var cret  C.gboolean    // return

	carg0 = (*C.PangoColor)(UnsafeColorToGlibNone(color))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(spec)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_color_parse(carg0, carg1)
	runtime.KeepAlive(color)
	runtime.KeepAlive(spec)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// ParseWithAlpha wraps pango_color_parse_with_alpha
// 
// The function takes the following parameters:
// 
// 	- spec string: a string specifying the new color 
// 
// The function returns the following values:
// 
// 	- alpha uint16: return location for alpha 
// 	- goret bool 
//
// Fill in the fields of a color from a string specification.
// 
// The string can either one of a large set of standard names.
// (Taken from the CSS Color [specification](https://www.w3.org/TR/css-color-4/#named-colors),
// or it can be a hexadecimal value in the form `#rgb`,
// `#rrggbb`, `#rrrgggbbb` or `#rrrrggggbbbb` where `r`, `g`
// and `b` are hex digits of the red, green, and blue components
// of the color, respectively. (White in the four forms is
// `#fff`, `#ffffff`, `#fffffffff` and `#ffffffffffff`.)
// 
// Additionally, parse strings of the form `#rgba`, `#rrggbbaa`,
// `#rrrrggggbbbbaaaa`, if @alpha is not %NULL, and set @alpha
// to the value specified by the hex digits for `a`. If no alpha
// component is found in @spec, @alpha is set to 0xffff (for a
// solid color).
func (color *Color) ParseWithAlpha(spec string) (uint16, bool) {
	var carg0 *C.PangoColor // in, none, converted
	var carg2 *C.char       // in, none, string, casted *C.gchar
	var carg1 C.guint16     // out, full, casted
	var cret  C.gboolean    // return

	carg0 = (*C.PangoColor)(UnsafeColorToGlibNone(color))
	carg2 = (*C.char)(unsafe.Pointer(C.CString(spec)))
	defer C.free(unsafe.Pointer(carg2))

	cret = C.pango_color_parse_with_alpha(carg0, &carg1, carg2)
	runtime.KeepAlive(color)
	runtime.KeepAlive(spec)

	var alpha uint16
	var goret bool

	alpha = uint16(carg1)
	if cret != 0 {
		goret = true
	}

	return alpha, goret
}

// ToString wraps pango_color_to_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Returns a textual specification of @color.
// 
// The string is in the hexadecimal form `#rrrrggggbbbb`,
// where `r`, `g` and `b` are hex digits representing the
// red, green, and blue components respectively.
func (color *Color) ToString() string {
	var carg0 *C.PangoColor // in, none, converted
	var cret  *C.char       // return, full, string, casted *C.gchar

	carg0 = (*C.PangoColor)(UnsafeColorToGlibNone(color))

	cret = C.pango_color_to_string(carg0)
	runtime.KeepAlive(color)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return goret
}

// ContextClass wraps PangoContextClass
// 
// ContextClass is the type struct for [Context]
type ContextClass struct {
	*contextClass
}

// contextClass is the struct that's finalized
type contextClass struct {
	native *C.PangoContextClass
}

// UnsafeContextClassFromGlibBorrow is used to convert raw C.PangoContextClass pointers to go. This is used by the bindings internally.
func UnsafeContextClassFromGlibBorrow(p unsafe.Pointer) *ContextClass {
	return &ContextClass{&contextClass{(*C.PangoContextClass)(p)}}
}

// UnsafeContextClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [ContextClass] is expected to work anymore.
func UnsafeContextClassFree(c *ContextClass) {
	C.free(unsafe.Pointer(c.native))
}

// UnsafeContextClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeContextClassToGlibNone(c *ContextClass) unsafe.Pointer {
	return unsafe.Pointer(c.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (c *ContextClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeContextClassToGlibNone(c))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *ContextClass) {}, c)
	return parent
}

// FontClass wraps PangoFontClass
// 
// FontClass is the type struct for [Font]
type FontClass struct {
	*fontClass
}

// fontClass is the struct that's finalized
type fontClass struct {
	native *C.PangoFontClass
}

// UnsafeFontClassFromGlibBorrow is used to convert raw C.PangoFontClass pointers to go. This is used by the bindings internally.
func UnsafeFontClassFromGlibBorrow(p unsafe.Pointer) *FontClass {
	return &FontClass{&fontClass{(*C.PangoFontClass)(p)}}
}

// UnsafeFontClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontClass] is expected to work anymore.
func UnsafeFontClassFree(f *FontClass) {
	C.free(unsafe.Pointer(f.native))
}

// UnsafeFontClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontClassToGlibNone(f *FontClass) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (f *FontClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeFontClassToGlibNone(f))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *FontClass) {}, f)
	return parent
}

// FontDescription wraps PangoFontDescription
//
// A `PangoFontDescription` describes a font in an implementation-independent
// manner.
// 
// `PangoFontDescription` structures are used both to list what fonts are
// available on the system and also for specifying the characteristics of
// a font to load.
type FontDescription struct {
	*fontDescription
}

// fontDescription is the struct that's finalized
type fontDescription struct {
	native *C.PangoFontDescription
}

var _ gobject.GoValueInitializer = (*FontDescription)(nil)

func marshalFontDescription(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeFontDescriptionFromGlibBorrow(b), nil
}

func (r *FontDescription) GoValueType() gobject.Type {
	return TypeFontDescription
}

func (r *FontDescription) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeFontDescriptionFromGlibBorrow is used to convert raw C.PangoFontDescription pointers to go. This is used by the bindings internally.
func UnsafeFontDescriptionFromGlibBorrow(p unsafe.Pointer) *FontDescription {
	return &FontDescription{&fontDescription{(*C.PangoFontDescription)(p)}}
}

// UnsafeFontDescriptionFromGlibNone is used to convert raw C.PangoFontDescription pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeFontDescriptionFromGlibNone(p unsafe.Pointer) *FontDescription {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeFontDescriptionFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.fontDescription,
		func (intern *fontDescription) {
			C.pango_font_description_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeFontDescriptionFromGlibFull is used to convert raw C.PangoFontDescription pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeFontDescriptionFromGlibFull(p unsafe.Pointer) *FontDescription {
	wrapped := UnsafeFontDescriptionFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.fontDescription,
		func (intern *fontDescription) {
			C.pango_font_description_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeFontDescriptionFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontDescription] is expected to work anymore.
func UnsafeFontDescriptionFree(f *FontDescription) {
	C.pango_font_description_free(f.native)
}

// UnsafeFontDescriptionToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontDescriptionToGlibNone(f *FontDescription) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// UnsafeFontDescriptionToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeFontDescriptionToGlibFull(f *FontDescription) unsafe.Pointer {
	runtime.SetFinalizer(f.fontDescription, nil)
	_p := unsafe.Pointer(f.native)
	f.native = nil // FontDescription is invalid from here on
	return _p
}

// NewFontDescription wraps pango_font_description_new
// 
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Creates a new font description structure with all fields unset.
func NewFontDescription() *FontDescription {
	var cret *C.PangoFontDescription // return, full, converted

	cret = C.pango_font_description_new()

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// FontDescriptionFromString wraps pango_font_description_from_string
// 
// The function takes the following parameters:
// 
// 	- str string: string representation of a font description. 
// 
// The function returns the following values:
// 
// 	- goret *FontDescription 
//
// Creates a new font description from a string representation.
// 
// The string must have the form
// 
//     [FAMILY-LIST] [STYLE-OPTIONS] [SIZE] [VARIATIONS] [FEATURES]
// 
// where FAMILY-LIST is a comma-separated list of families optionally
// terminated by a comma, STYLE_OPTIONS is a whitespace-separated list
// of words where each word describes one of style, variant, weight,
// stretch, or gravity, and SIZE is a decimal number (size in points)
// or optionally followed by the unit modifier "px" for absolute size.
// 
// The following words are understood as styles:
// "Normal", "Roman", "Oblique", "Italic".
// 
// The following words are understood as variants:
// "Small-Caps", "All-Small-Caps", "Petite-Caps", "All-Petite-Caps",
// "Unicase", "Title-Caps".
// 
// The following words are understood as weights:
// "Thin", "Ultra-Light", "Extra-Light", "Light", "Semi-Light",
// "Demi-Light", "Book", "Regular", "Medium", "Semi-Bold", "Demi-Bold",
// "Bold", "Ultra-Bold", "Extra-Bold", "Heavy", "Black", "Ultra-Black",
// "Extra-Black".
// 
// The following words are understood as stretch values:
// "Ultra-Condensed", "Extra-Condensed", "Condensed", "Semi-Condensed",
// "Semi-Expanded", "Expanded", "Extra-Expanded", "Ultra-Expanded".
// 
// The following words are understood as gravity values:
// "Not-Rotated", "South", "Upside-Down", "North", "Rotated-Left",
// "East", "Rotated-Right", "West".
// 
// VARIATIONS is a comma-separated list of font variations
// of the form @‍axis1=value,axis2=value,...
// 
// FEATURES is a comma-separated list of font features of the form
// \#‍feature1=value,feature2=value,...
// 
// Any one of the options may be absent. If FAMILY-LIST is absent, then
// the family_name field of the resulting font description will be
// initialized to %NULL. If STYLE-OPTIONS is missing, then all style
// options will be set to the default values. If SIZE is missing, the
// size in the resulting font description will be set to 0.
// 
// A typical example:
// 
//     Cantarell Italic Light 15 @‍wght=200 #‍tnum=1
func FontDescriptionFromString(str string) *FontDescription {
	var carg1 *C.char                 // in, none, string, casted *C.gchar
	var cret  *C.PangoFontDescription // return, full, converted

	carg1 = (*C.char)(unsafe.Pointer(C.CString(str)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_font_description_from_string(carg1)
	runtime.KeepAlive(str)

	var goret *FontDescription

	goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// BetterMatch wraps pango_font_description_better_match
// 
// The function takes the following parameters:
// 
// 	- oldMatch *FontDescription (nullable): a `PangoFontDescription`, or %NULL 
// 	- newMatch *FontDescription: a `PangoFontDescription` 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Determines if the style attributes of @new_match are a closer match
// for @desc than those of @old_match are, or if @old_match is %NULL,
// determines if @new_match is a match at all.
// 
// Approximate matching is done for weight and style; other style attributes
// must match exactly. Style attributes are all attributes other than family
// and size-related attributes. Approximate matching for style considers
// %PANGO_STYLE_OBLIQUE and %PANGO_STYLE_ITALIC as matches, but not as good
// a match as when the styles are equal.
// 
// Note that @old_match must match @desc.
func (desc *FontDescription) BetterMatch(oldMatch *FontDescription, newMatch *FontDescription) bool {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted, nullable
	var carg2 *C.PangoFontDescription // in, none, converted
	var cret  C.gboolean              // return

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	if oldMatch != nil {
		carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(oldMatch))
	}
	carg2 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(newMatch))

	cret = C.pango_font_description_better_match(carg0, carg1, carg2)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(oldMatch)
	runtime.KeepAlive(newMatch)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Copy wraps pango_font_description_copy
// 
// The function returns the following values:
// 
// 	- goret *FontDescription (nullable) 
//
// Make a copy of a `PangoFontDescription`.
func (desc *FontDescription) Copy() *FontDescription {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.PangoFontDescription // return, full, converted, nullable

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_copy(carg0)
	runtime.KeepAlive(desc)

	var goret *FontDescription

	if cret != nil {
		goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// CopyStatic wraps pango_font_description_copy_static
// 
// The function returns the following values:
// 
// 	- goret *FontDescription (nullable) 
//
// Make a copy of a `PangoFontDescription`, but don't duplicate
// allocated fields.
// 
// This is like [method@Pango.FontDescription.copy], but only a shallow
// copy is made of the family name and other allocated fields. The result
// can only be used until @desc is modified or freed. This is meant
// to be used when the copy is only needed temporarily.
func (desc *FontDescription) CopyStatic() *FontDescription {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.PangoFontDescription // return, full, converted, nullable

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_copy_static(carg0)
	runtime.KeepAlive(desc)

	var goret *FontDescription

	if cret != nil {
		goret = UnsafeFontDescriptionFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Equal wraps pango_font_description_equal
// 
// The function takes the following parameters:
// 
// 	- desc2 *FontDescription: another `PangoFontDescription` 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Compares two font descriptions for equality.
// 
// Two font descriptions are considered equal if the fonts they describe
// are provably identical. This means that their masks do not have to match,
// as long as other fields are all the same. (Two font descriptions may
// result in identical fonts being loaded, but still compare %FALSE.)
func (desc1 *FontDescription) Equal(desc2 *FontDescription) bool {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted
	var cret  C.gboolean              // return

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc1))
	carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc2))

	cret = C.pango_font_description_equal(carg0, carg1)
	runtime.KeepAlive(desc1)
	runtime.KeepAlive(desc2)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetFamily wraps pango_font_description_get_family
// 
// The function returns the following values:
// 
// 	- goret string (nullable) 
//
// Gets the family name field of a font description.
// 
// See [method@Pango.FontDescription.set_family].
func (desc *FontDescription) GetFamily() string {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.char                 // return, none, string, casted *C.gchar, nullable

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_family(carg0)
	runtime.KeepAlive(desc)

	var goret string

	if cret != nil {
		goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	}

	return goret
}

// GetFeatures wraps pango_font_description_get_features
// 
// The function returns the following values:
// 
// 	- goret string (nullable) 
//
// Gets the features field of a font description.
// 
// See [method@Pango.FontDescription.set_features].
func (desc *FontDescription) GetFeatures() string {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.char                 // return, none, string, casted *C.gchar, nullable

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_features(carg0)
	runtime.KeepAlive(desc)

	var goret string

	if cret != nil {
		goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	}

	return goret
}

// GetGravity wraps pango_font_description_get_gravity
// 
// The function returns the following values:
// 
// 	- goret Gravity 
//
// Gets the gravity field of a font description.
// 
// See [method@Pango.FontDescription.set_gravity].
func (desc *FontDescription) GetGravity() Gravity {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.PangoGravity          // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_gravity(carg0)
	runtime.KeepAlive(desc)

	var goret Gravity

	goret = Gravity(cret)

	return goret
}

// GetSetFields wraps pango_font_description_get_set_fields
// 
// The function returns the following values:
// 
// 	- goret FontMask 
//
// Determines which fields in a font description have been set.
func (desc *FontDescription) GetSetFields() FontMask {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.PangoFontMask         // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_set_fields(carg0)
	runtime.KeepAlive(desc)

	var goret FontMask

	goret = FontMask(cret)

	return goret
}

// GetSize wraps pango_font_description_get_size
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the size field of a font description.
// 
// See [method@Pango.FontDescription.set_size].
func (desc *FontDescription) GetSize() int32 {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.gint                  // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_size(carg0)
	runtime.KeepAlive(desc)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetSizeIsAbsolute wraps pango_font_description_get_size_is_absolute
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Determines whether the size of the font is in points (not absolute)
// or device units (absolute).
// 
// See [method@Pango.FontDescription.set_size]
// and [method@Pango.FontDescription.set_absolute_size].
func (desc *FontDescription) GetSizeIsAbsolute() bool {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.gboolean              // return

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_size_is_absolute(carg0)
	runtime.KeepAlive(desc)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetStretch wraps pango_font_description_get_stretch
// 
// The function returns the following values:
// 
// 	- goret Stretch 
//
// Gets the stretch field of a font description.
// 
// See [method@Pango.FontDescription.set_stretch].
func (desc *FontDescription) GetStretch() Stretch {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.PangoStretch          // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_stretch(carg0)
	runtime.KeepAlive(desc)

	var goret Stretch

	goret = Stretch(cret)

	return goret
}

// GetStyle wraps pango_font_description_get_style
// 
// The function returns the following values:
// 
// 	- goret Style 
//
// Gets the style field of a `PangoFontDescription`.
// 
// See [method@Pango.FontDescription.set_style].
func (desc *FontDescription) GetStyle() Style {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.PangoStyle            // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_style(carg0)
	runtime.KeepAlive(desc)

	var goret Style

	goret = Style(cret)

	return goret
}

// GetVariant wraps pango_font_description_get_variant
// 
// The function returns the following values:
// 
// 	- goret Variant 
//
// Gets the variant field of a `PangoFontDescription`.
// 
// See [method@Pango.FontDescription.set_variant].
func (desc *FontDescription) GetVariant() Variant {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.PangoVariant          // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_variant(carg0)
	runtime.KeepAlive(desc)

	var goret Variant

	goret = Variant(cret)

	return goret
}

// GetVariations wraps pango_font_description_get_variations
// 
// The function returns the following values:
// 
// 	- goret string (nullable) 
//
// Gets the variations field of a font description.
// 
// See [method@Pango.FontDescription.set_variations].
func (desc *FontDescription) GetVariations() string {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.char                 // return, none, string, casted *C.gchar, nullable

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_variations(carg0)
	runtime.KeepAlive(desc)

	var goret string

	if cret != nil {
		goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	}

	return goret
}

// GetWeight wraps pango_font_description_get_weight
// 
// The function returns the following values:
// 
// 	- goret Weight 
//
// Gets the weight field of a font description.
// 
// See [method@Pango.FontDescription.set_weight].
func (desc *FontDescription) GetWeight() Weight {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.PangoWeight           // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_get_weight(carg0)
	runtime.KeepAlive(desc)

	var goret Weight

	goret = Weight(cret)

	return goret
}

// Hash wraps pango_font_description_hash
// 
// The function returns the following values:
// 
// 	- goret uint 
//
// Computes a hash of a `PangoFontDescription` structure.
// 
// This is suitable to be used, for example, as an argument
// to g_hash_table_new(). The hash value is independent of @desc-&gt;mask.
func (desc *FontDescription) Hash() uint {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  C.guint                 // return, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_hash(carg0)
	runtime.KeepAlive(desc)

	var goret uint

	goret = uint(cret)

	return goret
}

// Merge wraps pango_font_description_merge
// 
// The function takes the following parameters:
// 
// 	- descToMerge *FontDescription (nullable): the `PangoFontDescription` to merge from,
//   or %NULL 
// 	- replaceExisting bool: if %TRUE, replace fields in @desc with the
//   corresponding values from @desc_to_merge, even if they
//   are already exist. 
//
// Merges the fields that are set in @desc_to_merge into the fields in
// @desc.
// 
// If @replace_existing is %FALSE, only fields in @desc that
// are not already set are affected. If %TRUE, then fields that are
// already set will be replaced as well.
// 
// If @desc_to_merge is %NULL, this function performs nothing.
func (desc *FontDescription) Merge(descToMerge *FontDescription, replaceExisting bool) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted, nullable
	var carg2 C.gboolean              // in

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	if descToMerge != nil {
		carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(descToMerge))
	}
	if replaceExisting {
		carg2 = C.TRUE
	}

	C.pango_font_description_merge(carg0, carg1, carg2)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(descToMerge)
	runtime.KeepAlive(replaceExisting)
}

// MergeStatic wraps pango_font_description_merge_static
// 
// The function takes the following parameters:
// 
// 	- descToMerge *FontDescription: the `PangoFontDescription` to merge from 
// 	- replaceExisting bool: if %TRUE, replace fields in @desc with the
//   corresponding values from @desc_to_merge, even if they
//   are already exist. 
//
// Merges the fields that are set in @desc_to_merge into the fields in
// @desc, without copying allocated fields.
// 
// This is like [method@Pango.FontDescription.merge], but only a shallow copy
// is made of the family name and other allocated fields. @desc can only
// be used until @desc_to_merge is modified or freed. This is meant to
// be used when the merged font description is only needed temporarily.
func (desc *FontDescription) MergeStatic(descToMerge *FontDescription, replaceExisting bool) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.PangoFontDescription // in, none, converted
	var carg2 C.gboolean              // in

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(descToMerge))
	if replaceExisting {
		carg2 = C.TRUE
	}

	C.pango_font_description_merge_static(carg0, carg1, carg2)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(descToMerge)
	runtime.KeepAlive(replaceExisting)
}

// SetAbsoluteSize wraps pango_font_description_set_absolute_size
// 
// The function takes the following parameters:
// 
// 	- size float64: the new size, in Pango units. There are %PANGO_SCALE Pango units
//   in one device unit. For an output backend where a device unit is a pixel,
//   a @size value of 10 * PANGO_SCALE gives a 10 pixel font. 
//
// Sets the size field of a font description, in device units.
// 
// This is mutually exclusive with [method@Pango.FontDescription.set_size]
// which sets the font size in points.
func (desc *FontDescription) SetAbsoluteSize(size float64) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.double                // in, none, casted, casted C.gdouble

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.double(size)

	C.pango_font_description_set_absolute_size(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(size)
}

// SetFamily wraps pango_font_description_set_family
// 
// The function takes the following parameters:
// 
// 	- family string: a string representing the family name. 
//
// Sets the family name field of a font description.
// 
// The family
// name represents a family of related font styles, and will
// resolve to a particular `PangoFontFamily`. In some uses of
// `PangoFontDescription`, it is also possible to use a comma
// separated list of family names for this field.
func (desc *FontDescription) SetFamily(family string) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.char                 // in, none, string, casted *C.gchar

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(family)))
	defer C.free(unsafe.Pointer(carg1))

	C.pango_font_description_set_family(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(family)
}

// SetFamilyStatic wraps pango_font_description_set_family_static
// 
// The function takes the following parameters:
// 
// 	- family string: a string representing the family name 
//
// Sets the family name field of a font description, without copying the string.
// 
// This is like [method@Pango.FontDescription.set_family], except that no
// copy of @family is made. The caller must make sure that the
// string passed in stays around until @desc has been freed or the
// name is set again. This function can be used if @family is a static
// string such as a C string literal, or if @desc is only needed temporarily.
func (desc *FontDescription) SetFamilyStatic(family string) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.char                 // in, none, string, casted *C.gchar

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(family)))
	defer C.free(unsafe.Pointer(carg1))

	C.pango_font_description_set_family_static(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(family)
}

// SetFeatures wraps pango_font_description_set_features
// 
// The function takes the following parameters:
// 
// 	- features string (nullable): a string representing the features 
//
// Sets the features field of a font description.
// 
// OpenType font features allow to enable or disable certain optional
// features of a font, such as tabular numbers.
// 
// The format of the features string is comma-separated list of
// feature assignments, with each assignment being one of these forms:
// 
//     FEATURE=n
// 
// where FEATURE must be a 4 character tag that identifies and OpenType
// feature, and n an integer (depending on the feature, the allowed
// values may be 0, 1 or bigger numbers). Unknown features are ignored.
// 
// Note that font features set in this way are enabled for the entire text
// that is using the font, which is not appropriate for all OpenType features.
// The intended use case is to select character variations (features cv01 - c99),
// style sets (ss01 - ss20) and the like.
// 
// Pango does not currently have a way to find supported OpenType features
// of a font. Both harfbuzz and freetype have API for this. See for example
// [hb_ot_layout_table_get_feature_tags](https://harfbuzz.github.io/harfbuzz-hb-ot-layout.html#hb-ot-layout-table-get-feature-tags).
// 
// Features that are not supported by the font are silently ignored.
func (desc *FontDescription) SetFeatures(features string) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.char                 // in, none, string, nullable-string

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	if features != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(features)))
		defer C.free(unsafe.Pointer(carg1))
	}

	C.pango_font_description_set_features(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(features)
}

// SetFeaturesStatic wraps pango_font_description_set_features_static
// 
// The function takes the following parameters:
// 
// 	- features string: a string representing the features 
//
// Sets the features field of a font description.
// 
// This is like [method@Pango.FontDescription.set_features], except
// that no copy of @featuresis made. The caller must make sure that
// the string passed in stays around until @desc has been freed
// or the name is set again. This function can be used if
// @features is a static string such as a C string literal,
// or if @desc is only needed temporarily.
func (desc *FontDescription) SetFeaturesStatic(features string) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.char                 // in, none, string, casted *C.gchar

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(features)))
	defer C.free(unsafe.Pointer(carg1))

	C.pango_font_description_set_features_static(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(features)
}

// SetGravity wraps pango_font_description_set_gravity
// 
// The function takes the following parameters:
// 
// 	- gravity Gravity: the gravity for the font description. 
//
// Sets the gravity field of a font description.
// 
// The gravity field
// specifies how the glyphs should be rotated. If @gravity is
// %PANGO_GRAVITY_AUTO, this actually unsets the gravity mask on
// the font description.
// 
// This function is seldom useful to the user. Gravity should normally
// be set on a `PangoContext`.
func (desc *FontDescription) SetGravity(gravity Gravity) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.PangoGravity          // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.PangoGravity(gravity)

	C.pango_font_description_set_gravity(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(gravity)
}

// SetSize wraps pango_font_description_set_size
// 
// The function takes the following parameters:
// 
// 	- size int32: the size of the font in points, scaled by %PANGO_SCALE.
//   (That is, a @size value of 10 * PANGO_SCALE is a 10 point font.
//   The conversion factor between points and device units depends on
//   system configuration and the output device. For screen display, a
//   logical DPI of 96 is common, in which case a 10 point font corresponds
//   to a 10 * (96 / 72) = 13.3 pixel font.
//   Use [method@Pango.FontDescription.set_absolute_size] if you need
//   a particular size in device units. 
//
// Sets the size field of a font description in fractional points.
// 
// This is mutually exclusive with
// [method@Pango.FontDescription.set_absolute_size].
func (desc *FontDescription) SetSize(size int32) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.gint                  // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.gint(size)

	C.pango_font_description_set_size(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(size)
}

// SetStretch wraps pango_font_description_set_stretch
// 
// The function takes the following parameters:
// 
// 	- stretch Stretch: the stretch for the font description 
//
// Sets the stretch field of a font description.
// 
// The [enum@Pango.Stretch] field specifies how narrow or
// wide the font should be.
func (desc *FontDescription) SetStretch(stretch Stretch) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.PangoStretch          // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.PangoStretch(stretch)

	C.pango_font_description_set_stretch(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(stretch)
}

// SetStyle wraps pango_font_description_set_style
// 
// The function takes the following parameters:
// 
// 	- style Style: the style for the font description 
//
// Sets the style field of a `PangoFontDescription`.
// 
// The [enum@Pango.Style] enumeration describes whether the font is
// slanted and the manner in which it is slanted; it can be either
// %PANGO_STYLE_NORMAL, %PANGO_STYLE_ITALIC, or %PANGO_STYLE_OBLIQUE.
// 
// Most fonts will either have a italic style or an oblique style,
// but not both, and font matching in Pango will match italic
// specifications with oblique fonts and vice-versa if an exact
// match is not found.
func (desc *FontDescription) SetStyle(style Style) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.PangoStyle            // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.PangoStyle(style)

	C.pango_font_description_set_style(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(style)
}

// SetVariant wraps pango_font_description_set_variant
// 
// The function takes the following parameters:
// 
// 	- variant Variant: the variant type for the font description. 
//
// Sets the variant field of a font description.
// 
// The [enum@Pango.Variant] can either be %PANGO_VARIANT_NORMAL
// or %PANGO_VARIANT_SMALL_CAPS.
func (desc *FontDescription) SetVariant(variant Variant) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.PangoVariant          // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.PangoVariant(variant)

	C.pango_font_description_set_variant(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(variant)
}

// SetVariations wraps pango_font_description_set_variations
// 
// The function takes the following parameters:
// 
// 	- variations string (nullable): a string representing the variations 
//
// Sets the variations field of a font description.
// 
// OpenType font variations allow to select a font instance by
// specifying values for a number of axes, such as width or weight.
// 
// The format of the variations string is
// 
//     AXIS1=VALUE,AXIS2=VALUE...
// 
// with each AXIS a 4 character tag that identifies a font axis,
// and each VALUE a floating point number. Unknown axes are ignored,
// and values are clamped to their allowed range.
// 
// Pango does not currently have a way to find supported axes of
// a font. Both harfbuzz and freetype have API for this. See
// for example [hb_ot_var_get_axis_infos](https://harfbuzz.github.io/harfbuzz-hb-ot-var.html#hb-ot-var-get-axis-infos).
func (desc *FontDescription) SetVariations(variations string) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.char                 // in, none, string, nullable-string

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	if variations != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(variations)))
		defer C.free(unsafe.Pointer(carg1))
	}

	C.pango_font_description_set_variations(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(variations)
}

// SetVariationsStatic wraps pango_font_description_set_variations_static
// 
// The function takes the following parameters:
// 
// 	- variations string: a string representing the variations 
//
// Sets the variations field of a font description.
// 
// This is like [method@Pango.FontDescription.set_variations], except
// that no copy of @variations is made. The caller must make sure that
// the string passed in stays around until @desc has been freed
// or the name is set again. This function can be used if
// @variations is a static string such as a C string literal,
// or if @desc is only needed temporarily.
func (desc *FontDescription) SetVariationsStatic(variations string) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 *C.char                 // in, none, string, casted *C.gchar

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(variations)))
	defer C.free(unsafe.Pointer(carg1))

	C.pango_font_description_set_variations_static(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(variations)
}

// SetWeight wraps pango_font_description_set_weight
// 
// The function takes the following parameters:
// 
// 	- weight Weight: the weight for the font description. 
//
// Sets the weight field of a font description.
// 
// The weight field
// specifies how bold or light the font should be. In addition
// to the values of the [enum@Pango.Weight] enumeration, other
// intermediate numeric values are possible.
func (desc *FontDescription) SetWeight(weight Weight) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.PangoWeight           // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.PangoWeight(weight)

	C.pango_font_description_set_weight(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(weight)
}

// ToFilename wraps pango_font_description_to_filename
// 
// The function returns the following values:
// 
// 	- goret string (nullable) 
//
// Creates a filename representation of a font description.
// 
// The filename is identical to the result from calling
// [method@Pango.FontDescription.to_string], but with underscores
// instead of characters that are untypical in filenames, and in
// lower case only.
func (desc *FontDescription) ToFilename() string {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.char                 // return, full, string, casted *C.gchar, nullable

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_to_filename(carg0)
	runtime.KeepAlive(desc)

	var goret string

	if cret != nil {
		goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
		defer C.free(unsafe.Pointer(cret))
	}

	return goret
}

// ToString wraps pango_font_description_to_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Creates a string representation of a font description.
// 
// See [func@Pango.FontDescription.from_string] for a description
// of the format of the string representation. The family list in
// the string description will only have a terminating comma if
// the last word of the list is a valid style option.
func (desc *FontDescription) ToString() string {
	var carg0 *C.PangoFontDescription // in, none, converted
	var cret  *C.char                 // return, full, string, casted *C.gchar

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))

	cret = C.pango_font_description_to_string(carg0)
	runtime.KeepAlive(desc)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return goret
}

// UnsetFields wraps pango_font_description_unset_fields
// 
// The function takes the following parameters:
// 
// 	- toUnset FontMask: bitmask of fields in the @desc to unset. 
//
// Unsets some of the fields in a `PangoFontDescription`.
// 
// The unset fields will get back to their default values.
func (desc *FontDescription) UnsetFields(toUnset FontMask) {
	var carg0 *C.PangoFontDescription // in, none, converted
	var carg1 C.PangoFontMask         // in, none, casted

	carg0 = (*C.PangoFontDescription)(UnsafeFontDescriptionToGlibNone(desc))
	carg1 = C.PangoFontMask(toUnset)

	C.pango_font_description_unset_fields(carg0, carg1)
	runtime.KeepAlive(desc)
	runtime.KeepAlive(toUnset)
}

// FontFaceClass wraps PangoFontFaceClass
// 
// FontFaceClass is the type struct for [FontFace]
type FontFaceClass struct {
	*fontFaceClass
}

// fontFaceClass is the struct that's finalized
type fontFaceClass struct {
	native *C.PangoFontFaceClass
}

// UnsafeFontFaceClassFromGlibBorrow is used to convert raw C.PangoFontFaceClass pointers to go. This is used by the bindings internally.
func UnsafeFontFaceClassFromGlibBorrow(p unsafe.Pointer) *FontFaceClass {
	return &FontFaceClass{&fontFaceClass{(*C.PangoFontFaceClass)(p)}}
}

// UnsafeFontFaceClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontFaceClass] is expected to work anymore.
func UnsafeFontFaceClassFree(f *FontFaceClass) {
	C.free(unsafe.Pointer(f.native))
}

// UnsafeFontFaceClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontFaceClassToGlibNone(f *FontFaceClass) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (f *FontFaceClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeFontFaceClassToGlibNone(f))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *FontFaceClass) {}, f)
	return parent
}

// FontFamilyClass wraps PangoFontFamilyClass
// 
// FontFamilyClass is the type struct for [FontFamily]
type FontFamilyClass struct {
	*fontFamilyClass
}

// fontFamilyClass is the struct that's finalized
type fontFamilyClass struct {
	native *C.PangoFontFamilyClass
}

// UnsafeFontFamilyClassFromGlibBorrow is used to convert raw C.PangoFontFamilyClass pointers to go. This is used by the bindings internally.
func UnsafeFontFamilyClassFromGlibBorrow(p unsafe.Pointer) *FontFamilyClass {
	return &FontFamilyClass{&fontFamilyClass{(*C.PangoFontFamilyClass)(p)}}
}

// UnsafeFontFamilyClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontFamilyClass] is expected to work anymore.
func UnsafeFontFamilyClassFree(f *FontFamilyClass) {
	C.free(unsafe.Pointer(f.native))
}

// UnsafeFontFamilyClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontFamilyClassToGlibNone(f *FontFamilyClass) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (f *FontFamilyClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeFontFamilyClassToGlibNone(f))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *FontFamilyClass) {}, f)
	return parent
}

// FontMapClass wraps PangoFontMapClass
//
// The `PangoFontMapClass` structure holds the virtual functions for
// a particular `PangoFontMap` implementation.
// 
// FontMapClass is the type struct for [FontMap]
type FontMapClass struct {
	*fontMapClass
}

// fontMapClass is the struct that's finalized
type fontMapClass struct {
	native *C.PangoFontMapClass
}

// UnsafeFontMapClassFromGlibBorrow is used to convert raw C.PangoFontMapClass pointers to go. This is used by the bindings internally.
func UnsafeFontMapClassFromGlibBorrow(p unsafe.Pointer) *FontMapClass {
	return &FontMapClass{&fontMapClass{(*C.PangoFontMapClass)(p)}}
}

// UnsafeFontMapClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontMapClass] is expected to work anymore.
func UnsafeFontMapClassFree(f *FontMapClass) {
	C.free(unsafe.Pointer(f.native))
}

// UnsafeFontMapClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontMapClassToGlibNone(f *FontMapClass) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (f *FontMapClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeFontMapClassToGlibNone(f))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *FontMapClass) {}, f)
	return parent
}

// FontMetrics wraps PangoFontMetrics
//
// A `PangoFontMetrics` structure holds the overall metric information
// for a font.
// 
// The information in a `PangoFontMetrics` structure may be restricted
// to a script. The fields of this structure are private to implementations
// of a font backend. See the documentation of the corresponding getters
// for documentation of their meaning.
// 
// For an overview of the most important metrics, see:
// 
// &lt;picture&gt;
//   &lt;source srcset="fontmetrics-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Font metrics" src="fontmetrics-light.png"&gt;
// &lt;/picture&gt;
type FontMetrics struct {
	*fontMetrics
}

// fontMetrics is the struct that's finalized
type fontMetrics struct {
	native *C.PangoFontMetrics
}

var _ gobject.GoValueInitializer = (*FontMetrics)(nil)

func marshalFontMetrics(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeFontMetricsFromGlibBorrow(b), nil
}

func (r *FontMetrics) GoValueType() gobject.Type {
	return TypeFontMetrics
}

func (r *FontMetrics) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeFontMetricsFromGlibBorrow is used to convert raw C.PangoFontMetrics pointers to go. This is used by the bindings internally.
func UnsafeFontMetricsFromGlibBorrow(p unsafe.Pointer) *FontMetrics {
	return &FontMetrics{&fontMetrics{(*C.PangoFontMetrics)(p)}}
}

// UnsafeFontMetricsFromGlibNone is used to convert raw C.PangoFontMetrics pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeFontMetricsFromGlibNone(p unsafe.Pointer) *FontMetrics {
	C.pango_font_metrics_ref((*C.PangoFontMetrics)(p))
	wrapped := UnsafeFontMetricsFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.fontMetrics,
		func (intern *fontMetrics) {
			C.pango_font_metrics_unref(intern.native)
		},
	)
	return wrapped
}

// UnsafeFontMetricsFromGlibFull is used to convert raw C.PangoFontMetrics pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeFontMetricsFromGlibFull(p unsafe.Pointer) *FontMetrics {
	wrapped := UnsafeFontMetricsFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.fontMetrics,
		func (intern *fontMetrics) {
			C.pango_font_metrics_unref(intern.native)
		},
	)
	return wrapped
}

// UnsafeFontMetricsRef increases the refcount on the underlying resource. This is used by the bindings internally.
// 
// When this is called without an associated call to [FontMetrics.UnsafeFontMetricsUnref], then [FontMetrics] will leak memory.
func UnsafeFontMetricsRef(f *FontMetrics) {
	C.pango_font_metrics_ref(f.native)
}

// UnsafeFontMetricsUnref unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontMetrics] is expected to work anymore.
func UnsafeFontMetricsUnref(f *FontMetrics) {
	C.pango_font_metrics_unref(f.native)
}

// UnsafeFontMetricsToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontMetricsToGlibNone(f *FontMetrics) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// UnsafeFontMetricsToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeFontMetricsToGlibFull(f *FontMetrics) unsafe.Pointer {
	runtime.SetFinalizer(f.fontMetrics, nil)
	_p := unsafe.Pointer(f.native)
	f.native = nil // FontMetrics is invalid from here on
	return _p
}

// GetApproximateCharWidth wraps pango_font_metrics_get_approximate_char_width
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the approximate character width for a font metrics structure.
// 
// This is merely a representative value useful, for example, for
// determining the initial size for a window. Actual characters in
// text will be wider and narrower than this.
func (metrics *FontMetrics) GetApproximateCharWidth() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_approximate_char_width(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetApproximateDigitWidth wraps pango_font_metrics_get_approximate_digit_width
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the approximate digit width for a font metrics structure.
// 
// This is merely a representative value useful, for example, for
// determining the initial size for a window. Actual digits in
// text can be wider or narrower than this, though this value
// is generally somewhat more accurate than the result of
// pango_font_metrics_get_approximate_char_width() for digits.
func (metrics *FontMetrics) GetApproximateDigitWidth() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_approximate_digit_width(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetAscent wraps pango_font_metrics_get_ascent
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the ascent from a font metrics structure.
// 
// The ascent is the distance from the baseline to the logical top
// of a line of text. (The logical top may be above or below the top
// of the actual drawn ink. It is necessary to lay out the text to
// figure where the ink will be.)
func (metrics *FontMetrics) GetAscent() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_ascent(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetDescent wraps pango_font_metrics_get_descent
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the descent from a font metrics structure.
// 
// The descent is the distance from the baseline to the logical bottom
// of a line of text. (The logical bottom may be above or below the
// bottom of the actual drawn ink. It is necessary to lay out the text
// to figure where the ink will be.)
func (metrics *FontMetrics) GetDescent() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_descent(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetHeight wraps pango_font_metrics_get_height
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the line height from a font metrics structure.
// 
// The line height is the recommended distance between successive
// baselines in wrapped text using this font.
// 
// If the line height is not available, 0 is returned.
func (metrics *FontMetrics) GetHeight() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_height(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetStrikethroughPosition wraps pango_font_metrics_get_strikethrough_position
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the suggested position to draw the strikethrough.
// 
// The value returned is the distance *above* the
// baseline of the top of the strikethrough.
func (metrics *FontMetrics) GetStrikethroughPosition() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_strikethrough_position(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetStrikethroughThickness wraps pango_font_metrics_get_strikethrough_thickness
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the suggested thickness to draw for the strikethrough.
func (metrics *FontMetrics) GetStrikethroughThickness() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_strikethrough_thickness(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetUnderlinePosition wraps pango_font_metrics_get_underline_position
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the suggested position to draw the underline.
// 
// The value returned is the distance *above* the baseline of the top
// of the underline. Since most fonts have underline positions beneath
// the baseline, this value is typically negative.
func (metrics *FontMetrics) GetUnderlinePosition() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_underline_position(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetUnderlineThickness wraps pango_font_metrics_get_underline_thickness
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the suggested thickness to draw for the underline.
func (metrics *FontMetrics) GetUnderlineThickness() int32 {
	var carg0 *C.PangoFontMetrics // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoFontMetrics)(UnsafeFontMetricsToGlibNone(metrics))

	cret = C.pango_font_metrics_get_underline_thickness(carg0)
	runtime.KeepAlive(metrics)

	var goret int32

	goret = int32(cret)

	return goret
}

// FontsetClass wraps PangoFontsetClass
//
// The `PangoFontsetClass` structure holds the virtual functions for
// a particular `PangoFontset` implementation.
// 
// FontsetClass is the type struct for [Fontset]
type FontsetClass struct {
	*fontsetClass
}

// fontsetClass is the struct that's finalized
type fontsetClass struct {
	native *C.PangoFontsetClass
}

// UnsafeFontsetClassFromGlibBorrow is used to convert raw C.PangoFontsetClass pointers to go. This is used by the bindings internally.
func UnsafeFontsetClassFromGlibBorrow(p unsafe.Pointer) *FontsetClass {
	return &FontsetClass{&fontsetClass{(*C.PangoFontsetClass)(p)}}
}

// UnsafeFontsetClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontsetClass] is expected to work anymore.
func UnsafeFontsetClassFree(f *FontsetClass) {
	C.free(unsafe.Pointer(f.native))
}

// UnsafeFontsetClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontsetClassToGlibNone(f *FontsetClass) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (f *FontsetClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeFontsetClassToGlibNone(f))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *FontsetClass) {}, f)
	return parent
}

// FontsetSimpleClass wraps PangoFontsetSimpleClass
// 
// FontsetSimpleClass is the type struct for [FontsetSimple]
type FontsetSimpleClass struct {
	*fontsetSimpleClass
}

// fontsetSimpleClass is the struct that's finalized
type fontsetSimpleClass struct {
	native *C.PangoFontsetSimpleClass
}

// UnsafeFontsetSimpleClassFromGlibBorrow is used to convert raw C.PangoFontsetSimpleClass pointers to go. This is used by the bindings internally.
func UnsafeFontsetSimpleClassFromGlibBorrow(p unsafe.Pointer) *FontsetSimpleClass {
	return &FontsetSimpleClass{&fontsetSimpleClass{(*C.PangoFontsetSimpleClass)(p)}}
}

// UnsafeFontsetSimpleClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [FontsetSimpleClass] is expected to work anymore.
func UnsafeFontsetSimpleClassFree(f *FontsetSimpleClass) {
	C.free(unsafe.Pointer(f.native))
}

// UnsafeFontsetSimpleClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeFontsetSimpleClassToGlibNone(f *FontsetSimpleClass) unsafe.Pointer {
	return unsafe.Pointer(f.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (f *FontsetSimpleClass) ParentClass() *FontsetClass {
	parent := UnsafeFontsetClassFromGlibBorrow(UnsafeFontsetSimpleClassToGlibNone(f))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *FontsetSimpleClass) {}, f)
	return parent
}

// GlyphGeometry wraps PangoGlyphGeometry
//
// The `PangoGlyphGeometry` structure contains width and positioning
// information for a single glyph.
// 
// Note that @width is not guaranteed to be the same as the glyph
// extents. Kerning and other positioning applied during shaping will
// affect both the @width and the @x_offset for the glyphs in the
// glyph string that results from shaping.
// 
// The information in this struct is intended for rendering the glyphs,
// as follows:
// 
// 1. Assume the current point is (x, y)
// 2. Render the current glyph at (x + x_offset, y + y_offset),
// 3. Advance the current point to (x + width, y)
// 4. Render the next glyph
type GlyphGeometry struct {
	*glyphGeometry
}

// glyphGeometry is the struct that's finalized
type glyphGeometry struct {
	native *C.PangoGlyphGeometry
}

// UnsafeGlyphGeometryFromGlibBorrow is used to convert raw C.PangoGlyphGeometry pointers to go. This is used by the bindings internally.
func UnsafeGlyphGeometryFromGlibBorrow(p unsafe.Pointer) *GlyphGeometry {
	return &GlyphGeometry{&glyphGeometry{(*C.PangoGlyphGeometry)(p)}}
}

// UnsafeGlyphGeometryFromGlibNone is used to convert raw C.PangoGlyphGeometry pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeGlyphGeometryFromGlibNone(p unsafe.Pointer) *GlyphGeometry {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeGlyphGeometryFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphGeometry,
		func (intern *glyphGeometry) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeGlyphGeometryFromGlibFull is used to convert raw C.PangoGlyphGeometry pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeGlyphGeometryFromGlibFull(p unsafe.Pointer) *GlyphGeometry {
	wrapped := UnsafeGlyphGeometryFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphGeometry,
		func (intern *glyphGeometry) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeGlyphGeometryFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [GlyphGeometry] is expected to work anymore.
func UnsafeGlyphGeometryFree(g *GlyphGeometry) {
	C.free(unsafe.Pointer(g.native))
}

// UnsafeGlyphGeometryToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeGlyphGeometryToGlibNone(g *GlyphGeometry) unsafe.Pointer {
	return unsafe.Pointer(g.native)
}

// UnsafeGlyphGeometryToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeGlyphGeometryToGlibFull(g *GlyphGeometry) unsafe.Pointer {
	runtime.SetFinalizer(g.glyphGeometry, nil)
	_p := unsafe.Pointer(g.native)
	g.native = nil // GlyphGeometry is invalid from here on
	return _p
}

// GlyphInfo wraps PangoGlyphInfo
//
// A `PangoGlyphInfo` structure represents a single glyph with
// positioning information and visual attributes.
type GlyphInfo struct {
	*glyphInfo
}

// glyphInfo is the struct that's finalized
type glyphInfo struct {
	native *C.PangoGlyphInfo
}

// UnsafeGlyphInfoFromGlibBorrow is used to convert raw C.PangoGlyphInfo pointers to go. This is used by the bindings internally.
func UnsafeGlyphInfoFromGlibBorrow(p unsafe.Pointer) *GlyphInfo {
	return &GlyphInfo{&glyphInfo{(*C.PangoGlyphInfo)(p)}}
}

// UnsafeGlyphInfoFromGlibNone is used to convert raw C.PangoGlyphInfo pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeGlyphInfoFromGlibNone(p unsafe.Pointer) *GlyphInfo {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeGlyphInfoFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphInfo,
		func (intern *glyphInfo) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeGlyphInfoFromGlibFull is used to convert raw C.PangoGlyphInfo pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeGlyphInfoFromGlibFull(p unsafe.Pointer) *GlyphInfo {
	wrapped := UnsafeGlyphInfoFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphInfo,
		func (intern *glyphInfo) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeGlyphInfoFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [GlyphInfo] is expected to work anymore.
func UnsafeGlyphInfoFree(g *GlyphInfo) {
	C.free(unsafe.Pointer(g.native))
}

// UnsafeGlyphInfoToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeGlyphInfoToGlibNone(g *GlyphInfo) unsafe.Pointer {
	return unsafe.Pointer(g.native)
}

// UnsafeGlyphInfoToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeGlyphInfoToGlibFull(g *GlyphInfo) unsafe.Pointer {
	runtime.SetFinalizer(g.glyphInfo, nil)
	_p := unsafe.Pointer(g.native)
	g.native = nil // GlyphInfo is invalid from here on
	return _p
}

// GlyphItem wraps PangoGlyphItem
//
// A `PangoGlyphItem` is a pair of a `PangoItem` and the glyphs
// resulting from shaping the items text.
// 
// As an example of the usage of `PangoGlyphItem`, the results
// of shaping text with `PangoLayout` is a list of `PangoLayoutLine`,
// each of which contains a list of `PangoGlyphItem`.
type GlyphItem struct {
	*glyphItem
}

// glyphItem is the struct that's finalized
type glyphItem struct {
	native *C.PangoGlyphItem
}

var _ gobject.GoValueInitializer = (*GlyphItem)(nil)

func marshalGlyphItem(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeGlyphItemFromGlibBorrow(b), nil
}

func (r *GlyphItem) GoValueType() gobject.Type {
	return TypeGlyphItem
}

func (r *GlyphItem) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeGlyphItemFromGlibBorrow is used to convert raw C.PangoGlyphItem pointers to go. This is used by the bindings internally.
func UnsafeGlyphItemFromGlibBorrow(p unsafe.Pointer) *GlyphItem {
	return &GlyphItem{&glyphItem{(*C.PangoGlyphItem)(p)}}
}

// UnsafeGlyphItemFromGlibNone is used to convert raw C.PangoGlyphItem pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeGlyphItemFromGlibNone(p unsafe.Pointer) *GlyphItem {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeGlyphItemFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphItem,
		func (intern *glyphItem) {
			C.pango_glyph_item_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeGlyphItemFromGlibFull is used to convert raw C.PangoGlyphItem pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeGlyphItemFromGlibFull(p unsafe.Pointer) *GlyphItem {
	wrapped := UnsafeGlyphItemFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphItem,
		func (intern *glyphItem) {
			C.pango_glyph_item_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeGlyphItemFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [GlyphItem] is expected to work anymore.
func UnsafeGlyphItemFree(g *GlyphItem) {
	C.pango_glyph_item_free(g.native)
}

// UnsafeGlyphItemToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeGlyphItemToGlibNone(g *GlyphItem) unsafe.Pointer {
	return unsafe.Pointer(g.native)
}

// UnsafeGlyphItemToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeGlyphItemToGlibFull(g *GlyphItem) unsafe.Pointer {
	runtime.SetFinalizer(g.glyphItem, nil)
	_p := unsafe.Pointer(g.native)
	g.native = nil // GlyphItem is invalid from here on
	return _p
}

// ApplyAttrs wraps pango_glyph_item_apply_attrs
// 
// The function takes the following parameters:
// 
// 	- text string: text that @list applies to 
// 	- list *AttrList: a `PangoAttrList` 
// 
// The function returns the following values:
// 
// 	- goret []*GlyphItem 
//
// Splits a shaped item (`PangoGlyphItem`) into multiple items based
// on an attribute list.
// 
// The idea is that if you have attributes that don't affect shaping,
// such as color or underline, to avoid affecting shaping, you filter
// them out ([method@Pango.AttrList.filter]), apply the shaping process
// and then reapply them to the result using this function.
// 
// All attributes that start or end inside a cluster are applied
// to that cluster; for instance, if half of a cluster is underlined
// and the other-half strikethrough, then the cluster will end
// up with both underline and strikethrough attributes. In these
// cases, it may happen that @item-&gt;extra_attrs for some of the
// result items can have multiple attributes of the same type.
// 
// This function takes ownership of @glyph_item; it will be reused
// as one of the elements in the list.
func (glyphItem *GlyphItem) ApplyAttrs(text string, list *AttrList) []*GlyphItem {
	var carg0 *C.PangoGlyphItem // in, none, converted
	var carg1 *C.char           // in, none, string, casted *C.gchar
	var carg2 *C.PangoAttrList  // in, none, converted
	var cret  *C.GSList         // container, transfer: full

	carg0 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(glyphItem))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = (*C.PangoAttrList)(UnsafeAttrListToGlibNone(list))

	cret = C.pango_glyph_item_apply_attrs(carg0, carg1, carg2)
	runtime.KeepAlive(glyphItem)
	runtime.KeepAlive(text)
	runtime.KeepAlive(list)

	var goret []*GlyphItem

	goret = glib.UnsafeSListFromGlibFull(
		unsafe.Pointer(cret),
		func(v unsafe.Pointer) *GlyphItem {
			var dst *GlyphItem // converted
			dst = UnsafeGlyphItemFromGlibFull(v)
			return dst
		},
	)

	return goret
}

// Copy wraps pango_glyph_item_copy
// 
// The function returns the following values:
// 
// 	- goret *GlyphItem (nullable) 
//
// Make a deep copy of an existing `PangoGlyphItem` structure.
func (orig *GlyphItem) Copy() *GlyphItem {
	var carg0 *C.PangoGlyphItem // in, none, converted
	var cret  *C.PangoGlyphItem // return, full, converted, nullable

	carg0 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(orig))

	cret = C.pango_glyph_item_copy(carg0)
	runtime.KeepAlive(orig)

	var goret *GlyphItem

	if cret != nil {
		goret = UnsafeGlyphItemFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Split wraps pango_glyph_item_split
// 
// The function takes the following parameters:
// 
// 	- text string: text to which positions in @orig apply 
// 	- splitIndex int32: byte index of position to split item, relative to the
//   start of the item 
// 
// The function returns the following values:
// 
// 	- goret *GlyphItem (nullable) 
//
// Modifies @orig to cover only the text after @split_index, and
// returns a new item that covers the text before @split_index that
// used to be in @orig.
// 
// You can think of @split_index as the length of the returned item.
// @split_index may not be 0, and it may not be greater than or equal
// to the length of @orig (that is, there must be at least one byte
// assigned to each item, you can't create a zero-length item).
// 
// This function is similar in function to pango_item_split() (and uses
// it internally.)
func (orig *GlyphItem) Split(text string, splitIndex int32) *GlyphItem {
	var carg0 *C.PangoGlyphItem // in, none, converted
	var carg1 *C.char           // in, none, string, casted *C.gchar
	var carg2 C.int             // in, none, casted, casted C.gint
	var cret  *C.PangoGlyphItem // return, full, converted, nullable

	carg0 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(orig))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(splitIndex)

	cret = C.pango_glyph_item_split(carg0, carg1, carg2)
	runtime.KeepAlive(orig)
	runtime.KeepAlive(text)
	runtime.KeepAlive(splitIndex)

	var goret *GlyphItem

	if cret != nil {
		goret = UnsafeGlyphItemFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// GlyphItemIter wraps PangoGlyphItemIter
//
// A `PangoGlyphItemIter` is an iterator over the clusters in a
// `PangoGlyphItem`.
// 
// The *forward direction* of the iterator is the logical direction of text.
// That is, with increasing @start_index and @start_char values. If @glyph_item
// is right-to-left (that is, if `glyph_item-&gt;item-&gt;analysis.level` is odd),
// then @start_glyph decreases as the iterator moves forward.  Moreover,
// in right-to-left cases, @start_glyph is greater than @end_glyph.
// 
// An iterator should be initialized using either
// pango_glyph_item_iter_init_start() or
// pango_glyph_item_iter_init_end(), for forward and backward iteration
// respectively, and walked over using any desired mixture of
// pango_glyph_item_iter_next_cluster() and
// pango_glyph_item_iter_prev_cluster().
// 
// A common idiom for doing a forward iteration over the clusters is:
// 
// ```
// PangoGlyphItemIter cluster_iter;
// gboolean have_cluster;
// 
// for (have_cluster = pango_glyph_item_iter_init_start (&amp;cluster_iter,
//                                                       glyph_item, text);
//      have_cluster;
//      have_cluster = pango_glyph_item_iter_next_cluster (&amp;cluster_iter))
// {
//   ...
// }
// ```
// 
// Note that @text is the start of the text for layout, which is then
// indexed by `glyph_item-&gt;item-&gt;offset` to get to the text of @glyph_item.
// The @start_index and @end_index values can directly index into @text. The
// @start_glyph, @end_glyph, @start_char, and @end_char values however are
// zero-based for the @glyph_item.  For each cluster, the item pointed at by
// the start variables is included in the cluster while the one pointed at by
// end variables is not.
// 
// None of the members of a `PangoGlyphItemIter` should be modified manually.
type GlyphItemIter struct {
	*glyphItemIter
}

// glyphItemIter is the struct that's finalized
type glyphItemIter struct {
	native *C.PangoGlyphItemIter
}

var _ gobject.GoValueInitializer = (*GlyphItemIter)(nil)

func marshalGlyphItemIter(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeGlyphItemIterFromGlibBorrow(b), nil
}

func (r *GlyphItemIter) GoValueType() gobject.Type {
	return TypeGlyphItemIter
}

func (r *GlyphItemIter) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeGlyphItemIterFromGlibBorrow is used to convert raw C.PangoGlyphItemIter pointers to go. This is used by the bindings internally.
func UnsafeGlyphItemIterFromGlibBorrow(p unsafe.Pointer) *GlyphItemIter {
	return &GlyphItemIter{&glyphItemIter{(*C.PangoGlyphItemIter)(p)}}
}

// UnsafeGlyphItemIterFromGlibNone is used to convert raw C.PangoGlyphItemIter pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeGlyphItemIterFromGlibNone(p unsafe.Pointer) *GlyphItemIter {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeGlyphItemIterFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphItemIter,
		func (intern *glyphItemIter) {
			C.pango_glyph_item_iter_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeGlyphItemIterFromGlibFull is used to convert raw C.PangoGlyphItemIter pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeGlyphItemIterFromGlibFull(p unsafe.Pointer) *GlyphItemIter {
	wrapped := UnsafeGlyphItemIterFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphItemIter,
		func (intern *glyphItemIter) {
			C.pango_glyph_item_iter_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeGlyphItemIterFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [GlyphItemIter] is expected to work anymore.
func UnsafeGlyphItemIterFree(g *GlyphItemIter) {
	C.pango_glyph_item_iter_free(g.native)
}

// UnsafeGlyphItemIterToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeGlyphItemIterToGlibNone(g *GlyphItemIter) unsafe.Pointer {
	return unsafe.Pointer(g.native)
}

// UnsafeGlyphItemIterToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeGlyphItemIterToGlibFull(g *GlyphItemIter) unsafe.Pointer {
	runtime.SetFinalizer(g.glyphItemIter, nil)
	_p := unsafe.Pointer(g.native)
	g.native = nil // GlyphItemIter is invalid from here on
	return _p
}

// Copy wraps pango_glyph_item_iter_copy
// 
// The function returns the following values:
// 
// 	- goret *GlyphItemIter (nullable) 
//
// Make a shallow copy of an existing `PangoGlyphItemIter` structure.
func (orig *GlyphItemIter) Copy() *GlyphItemIter {
	var carg0 *C.PangoGlyphItemIter // in, none, converted
	var cret  *C.PangoGlyphItemIter // return, full, converted, nullable

	carg0 = (*C.PangoGlyphItemIter)(UnsafeGlyphItemIterToGlibNone(orig))

	cret = C.pango_glyph_item_iter_copy(carg0)
	runtime.KeepAlive(orig)

	var goret *GlyphItemIter

	if cret != nil {
		goret = UnsafeGlyphItemIterFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// InitEnd wraps pango_glyph_item_iter_init_end
// 
// The function takes the following parameters:
// 
// 	- glyphItem *GlyphItem: the glyph item to iterate over 
// 	- text string: text corresponding to the glyph item 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Initializes a `PangoGlyphItemIter` structure to point to the
// last cluster in a glyph item.
// 
// See `PangoGlyphItemIter` for details of cluster orders.
func (iter *GlyphItemIter) InitEnd(glyphItem *GlyphItem, text string) bool {
	var carg0 *C.PangoGlyphItemIter // in, none, converted
	var carg1 *C.PangoGlyphItem     // in, none, converted
	var carg2 *C.char               // in, none, string, casted *C.gchar
	var cret  C.gboolean            // return

	carg0 = (*C.PangoGlyphItemIter)(UnsafeGlyphItemIterToGlibNone(iter))
	carg1 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(glyphItem))
	carg2 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg2))

	cret = C.pango_glyph_item_iter_init_end(carg0, carg1, carg2)
	runtime.KeepAlive(iter)
	runtime.KeepAlive(glyphItem)
	runtime.KeepAlive(text)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// InitStart wraps pango_glyph_item_iter_init_start
// 
// The function takes the following parameters:
// 
// 	- glyphItem *GlyphItem: the glyph item to iterate over 
// 	- text string: text corresponding to the glyph item 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Initializes a `PangoGlyphItemIter` structure to point to the
// first cluster in a glyph item.
// 
// See `PangoGlyphItemIter` for details of cluster orders.
func (iter *GlyphItemIter) InitStart(glyphItem *GlyphItem, text string) bool {
	var carg0 *C.PangoGlyphItemIter // in, none, converted
	var carg1 *C.PangoGlyphItem     // in, none, converted
	var carg2 *C.char               // in, none, string, casted *C.gchar
	var cret  C.gboolean            // return

	carg0 = (*C.PangoGlyphItemIter)(UnsafeGlyphItemIterToGlibNone(iter))
	carg1 = (*C.PangoGlyphItem)(UnsafeGlyphItemToGlibNone(glyphItem))
	carg2 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg2))

	cret = C.pango_glyph_item_iter_init_start(carg0, carg1, carg2)
	runtime.KeepAlive(iter)
	runtime.KeepAlive(glyphItem)
	runtime.KeepAlive(text)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// NextCluster wraps pango_glyph_item_iter_next_cluster
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Advances the iterator to the next cluster in the glyph item.
// 
// See `PangoGlyphItemIter` for details of cluster orders.
func (iter *GlyphItemIter) NextCluster() bool {
	var carg0 *C.PangoGlyphItemIter // in, none, converted
	var cret  C.gboolean            // return

	carg0 = (*C.PangoGlyphItemIter)(UnsafeGlyphItemIterToGlibNone(iter))

	cret = C.pango_glyph_item_iter_next_cluster(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// PrevCluster wraps pango_glyph_item_iter_prev_cluster
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Moves the iterator to the preceding cluster in the glyph item.
// See `PangoGlyphItemIter` for details of cluster orders.
func (iter *GlyphItemIter) PrevCluster() bool {
	var carg0 *C.PangoGlyphItemIter // in, none, converted
	var cret  C.gboolean            // return

	carg0 = (*C.PangoGlyphItemIter)(UnsafeGlyphItemIterToGlibNone(iter))

	cret = C.pango_glyph_item_iter_prev_cluster(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GlyphString wraps PangoGlyphString
//
// A `PangoGlyphString` is used to store strings of glyphs with geometry
// and visual attribute information.
// 
// The storage for the glyph information is owned by the structure
// which simplifies memory management.
type GlyphString struct {
	*glyphString
}

// glyphString is the struct that's finalized
type glyphString struct {
	native *C.PangoGlyphString
}

var _ gobject.GoValueInitializer = (*GlyphString)(nil)

func marshalGlyphString(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeGlyphStringFromGlibBorrow(b), nil
}

func (r *GlyphString) GoValueType() gobject.Type {
	return TypeGlyphString
}

func (r *GlyphString) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeGlyphStringFromGlibBorrow is used to convert raw C.PangoGlyphString pointers to go. This is used by the bindings internally.
func UnsafeGlyphStringFromGlibBorrow(p unsafe.Pointer) *GlyphString {
	return &GlyphString{&glyphString{(*C.PangoGlyphString)(p)}}
}

// UnsafeGlyphStringFromGlibNone is used to convert raw C.PangoGlyphString pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeGlyphStringFromGlibNone(p unsafe.Pointer) *GlyphString {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeGlyphStringFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphString,
		func (intern *glyphString) {
			C.pango_glyph_string_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeGlyphStringFromGlibFull is used to convert raw C.PangoGlyphString pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeGlyphStringFromGlibFull(p unsafe.Pointer) *GlyphString {
	wrapped := UnsafeGlyphStringFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphString,
		func (intern *glyphString) {
			C.pango_glyph_string_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeGlyphStringFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [GlyphString] is expected to work anymore.
func UnsafeGlyphStringFree(g *GlyphString) {
	C.pango_glyph_string_free(g.native)
}

// UnsafeGlyphStringToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeGlyphStringToGlibNone(g *GlyphString) unsafe.Pointer {
	return unsafe.Pointer(g.native)
}

// UnsafeGlyphStringToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeGlyphStringToGlibFull(g *GlyphString) unsafe.Pointer {
	runtime.SetFinalizer(g.glyphString, nil)
	_p := unsafe.Pointer(g.native)
	g.native = nil // GlyphString is invalid from here on
	return _p
}

// NewGlyphString wraps pango_glyph_string_new
// 
// The function returns the following values:
// 
// 	- goret *GlyphString 
//
// Create a new `PangoGlyphString`.
func NewGlyphString() *GlyphString {
	var cret *C.PangoGlyphString // return, full, converted

	cret = C.pango_glyph_string_new()

	var goret *GlyphString

	goret = UnsafeGlyphStringFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Copy wraps pango_glyph_string_copy
// 
// The function returns the following values:
// 
// 	- goret *GlyphString (nullable) 
//
// Copy a glyph string and associated storage.
func (str *GlyphString) Copy() *GlyphString {
	var carg0 *C.PangoGlyphString // in, none, converted
	var cret  *C.PangoGlyphString // return, full, converted, nullable

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(str))

	cret = C.pango_glyph_string_copy(carg0)
	runtime.KeepAlive(str)

	var goret *GlyphString

	if cret != nil {
		goret = UnsafeGlyphStringFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Extents wraps pango_glyph_string_extents
// 
// The function takes the following parameters:
// 
// 	- font Font: a `PangoFont` 
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of the glyph string as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical extents of the glyph string 
//
// Compute the logical and ink extents of a glyph string.
// 
// See the documentation for [method@Pango.Font.get_glyph_extents] for details
// about the interpretation of the rectangles.
// 
// Examples of logical (red) and ink (green) rects:
// 
// ![](rects1.png) ![](rects2.png)
func (glyphs *GlyphString) Extents(font Font) (Rectangle, Rectangle) {
	var carg0 *C.PangoGlyphString // in, none, converted
	var carg1 *C.PangoFont        // in, none, converted
	var carg2 C.PangoRectangle    // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg3 C.PangoRectangle    // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg1 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	C.pango_glyph_string_extents(carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(font)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg3
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// ExtentsRange wraps pango_glyph_string_extents_range
// 
// The function takes the following parameters:
// 
// 	- start int32: start index 
// 	- end int32: end index (the range is the set of bytes with
//   indices such that start &lt;= index &lt; end) 
// 	- font Font: a `PangoFont` 
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to
//   store the extents of the glyph string range as drawn 
// 	- logicalRect Rectangle: rectangle used to
//   store the logical extents of the glyph string range 
//
// Computes the extents of a sub-portion of a glyph string.
// 
// The extents are relative to the start of the glyph string range
// (the origin of their coordinate system is at the start of the range,
// not at the start of the entire glyph string).
func (glyphs *GlyphString) ExtentsRange(start int32, end int32, font Font) (Rectangle, Rectangle) {
	var carg0 *C.PangoGlyphString // in, none, converted
	var carg1 C.int               // in, none, casted, casted C.gint
	var carg2 C.int               // in, none, casted, casted C.gint
	var carg3 *C.PangoFont        // in, none, converted
	var carg4 C.PangoRectangle    // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg5 C.PangoRectangle    // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg1 = C.int(start)
	carg2 = C.int(end)
	carg3 = (*C.PangoFont)(UnsafeFontToGlibNone(font))

	C.pango_glyph_string_extents_range(carg0, carg1, carg2, carg3, &carg4, &carg5)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(start)
	runtime.KeepAlive(end)
	runtime.KeepAlive(font)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg4
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg5
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetWidth wraps pango_glyph_string_get_width
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Computes the logical width of the glyph string.
// 
// This can also be computed using [method@Pango.GlyphString.extents].
// However, since this only computes the width, it's much faster. This
// is in fact only a convenience function that computes the sum of
// @geometry.width for each glyph in the @glyphs.
func (glyphs *GlyphString) GetWidth() int32 {
	var carg0 *C.PangoGlyphString // in, none, converted
	var cret  C.int               // return, none, casted, casted C.gint

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))

	cret = C.pango_glyph_string_get_width(carg0)
	runtime.KeepAlive(glyphs)

	var goret int32

	goret = int32(cret)

	return goret
}

// IndexToX wraps pango_glyph_string_index_to_x
// 
// The function takes the following parameters:
// 
// 	- text string: the text for the run 
// 	- length int32: the number of bytes (not characters) in @text. 
// 	- analysis *Analysis: the analysis information return from [func@itemize] 
// 	- index_ int32: the byte index within @text 
// 	- trailing bool: whether we should compute the result for the beginning (%FALSE)
//   or end (%TRUE) of the character. 
// 
// The function returns the following values:
// 
// 	- xPos int32: location to store result 
//
// Converts from character position to x position.
// 
// The X position is measured from the left edge of the run.
// Character positions are obtained using font metrics for ligatures
// where available, and computed by dividing up each cluster
// into equal portions, otherwise.
// 
// &lt;picture&gt;
//   &lt;source srcset="glyphstring-positions-dark.png" media="(prefers-color-scheme: dark)"&gt;
//   &lt;img alt="Glyph positions" src="glyphstring-positions-light.png"&gt;
// &lt;/picture&gt;
func (glyphs *GlyphString) IndexToX(text string, length int32, analysis *Analysis, index_ int32, trailing bool) int32 {
	var carg0 *C.PangoGlyphString // in, none, converted
	var carg1 *C.char             // in, none, string, casted *C.gchar
	var carg2 C.int               // in, none, casted, casted C.gint
	var carg3 *C.PangoAnalysis    // in, none, converted
	var carg4 C.int               // in, none, casted, casted C.gint
	var carg5 C.gboolean          // in
	var carg6 C.int               // out, full, casted, casted C.gint

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)
	carg3 = (*C.PangoAnalysis)(UnsafeAnalysisToGlibNone(analysis))
	carg4 = C.int(index_)
	if trailing {
		carg5 = C.TRUE
	}

	C.pango_glyph_string_index_to_x(carg0, carg1, carg2, carg3, carg4, carg5, &carg6)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)
	runtime.KeepAlive(analysis)
	runtime.KeepAlive(index_)
	runtime.KeepAlive(trailing)

	var xPos int32

	xPos = int32(carg6)

	return xPos
}

// IndexToXFull wraps pango_glyph_string_index_to_x_full
// 
// The function takes the following parameters:
// 
// 	- text string: the text for the run 
// 	- length int32: the number of bytes (not characters) in @text. 
// 	- analysis *Analysis: the analysis information return from [func@itemize] 
// 	- attrs *LogAttr (nullable): `PangoLogAttr` array for @text 
// 	- index_ int32: the byte index within @text 
// 	- trailing bool: whether we should compute the result for the beginning (%FALSE)
//   or end (%TRUE) of the character. 
// 
// The function returns the following values:
// 
// 	- xPos int32: location to store result 
//
// Converts from character position to x position.
// 
// This variant of [method@Pango.GlyphString.index_to_x] additionally
// accepts a `PangoLogAttr` array. The grapheme boundary information
// in it can be used to disambiguate positioning inside some complex
// clusters.
func (glyphs *GlyphString) IndexToXFull(text string, length int32, analysis *Analysis, attrs *LogAttr, index_ int32, trailing bool) int32 {
	var carg0 *C.PangoGlyphString // in, none, converted
	var carg1 *C.char             // in, none, string, casted *C.gchar
	var carg2 C.int               // in, none, casted, casted C.gint
	var carg3 *C.PangoAnalysis    // in, none, converted
	var carg4 *C.PangoLogAttr     // in, none, converted, nullable
	var carg5 C.int               // in, none, casted, casted C.gint
	var carg6 C.gboolean          // in
	var carg7 C.int               // out, full, casted, casted C.gint

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)
	carg3 = (*C.PangoAnalysis)(UnsafeAnalysisToGlibNone(analysis))
	if attrs != nil {
		carg4 = (*C.PangoLogAttr)(UnsafeLogAttrToGlibNone(attrs))
	}
	carg5 = C.int(index_)
	if trailing {
		carg6 = C.TRUE
	}

	C.pango_glyph_string_index_to_x_full(carg0, carg1, carg2, carg3, carg4, carg5, carg6, &carg7)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)
	runtime.KeepAlive(analysis)
	runtime.KeepAlive(attrs)
	runtime.KeepAlive(index_)
	runtime.KeepAlive(trailing)

	var xPos int32

	xPos = int32(carg7)

	return xPos
}

// SetSize wraps pango_glyph_string_set_size
// 
// The function takes the following parameters:
// 
// 	- newLen int32: the new length of the string 
//
// Resize a glyph string to the given length.
func (str *GlyphString) SetSize(newLen int32) {
	var carg0 *C.PangoGlyphString // in, none, converted
	var carg1 C.int               // in, none, casted, casted C.gint

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(str))
	carg1 = C.int(newLen)

	C.pango_glyph_string_set_size(carg0, carg1)
	runtime.KeepAlive(str)
	runtime.KeepAlive(newLen)
}

// XToIndex wraps pango_glyph_string_x_to_index
// 
// The function takes the following parameters:
// 
// 	- text string: the text for the run 
// 	- length int32: the number of bytes (not characters) in text. 
// 	- analysis *Analysis: the analysis information return from [func@itemize] 
// 	- xPos int32: the x offset (in Pango units) 
// 
// The function returns the following values:
// 
// 	- index_ int32: location to store calculated byte index
//   within @text 
// 	- trailing int32: location to store a boolean indicating
//   whether the user clicked on the leading or trailing edge of the
//   character 
//
// Convert from x offset to character position.
// 
// Character positions are computed by dividing up each cluster into
// equal portions. In scripts where positioning within a cluster is
// not allowed (such as Thai), the returned value may not be a valid
// cursor position; the caller must combine the result with the logical
// attributes for the text to compute the valid cursor position.
func (glyphs *GlyphString) XToIndex(text string, length int32, analysis *Analysis, xPos int32) (int32, int32) {
	var carg0 *C.PangoGlyphString // in, none, converted
	var carg1 *C.char             // in, none, string, casted *C.gchar
	var carg2 C.int               // in, none, casted, casted C.gint
	var carg3 *C.PangoAnalysis    // in, none, converted
	var carg4 C.int               // in, none, casted, casted C.gint
	var carg5 C.int               // out, full, casted, casted C.gint
	var carg6 C.int               // out, full, casted, casted C.gint

	carg0 = (*C.PangoGlyphString)(UnsafeGlyphStringToGlibNone(glyphs))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)
	carg3 = (*C.PangoAnalysis)(UnsafeAnalysisToGlibNone(analysis))
	carg4 = C.int(xPos)

	C.pango_glyph_string_x_to_index(carg0, carg1, carg2, carg3, carg4, &carg5, &carg6)
	runtime.KeepAlive(glyphs)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)
	runtime.KeepAlive(analysis)
	runtime.KeepAlive(xPos)

	var index_   int32
	var trailing int32

	index_ = int32(carg5)
	trailing = int32(carg6)

	return index_, trailing
}

// GlyphVisAttr wraps PangoGlyphVisAttr
//
// A `PangoGlyphVisAttr` structure communicates information between
// the shaping and rendering phases.
// 
// Currently, it contains cluster start and color information.
// More attributes may be added in the future.
// 
// Clusters are stored in visual order, within the cluster, glyphs
// are always ordered in logical order, since visual order is meaningless;
// that is, in Arabic text, accent glyphs follow the glyphs for the
// base character.
type GlyphVisAttr struct {
	*glyphVisAttr
}

// glyphVisAttr is the struct that's finalized
type glyphVisAttr struct {
	native *C.PangoGlyphVisAttr
}

// UnsafeGlyphVisAttrFromGlibBorrow is used to convert raw C.PangoGlyphVisAttr pointers to go. This is used by the bindings internally.
func UnsafeGlyphVisAttrFromGlibBorrow(p unsafe.Pointer) *GlyphVisAttr {
	return &GlyphVisAttr{&glyphVisAttr{(*C.PangoGlyphVisAttr)(p)}}
}

// UnsafeGlyphVisAttrFromGlibNone is used to convert raw C.PangoGlyphVisAttr pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeGlyphVisAttrFromGlibNone(p unsafe.Pointer) *GlyphVisAttr {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeGlyphVisAttrFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphVisAttr,
		func (intern *glyphVisAttr) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeGlyphVisAttrFromGlibFull is used to convert raw C.PangoGlyphVisAttr pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeGlyphVisAttrFromGlibFull(p unsafe.Pointer) *GlyphVisAttr {
	wrapped := UnsafeGlyphVisAttrFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.glyphVisAttr,
		func (intern *glyphVisAttr) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeGlyphVisAttrFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [GlyphVisAttr] is expected to work anymore.
func UnsafeGlyphVisAttrFree(g *GlyphVisAttr) {
	C.free(unsafe.Pointer(g.native))
}

// UnsafeGlyphVisAttrToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeGlyphVisAttrToGlibNone(g *GlyphVisAttr) unsafe.Pointer {
	return unsafe.Pointer(g.native)
}

// UnsafeGlyphVisAttrToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeGlyphVisAttrToGlibFull(g *GlyphVisAttr) unsafe.Pointer {
	runtime.SetFinalizer(g.glyphVisAttr, nil)
	_p := unsafe.Pointer(g.native)
	g.native = nil // GlyphVisAttr is invalid from here on
	return _p
}

// Item wraps PangoItem
//
// The `PangoItem` structure stores information about a segment of text.
// 
// You typically obtain `PangoItems` by itemizing a piece of text
// with [func@itemize].
type Item struct {
	*item
}

// item is the struct that's finalized
type item struct {
	native *C.PangoItem
}

var _ gobject.GoValueInitializer = (*Item)(nil)

func marshalItem(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeItemFromGlibBorrow(b), nil
}

func (r *Item) GoValueType() gobject.Type {
	return TypeItem
}

func (r *Item) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeItemFromGlibBorrow is used to convert raw C.PangoItem pointers to go. This is used by the bindings internally.
func UnsafeItemFromGlibBorrow(p unsafe.Pointer) *Item {
	return &Item{&item{(*C.PangoItem)(p)}}
}

// UnsafeItemFromGlibNone is used to convert raw C.PangoItem pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeItemFromGlibNone(p unsafe.Pointer) *Item {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeItemFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.item,
		func (intern *item) {
			C.pango_item_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeItemFromGlibFull is used to convert raw C.PangoItem pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeItemFromGlibFull(p unsafe.Pointer) *Item {
	wrapped := UnsafeItemFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.item,
		func (intern *item) {
			C.pango_item_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeItemFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Item] is expected to work anymore.
func UnsafeItemFree(i *Item) {
	C.pango_item_free(i.native)
}

// UnsafeItemToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeItemToGlibNone(i *Item) unsafe.Pointer {
	return unsafe.Pointer(i.native)
}

// UnsafeItemToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeItemToGlibFull(i *Item) unsafe.Pointer {
	runtime.SetFinalizer(i.item, nil)
	_p := unsafe.Pointer(i.native)
	i.native = nil // Item is invalid from here on
	return _p
}

// NewItem wraps pango_item_new
// 
// The function returns the following values:
// 
// 	- goret *Item 
//
// Creates a new `PangoItem` structure initialized to default values.
func NewItem() *Item {
	var cret *C.PangoItem // return, full, converted

	cret = C.pango_item_new()

	var goret *Item

	goret = UnsafeItemFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// ApplyAttrs wraps pango_item_apply_attrs
// 
// The function takes the following parameters:
// 
// 	- iter *AttrIterator: a `PangoAttrIterator` 
//
// Add attributes to a `PangoItem`.
// 
// The idea is that you have attributes that don't affect itemization,
// such as font features, so you filter them out using
// [method@Pango.AttrList.filter], itemize your text, then reapply the
// attributes to the resulting items using this function.
// 
// The @iter should be positioned before the range of the item,
// and will be advanced past it. This function is meant to be called
// in a loop over the items resulting from itemization, while passing
// the iter to each call.
func (item *Item) ApplyAttrs(iter *AttrIterator) {
	var carg0 *C.PangoItem         // in, none, converted
	var carg1 *C.PangoAttrIterator // in, none, converted

	carg0 = (*C.PangoItem)(UnsafeItemToGlibNone(item))
	carg1 = (*C.PangoAttrIterator)(UnsafeAttrIteratorToGlibNone(iter))

	C.pango_item_apply_attrs(carg0, carg1)
	runtime.KeepAlive(item)
	runtime.KeepAlive(iter)
}

// Copy wraps pango_item_copy
// 
// The function returns the following values:
// 
// 	- goret *Item (nullable) 
//
// Copy an existing `PangoItem` structure.
func (item *Item) Copy() *Item {
	var carg0 *C.PangoItem // in, none, converted
	var cret  *C.PangoItem // return, full, converted, nullable

	carg0 = (*C.PangoItem)(UnsafeItemToGlibNone(item))

	cret = C.pango_item_copy(carg0)
	runtime.KeepAlive(item)

	var goret *Item

	if cret != nil {
		goret = UnsafeItemFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// GetCharOffset wraps pango_item_get_char_offset
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Returns the character offset of the item from the beginning
// of the itemized text.
// 
// If the item has not been obtained from Pango's itemization
// machinery, then the character offset is not available. In
// that case, this function returns -1.
func (item *Item) GetCharOffset() int32 {
	var carg0 *C.PangoItem // in, none, converted
	var cret  C.int        // return, none, casted, casted C.gint

	carg0 = (*C.PangoItem)(UnsafeItemToGlibNone(item))

	cret = C.pango_item_get_char_offset(carg0)
	runtime.KeepAlive(item)

	var goret int32

	goret = int32(cret)

	return goret
}

// Split wraps pango_item_split
// 
// The function takes the following parameters:
// 
// 	- splitIndex int32: byte index of position to split item, relative to the
//   start of the item 
// 	- splitOffset int32: number of chars between start of @orig and @split_index 
// 
// The function returns the following values:
// 
// 	- goret *Item 
//
// Modifies @orig to cover only the text after @split_index, and
// returns a new item that covers the text before @split_index that
// used to be in @orig.
// 
// You can think of @split_index as the length of the returned item.
// @split_index may not be 0, and it may not be greater than or equal
// to the length of @orig (that is, there must be at least one byte
// assigned to each item, you can't create a zero-length item).
// @split_offset is the length of the first item in chars, and must be
// provided because the text used to generate the item isn't available,
// so `pango_item_split()` can't count the char length of the split items
// itself.
func (orig *Item) Split(splitIndex int32, splitOffset int32) *Item {
	var carg0 *C.PangoItem // in, none, converted
	var carg1 C.int        // in, none, casted, casted C.gint
	var carg2 C.int        // in, none, casted, casted C.gint
	var cret  *C.PangoItem // return, full, converted

	carg0 = (*C.PangoItem)(UnsafeItemToGlibNone(orig))
	carg1 = C.int(splitIndex)
	carg2 = C.int(splitOffset)

	cret = C.pango_item_split(carg0, carg1, carg2)
	runtime.KeepAlive(orig)
	runtime.KeepAlive(splitIndex)
	runtime.KeepAlive(splitOffset)

	var goret *Item

	goret = UnsafeItemFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// Language wraps PangoLanguage
//
// The `PangoLanguage` structure is used to
// represent a language.
// 
// `PangoLanguage` pointers can be efficiently
// copied and compared with each other.
type Language struct {
	*language
}

// language is the struct that's finalized
type language struct {
	native *C.PangoLanguage
}

var _ gobject.GoValueInitializer = (*Language)(nil)

func marshalLanguage(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeLanguageFromGlibBorrow(b), nil
}

func (r *Language) GoValueType() gobject.Type {
	return TypeLanguage
}

func (r *Language) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeLanguageFromGlibBorrow is used to convert raw C.PangoLanguage pointers to go. This is used by the bindings internally.
func UnsafeLanguageFromGlibBorrow(p unsafe.Pointer) *Language {
	return &Language{&language{(*C.PangoLanguage)(p)}}
}

// UnsafeLanguageFromGlibNone is used to convert raw C.PangoLanguage pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeLanguageFromGlibNone(p unsafe.Pointer) *Language {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeLanguageFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.language,
		func (intern *language) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeLanguageFromGlibFull is used to convert raw C.PangoLanguage pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeLanguageFromGlibFull(p unsafe.Pointer) *Language {
	wrapped := UnsafeLanguageFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.language,
		func (intern *language) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeLanguageFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Language] is expected to work anymore.
func UnsafeLanguageFree(l *Language) {
	C.free(unsafe.Pointer(l.native))
}

// UnsafeLanguageToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeLanguageToGlibNone(l *Language) unsafe.Pointer {
	return unsafe.Pointer(l.native)
}

// UnsafeLanguageToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeLanguageToGlibFull(l *Language) unsafe.Pointer {
	runtime.SetFinalizer(l.language, nil)
	_p := unsafe.Pointer(l.native)
	l.native = nil // Language is invalid from here on
	return _p
}

// LanguageFromString wraps pango_language_from_string
// 
// The function takes the following parameters:
// 
// 	- language string (nullable): a string representing a language tag 
// 
// The function returns the following values:
// 
// 	- goret *Language (nullable) 
//
// Convert a language tag to a `PangoLanguage`.
// 
// The language tag must be in a RFC-3066 format. `PangoLanguage` pointers
// can be efficiently copied (copy the pointer) and compared with other
// language tags (compare the pointer.)
// 
// This function first canonicalizes the string by converting it to
// lowercase, mapping '_' to '-', and stripping all characters other
// than letters and '-'.
// 
// Use [func@Pango.Language.get_default] if you want to get the
// `PangoLanguage` for the current locale of the process.
func LanguageFromString(language string) *Language {
	var carg1 *C.char          // in, none, string, nullable-string
	var cret  *C.PangoLanguage // return, none, converted, nullable

	if language != "" {
		carg1 = (*C.char)(unsafe.Pointer(C.CString(language)))
		defer C.free(unsafe.Pointer(carg1))
	}

	cret = C.pango_language_from_string(carg1)
	runtime.KeepAlive(language)

	var goret *Language

	if cret != nil {
		goret = UnsafeLanguageFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// LanguageGetDefault wraps pango_language_get_default
// 
// The function returns the following values:
// 
// 	- goret *Language 
//
// Returns the `PangoLanguage` for the current locale of the process.
// 
// On Unix systems, this is the return value is derived from
// `setlocale (LC_CTYPE, NULL)`, and the user can
// affect this through the environment variables LC_ALL, LC_CTYPE or
// LANG (checked in that order). The locale string typically is in
// the form lang_COUNTRY, where lang is an ISO-639 language code, and
// COUNTRY is an ISO-3166 country code. For instance, sv_FI for
// Swedish as written in Finland or pt_BR for Portuguese as written in
// Brazil.
// 
// On Windows, the C library does not use any such environment
// variables, and setting them won't affect the behavior of functions
// like ctime(). The user sets the locale through the Regional Options
// in the Control Panel. The C library (in the setlocale() function)
// does not use country and language codes, but country and language
// names spelled out in English.
// However, this function does check the above environment
// variables, and does return a Unix-style locale string based on
// either said environment variables or the thread's current locale.
// 
// Your application should call `setlocale(LC_ALL, "")` for the user
// settings to take effect. GTK does this in its initialization
// functions automatically (by calling gtk_set_locale()).
// See the setlocale() manpage for more details.
// 
// Note that the default language can change over the life of an application.
// 
// Also note that this function will not do the right thing if you
// use per-thread locales with uselocale(). In that case, you should
// just call pango_language_from_string() yourself.
func LanguageGetDefault() *Language {
	var cret *C.PangoLanguage // return, none, converted

	cret = C.pango_language_get_default()

	var goret *Language

	goret = UnsafeLanguageFromGlibNone(unsafe.Pointer(cret))

	return goret
}

// LanguageGetPreferred wraps pango_language_get_preferred
// 
// The function returns the following values:
// 
// 	- goret []*Language (nullable) 
//
// Returns the list of languages that the user prefers.
// 
// The list is specified by the `PANGO_LANGUAGE` or `LANGUAGE`
// environment variables, in order of preference. Note that this
// list does not necessarily include the language returned by
// [func@Pango.Language.get_default].
// 
// When choosing language-specific resources, such as the sample
// text returned by [method@Pango.Language.get_sample_string],
// you should first try the default language, followed by the
// languages returned by this function.
func LanguageGetPreferred() []*Language {
	var cret **C.PangoLanguage // return, transfer: none, C Pointers: 2, Name: array[Language], scope: , nullable, array (inner: *typesystem.Record, zero-terminated)

	cret = C.pango_language_get_preferred()

	var goret []*Language

	_ = goret
	_ = cret
	panic("unimplemented conversion of []*Language (PangoLanguage**)")

	return goret
}

// GetSampleString wraps pango_language_get_sample_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Get a string that is representative of the characters needed to
// render a particular language.
// 
// The sample text may be a pangram, but is not necessarily. It is chosen
// to be demonstrative of normal text in the language, as well as exposing
// font feature requirements unique to the language. It is suitable for use
// as sample text in a font selection dialog.
// 
// If @language is %NULL, the default language as found by
// [func@Pango.Language.get_default] is used.
// 
// If Pango does not have a sample string for @language, the classic
// "The quick brown fox..." is returned.  This can be detected by
// comparing the returned pointer value to that returned for (non-existent)
// language code "xx".  That is, compare to:
// 
// ```
// pango_language_get_sample_string (pango_language_from_string ("xx"))
// ```
func (language *Language) GetSampleString() string {
	var carg0 *C.PangoLanguage // in, none, converted
	var cret  *C.char          // return, none, string, casted *C.gchar

	carg0 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_language_get_sample_string(carg0)
	runtime.KeepAlive(language)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// GetScripts wraps pango_language_get_scripts
// 
// The function returns the following values:
// 
// 	- numScripts int32: location to return number of scripts 
// 	- goret []Script (nullable) 
//
// Determines the scripts used to to write @language.
// 
// If nothing is known about the language tag @language,
// or if @language is %NULL, then %NULL is returned.
// The list of scripts returned starts with the script that the
// language uses most and continues to the one it uses least.
// 
// The value @num_script points at will be set to the number
// of scripts in the returned array (or zero if %NULL is returned).
// 
// Most languages use only one script for writing, but there are
// some that use two (Latin and Cyrillic for example), and a few
// use three (Japanese for example). Applications should not make
// any assumptions on the maximum number of scripts returned
// though, except that it is positive if the return value is not
// %NULL, and it is a small number.
// 
// The [method@Pango.Language.includes_script] function uses this
// function internally.
// 
// Note: while the return value is declared as `PangoScript`, the
// returned values are from the `GUnicodeScript` enumeration, which
// may have more values. Callers need to handle unknown values.
func (language *Language) GetScripts() (int32, []Script) {
	var carg0 *C.PangoLanguage // in, none, converted
	var carg1 C.int            // out, full, casted, casted C.gint
	var cret  *C.PangoScript   // return, transfer: none, C Pointers: 1, Name: array[Script], scope: , nullable, array (inner: *typesystem.Enum)

	carg0 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_language_get_scripts(carg0, &carg1)
	runtime.KeepAlive(language)

	var numScripts int32
	var goret      []Script

	numScripts = int32(carg1)
	_ = goret
	_ = cret
	panic("unimplemented conversion of []Script (const PangoScript*)")

	return numScripts, goret
}

// IncludesScript wraps pango_language_includes_script
// 
// The function takes the following parameters:
// 
// 	- script Script: a `PangoScript` 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Determines if @script is one of the scripts used to
// write @language.
// 
// The returned value is conservative; if nothing is known about
// the language tag @language, %TRUE will be returned, since, as
// far as Pango knows, @script might be used to write @language.
// 
// This routine is used in Pango's itemization process when
// determining if a supplied language tag is relevant to
// a particular section of text. It probably is not useful
// for applications in most circumstances.
// 
// This function uses [method@Pango.Language.get_scripts] internally.
func (language *Language) IncludesScript(script Script) bool {
	var carg0 *C.PangoLanguage // in, none, converted
	var carg1 C.PangoScript    // in, none, casted
	var cret  C.gboolean       // return

	carg0 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))
	carg1 = C.PangoScript(script)

	cret = C.pango_language_includes_script(carg0, carg1)
	runtime.KeepAlive(language)
	runtime.KeepAlive(script)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Matches wraps pango_language_matches
// 
// The function takes the following parameters:
// 
// 	- rangeList string: a list of language ranges, separated by ';', ':',
//   ',', or space characters.
//   Each element must either be '*', or a RFC 3066 language range
//   canonicalized as by [func@Pango.Language.from_string] 
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Checks if a language tag matches one of the elements in a list of
// language ranges.
// 
// A language tag is considered to match a range in the list if the
// range is '*', the range is exactly the tag, or the range is a prefix
// of the tag, and the character after it in the tag is '-'.
func (language *Language) Matches(rangeList string) bool {
	var carg0 *C.PangoLanguage // in, none, converted
	var carg1 *C.char          // in, none, string, casted *C.gchar
	var cret  C.gboolean       // return

	carg0 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))
	carg1 = (*C.char)(unsafe.Pointer(C.CString(rangeList)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_language_matches(carg0, carg1)
	runtime.KeepAlive(language)
	runtime.KeepAlive(rangeList)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// ToString wraps pango_language_to_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Gets the RFC-3066 format string representing the given language tag.
// 
// Returns (transfer none): a string representing the language tag
func (language *Language) ToString() string {
	var carg0 *C.PangoLanguage // in, none, converted
	var cret  *C.char          // return, none, string, casted *C.gchar

	carg0 = (*C.PangoLanguage)(UnsafeLanguageToGlibNone(language))

	cret = C.pango_language_to_string(carg0)
	runtime.KeepAlive(language)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))

	return goret
}

// LayoutClass wraps PangoLayoutClass
// 
// LayoutClass is the type struct for [Layout]
type LayoutClass struct {
	*layoutClass
}

// layoutClass is the struct that's finalized
type layoutClass struct {
	native *C.PangoLayoutClass
}

// UnsafeLayoutClassFromGlibBorrow is used to convert raw C.PangoLayoutClass pointers to go. This is used by the bindings internally.
func UnsafeLayoutClassFromGlibBorrow(p unsafe.Pointer) *LayoutClass {
	return &LayoutClass{&layoutClass{(*C.PangoLayoutClass)(p)}}
}

// UnsafeLayoutClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [LayoutClass] is expected to work anymore.
func UnsafeLayoutClassFree(l *LayoutClass) {
	C.free(unsafe.Pointer(l.native))
}

// UnsafeLayoutClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeLayoutClassToGlibNone(l *LayoutClass) unsafe.Pointer {
	return unsafe.Pointer(l.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (l *LayoutClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeLayoutClassToGlibNone(l))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *LayoutClass) {}, l)
	return parent
}

// LayoutIter wraps PangoLayoutIter
//
// A `PangoLayoutIter` can be used to iterate over the visual
// extents of a `PangoLayout`.
// 
// To obtain a `PangoLayoutIter`, use [method@Pango.Layout.get_iter].
// 
// The `PangoLayoutIter` structure is opaque, and has no user-visible fields.
type LayoutIter struct {
	*layoutIter
}

// layoutIter is the struct that's finalized
type layoutIter struct {
	native *C.PangoLayoutIter
}

var _ gobject.GoValueInitializer = (*LayoutIter)(nil)

func marshalLayoutIter(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeLayoutIterFromGlibBorrow(b), nil
}

func (r *LayoutIter) GoValueType() gobject.Type {
	return TypeLayoutIter
}

func (r *LayoutIter) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeLayoutIterFromGlibBorrow is used to convert raw C.PangoLayoutIter pointers to go. This is used by the bindings internally.
func UnsafeLayoutIterFromGlibBorrow(p unsafe.Pointer) *LayoutIter {
	return &LayoutIter{&layoutIter{(*C.PangoLayoutIter)(p)}}
}

// UnsafeLayoutIterFromGlibNone is used to convert raw C.PangoLayoutIter pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeLayoutIterFromGlibNone(p unsafe.Pointer) *LayoutIter {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeLayoutIterFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.layoutIter,
		func (intern *layoutIter) {
			C.pango_layout_iter_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeLayoutIterFromGlibFull is used to convert raw C.PangoLayoutIter pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeLayoutIterFromGlibFull(p unsafe.Pointer) *LayoutIter {
	wrapped := UnsafeLayoutIterFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.layoutIter,
		func (intern *layoutIter) {
			C.pango_layout_iter_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeLayoutIterFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [LayoutIter] is expected to work anymore.
func UnsafeLayoutIterFree(l *LayoutIter) {
	C.pango_layout_iter_free(l.native)
}

// UnsafeLayoutIterToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeLayoutIterToGlibNone(l *LayoutIter) unsafe.Pointer {
	return unsafe.Pointer(l.native)
}

// UnsafeLayoutIterToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeLayoutIterToGlibFull(l *LayoutIter) unsafe.Pointer {
	runtime.SetFinalizer(l.layoutIter, nil)
	_p := unsafe.Pointer(l.native)
	l.native = nil // LayoutIter is invalid from here on
	return _p
}

// AtLastLine wraps pango_layout_iter_at_last_line
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Determines whether @iter is on the last line of the layout.
func (iter *LayoutIter) AtLastLine() bool {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_at_last_line(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// Copy wraps pango_layout_iter_copy
// 
// The function returns the following values:
// 
// 	- goret *LayoutIter (nullable) 
//
// Copies a `PangoLayoutIter`.
func (iter *LayoutIter) Copy() *LayoutIter {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  *C.PangoLayoutIter // return, full, converted, nullable

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_copy(carg0)
	runtime.KeepAlive(iter)

	var goret *LayoutIter

	if cret != nil {
		goret = UnsafeLayoutIterFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// GetBaseline wraps pango_layout_iter_get_baseline
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the Y position of the current line's baseline, in layout
// coordinates.
// 
// Layout coordinates have the origin at the top left of the entire layout.
func (iter *LayoutIter) GetBaseline() int32 {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.int              // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_baseline(carg0)
	runtime.KeepAlive(iter)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetCharExtents wraps pango_layout_iter_get_char_extents
// 
// The function returns the following values:
// 
// 	- logicalRect Rectangle: rectangle to fill with
//   logical extents 
//
// Gets the extents of the current character, in layout coordinates.
// 
// Layout coordinates have the origin at the top left of the entire layout.
// 
// Only logical extents can sensibly be obtained for characters;
// ink extents make sense only down to the level of clusters.
func (iter *LayoutIter) GetCharExtents() Rectangle {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, caller-allocates

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	C.pango_layout_iter_get_char_extents(carg0, &carg1)
	runtime.KeepAlive(iter)

	var logicalRect Rectangle

	_ = logicalRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return logicalRect
}

// GetClusterExtents wraps pango_layout_iter_get_cluster_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle to fill with ink extents 
// 	- logicalRect Rectangle: rectangle to fill with logical extents 
//
// Gets the extents of the current cluster, in layout coordinates.
// 
// Layout coordinates have the origin at the top left of the entire layout.
func (iter *LayoutIter) GetClusterExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	C.pango_layout_iter_get_cluster_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(iter)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetIndex wraps pango_layout_iter_get_index
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the current byte index.
// 
// Note that iterating forward by char moves in visual order,
// not logical order, so indexes may not be sequential. Also,
// the index may be equal to the length of the text in the
// layout, if on the %NULL run (see [method@Pango.LayoutIter.get_run]).
func (iter *LayoutIter) GetIndex() int32 {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.int              // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_index(carg0)
	runtime.KeepAlive(iter)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetLayout wraps pango_layout_iter_get_layout
// 
// The function returns the following values:
// 
// 	- goret Layout (nullable) 
//
// Gets the layout associated with a `PangoLayoutIter`.
func (iter *LayoutIter) GetLayout() Layout {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  *C.PangoLayout     // return, none, converted, nullable

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_layout(carg0)
	runtime.KeepAlive(iter)

	var goret Layout

	if cret != nil {
		goret = UnsafeLayoutFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLayoutExtents wraps pango_layout_iter_get_layout_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle to fill with ink extents 
// 	- logicalRect Rectangle: rectangle to fill with logical extents 
//
// Obtains the extents of the `PangoLayout` being iterated over.
func (iter *LayoutIter) GetLayoutExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	C.pango_layout_iter_get_layout_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(iter)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetLine wraps pango_layout_iter_get_line
// 
// The function returns the following values:
// 
// 	- goret *LayoutLine (nullable) 
//
// Gets the current line.
// 
// Use the faster [method@Pango.LayoutIter.get_line_readonly] if
// you do not plan to modify the contents of the line (glyphs,
// glyph widths, etc.).
func (iter *LayoutIter) GetLine() *LayoutLine {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  *C.PangoLayoutLine // return, none, converted, nullable

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_line(carg0)
	runtime.KeepAlive(iter)

	var goret *LayoutLine

	if cret != nil {
		goret = UnsafeLayoutLineFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLineExtents wraps pango_layout_iter_get_line_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle to fill with ink extents 
// 	- logicalRect Rectangle: rectangle to fill with logical extents 
//
// Obtains the extents of the current line.
// 
// Extents are in layout coordinates (origin is the top-left corner
// of the entire `PangoLayout`). Thus the extents returned by this
// function will be the same width/height but not at the same x/y
// as the extents returned from [method@Pango.LayoutLine.get_extents].
func (iter *LayoutIter) GetLineExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	C.pango_layout_iter_get_line_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(iter)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetLineReadonly wraps pango_layout_iter_get_line_readonly
// 
// The function returns the following values:
// 
// 	- goret *LayoutLine (nullable) 
//
// Gets the current line for read-only access.
// 
// This is a faster alternative to [method@Pango.LayoutIter.get_line],
// but the user is not expected to modify the contents of the line
// (glyphs, glyph widths, etc.).
func (iter *LayoutIter) GetLineReadonly() *LayoutLine {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  *C.PangoLayoutLine // return, none, converted, nullable

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_line_readonly(carg0)
	runtime.KeepAlive(iter)

	var goret *LayoutLine

	if cret != nil {
		goret = UnsafeLayoutLineFromGlibNone(unsafe.Pointer(cret))
	}

	return goret
}

// GetLineYrange wraps pango_layout_iter_get_line_yrange
// 
// The function returns the following values:
// 
// 	- y0 int32: start of line 
// 	- y1 int32: end of line 
//
// Divides the vertical space in the `PangoLayout` being iterated over
// between the lines in the layout, and returns the space belonging to
// the current line.
// 
// A line's range includes the line's logical extents. plus half of the
// spacing above and below the line, if [method@Pango.Layout.set_spacing]
// has been called to set layout spacing. The Y positions are in layout
// coordinates (origin at top left of the entire layout).
// 
// Note: Since 1.44, Pango uses line heights for placing lines, and there
// may be gaps between the ranges returned by this function.
func (iter *LayoutIter) GetLineYrange() (int32, int32) {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var carg1 C.int              // out, full, casted, casted C.gint
	var carg2 C.int              // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	C.pango_layout_iter_get_line_yrange(carg0, &carg1, &carg2)
	runtime.KeepAlive(iter)

	var y0 int32
	var y1 int32

	y0 = int32(carg1)
	y1 = int32(carg2)

	return y0, y1
}

// GetRun wraps pango_layout_iter_get_run
// 
// The function returns the following values:
// 
// 	- goret *LayoutRun (nullable) 
//
// Gets the current run.
// 
// When iterating by run, at the end of each line, there's a position
// with a %NULL run, so this function can return %NULL. The %NULL run
// at the end of each line ensures that all lines have at least one run,
// even lines consisting of only a newline.
// 
// Use the faster [method@Pango.LayoutIter.get_run_readonly] if you do not
// plan to modify the contents of the run (glyphs, glyph widths, etc.).
func (iter *LayoutIter) GetRun() *LayoutRun {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  *C.PangoLayoutRun  // return, transfer: none, C Pointers: 1, Name: LayoutRun, scope: , nullable, nullable

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_run(carg0)
	runtime.KeepAlive(iter)

	var goret *LayoutRun

	if cret != nil {
		_ = goret
		_ = cret
		panic("unimplemented conversion of *LayoutRun (PangoLayoutRun*)")
	}

	return goret
}

// GetRunBaseline wraps pango_layout_iter_get_run_baseline
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the Y position of the current run's baseline, in layout
// coordinates.
// 
// Layout coordinates have the origin at the top left of the entire layout.
// 
// The run baseline can be different from the line baseline, for
// example due to superscript or subscript positioning.
func (iter *LayoutIter) GetRunBaseline() int32 {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.int              // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_run_baseline(carg0)
	runtime.KeepAlive(iter)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetRunExtents wraps pango_layout_iter_get_run_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle to fill with ink extents 
// 	- logicalRect Rectangle: rectangle to fill with logical extents 
//
// Gets the extents of the current run in layout coordinates.
// 
// Layout coordinates have the origin at the top left of the entire layout.
func (iter *LayoutIter) GetRunExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	C.pango_layout_iter_get_run_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(iter)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetRunReadonly wraps pango_layout_iter_get_run_readonly
// 
// The function returns the following values:
// 
// 	- goret *LayoutRun (nullable) 
//
// Gets the current run for read-only access.
// 
// When iterating by run, at the end of each line, there's a position
// with a %NULL run, so this function can return %NULL. The %NULL run
// at the end of each line ensures that all lines have at least one run,
// even lines consisting of only a newline.
// 
// This is a faster alternative to [method@Pango.LayoutIter.get_run],
// but the user is not expected to modify the contents of the run (glyphs,
// glyph widths, etc.).
func (iter *LayoutIter) GetRunReadonly() *LayoutRun {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  *C.PangoLayoutRun  // return, transfer: none, C Pointers: 1, Name: LayoutRun, scope: , nullable, nullable

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_get_run_readonly(carg0)
	runtime.KeepAlive(iter)

	var goret *LayoutRun

	if cret != nil {
		_ = goret
		_ = cret
		panic("unimplemented conversion of *LayoutRun (PangoLayoutRun*)")
	}

	return goret
}

// NextChar wraps pango_layout_iter_next_char
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Moves @iter forward to the next character in visual order.
// 
// If @iter was already at the end of the layout, returns %FALSE.
func (iter *LayoutIter) NextChar() bool {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_next_char(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// NextCluster wraps pango_layout_iter_next_cluster
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Moves @iter forward to the next cluster in visual order.
// 
// If @iter was already at the end of the layout, returns %FALSE.
func (iter *LayoutIter) NextCluster() bool {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_next_cluster(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// NextLine wraps pango_layout_iter_next_line
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Moves @iter forward to the start of the next line.
// 
// If @iter is already on the last line, returns %FALSE.
func (iter *LayoutIter) NextLine() bool {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_next_line(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// NextRun wraps pango_layout_iter_next_run
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Moves @iter forward to the next run in visual order.
// 
// If @iter was already at the end of the layout, returns %FALSE.
func (iter *LayoutIter) NextRun() bool {
	var carg0 *C.PangoLayoutIter // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutIter)(UnsafeLayoutIterToGlibNone(iter))

	cret = C.pango_layout_iter_next_run(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// LayoutLine wraps PangoLayoutLine
//
// A `PangoLayoutLine` represents one of the lines resulting from laying
// out a paragraph via `PangoLayout`.
// 
// `PangoLayoutLine` structures are obtained by calling
// [method@Pango.Layout.get_line] and are only valid until the text,
// attributes, or settings of the parent `PangoLayout` are modified.
type LayoutLine struct {
	*layoutLine
}

// layoutLine is the struct that's finalized
type layoutLine struct {
	native *C.PangoLayoutLine
}

var _ gobject.GoValueInitializer = (*LayoutLine)(nil)

func marshalLayoutLine(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeLayoutLineFromGlibBorrow(b), nil
}

func (r *LayoutLine) GoValueType() gobject.Type {
	return TypeLayoutLine
}

func (r *LayoutLine) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeLayoutLineFromGlibBorrow is used to convert raw C.PangoLayoutLine pointers to go. This is used by the bindings internally.
func UnsafeLayoutLineFromGlibBorrow(p unsafe.Pointer) *LayoutLine {
	return &LayoutLine{&layoutLine{(*C.PangoLayoutLine)(p)}}
}

// UnsafeLayoutLineFromGlibNone is used to convert raw C.PangoLayoutLine pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeLayoutLineFromGlibNone(p unsafe.Pointer) *LayoutLine {
	C.pango_layout_line_ref((*C.PangoLayoutLine)(p))
	wrapped := UnsafeLayoutLineFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.layoutLine,
		func (intern *layoutLine) {
			C.pango_layout_line_unref(intern.native)
		},
	)
	return wrapped
}

// UnsafeLayoutLineFromGlibFull is used to convert raw C.PangoLayoutLine pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeLayoutLineFromGlibFull(p unsafe.Pointer) *LayoutLine {
	wrapped := UnsafeLayoutLineFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.layoutLine,
		func (intern *layoutLine) {
			C.pango_layout_line_unref(intern.native)
		},
	)
	return wrapped
}

// UnsafeLayoutLineRef increases the refcount on the underlying resource. This is used by the bindings internally.
// 
// When this is called without an associated call to [LayoutLine.UnsafeLayoutLineUnref], then [LayoutLine] will leak memory.
func UnsafeLayoutLineRef(l *LayoutLine) {
	C.pango_layout_line_ref(l.native)
}

// UnsafeLayoutLineUnref unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [LayoutLine] is expected to work anymore.
func UnsafeLayoutLineUnref(l *LayoutLine) {
	C.pango_layout_line_unref(l.native)
}

// UnsafeLayoutLineToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeLayoutLineToGlibNone(l *LayoutLine) unsafe.Pointer {
	return unsafe.Pointer(l.native)
}

// UnsafeLayoutLineToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeLayoutLineToGlibFull(l *LayoutLine) unsafe.Pointer {
	runtime.SetFinalizer(l.layoutLine, nil)
	_p := unsafe.Pointer(l.native)
	l.native = nil // LayoutLine is invalid from here on
	return _p
}

// GetExtents wraps pango_layout_line_get_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of
//   the glyph string as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical
//   extents of the glyph string 
//
// Computes the logical and ink extents of a layout line.
// 
// See [method@Pango.Font.get_glyph_extents] for details
// about the interpretation of the rectangles.
func (line *LayoutLine) GetExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))

	C.pango_layout_line_get_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(line)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetHeight wraps pango_layout_line_get_height
// 
// The function returns the following values:
// 
// 	- height int32: return location for the line height 
//
// Computes the height of the line, as the maximum of the heights
// of fonts used in this line.
// 
// Note that the actual baseline-to-baseline distance between lines
// of text is influenced by other factors, such as
// [method@Pango.Layout.set_spacing] and
// [method@Pango.Layout.set_line_spacing].
func (line *LayoutLine) GetHeight() int32 {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var carg1 C.int              // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))

	C.pango_layout_line_get_height(carg0, &carg1)
	runtime.KeepAlive(line)

	var height int32

	height = int32(carg1)

	return height
}

// GetLength wraps pango_layout_line_get_length
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Returns the length of the line, in bytes.
func (line *LayoutLine) GetLength() int32 {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var cret  C.int              // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))

	cret = C.pango_layout_line_get_length(carg0)
	runtime.KeepAlive(line)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetPixelExtents wraps pango_layout_line_get_pixel_extents
// 
// The function returns the following values:
// 
// 	- inkRect Rectangle: rectangle used to store the extents of
//   the glyph string as drawn 
// 	- logicalRect Rectangle: rectangle used to store the logical
//   extents of the glyph string 
//
// Computes the logical and ink extents of @layout_line in device units.
// 
// This function just calls [method@Pango.LayoutLine.get_extents] followed by
// two [func@extents_to_pixels] calls, rounding @ink_rect and @logical_rect
// such that the rounded rectangles fully contain the unrounded one (that is,
// passes them as first argument to [func@extents_to_pixels]).
func (layoutLine *LayoutLine) GetPixelExtents() (Rectangle, Rectangle) {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var carg1 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates
	var carg2 C.PangoRectangle   // out, transfer: none, C Pointers: 0, Name: Rectangle, optional, caller-allocates

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(layoutLine))

	C.pango_layout_line_get_pixel_extents(carg0, &carg1, &carg2)
	runtime.KeepAlive(layoutLine)

	var inkRect     Rectangle
	var logicalRect Rectangle

	_ = inkRect
	_ = carg1
	panic("unimplemented conversion of Rectangle (PangoRectangle)")
	_ = logicalRect
	_ = carg2
	panic("unimplemented conversion of Rectangle (PangoRectangle)")

	return inkRect, logicalRect
}

// GetResolvedDirection wraps pango_layout_line_get_resolved_direction
// 
// The function returns the following values:
// 
// 	- goret Direction 
//
// Returns the resolved direction of the line.
func (line *LayoutLine) GetResolvedDirection() Direction {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var cret  C.PangoDirection   // return, none, casted

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))

	cret = C.pango_layout_line_get_resolved_direction(carg0)
	runtime.KeepAlive(line)

	var goret Direction

	goret = Direction(cret)

	return goret
}

// GetStartIndex wraps pango_layout_line_get_start_index
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Returns the start index of the line, as byte index
// into the text of the layout.
func (line *LayoutLine) GetStartIndex() int32 {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var cret  C.int              // return, none, casted, casted C.gint

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))

	cret = C.pango_layout_line_get_start_index(carg0)
	runtime.KeepAlive(line)

	var goret int32

	goret = int32(cret)

	return goret
}

// IndexToX wraps pango_layout_line_index_to_x
// 
// The function takes the following parameters:
// 
// 	- index_ int32: byte offset of a grapheme within the layout 
// 	- trailing bool: an integer indicating the edge of the grapheme to retrieve
//   the position of. If &gt; 0, the trailing edge of the grapheme,
//   if 0, the leading of the grapheme 
// 
// The function returns the following values:
// 
// 	- xPos int32: location to store the x_offset (in Pango units) 
//
// Converts an index within a line to a X position.
func (line *LayoutLine) IndexToX(index_ int32, trailing bool) int32 {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var carg1 C.int              // in, none, casted, casted C.gint
	var carg2 C.gboolean         // in
	var carg3 C.int              // out, full, casted, casted C.gint

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))
	carg1 = C.int(index_)
	if trailing {
		carg2 = C.TRUE
	}

	C.pango_layout_line_index_to_x(carg0, carg1, carg2, &carg3)
	runtime.KeepAlive(line)
	runtime.KeepAlive(index_)
	runtime.KeepAlive(trailing)

	var xPos int32

	xPos = int32(carg3)

	return xPos
}

// IsParagraphStart wraps pango_layout_line_is_paragraph_start
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Returns whether this is the first line of the paragraph.
func (line *LayoutLine) IsParagraphStart() bool {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))

	cret = C.pango_layout_line_is_paragraph_start(carg0)
	runtime.KeepAlive(line)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// XToIndex wraps pango_layout_line_x_to_index
// 
// The function takes the following parameters:
// 
// 	- xPos int32: the X offset (in Pango units) from the left edge of the line. 
// 
// The function returns the following values:
// 
// 	- index_ int32: location to store calculated byte index for the grapheme
//   in which the user clicked 
// 	- trailing int32: location to store an integer indicating where in the
//   grapheme the user clicked. It will either be zero, or the number of
//   characters in the grapheme. 0 represents the leading edge of the grapheme. 
// 	- goret bool 
//
// Converts from x offset to the byte index of the corresponding character
// within the text of the layout.
// 
// If @x_pos is outside the line, @index_ and @trailing will point to the very
// first or very last position in the line. This determination is based on the
// resolved direction of the paragraph; for example, if the resolved direction
// is right-to-left, then an X position to the right of the line (after it)
// results in 0 being stored in @index_ and @trailing. An X position to the
// left of the line results in @index_ pointing to the (logical) last grapheme
// in the line and @trailing being set to the number of characters in that
// grapheme. The reverse is true for a left-to-right line.
func (line *LayoutLine) XToIndex(xPos int32) (int32, int32, bool) {
	var carg0 *C.PangoLayoutLine // in, none, converted
	var carg1 C.int              // in, none, casted, casted C.gint
	var carg2 C.int              // out, full, casted, casted C.gint
	var carg3 C.int              // out, full, casted, casted C.gint
	var cret  C.gboolean         // return

	carg0 = (*C.PangoLayoutLine)(UnsafeLayoutLineToGlibNone(line))
	carg1 = C.int(xPos)

	cret = C.pango_layout_line_x_to_index(carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(line)
	runtime.KeepAlive(xPos)

	var index_   int32
	var trailing int32
	var goret    bool

	index_ = int32(carg2)
	trailing = int32(carg3)
	if cret != 0 {
		goret = true
	}

	return index_, trailing, goret
}

// LogAttr wraps PangoLogAttr
//
// The `PangoLogAttr` structure stores information about the attributes of a
// single character.
type LogAttr struct {
	*logAttr
}

// logAttr is the struct that's finalized
type logAttr struct {
	native *C.PangoLogAttr
}

// UnsafeLogAttrFromGlibBorrow is used to convert raw C.PangoLogAttr pointers to go. This is used by the bindings internally.
func UnsafeLogAttrFromGlibBorrow(p unsafe.Pointer) *LogAttr {
	return &LogAttr{&logAttr{(*C.PangoLogAttr)(p)}}
}

// UnsafeLogAttrFromGlibNone is used to convert raw C.PangoLogAttr pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeLogAttrFromGlibNone(p unsafe.Pointer) *LogAttr {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeLogAttrFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.logAttr,
		func (intern *logAttr) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeLogAttrFromGlibFull is used to convert raw C.PangoLogAttr pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeLogAttrFromGlibFull(p unsafe.Pointer) *LogAttr {
	wrapped := UnsafeLogAttrFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.logAttr,
		func (intern *logAttr) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeLogAttrFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [LogAttr] is expected to work anymore.
func UnsafeLogAttrFree(l *LogAttr) {
	C.free(unsafe.Pointer(l.native))
}

// UnsafeLogAttrToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeLogAttrToGlibNone(l *LogAttr) unsafe.Pointer {
	return unsafe.Pointer(l.native)
}

// UnsafeLogAttrToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeLogAttrToGlibFull(l *LogAttr) unsafe.Pointer {
	runtime.SetFinalizer(l.logAttr, nil)
	_p := unsafe.Pointer(l.native)
	l.native = nil // LogAttr is invalid from here on
	return _p
}

// Matrix wraps PangoMatrix
//
// A `PangoMatrix` specifies a transformation between user-space
// and device coordinates.
// 
// The transformation is given by
// 
// ```
// x_device = x_user * matrix-&gt;xx + y_user * matrix-&gt;xy + matrix-&gt;x0;
// y_device = x_user * matrix-&gt;yx + y_user * matrix-&gt;yy + matrix-&gt;y0;
// ```
type Matrix struct {
	*matrix
}

// matrix is the struct that's finalized
type matrix struct {
	native *C.PangoMatrix
}

var _ gobject.GoValueInitializer = (*Matrix)(nil)

func marshalMatrix(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeMatrixFromGlibBorrow(b), nil
}

func (r *Matrix) GoValueType() gobject.Type {
	return TypeMatrix
}

func (r *Matrix) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeMatrixFromGlibBorrow is used to convert raw C.PangoMatrix pointers to go. This is used by the bindings internally.
func UnsafeMatrixFromGlibBorrow(p unsafe.Pointer) *Matrix {
	return &Matrix{&matrix{(*C.PangoMatrix)(p)}}
}

// UnsafeMatrixFromGlibNone is used to convert raw C.PangoMatrix pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeMatrixFromGlibNone(p unsafe.Pointer) *Matrix {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeMatrixFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.matrix,
		func (intern *matrix) {
			C.pango_matrix_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeMatrixFromGlibFull is used to convert raw C.PangoMatrix pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeMatrixFromGlibFull(p unsafe.Pointer) *Matrix {
	wrapped := UnsafeMatrixFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.matrix,
		func (intern *matrix) {
			C.pango_matrix_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeMatrixFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Matrix] is expected to work anymore.
func UnsafeMatrixFree(m *Matrix) {
	C.pango_matrix_free(m.native)
}

// UnsafeMatrixToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeMatrixToGlibNone(m *Matrix) unsafe.Pointer {
	return unsafe.Pointer(m.native)
}

// UnsafeMatrixToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeMatrixToGlibFull(m *Matrix) unsafe.Pointer {
	runtime.SetFinalizer(m.matrix, nil)
	_p := unsafe.Pointer(m.native)
	m.native = nil // Matrix is invalid from here on
	return _p
}

// Concat wraps pango_matrix_concat
// 
// The function takes the following parameters:
// 
// 	- newMatrix *Matrix: a `PangoMatrix` 
//
// Changes the transformation represented by @matrix to be the
// transformation given by first applying transformation
// given by @new_matrix then applying the original transformation.
func (matrix *Matrix) Concat(newMatrix *Matrix) {
	var carg0 *C.PangoMatrix // in, none, converted
	var carg1 *C.PangoMatrix // in, none, converted

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	carg1 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(newMatrix))

	C.pango_matrix_concat(carg0, carg1)
	runtime.KeepAlive(matrix)
	runtime.KeepAlive(newMatrix)
}

// Copy wraps pango_matrix_copy
// 
// The function returns the following values:
// 
// 	- goret *Matrix (nullable) 
//
// Copies a `PangoMatrix`.
func (matrix *Matrix) Copy() *Matrix {
	var carg0 *C.PangoMatrix // in, none, converted
	var cret  *C.PangoMatrix // return, full, converted, nullable

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))

	cret = C.pango_matrix_copy(carg0)
	runtime.KeepAlive(matrix)

	var goret *Matrix

	if cret != nil {
		goret = UnsafeMatrixFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// GetFontScaleFactor wraps pango_matrix_get_font_scale_factor
// 
// The function returns the following values:
// 
// 	- goret float64 
//
// Returns the scale factor of a matrix on the height of the font.
// 
// That is, the scale factor in the direction perpendicular to the
// vector that the X coordinate is mapped to.  If the scale in the X
// coordinate is needed as well, use [method@Pango.Matrix.get_font_scale_factors].
func (matrix *Matrix) GetFontScaleFactor() float64 {
	var carg0 *C.PangoMatrix // in, none, converted
	var cret  C.double       // return, none, casted, casted C.gdouble

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))

	cret = C.pango_matrix_get_font_scale_factor(carg0)
	runtime.KeepAlive(matrix)

	var goret float64

	goret = float64(cret)

	return goret
}

// GetFontScaleFactors wraps pango_matrix_get_font_scale_factors
// 
// The function returns the following values:
// 
// 	- xscale float64: output scale factor in the x direction 
// 	- yscale float64: output scale factor perpendicular to the x direction 
//
// Calculates the scale factor of a matrix on the width and height of the font.
// 
// That is, @xscale is the scale factor in the direction of the X coordinate,
// and @yscale is the scale factor in the direction perpendicular to the
// vector that the X coordinate is mapped to.
// 
// Note that output numbers will always be non-negative.
func (matrix *Matrix) GetFontScaleFactors() (float64, float64) {
	var carg0 *C.PangoMatrix // in, none, converted
	var carg1 C.double       // out, full, casted, casted C.gdouble
	var carg2 C.double       // out, full, casted, casted C.gdouble

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))

	C.pango_matrix_get_font_scale_factors(carg0, &carg1, &carg2)
	runtime.KeepAlive(matrix)

	var xscale float64
	var yscale float64

	xscale = float64(carg1)
	yscale = float64(carg2)

	return xscale, yscale
}

// GetSlantRatio wraps pango_matrix_get_slant_ratio
// 
// The function returns the following values:
// 
// 	- goret float64 
//
// Gets the slant ratio of a matrix.
// 
// For a simple shear matrix in the form:
// 
//     1 λ
//     0 1
// 
// this is simply λ.
func (matrix *Matrix) GetSlantRatio() float64 {
	var carg0 *C.PangoMatrix // in, none, converted
	var cret  C.double       // return, none, casted, casted C.gdouble

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))

	cret = C.pango_matrix_get_slant_ratio(carg0)
	runtime.KeepAlive(matrix)

	var goret float64

	goret = float64(cret)

	return goret
}

// Rotate wraps pango_matrix_rotate
// 
// The function takes the following parameters:
// 
// 	- degrees float64: degrees to rotate counter-clockwise 
//
// Changes the transformation represented by @matrix to be the
// transformation given by first rotating by @degrees degrees
// counter-clockwise then applying the original transformation.
func (matrix *Matrix) Rotate(degrees float64) {
	var carg0 *C.PangoMatrix // in, none, converted
	var carg1 C.double       // in, none, casted, casted C.gdouble

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	carg1 = C.double(degrees)

	C.pango_matrix_rotate(carg0, carg1)
	runtime.KeepAlive(matrix)
	runtime.KeepAlive(degrees)
}

// Scale wraps pango_matrix_scale
// 
// The function takes the following parameters:
// 
// 	- scaleX float64: amount to scale by in X direction 
// 	- scaleY float64: amount to scale by in Y direction 
//
// Changes the transformation represented by @matrix to be the
// transformation given by first scaling by @sx in the X direction
// and @sy in the Y direction then applying the original
// transformation.
func (matrix *Matrix) Scale(scaleX float64, scaleY float64) {
	var carg0 *C.PangoMatrix // in, none, converted
	var carg1 C.double       // in, none, casted, casted C.gdouble
	var carg2 C.double       // in, none, casted, casted C.gdouble

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	carg1 = C.double(scaleX)
	carg2 = C.double(scaleY)

	C.pango_matrix_scale(carg0, carg1, carg2)
	runtime.KeepAlive(matrix)
	runtime.KeepAlive(scaleX)
	runtime.KeepAlive(scaleY)
}

// Translate wraps pango_matrix_translate
// 
// The function takes the following parameters:
// 
// 	- tx float64: amount to translate in the X direction 
// 	- ty float64: amount to translate in the Y direction 
//
// Changes the transformation represented by @matrix to be the
// transformation given by first translating by (@tx, @ty)
// then applying the original transformation.
func (matrix *Matrix) Translate(tx float64, ty float64) {
	var carg0 *C.PangoMatrix // in, none, converted
	var carg1 C.double       // in, none, casted, casted C.gdouble
	var carg2 C.double       // in, none, casted, casted C.gdouble

	carg0 = (*C.PangoMatrix)(UnsafeMatrixToGlibNone(matrix))
	carg1 = C.double(tx)
	carg2 = C.double(ty)

	C.pango_matrix_translate(carg0, carg1, carg2)
	runtime.KeepAlive(matrix)
	runtime.KeepAlive(tx)
	runtime.KeepAlive(ty)
}

// Rectangle wraps PangoRectangle
//
// The `PangoRectangle` structure represents a rectangle.
// 
// `PangoRectangle` is frequently used to represent the logical or ink
// extents of a single glyph or section of text. (See, for instance,
// [method@Pango.Font.get_glyph_extents].)
type Rectangle struct {
	*rectangle
}

// rectangle is the struct that's finalized
type rectangle struct {
	native *C.PangoRectangle
}

// UnsafeRectangleFromGlibBorrow is used to convert raw C.PangoRectangle pointers to go. This is used by the bindings internally.
func UnsafeRectangleFromGlibBorrow(p unsafe.Pointer) *Rectangle {
	return &Rectangle{&rectangle{(*C.PangoRectangle)(p)}}
}

// UnsafeRectangleFromGlibNone is used to convert raw C.PangoRectangle pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeRectangleFromGlibNone(p unsafe.Pointer) *Rectangle {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeRectangleFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.rectangle,
		func (intern *rectangle) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeRectangleFromGlibFull is used to convert raw C.PangoRectangle pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeRectangleFromGlibFull(p unsafe.Pointer) *Rectangle {
	wrapped := UnsafeRectangleFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.rectangle,
		func (intern *rectangle) {
			C.free(unsafe.Pointer(intern.native))
		},
	)
	return wrapped
}

// UnsafeRectangleFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [Rectangle] is expected to work anymore.
func UnsafeRectangleFree(r *Rectangle) {
	C.free(unsafe.Pointer(r.native))
}

// UnsafeRectangleToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeRectangleToGlibNone(r *Rectangle) unsafe.Pointer {
	return unsafe.Pointer(r.native)
}

// UnsafeRectangleToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeRectangleToGlibFull(r *Rectangle) unsafe.Pointer {
	runtime.SetFinalizer(r.rectangle, nil)
	_p := unsafe.Pointer(r.native)
	r.native = nil // Rectangle is invalid from here on
	return _p
}

// RendererClass wraps PangoRendererClass
//
// Class structure for `PangoRenderer`.
// 
// The following vfuncs take user space coordinates in Pango units
// and have default implementations:
// - draw_glyphs
// - draw_rectangle
// - draw_error_underline
// - draw_shape
// - draw_glyph_item
// 
// The default draw_shape implementation draws nothing.
// 
// The following vfuncs take device space coordinates as doubles
// and must be implemented:
// - draw_trapezoid
// - draw_glyph
// 
// RendererClass is the type struct for [Renderer]
type RendererClass struct {
	*rendererClass
}

// rendererClass is the struct that's finalized
type rendererClass struct {
	native *C.PangoRendererClass
}

// UnsafeRendererClassFromGlibBorrow is used to convert raw C.PangoRendererClass pointers to go. This is used by the bindings internally.
func UnsafeRendererClassFromGlibBorrow(p unsafe.Pointer) *RendererClass {
	return &RendererClass{&rendererClass{(*C.PangoRendererClass)(p)}}
}

// UnsafeRendererClassFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [RendererClass] is expected to work anymore.
func UnsafeRendererClassFree(r *RendererClass) {
	C.free(unsafe.Pointer(r.native))
}

// UnsafeRendererClassToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeRendererClassToGlibNone(r *RendererClass) unsafe.Pointer {
	return unsafe.Pointer(r.native)
}

// ParentClass returns the type struct of the parent class of this type struct.
// This essentially casts the underlying c pointer.
func (r *RendererClass) ParentClass() *gobject.ObjectClass {
	parent := gobject.UnsafeObjectClassFromGlibBorrow(UnsafeRendererClassToGlibNone(r))
	// attach a cleanup to keep the instance alive as long as the parent is referenced
	runtime.AddCleanup(parent, func(_ *RendererClass) {}, r)
	return parent
}

// ScriptIter wraps PangoScriptIter
//
// A `PangoScriptIter` is used to iterate through a string
// and identify ranges in different scripts.
type ScriptIter struct {
	*scriptIter
}

// scriptIter is the struct that's finalized
type scriptIter struct {
	native *C.PangoScriptIter
}

var _ gobject.GoValueInitializer = (*ScriptIter)(nil)

func marshalScriptIter(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeScriptIterFromGlibBorrow(b), nil
}

func (r *ScriptIter) GoValueType() gobject.Type {
	return TypeScriptIter
}

func (r *ScriptIter) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeScriptIterFromGlibBorrow is used to convert raw C.PangoScriptIter pointers to go. This is used by the bindings internally.
func UnsafeScriptIterFromGlibBorrow(p unsafe.Pointer) *ScriptIter {
	return &ScriptIter{&scriptIter{(*C.PangoScriptIter)(p)}}
}

// UnsafeScriptIterFromGlibNone is used to convert raw C.PangoScriptIter pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeScriptIterFromGlibNone(p unsafe.Pointer) *ScriptIter {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeScriptIterFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.scriptIter,
		func (intern *scriptIter) {
			C.pango_script_iter_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeScriptIterFromGlibFull is used to convert raw C.PangoScriptIter pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeScriptIterFromGlibFull(p unsafe.Pointer) *ScriptIter {
	wrapped := UnsafeScriptIterFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.scriptIter,
		func (intern *scriptIter) {
			C.pango_script_iter_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeScriptIterFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [ScriptIter] is expected to work anymore.
func UnsafeScriptIterFree(s *ScriptIter) {
	C.pango_script_iter_free(s.native)
}

// UnsafeScriptIterToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeScriptIterToGlibNone(s *ScriptIter) unsafe.Pointer {
	return unsafe.Pointer(s.native)
}

// UnsafeScriptIterToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeScriptIterToGlibFull(s *ScriptIter) unsafe.Pointer {
	runtime.SetFinalizer(s.scriptIter, nil)
	_p := unsafe.Pointer(s.native)
	s.native = nil // ScriptIter is invalid from here on
	return _p
}

// NewScriptIter wraps pango_script_iter_new
// 
// The function takes the following parameters:
// 
// 	- text string: a UTF-8 string 
// 	- length int32: length of @text, or -1 if @text is nul-terminated 
// 
// The function returns the following values:
// 
// 	- goret *ScriptIter 
//
// Create a new `PangoScriptIter`, used to break a string of
// Unicode text into runs by Unicode script.
// 
// No copy is made of @text, so the caller needs to make
// sure it remains valid until the iterator is freed with
// [method@Pango.ScriptIter.free].
func NewScriptIter(text string, length int32) *ScriptIter {
	var carg1 *C.char            // in, none, string, casted *C.gchar
	var carg2 C.int              // in, none, casted, casted C.gint
	var cret  *C.PangoScriptIter // return, full, converted

	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))
	carg2 = C.int(length)

	cret = C.pango_script_iter_new(carg1, carg2)
	runtime.KeepAlive(text)
	runtime.KeepAlive(length)

	var goret *ScriptIter

	goret = UnsafeScriptIterFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetRange wraps pango_script_iter_get_range
// 
// The function returns the following values:
// 
// 	- start string: location to store start
//   position of the range 
// 	- end string: location to store end position
//   of the range 
// 	- script Script: location to store script for range 
//
// Gets information about the range to which @iter currently points.
// 
// The range is the set of locations p where *start &lt;= p &lt; *end.
// (That is, it doesn't include the character stored at *end)
// 
// Note that while the type of the @script argument is declared
// as `PangoScript`, as of Pango 1.18, this function simply returns
// `GUnicodeScript` values. Callers must be prepared to handle unknown
// values.
func (iter *ScriptIter) GetRange() (string, string, Script) {
	var carg0 *C.PangoScriptIter // in, none, converted
	var carg1 *C.char            // out, none, string, casted *C.gchar
	var carg2 *C.char            // out, none, string, casted *C.gchar
	var carg3 C.PangoScript      // out, full, casted

	carg0 = (*C.PangoScriptIter)(UnsafeScriptIterToGlibNone(iter))

	C.pango_script_iter_get_range(carg0, &carg1, &carg2, &carg3)
	runtime.KeepAlive(iter)

	var start  string
	var end    string
	var script Script

	start = C.GoString((*C.char)(unsafe.Pointer(carg1)))
	end = C.GoString((*C.char)(unsafe.Pointer(carg2)))
	script = Script(carg3)

	return start, end, script
}

// Next wraps pango_script_iter_next
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Advances a `PangoScriptIter` to the next range.
// 
// If @iter is already at the end, it is left unchanged
// and %FALSE is returned.
func (iter *ScriptIter) Next() bool {
	var carg0 *C.PangoScriptIter // in, none, converted
	var cret  C.gboolean         // return

	carg0 = (*C.PangoScriptIter)(UnsafeScriptIterToGlibNone(iter))

	cret = C.pango_script_iter_next(carg0)
	runtime.KeepAlive(iter)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// TabArray wraps PangoTabArray
//
// A `PangoTabArray` contains an array of tab stops.
// 
// `PangoTabArray` can be used to set tab stops in a `PangoLayout`.
// Each tab stop has an alignment, a position, and optionally
// a character to use as decimal point.
type TabArray struct {
	*tabArray
}

// tabArray is the struct that's finalized
type tabArray struct {
	native *C.PangoTabArray
}

var _ gobject.GoValueInitializer = (*TabArray)(nil)

func marshalTabArray(p unsafe.Pointer) (interface{}, error) {
	b := gobject.ValueFromNative(p).Boxed()
	return UnsafeTabArrayFromGlibBorrow(b), nil
}

func (r *TabArray) GoValueType() gobject.Type {
	return TypeTabArray
}

func (r *TabArray) SetGoValue(v *gobject.Value) {
	v.SetBoxed(unsafe.Pointer(r.native))
}

// UnsafeTabArrayFromGlibBorrow is used to convert raw C.PangoTabArray pointers to go. This is used by the bindings internally.
func UnsafeTabArrayFromGlibBorrow(p unsafe.Pointer) *TabArray {
	return &TabArray{&tabArray{(*C.PangoTabArray)(p)}}
}

// UnsafeTabArrayFromGlibNone is used to convert raw C.PangoTabArray pointers to go without transferring ownership. This is used by the bindings internally.
func UnsafeTabArrayFromGlibNone(p unsafe.Pointer) *TabArray {
	// FIXME: this has no ref function, what should we do here?
	wrapped := UnsafeTabArrayFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.tabArray,
		func (intern *tabArray) {
			C.pango_tab_array_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeTabArrayFromGlibFull is used to convert raw C.PangoTabArray pointers to go while taking ownership. This is used by the bindings internally.
func UnsafeTabArrayFromGlibFull(p unsafe.Pointer) *TabArray {
	wrapped := UnsafeTabArrayFromGlibBorrow(p)
	runtime.SetFinalizer(
		wrapped.tabArray,
		func (intern *tabArray) {
			C.pango_tab_array_free(intern.native)
		},
	)
	return wrapped
}

// UnsafeTabArrayFree unrefs/frees the underlying resource. This is used by the bindings internally.
// 
// After this is called, no other method on [TabArray] is expected to work anymore.
func UnsafeTabArrayFree(t *TabArray) {
	C.pango_tab_array_free(t.native)
}

// UnsafeTabArrayToGlibNone returns the underlying C pointer. This is used by the bindings internally.
func UnsafeTabArrayToGlibNone(t *TabArray) unsafe.Pointer {
	return unsafe.Pointer(t.native)
}

// UnsafeTabArrayToGlibFull returns the underlying C pointer and gives up ownership.
// This is used by the bindings internally.
func UnsafeTabArrayToGlibFull(t *TabArray) unsafe.Pointer {
	runtime.SetFinalizer(t.tabArray, nil)
	_p := unsafe.Pointer(t.native)
	t.native = nil // TabArray is invalid from here on
	return _p
}

// NewTabArray wraps pango_tab_array_new
// 
// The function takes the following parameters:
// 
// 	- initialSize int32: Initial number of tab stops to allocate, can be 0 
// 	- positionsInPixels bool: whether positions are in pixel units 
// 
// The function returns the following values:
// 
// 	- goret *TabArray 
//
// Creates an array of @initial_size tab stops.
// 
// Tab stops are specified in pixel units if @positions_in_pixels is %TRUE,
// otherwise in Pango units. All stops are initially at position 0.
func NewTabArray(initialSize int32, positionsInPixels bool) *TabArray {
	var carg1 C.gint           // in, none, casted
	var carg2 C.gboolean       // in
	var cret  *C.PangoTabArray // return, full, converted

	carg1 = C.gint(initialSize)
	if positionsInPixels {
		carg2 = C.TRUE
	}

	cret = C.pango_tab_array_new(carg1, carg2)
	runtime.KeepAlive(initialSize)
	runtime.KeepAlive(positionsInPixels)

	var goret *TabArray

	goret = UnsafeTabArrayFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// TabArrayFromString wraps pango_tab_array_from_string
// 
// The function takes the following parameters:
// 
// 	- text string: a string 
// 
// The function returns the following values:
// 
// 	- goret *TabArray (nullable) 
//
// Deserializes a `PangoTabArray` from a string.
// 
// This is the counterpart to [method@Pango.TabArray.to_string].
// See that functions for details about the format.
func TabArrayFromString(text string) *TabArray {
	var carg1 *C.char          // in, none, string, casted *C.gchar
	var cret  *C.PangoTabArray // return, full, converted, nullable

	carg1 = (*C.char)(unsafe.Pointer(C.CString(text)))
	defer C.free(unsafe.Pointer(carg1))

	cret = C.pango_tab_array_from_string(carg1)
	runtime.KeepAlive(text)

	var goret *TabArray

	if cret != nil {
		goret = UnsafeTabArrayFromGlibFull(unsafe.Pointer(cret))
	}

	return goret
}

// Copy wraps pango_tab_array_copy
// 
// The function returns the following values:
// 
// 	- goret *TabArray 
//
// Copies a `PangoTabArray`.
func (src *TabArray) Copy() *TabArray {
	var carg0 *C.PangoTabArray // in, none, converted
	var cret  *C.PangoTabArray // return, full, converted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(src))

	cret = C.pango_tab_array_copy(carg0)
	runtime.KeepAlive(src)

	var goret *TabArray

	goret = UnsafeTabArrayFromGlibFull(unsafe.Pointer(cret))

	return goret
}

// GetDecimalPoint wraps pango_tab_array_get_decimal_point
// 
// The function takes the following parameters:
// 
// 	- tabIndex int32: the index of a tab stop 
// 
// The function returns the following values:
// 
// 	- goret uint32 
//
// Gets the Unicode character to use as decimal point.
// 
// This is only relevant for tabs with %PANGO_TAB_DECIMAL alignment,
// which align content at the first occurrence of the decimal point
// character.
// 
// The default value of 0 means that Pango will use the
// decimal point according to the current locale.
func (tabArray *TabArray) GetDecimalPoint(tabIndex int32) uint32 {
	var carg0 *C.PangoTabArray // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var cret  C.gunichar       // return, none, casted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))
	carg1 = C.int(tabIndex)

	cret = C.pango_tab_array_get_decimal_point(carg0, carg1)
	runtime.KeepAlive(tabArray)
	runtime.KeepAlive(tabIndex)

	var goret uint32

	goret = uint32(cret)

	return goret
}

// GetPositionsInPixels wraps pango_tab_array_get_positions_in_pixels
// 
// The function returns the following values:
// 
// 	- goret bool 
//
// Returns %TRUE if the tab positions are in pixels,
// %FALSE if they are in Pango units.
func (tabArray *TabArray) GetPositionsInPixels() bool {
	var carg0 *C.PangoTabArray // in, none, converted
	var cret  C.gboolean       // return

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))

	cret = C.pango_tab_array_get_positions_in_pixels(carg0)
	runtime.KeepAlive(tabArray)

	var goret bool

	if cret != 0 {
		goret = true
	}

	return goret
}

// GetSize wraps pango_tab_array_get_size
// 
// The function returns the following values:
// 
// 	- goret int32 
//
// Gets the number of tab stops in @tab_array.
func (tabArray *TabArray) GetSize() int32 {
	var carg0 *C.PangoTabArray // in, none, converted
	var cret  C.gint           // return, none, casted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))

	cret = C.pango_tab_array_get_size(carg0)
	runtime.KeepAlive(tabArray)

	var goret int32

	goret = int32(cret)

	return goret
}

// GetTab wraps pango_tab_array_get_tab
// 
// The function takes the following parameters:
// 
// 	- tabIndex int32: tab stop index 
// 
// The function returns the following values:
// 
// 	- alignment TabAlign: location to store alignment 
// 	- location int32: location to store tab position 
//
// Gets the alignment and position of a tab stop.
func (tabArray *TabArray) GetTab(tabIndex int32) (TabAlign, int32) {
	var carg0 *C.PangoTabArray // in, none, converted
	var carg1 C.gint           // in, none, casted
	var carg2 C.PangoTabAlign  // out, full, casted
	var carg3 C.gint           // out, full, casted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))
	carg1 = C.gint(tabIndex)

	C.pango_tab_array_get_tab(carg0, carg1, &carg2, &carg3)
	runtime.KeepAlive(tabArray)
	runtime.KeepAlive(tabIndex)

	var alignment TabAlign
	var location  int32

	alignment = TabAlign(carg2)
	location = int32(carg3)

	return alignment, location
}

// Resize wraps pango_tab_array_resize
// 
// The function takes the following parameters:
// 
// 	- newSize int32: new size of the array 
//
// Resizes a tab array.
// 
// You must subsequently initialize any tabs
// that were added as a result of growing the array.
func (tabArray *TabArray) Resize(newSize int32) {
	var carg0 *C.PangoTabArray // in, none, converted
	var carg1 C.gint           // in, none, casted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))
	carg1 = C.gint(newSize)

	C.pango_tab_array_resize(carg0, carg1)
	runtime.KeepAlive(tabArray)
	runtime.KeepAlive(newSize)
}

// SetDecimalPoint wraps pango_tab_array_set_decimal_point
// 
// The function takes the following parameters:
// 
// 	- tabIndex int32: the index of a tab stop 
// 	- decimalPoint uint32: the decimal point to use 
//
// Sets the Unicode character to use as decimal point.
// 
// This is only relevant for tabs with %PANGO_TAB_DECIMAL alignment,
// which align content at the first occurrence of the decimal point
// character.
// 
// By default, Pango uses the decimal point according
// to the current locale.
func (tabArray *TabArray) SetDecimalPoint(tabIndex int32, decimalPoint uint32) {
	var carg0 *C.PangoTabArray // in, none, converted
	var carg1 C.int            // in, none, casted, casted C.gint
	var carg2 C.gunichar       // in, none, casted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))
	carg1 = C.int(tabIndex)
	carg2 = C.gunichar(decimalPoint)

	C.pango_tab_array_set_decimal_point(carg0, carg1, carg2)
	runtime.KeepAlive(tabArray)
	runtime.KeepAlive(tabIndex)
	runtime.KeepAlive(decimalPoint)
}

// SetPositionsInPixels wraps pango_tab_array_set_positions_in_pixels
// 
// The function takes the following parameters:
// 
// 	- positionsInPixels bool: whether positions are in pixels 
//
// Sets whether positions in this array are specified in
// pixels.
func (tabArray *TabArray) SetPositionsInPixels(positionsInPixels bool) {
	var carg0 *C.PangoTabArray // in, none, converted
	var carg1 C.gboolean       // in

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))
	if positionsInPixels {
		carg1 = C.TRUE
	}

	C.pango_tab_array_set_positions_in_pixels(carg0, carg1)
	runtime.KeepAlive(tabArray)
	runtime.KeepAlive(positionsInPixels)
}

// SetTab wraps pango_tab_array_set_tab
// 
// The function takes the following parameters:
// 
// 	- tabIndex int32: the index of a tab stop 
// 	- alignment TabAlign: tab alignment 
// 	- location int32: tab location in Pango units 
//
// Sets the alignment and location of a tab stop.
func (tabArray *TabArray) SetTab(tabIndex int32, alignment TabAlign, location int32) {
	var carg0 *C.PangoTabArray // in, none, converted
	var carg1 C.gint           // in, none, casted
	var carg2 C.PangoTabAlign  // in, none, casted
	var carg3 C.gint           // in, none, casted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))
	carg1 = C.gint(tabIndex)
	carg2 = C.PangoTabAlign(alignment)
	carg3 = C.gint(location)

	C.pango_tab_array_set_tab(carg0, carg1, carg2, carg3)
	runtime.KeepAlive(tabArray)
	runtime.KeepAlive(tabIndex)
	runtime.KeepAlive(alignment)
	runtime.KeepAlive(location)
}

// Sort wraps pango_tab_array_sort
//
// Utility function to ensure that the tab stops are in increasing order.
func (tabArray *TabArray) Sort() {
	var carg0 *C.PangoTabArray // in, none, converted

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))

	C.pango_tab_array_sort(carg0)
	runtime.KeepAlive(tabArray)
}

// ToString wraps pango_tab_array_to_string
// 
// The function returns the following values:
// 
// 	- goret string 
//
// Serializes a `PangoTabArray` to a string.
// 
// In the resulting string, serialized tabs are separated by newlines or commas.
// 
// Individual tabs are serialized to a string of the form
// 
//     [ALIGNMENT:]POSITION[:DECIMAL_POINT]
// 
// Where ALIGNMENT is one of _left_, _right_, _center_ or _decimal_, and
// POSITION is the position of the tab, optionally followed by the unit _px_.
// If ALIGNMENT is omitted, it defaults to _left_. If ALIGNMENT is _decimal_,
// the DECIMAL_POINT character may be specified as a Unicode codepoint.
// 
// Note that all tabs in the array must use the same unit.
// 
// A typical example:
// 
//     100px 200px center:300px right:400px
func (tabArray *TabArray) ToString() string {
	var carg0 *C.PangoTabArray // in, none, converted
	var cret  *C.char          // return, full, string, casted *C.gchar

	carg0 = (*C.PangoTabArray)(UnsafeTabArrayToGlibNone(tabArray))

	cret = C.pango_tab_array_to_string(carg0)
	runtime.KeepAlive(tabArray)

	var goret string

	goret = C.GoString((*C.char)(unsafe.Pointer(cret)))
	defer C.free(unsafe.Pointer(cret))

	return goret
}

